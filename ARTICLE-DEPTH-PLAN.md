# 학습 글 뼈대·분량 계획

`CLAUDE.md`의 「글의 뼈대」와 「산문 6,000자」를 **이미 쓴 글에 적용하는 남은 일
목록**이다. 새 글은 처음부터 그 기준으로 쓰므로 여기 오르지 않는다.

이 파일은 사람이 읽으라고 쓴 설명이 아니라 **루틴이 매일 한 항목씩 집어 가는
작업 큐**다. 마친 항목은 그 줄을 지운다 — 무엇을 언제 마쳤는지는 git log가 안다.

마지막 갱신: 2026-09-10 (agents-rag 넷을 채우고 지움 — 374편 남음)
이전 갱신: 2026-09-09 (6,000자 아래로 되돌아간 build-with-ai 둘을 다시 올림 — 390편 남음)
이전 갱신: 2026-09-09 (ai-guide 16편을 한 번에 마치고 루틴에서 뺌)

---

## 왜 이 목록이 있나

2026-09-07에 학습 글 534편의 산문(코드·수식 제외)을 전부 셌다. 아래는 합치기를 마친 뒤의 값이다.

| 카테고리 | 편수 | 산문 중앙값 | 소절(`###`)을 쓰는 글 | 채우기 목록 |
| --- | ---: | ---: | ---: | ---: |
| `deep-learning` | 39 | 1,767자 | 41% | 31 |
| `build-with-ai` | 22 | 1,740자 | 64% | 14 |
| `domain-models` | 55 | 3,408자 | 53% | 45 |
| `agents-rag` | 68 | 2,640자 | 35% | 58 |
| `llm-core` | 51 | 2,688자 | 45% | 41 |
| `ml-ops` | 92 | 3,083자 | 39% | 82 |
| `ai-guide` | 19 | 3,198자 | 32% | 끝 |
| `math-for-ai` | 120 | 3,562자 | 58% | 119 |
| **합계** | **466** | **3,151자** | — | **390** |

**2026-09-08에 합치기 57짝을 마치고 다시 잰 값이다.** 시작할 때는 534편에 중앙값 2,662자였고
6,000자를 넘는 글이 둘이었다. 지금은 466편에 3,151자이고 6,000자를 넘는 글이 57편이다 —
늘어난 57편이 합쳐 다시 쓴 그 글들이다.

리서치 글의 중앙값이 5,562자이니 기준 자체가 무리한 값은 아니다 — 학습 글만 안 쓴 것이다.

**얇은 이유는 주제가 작아서가 아니라 코드 갤러리로 썼기 때문이다.** 여덟 카테고리를
전부 열어 본 결론이 같았다. 절이 「한 문단 + 코드 한 덩이」로 되어 있고, 코드를 빼면
절마다 두세 문장이 남는다. `app-data-analysis`는 절이 여섯인데 산문이 645자다 —
절당 100자다. 그래서 **채우기는 코드를 늘리는 일이 아니다.** 무엇을 왜 그렇게 했는지,
무엇이 틀어지는지, 숫자를 넣어 한 번 따라가 보는 대목을 쓰는 일이다.

## 두 가지 일

**합치기 57짝은 2026-09-08에 전부 끝났다.** 578 → 501편이 됐고, 아래 「합치기는 끝났다」에
그때 밟은 자리를 남겼다.

**채우기 404편 — 미달인 글 전부다.** 2026-09-09에 넓혔다. 그전에는 카테고리마다
열둘씩 96편만 담고 있었는데, 하루 열 편이면 **아흐레 만에 목록이 비고** 그 자리에서
「목록이 비면 신규를 다시 켠다」는 규칙이 걸린다 — **316편이 미달인 채로 신규가
재개될 참이었다.** 그래서 6,000자를 못 넘긴 글을 전부 열어 절·소절 얼개를 짜 넣었다.
이제 목록이 비는 날이 곧 기준을 다 채운 날이다(하루 열 편이면 마흔 날쯤이다).

**목록은 얇은 쪽부터 적혀 있다.** 루틴은 제 칸에서 위에서부터 집으면 된다.
항목 머리는 `- \`슬러그\` (지금 N자 · 절 M)` 꼴이고 둘 다 2026-09-09에 실측한 값이다 —
N은 코드 펜스·수식·이미지를 뺀 산문 글자 수, M은 `##` 개수다.

**합치기는 6,000자를 만들어 주지 않았다 — 채우기가 만들었다.** 57짝의 원본 산문을 더하면
6,000자를 넘는 것이 한 짝뿐이었는데, 합친 뒤 그 자리에서 채워 전부 6,800~15,000자가 됐다.
합치기는 채울 거리가 남은 상태를 만드는 일이고, 채우는 것은 그다음이다.

## 루틴이 고르는 법

**2026-09-07부터 글 루틴 셋은 새 글을 쓰지 않는다.** 하루 열 편을 여기서 집어 간다 —
AI 글 루틴 4편, 수학 루틴 4편, 리서치 루틴 2편이다. 편수를 더 늘리기 전에 있는 글을
기준까지 끌어올리기로 했고, **이 목록이 비면 그날 신규를 다시 켠다.** 각 루틴의
예정 주제 목록과 `MATH-PLAN.md`·`RESEARCH-PLAN.md`는 그대로 두었다.
(시험 노트 루틴은 여기 해당하지 않는다 — 계획 477편을 하루 4편씩 그대로 쓴다.)

1. 자기 카테고리 섹션에서 **위에서부터** 집는다. 합치기가 남아 있으면 합치기가 먼저다.
2. 열어 보고 **채울 거리가 없으면 억지로 채우지 않는다.** 그 줄을 지우는 대신
   「합치기 후보」로 고쳐 적고 이웃 글을 지목한 뒤 다음 항목으로 간다.
3. 마쳤으면 그 줄을 지우고 같은 커밋에 넣는다.

## 합치기 절차

1. **두 글을 다 읽는다.** 겹치는 절을 짝지어 표로 적어 두고 시작한다.
2. **남길 글의 뼈대를 다시 짠다** — 절 4~7, 절마다 소절 2~4. 겹치는 절은 하나로 합치고
   제목은 **그 절이 다루는 것의 이름**으로 단다(`CLAUDE.md`의 「글의 뼈대」).
   흡수하는 글의 고유한 절만 새 절로 들어간다.
3. 흡수하는 글의 `.md`를 `git rm` 한다. `pubDate`는 **남길 글의 것을 그대로 둔다** —
   5월 글이 9월 글을 흡수하는 자리가 있고, 날짜를 옮기면 사슬의 날짜 순서가 뒤집힌다.
4. **사슬을 다시 잇는다.** 흡수한 글의 앞뒤가 서로를 가리키게 하고, 그 글을
   「지난 글」로 부르던 뒤 글의 **도입 첫 문단을 다시 쓴다.** 링크만 갈아 끼우면
   본문 첫 줄이 없는 글을 부른다.
5. **예고 문장을 고친다.** 앞 글의 마지막 문단이 「다음 글에서는 ~」으로 옛 제목을
   부르고 있다. 마무리 블록의 「다음 글」 줄만 고치고 끝내면 안 된다.
6. **저장소 전체에서 슬러그를 지운다.**

   ```bash
   grep -rn "/articles/<흡수한-슬러그>" src/ *.md
   ```

   0이 될 때까지 고친다. 원고 밖에도 있다 — `src/data/certs.ts`의 `studyPath`,
   `src/content/certs/**`의 시험 노트, 다른 카테고리의 수학·리서치 글 본문.
   아래 카테고리별 목록의 들여쓴 줄이 그 자리를 미리 세어 둔 것이다.
7. `npm test && npm run build`.

## 채우기 절차

1. 목록에 적힌 절·소절은 **뼈대 제안**이다. 그대로 베끼지 말고 글을 열어 본 뒤
   그 글에 맞게 고쳐 쓴다. 지금 있는 절을 버리라는 뜻도 아니다.
2. **코드를 늘려서 채우지 않는다.** 늘릴 것은 산문이다.
3. 절 제목은 **그 절이 다루는 것의 이름**이다(`CLAUDE.md`의 「글의 뼈대」).
   지금 붙어 있는 문장형 제목도 그 김에 함께 고친다.

   **아래 목록의 소절 설명을 그대로 제목으로 옮기지 않는다.** 이 목록의 「소절: …」은
   *무엇을 쓸지* 적은 서술문이지 제목이 아니다. 그대로 옮기면 「log₂3이 유리수가
   아니라는 것」이 「log₂3은 유리수가 아니다」라는 문장형 소제목이 된다 —
   2026-09-09에 하루치 소절 제목 30개가 이렇게 샜다. 설명을 읽고 **이름으로 줄여**
   단다(그 예는 「log₂3의 무리수성」).
4. 수학 글은 그림을 서넛 둔다. 채우면서 그림이 그대로면 절이 는 만큼 비는 것이다.
5. 억지로 채우지 않는다 — 위 「고르는 법」 2번.
6. **항목 머리의 「절 M」이 지금 원고의 `##` 개수다.** 정리·연습 절까지 센 값이라
   그대로 믿으면 된다. 얼개의 절 줄은 **내용 절만** 적었으므로, 머리의 M과 얼개의
   절 수를 견줘 여덟을 넘으면 가까운 절 둘을 합친다.
7. **절을 셀 때 원고의 「정리」·「연습 문제」·「코드로 확인하기」 절을 함께 센다.**
   목록의 얼개는 내용 절만 적어 두었으므로, 원고에 정리 절이 있으면 얼개가 일곱일 때
   실제로는 여덟이 된다. 여덟을 넘으면 **가까운 절 둘을 합쳐 일곱 이하로 맞춘다** —
   채우는 김에 하는 일이지 따로 하는 일이 아니다.

   이건 얼개의 흠이 아니라 원래 있던 것이다. 2026-09-09에 학습 466편의 `##`를 세니
   **157편이 이미 여덟 이상**이었고(아홉 40편, 열 이상 33편), 절반이 「정리」로 끝난다.
   절이 열둘인 글은 목차가 화면 한 칸을 넘겨 뼈대가 안 보인다.

## 수학은 합치지 않는다

`math-for-ai` 120편에는 합치기가 없다. 수학은 날짜가 아니라 `curriculum.ts`의
목록이 순서를 정하고 카드 번호도 거기서 나오는데, **글 안에 「24번 · 좌표평면과 직선」
같은 번호 참조가 479곳, 89편에 박혀 있다.** 한 짝만 합쳐도 그 트랙의 뒤 번호가 전부
한 칸씩 당겨져 참조가 통째로 어긋나고 `MATH-PLAN.md`와 `mathSupport`까지 함께 고쳐야
한다. 그리고 **어긋난 번호는 `npm test`가 못 잡는다** — 링크가 아니라 본문의 글자다.
수학은 채우기만 한다.

## 리서치 글은 문제가 반대다

`lab-notes` 21편, `paper-notes` 11편, `tools` 12편은 산문 중앙값이 5,320~5,651자로
학습 글의 두 배다. 여기는 채울 것이 모자란 것이 아니라 **절이 너무 많다** — 세 칸
모두 절 중앙값이 9개다(기준은 4~7). 그리고 `tools`는 소절을 쓰는 글이 33%뿐이라
절 아홉이 한 층에 평평하게 놓여 있다.

그래서 리서치의 보강은 **묶기**다. 가까운 절 둘셋을 한 절 아래 소절로 내리고 절
제목을 **그 절이 다루는 것의 이름**으로 단다. 실험 결과와 터미널 출력은 건드리지
않는다 — 리서치 글의 값은 거기 있고, 「직접 돌려서 확인했다」가 이 섹션의 조건이다.

목록을 손으로 만들어 두지 않는다. 절이 많은 쪽부터 집는다.

```bash
for f in src/content/articles/{lab,paper,bench,cost,spec}-*.md; do
  echo "$(grep -c '^## ' "$f") $(grep -c '^### ' "$f") $f"
done | sort -rn | head -20
```

앞 숫자가 절, 뒤 숫자가 소절이다. **절이 여덟 이상인데 소절이 0인 글**이 먼저다.

## 합치기는 끝났다 — 57짝 전부(2026-09-07~08)

**578편이 501편이 됐다.** 남은 일은 채우기뿐이다. 아래는 그 57짝을 하면서 실제로 밟은
자리들이라, 앞으로 글을 합치거나 옮길 때 그대로 다시 쓴다.

- **합쳐 쓴 뒤에는 사실부터 다시 본다.** 이번에 가장 많이 나온 결함이 「그때는 맞았던 사실」이었다 —
  `huggingface-cli`가 `hf`로 바뀐 것, `HfApi.list_models`의 인자가 없어진 것, Git LFS 자리를
  Xet이 넘겨받은 것, LangChain v1이 `AgentExecutor`를 `langchain-classic`으로 옮긴 것,
  MCP 파이썬 SDK가 데코레이터 등록을 없앤 것, Ollama가 더는 llama.cpp를 엔진으로 쓰지 않는 것,
  GitHub Copilot 요금제와 단축키, PyTorch 공개 연도(2016년 9월), ResNet의 3.57%가 단일 모델이
  아니라 앙상블 기록인 것. **제품 이름·요금·CLI·API 시그니처·버전·연도·논문 수치는 공식 문서로
  확인하고 쓰거나, 확인이 안 되면 단정을 걷어낸다.**
- **지운 슬러그는 저장소 전체에서 지운다.** 원고 밖에 세 종류가 있었다 — `src/data/certs.ts`의
  `studyPath`(같은 묶음에 남을 슬러그가 이미 있으면 갈아 끼우지 말고 줄을 지운다),
  `MATH-PLAN.md`·`RESEARCH-PLAN.md`의 계획 메모, 그리고 시험 노트 본문.
  `grep -rn "/articles/<슬러그>" src/ *.md` 가 0이 될 때까지 본다.
- **사슬은 합치기 전 상태에서 다시 세운다.** 합쳐 쓴 글이 이미 새 이웃을 가리키므로 현재
  파일만 보면 사슬이 끊겨 보인다. `git show HEAD:` 로 원본 차례를 읽어 흡수된 슬러그만 빼고
  다시 잇는 편이 안전하다. 한 카테고리에 사슬이 여럿일 수 있다(agents-rag는 넷이었다).
- **사이에 다른 글이 낀 합치기가 많다.** 「지난 글 / 다음 글」 한 줄 교체로 안 끝나고 낀 글의
  **도입 첫 문단**까지 고쳐야 한다. 그리고 **흡수한 글의 바로 뒤 글이 그 글을 부르는 것이 아닐 수도
  있다** — 연속으로 세 편이 사라진 자리(ml-ops의 `mlops-drift-detection`·`llmops-tracing`·
  `data-versioning`)에서는 살아남은 첫 뒤 글이 셋 중 마지막을 부르고 있었다.
- **흡수된 글이 사슬의 머리면 그다음 글이 머리가 된다.** 그 글의 도입부에서 「지난 글」을 아예
  걷어낸다(`llm-core`의 추론 11편이 그랬다). 링크만 갈아 끼우면 본문 첫 줄이 없는 글을 부른다.
- **닫는 강조 앞의 문장부호를 다시 본다.** 합쳐 쓰면서 `**DPM-Solver++**는`, `**「어디서 멈출까」**다`
  같은 자리가 새로 생겼다. `npm test`의 `emphasis.test.ts`가 잡지만, 원인이 멀어 보이므로 알아 둔다.

---

## 카테고리별 목록

루틴은 자기 카테고리 항목만 읽으면 된다.

```bash
sed -n "/^### deep-learning$/,/^### /p" ARTICLE-DEPTH-PLAN.md
```

### deep-learning

**합치기 — 여덟 짝 모두 끝났다(2026-09-07).** 48편 → 39편.


**채우기**

- `rnn-seq2seq` (지금 932자 · 절 4)
  - 절「길이가 다른 입출력」 — 소절: 정렬형 many-to-many가 못 푸는 자리(입력 5토큰 → 출력 3토큰) / 인코더와 디코더를 잘라 붙인다는 발상 / SOS·EOS 토큰이 생성의 시작과 종료 조건을 맡는 방식
  - 절「컨텍스트 벡터의 자리」 — 소절: 인코더의 마지막 (h, c)만 넘길 때 나머지 타임스텝이 버려진다는 것 / 디코더 초기 상태로 넣는 방식과 매 스텝 입력에 이어 붙이는 방식의 차이 / 입력 문장을 뒤집어 넣었더니 성능이 올랐던 Sutskever의 관찰과 그 해석
  - 절「Teacher Forcing」 — 소절: `tgt[:, :-1]`과 `tgt[:, 1:]`로 입력과 정답을 한 칸 어긋나게 자르는 이유 / 학습이 빨라지는 대신 생기는 노출 편향(훈련에서는 정답만 봤다) / Scheduled Sampling으로 정답과 자기 예측을 확률적으로 섞는 절차
  - 절「자기회귀 디코딩」 — 소절: (h, c)를 물려주며 한 토큰씩 뽑는 루프와 argmax의 자리 / max_len과 EOS 중 무엇이 먼저 걸리는가에 따라 달라지는 출력 / 같은 구절을 되풀이하는 반복 생성이 나타나는 지점
  - 절「Beam Search」 — 소절: beam_size=4에서 스텝마다 4×V 후보를 4개로 추리는 절차를 두 스텝 손으로 / 로그 확률의 합이 짧은 문장을 편애하는 문제와 길이 정규화 / EOS에 도달한 가설을 따로 모아 두고 남은 빔만 이어 가는 처리
  - 절「고정 벡터의 병목」 — 소절: 100토큰의 정보를 256차원 하나에 넣는다는 것의 뜻 / 문장이 길어질수록 번역 품질이 떨어진 실험 결과 / 인코더 은닉 상태를 전부 남겨 두고 골라 보는 Attention으로 넘어가는 갈림길

- `nn-dropout` (지금 1,225자 · 절 7)
  - 절「무작위로 끈다는 아이디어」 — 소절: 학습과 추론의 비대칭 / 공적응(co-adaptation)이라는 말 / p가 뜻하는 것(끄는 확률)
  - 절「Inverted Dropout」 — 소절: 기대값을 맞추는 1/(1-p) 스케일 / 나이브 방식과 수식으로 비교 / model.eval()을 빼먹었을 때 벌어지는 일
  - 절「앙상블 해석」 — 소절: 2ⁿ 서브네트워크 / 추론이 기하평균 근사라는 논거 / 배깅과 다른 점(가중치를 공유한다)
  - 절「변형들」 — 소절: DropConnect·SpatialDropout2d·DropPath / 각 변형이 왜 필요했는가(공간 상관·잔차 분기) / 트랜스포머의 attention dropout
  - 절「MC Dropout」 — 소절: 추론 때 켜서 분산을 재는 절차 / 베이지안 근사라는 주장의 범위 / 실제로 쓸 때의 주의
  - 절「p 고르기와 함께 쓰는 것」 — 소절: 자리별 권장값의 근거 / BN과 같이 쓸 때 생기는 분산 불일치 / 요즘 큰 모델이 p를 낮추는 이유

- `ml-ranking-metrics` (지금 1,251자 · 절 5)
  - 절「순서를 재는 지표의 필요」 — 소절: 관련 문서가 1~5위인 모델과 6~10위인 모델이 Precision@10에서 같은 값을 받는 예 / 이진 관련도와 등급 관련도(0~3)를 나누는 기준과 각각의 수집 비용 / 순위가 낮을수록 클릭이 줄어드는 위치 편향을 지표가 어떻게 흉내 내는가
  - 절「DCG의 두 조각」 — 소절: 이득 2^rel−1이 등급 3과 2를 7 대 3으로 벌리는 계산 / 할인 log₂(i+1)을 1위부터 1·1.58·2·2.32로 펼쳐 보기 / 선형 이득(rel)과 지수 이득(2^rel−1)이 갈리는 지점과 라이브러리마다 기본값이 다른 문제
  - 절「NDCG로 정규화」 — 소절: rel=[3,2,3,0,1]로 DCG·IDCG·NDCG를 끝까지 손계산 / 관련 문서가 없어 IDCG=0인 쿼리를 0으로 둘지 제외할지 / 쿼리마다 관련 문서 수가 달라도 평균 낼 수 있게 되는 이유
  - 절「MAP와 평균 정밀도」 — 소절: [T,F,T,F,T]에서 P@1·P@3·P@5를 세어 AP를 만드는 과정 / 분모 R을 검색 결과가 아니라 전체 관련 문서 수로 두는 이유 / 등급 관련도를 못 쓰고 이진으로 뭉개야 하는 제약
  - 절「MRR와 첫 정답의 순위」 — 소절: 1·2·3위가 1.0·0.5·0.33으로 급히 떨어지는 성질 / 두 번째 이후 정답을 통째로 버리는 것이 맞는 자리(QA·단답 검색) / 정답이 없는 쿼리를 0으로 세면 평균이 어떻게 눌리는가
  - 절「k와 지표 고르기」 — 소절: 같은 두 모델이 @3·@5·@10에서 순위가 뒤집히는 예를 만들어 보기 / 관련도를 사람이 라벨링했는지 클릭 로그에서 뽑았는지가 지표 선택을 제한한다 / 오프라인 NDCG와 실서비스 CTR이 갈릴 때 먼저 의심할 것

- `rnn-basics` (지금 1,252자 · 절 5)
  - 절「순서가 있는 데이터」 — 소절: MLP에 시퀀스를 넣을 때 잃는 것 / 가변 길이 문제 / 파라미터 공유가 답인 이유
  - 절「셀 하나의 계산」 — 소절: h_t 식과 shape 추적 / 은닉 상태를 '기억'이라 부를 때의 한계 / 초기 상태 h_0을 정하는 법
  - 절「입출력 형태 네 가지」 — 소절: one-to-many·many-to-one·정렬/비정렬 many-to-many / 각 형태의 실제 과제 / 손실을 어느 타임스텝에서 재는가
  - 절「배치와 패딩」 — 소절: 길이가 다른 문장 묶기 / pack_padded_sequence / 패딩이 손실로 새는 것 막기
  - 절「시간 전개와 BPTT」 — 소절: 전개 그래프를 그림으로 / 기울기 곱 전개식 한 스텝 손계산 / 특잇값 1을 경계로 갈리는 두 방향
  - 절「Truncated BPTT와 클리핑」 — 소절: 청크로 끊고 detach하기 / 청크 길이가 배울 수 있는 범위를 정한다 / 클리핑 임계값 고르기
  - 절「RNN을 지금 쓰는 자리」 — 소절: O(1) 추론 메모리 / 스트리밍·온디바이스 / SSM(Mamba)으로 이어지는 줄기

- `nn-forward-backward` (지금 1,263자 · 절 7)
  - 절「학습이 푸는 문제」 — 소절: 손실 최소화와 경사하강 / 수치 미분이 못 쓰이는 이유(파라미터당 순전파 2회) / 역전파 비용이 순전파와 같은 자릿수인 근거
  - 절「순전파가 남기는 것」 — 소절: z와 a를 나눠 두는 이유 / 역전파에 필요한 중간값 목록 / 활성값 메모리가 배치 크기에 비례한다
  - 절「연쇄 법칙을 층으로」 — 소절: 2층 MLP의 ∂L/∂W₁을 끝까지 전개 / shape로 검산하기 / 지역 기울기와 상류 기울기의 분리
  - 절「손으로 한 스텝」 — 소절: 작은 숫자로 순전파→손실→역전파→갱신 완주 / 같은 값을 PyTorch로 재현 / 수치 기울기와 대조(gradcheck)
  - 절「Autograd가 하는 일」 — 소절: 계산 그래프 구성과 backward 순회 / zero_grad를 빼먹었을 때의 누적 / detach·no_grad·retain_graph의 구분
  - 절「기울기가 이상할 때」 — 소절: NaN·0·폭발 세 갈래 / 층별 기울기 노름 찍어 보기 / 흔한 원인 셋(스케일·학습률·손실 선택)

- `ml-clustering-kmeans` (지금 1,297자 · 절 7)
  - 절「군집화가 푸는 문제」 — 소절: 정답 없이 구조를 찾는다는 것 / 분할·계층·밀도 세 갈래 / K-평균이 세우는 가정
  - 절「알고리즘 네 걸음」 — 소절: 할당과 갱신을 좌표로 따라가기 / 목적 함수가 단조 감소하는 이유 / 수렴해도 전역 최적이 아닌 이유
  - 절「초기화: K-means++」 — 소절: 무작위 초기화가 실패하는 예 / D² 확률 표집 절차를 손으로 / n_init이 실제로 하는 일
  - 절「K 고르기」 — 소절: WCSS가 항상 줄어드는 문제와 엘보우의 한계 / 실루엣으로 고르기 / 갭 통계량, 그리고 도메인이 K를 정하는 경우
  - 절「전처리가 결과를 바꾼다」 — 소절: 스케일링 없이 돌리면 벌어지는 일 / 범주형·혼합형 데이터(K-modes·K-prototypes) / 고차원에서 거리가 무너지는 지점
  - 절「한계와 대안」 — 소절: 구형 가정·이상치 민감·K 지정 / DBSCAN과 GMM으로 가는 갈림길 / Mini-Batch K-Means로 규모 키우기

- `ml-knn` (지금 1,317자 · 절 7)
  - 절「게으른 학습기」 — 소절: `fit`이 저장만 하고 `predict`가 전부 하는 비대칭 / 학습 O(1)·예측 O(n·d)를 붓꽃 120행과 100만 행에 각각 대입해 보기 / 파라미터가 없는 대신 훈련 데이터 전체가 모델이 되어 배포 용량이 데이터 크기가 된다는 것
  - 절「가까움의 정의」 — 소절: 유클리드·맨해튼·체비쇼프가 민코프스키 p=2·1·∞로 이어지는 관계 / 텍스트·임베딩에 코사인, 범주형에 해밍을 쓰는 이유 / 스케일링을 빼먹으면 단위가 큰 특성 하나가 거리를 독점하는 과정을 두 특성 예로
  - 절「K를 고르는 절차」 — 소절: K=1의 훈련 오차 0과 K=n의 상수 예측 사이에서 무엇이 움직이는가 / 5-겹 교차 검증으로 K=1~30을 훑고 곡선의 평평한 구간을 고르는 법 / 이진 분류에서 짝수 K가 만드는 동점과 그 처리
  - 절「거리 가중과 결정 경계」 — 소절: `weights='uniform'`과 `'distance'`가 경계 근처 한 점에서 갈리는 계산 / K가 커질수록 경계가 매끄러워지다 뭉개지는 과정 / 클래스 불균형에서 다수 클래스가 이웃 K개를 채워 버리는 문제
  - 절「차원의 저주」 — 소절: d=2·10·100·1000에서 최대/최소 거리 비가 1로 수렴하는 실험 / '가장 가까운 이웃'이라는 말이 뜻을 잃는 지점 / PCA·특성 선택으로 줄이고 같은 지표를 다시 재는 순서
  - 절「탐색 자료구조」 — 소절: 완전 탐색·KD 트리·Ball 트리를 차원 수로 고르는 기준(KD는 대략 d<20) / `leaf_size`가 트리 탐색과 완전 탐색을 섞는 지점 / 수백만 행부터 근사 최근접(FAISS·HNSW)으로 넘어가는 경계와 그때 포기하는 것
  - 절「KNN 회귀와 남는 한계」 — 소절: 이웃 K개의 평균이라 훈련 데이터 범위 밖을 절대 못 넘는다는 것 / 거리 가중 평균으로 계단 모양 예측을 완만하게 만들기 / 벡터 DB의 유사 문서 검색이 결국 같은 계산이라는 연결

- `nn-weight-init` (지금 1,342자 · 절 7)
  - 절「초기값이 정하는 것」 — 소절: Var(z)=n·σ²_w·σ²_x 유도 / 층을 지나며 분산이 커지거나 줄어드는 모습 / 활성값 히스토그램으로 보기
  - 절「0과 상수 초기화」 — 소절: 대칭성이 깨지지 않는 과정을 한 스텝씩 / 편향은 0이어도 되는 이유 / 같은 값으로 시작한 뉴런이 받는 같은 기울기
  - 절「Xavier/Glorot」 — 소절: 순전파·역전파 분산을 함께 맞추는 조건 / 균등형과 정규형의 관계(√6 상수의 출처) / tanh·sigmoid에 맞는 이유
  - 절「He/Kaiming」 — 소절: ReLU가 분산을 반으로 줄이는 계산 / 2배 상수가 어디서 오는가 / fan_in과 fan_out 중 무엇을 쓰는가
  - 절「직접 재 보기」 — 소절: 20층에서 네 초기화의 층별 활성값 분산 / 수렴 속도 비교 / 어느 지점부터 차이가 사라지는가
  - 절「정규화 레이어가 있을 때」 — 소절: BN·LN이 초기화 의존을 줄이는 방식 / 그래도 초기화가 남는 자리(잔차 분기·임베딩·출력층) / 트랜스포머의 N(0,0.02) 관례

- `nn-vanishing-gradient` (지금 1,344자 · 절 7)
  - 절「연쇄 곱이 만드는 것」 — 소절: N층 기울기 전개식 / sigmoid 0.25ᴺ를 층수별로 계산 / 특잇값 기준으로 소실과 폭발이 갈리는 지점
  - 절「소실을 눈으로 확인하기」 — 소절: 층별 기울기 노름을 찍는 코드와 그래프 / 학습이 멈춘 층 찾기 / '손실이 안 떨어진다'와 구별하는 법
  - 절「폭발과 클리핑」 — 소절: NaN이 나오기까지의 경로 / norm clipping과 value clipping의 차이 / 임계값을 정하는 절차
  - 절「해결 1: 활성화와 초기화」 — 소절: ReLU가 통과시키는 기울기 / He 초기화와의 결합 / 그래도 남는 문제
  - 절「해결 2: 잔차 연결」 — 소절: x+f(x)의 기울기가 1을 갖는 계산 / ResNet이 100층을 학습한 근거 / Pre-LN 잔차 블록
  - 절「해결 3: 게이트와 정규화」 — 소절: LSTM 셀 경로의 ∂C_t/∂C_{t-1}=f_t / BN·LN이 기여하는 부분 / 현대 아키텍처의 조합표

- `ml-roc-auc` (지금 1,377자 · 절 7)
  - 절「임계값에 묶인 지표들」 — 소절: 0.5가 특별하지 않다는 것 / 임계값을 바꿀 때 네 칸이 움직이는 표 / 순위만 보는 지표라는 발상
  - 절「ROC 곡선 그리기」 — 소절: 점수를 정렬해 한 점씩 찍는 절차를 손으로 / 곡선 위 한 점이 뜻하는 것 / 완벽·랜덤·역전 세 모양
  - 절「AUC의 확률 해석」 — 소절: '양성이 음성보다 높은 점수를 받을 확률' 유도 / Mann-Whitney U와 같은 값인 이유 / AUC가 말해 주지 않는 것
  - 절「불균형과 PR 곡선」 — 소절: FPR 분모의 TN이 만드는 낙관 / PR 기준선이 양성 비율인 이유 / 언제 PR-AUC로 갈아타는가
  - 절「임계값을 실제로 고르기」 — 소절: Youden J·F1 최대·비용 행렬 / 검증에서 고르고 테스트에서 확인 / 배포 후 재조정
  - 절「보정(calibration)」 — 소절: 순위가 좋아도 확률은 틀릴 수 있다 / 신뢰도 곡선과 Brier 점수 / Platt 스케일링과 isotonic
  - 절「다중 클래스로 넓히기」 — 소절: OvR과 OvO / macro·weighted 평균의 차이 / 클래스별로 나눠 봐야 하는 이유

- `nn-mlp` (지금 1,441자 · 절 6)
  - 절「MLP의 구조」 — 소절: 완전 연결 층의 반복 / XOR을 2층으로 푸는 과정을 좌표로 / 은닉 표현이 바꿔 놓는 것
  - 절「보편 근사 정리」 — 소절: 정리의 정확한 진술과 조건 / 존재 증명일 뿐이라는 것(찾을 수 있는가는 별개) / 폭으로 가는 길과 깊이로 가는 길
  - 절「깊이 대 너비」 — 소절: 같은 파라미터 예산에서의 비교 실험 / 계층적 특성이라는 관찰 / 깊어질 때 따라오는 비용
  - 절「MNIST 분류기 완성」 — 소절: 데이터·모델·학습 루프 / 학습 곡선 읽기 / 틀린 예측을 직접 들여다보기
  - 절「하이퍼파라미터 고르기」 — 소절: 폭·깊이의 출발점 / 학습률과 배치 크기 / 정규화를 언제 넣는가
  - 절「MLP가 못 하는 것」 — 소절: 28×28을 펴면 잃는 공간 구조 / 파라미터가 입력 크기에 비례한다 / CNN·Transformer로 가는 이유

- `ml-tsne-umap` (지금 1,451자 · 절 9)
  - 절「선형 투영의 한계」 — 소절: PCA가 전역 분산이 큰 방향만 본다는 제약 / digits 64차원을 PCA 2D로 줄였을 때 실루엣이 0.1~0.2에 머무는 이유 / 스위스 롤·동심원처럼 휘어 있는 구조는 어느 평면에 눌러도 겹친다
  - 절「t-SNE의 두 분포」 — 소절: 고차원 이웃 확률을 가우시안, 저차원을 자유도 1의 t분포로 두는 비대칭 설계 / 꼬리가 두꺼워야 풀리는 밀집 문제(crowding) / KL 발산이 비대칭이라 '가까운 것을 멀리 놓는 쪽'에 더 큰 벌이 붙는다는 것
  - 절「perplexity가 정하는 것」 — 소절: 점마다 σᵢ를 이분 탐색으로 맞춰 유효 이웃 수를 고정하는 절차 / 5·15·30·50·100에서 군집 모양이 갈리는 관찰과 실루엣 변화 / 표본 수 대비 어느 범위를 볼지 정하는 경험칙과 그 한계
  - 절「UMAP의 이웃 그래프」 — 소절: k-최근접 그래프를 만들고 가중치를 대칭화하는 두 단계 / `n_neighbors`가 지역·전역 균형을, `min_dist`가 군집 내 뭉침을 따로 정한다 / t-SNE에 없는 `transform()`이 가능한 구조적 이유
  - 절「PCA를 앞에 두는 순서」 — 소절: 50차원으로 먼저 줄여 잡음과 계산 시간을 함께 줄이는 표준 절차 / `random_state`를 고정해도 남는 비결정성과 재현 보고의 최소 조건 / 768차원 문장 임베딩을 2D로 내려 라벨 색으로 검증하는 흐름
  - 절「해석에서 새는 자리」 — 소절: 군집 사이 거리를 읽으면 안 되는 이유를 알고리즘 목적 함수에서 / 군집 크기가 실제 밀도를 반영하지 않는다는 것 / 하이퍼파라미터를 세 번 바꿔도 남는 패턴만 결론으로 쓰는 검증 규칙

- `ml-supervised-vs-unsupervised` (지금 1,465자 · 절 5)
  - 절「레이블의 유무」 — 소절: 같은 고객 데이터가 분류·회귀·군집화로 갈리는 갈림길 하나 / 레이블 획득 비용이 알고리즘 선택을 앞서는 자리(의료 영상·법률 문서) / 네 패러다임을 이 질문 하나의 변주로 늘어놓기
  - 절「지도 학습의 두 갈래」 — 소절: 이산 클래스와 연속 값이 손실 함수를 각각 무엇으로 정하는가 / `fit → predict → score`라는 같은 인터페이스가 알고리즘 교체를 싸게 만드는 것 / 레이블 잡음이 성능 상한을 미리 정해 버린다는 것
  - 절「비지도 학습의 네 과제」 — 소절: 군집화·차원 축소·이상 탐지·밀도 추정을 '무엇을 얻으려 하는가'로 가르기 / 정답이 없으니 평가도 내부 지표로 한다는 연결 / 스케일링이 지도 학습보다 결과를 더 크게 바꾸는 이유(거리가 곧 목적)
  - 절「반지도 학습의 가정」 — 소절: 군집 가정과 매니폴드 가정이 깨지면 성능이 오히려 내려간다는 것 / LabelPropagation으로 30%만 레이블을 남기고 전파를 돌려 정확도 확인 / 의사 레이블이 자기 오류를 되먹임해 굳어지는 고리
  - 절「자기지도 학습의 과제 설계」 — 소절: 다음 토큰 예측·15% 마스킹·두 증강 대조를 '레이블을 데이터에서 만든다'로 묶기 / NT-Xent의 온도가 유사도 분포의 날카로움을 정하는 자리 / 사전 과제가 하류 성능과 어긋나 무의미한 표현이 나오는 경우
  - 절「패러다임 고르기」 — 소절: 레이블 수·데이터 규모·라벨링 비용 셋으로 표를 채우는 절차 / 사전학습 → 지도 파인튜닝 → 선호 정렬로 이어 붙이는 현재의 표준 흐름 / RLHF를 반지도로 부를 때 실제로는 지도인 부분(보상 모델 학습)을 구별하기

- `ml-svm` (지금 1,494자 · 절 8)
  - 절「최대 마진 초평면」 — 소절: 두 클래스를 가르는 직선이 무수히 많다는 데서 출발하는 문제 설정 / 마진 폭 2/‖w‖를 유도하고 ‖w‖ 최소화 문제로 바꾸는 과정 / yᵢ(wᵀxᵢ+b) ≥ 1이라는 제약이 '마진 안에 아무도 없다'를 뜻하는 이유
  - 절「서포트 벡터가 하는 일」 — 소절: 마진 위·안의 점만 남고 나머지를 지워도 경계가 그대로인 성질 / `n_support_`와 `support_vectors_`로 실제 개수와 위치를 확인하기 / 서포트 벡터가 훈련 데이터의 절반을 넘을 때 읽어야 할 신호
  - 절「소프트 마진과 C」 — 소절: 여유 변수 ξ를 넣어 오분류를 값으로 사는 구조 / 같은 식을 힌지 손실 + L2 정규화로 다시 쓰면 보이는 것 / C를 0.01→100으로 올리며 CV 정확도와 서포트 벡터 수를 함께 표로 보기
  - 절「커널 트릭」 — 소절: φ(x)를 계산하지 않고 내적 K(x, x′)만 갈아 끼우는 것이 왜 성립하는가 / 동심원 데이터에서 linear 0.54와 rbf 0.99로 갈리는 자리 / RBF·다항식·선형을 데이터 모양과 특성 수로 고르는 기준
  - 절「C와 gamma의 상호작용」 — 소절: gamma가 가우시안 폭을 정해 경계의 굴곡을 바꾸는 과정 / `gamma='scale'`이 1/(특성 수 × 분산)인 이유와 좋은 출발점인 근거 / 격자 탐색 결과를 표로 놓았을 때 과적합이 한쪽 구석에 몰리는 모습
  - 절「규모·확률·회귀」 — 소절: 학습이 O(n²~n³)이라 수만 샘플에서 LinearSVC·SGDClassifier로 갈아타는 경계 / `probability=True`가 내부 교차 검증으로 확률을 만드느라 느려지는 구조 / SVR의 ε-튜브가 '틀려도 봐주는 폭'을 정하는 방식과 C·ε의 역할 분담

- `ml-clustering-metrics` (지금 1,563자 · 절 7)
  - 절「응집도와 분리도」 — 소절: 같은 군집 안은 가깝고 다른 군집과는 멀다는 두 조건을 수치로 옮기는 방향 / 내부 지표와 외부 지표가 갈리는 지점(정답 레이블의 유무) / 군집화가 쓴 거리와 지표가 쓰는 거리가 다르면 생기는 어긋남
  - 절「실루엣 계수」 — 소절: a(x)와 b(x)를 정의대로 한 점에서 손계산 / −1·0·+1 세 값이 각각 어떤 배정 상태인가 / `silhouette_samples`로 군집별 평균을 내 잘못 묶인 군집 하나를 집어내는 절차
  - 절「DB 지수와 최악의 쌍」 — 소절: 군집마다 '가장 닮은 이웃 하나'만 보고 평균 내는 구조 / (σᵢ+σⱼ)/d(cᵢ,cⱼ)를 세 군집 예로 계산 / 낮을수록 좋다는 방향이 다른 둘과 반대라 표에서 헷갈리는 자리
  - 절「CH 지수와 분산비」 — 소절: 군집 간 분산과 군집 내 분산의 비를 자유도 (k−1)·(n−k)로 나눠 보정하는 식 / O(n)이라 K를 2~30까지 훑기 좋은 이유 / 볼록 군집을 전제해 DBSCAN 결과에 그대로 쓰면 안 되는 근거
  - 절「ARI와 NMI」 — 소절: 랜드 지수를 무작위 기준선으로 보정해 '랜덤=0'을 만드는 이유 / 동질성·완전성·V-measure가 각각 묻는 서로 다른 질문 / 군집 번호가 뒤바뀌어도 값이 같은 성질(순열 불변)
  - 절「K 선택과 결과 검증」 — 소절: 세 내부 지표를 K=2~10에서 한 표에 놓고 봉우리가 어긋날 때의 판단 / DBSCAN의 잡음 −1을 빼고 다시 재야 하는 이유 / 지표가 좋아도 쓸모없는 군집이 나오는 경우와 도메인 검증 한 단계

- `neural-network-basics` (지금 1,573자 · 절 8)
  - 절「층의 세 종류」 — 소절: 입력층이 계산하지 않는 이유 / 은닉층의 폭이 곧 용량 / 출력층 노드 수는 문제가 정한다
  - 절「층 하나의 계산」 — 소절: (B,in)×(in,out) shape 추적 / 편향 브로드캐스트 / 배치 차원이 하는 일
  - 절「파라미터 세기」 — 소절: 손으로 세는 규칙 / 784-256-128-10 MLP를 실제로 계산 / 파라미터 수와 메모리·FLOPs의 관계
  - 절「순전파를 손으로」 — 소절: 2-3-1 네트워크를 숫자로 끝까지 / 중간 활성값이 저장되는 자리 / PyTorch 출력과 대조
  - 절「층을 쌓는다는 것」 — 소절: 선형 합성이 다시 선형이라는 한 줄 증명 / 깊이가 늘리는 표현력 / 활성화가 그 자리에 있는 이유
  - 절「Linear 말고 다른 층들」 — 소절: Conv2d·Embedding·Attention이 각각 무엇을 아끼는가 / 가중치 공유라는 공통 발상 / 이 사슬에서 어디로 이어지는가

- `embedding-basics` (지금 1,608자 · 절 6)
  - 절「토큰 ID로는 안 되는 이유」 — 소절: 정수의 순서가 뜻을 갖지 않는다 / 원-핫의 두 문제(희소성·직교성) / 임베딩을 차원 축소로 보기
  - 절「룩업 테이블」 — 소절: 행렬 곱이 아니라 인덱싱인 이유 / (V,d) 행렬의 shape와 초기화 / 역전파가 그 행만 갱신하는 과정
  - 절「분포 가설」 — 소절: '맥락이 같으면 뜻이 같다'의 출처와 뜻 / king-man+woman이 성립하는 조건과 한계 / 학습되지 않은 임베딩은 아무것도 아니다
  - 절「거리와 유사도」 — 소절: 코사인·내적·유클리드의 차이 / 정규화가 바꾸는 것 / 이방성(anisotropy)과 허브 문제
  - 절「차원 d 고르기」 — 소절: 어휘 크기와 d의 관계 / 임베딩이 파라미터의 5~15%인 계산 / weight tying이 절약하는 것과 잃는 것
  - 절「임베딩의 갈래」 — 소절: 토큰·정적 단어·문맥·문장·다중모달 / 이 사슬에서 다룰 순서 / 검색용 임베딩은 왜 다른 칸(agents-rag)으로 가는가

- `ml-regression-metrics` (지금 1,721자 · 절 8)
  - 절「오차를 모으는 방식」 — 소절: 잔차 e=y−ŷ 하나를 절댓값·제곱·비율로 집계하면 서로 다른 질문이 된다 / 각 지표가 최적으로 만드는 예측이 다르다는 것(MAE는 중앙값, MSE는 평균) / 단위가 남는 지표와 사라지는 지표를 갈라 두기
  - 절「MAE와 RMSE의 간격」 — 소절: RMSE ≥ MAE가 항상 성립하는 이유 / 캘리포니아 주택 데이터에서 두 값과 그 비를 실제로 찍어 보기 / 비가 1에서 멀어질수록 큰 오차가 몇 건인지 되짚어 찾아가는 절차
  - 절「손실 함수로서의 MSE」 — 소절: 제곱이 미분을 쉽게 만들어 대부분의 회귀가 이것을 최소화한다는 배경 / 이상치 한 점이 기울기를 끌고 가는 과정을 다섯 점 예로 / MAE를 손실로 쓰려면 0에서의 미분 불가를 어떻게 우회하는가
  - 절「MAPE의 비대칭」 — 소절: 실제 값이 작을수록 같은 절대 오차가 크게 세어지는 구조 / y≈0에서 발산하고 음수 타깃에서 뜻을 잃는다 / 과대예측과 과소예측에 다른 벌을 주는 성질과 sMAPE가 고치는 부분
  - 절「R²가 재는 것」 — 소절: 평균으로만 예측하는 모델을 기준선 0으로 두는 정의 / 음수가 나오는 경우와 그때 무엇이 잘못됐는지 / 특성을 늘리면 저절로 오르는 성질과 Adjusted R²의 (n−1)/(n−p−1) 보정
  - 절「잔차 진단」 — 소절: 잔차 평균이 0인지, 예측값이 커질수록 분산이 커지는지(이분산) 보기 / 3σ 밖 잔차를 세어 이상치 비율로 환산하기 / Huber 회귀로 갈아탄 뒤 MAE가 실제로 내려가는지 대조하고 epsilon을 조정

- `nn-activation-functions` (지금 1,757자 · 절 7)
  - 절「좋은 활성화의 조건」 — 소절: 비선형성·미분 가능성·계산 비용·기울기 보존 / '포화(saturation)'라는 말의 뜻 / 제로 중심이 아닐 때 생기는 지그재그
  - 절「포화 계열: Sigmoid·Tanh」 — 소절: 미분 최대 0.25가 낳는 결과 / 두 함수의 대응 관계 σ(2z) / 지금도 남아 있는 자리(게이트·출력층)
  - 절「ReLU 계열」 — 소절: 희소 활성의 이점과 비용 / Dying ReLU를 작은 실험으로 재현 / Leaky·PReLU·ELU가 각각 고치는 지점
  - 절「부드러운 계열: GELU·SiLU」 — 소절: z·Φ(z)라는 확률적 해석 / tanh 근사식과 정확식의 차이 / 트랜스포머가 GELU를 고른 경위와 SwiGLU
  - 절「출력층의 활성화」 — 소절: 문제 유형이 정한다(sigmoid·softmax·항등) / 손실 함수와 짝을 이루는 이유(BCEWithLogits·CrossEntropy) / 다중 레이블은 왜 softmax가 아닌가
  - 절「고르는 순서」 — 소절: 은닉층·출력층·게이트 세 자리 / 정규화 레이어와의 궁합 / 바꿔 볼 때 무엇을 보고 판단하는가

- `nn-perceptron` (지금 1,767자 · 절 7)
  - 절「생물 뉴런과 수학 모델」 — 소절: 수상돌기·축삭과 가중합·임계값의 대응 / McCulloch-Pitts 뉴런(1943)과의 차이 / 계단 함수가 하는 일
  - 절「퍼셉트론 학습 규칙」 — 소절: AND 게이트를 세 스텝 손으로 갱신 / 학습률이 수렴 속도에 미치는 영향 / 오분류일 때만 갱신하는 이유
  - 절「수렴 정리」 — 소절: 마진 γ와 반지름 R로 본 업데이트 횟수 상한 (R/γ)² / 선형 분리 불가능할 때의 무한 진동 / Pocket 알고리즘
  - 절「XOR과 한계」 — 소절: 네 점을 직선으로 못 가르는 이유를 좌표로 / Minsky·Papert 1969와 첫 겨울 / 2층이면 풀리는 이유 미리보기
  - 절「퍼셉트론 vs 로지스틱 회귀」 — 소절: 계단 함수와 시그모이드의 차이가 낳는 것 / 퍼셉트론 손실·힌지 손실·로지스틱 손실 비교 / 확률 해석이 생기는 지점
  - 절「퍼셉트론이 남긴 것」 — 소절: 미분 가능한 활성화의 필요 / 오늘의 SGD와 같은 뼈대 / 뉴런 하나에서 층으로 넘어가는 다리

- `ml-naive-bayes` (지금 1,902자 · 절 7)
  - 절「베이즈 정리의 네 항」 — 소절: 사후·가능도·사전·증거를 스팸 메일 한 통으로 채워 보기 / 분모 P(x)가 모든 클래스에 같아 argmax에서 사라지는 계산 / 사전 확률이 클래스 불균형을 그대로 예측에 실어 나른다는 것
  - 절「조건부 독립 가정」 — 소절: '무료'와 '당첨'이 함께 오는데도 곱해 버리는 지점 / 결합 확률 추정이 O(2^d)에서 O(d)로 내려가며 얻는 것 / 확률값은 틀려도 클래스 순위는 자주 맞는 이유
  - 절「언더플로와 로그 합」 — 소절: 단어 수백 개의 확률을 곱하면 언더플로가 나는 계산 / log P로 바꿔 더하는 구현과 `predict_log_proba` / `predict_proba`가 0·1로 몰려 신뢰도로 쓰기 어려운 성질
  - 절「세 변형과 특성 분포」 — 소절: GaussianNB가 클래스별 평균·분산(`theta_`·`var_`)만 저장한다는 것 / MultinomialNB의 빈도와 BernoulliNB의 출현 여부가 짧은 문서에서 갈리는 지점 / 불균형 텍스트에서 ComplementNB로 바꿔 보는 자리
  - 절「라플라스 스무딩」 — 소절: 한 번도 못 본 단어가 확률 0을 만들어 곱 전체를 0으로 만드는 경로 / (count+α)/(total+α·V)에서 α가 커질수록 균등 분포로 끌려가는 과정 / α를 0.01~10으로 훑어 교차 검증으로 고르기
  - 절「나이브 베이즈를 고르는 자리」 — 소절: TF-IDF + MultinomialNB로 20뉴스그룹 베이스라인을 20초 안에 잡는 절차 / `partial_fit`으로 스트리밍 학습이 되는 이유(누적 카운트만 갱신) / 특성 간 상관이 강한 정형 데이터에서 무너지는 지점

- `ml-pca` (지금 2,598자 · 절 12)
  - 절「차원이 늘 때 생기는 것」 — 소절: 거리 기반 알고리즘이 고차원에서 무력해지는 지점 / 키와 몸무게처럼 상관 높은 특성이 같은 정보를 되풀이한다는 관찰 / 압축·시각화·잡음 제거라는 세 목적이 서로 다른 `n_components`를 부른다
  - 절「분산이 최대인 방향」 — 소절: 투영 후 분산이 최대인 축이 정보 손실이 가장 적은 축인 이유 / PC2를 PC1과 직교하도록 잡는 조건 / 원래 특성이 아니라 조합된 축이라 해석이 사라진다는 대가
  - 절「공분산 행렬과 고유값」 — 소절: 중심화 → C = XᵀX/n → 고유값 분해 세 단계를 shape로 따라가기 / 고유값이 곧 그 방향의 분산량이라는 대응 / sklearn이 고유값 분해 대신 SVD를 쓰는 수치적 이유
  - 절「설명 분산비로 차원 정하기」 — 소절: λ_k/Σλ를 누적해 그리는 Scree Plot 읽는 법 / digits 64차원이 95% 기준에서 몇 차원으로 줄어드는지 직접 찍어 확인 / 95%·99%·팔꿈치 셋이 다른 답을 줄 때의 판단 순서
  - 절「스케일링과 화이트닝」 — 소절: 단위가 다른 특성을 그대로 넣으면 큰 단위가 PC1을 독점하는 과정 / `whiten=True`가 각 주성분 분산을 1로 맞추는 계산과 `X.var(axis=0)`으로 확인하기 / 화이트닝이 이득인 자리(SVM·신경망 입력)와 꼬리 성분의 잡음을 키우는 자리
  - 절「복원과 압축률」 — 소절: `inverse_transform`으로 되돌린 뒤 MSE를 재는 절차 / 784차원을 50차원으로 줄일 때의 압축률과 분류 정확도 손실을 함께 표로 / 잡음까지 버려 오히려 성능이 올라가는 경우와 그 조건
  - 절「PCA로 안 되는 자리」 — 소절: 선형 투영이라 스위스 롤을 못 편다는 것과 커널 PCA가 치르는 O(n³) 비용 / 레이블을 안 보므로 분류에 유리한 방향을 놓친다 — LDA가 클래스 수−1까지만 뽑는 제약과 함께 / 데이터가 메모리에 안 들어갈 때 `IncrementalPCA.partial_fit`으로 배치 처리

- `ml-clustering-hierarchical` (지금 2,599자 · 절 9)
  - 절「합치는 방향과 나누는 방향」 — 소절: 점 n개에서 시작해 하나로 합칠 때까지 정확히 n−1번 병합한다는 것 / 분리형이 첫 분할에서 봐야 하는 경우의 수와 그 비용 / 응집형만 실무에 남은 이유
  - 절「덴드로그램 읽기」 — 소절: 잎·가지·높이·루트가 각각 무엇을 가리키는가 / 낮은 높이의 병합과 높은 높이의 병합이 뜻하는 차이 / `truncate_mode='level'`로 잘라 그려도 절단 판단이 살아 있는 조건
  - 절「절단선과 군집 수」 — 소절: 가장 긴 수직 구간의 중간을 자르는 규칙과 그 근거 / `fcluster`의 `maxclust`와 `distance`가 서로 다른 답을 주는 예 / 비일관성 계수로 '유독 튀는 병합'을 찾아 절단 후보로 삼기
  - 절「네 연결 기준」 — 소절: single의 연쇄 효과를 이상치 한 점을 넣어 재현 / complete가 균일한 크기의 군집을 선호하는 이유 / Ward의 ΔW = |A||B|/(|A|+|B|)·d(μA,μB)² 계산과 유클리드 전용이라는 제약
  - 절「전처리와 거리 선택」 — 소절: 스케일이 다른 특성이 거리를 독점하는 자리와 StandardScaler·RobustScaler를 가르는 기준 / Ward를 못 쓸 때 average + 맨해튼으로 가는 조합 / 코사인 거리로 문서를 묶을 때 덴드로그램의 높이가 뜻하는 것이 달라진다
  - 절「규모의 벽과 품질 확인」 — 소절: n×n 거리 행렬이 n=10,000에서 몇 GB인지 직접 계산 / 코페네틱(cophenetic) 상관계수로 덴드로그램이 원 거리를 얼마나 보존했는지 재기 — 원고의 「공표적 상관계수」는 cophenetic의 오역이므로 본문·코드 주석·print 문구를 함께 바로잡는다 / '결정론적'이라는 말이 어디까지 참인가(연결 기준은 결정론, 동점 병합은 구현 의존)

- `embedding-glove` (지금 2,612자 · 절 9)
  - 절「공기 행렬과 조건부 확률」 — 소절: 창 안의 동시 등장을 세어 X_ij를 만드는 절차와 거리 가중 1/d / P(k|i) = X_ik/X_i로 정규화해 단어별 빈도 차이를 걷어내기 / 어휘 20만이면 X가 400억 칸이라 희소 표현이 전제가 되는 계산
  - 절「공기 확률의 비율」 — 소절: k=cold·hot·water일 때 P(k|ice)/P(k|steam)이 크게·작게·1 근처로 갈리는 표 / 확률 하나가 아니라 비율을 보면 두 단어의 공통 문맥이 상쇄된다는 것 / 이 비율을 벡터 차이로 옮기려는 요구가 목적 함수를 낳은 경위
  - 절「목적 함수의 네 조각」 — 소절: wᵢᵀw̃ⱼ + bᵢ + b̃ⱼ ≈ log X_ij라는 근사 대상이 뜻하는 것 / 편향 항 두 개가 단어별 총빈도를 흡수하는 자리 / X_ij=0인 칸을 합에서 아예 빼는 이유(log가 정의되지 않는다)
  - 절「가중치 함수 f」 — 소절: x_max=100, α=0.75에서 f(x)가 그리는 곡선을 몇 점 찍어 보기 / 희소 공기 쌍의 잡음과 the·of 같은 초고빈도 쌍을 한 함수로 동시에 누르는 방식 / 가중치를 빼고 학습하면 무엇부터 무너지는가
  - 절「유추와 선형 구조」 — 원고의 같은 이름 절을 살린다(얼개에서 빠져 있었다). 소절: P(k|ice)/P(k|steam) 비율이 벡터 차이 w_ice − w_steam으로 인코딩되어야 한다는 요구가 목적 함수를 거쳐 선형 구조로 돌아오는 경로 / 비율의 로그가 내적으로 근사되므로 의미 관계가 방향뿐 아니라 크기에도 실린다는 것 / the·a 같은 고빈도 기능어의 벡터 크기가 작아지는 관찰과 그 해석 — 유추 계산 절차 자체는 `embedding-word2vec`이 맡으므로 여기서는 GloVe 목적 함수에서 유도되는 부분만 다룬다
  - 절「Word2Vec과 갈리는 지점」 — 소절: 쌍을 샘플링하는 온라인 학습과 행렬을 미리 세는 배치 학습의 비용 구조 / 코퍼스를 키울 때 각각 늘어나는 것(메모리 대 스텝 수) / 학습이 끝난 뒤 두 벡터 집합 w와 w̃를 더해서 쓰는 관례와 그 근거
  - 절「사전 학습 벡터 활용」 — 소절: glove.6B가 60억 토큰 학습본이라는 것 — 파일 이름의 6B를 6억으로 읽지 않기(지금 원고가 그렇게 적혀 있다) / 어휘 인덱스로 임베딩 행렬을 만들고 `trainable`을 켤지 끌지 정하는 기준 / 다의어·OOV·행렬 메모리 세 한계와 FastText로 넘어가는 자리

- `ml-dbscan` (지금 2,613자 · 절 10)
  - 절「밀도 기반 군집의 정의」 — 소절: K-평균의 결정 경계가 보로노이 선분이라 달·링 모양에서 깨지는 이유 / '빽빽한 곳이 군집'이라는 정의가 모양 가정을 없애는 지점 / 군집 수를 안 정하는 대신 밀도 기준 둘을 정해야 한다는 교환
  - 절「ε과 MinPts」 — 소절: ε 반경 안 이웃을 세는 계산과 자기 자신을 포함하는 sklearn의 규약 / MinPts ≥ 차원+1이라는 출발 규칙과 잡음이 많을 때 올리는 이유 / ε을 키우면 MinPts도 함께 올려야 하는 관계
  - 절「핵심·경계·잡음」 — 소절: `core_sample_indices_`로 세 유형을 갈라 개수를 세기 / 경계 포인트가 방문 순서에 따라 다른 군집에 붙을 수 있다는 것 — 원고의 「결정론적: 초기화에 무관하게 동일한 결과」는 K-평균의 무작위 초기화와 견준 말이라 참이고, 흔들리는 것은 초기화가 아니라 데이터 순서이므로 그 둘을 갈라 적는다 / 레이블 −1이 '군집 없음'이지 '잡음이라는 이름의 군집'이 아니라는 것
  - 절「밀도 도달과 밀도 연결」 — 소절: 핵심 포인트에서 재귀적으로 확장하는 여섯 걸음을 점 몇 개로 따라가기 / 도달 가능(비대칭)과 밀도 연결(대칭)의 차이 / 얇게 이어진 다리 하나가 두 군집을 붙여 버리는 경우
  - 절「k-거리 그래프로 ε 잡기」 — 소절: 각 점의 k번째 이웃 거리를 정렬해 내림차순으로 그리는 절차 / 팔꿈치 지점을 읽고 ε 후보를 두세 개 남기기 / ε을 0.1씩 옮기며 군집 수와 잡음 비율이 어디서 뒤집히는지 표로 확인
  - 절「균일 밀도 가정과 HDBSCAN」 — 소절: std 0.3과 1.5인 두 군집에 하나의 ε을 쓸 때 벌어지는 일 / `min_cluster_size` 하나로 바꿔 놓은 HDBSCAN의 인터페이스와 상호 도달 거리 / `probabilities_`로 소속 확신도까지 함께 얻는 것
  - 절「이상치 탐지와 규모」 — 소절: −1을 그대로 이상치로 쓰는 파이프라인과 그때 스케일링이 필수인 이유(ε은 절대 거리다) / 고차원에서 ε의 뜻이 무너져 PCA·UMAP을 앞에 세우는 순서 / 인덱스가 먹힐 때 O(n log n), 안 먹힐 때 O(n²)로 갈리는 지점

- `embedding-contextual` (지금 2,751자 · 절 7)
  - 절「하나의 벡터가 못 담는 것」 — 소절: '은행'의 두 뜻이 한 벡터에서 평균으로 뭉개지는 과정 / e(w)와 e(w, C)를 나란히 놓고 무엇이 입력에 더 들어왔는지 / 다의어가 실제로 성능을 깎는 하류 과제(검색·NER·감성 분류)
  - 절「ELMo의 층별 표현」 — 소절: 문자 CNN → 양방향 LSTM 2층 → 층 가중 합이라는 세 단계 / 방향마다 LSTM을 따로 두고 토큰 임베딩과 softmax 층만 공유한 뒤 두 표현을 이어 붙인다는 것(어텐션처럼 동시에 보지 않는다) / 하위 층은 구문, 상위 층은 의미라는 관찰과 태스크별 가중치가 학습되는 구조
  - 절「BERT의 사전 학습 과제」 — 소절: 마스킹 15%를 다시 80/10/10(=[MASK]/무작위/그대로)으로 나누는 이유 / 셀프 어텐션이 양쪽을 동시에 보므로 마스킹이 필요해진 순서 / NSP가 맡은 일과 이후 후속 모델에서 빠진 경위
  - 절「입력 임베딩 세 겹」 — 소절: 토큰·위치·세그먼트 임베딩을 더해서 넣는 구조 / 위치 임베딩이 512개라 긴 문서를 자르거나 나눠야 하는 제약 / WordPiece가 '학습하다'를 `##` 조각으로 나눴을 때 단어 벡터를 되모으는 방법
  - 절「표현을 꺼낼 층」 — 소절: `output_hidden_states=True`로 13개 표현을 꺼내 비교하는 절차 / 마지막 층·마지막 4개 평균·토큰 평균이 과제마다 갈리는 지점 / 미세조정 없이 [CLS]를 문장 벡터로 쓰면 잘 안 되는 이유
  - 절「추출해서 쓸 때의 함정」 — 소절: 같은 '은행'을 두 문장에서 뽑아 코사인이 실제로 벌어지는지 확인하는 검증 절차 / 문맥 벡터끼리 코사인이 전반적으로 높게 나오는 성질과 그때 필요한 후처리 / 배치·패딩·`truncation`이 같은 문장의 벡터를 바꾸는 자리

- `embedding-word2vec` (지금 2,929자 · 절 8)
  - 절「카운트 기반과 예측 기반」 — 소절: V×V 행렬에 SVD를 돌리는 비용이 막히는 지점 / '주변 단어로 중심 단어를 맞춘다'는 과제로 바꿔 얻은 것 / 임베딩이 목표가 아니라 은닉층 가중치라는 부산물이라는 구조
  - 절「CBOW와 Skip-gram」 — 소절: 문맥 벡터를 평균 내는 쪽과 쌍을 여러 개 만드는 쪽의 스텝 수 차이 / 희소 단어가 Skip-gram에서 더 많은 학습 신호를 받는 계산 / 창 크기가 작을 때 구문적, 클 때 주제적 유사도로 기우는 관찰
  - 절「소프트맥스의 분모 비용」 — 소절: 어휘 100만이면 한 스텝마다 100만 번 내적이 필요한 계산 / 계층적 소프트맥스가 허프만 경로를 따라 O(log V)로 줄이는 방식 / 빈번한 단어를 루트 가까이 두어 평균 경로를 줄이는 이유
  - 절「네거티브 샘플링」 — 소절: 다중 분류를 (정답 1개 + 노이즈 k개) 이진 분류로 바꿔 쓰는 목적 함수 / 노이즈 분포를 빈도의 3/4 거듭제곱으로 두면 무엇이 달라지는가 / 데이터가 작을 때 k=5~20, 클 때 2~5로 낮추는 기준
  - 절「빈번 단어 서브샘플링」 — 소절: '의'·'the'가 창마다 들어와 학습 스텝을 잡아먹는 문제 / t=1e-5 기준으로 확률적으로 버리는 규칙과 그 식 / 버리고 나면 실효 창이 넓어져 먼 관계까지 잡히는 부수 효과
  - 절「유추와 벡터 공간의 기하」 — 소절: 왕−남자+여자를 코사인 최댓값으로 푸는 절차와 입력 세 단어를 후보에서 빼는 관례 / 유추가 성립하려면 두 관계의 변위가 평행해야 한다는 조건과 깨지는 예 / 정적 벡터라 '배'의 세 뜻이 한 점으로 뭉치는 한계와 OOV로 이어지는 다음 글

- `ml-decision-tree` (지금 2,990자 · 절 9)
  - 절「트리의 구조와 분기」 — 소절: 루트·내부·리프가 하는 일과 예측값이 리프에 저장되는 형태 / 붓꽃에서 '꽃잎 길이 ≤ 2.45' 하나로 Setosa가 떨어져 나오는 분기 / 분기 하나가 특성 축에 수직인 선 하나라는 기하적 대응
  - 절「불순도 두 가지」 — 소절: 이진 분류에서 p를 0→1로 옮기며 엔트로피와 지니를 나란히 그려 보기 / 최댓값이 1.0과 0.5로 다른데도 고르는 분기가 거의 같은 이유 / log 연산 비용 차이와 실무에서 기본값을 지니로 두는 근거
  - 절「정보 이득으로 분기 고르기」 — 소절: H(부모) − 가중 평균 H(자식)을 [5A,5B] → [4A,1B]·[1A,4B]로 손계산 / 연속형 특성에서 정렬 후 인접 값의 중점을 후보로 삼는 절차와 후보 수 / 이득이 가장 큰 분기만 보는 탐욕 선택이 전역 최적을 놓치는 예
  - 절「CART가 정한 규칙」 — 소절: 항상 이진 분기라 같은 특성을 여러 번 쓰게 되는 모습 / 회귀에서 불순도가 MSE로 바뀌고 리프가 평균값을 갖는 대응 / 범주형을 그대로 다루는 구현과 원-핫이 필요한 sklearn의 차이
  - 절「가지치기 두 방식」 — 소절: `max_depth`·`min_samples_leaf`가 성장을 어디서 멈추는가 / 비용-복잡도 R_α(T) = R(T) + α·|리프|에서 α가 커질 때 트리가 줄어드는 경로 / `cost_complexity_pruning_path`로 α 후보를 뽑아 교차 검증으로 고르기
  - 절「특성 중요도와 분산」 — 소절: MDI가 카디널리티 높은 특성을 부풀리는 구조와 중요도 0이 뜻하는 것 / `permutation_importance`로 테스트 세트에서 다시 재는 절차 / 훈련 데이터를 조금 바꿨을 때 트리 구조가 통째로 달라지는 실험과 앙상블로 가는 이유

- `embedding-fasttext` (지금 3,014자 · 절 11)
  - 절「OOV가 생기는 자리」 — 소절: 신조어·전문 용어·활용형 셋으로 나눠 각각 어디서 들어오는지 / 교착어에서 어간 하나가 수십~수백 형태로 늘어나는 계산 / Word2Vec이 `KeyError`를 던지는 지점과 흔히 쓰던 미봉책(UNK 벡터, 형태소 분석 선행)
  - 절「문자 n-gram 분해」 — 소절: `<`와 `>`로 경계를 표시해 '한'과 '<한'을 구별하는 이유 / '학습한다'를 n=3~6으로 실제로 펼쳐 몇 조각이 나오는지 세기 / 단어 벡터를 n-gram 합으로 정의하되 전체 단어 토큰도 함께 넣는 것
  - 절「OOV 벡터를 만드는 경로」 — 소절: 못 본 단어를 조각내 알려진 조각만 더하는 세 걸음 / 조각이 하나도 안 맞을 때 나오는 벡터의 품질과 그때의 대비 / 학습된 벡터와 합성된 벡터를 같은 공간에서 비교해도 되는 근거
  - 절「해시 버킷과 메모리」 — 소절: 모든 n-gram을 따로 저장하면 테이블이 터지는 계산 / bucket 200만 칸에 해시로 밀어 넣을 때 생기는 충돌과 그 영향 / `min_n`·`max_n`을 넓히면 조각 수와 학습 시간이 함께 늘어나는 관계
  - 절「한국어에서의 이득과 설정」 — 소절: '공부하다·공부시켰다·공부벌레'가 공유하는 n-gram을 뽑아 유사도로 확인하기 / 음절 단위 n-gram과 자모로 풀어 쓴 n-gram이 잡아내는 것이 다른 지점 / min_n=2, max_n=5 부근을 한국어 출발점으로 두는 근거
  - 절「지도 분류 모드와 한계」 — 소절: `__label__` 형식 입력과 `wordNgrams`로 어순 정보를 조금 넣는 방법 / 선형 분류기라 빠른 대신 문맥을 못 보는 구조 / 여전히 정적 임베딩이라 '배'의 세 뜻이 한 벡터에 남는다는 것

- `ml-random-forest` (지금 3,044자 · 절 10)
  - 절「앙상블의 두 갈래」 — 소절: 병렬로 독립 학습해 평균 내는 배깅과 순차로 오차를 고치는 부스팅 / 배깅은 분산을, 부스팅은 편향까지 줄인다는 대응 / 랜덤 포레스트가 배깅 위에 하나 더 얹은 것이 무엇인가
  - 절「부트스트랩과 63.2%」 — 소절: 복원 추출 N번에서 한 샘플이 한 번도 안 뽑힐 확률 (1−1/N)^N이 1/e로 가는 계산 / 각 트리가 서로 다른 63%를 보게 되는 결과 / 남은 36.8%를 버리지 않는다는 예고
  - 절「트리 사이의 상관」 — 소절: 독립일 때 σ²/B, 상관 ρ가 있을 때 ρσ² + (1−ρ)σ²/B로 바뀌는 식 / 트리를 아무리 늘려도 ρσ²가 남아 수확이 체감하는 결론 / 그래서 다양성 확보가 트리 수보다 먼저인 이유
  - 절「랜덤 서브스페이스」 — 소절: 매 분기마다 √d개만 후보로 두어 지배적 특성의 독점을 깨는 방식 / 분류는 √d, 회귀는 d/3이라는 관례가 나온 자리 / `max_features`를 낮추면 개별 트리는 나빠지고 앙상블은 좋아지는 교환을 실제로 재 보기
  - 절「OOB 오류 추정」 — 소절: 각 샘플을 그 샘플을 안 본 트리들만으로 예측해 모으는 절차 / `oob_score_`와 5-겹 교차 검증 값을 나란히 대 보기 / 트리 수가 적으면 OOB 추정이 흔들리는 이유
  - 절「ExtraTrees와 트리 수」 — 소절: 임계값까지 무작위로 뽑아 편향은 올리고 분산은 더 낮추는 교환 / `n_estimators`를 100→500으로 올릴 때 정확도와 학습 시간이 그리는 곡선 / '트리를 늘려도 과적합하지 않는다'는 말이 성립하는 정확한 범위
  - 절「해석과 비용」 — 소절: MDI와 permutation importance가 어긋날 때 어느 쪽을 믿는가 / 수백 트리를 모두 거치는 예측 지연과 메모리(트리 수 × 트리 크기) / 증분 학습이 안 되어 새 데이터마다 재학습해야 하는 운영 부담

- `ml-gradient-boosting` (지금 3,188자 · 절 10)
  - 절「순차 학습의 발상」 — 소절: AdaBoost가 오분류 샘플의 가중치를 올리는 방식과 그 한계(손실 함수를 갈아 끼우기 어렵다) / GBM이 가중치 대신 잔차를 다음 모델의 목표로 두는 전환 / 배깅과 달리 트리끼리 병렬화가 안 되는 구조
  - 절「잔차와 음의 기울기」 — 소절: MSE에서 −∂L/∂ŷ = y−ŷ가 되는 한 줄 계산 / 손실을 로그 손실로 바꾸면 목표가 잔차가 아닌 다른 값이 되는 것 / 트리가 구조를 정한 뒤 리프 값을 한 번 더 최적화한다는 것
  - 절「학습률과 트리 수」 — 소절: lr을 0.1에서 0.02로 내리면 필요한 트리 수가 어떻게 늘어나는가 / 두 값을 따로 튜닝하면 안 되는 이유(곱이 총 기여를 정한다) / 조기 종료로 `best_iteration`을 찾아 고정하는 절차와 검증 세트 분리
  - 절「서브샘플링과 정규화」 — 소절: `subsample<1`이 만드는 다양성과 속도 두 이득 / `colsample_bytree`가 특성 쪽에서 같은 일을 하는 것 / XGBoost가 손실에 직접 얹는 L1·L2와 리프 수 벌점 γ가 각각 무엇을 억제하는가
  - 절「XGBoost의 세 개선」 — 소절: 1차 기울기에 2차 항(헤시안)까지 써서 분기 이득을 계산하는 방식 / 결측값이 갈 기본 방향을 학습으로 정하는 희소 인식 분기 / '병렬 처리'가 트리 순서가 아니라 분기점 탐색에 걸린다는 정확한 범위
  - 절「LightGBM과 CatBoost」 — 소절: 레벨 단위 성장과 리프 단위 성장이 같은 리프 수에서 손실이 갈리는 지점 / 히스토그램 비닝이 정확한 분기점을 포기하고 얻는 메모리·속도 / CatBoost가 타깃 통계를 순서대로 계산해 누출을 막는 방식과 `cat_features` 지정
  - 절「부스팅과 배깅의 갈림길」 — 소절: 잡음이 많은 데이터에서 부스팅이 잡음을 목표로 삼아 따라가는 경로 / `num_leaves`·`max_depth`·`min_child_samples`를 데이터 크기에 맞춰 정하는 순서 / 정형 데이터에서 여전히 먼저 잡는 베이스라인이라는 자리


### build-with-ai

**합치기 — 짝이 남지 않았다(2026-09-08).** 28편 → 22편.


**채우기**

- `huggingface-transformers` (지금 1,010자 · 절 5)
  - pipeline이 감추는 것 — 토크나이즈·패딩·트런케이션·후처리를 손으로 펼쳐 보기
  - 토크나이저의 실제 문제 — 한국어 서브워드 분해, special token, max_length 초과 시 잘리는 자리
  - Trainer가 대신 해 주는 일 목록과 직접 루프를 써야 하는 경우
  - PEFT(LoRA) 최소 예제 — 전체 파인튜닝과 메모리·시간·품질 비교
  - generate() 파라미터가 출력에 미치는 영향 — 같은 입력에 temperature·top_p·repetition_penalty만 바꿔 나란히 보기
  - 모델 고르는 기준 — 라이선스, 파라미터 수, 한국어 지원, 추론 비용
  - 로딩 옵션의 뜻 — device_map, torch_dtype, 양자화 로딩이 각각 무엇을 바꾸는가

- `app-customer-support` (지금 1,231자 · 절 7)
  - 자동화 범위를 감이 아니라 데이터로 — 지난 3개월 티켓을 유형별로 세어 상위 20% 유형이 문의의 몇 %를 덮는지부터 확인
  - 티켓 분류기 만드는 과정 — 라벨 체계 설계, 200건 라벨링으로 프롬프트 고정, 혼동 행렬로 경계 유형 찾기
  - 환각을 막는 답변 생성 — FAQ 근거 없는 답 금지, 근거 문장 인용, 「답할 수 없습니다」 문장을 고정해 두기
  - 에스컬레이션 임계값 정하기 — 자동 처리율과 CSAT의 트레이드오프를 어떻게 그리고 어디서 끊는가
  - 회로 차단기 구현 — 무엇을 세고(연속 부정 피드백·재문의율) 어떤 조건에서 자동 답변을 끄는가
  - 상담원에게 넘길 때 붙이는 컨텍스트 카드 — 구성 요소와 상담원이 실제로 읽는 순서
  - 감정 분석을 답변 톤에 반영할 때의 경계: 과한 공감 표현이 오히려 역효과를 내는 자리

- `tensorflow-keras` (지금 1,238자 · 절 5)
  - 절「Keras 3의 백엔드 구조」 — 소절: 백엔드 셋(TF·PyTorch·JAX)과 `KERAS_BACKEND`를 keras import 전에 정해야 하는 이유 / `tf.keras`와 독립 `keras` 패키지를 한 프로세스에 섞었을 때의 객체 타입 불일치, 예전 코드를 그대로 돌리려면 `tf_keras` / 백엔드를 갈아 끼워도 도는 코드의 조건 — `tf.*` 호출 대신 `keras.ops`로 쓰기
  - 절「세 API가 갈리는 자리」 — 소절: Functional은 그래프를 먼저 세워 `summary()`에 출력 형상이 뜨지만 Subclassing은 한 번 호출하기 전까지 형상이 없다 / `keras.Input` 없이 Sequential을 만들면 가중치가 아직 없어 `summary()`가 서는 자리 / 직렬화 — Sequential·Functional은 `.keras` 하나로 구조까지 복원되고 Subclassing은 `get_config`/`from_config`나 등록 데코레이터를 직접 달아야 한다 / 셋을 고르는 기준 — 분기·잔차·다중 입력이면 Functional, 학습 로직 자체가 다르면 Subclassing
  - 절「compile이 정하는 것」 — 소절: 지금 글이 어긋나 있다 — Sequential 마지막 층이 `softmax`인데 compile은 `from_logits=True`다. 소프트맥스가 두 번 걸리면 손실이 어떻게 되는지 보이고 둘 중 하나로 통일 / Sparse와 원-핫 손실의 라벨 형상 차이(`(N,)` vs `(N, C)`)와 잘못 넣었을 때 뜨는 형상 오류 / `metrics=["accuracy"]`가 손실과 출력 형상을 보고 binary·categorical·sparse 중 하나로 풀린다 — 이진 분류에서 값이 엉뚱해지는 경우 / `run_eagerly=True`로 그래프 안을 찍어 보는 디버깅 순서
  - 절「fit이 도는 한 바퀴」 — 소절: 배치 → 순전파 → 손실 → 기울기 → 갱신 → 지표 → 콜백 훅의 순서를 적고 어느 자리에 무엇이 끼어드는지 / `validation_split=0.2`는 셔플 전에 뒤 20%를 자른다 — 클래스별로 정렬된 데이터에서 검증셋이 통째로 한 클래스가 되는 사고 / 학습 손실은 에폭 평균이고 검증 손실은 에폭 끝 한 번이라 첫 에폭에 train > val이 나오는 이유 / `train_step()`만 재정의해 콜백·지표는 fit에 맡기고 커스텀 손실을 넣기
  - 절「콜백이 붙는 자리」 — 소절: `monitor` 이름을 틀리면 조용히 아무 일도 안 하는 EarlyStopping, patience·min_delta를 검증 곡선의 흔들림 폭에서 정하는 법 / `ModelCheckpoint`의 확장자 규칙 — 전체 저장은 `.keras`, 가중치만은 `.weights.h5` / `LearningRateScheduler`와 `ReduceLROnPlateau`를 같이 걸면 스케줄러가 매 에폭 lr을 덮어써 감소가 사라진다 / `on_epoch_end(logs)`로 커스텀 콜백 하나 만들기 — logs에 실제로 무엇이 들어 있는가
  - 절「tf.data 파이프라인 순서」 — 소절: map·cache·shuffle·batch·prefetch의 순서가 결과를 바꾼다 — shuffle을 batch 뒤에 두면 배치끼리만 섞이고, 랜덤 증강 뒤에 cache를 두면 매 에폭 같은 증강이 나온다 / `shuffle(buffer_size)`는 버퍼 안에서만 섞는다 — 정렬된 파일에서 버퍼가 작을 때의 편향과 버퍼 크기 정하는 기준 / GPU가 노는지 확인하는 절차 — 스텝 시간을 재고 `num_parallel_calls`·`prefetch` 넣기 전후를 비교 / Keras 3에서 백엔드가 torch·jax여도 tf.data를 입력 파이프라인으로 그대로 쓸 수 있다
  - 절「저장 형식과 배포 경로」 — 소절: `.keras`·`.weights.h5`·SavedModel(`model.export()`)이 각각 무엇을 담는가 — 옵티마이저 상태가 들어 있어야 학습을 이어 붙일 수 있다 / TF Serving은 SavedModel만 읽고 마운트 경로에 버전 숫자 디렉터리를 요구한다 — `.keras`를 넣었을 때의 증상 / 커스텀 레이어가 든 모델을 다른 프로세스에서 로드하는 절차(`custom_objects` 또는 등록 데코레이터) / TFLite 변환이 지원 안 되는 연산을 만났을 때의 갈림 — 연산을 바꿀지 SELECT_TF_OPS를 켤지

- `ai-coding-claude-code` (지금 1,273자 · 절 11)
  - 에이전트가 도는 한 사이클 — 계획 → 도구 호출 → 결과 읽기 → 수정의 반복을 실제 세션 로그로 따라가기
  - CLAUDE.md에 무엇이 듣고 무엇이 무시되는가 — 길이와 구체성의 경계, 규칙이 지켜지지 않을 때 확인할 것
  - 권한 설계 — 허용·거부 목록을 가르는 기준, 자동 승인 모드를 켜도 되는 조건과 절대 안 되는 명령
  - 서브 에이전트를 나눠야 할 때와 오히려 느려지는 때 — 작업 독립성과 컨텍스트 재전달 비용
  - MCP 서버 하나를 붙이는 전 과정 — 설정, 도구 이름 충돌, 연결 실패 진단
  - Hooks로 린트·테스트를 묶었을 때의 실제 이득과 세션이 느려지는 지점
  - CI에서 돌릴 때 — 비대화식 실행, 승인 없는 환경의 권한 설계, 실패 처리

- `ai-coding-codex` (지금 1,521자 · 절 8)
  - HumanEval 28.8% → 92%가 실제로 뜻하는 것 — 이 벤치마크가 재는 것과 재지 못하는 것
  - 추론 모델(o-series)과 일반 모델을 언제 가르는가 — 문제 유형별 비용·지연·성공률 비교
  - Code Interpreter 샌드박스의 한계 — 네트워크 차단, 파일 크기, 세션 만료가 작업 방식에 미치는 영향
  - Codex CLI 실전 한 사이클 — 저장소 붙이기부터 변경 검토·되돌리기까지
  - Tool Calling으로 코드를 실행시킬 때의 격리 설계 — 컨테이너, 타임아웃, 출력 크기 제한
  - 팀에 들일 때 확인할 데이터 처리 정책 — 코드 전송 범위, 보존 기간, 학습 사용 여부
  - Canvas와 채팅의 갈림 — 어느 크기의 수정부터 Canvas가 이득인가

- `ai-coding-aider` (지금 1,588자 · 절 9)
  - 레포 맵이 만들어지는 방식 — tree-sitter로 심볼을 뽑아 토큰 예산 안에서 순위를 매기는 절차
  - 편집 형식(diff·whole·udiff)이 실패하는 자리와 모델마다 다른 적용 성공률
  - Architect 모드의 비용 계산 — 설계 모델과 편집 모델을 갈랐을 때 실제로 얼마나 줄어드는가
  - 자동 커밋이 만든 히스토리를 사람이 다시 정리하는 법 — 언제 squash하고 무엇을 커밋 메시지로 남길 것인가
  - 로컬 모델(Ollama)로 돌릴 때 쓸 만한 크기와 못 하는 작업
  - /undo·/diff·/test를 엮은 버그 수정 한 사이클을 처음부터 끝까지
  - 여러 파일을 한 번에 고칠 때 컨텍스트에 넣을 파일을 고르는 기준 — /add를 남발하면 생기는 일

- `python-for-ai` (지금 1,688자 · 절 6)
  - NumPy에서 실제로 막히는 곳 — 브로드캐스팅 규칙과 축(axis) 잡기, 형상 오류 메시지를 읽는 법
  - Pandas 성능의 갈림길 — 반복문·apply·벡터화의 차이, 메모리를 줄이는 dtype 선택
  - Scikit-learn 파이프라인이 데이터 누수를 막는 구조와 누수가 나는 전형적인 코드
  - 환경 재현 — 버전 고정, CUDA·PyTorch 조합 확인, uv·venv·conda를 언제 쓰는가
  - 라이브러리 선택 지도 — 표 하나가 아니라 「이 작업에는 이것」 갈래로 다시 세우기
  - 각 라이브러리를 처음 쓸 때 겪는 오류와 원인 — import 실패, 버전 충돌, GPU 인식 실패
  - 시각화를 학습 진단에 쓰는 법 — 손실 곡선의 모양으로 과적합·학습률 문제를 읽기

- `anthropic-sdk` (지금 1,792자 · 절 10)
  - 메시지 구조를 정확히 — system·user·assistant의 역할, assistant 선입력(prefill), 멀티턴에서 무엇을 다시 보내는가
  - Tool Use 한 사이클 전체 — 도구 정의 → tool_use 응답 → 결과 회신 → 최종 답, 그리고 병렬 도구 호출
  - 프롬프트 캐싱이 이득이 되는 경계 — 캐시 쓰기 비용, TTL, breakpoint를 어디에 두는가
  - 스트리밍 이벤트의 종류와 부분 JSON을 안전하게 파싱하는 법
  - 에러와 재시도 설계 — 429와 overloaded의 차이, 지수 백오프, 중복 요청을 막는 멱등성
  - 토큰 세기와 비용 추정 — 배포 전에 요금을 계산하는 절차
  - 배치 API를 쓸 조건 — 지연을 얼마나 감수하면 얼마를 아끼는가, 결과 회수와 실패 처리

- `app-chatbot-design` (지금 1,861자 · 절 7)
  - 대화 이력을 어디에 두는가 — 클라이언트·Redis·DB 세 방식의 비용, 장애 시 동작, 여러 기기에서 이어 쓰기
  - 요약 압축이 잃는 것 — 지시사항·수치·고유명사가 요약에서 사라지는 사례와 압축에서 보호할 항목 목록
  - 스트리밍의 뒷면 — 취소·재연결 처리, 중간에 끊긴 응답을 이력에 남길지 버릴지
  - 프롬프트 인젝션 방어 — 사용자 입력과 검색 결과를 구조로 분리하기, 시스템 프롬프트 지시만으로 부족한 이유
  - 모델 라우팅 설계 — 어떤 신호로 큰 모델을 부를지, 작은 모델이 실패했을 때의 승급 경로
  - 부하와 비용 추정 — 동시 접속 100명 기준 토큰·지연·요금을 배포 전에 계산하는 예시
  - 평가 — 답변 품질을 무엇으로 재는가: 사람 평가 표본, thumbs 데이터의 편향, 회귀 세트 만들기

- `notebook-jupyter` (지금 2,557자 · 절 9)
  - 절「브라우저·서버·커널의 분리」 — 소절: 셀을 실행했을 때 브라우저 → 서버 → 커널로 무엇이 흘러가고 결과가 어느 소켓(iopub)으로 돌아오는가 / 커널이 죽어도 서버와 파일은 남는다 — 「Restart Kernel」·「Reconnect」·브라우저 새로고침이 각각 되돌리는 범위 / 노트북이 엉뚱한 파이썬을 쓰는 사고 — `jupyter kernelspec list`로 확인하고 가상환경마다 `ipykernel install --user --name`으로 커널을 따로 등록하는 절차
  - 절「.ipynb의 구조와 실행 순서」 — 소절: outputs가 파일에 그대로 박혀 git diff가 base64 이미지로 뒤덮이는 문제와 nbstripout·jupytext 두 갈래 / `execution_count`가 [3] 다음 [1]인 노트북은 남이 재현할 수 없다 — 「Restart & Run All」을 커밋 전 확인 절차로 두기 / 파일에서 셀을 지워도 커널 네임스페이스에는 변수가 남는다 — `%whos`로 확인하고 `%reset`·`del`로 지우는 자리
  - 절「매직 커맨드의 갈래」 — 소절: 라인(`%`)·셀(`%%`)·셸(`!`)의 규칙과 `%%`가 셀 첫 줄이어야 하는 제약 / `!pip`은 셸의 파이썬에, `%pip`은 지금 커널의 파이썬에 설치한다 — 「분명히 설치했는데 ImportError」의 대부분이 여기다 / `%timeit`은 코드를 여러 번 돌리므로 파일 쓰기·API 호출이 든 셀에 쓰면 안 되고, GPU 코드는 비동기라 동기화 없이 잰 시간이 거짓이다 / `%autoreload 2`가 못 잡는 것 — 이미 만들어 둔 인스턴스, `from m import f`로 가져온 이름, C 확장
  - 절「메모리와 죽는 커널」 — 소절: 호스트 RAM 초과로 커널이 통째로 죽는 것과 CUDA OOM이 예외로 뜨는 것은 다른 사건이다 — 로그에서 구분하는 법 / `Out[n]`·`_`·`__`가 이전 출력을 붙들고 있어 `del` 뒤에도 메모리가 안 풀리는 자리와 `%reset out` / `del` → `gc.collect()` → `torch.cuda.empty_cache()`가 각각 무엇을 반납하는가(마지막 것은 파이토치 캐시만이다) / 조용히 쌓이는 것들 — `plt.close()`를 빠뜨린 반복문, 큰 중간 데이터프레임을 셀에 남겨 두는 습관
  - 절「실험 노트북의 뼈대」 — 소절: 맨 위 환경 셀 — 파이썬·라이브러리 버전과 GPU 이름·VRAM을 찍어 두면 몇 주 뒤 결과가 갈렸을 때 무엇이 달라졌는지 바로 보인다 / 설정을 한 셀에 모으고 시드를 고정하되, 고정해도 완전히 같아지지 않는 자리(비결정 연산·데이터 순서)를 함께 적기 / `ipywidgets` 슬라이더는 값을 눈으로 훑을 때만 쓰고 확정된 값은 config 셀로 옮긴다 — 위젯에만 남은 값은 재현되지 않는다 / 커널이 죽어도 잃지 않게 — 에폭마다 체크포인트, 몇 시간짜리 학습은 `%%writefile`로 스크립트로 빼는 경계
  - 절「노트북을 내보내는 길」 — 소절: `nbconvert --to html --execute`를 CI에 걸 때 — 실패한 셀에서 멈출지(`--allow-errors`) 정하는 기준과 실행된 HTML을 아티팩트로 남기기 / papermill의 `parameters` 태그 셀로 값을 주입해 같은 노트북을 데이터셋 다섯 개에 돌리기 / 노트북에 남길 것과 `.py`로 뺄 것의 경계 — 전처리·모델 정의는 모듈로, 탐색과 그림은 노트북에
  - 절「Lab과 원격 실행」 — 소절: 확장 중 값하는 것과 버전이 어긋나면 통째로 깨지는 자리(`jupyter labextension list`로 상태 보기) / 원격 GPU 서버에 붙기 — SSH 포트 포워딩과 토큰 인증, `--ip=0.0.0.0`을 그냥 여는 것이 위험한 이유 / Colab과 로컬의 갈림 — 런타임이 끊기는 조건, Drive 마운트, 로컬 런타임 연결로 둘을 섞는 방법

- `gemini-sdk` (지금 2,799자 · 절 14)
  - 절「클라이언트와 인증」 — 소절: 전역 `genai.configure()` 방식과 클라이언트 객체 방식 중 지금 공식 문서가 기본으로 두는 쪽을 확인해 글 전체 예제를 하나로 통일하기 / API 키가 새는 경로 셋 — 노트북 출력, 커밋된 설정 파일, 클라이언트 코드에 박은 키 / AI Studio 키와 Vertex AI 서비스 계정 — 인증 주체·데이터 처리 정책·리전이 갈리는 지점과 옮겨 갈 때 다시 해야 하는 것 / 모델 이름을 코드 곳곳에 박지 않고 상수로 빼기 — 별칭과 고정 버전 중 프로덕션에 둘 쪽
  - 절「응답 객체 읽는 법」 — 소절: `response.text` 한 줄이 예외를 던지는 세 경우(후보 없음·안전 차단·빈 parts)와 `candidates`부터 확인하는 헬퍼 하나 / parts 배열에 텍스트와 `function_call`이 섞여 오므로 `parts[0].text`를 가정하면 깨지는 자리 / `finish_reason` 값별 대응 — STOP·MAX_TOKENS·SAFETY를 각각 어떻게 처리하는가 / 스트리밍 청크의 구조 — 빈 `chunk.text`, 마지막 청크에 오는 `usage_metadata`, 스트림 도중 차단을 알아채는 방법
  - 절「생성 파라미터의 실제 효과」 — 소절: 지금 글의 「temperature=0.0은 항상 같은 응답」을 고치는 자리 — 그리디 디코딩이지 재현 보장이 아니다 / 같은 프롬프트를 temperature·top_p·top_k만 바꿔 돌린 출력을 나란히 놓고 무엇이 달라지는지 / `max_output_tokens`에 걸려 잘린 JSON을 파싱하다 깨지는 자리 — 잘림을 감지해 다시 부르는 코드 / `response_mime_type="application/json"`과 `response_schema`로 형식을 강제하는 방법, 그래도 내용은 틀릴 수 있다는 구분
  - 절「멀티모달 입력의 규칙」 — 소절: 인라인 바이트와 Files API의 갈림 — 요청 전체 크기 한도를 넘으면 업로드 경로이고, 업로드 파일에는 보관 기간이 있어 오래 쓸 캐시로 삼을 수 없다 / 동영상은 길이가 그대로 토큰이 된다 — 3분짜리 하나를 넣고 `usage_metadata`로 실제 토큰을 재 본 값 / 이미지와 지시문의 순서 — 지금 글의 「이미지를 앞에」 주장을 같은 질문으로 양쪽 다 돌려 보고 다시 적기 / PDF·오디오를 넣었을 때 무엇이 텍스트로 읽히고 무엇이 이미지로 처리되는가
  - 절「도구 호출 한 사이클」 — 소절: `protos`로 스키마를 직접 짜는 방식과 파이썬 함수를 그대로 넘겨 자동 선언·자동 호출에 맡기는 방식의 갈림 / 왕복 전체 — `function_call`을 받아 실제 함수를 돌리고 `FunctionResponse`로 되돌려 최종 답까지, 지금 글이 멈춘 자리부터 이어 쓰기 / `tool_config`의 AUTO·ANY·NONE과 `allowed_function_names` — 「특정 함수만 허용」은 NONE이 아니라 ANY + 이름 목록이다(지금 글의 설명을 고친다) / 도구가 오류를 냈거나 결과가 비었을 때 모델에 무엇을 돌려줄 것인가
  - 절「안전 필터와 실패 처리」 — 소절: HarmCategory 넷과 임계값이 각각 무엇을 막는가 — `prompt_feedback.block_reason`으로 입력 차단과 출력 차단을 구분하기 / 차단됐을 때 사용자에게 보일 문장과 로그에 남길 것 — `safety_ratings`를 그대로 화면에 내보내지 않는 이유 / 재시도해도 되는 예외와 안 되는 예외 — `ResourceExhausted`는 백오프, `InvalidArgument`는 즉시 실패 / 무료 티어의 분당 한도에 부딪히는 지점과 동시 호출 수를 정하는 방법
  - 절「대화 세션과 컨텍스트 비용」 — 소절: `start_chat()` 히스토리는 매 요청에 통째로 다시 올라간다 — 20턴 대화의 누적 입력 토큰을 계산해 어디서 요약·절단할지 정하기 / 반복되는 긴 문서는 컨텍스트 캐싱으로 옮긴다 — 최소 조건과 저장 요금, 몇 번 재사용해야 이득인가 / 지금 글의 `asyncio.to_thread`는 스레드로 감싼 것이지 비동기 호출이 아니다 — SDK의 async 메서드로 바꾸고 세마포어로 동시 실행 수를 묶기 / 1M 컨텍스트를 실제로 채웠을 때의 지연과 요금 — 「넣을 수 있다」와 「넣는 것이 낫다」의 구분

- `openai-sdk` (지금 3,041자 · 절 13)
  - 절「클라이언트와 요청 설정」 — 소절: 키를 환경변수로 두는 이유와 조직·프로젝트 헤더, `base_url`을 바꿔 호환 엔드포인트에 붙이는 경우 / SDK가 이미 재시도(기본 2회)와 타임아웃을 하고 있다 — 바깥에 또 백오프를 두면 한 번의 장애에 요금이 곱해지는 자리 / 지금 글의 비동기 예제처럼 함수 안에서 클라이언트를 만들면 커넥션 풀이 매번 새로 선다 — 모듈 수준에 하나만 두기
  - 절「메시지와 응답의 구조」 — 소절: 멀티턴은 매 요청에 히스토리 전체를 다시 보내고 다시 과금된다 — 10턴 대화의 누적 입력 토큰을 세어 보기 / 응답에서 반드시 볼 것 — `finish_reason`의 stop·length·tool_calls·content_filter와 `usage` 세 값, length로 잘린 답을 감지하는 코드 / Chat Completions와 Responses API — 같은 호출을 두 API로 나란히 적어 `messages`↔`input`, `choices`↔`output_text`가 어떻게 대응하는지 / 스트리밍에서 usage를 받으려면 `stream_options`가 필요하고, 중간에 끊긴 응답을 이력에 남길지 정해야 한다
  - 절「모델을 고르는 축」 — 소절: 이름이 아니라 세 축(품질·지연·100만 토큰당 요금)으로 표를 세우고 실제 값은 가격표에서 확인하도록 두기 / 추론 모델이 다른 점 — 보이지 않는 추론 토큰이 출력 토큰으로 과금되고, 한도를 짜게 잡으면 내용이 빈 채 length로 끝난다 / 지금 글이 적어 둔 추론 모델의 제약(temperature·system 미지원)이 아직 유효한지 문서로 확인해 고쳐 적기 / 작은 모델로 시작해 승급하는 절차 — JSON 파싱 실패율·재작업률 같은 신호로 올릴 때를 정하고 라우팅을 코드 어디에 둘지
  - 절「도구 호출 한 사이클」 — 소절: 도구 정의에서 `description`이 선택에 실제로 영향을 주는 자리, `enum`·`required`·strict로 인자 오류를 줄이기 / 왕복 전체 — `tool_calls`가 든 assistant 메시지를 그대로 되붙이고 `tool_call_id`를 맞춰 답하기, 병렬로 여러 개가 왔으면 전부 답해야 한다 / 지금 글의 예제는 왕복 한 번은 온전히 돈다 — `tool_calls`를 받아 assistant 메시지를 되붙이고 `role: "tool"`로 답한 뒤 최종 호출까지 간다. 없는 것은 **두 번째 왕복을 받는 루프**이고, 그래서 도구가 연달아 불리는 경우를 못 다룬다. 있는 코드를 지우지 말고 최대 왕복 수를 둔 while로 감싸며, `tool_calls[0]`만 보는 자리를 전부 순회하도록 고치고 도구 오류를 돌려줄 형식도 그 루프 안에서 정한다 / `tool_choice`로 강제·금지할 때와 모델 판단에 맡길 때의 갈림
  - 절「구조화 출력의 보장 범위」 — 소절: `json_object` 모드는 「JSON이긴 한 것」만 보장하고 스키마 강제는 필드까지 보장한다 — 둘을 언제 쓰는가 / Pydantic이 JSON 스키마로 바뀌며 걸리는 제약 — 모든 필드가 required여야 하고 `additionalProperties`가 막히며 지원 타입이 한정된다, 선택 필드를 null 합집합으로 우회하기 / 형식이 맞아도 내용은 틀릴 수 있다 — 거절(refusal) 응답 처리와 스키마 밖의 검증을 따로 두는 이유 / `parse()`의 현재 경로가 아직 `beta` 아래인지 확인해 예제를 고치기
  - 절「임베딩과 이미지 입력」 — 소절: 임베딩 모델 둘의 갈림과 `dimensions`로 차원을 줄였을 때 자기 데이터에서 검색 정확도가 얼마나 떨어지는지 재 보는 절차 / 한 요청에 넣을 수 있는 입력 개수와 모델의 입력 토큰 한도(8,191) — 긴 문서를 자르는 기준과 청크 경계 / 이미지 비용 — `detail`이 low·high로 갈릴 때 타일 수와 토큰이 어떻게 달라지는가, 큰 이미지를 미리 줄여 보내기 / base64와 URL 방식의 갈림 — 사설망 이미지, 만료되는 URL, 요청 크기
  - 절「요금·한도와 실패 처리」 — 소절: 지금 글의 `+4`·`+2` 오버헤드 계산은 모델마다 다르다 — 실제 `usage`와 대조해 검증하고 어긋나면 고치기 / 429가 뜨는 두 이유(분당 요청 수와 분당 토큰 수)와 응답 헤더의 남은 한도로 스스로 속도를 줄이는 방법 / 재시도해도 되는 예외와 안 되는 예외, 지수 백오프에 지터를 넣는 이유와 SDK 내장 재시도와 겹치지 않게 하기 / 배치 API를 쓸 조건 — 24시간 창과 절반 요금, 실패한 줄만 골라 다시 넣는 절차, 프롬프트 캐싱이 듣게 공통 접두사를 앞에 모으기
- `app-content-generation` (지금 5,916자 · 절 7)
  - **얼개는 이미 절 7 · 소절 21로 서 있다 — 새 절을 세우지 말고 코드가 대신 말하고 있는 소절만 채운다.** 2026-09-08에 6,034자로 채우고 이 줄을 지웠는데, 다음 날 절 제목을 짧게 고치는 손질에 118자가 줄어 다시 미달이 됐다
  - 소절「절별 동시 호출」 94자 — 문장 하나 뒤가 전부 코드다. 동시 호출 수를 무엇으로 정하는가(레이트 리밋과 절 개수 중 어느 쪽이 먼저 걸리는가), 한 절이 실패했을 때 전체를 버릴지 그 절만 다시 부를지, 순차 작성과 견준 실제 소요 시간
  - 소절「형식별 프롬프트 표」 122자 — 표를 코드로 보여 주고 끝난다. 형식마다 실제로 갈리는 것이 구조·길이·마무리 셋뿐인지, 뉴스레터에 붙였을 때 표만으로 안 되던 자리
  - 소절「생성 호출」 138자 — 아웃라인을 무엇으로 받는지(자유 형식과 스키마 강제의 갈림), 절 개수를 모델에 맡길 때와 못박을 때의 결과 차이
  - 소절「통과·불통과 기준」 145자 — 「점수가 아니라 통과·불통과」라고 적어 두고 실제 기준 목록이 없다. 판정이 갈리지 않는 기준 대여섯 개를 적고, 그 기준으로 걸러진 초안이 무엇이었는지
  - 소절「단정 자리 표시」 156자 · 「자동 발행 금지 항목」 209자 — 사실 주장·수치·고유명사를 어떻게 표시해 검수자에게 넘기는가, 표시가 본문에 남는 사고를 막는 법
  - 소절「반복 상한」 234자 — 3회가 대개 2회에서 멎는다고만 적혀 있다. 세 번 돌린 출력이 회마다 얼마나 달라지는지 실제로 재 본 값
- `app-translation` (지금 5,920자 · 절 7)
  - 위와 같은 자리다 — 6,015자에서 멎었다가 제목을 고치며 95자가 줄었다. 얼개는 절 7 · 소절 21로 서 있으므로 얇은 소절만 채운다
  - 소절「용어집 주입」 54자 — 한 문장 뒤가 전부 코드다. 용어집을 시스템 프롬프트에 두는 것과 사용자 메시지에 두는 것의 차이, 청크마다 골라 넣을 때 걸리는 것(용어가 청크 경계에 걸친 경우), 항목 수가 몇 개를 넘으면 뒤쪽이 덜 지켜지는지
  - 소절「치환과 복원」 102자 · 「복원 검사」 172자 — 코드만 있다. 복원이 실패하는 실제 모양(자리 표시가 번역문에서 사라짐·중복됨·순서가 바뀜)과 각각을 어떻게 잡아내는가
  - 소절「도메인별 문체」 129자 — 도메인마다 문체가 다르다는 말에서 멎는다. 같은 문장을 법무·마케팅·기술 문서 문체로 옮긴 것을 나란히 두고 무엇이 갈리는지
  - 소절「용어 준수율」 135자 — 준수율을 어떻게 세는가(용어가 문장에 없을 때·활용형으로 나올 때의 처리), 몇 %면 통과로 볼 것인가
  - 소절「TM과 용어집」 160자 — 둘의 차이를 한 문단으로만 적었다. 같은 문서를 TM만·용어집만·둘 다로 돌렸을 때의 결과 차이
  - 소절「시맨틱 캐싱」 176자 — 함정을 이름만 적고 다음 소절로 넘어간다. 문장은 같은데 문맥이 달라 캐시가 잘못 맞은 실제 예


### domain-models

**합치기 — 짝이 남지 않았다(2026-09-08).** 67편 → 55편.


**채우기**

- `nlp-text-generation` (지금 1,626자 · 절 6)
  - 로짓에서 토큰까지 — temperature·top-k·top-p가 같은 분포를 각각 어떻게 자르는지 수치 예 하나로 나란히 보기
  - Beam search의 길이 편향 — 왜 짧은 답이 이기는가, length_penalty로 되돌리는 방법
  - 반복이 생기는 이유 — likelihood trap과 자기 강화, no_repeat_ngram_size·repetition_penalty·frequency penalty가 각각 무엇을 벌하는가
  - 최근 전략 — contrastive search, min-p, typical sampling이 top-p보다 나은 상황
  - 제약 디코딩 — 문법·정규식·JSON 스키마로 토큰을 막는 방식과 그것이 품질에 미치는 영향
  - 속도 — KV 캐시가 아끼는 연산량, 연속 배칭, 추측 디코딩(speculative decoding)의 원리
  - 평가 — 퍼플렉시티가 생성 품질과 어긋나는 자리, MAUVE와 사람 쌍 비교

- `cv-vision-transformer` (지금 1,766자 · 절 7)
  - 패치 크기가 정하는 것 — 시퀀스 길이와 O(n²) 어텐션 비용, 16×16과 14×14의 실제 연산량·정확도 차이
  - 귀납 편향이 없다는 뜻 — ViT가 JFT-300M 규모를 요구한 이유, 데이터 양에 따른 CNN 대비 성능 역전 지점
  - 위치 임베딩 — 학습형 1D 임베딩의 한계와 추론 해상도를 바꿀 때의 보간 절차
  - DeiT의 증류 토큰 — 교사 CNN을 써서 ImageNet-1k만으로 학습시키는 방법
  - Swin — 창 어텐션과 shifted window가 계층 구조와 선형 복잡도를 되찾는 방식
  - MAE 자기지도 — 패치 75%를 가리고 재구성하는 것이 라벨 없이 표현을 만드는 원리
  - 어텐션 맵으로 보는 ViT — 무엇을 보고 분류하는가, CNN 특징 맵과 나란히 놓았을 때의 차이
  - 실전 파인튜닝 레시피 — layer-wise learning rate decay, 해상도 상향, 작은 데이터에서의 정규화·증강

- `nlp-coreference` (지금 1,823자 · 절 7)
  - 멘션 후보를 어떻게 줄이는가 — 가능한 모든 스팬이 O(n²)인 문제, 길이 제한과 점수 상위 가지치기
  - e2e-coref의 점수 함수 — 멘션 점수와 선행사 점수의 합, 더미 선행사 ε가 「선행사 없음」을 표현하는 방식
  - 클러스터링 — 최우선 선행사 랭킹과 그리디 병합, 한 번의 오답이 클러스터 전체를 오염시키는 사슬 오류
  - 한국어 제로 대명사 복원 — 생략된 주어를 찾는 절차, 격조사·서술어 자질 단서, 어디까지를 지시 해소로 볼 것인가
  - 높임 표현과 지시 — 「선생님께서」·「그분」이 만드는 자질 일치 규칙
  - 평가 지표 셋 — MUC·B³·CEAF가 서로 다른 오류를 벌하는 방식과 셋의 평균인 CoNLL F1
  - 상위 태스크로 번지는 오류 — 요약·QA·정보 추출에서 지시 해소 실패가 어떤 결과로 나타나는가

- `rl-policy-gradient` (지금 1,913자 · 절 7)
  - 정책 경사 정리 유도 — log-derivative trick으로 기대값 안의 미분을 밖으로 옮기는 세 줄
  - 베이스라인이 편향을 만들지 않는 이유 — E[∇log π]=0을 보이고 분산이 줄어드는 원리를 잇기
  - 연속 행동 정책 설계 — σ를 상태 의존으로 둘지 학습 파라미터로 둘지, tanh 스쿼싱과 로그 확률 보정
  - 엔트로피 보너스 — 조기 결정론화를 막는 계수 범위와 감쇠 스케줄
  - 보상 스케일과 할인율 — 리턴 표준화가 학습을 살리는 자리, γ가 정하는 실질 지평선 1/(1−γ) 계산
  - REINFORCE가 실패하는 실측 — 같은 환경·다른 시드에서 학습 곡선이 갈리는 정도와 필요한 에피소드 수
  - 신뢰 영역으로 가는 다리 — 한 번의 큰 스텝이 정책을 되돌릴 수 없게 만드는 구체적 장면

- `nlp-summarization` (지금 1,978자 · 절 6)
  - TextRank 안쪽 — 문장 유사도 행렬 만들기, 감쇠 계수 d와 수렴 판정, 한국어 문장 분리의 함정
  - BART의 사전학습 노이즈 다섯 가지와 그중 요약에 실제로 효과가 있는 것
  - 사실성 — 환각이 생기는 세 유형(개체 뒤바뀜·수치 오류·없는 인과)과 각각의 점검 방법
  - ROUGE가 못 보는 것 — 같은 뜻 다른 표현, 순서 뒤바뀜, BERTScore·QAEval을 함께 쓰는 이유
  - 한국어 형태소 단위 ROUGE 계산 절차와 어절 단위와의 점수 차
  - 긴 문서 — 계층 요약의 청크 크기·겹침 설계, 중간 요약 단계에서 정보가 새는 지점
  - KoBART 파인튜닝 실전 — 데이터 정제 기준, 최대 입력 길이, min/max_length와 length_penalty로 생성 길이 잡기

- `nlp-machine-translation` (지금 2,042자 · 절 6)
  - 인코더-디코더 어텐션이 정렬을 대신하는 방식 — 통계 기반 정렬 모델과 나란히 놓고 보기
  - 서브워드가 번역 품질에 미치는 영향 — 공유 어휘와 분리 어휘, 한국어 형태소 경계와 BPE 분할의 충돌
  - NLLB-200의 다국어 학습 — 언어별 샘플링 온도와 저자원 언어의 성능 편차
  - 평가 — BLEU 계산 절차와 한국어 토큰화 의존성, chrF·COMET이 사람 판단에 더 가까운 이유
  - 문장 단위 번역이 깨뜨리는 것 — 대명사·경어·용어 일관성, 문맥 창을 주는 문서 단위 번역
  - 용어집과 도메인 적응 — 강제 사전, 파인튜닝, 후처리 치환의 장단
  - LLM 번역과 전용 NMT — 지연·비용·일관성 세 기준으로 고르는 표

- `nlp-text-classification` (지금 2,076자 · 절 7)
  - 절「세 가지 레이블 구조」 — 소절: 이진·다중 클래스·다중 레이블이 갈리는 자리 / 소프트맥스는 합이 1이고 시그모이드는 아니라는 차이가 손실 선택(CE·BCE)까지 끌고 가는 것 / 다중 레이블에서 임계값 0.5를 클래스마다 따로 정해야 하는 이유
  - 절「TF-IDF가 아직 이기는 자리」 — 소절: 나이브 베이즈·SVM·로지스틱 회귀가 각각 강한 데이터 모양 / `ngram_range=(1,2)`·`sublinear_tf`·`max_features`를 켜고 끌 때 F1이 움직이는 폭 / 학습 3초짜리 기준선을 먼저 세워 두고 BERT의 이득을 그 위에서 재는 순서
  - 절「BERT 파인튜닝의 손잡이」 — 소절: `[CLS]` 풀링과 평균 풀링이 갈리는 자리 / 학습률 2e-5~5e-5·에폭 2~4가 관례가 된 이유와 벗어났을 때 나타나는 증상(손실이 안 내려감·과적합) / `max_length`를 토큰 길이 분포의 95 지점에서 자르는 절차
  - 절「불균형과 임계값」 — 소절: `class_weight`·오버샘플링·Focal Loss가 각각 손대는 자리 / 정확도 대신 macro-F1을 봐야 소수 클래스가 보이는 이유 / 소프트맥스 최댓값을 신뢰도로 쓸 때의 과신과 「0.7 미만은 분류 불가」 게이팅
  - 절「많은 레이블의 계층 분류」 — 소절: 클래스 50개를 넘으면 대분류→소분류 2단이 이기는 지점 / 혼동 행렬에서 두 클래스가 서로 먹는 짝을 찾아 레이블 체계 자체를 의심하기 / 새 클래스가 생길 때 전체 재학습을 피하는 구성
  - 절「한국어에서 갈리는 자리」 — 소절: TF-IDF에는 형태소 토큰이, BERT에는 서브워드가 필요한 이유(「좋다/좋아서/좋았던」) / 「ㅋㅋㅋㅋ」·오탈자·이모지를 정규화할 때와 남겨야 할 때 / KLUE-RoBERTa·KoELECTRA·DistilKoBERT를 정확도와 추론 속도로 고르는 표
  - 절「틀린 것을 읽는 순서」 — 소절: 오답 50건을 직접 읽어 라벨 오류와 모델 오류를 가르기 / 역번역·동의어 치환을 전부가 아니라 적은 클래스에만 거는 이유 / 서비스에 나간 뒤 분포가 밀릴 때 재학습 시점을 정하는 신호

- `nlp-korean-processing` (지금 2,168자 · 절 6)
  - 절「교착어가 만드는 문제」 — 소절: 영어 eat이 여섯 형태인데 한국어 동사는 수백 활용형이라는 사실이 어휘 크기에 미치는 몫 / 조사가 문법 역할을 지므로 어순을 바꿔도 뜻이 같은 것(「철수가 영희를」/「영희를 철수가」) / 경어법이 어미가 아니라 어휘 자체를 바꾸는 자리(먹다·드시다·잡수시다)
  - 절「형태소 분석기 고르기」 — 소절: Okt·Komoran·Mecab·Kiwi를 속도·신조어 강건성·설치 난이도로 가른 표 / 자바 의존성이 컨테이너 이미지와 배포에서 늘리는 것 / 같은 문장을 넷에 넣어 결과가 갈리는 실제 예와 그때 무엇을 근거로 고르는가
  - 절「사용자 사전과 띄어쓰기」 — 소절: 도메인 고유명사가 쪼개질 때 사전에 넣는 절차와 품사 태그를 함께 적어야 하는 이유 / 「서울역을」과 「서울 역을」이 분석기마다 다르게 갈리는 것을 파이프라인에서 흡수하기 / 사전을 키우다 오히려 나빠지는 자리
  - 절「KLUE 여덟 태스크」 — 소절: TC·STS·NLI·NER·RE·DP·MRC·WOS가 각각 재는 능력 / RE의 F1 0.73과 WOS의 JGA 0.51이 다른 태스크보다 유독 낮은 이유 / 우리 문제에 가장 가까운 태스크를 골라 기준선 삼기
  - 절「한국어 모델 생태계」 — 소절: KoBERT → KLUE-BERT·KLUE-RoBERTa → KoELECTRA로 오면서 무엇이 달라졌나 / HyperCLOVA X·EXAONE·SOLAR를 API인가 오픈 웨이트인가로 가르기 / 금융·의료처럼 도메인 사전학습(CPT)까지 갈 기준과 그 비용
  - 절「토크나이저가 먹는 비용」 — 분절률의 정의·측정 절차와 「문맥 창·비용·표현 흐림 셋이 함께 나빠지는 구조」는 `nlp-multilingual-transfer`가 통째로 맡는다. 여기서는 한국어 쪽 실측과 선택지만 다루고 그 글을 가리킨다. 소절: `/articles/cost-korean-token-tax`가 토크나이저 아홉 개를 실제로 재서 얻은 자/토큰 0.89~2.67(3.00배)을 가져와 문맥 창과 청구서로 환산하기 — 어휘 크기와의 상관이 0.078이라 「어휘가 크면 낫다」가 안 통한다는 것까지 / 형태소 분석기가 세는 토큰과 서브워드 토크나이저가 세는 토큰이 다른 단위라 한 표에 나란히 놓으면 안 되는 자리 / 한국어를 따로 학습한 모델과 아닌 모델을 골랐을 때 갈리는 증상과 그때의 선택지(모델 교체·어휘 확장·프롬프트 압축)
  - 절「데이터를 구하는 자리」 — 소절: KLUE·KorQuAD·NSMC·AI Hub·모두의 말뭉치가 각각 덮는 범위 — 원고 241행 표의 「NSMC (네이버 쇼핑)」은 틀렸다. NSMC는 Naver Sentiment Movie Corpus, 네이버 **영화** 리뷰 20만 건이므로 「NSMC (네이버 영화 리뷰)」로 고친다 / 라이선스와 재배포 조건을 쓰기 전에 확인하는 이유 / 우리 도메인 데이터 수천 건이 공개 데이터 수십만 건을 이기는 지점

- `rl-rlhf-deep` (지금 2,196자 · 절 7)
  - 보상 모델 학습 — 선호 쌍 데이터와 Bradley-Terry 손실, 라벨러 일치도가 낮을 때 무엇이 망가지는가
  - KL 계수 β 튜닝 — 크면 안 움직이고 작으면 무너지는 구간, 적응형 KL이 하는 일
  - 보상 해킹 실례 — 길이 편향, 특정 문구 반복, 서식 남용과 각각의 탐지·완화
  - RLHF 학습 인프라 — 정책·참조·보상·가치 네 모델의 메모리 예산과 롤아웃 비용
  - DPO 계열 비교 — DPO·IPO·KTO가 각각 무엇을 바꾸는가, 보상 모델 없이도 되는 조건
  - GRPO — 가치 네트워크를 그룹 평균으로 대신하는 방식과 추론 모델 학습에서의 자리
  - 평가 — 승률의 길이 편향 보정, 보상 점수와 사람 평가가 갈리는 지점

- `nlp-sentiment-analysis` (지금 2,274자 · 절 8)
  - 절「감성 분석의 네 층위」 — 소절: 극성·강도·감정·속성이 각각 요구하는 라벨의 모양 / 별점 1~5를 회귀로 풀 때와 분류로 풀 때 손실과 지표가 달라지는 자리 / 중립 클래스를 두는 순간 라벨러 간 일치도가 떨어지는 이유
  - 절「사전 기반이 남아 있는 이유」 — 소절: VADER가 대문자·느낌표·강조어·부정어를 점수에 반영하는 방식 / KNU 감성사전 약 14,000어휘에 -2~+2를 매긴 구조와 도메인을 옮길 때의 한계 / 학습 데이터 0건에서 하루 만에 기준선을 세우는 값
  - 절「부정과 이중 부정」 — 소절: 「나쁘지 않다」가 사전 기반에서 뒤집히는 자리 / 부정어를 불용어 목록에서 반드시 빼야 하는 이유 / 부정이 미치는 범위가 어디까지인지를 규칙으로 잡을 때의 한계와 BERT가 이기는 지점
  - 절「BERT 파인튜닝의 실제」 — 소절: NSMC 15만 건으로 기준선을 세우는 절차와 도달하는 정확도 대역 / 리뷰 길이 분포를 보고 `max_length`를 정하는 것이 성능에 미치는 몫 / 영화 리뷰로 학습한 모델을 쇼핑·상담에 그대로 썼을 때 떨어지는 폭과 대응
  - 절「속성별로 나눠 보기」 — 소절: 속성 추출과 속성별 극성 판정 두 단계로 나뉘는 구조 / 「배터리는 좋은데 카메라가 별로」에서 속성 경계가 겹칠 때의 처리 / 속성 목록을 미리 고정할 때와 열어 둘 때의 운영 차이
  - 절「라벨이 흔들리는 자리」 — 소절: 풍자·반어는 사람도 못 맞히므로 그 비율이 곧 정확도의 상한이 되는 것 / 「부작용이 심각하다」처럼 도메인이 극성을 뒤집는 예 / 「그냥 그래요」·「뭐 나쁘지는 않아요」처럼 문화가 정하는 완곡 부정
  - 절「점수를 쓰는 자리」 — 소절: 건별 판정을 쓸 것인가 집계 추세를 쓸 것인가가 필요한 정확도를 정한다 / 신뢰도가 낮은 건을 사람에게 넘기는 선 긋기 / 감성 지표만 보고 제품 판단을 내릴 때 생기는 착시(표본 편향·리뷰 유입 시점)

- `cv-pose-estimation` (지금 2,733자 · 절 6)
  - 절「점 하나가 담는 것」 — 소절: 열일곱 점의 구성(얼굴 5·팔 6·다리 6)과 손발까지 세는 더 촘촘한 규격 / 값이 좌표 둘이 아니라 신뢰도를 포함한 셋인 이유 / 가려진 관절에도 좌표가 나오므로 신뢰도 거르기가 코드의 첫 줄이 되는 것 / 점이 없는 프레임을 버릴지 이어 쓸지 보류할지 미리 정해 두기
  - 절「두 갈래 경로」 — 소절: 사람부터 찾는 경로가 잘라 키우기 덕에 정확하지만 비용이 사람 수에 비례하는 구조 / 관절부터 찾는 경로에서 겹쳐 선 두 사람의 팔이 바뀌는 자리 / 앞 경로를 고르면 검출기의 오류가 그대로 상속된다는 사실
  - 절「히트맵과 좌표 회귀」 — 소절: 관절마다 지도 한 장을 드는 비용과 숫자 둘만 내는 가벼움 / 격자 해상도가 정밀도의 상한이 되므로 소수점 보정이 필수인 계산 / 두 봉우리가 비슷한 높이일 때 그 애매함을 후처리에서 쓰는 법
  - 절「관절 각도로 바꾸기」 — 소절: 세 점으로 각도를 재는 계산과 화면 좌표라서 생기는 왜곡 / 어깨 폭 같은 기준으로 정규화해야 카메라 거리에 안 흔들리는 이유 / 각도 시계열에서 동작 한 번을 끊는 규칙(임계값 통과·최소 지속 프레임)
  - 절「3D 좌표를 얻는 세 길」 — 소절: 끌어올리기·다시점 삼각측량·깊이 센서 셋의 설치 비용과 정확도 / 3D를 얻어도 절대 크기는 여전히 안 정해진다는 것 / 키나 바닥면 같은 기준값을 밖에서 넣어 크기를 정하는 절차
  - 절「시간축에서 다듬기」 — 소절: 정지한 사람인데도 좌표가 몇 픽셀씩 흔들리는 이유 / 각도를 미분해 속도를 재는 순간 그 떨림이 증폭되는 계산 / 평활화 세기와 지연의 맞바꿈 — 빠른 동작을 잡아야 할 때의 상한
  - 절「실무에서 튀는 자리」 — 소절: 뒤돌아선 사람의 좌우 뒤집힘과 앞뒤 프레임 일관성으로 잡기 / 검출 상자를 사방 10~25% 넓혀 자르지 않으면 뻗은 팔다리가 잘리는 것 / 관절 열일곱을 평균 내는 표준 지표가 「딱 하나 중요한 관절」의 오류를 감추는 구조

- `rl-basics` (지금 2,741자 · 절 8)
  - MDP를 끝까지 — 상태 전이 확률과 보상 함수의 정의, 정책 평가와 정책 개선의 순환
  - 가치 함수 둘 — V(s)와 Q(s,a)의 관계, Bellman 기대 방정식과 최적 방정식의 차이
  - 모델을 알 때 먼저 풀어 보기 — 정책 반복과 가치 반복을 FrozenLake에서 손으로 돌리기
  - 모델 프리로 넘어가는 이유 — 몬테카를로 평가와 TD(0)의 차이, 부트스트래핑이란 무엇인가
  - 할인율 γ가 정하는 실질 지평선 1/(1−γ) 계산과 과제별 권장값
  - 탐험 전략 비교 — ε-greedy 감쇠 스케줄, 볼츠만(소프트맥스), UCB가 각각 나은 상황
  - 온-정책과 오프-정책 — SARSA와 Q-러닝을 절벽 걷기 한 환경에서 나란히 돌려 차이 보이기

- `nlp-named-entity-recognition` (지금 2,836자 · 절 7)
  - 절「스팬을 맞힌다는 것」 — 소절: 출력이 레이블 하나가 아니라 시작·끝·유형 셋인 구조 / 경계가 한 토큰만 어긋나도 그 개체가 통째로 오답인 채점 방식 / 유형을 몇 개로 둘지가 라벨링 비용과 일치도를 동시에 정하는 자리
  - 절「BIO와 BIOES」 — 소절: B·I·O 세 기호로 스팬 문제를 시퀀스 레이블링으로 바꾸는 변환 / BIOES가 경계 정보를 늘리는 대신 레이블 공간을 키우는 맞바꿈 / O 뒤에 I-가 오는 것 같은 불가능한 시퀀스와 CRF 층으로 막는 법
  - 절「서브워드와 라벨 정렬」 — 소절: 「서울대학교」가 `["서울","##대","##학","##교"]`로 쪼개질 때 첫 조각에만 라벨을 주는 관례 / 나머지를 -100으로 손실에서 빼는 자리와 그걸 빼먹었을 때의 증상 / `aggregation_strategy`의 none·simple·first·average가 합치는 방식의 차이
  - 절「스팬 단위 채점」 — 소절: 토큰 정확도 97%와 스팬 F1 80%가 함께 나오는 상황이 생기는 이유 / 유형별로 나눠 보면 DATE는 높고 MISC는 낮은 것과 그 원인 / 부분 일치를 인정할 것인가를 쓰임새로 정하기
  - 절「한국어에서 새는 자리」 — 소절: 형태소 분석이 「삼성전자에」를 `["삼성","전자에"]`로 가르면 경계가 통째로 틀어지는 전파 / 「배」처럼 성씨·과일·선박이 겹치는 동형이의어를 문맥으로 가르기 / 신인·스타트업·새 지명 같은 미등장 개체에서 떨어지는 폭
  - 절「사전과 모델을 겹치기」 — 소절: 부서명·제품 코드처럼 목록이 확정된 유형은 사전 매칭이 더 정확한 이유 / 사전과 모델이 다르게 말할 때의 우선순위 규칙 / 사전 갱신 주기와 모델 재학습 주기를 따로 두는 운영
  - 절「뽑은 뒤에 하는 일」 — 소절: 「삼성전자」·「삼성전자㈜」·「삼성」을 하나로 묶는 개체 연결 / 관계 추출·이벤트 추출의 앞 단계로 놓일 때 요구되는 정확도의 상승 / 원문 위치를 함께 남겨 사람이 검수할 화면을 만드는 구성

- `nlp-pos-tagging` (지금 2,841자 · 절 8)
  - 절「문맥이 정하는 품사」 — 소절: 영어 flies와 한국어 「이」처럼 같은 표기에 품사가 여럿인 자리 / 시퀀스 레이블링으로 두면 앞뒤 토큰이 후보를 좁히는 구조 / 어절과 형태소가 1:N인 한국어에서 「토큰」의 뜻이 영어와 달라지는 것
  - 절「태그셋의 설계 차이」 — 소절: Penn Treebank 36개가 굴절을 태그로 나눈 방식 / 세종 태그셋의 결합 태그(「달린다」 = VV+EF)가 형태론을 담는 방식 / UD 17개 보편 태그가 언어 간 비교를 가능하게 하는 대가
  - 절「HMM과 CRF의 구조」 — 소절: 전이 확률과 방출 확률 둘로 문제를 나누는 구조 / 비터비가 지수 개 경로를 선형 시간으로 줄이는 방식 / bigram의 한계와 CRF의 특징 함수가 넓히는 문맥 — 그리고 미등장 단어 처리의 차이
  - 절「BERT 태거의 실제」 — 소절: 양방향 어텐션이 「배」·「사과」 같은 자리에서 실제로 이기는 예 / 형태소 단위 정답과 서브워드 토큰을 정렬하는 문제 / 정확도 97%대에서 남은 3%가 어디에 몰려 있는가
  - 절「분석기를 고르는 기준」 — 소절: Okt·Komoran·Mecab의 속도와 구어체 강건성이 갈리는 자리 / 같은 문장을 셋에 넣어 결과가 다른 것을 직접 확인하는 절차 / Komoran의 사용자 사전으로 도메인 용어를 붙이는 순서
  - 절「태깅 결과를 쓰는 자리」 — 소절: 불용어 목록 대신 품사(조사·어미·접사)로 거르는 편이 정확한 이유 / 명사만 뽑아 TF-IDF에 넣었을 때 달라지는 키워드 / 검색 색인에서 표제어를 통일해 재현율을 올리는 자리
  - 절「한국어에서 남는 문제」 — 소절: 「먹었겠죠」가 VV+EP+EP+EF로 갈리는 어절-형태소 불일치 / 띄어쓰기 오류와 신조어(「힙하다」)가 미등록으로 떨어질 때 / 분석기를 바꾸면 하류 지표가 함께 움직이므로 실험 중에는 고정해 두는 이유

- `cv-depth-estimation` (지금 2,856자 · 절 7)
  - 절「크기와 거리의 얽힘」 — 소절: 작은 것이 가까이 있는 배치와 큰 것이 멀리 있는 배치가 같은 그림을 만드는 것 / 사람이 아는 물건 크기로 그 모호함을 넘는 방식과 모델도 같은 일을 한다는 것 / 처음 보는 크기의 물체만 있는 장면에서 크게 틀리는 이유
  - 절「세 등급의 출력」 — 소절: 순서·배율 미상·미터 단위가 각각 할 수 있는 일과 못 하는 일 / 범용 단안 모델의 기본이 가운데 등급인 이유 / 등급을 모른 채 값을 쓰면 오류 없이 조용히 틀리는 자리
  - 절「시차와 깊이의 역수」 — 소절: 가까운 곳을 촘촘히, 하늘을 0 가까이 담는 표현의 이점 / $$d = 1/(sp+t)$$ 를 뒤집을 때 먼 곳의 오차가 폭발하는 계산(0.01 → 0.005면 두 배) / 가까운 곳은 값으로, 먼 곳은 순서로만 믿는 태도
  - 절「미터로 올리기」 — 소절: 미지수가 $$s$$·$$t$$ 둘이라 실제 거리를 아는 점이 최소 둘 필요한 이유 / 바닥면·규격 물체·거리계 몇 점 중 무엇을 기준으로 삼을지 / 최소제곱으로 맞추는 절차와 분모가 0에 가까워지는 자리를 잘라 두기
  - 절「다른 수단과 견주기」 — 소절: 단안·스테레오·구조광·라이다·다시점의 강한 곳과 약한 곳 / 성긴 라이다 점으로 촘촘한 단안 지도의 배율을 맞추는 조합이 잘 맞는 이유 / 실내 근거리와 실외 원거리에서 답이 갈리는 지점
  - 절「점구름 변환의 함정」 — 소절: 깊이 지도와 초점거리로 3차원 점을 만드는 계산 / 초점거리를 틀리게 넣으면 장면이 늘어나거나 눌리는 모양 / 잘라 낸 사진·화면 캡처의 메타데이터를 못 믿는 자리
  - 절「자주 무너지는 자리」 — 소절: 하늘·거울·유리는 틀린 게 아니라 정의되지 않은 값이라 마스크로 빼는 것 / 경계에서 앞뒤 값이 섞여 생기는 날아다니는 픽셀과 타일 이음매 / 프레임마다 배율이 출렁이는 영상, 그리고 평가 전에 배율·치우침을 맞추는 규칙

- `nlp-text-preprocessing` (지금 2,870자 · 절 9)
  - 절「전처리가 바꾸는 것」 — 소절: 어휘 폭발과 희소성이 TF-IDF 계열 성능에 미치는 실제 몫 / 서브워드 토크나이저를 쓰는 모델에서도 전처리가 남는 자리(검색 색인·도메인 사전학습·규칙 기반 필터) / 세게 걸어서 오히려 나빠지는 예 — 감성 분석의 이모지와 느낌표
  - 절「정제에서 버릴 것과 남길 것」 — 소절: HTML 태그·URL·이메일을 지울지 자리표시자로 바꿀지 / 이모지와 「ㅋㅋㅋㅋ」를 태스크별로 가르는 기준 / 중복 문서 제거를 맨 앞에 두는 이유와 그때 쓰는 근사 중복 판정
  - 절「토큰화의 갈림」 — 소절: 영어에서 공백만으로는 안 되는 자리(don't → do/n't, U.S.A.) / 한국어가 어절이 아니라 형태소를 단위로 삼아야 하는 이유 / 서브워드 토크나이저를 쓸 때 앞단 전처리를 오히려 줄여야 하는 자리
  - 절「정규화의 세 축」 — 소절: 소문자 통일이 고유명사(Apple/apple)와 약어에서 손해를 보는 경우 / 유니코드 NFC·NFD가 한글에서 자모를 분리해 만드는 실제 버그 / 「최고오오오오」 같은 반복 문자와 숫자·단위·날짜를 자리표시자로 바꿀 때의 기준
  - 절「불용어와 어간」 — 소절: 「not good」의 not을 지우면 판정이 뒤집히므로 부정어를 목록에서 빼는 것 / 어간 추출과 원형 복원의 속도·정확도 맞바꿈 / 한국어에는 표준 불용어 목록이 없어 문서 빈도로 직접 뽑는 절차
  - 절「파이프라인을 고정하기」 — 소절: 학습과 추론이 다른 전처리를 쓰면 조용히 성능이 떨어지는 자리 / 단계마다 남는 토큰 수를 세어 어디서 얼마나 지웠는지 확인하기 / 전처리 버전을 모델 체크포인트와 함께 기록해 두는 이유
  - 절「도메인별 규칙 차이」 — 소절: 의료·법률의 약어 사전(고혈압/HTN)과 전문 용어 처리 / 한 문장에 한국어와 영어가 섞일 때의 처리 순서 / 오류 50건을 읽고 규칙을 고치는 반복이 실제로 도는 모양

- `multimodal-spatial-grounding` (지금 2,926자 · 절 6)
  - 절「세 갈래 작업」 — 소절: 지시 표현 이해·구절 그라운딩·영역 설명이 요구하는 답의 모양 / 정답이 하나로 좁혀지는 표현과 그렇지 않은 표현의 채점 차이 / 구절 그라운딩에서 새로 생기는 빠뜨림과 중복
  - 절「좌표를 내는 세 방식」 — 소절: 좌표 전용 토큰에서 구간 수가 곧 해상도 상한이 되는 구조 / 숫자를 평범한 텍스트로 낼 때의 자릿수 비용과 형식이 깨질 여지 / 별도 검출 헤드가 작은 대상에서 이기는 대신 학습이 복잡해지는 것
  - 절「형식을 못 박는 지시」 — 소절: 좌상단·우하단인가 좌상단·폭높이인가를 지시에 적는 자리 / 픽셀인지 0~1인지 0~1000인지 안 적으면 모델이 학습 때 가장 자주 본 형식을 낸다는 것 / 스키마로 받아 파싱 실패를 없애고 부재 응답까지 형식에 넣기
  - 절「레터박스와 역변환」 — 소절: 비율을 지킨 축소와 여백 채우기가 좌표를 어긋내는 계산 / 정사각형 샘플에서만 멀쩡하다가 가로 긴 사진에서 어긋나는 이유 / 원본 크기를 좌표와 함께 저장하고 리사이즈를 한 곳에만 두는 규칙
  - 절「자주 밟는 실패」 — 소절: 대상이 없어도 무언가를 하나 골라 답하는 것과 「없으면 없다고」를 지시에 넣기 / 「왼쪽에서 두 번째」 같은 서수 표현에서 정확도가 떨어지는 자리 / 「왼쪽」의 기준이 보는 사람인지 사진 속 인물인지 명시하기
  - 절「작은 대상 다루기」 — 소절: 입력이 정사각형으로 줄면서 픽셀이 사라지는 구조 / 겹치게 타일로 나눠 묻고 좌표를 원본 기준으로 되돌리는 두 번의 변환 / 타일 경계에 걸친 대상의 중복 상자를 정리하는 규칙
  - 절「VLM과 전용 검출기」 — 소절: 찾을 대상을 미리 나열할 수 있는가로 갈리는 기준 / 「금액이 적힌 칸」처럼 클래스 목록으로 못 적는 요청에서 VLM이 이기는 자리 / 검출기가 후보 상자를 내고 VLM은 번호만 고르게 해 좌표 사고를 없애는 조합

- `recsys-deep-learning` (지금 2,963자 · 절 7)
  - 절「임베딩이 푸는 문제」 — 소절: 사용자 100만 명의 원-핫을 64~256차원으로 줄이는 이유 / 임베딩 테이블이 파라미터의 대부분을 차지하는 구조와 그 메모리 계산 / ID가 계속 늘어나는 서비스에서 해싱·최소 등장 횟수로 어휘를 자르는 절차
  - 절「내적을 대신하는 신경망」 — 소절: GMF가 원소별 곱에 가중치를 얹어 행렬 분해를 일반화하는 자리 / MLP가 연결 뒤 ReLU로 잡는 비선형 상호작용 / 둘을 합친 NCF가 실제로 이기는 데이터 조건과 안 이기는 조건
  - 절「기억과 일반화의 분업」 — 소절: Wide 쪽의 교차 특성(「언어=한국어 AND 설치앱=카카오톡」)이 기억하는 것 / Deep 쪽이 본 적 없는 조합으로 넓히는 것 / 두 쪽을 함께 학습시킬 때 옵티마이저를 나눠 쓰는 이유
  - 절「행동별 가중과 어텐션」 — 소절: 과거 클릭을 동등하게 평균 내면 「요리책 구매」가 운동화 추천을 흐리는 자리 / 후보 아이템을 질의로 삼아 관련 행동만 살리는 구조 / 이력이 길 때 최근 N개로 자르는 것과 무작위 표본을 섞는 것의 차이
  - 절「순서를 읽는 모델」 — 소절: 단방향 어텐션으로 다음 아이템을 예측하는 쪽과 중간을 가려 맞히는 쪽이 갈리는 자리 / 위치 임베딩이 「몇 번째」를 담는지 「언제」를 담는지 / 단기 관심과 장기 취향을 한 모델에서 함께 다루는 구성
  - 절「학습을 정하는 것들」 — 소절: 네거티브 샘플링을 균등으로 할 때와 인기도 기반으로 할 때의 차이 / 손실을 점별·쌍별·목록별 중 무엇으로 둘지 / 시간 기준으로 나눠야 미래 정보 누출이 안 생기는 이유
  - 절「딥러닝으로 갈 시점」 — 소절: 상호작용 수천만 건이 기준선으로 자주 언급되는 이유 / 학습·추론 비용 증가와 정확도 이득을 나란히 놓고 재기 / 부가 특성이 많을 때 행렬 분해가 못 하는 일

- `recsys-matrix-factorization` (지금 3,011자 · 절 7)
  - 절「잠재 요인이라는 가정」 — 소절: 사용자와 아이템을 같은 $$k$$ 차원 공간에 놓고 내적으로 평점을 근사한다는 것 / 요인에 이름이 없고 사후 해석만 가능한 이유 / $$k$$ 를 10에서 200까지 움직일 때 언더핏과 과적합이 갈리는 자리
  - 절「희소 행렬과 관찰된 값」 — 소절: 99% 이상 비어 있는 행렬에서 빈칸을 0으로 채우면 「관심 없음」과 「평가 안 함」이 섞이는 왜곡 / 관찰된 평점에만 손실을 거는 FunkSVD의 발상 / 정규화 계수 $$\lambda$$ 가 평점 몇 개뿐인 사용자를 붙잡는 역할
  - 절「바이어스 항의 몫」 — 소절: $$\mu + b_u + b_i$$ 만으로 얻는 RMSE와 잠재 요인이 더하는 몫 / 짜게 주는 사람과 후하게 주는 사람을 빼고 나서야 요인이 의미를 갖는 이유 / 편향 항의 정규화를 따로 두는 관례
  - 절「SGD와 ALS」 — 소절: 한 샘플씩 갱신하는 쪽과 한쪽을 고정해 최소제곱으로 한꺼번에 푸는 쪽 / ALS가 사용자·아이템마다 독립이라 완전 병렬이 되는 구조 / 손댈 손잡이가 학습률·에폭인가 반복 횟수인가로 달라지는 것
  - 절「암묵적 피드백」 — 소절: 클릭·재생 횟수를 신뢰도 가중치로 바꾸는 변환 / 안 본 것이 「싫다」가 아니라 「모른다」인 자리와 그것을 손실에 반영하는 법 / 쌍별 선호를 학습하는 BPR이 전자상거래에서 강한 이유
  - 절「지표를 고르는 자리」 — 소절: RMSE 0.87이 무작위 1.5~1.7보다 낫다는 것과 그것이 목록 품질을 보장하지 않는 구조 / Precision@K·NDCG@K가 순위를 반영하는 방식 / 시간 기준 분할과 무작위 분할이 다른 답을 주는 이유
  - 절「행렬 분해가 남은 자리」 — 소절: 데이터가 적을 때 딥러닝 추천보다 나은 지점 / 후보 생성 단계의 기준선으로 지금도 쓰이는 이유와 Spark MLlib·implicit의 자리 / 콜드 스타트와 인기 편향 — 행렬 분해만으로는 못 푸는 둘

- `recsys-collaborative-filtering` (지금 3,090자 · 절 7)
  - 절「사용자-아이템 행렬」 — 소절: 행·열·셀이 담는 것과 평점 대 암시적 피드백의 차이 / 희소도가 1% 미만이라는 값이 알고리즘 선택에 미치는 영향 / 미평가를 0으로 두고 코사인을 재면 왜곡되는 자리와 마스킹
  - 절「유사도를 고르는 기준」 — 소절: 코사인이 크기를 무시하고 방향만 보는 것 / 피어슨이 각자의 평균을 빼서 평점 기준 차이를 없애는 계산 / 공통 평가 아이템이 두세 개뿐일 때 유사도가 1로 튀는 것과 최소 공통 개수 조건
  - 절「사용자 기준과 아이템 기준」 — 소절: 두 예측 공식이 각각 무엇을 가중 평균하는가 / 아이템 수가 적고 덜 변하는 서비스에서 아이템 기준이 실용적인 이유 / 유사도 행렬을 미리 계산해 두는 갱신 주기와 캐시
  - 절「이웃 수와 가중치」 — 소절: $$K$$ 를 키우면 안정되고 줄이면 개인화되는 맞바꿈 / 음수 유사도를 버릴 것인가 그대로 쓸 것인가 / 예측값을 평점 범위로 자를 때 생기는 편향과 그 확인법
  - 절「메모리 기반의 한계」 — 소절: 사용자 1억·아이템 100만에서 유사도 행렬 크기를 직접 계산해 보기 / 근사 최근접 이웃으로 후보를 먼저 좁히는 구성 / 실시간 갱신이 필요한 자리와 하루 한 번 배치로 충분한 자리
  - 절「CF가 못 하는 것」 — 소절: 신규 사용자와 신규 아이템, 성격이 다른 두 콜드 스타트 / 인기 편향이 추천→클릭→학습으로 자기 강화되는 과정 / 롱테일 아이템이 유사도 계산에서 배제되는 구조
  - 절「도구를 고르는 자리」 — 소절: Surprise·implicit·LightFM이 각각 잘 맞는 데이터 모양(명시 평점·암묵 피드백·메타데이터 결합) / 오프라인 지표와 온라인 CTR이 갈리는 이유 / CF를 후보 생성에만 두고 랭킹은 따로 두는 구성

- `cv-promptable-segmentation` (지금 3,149자 · 절 7)
  - 절「클래스 목록을 버린 자리」 — 소절: 의미·인스턴스·파놉틱 분할이 공유하던 「미리 나열해 두었다」는 전제 / 이름을 안 붙이기로 하는 순간 라벨링 비용이 내려가고 데이터가 늘어나는 구조 / 목록으로는 못 적는 실제 요청의 모양(「저 사람이 든 것」)
  - 절「인코더와 디코더의 분업」 — 소절: 이미지 인코더가 프롬프트와 무관하다는 성질 / 임베딩을 한 번 만들고 프롬프트만 바꿔 수십 번 묻는 흐름이 실시간 편집을 가능하게 하는 것 / 서비스로 감쌀 때 임베딩 캐시의 키 설계와 수명
  - 절「점 하나의 모호함」 — 소절: 주머니인가 셔츠인가 사람인가 — 그림만으로 못 가르는 자리 / 크기 대역이 다른 후보 셋과 각각에 붙는 예상 품질 점수 / 점수 최고를 고르는 대신 넓이 대역으로 먼저 거르는 이유
  - 절「프롬프트를 겹치기」 — 소절: 양수 점·음수 점·상자·거친 마스크·텍스트가 각각 맞는 자리 / 옆 대상까지 먹은 마스크를 음수 점 하나로 고치는 값 / 자동 파이프라인에서 점 하나 대신 안쪽에 여러 개를 흩뿌려 안정시키기
  - 절「텍스트로 지시하기」 — 소절: 개방 어휘 분할을 한 모델이 통째로 하는 경우가 드문 이유 / 검출기·그라운딩 모델에게 상자를 받아 분할기에 넘기는 두 단계 / 중간에 상자가 남아 「잘못 잡았나 경계가 지저분한가」를 가릴 수 있는 이점
  - 절「영상으로 넓힐 때」 — 소절: 앞 프레임의 임베딩과 마스크를 일정 개수 들고 참조하는 메모리 구조 / 가려짐·표류·메모리 예산 셋이 새로 생기는 자리 / 최근 몇 프레임과 첫 지시 프레임을 함께 두는 절충, 그리고 사람이 점을 찍어 바로잡는 경로
  - 절「마스크를 쓸 수 있게 만들기」 — 소절: 저해상도 격자에서 나와 확대되므로 머리카락·나뭇가지가 뭉개지는 것 / 구멍과 떨어진 조각을 연결 성분·최소 넓이로 정리하는 후처리 / IoU가 넓은 대상의 가장자리 품질을 거의 못 잡는 구조와 경계 전용 지표

- `multimodal-video-understanding` (지금 3,230자 · 절 7)
  - 절「토큰 예산과 시간 해상도」 — 소절: 프레임 한 장이 250~800토큰이라는 값에서 넣을 수 있는 장수를 계산하기 / 영상 길이 ÷ 프레임 수가 곧 간격이 되는 식 / 10분 영상에 32장이면 간격 18.75초이고 3초짜리 사건이 잡힐 확률이 16%라는 계산
  - 절「샘플링 방식 고르기」 — 소절: 편집된 콘텐츠에서 컷이 바뀌는 자리가 곧 내용이 바뀌는 자리인 이유 / 고정 카메라·회의 녹화에서 전환 기반이 무너지고 움직임 가중이 맞는 자리 / 값싼 탐지기로 후보 구간을 먼저 좁혀 무거운 모델 호출을 수십 분의 일로 줄이기
  - 절「프레임에 시각 붙이기」 — 소절: 순서만 주면 모델이 「몇 분」을 지어내는 자리 / 시각을 텍스트로 함께 넣는 것과 화면에 타임코드를 그리는 것의 차이 / 「이 프레임들은 18.75초 간격이며 사이는 보지 못했다」를 알려 주면 단정이 줄어드는 것
  - 절「자막에서 구간 좁히기」 — 소절: 1분 말 150토큰과 프레임 한 장 400토큰을 나란히 놓고 보기 / 자막에 이미 시각이 붙어 있으므로 관심 구간을 먼저 찾고 그 앞뒤만 프레임으로 뽑기 / 말이 없는 영상(CCTV·화면 녹화)에서는 이 경로가 아예 없다는 것
  - 절「두 번 훑는 구조」 — 소절: 30~60초 토막마다 시각이 붙은 한 줄 요약으로 텍스트 색인을 만들기 / 질문을 받아 후보 토막을 두세 개로 좁힌 뒤 초당 두 장으로 다시 읽기 / 한 시간 영상에서 7,200장과 600장을 비교해 비용 차이를 세어 보기
  - 절「토막 경계와 이어짐」 — 소절: 사건이 토막 경계에 걸쳐 잘릴 때 겹치게 자르는 대응 / 앞 토막의 맥락을 못 받아 요약의 뜻이 뒤집히는 자리 / 토막 길이 30초와 60초가 만드는 색인 길이와 2차 정확도의 맞바꿈
  - 절「근거 시각까지 재기」 — 소절: 정답 구간과 예측 구간의 겹침 비율을 임계값 0.5에서 가르는 채점 / 프레임 8·16·32장으로 정확도 곡선을 그려 평평해지는 지점을 찾기 / 프레임을 다 빼고 자막만 준 점수와의 차이가 실제로 영상을 봐서 얻은 몫

- `recsys-content-based` (지금 3,375자 · 절 7)
  - 절「아이템만으로 추천하기」 — 소절: 다른 사용자의 행동이 하나도 없어도 도는 구조 / CF와 갈리는 네 축 — 추천 근거·필요 데이터·콜드 스타트·설명 가능성 / 신규 아이템이 등록 즉시 추천에 들어가는 것이 값이 되는 서비스
  - 절「아이템을 숫자로 만들기」 — 소절: IDF가 모든 영화에 붙은 「SF」의 무게를 깎고 「크리스토퍼 놀란」을 살리는 계산 / 원-핫이 카테고리 수에 따라 희소해지는 자리 / 문장 임베딩이 「우주 탐험」과 「외계 행성 여행」을 잇는 지점
  - 절「특성을 섞는 법」 — 소절: 줄거리·장르·감독을 한 문서로 합칠 때와 필드마다 벡터를 두고 가중합할 때 / 필드 가중치를 정할 근거를 어디서 얻는가(오프라인 지표 대 사람 판단) / 메타데이터가 비어 있는 아이템을 다루는 규칙
  - 절「사용자 프로파일 세우기」 — 소절: 좋아한 아이템 벡터의 가중 평균이 담는 것과 잃는 것 / 취향이 둘 이상일 때 평균 하나가 「아무것도 아닌 가운데 점」이 되는 자리와 군집으로 나누기 / 반감기를 도메인에 맞추는 시간 감쇠 — 뉴스 1~3일, 영화·음악 30~90일
  - 절「비슷한 것만 나오는 문제」 — 소절: 오버-스페셜라이제이션이 생기는 구조와 필터 버블 / 추천 목록 안의 다양성을 재는 법과 재랭킹으로 벌리기 / 새로운 것을 섞는 비율을 정하는 기준과 그때 감수하는 클릭률 손실
  - 절「설명할 수 있다는 값」 — 소절: 「SF이고 우주 배경이라 추천」처럼 근거 문장을 만드는 절차 / 어느 특성이 점수를 만들었는지 되짚는 계산 / 설명이 신뢰에 미치는 몫과 틀린 설명이 만드는 역효과
  - 절「CF와 겹쳐 쓰기」 — 소절: 점수를 가중 합산하는 방식과 조건에 따라 전환하는 방식 / 사용자 이력 길이로 $$\alpha$$ 를 움직이는 규칙 / 여러 신호를 랭킹 모델 하나로 합치고 뒤에 다양성 재랭킹을 두는 구성

- `multimodal-any-to-any` (지금 3,408자 · 절 7)
  - 절「입구만 열린 모델들」 — 소절: text-to-text·any-to-text·text-to-image·any-to-any 네 구조를 나란히 놓기 / 흔히 「멀티모달 모델」이라 부르는 것 대부분이 실제로는 any-to-text인 자리 / 출구가 텍스트뿐이면 그림을 만들 때 결국 조립형으로 되돌아가는 이유
  - 절「조립형이 포기하는 셋」 — 소절: 소리가 글자가 되는 순간 억양·웃음·머뭇거림이 사라지는 것 / 단계가 겹치지 않아 더해지는 지연과 문장 경계에서만 겹칠 수 있다는 한계 / 사진을 캡션으로 떨어뜨리면 「왼쪽 사람이 든 것」을 되물을 수 없는 자리
  - 절「토큰으로 바꾸는 양자화」 — 소절: 자기회귀 모델이 아는 일이 「다음 번호 고르기」 하나라는 사실 / 이미지를 격자 조각으로 잘라 코드북에서 가장 가까운 항목의 번호로 바꾸는 벡터 양자화 / 오디오의 잔차 양자화 — 겹 수로 음질과 토큰 수를 맞바꾸는 자리
  - 절「출력 쪽의 비대칭」 — 소절: 입력은 연속 벡터를 프로젝터 하나로 끼워 넣으면 되는 이유 / 출력에는 고를 후보 목록이 있어야 한다는 제약 / 이산 토큰 자기회귀와 백본+전용 디코더를 학습·품질·지연으로 견주기
  - 절「경계 표시와 섞어 쓰기」 — 소절: 한 줄 안에서 모달리티가 바뀌는 자리를 표시 토큰으로 가르기 / 모달리티마다 다른 출력 방식을 고르는 실제 구성(오디오는 토큰, 이미지는 디코더) / 텍스트 출력 경로만은 손대지 않는 이유
  - 절「통합형이 치르는 값」 — 소절: 한 모델이 셋을 다 하면 각각에 쓸 용량이 나뉘어 생기는 품질 천장 / 중간 텍스트가 없어 금칙어 검사·사람 검수를 끼울 자리가 사라지는 것 / 토큰 예산이 열 배로 뛰어 몇 턴 만에 창이 차는 구조와 이전 그림을 요약으로 대체하는 관리
  - 절「고르는 기준」 — 소절: 품질이 상품인가·검수가 필수인가·비언어 정보가 답을 바꾸는가를 위에서부터 묻기 / 조립형으로 먼저 만들고 병목이 「경계 자체」로 좁혀졌을 때 옮기는 순서 / 출력이 그림·소리로 갈릴 때 무엇을 정답으로 볼지부터 정해야 하는 평가

- `audio-music-generation` (지금 3,426자 · 절 7)
  - 절「신경망 코덱의 토큰화」 — 소절: 초당 44,100 샘플을 트랜스포머에 그대로 못 넣는 계산 / 신경망 코덱이 파형을 이산 토큰으로 압축하는 구조와 전통 코덱과의 차이 / 초당 50토큰에 코드북 넷이면 초당 200토큰이 되는 셈과 30초 곡의 총 토큰 수
  - 절「잔차 양자화의 겹」 — 소절: 첫 코드북이 큰 줄기를 잡고 뒤 겹이 오차를 메우는 방식 / 겹 수를 늘리면 음질과 토큰 수가 함께 오르는 맞바꿈 / 여러 겹을 어떤 순서로 예측할 것인가(동시·계층·지연 배치)
  - 절「텍스트로 조건 걸기」 — 소절: 텍스트 인코더가 낸 벡터를 크로스 어텐션으로 참조하는 흐름 / 장르·악기·분위기·템포를 나눠 적을 때와 뭉뚱그릴 때의 결과 차이 / 멜로디를 조건으로 함께 줄 때 무엇이 고정되고 무엇이 열리는가
  - 절「자기회귀와 확산」 — 소절: 토큰을 하나씩 잇는 쪽과 잠재 공간에서 노이즈를 깎는 쪽 / 생성 길이를 미리 지정할 수 있는지가 갈리는 자리 / 길이 상한 30초와 95초가 쓰임새(효과음·배경음악·완곡)를 가르는 것
  - 절「음악에만 있는 어려움」 — 소절: 박자와 조성이 수십 초 뒤까지 유지되어야 하는 장거리 일관성 / 도입·전개·후렴 같은 곡 구조가 자기회귀 생성에서 흐려지는 자리 / 보컬과 반주가 함께 나올 때 발음과 화성이 동시에 무너지는 것
  - 절「음악 생성의 지표」 — 소절: FAD가 임베딩 공간에서 실제 음악과의 분포 거리를 재는 방식 / CLAP 점수가 프롬프트 충실도만 재고 음악성은 못 재는 자리 / 사람 평가를 음질·텍스트 충실도·음악성 셋으로 나누는 관례와 그 비용
  - 절「쓰기 전에 볼 것」 — 소절: 학습 데이터 저작권을 둘러싼 분쟁이 진행 중이라는 현재 상태 / 모델·서비스마다 다른 상업적 사용 조건을 쓰기 전에 확인하는 자리(MusicGen의 CC-BY-NC 4.0) / 생성물의 권리 귀속이 아직 정해지지 않았다는 사실과 그때의 실무 대응

- `audio-speech-to-speech` (지금 3,482자 · 절 7)
  - 절「글자를 지나며 버려지는 것」 — 소절: 운율이 담는 것 — 억양·속도·쉼·떨림·웃음 / 「네, 알겠습니다」가 흔쾌한 수락인지 체념인지 전사에 안 남는 자리 / 화자 특성·문자로 안 적히는 말·앞 단계 오류의 누적까지 함께 잃는 것
  - 절「소리를 다루는 토큰」 — 소절: 뉴럴 코덱이 짧은 구간마다 사전에서 가장 가까운 항목의 번호를 남기는 구조 / 의미 토큰과 음향 토큰이 화자가 바뀔 때 각각 어떻게 반응하는가 / 둘을 갈라 놓은 덕에 「무슨 말」과 「누구 목소리」를 따로 정할 수 있는 것
  - 절「토큰 수와 비용」 — 소절: 음향 토큰이 한 겹으로는 파형을 못 되살려 여러 겹을 쌓는 이유 / 겹 수만큼 초당 토큰이 불어나는 계산 / 오디오 단가가 텍스트보다 비싸고 컨텍스트가 몇 턴에 차는 자리
  - 절「연결형과 직결형」 — 소절: 지연·운율·목소리·검수·용어 교정·원인 추적 여섯 축의 비교 / 나가기 전에 읽고 막을 텍스트가 있는지가 금융·의료 도입을 가르는 자리 / 직결형으로 내보내며 전사를 함께 받아 차단은 포기하고 기록만 챙기는 절충
  - 절「목소리 복제의 위험」 — 소절: 몇 초짜리 견본으로 그 사람 목소리를 만들 수 있다는 뜻 / 동의 확인·워터마크·합성음 고지·견본 보관 기간 넷을 기능보다 먼저 정하기 / 목소리 견본을 생체정보에 준해 다루는 보관·삭제 경로
  - 절「평가의 우회로」 — 소절: 다시 인식해 재면 채점용 모델의 오류가 점수에 섞이고 또렷한 목소리가 유리해지는 편향 / 화자 임베딩 유사도와 쉼·속도·억양 곡선 비교를 따로 두는 이유 / 뜻과 소리를 한 점수로 합치면 내용이 틀어진 모델이 좋아 보이는 자리
  - 절「고르는 기준」 — 소절: 규제받는 말과 고유명사 발음은 연결형, 지연·감정·여러 화자는 직결형 / 자료가 적거나 문자 표기가 없는 언어에서 직결형밖에 없는 자리 / 절충 구성으로 시작해 어느 축이 걸리는지 확인한 뒤 옮기는 순서

- `audio-tts` (지금 3,552자 · 절 8)
  - 절「TTS 파이프라인의 네 칸」 — 소절: 전처리·G2P·음향 모델·보코더 네 칸이 각각 하는 일 / 멜 스펙트로그램이 중간 표현으로 자리 잡은 이유 / 두 단계를 하나로 합친 구조가 없애는 것과 함께 없어지는 검사 지점
  - 절「한국어 G2P의 자리」 — 소절: 연음·경음화가 표기와 발음을 갈라 놓는 실제 예 / 숫자·단위·영문 약어를 읽는 규칙을 앞단에서 정하기(「3km」·「2026년」·「AI」) / G2P가 틀리면 뒤 단계가 아무리 좋아도 못 고치는 이유
  - 절「지속·피치·에너지」 — 소절: 음소마다 몇 프레임인지 예측해 길이를 맞추는 Length Regulator / 병렬 생성이 자기회귀 대비 수십 배 빠른 구조 / 세 값을 수치로 돌려 속도·높낮이·강세를 바꾸는 자리와 그 한계
  - 절「보코더가 정하는 음질」 — 소절: WaveNet 0.003×·WaveGlow 25×·HiFi-GAN 167×라는 실시간 배율과 MOS를 나란히 보기 / 판별기를 여러 스케일·주기로 두는 이유 / 음향 모델과 보코더의 학습 조건이 어긋날 때 생기는 잡음
  - 절「종단간 합성 구조」 — 소절: VAE·정규화 흐름·GAN 셋을 겹친 구성에서 각각이 맡는 역할 / 확률적 샘플링이 같은 문장을 매번 다르게 읽게 만드는 성질 / 중간 표현이 사라져 어디서 틀렸는지 보기 어려워지는 대가
  - 절「몇 초로 목소리를 옮기기」 — 소절: 화자 임베딩을 조건 벡터로 넣는 제로샷 복제의 흐름 / 견본의 길이·잡음·발화 내용이 결과를 바꾸는 폭 / 감정과 스타일을 자연어로 지시하는 방향과 그때 형식을 못 박는 법
  - 절「고르는 기준과 지표」 — 소절: 한국어 품질·복제 가능 여부·오픈소스 여부로 후보를 좁히는 표 / MOS 측정 비용과 UTMOS 같은 대리 지표를 함께 쓰는 관행 / 합성음을 다시 인식해 WER로 명료성을 재는 방법과 그 값이 못 잡는 것

- `cv-image-generation-controls` (지금 3,564자 · 절 8)
  - 절「손잡이의 네 층」 — 소절: 시드·샘플링·밖에서 주는 조건·모델 가중치가 파이프라인의 어느 자리에 끼어드는가 / 왼쪽이 싸고 거칠며 오른쪽이 정밀하고 비싼 구조 / 오른쪽부터 손대는 것이 가장 흔한 시간 낭비인 이유
  - 절「시드와 재현성」 — 소절: 시드가 다르면 나머지가 같아도 그림이 통째로 달라지는 것 / 같은 시드가 같은 그림을 주는 범위 — GPU 종류·라이브러리 버전·정밀도·배치 크기가 바뀌면 깨진다 / 시드 넷 × 값 셋 격자로 「설정이 나은가」와 「시드가 운이 좋았나」를 가르기
  - 절「CFG·steps·샘플러」 — 소절: 조건 예측과 무조건 예측의 차이를 $$s$$ 배 증폭하는 식과 $$s=1$$ 의 뜻 / 지나치면 채도가 튀고 윤곽이 뭉개지는 자리 / steps가 25~40에서 평평해지는 것, 샘플러를 바꾸면 알맞은 CFG 구간이 함께 옮겨 가는 얽힘, 증류 모델의 CFG 1~2
  - 절「네거티브 프롬프트」 — 소절: 무조건 예측 자리에 네거티브를 넣어 「그것에서 멀어지는」 방향으로 미는 원리 / CFG가 1이면 아무 일도 안 하는 이유 / 스무 개를 나열하는 것보다 실제로 나오는 문제 두세 개를 적는 편이 나은 자리
  - 절「밖에서 주는 조건」 — 소절: ControlNet·인페인팅 마스크·이미지투이미지·참조 어댑터·지역 프롬프트가 각각 고정하는 것 / 「무엇을 바꾸지 말아야 하는가」로 도구를 고르는 기준 / conditioning scale과 denoise 강도를 내려 뻣뻣함을 푸는 첫 수
  - 절「LoRA까지 가는 기준」 — 소절: 모델이 모르는 대상·말로 안 되는 화풍·대량 일관성 셋일 때만 간다는 것 / 데이터 20~50장 수집과 학습, 강도 재튜닝까지의 실제 비용 / 한두 장이면 인페인팅으로 고치는 편이 빠른 이유
  - 절「실험을 남기는 법」 — 소절: 프롬프트 → CFG → steps·샘플러 → 조건 → LoRA 순서로 한 번에 하나만 바꾸기 / CFG는 올리는 쪽뿐 아니라 내리는 쪽도 반드시 넣어 훑기 / 프롬프트·네거티브·시드·CFG·steps·샘플러·모델 해시를 그림과 함께 남겨 두 주 뒤에 다시 찾기

- `multimodal-chart-table` (지금 3,586자 · 절 7)
  - 절「배치 속에 녹은 데이터」 — 소절: 62가 69픽셀이 된 과정을 거꾸로 푸는 일 / 축 눈금 둘로 비례 관계를 세우고 막대 밑변·윗변을 재는 세 단계 / 원본이 남아 있는 자리를 먼저 뒤지기 — 대시보드 쿼리, PPT 차트 개체(`chart.plots[0].categories`), 논문 부록
  - 절「차트에서 틀리는 방식」 — 소절: 눈금이 0과 150뿐이면 62인지 65인지 못 가르는 어림 / 누적과 그룹을 뒤집으면 값이 두 배쯤 틀리는 자리 / 잘린 축·로그 축·파이 각도 — 사람도 속는 셋 / 차트 종류와 값 라벨 유무를 앞단에서 분류해 위험한 것만 사람에게 보내기
  - 절「표가 잃는 관계」 — 소절: 머리가 두 층이면 셀 하나에 「수도권·2025년·상반기」 세 이름이 붙는 구조 / 한 줄로 펴는 순간 그 관계가 열의 자리 번호로만 남고 한 칸 밀리면 연도가 바뀌는 자리 / 병합 셀·각주 기호·소계 행·머리에 적힌 단위가 겹칠 때
  - 절「자리 번호에 안 기대기」 — 소절: 셀 하나를 한 줄로 펴고 머리를 전부 이름으로 붙이는 긴 형식 / `rowspan`·`colspan`을 살린 HTML로 뽑아 `read_html`로 받는 길 / 토큰이 늘어나는 대가와 순서가 뒤섞여도 뜻이 안 변한다는 이득
  - 절「바로 묻기와 되돌리기」 — 소절: 호출 수·검산 가능성·계산 정확도로 갈리는 두 갈래 / 「가장 높은 분기」는 바로 묻고 「몇 배인가」는 표로 되돌려 코드로 계산하는 기준 / 모델에게 읽는 일만 시키고 세는 일은 안 시키는 것이 가장 값싼 정확도 개선인 이유
  - 절「묻는 쪽을 고치기」 — 소절: 입력 축소로 축 눈금 숫자가 뭉개지는 자리와 타일 입력·영역 확대 / 값 뽑기와 계산을 나눠 물어 어디서 틀렸는지 보이게 하기 / 축의 최소·최대·단위를 먼저 물어 잘린 축과 로그 축을 걸러 내기 / 같은 양식이 반복되면 열 이름과 단위를 프롬프트에 적어 두기
  - 절「상대 오차로 재는 평가」 — 소절: 정확 일치로 재면 거의 다 오답이 되는 이유와 $$\tau = 0.05$$ 라는 관행 / 정답이 0일 때 분모가 무너지는 예외를 정해 두기 / 표는 셀 정확도가 아니라 필드 완전 일치율이 자동화율의 상한이 되는 자리

- `nlp-multilingual-transfer` (지금 3,662자 · 절 7)
  - 절「한 공간에 겹친 언어들」 — 소절: 언어를 알려 주지도 짝짓지도 않고 섞어 넣었는데 뜻이 가까운 문장이 모이는 현상 / 겹침을 만드는 셋 — 모델 공유, 숫자·이름처럼 겹치는 글자, 닮은 문맥 / 영어 라벨로 올린 분류기가 언어가 아니라 자리를 본다는 사실
  - 절「선택지 넷」 — 소절: 그대로 전이·번역해서 학습·번역해서 추론·소량 라벨의 비용과 상한 / 대상 언어 라벨 수백 건이 0건과 수만 건 사이 격차의 절반 이상을 메운다는 것 / 번역해서 추론하는 길이 원문 위치를 결과에 실어야 하는 작업에 못 쓰이는 이유
  - 절「겹침이 무너지는 자리」 — 소절: 사전학습 자료량이 가장 큰 요인이고 「100개 언어 지원」이 실제로는 상위 스무 개인 경우 / 문자 체계가 다르면 못이 숫자와 고유명사밖에 없는 자리, 그래도 자료가 많으면 되는 예 / 언어를 늘릴수록 언어마다 쓸 용량이 줄어 전부 조금씩 나빠지는 절충
  - 절「분절률이 정하는 것」 — 분절률의 정의와 재는 절차, 「셋이 함께 나빠지는 구조」는 이 절이 통째로 맡는다(`nlp-korean-processing`은 여기를 가리키고 한국어 실측만 든다). 소절: 한 낱말이 몇 조각으로 쪼개지는지를 재는 절차와 언어별로 같은 문단을 넣어 표로 만드는 법 / 영어 20토큰이 60토큰이 될 때 문맥 창·비용·표현 흐림 셋이 함께 나빠지는 구조 / 실제로 얼마나 벌어지는지는 `/articles/cost-korean-token-tax`가 토크나이저 아홉 개로 재 놓았다 — 자/토큰 0.89~2.67에 어휘 크기와의 상관은 0.078이라, 분절률이 나쁜 이유를 어휘 크기로 설명하면 안 된다는 것 / 새 언어를 지원 목록에 올리기 전에 이 값부터 재면 성능이 왜 안 나오는지가 미리 설명되는 것
  - 절「언어별로 나눠 재기」 — 소절: 평균 하나가 20~30%p 차이를 감추고 트래픽 많은 언어의 값에 가까워지는 자리 / 가장 나쁜 언어의 값과 표본 수를 함께 적기(30건의 95%와 3,000건의 95%) / 성능이 이상한 언어는 언어 판별부터 확인하기 / 기계번역으로 만든 평가셋이 실제 그 언어의 글과 다른 이유
  - 절「큰 모델에서 남는 격차」 — 소절: 지시문 언어와 입력 언어를 갈라 정하는 것이 더 안정적인 경우 / 출력 언어를 못 박지 않으면 긴 추론 중에 영어로 새는 자리 / 답의 후보가 정해진 형태가 자유 서술보다 언어를 덜 타는 성질
  - 절「한국어에서 볼 것」 — 소절: 위 절차로 잰 한국어 분절률을 다른 언어와 같은 표에 놓아 전이의 상한을 가늠하기(한국어만 놓고 보는 비용 계산은 `nlp-korean-processing`이 맡는다) / 존댓말·반말과 구어·문어가 판정을 흔드는데 영어 라벨로 학습한 모델은 그 축을 아예 모른다는 것 / 조사와 어미가 붙어 겹치는 조각이 적은 자리 / 영어를 거치면 문체 정보가 사라지므로 그 축이 답에 들어가는 작업에는 못 쓰는 것

- `cv-ocr-modern` (지금 3,697자 · 절 8)
  - 절「검출과 인식의 분업」 — 소절: 위치를 찾는 문제와 순서 있는 문자열을 만드는 문제가 성격이 다른 이유 / 검출이 객체 검출과 뼈대를 공유하고 인식은 오히려 언어 모델에 가까운 자리 / 인쇄된 A4는 풀렸고 간판·손글씨·구겨진 영수증·세로쓰기가 남은 지형
  - 절「글자 영역의 모양」 — 소절: 기울고 휘고 길쭉한 글자가 축에 나란한 사각형에 안 담기는 것 / 픽셀마다 확률을 예측해 이진화하고 외곽선을 다각형으로 뽑는 방식, 그리고 임계값을 학습으로 정하는 이유 / 글자 중심과 이음새를 각각 예측해 이어 붙이는 방식이 휜 줄에 강한 자리
  - 절「줄을 묶는 규칙」 — 소절: 한 문장이 두 줄로 갈리면 인식과 후처리가 통째로 어긋나는 것 / 표의 인접한 두 칸이 한 줄로 묶이는 반대 방향 오류 / 모델을 바꾸는 것보다 임계값과 병합 규칙을 데이터에 맞추는 일이 대부분인 이유
  - 절「글자 수를 모르는 문제」 — 소절: 세로로 잘게 썰어 조각마다 예측하고 빈칸 기호로 합치는 방식(`ㅅㅅ_ㅓㅓ_ㅇㅇ` → 「서울」) / 정렬 정보를 라벨에 안 넣어도 되는 것이 이 방식의 이점인 자리 / 디코더가 문맥으로 흐릿한 글자를 메우는 이점이 계좌번호·일련번호에서 그대로 약점이 되는 것
  - 절「한글에서 더 어려운 것」 — 소절: 음절 11,172자, 실사용 2,500자 안팎 — 알파벳 26자와 견줄 때의 출력 클래스와 데이터 요구 / 받침 하나로 갈리는 쌍(왼/외, 밝/밖)과 줄 높이 20픽셀이라는 경계 / 한 줄에 한글·영문·숫자가 섞일 때의 0/O·1/l·5/S 혼동 / 필드 성격을 아는 후처리 규칙과 체크섬으로 거르기
  - 절「경계를 지운 VLM」 — 소절: 이미지와 지시만 주고 필요한 값을 바로 받는 구성의 싼 비용 / 좌표가 안 남아 검수 화면을 못 만들고 틀렸을 때 어디를 볼지 모르는 자리 / 안 보이는 글자를 자신 있게 채우고 확률값도 없는 문제 / 검출로 좌표를 얻고 조각만 VLM에 넘겨 둘을 겹쳐 쓰기
  - 절「CER과 필드 일치율」 — 소절: 편집 연산 수를 정답 길이로 나누는 계산과 그 값이 필드 쓸모와 어긋나는 자리(주소 한 글자 = CER 6.3%) / CER은 모델을 고를 때, 필드 완전 일치율은 자동화 비율을 정할 때 / 신뢰도 구간별 실제 정확도를 재어 자동 통과선을 근거 있게 긋기

- `multimodal-audio-agents` (지금 3,853자 · 절 7)
  - 절「시간이 정하는 설계」 — 소절: 채팅의 3초는 「생각 중」이고 통화의 3초는 「끊겼나」인 자리 / 응답까지의 예산을 단계별로 쪼개 적어 보기 / 예산을 못 맞출 때 무엇을 먼저 포기할지(모델 크기·검색 단계·확인 질문) 미리 정하기
  - 절「두 가지 구성 방식」 — 소절: 이어 붙인 구성에서 단계 시간이 더해지고 앞이 끝나야 뒤가 시작하는 구간 / 부분 인식 결과로 미리 준비하고, 문장 단위로 잘라 합성하고, 조각으로 스트리밍해 800 ms 안쪽으로 넣기 / 응답 시간·억양·부품 교체·기록·출력 통제 다섯 축의 비교와 섞어 쓰는 구성
  - 절「발화 끝의 판단」 — 소절: 침묵 200 ms와 1,000 ms가 각각 만드는 실패(반만 듣고 답함 / 굼뜸) / 부분 인식 결과의 문장 완결성과 억양까지 함께 보는 방식 / 잘못 잘랐을 때를 끼어들기와 같은 경로로 처리하도록 정해 두기
  - 절「끼어들기와 되돌리기」 — 소절: 재생 중지만으로는 안 되는 세 가지 되돌림 — 안 나간 오디오, 대화 기록, 진행 중인 호출 / 들리지 않은 문장을 기록에 남기면 나중에 「아까 말씀드렸듯이」가 나오는 자리 / 재생기가 실제로 어디까지 냈는지 알려 주게 하고 「(도중에 끊김)」을 함께 남기기
  - 절「도구 호출 중의 침묵」 — 소절: 「네, 확인해 볼게요」를 먼저 내보내고 그 소리가 나가는 사이에 조회하기 / 채움말을 몇 개 돌려 써 반복을 피하기 / 도구마다 시간 상한을 두고 넘기면 사람에게 넘기거나 다른 차례로 미루기
  - 절「자주 깨지는 자리」 — 소절: 고유명사·숫자에 어휘 힌트를 주고 중요한 값은 다시 읽어 확인하기 / 한국어 문장 안에 영문 제품명·약어가 섞이는 구간 / 8 kHz 전화망·스피커폰 반향·매장 소음을 평가 데이터에 넣기 / 스피커로 나간 합성음이 마이크로 되돌아와 스스로 말을 끊는 것
  - 절「음성용 평가 항목」 — 소절: 평균 600 ms가 아니라 95·99 지점을 보는 이유 / 끼어들기 성공률 — 멈추기까지의 시간과 기록이 들린 데까지인지 / 이름·숫자·코드만 따로 잰 정확도, 그리고 조용한 방 녹음 대신 실제 통화로 평가하기

- `multimodal-evaluation` (지금 3,904자 · 절 7)
  - 절「멀티모달 평가의 어려움」 — 소절: 정답이 하나인 문제와 여럿인 문제가 섞여 측정 방식을 나눠 설계해야 하는 자리 / 「사과는 무슨 색입니까」처럼 이미지 없이 풀리는 언어 편향과 반례 쌍·균형 분포로 막는 설계 / 이미지를 빼고 텍스트만 줬을 때의 점수를 함께 재어 시각 능력의 몫을 분리하기
  - 절「인식과 추론을 가르기」 — 소절: VQA v2·GQA·TextVQA·OCRBench가 각각 재는 능력 / MMMU·MathVista·ScienceQA가 요구하는 도메인 지식과 다단계 추론 / 우리 문제(문서·차트·화면·사진)에 가장 가까운 벤치마크를 고르는 기준
  - 절「소프트 정확도와 선택형」 — 소절: 열 명의 답 중 몇 명과 일치했는지로 점수를 매기는 계산 / 선택형이 자동 채점을 쉽게 하는 대신 잃는 것(생성 품질·근거) / 보기 순서를 섞어 위치 편향이 있는지 확인하는 절차
  - 절「환각을 재는 법」 — 소절: 「이 이미지에 X가 있습니까」 예·아니오로 물체 환각을 재는 구조 / 무작위·인기 물체·인접 물체 세 샘플링이 난도를 가르고 마지막이 가장 어려운 이유 / 시각 착각과 사전 지식이 충돌할 때의 반응을 보는 평가
  - 절「심판 모델의 편향」 — 소절: 개방형 생성을 자동 채점할 때 심판 모델이 들여오는 편향(길이·형식·자기 계열 선호) / 채점 기준을 문장으로 적고 점수와 근거를 함께 받게 하기 / 사람 평가와의 상관을 한 번 재어 심판 자체를 검증하는 절차
  - 절「점수를 못 믿는 자리」 — 소절: 학습 데이터에 벤치마크가 섞이는 데이터 오염과 그것을 의심할 신호 / 리더보드 점수와 우리 데이터에서의 성능이 갈리는 이유 / 작은 오픈 모델이 환각 지표에서 큰 상용 모델을 앞서는 것처럼, 지표마다 순위가 뒤집히는 자리
  - 절「우리 평가셋 만들기」 — 소절: 실제 입력 100건으로 시작해 오답을 읽고 항목을 늘리는 순서 / 이미지 없이도 풀리는 문항을 걸러 내는 점검 / 모델을 바꿀 때마다 같은 셋으로 재어 회귀를 잡고, 합격선을 자동화 비율로 환산하기

- `audio-speech-realtime-api` (지금 3,907자 · 절 8)
  - 절「지연 예산의 구성」 — 소절: 500~800ms를 침묵 대기·인식 확정·첫 토큰·첫 소리·네트워크 다섯 조각으로 쪼개 밀리초를 배정한 표 / LLM을 400ms에서 250ms로 줄여도 1,200ms가 1,050ms에 그치는 산수를 그대로 보이기 / 체감을 정하는 것은 전체 응답 길이가 아니라 첫 소리 — TTS를 문장 단위로 끊어 첫 문장부터 내보내는 것과 다 만들고 보내는 것의 차이
  - 절「발화 종료 판정」 — 소절: VAD는 프레임마다 말소리 유무만 내므로 「얼마나 기다릴 것인가」는 여전히 우리 몫이라는 것 / 임계값 300ms와 1초의 교환 — 말허리 자름 비율과 턴당 지연이 정확히 반대로 움직인다 / 완화 셋: 입력 종류별 임계값(전화번호 대 예·아니오), 부분 인식 결과의 문장 완결 판단, 짧은 접수 응답으로 덮기 / 통합 모델이 억양과 문맥으로 판정할 때 사라지는 왕복 한 번
  - 절「3단 구성과 통합 모델」 — 소절: 표 일곱 줄 중 결정을 실제로 정하는 둘 — 중간 텍스트가 남는가, 억양이 살아남는가 / 검사를 끼울 자리 — 3단은 LLM 텍스트 앞에 가드레일이 서고 통합 모델은 소리가 나간 뒤라 전사가 사후 기록에 그친다 / 비용의 모양이 다르다 — 세 번 청구되는 구성과 오디오 토큰 단가가 붙는 구성을 통화 1분으로 환산해 견주기
  - 절「양방향 스트리밍의 이벤트」 — 소절: 오디오 조각·부분 인식·응답 오디오·응답 텍스트·취소 다섯 이벤트를 시간 순으로 놓고 각각 누가 만들어 누가 상태를 들고 있는지 / WebRTC와 WebSocket을 고르는 기준 — 브라우저 마이크·에코 제거·지터 버퍼가 필요한가, 전화망은 8kHz G.711이라 대역이 좁다 / 서버가 오디오를 앞질러 보내 끊김을 막는 대신 다음 절의 어긋남을 만드는 지점 / 턴 도중 연결이 끊겼을 때 무엇을 다시 보내고 무엇을 버리는가
  - 절「끼어들기와 되감기」 — 소절: 서버가 보낸 스무 조각과 사용자가 들은 세 조각의 어긋남이 다음 턴의 「아까 말씀드렸듯이」로 나오는 장면 / 세 걸음 — 남은 버퍼 버리기, 재생 위치를 밀리초로 보고하기, 대화 기록을 그 지점에서 자르기. 세 번째가 빠져도 당장은 멀쩡해 며칠 뒤에 발견되는 이유 / 스피커폰의 에코가 VAD를 속여 스스로 끼어드는 장면과 에코 제거를 켜는 자리
  - 절「도구 호출 중의 침묵」 — 소절: 화면의 진행 표시가 없는 자리에서 2초가 「연결이 끊겼다」로 읽히는 이유 / 채우는 말 셋 — 호출 전 예고, 2초를 넘기면 한 번 더, 부분 인식 결과로 미리 시작하는 조회 / 호출 중에도 VAD와 연결을 살려 둔다 — 동기 호출로 짜면 「아니에요, 다른 계좌요」를 못 받고 진행 중 호출을 버릴 경로도 없다
  - 절「재는 값과 남기는 로그」 — 소절: 넷을 각각 어떻게 재는가 — 첫 소리 지연 95백분위, 말허리 자름 비율, 끼어들기 성공률, 턴 완결률 / 앞의 둘을 VAD 임계값 한 축에 함께 놓고 보는 튜닝 절차 / 오디오와 이벤트 타임스탬프를 같은 시계로 남겨 통화 하나에서 3초가 어디로 갔는지 되짚기

- `audio-diarization` (지금 3,924자 · 절 9)
  - 절「분리와 인식의 경계」 — 소절: 「이 두 구간이 같은 사람인가」와 「이 목소리가 등록된 김민수인가」 두 물음의 차이 / 번호 라벨이 그 파일 안에서만 통한다는 것 — 같은 회의를 두 번 녹음하면 번호가 바뀐다 / 목소리 견본을 보관하는 순간 생체정보 시스템이 된다는 점과, 회의록 제품이 이름 매핑을 사용자에게 맡기는 이유
  - 절「네 걸음의 처리 과정」 — 소절: 말 찾기(VAD)에서 놓친 구간은 뒤 세 단계에서 절대 복구되지 않는다 / 1~2초씩 겹쳐 자르는 이유 — 조각이 짧으면 임베딩이 흔들리고 길면 화자 전환을 삼킨다 / 화자 임베딩이 놓이는 공간과 모델 계보(x-vector에서 ECAPA-TDNN으로) / 녹음 조건이 임베딩을 정한다 — 원거리 마이크의 잔향, 8kHz 전화 녹음의 고역 손실, 1초 미만 맞장구
  - 절「화자 수를 모른다는 것」 — 소절: 응집형 계층 군집화의 정지 임계값 하나가 결과를 좌우하는 구조 / 낮으면 한 사람이 여러 화자로 쪼개지고 높으면 목소리가 비슷한 둘이 붙는다, 좋은 값이 녹음마다 다른 이유 / 스펙트럴 군집화의 고윳값 간극으로 화자 수를 추정하는 다른 길 / 화상회의 도구처럼 참석자 수를 이미 아는 자리에서는 넘겨 주는 것이 임계값 튜닝보다 값싸다
  - 절「겹쳐 말한 구간」 — 소절: 한 조각에 화자 하나를 배정하는 구조가 겹침 10%짜리 녹음에 깔아 놓는 바닥 오류 / 대응 셋 — 겹침 감지 모델을 얹기, 소리를 먼저 분리하기, 처음부터 여러 명을 낼 수 있게 만들기 / 참가자별 트랙을 받을 수 있으면 이 문제가 통째로 사라진다 — 기술로 풀기 전에 먼저 확인할 길
  - 절「군집형과 종단형」 — 소절: 표의 여섯 축 중 실제로 선택을 정하는 둘 — 겹침 처리와 긴 녹음의 안정성 / 종단형이 다룰 수 있는 최대 인원에 묶이고 두세 시간 녹음은 나눠 이어 붙여야 한다는 제약 / 실무의 섞어 쓰기 — 군집형으로 뼈대를 잡고 겹침이 의심되는 짧은 구간만 다른 모델에 태우기
  - 절「DER의 세 조각」 — 소절: 누락·오검출·화자 혼동이 각각 가리키는 고칠 자리가 정반대라는 것(VAD인가 임계값·임베딩인가) / 같은 시스템의 DER을 두 배로 벌리는 두 조건 — 겹침 구간을 채점했는가, 경계 앞뒤 0.25초를 뺐는가 / 논문 표의 숫자와 우리 숫자를 비교하기 전에 이 둘을 맞추는 순서
  - 절「회의록으로 합치기」 — 소절: 단어별 타임스탬프와 화자 구간을 시각으로 맞춰 단어마다 화자를 붙이는 절차 / 문장 경계와 화자 경계는 원래 안 맞는다 — 「네, 그러면 다음 안건으로」의 「네」만 다른 사람인 장면 / 문장 단위 다수결, 0.5초 미만 조각 흡수, 전환 지점 앞뒤 첫 단어 의심 — 셋을 순서대로 적용하기

- `rl-offline-rl` (지금 4,008자 · 절 8)
  - 절「부풀어 오르는 Q값」 — 소절: 행동 복제의 상한 — 흉내는 로그를 만든 정책을 넘지 못하고, 개선하려면 로그에 없는 행동의 값을 물어야 한다 / $$\max$$ 가 모든 행동을 후보로 놓는데 신경망은 모른다고 답하지 않고 어림한 숫자를 낸다 / 과대평가 → max 선택 → 목표값 → 재학습의 한 바퀴를 작은 숫자로 따라가기 / 온라인에서는 실제 보상이 그 고리를 끊고 오프라인에는 끌어내릴 자료가 영영 안 온다
  - 절「누르는 네 갈래」 — 소절: 정책 제약(BCQ·BEAR 계열) — 행동 복제 항을 손실에 섞어 로그 근처에 묶어 두기 / 값 보수화(CQL) — 데이터 밖 Q를 낮추는 항과 계수가 세면 행동 복제와 같아지고 약하면 부풀음이 새는 구간 / 데이터 안 부트스트랩(IQL) — expectile 회귀로 로그에 있는 행동의 위쪽 값을 취해 $$\max$$ 를 대신하기 / 열 모델링(Decision Transformer) — return-to-go 조건부 예측이라 부풀음이 없는 대신 로그에 없는 조합을 새로 잇지 못한다
  - 절「기준선으로서의 행동 복제」 — 소절: 로그를 만든 것이 잘 다듬어진 규칙이나 숙련자면 복잡한 알고리즘이 지는 이유 / 보상이 높은 궤적만 걸러 학습하는 필터링 BC가 실제로 최고 성능인 자리 / 로그가 무작위에 가깝거나 여러 정책이 섞였을 때 강화학습이 크게 이기는 이유 — 좋은 조각과 나쁜 조각을 갈라 좋은 것만 이어 붙인다
  - 절「오프라인 정책 평가」 — 소절: 중요도 가중은 무편향인데 두 정책이 조금만 달라도 분산이 폭발한다 — 여러 단계면 확률의 곱이 되어 유효 표본이 몇 건으로 줄어드는 계산 / 모델 기반 평가는 분산이 작은 대신 보상 모델이 틀린 만큼 그대로 틀린다 / 이중 강건이 절충인 이유와, 셋을 함께 돌려 값이 갈리면 아직 못 믿는다는 판정
  - 절「로그에 남겨야 할 것」 — 소절: 성향 점수(그때 그 행동을 고를 확률)가 없으면 세 평가법이 전부 성립하지 않는데 대부분의 서비스 로그에 그 열이 없다 / 결정론적으로 1등만 내보내지 말고 아주 작은 비율이라도 무작위를 섞어 두는 이유 / 후보 집합·모델 버전·실험 배정까지 남기는 로그 스키마 — 이 셋이 없으면 양이 아무리 많아도 자료가 안 된다
  - 절「선호 학습과 같은 구조」 — 소절: 고정된 선호 짝 자료 위에서 도는 DPO가 전형적인 오프라인 학습인 이유 / 원래 모델에서 멀어지는 데 거는 벌점이 위의 정책 제약과 같은 처방이라는 것 / PPO에 보상 모델을 두는 구성에서는 부풀음의 자리가 Q 함수에서 보상 모델로 옮겨 갈 뿐 구조가 같다
  - 절「쓸 자리와 안 쓸 자리」 — 소절: 쓸 만한 조건 넷 — 탐색 비용·로그의 행동 다양성·개선 여지·성향 점수 유무 / 쓰지 말아야 할 조건 넷 — 결정론적 규칙 하나에서 나온 로그, 이미 아주 좋은 정책, 대리 지표 보상, 값싸게 돌릴 수 있는 온라인 실험 / 클릭 같은 대리 지표가 오프라인에서 더 위험한 이유 — 이상해진 것을 알아챌 고리가 없다

- `nlp-information-extraction` (지금 4,056자 · 절 8)
  - 절「세 층위와 스키마」 — 소절: 개체명·관계·사건 세 층과, 한 문서에 회사 이름이 다섯이면 1층만으로는 사람이 다시 읽게 되는 이유 / 목표는 언제나 3층 — 발주사·수주사·계약일·금액·기간이 한 줄로 묶여야 쓸모가 생긴다 / 스키마를 먼저 못 박는 절차 — 칸마다 자료형·필수 여부·다중값 허용·기본 단위를 정해 두기
  - 절「값을 다듬는 정규화」 — 소절: 수 — 「4,800만」·「48백만」·「USD 36,000」이 섞이고 부가세 포함 여부가 다른 문장에 적혀 있는 경우 / 날짜 — 「익월 말일」·「계약일로부터 30일 이내」의 기준일이 문서 안에 있을 때와 메일 발신 시각일 때 / 개체 연결 — 「㈜누리테크」·「누리테크 주식회사」·「NuriTech Co., Ltd.」를 거래처 대장의 한 행으로 모으기 / 뽑기와 다듬기를 나누면 뒷단계가 확정적 코드가 되고 틀린 자리도 보인다
  - 절「근거 스팬과 검수」 — 소절: 스팬 하나로 생기는 셋 — 검수 화면, 자동 검증, 감사 기록 / 위치 숫자를 세게 하는 것보다 원문 인용문을 받아 코드가 위치를 찾는 편이 잘 맞는 이유 / 인용문이 원문에 없으면 그 칸을 버리는 규칙과, 공백·조사 차이를 어디까지 허용할지 정하기
  - 절「세 가지 방법의 배치」 — 소절: 표 일곱 줄 중 실제 결정에 쓰는 둘 — 없는 값을 지어내는가, 칸 하나 추가에 무엇이 드는가 / 정규식이 이기는 칸(사업자등록번호 검증식·계좌번호·날짜 형식)과 언어 모델에 보낼 칸(「이 금액이 총액인가 월 단가인가」) / 물량이 커지고 양식이 안정됐을 때 언어 모델 결과로 전용 모델을 학습해 옮겨 태우는 순서와 건당 비용 계산
  - 절「언어 모델 추출의 규칙」 — 소절: 스키마 강제와 모든 칸 `null` 허용, 「원문에 없으면 반드시 null」 한 줄이 막는 오류가 실전에서 가장 많다 / 칸 서른 개를 한 번에 채우면 뒤쪽 품질이 떨어진다 — 성격이 비슷한 칸끼리 묶어 서너 번에 나눠 부르기 / 칸마다 갈 곳이 다르다 — 계약 금액은 앞쪽, 특약은 뒤쪽이므로 필드 묶음별로 관련 구간만 넣기
  - 절「칸 단위 평가」 — 소절: 칸마다 정밀도·재현율을 내고 무게를 달리 두는 이유 — 금액은 정밀도, 특약 조항은 재현율 / 완전 일치와 부분 일치를 나눈다 — 「㈜」가 빠진 것과 다른 회사를 뽑은 것은 전혀 다른 오류다 / 비어 있어야 하는 칸을 따로 센다 — 보통의 정밀도·재현율 계산에서는 안 드러난다 / 운영 지표는 검수 없이 통과한 비율이고, 위험한 것을 위로 올리는 정렬 품질이 정확도만큼 중요하다
  - 절「한국어에서 걸리는 자리」 — 소절: 조사가 붙어 스팬 경계가 한 글자씩 어긋나는 오류와 끝 조사를 떼는 후처리 / 띄어쓰기·한자·영문 표기가 문서마다 흔들릴 때 개체 연결에서 하는 최소 정리 / 공공 문서 별지 서식 — 같은 이름의 칸이 기관마다 위치와 표현을 바꿔 가며 존재한다

- `multimodal-document-understanding` (지금 4,094자 · 절 8)
  - 절「읽는 차례와 레이아웃」 — 소절: 두 단 문서를 위에서 아래로 훑으면 왼쪽 첫 줄과 오른쪽 첫 줄이 이어 붙고, 뒤의 LLM이 그럴듯한 답을 만들어 오류로 잡히지도 않는다 / 본문 흐름에서 따로 가려낼 넷 — 머리말·쪽 번호·각주·캡션 / 차례는 레이아웃 검출 → 영역 간 읽는 순서 → 영역 안 글자 인식이고 OCR이 첫 단계가 아니라는 것
  - 절「파이프라인과 VLM」 — 소절: 표 일곱 줄 중 결정을 정하는 하나 — 못 읽었을 때 빈칸이 나오는가 그럴듯한 값이 나오는가 / 나머지를 가르는 둘 — 서식이 고정인가 처음 보는 것인가, 좌표가 필요한가 / 섞어 쓰는 기본형 — 레이아웃과 OCR은 전용 도구로 하고, 뽑은 텍스트와 페이지 이미지를 함께 넣어 VLM이 구조화한다
  - 절「난도를 정하는 표」 — 소절: 병합 셀과 여러 층의 열 이름 — 「2026년」 아래 「1분기」 구조에서 값 하나를 지목하려면 두 층을 다 알아야 한다 / 여러 쪽에 걸친 표, 괘선 없이 공백으로만 정렬한 표, 빈칸이 0인지 해당 없음인지 안 적은 것인지 / 합계와 소계를 실제로 더해 보는 자체 검산 — 사람이 보기 전에 기계가 잡는 가장 값싼 검사
  - 절「값과 함께 뽑는 근거」 — 소절: 출력 구조에 넣을 넷 — 페이지·좌표, 신뢰도, 원문 그대로의 문자열, 출처 종류(표·본문·도장·손글씨) / `12500000`과 `"12,500,000"`을 둘 다 들고 있으면 잡히는 변환 오류 / JSON 스키마로 모양을 못 박고 검증 실패를 재시도 단계로 파이프라인에 넣기
  - 절「신뢰도를 만드는 신호」 — 소절: 프롬프트로 받아 낸 0.95가 틀린 값에도 붙는 이유 — 모델이 적은 숫자는 실제 정확도와 잘 안 맞는다 / 다섯 신호 — OCR 신뢰도, 온도를 올려 두 번 돌려 비교, 두 경로 대조, 규칙 검사(세액이 공급가액의 10%인지), 필수 필드 결측 / 신호를 필드 점수 하나로 합치고 임계값을 정하는 절차와 문서 종류마다 규칙이 열 개쯤 나온다는 경험
  - 절「사람 검수의 자리」 — 소절: 임계값으로 문서를 세 갈래로 — 전부 통과, 낮은 필드만 검수 큐, 문서 전체를 사람에게 / 좌표로 만드는 검수 화면 — 값 옆에 원본 이미지의 그 부분을 잘라 보여 주면 문서를 열 필요가 없다(3분에서 3초) / 고친 값을 모아 어느 필드·어느 서식이 자주 틀리는지 보고 평가 집합으로 되돌리기, 지표를 「사람 손이 몇 건에 갔는가」로 두기
  - 절「필드와 문서 두 지표」 — 소절: 필드 20개 중 19개를 맞혀 정확도 95%인데 그 건은 여전히 자동 처리가 안 된다는 산수 / 문서 완전 일치율이 자동화율의 상한이라는 것과 둘을 함께 보는 이유 / 평가 집합은 서식별로 고르게 만들고 기울어진 스캔·겹친 도장·손글씨를 일부러 넣는다

- `multimodal-llm` (지금 4,095자 · 절 7)
  - 절「이미지의 토큰화 경로」 — 이 글은 「한 장이 몇 토큰이 되는가」까지만 본다. 프로젝션·Q-Former·크로스 어텐션을 견주는 연결 방식 분류는 다음 글 `multimodal-vlm`이 통째로 맡으므로 한 문장으로 줄이고 넘긴다. 소절: 토크나이저 하나로 끝나던 입력 파이프라인이 모달리티별 인코더로 갈리는 지점 / 패치 분할이 정하는 토큰 수 — 14×14 패치로 한 장이 몇 토큰이 되는지 직접 세어 보기 / 이미지 토큰이 텍스트 토큰과 한 줄에 섞인 뒤 디코더가 하는 일과, 그래서 하나뿐인 컨텍스트 예산을 그림과 글이 나눠 쓰게 된다는 것
  - 절「해상도와 토큰 비용」 — 타일로 왜·어떻게 쪼개는가(동적 고해상도, 전역 축소 이미지를 함께 넣기)는 `multimodal-vlm`이 맡고 여기서는 그 결과로 붙는 청구서만 센다. 소절: 고해상도를 타일로 쪼개 넣으면 토큰이 몇 배가 되는지 벤더 문서를 보고 세는 법 / `detail` 파라미터와 전처리 단계의 크기 조절로 비용을 정하는 자리 / 해상도를 낮춰도 되는 질문(도표의 추세)과 낮추면 그냥 못 읽는 문서(작은 글자·스캔본)를 가르는 기준
  - 절「모델을 고르는 축」 — 소절: 이미지·오디오·비디오·컨텍스트 표의 각 칸이 제품 결정으로 이어지는 자리 — 원고 51~70행의 비교(GPT-4o·Gemini 1.5 Pro·Claude 3.5+)는 2026-05 시점 값이라 지금 파는 모델로 표와 그 앞의 모델별 소개 문단을 함께 다시 채우고, 표 아래에 기준 시점을 한 줄로 밝혀 다음에 또 낡았을 때 눈에 띄게 한다 / 처음부터 함께 학습한 것과 비전 모듈을 붙인 것의 차이가 실제로 드러나는 지점 — 음성 지연과 억양 / 벤더를 고르기 전에 자기 자료 30~50건으로 만드는 작은 비교 집합과 그때 재는 값
  - 절「할 수 있는 일과 그 경계」 — 소절: 캡셔닝·VQA·OCR에서 「맥락까지 읽는다」의 뜻 — 영수증에서 총액만 골라내기, 표지판 사진에서 국가·언어 판단하기 / 차트 해석은 값이 근사라는 전제로 다룬다 — 정확한 수가 필요하면 표나 원본 데이터로 되돌리기 / 문서를 이미지로 렌더해 넣는 방식이 텍스트 파싱보다 나은 조건과 그 대가
  - 절「API로 넣는 법」 — 소절: base64와 URL 두 경로를 고르는 기준 — 크기 제한, 같은 이미지의 재사용, 외부에서 접근 가능한 자료인가 / 여러 장을 넣을 때 순서와 이름표를 붙여 모델이 어느 그림을 말하는지 밝히게 하기 / 자주 밟는 실패 — 지원하지 않는 형식, 크기 초과, 이미지가 안 실린 채 그럴듯한 답만 오는 경우
  - 절「환각과 공간 추론」 — 소절: 없는 물체를 있다고 말하는 오류를 예·아니오 탐침으로 재는 방법(POPE식)과 그 결과를 읽는 법 / 개수 세기, 좌우·앞뒤 관계, 세밀 분류에서 무너지는 실제 질문들 / 완화 셋 — 영역을 잘라 되묻기, 근거 좌표를 함께 요구하기, 세밀 분류는 전용 모델로 넘기기
  - 절「사전학습과 인스트럭션 튜닝」 — LLaVA의 158K 합성 대화와 무엇을 얼리고 무엇을 여는가는 `multimodal-vlm`이 맡는다. 여기서는 「어느 단계에서 무엇이 붙는가」만 훑고 파인튜닝 판단으로 넘어간다. 소절: 이미지-텍스트 쌍 사전학습이 만드는 정렬과, 인터리빙 데이터가 붙이는 few-shot 능력 / 비주얼 인스트럭션 튜닝이 대화 능력을 얹는 자리 — 캡션만 보고 만든 합성 대화로 대화를 가르친다는 발상까지만 적고 데이터 규모와 단계 구성은 다음 글로 / 우리가 파인튜닝을 꺼낼 조건 — 프롬프트·해상도·도구 사용으로 해결되는 자리를 먼저 지우고 남는 것만

- `multimodal-vlm` (지금 4,261자 · 절 7)
  - 절「대조 학습의 공동 공간」 — 소절: 두 인코더가 배치 안에서 N×N 유사도 행렬을 만들고 대각선만 올리는 한 스텝 / 「A photo of a {}」 템플릿으로 어떤 분류 문제든 제로샷이 되는 절차 / 4억 쌍 자동 수집이 한 일과 그 대가 — 어노테이션 없이 넓은 개념을 덮는 대신 데이터 품질을 고를 수 없다
  - 절「시그모이드 손실의 차이」 — 소절: InfoNCE가 배치 안 음성 쌍에 기대는 구조와 그래서 커지는 배치 요구 / 쌍마다 독립 이진 분류로 바꾸면 배치 크기 의존이 끊긴다는 것 / SigLIP 계열이 지금 비전 인코더 기본값이 된 경위와, SO400M의 SO가 shape-optimized라는 것
  - 절「인코더와 LLM을 잇는 셋」 — 연결 방식은 이 절이 통째로 맡는다(`multimodal-llm`은 토큰 수만 세고 여기를 가리킨다). 소절: 선형·MLP 프로젝션(LLaVA) — 패치 수가 그대로 토큰 수가 되는 단순함과 LLaVA-1.5가 선형을 2층 MLP로 바꾼 이유 / Q-Former(BLIP2) — 학습 쿼리 32개로 줄여 넣고 ViT·LLM을 얼린 채 전체의 1~2%만 학습 / 크로스 어텐션 삽입 — LLM 안에 층을 끼워 이미지 특징을 참조하게 하는 계열 / 셋을 고르는 축: 학습 파라미터 비율, 토큰 예산, 기존 LLM을 얼마나 건드리는가
  - 절「해상도를 다루는 법」 — 타일링의 구조와 그 대가는 이 절이 맡고, 벤더별 토큰 청구는 `multimodal-llm`에 둔다. 소절: 224·336 고정 입력이 작은 글자와 빽빽한 표를 못 읽게 만드는 지점 / 타일로 쪼개 해상도를 올리는 대신 토큰이 느는 교환(LLaVA-NeXT의 동적 고해상도) / 전역 축소 이미지 한 장을 타일과 함께 넣어 전체 배치를 잃지 않게 하기
  - 절「동결과 학습 단계」 — 학습 단계 구성과 158K 합성 대화는 이 절이 맡는다(`multimodal-llm`은 발상만 적고 넘긴다). 소절: ViT·프로젝션·LLM 중 무엇을 여는가에 따라 달라지는 학습 파라미터 비율과 필요한 데이터 양 / 2단계 구성 — 정렬 사전학습에서 프로젝션만 맞추고 인스트럭션 튜닝에서 대화를 붙인다 / GPT-4에 캡션과 박스 정보만 주고 만든 158K 합성 대화가 대신한 라벨링과, 이미지를 안 보고 만든 데이터라는 한계
  - 절「검색과 이상 탐지」 — 소절: 임베딩을 L2 정규화해 코사인으로 견주는 검색 만들기와 인덱스 고르기 / 텍스트 쿼리로 상품을 찾는 교차 검색에서 실제로 걸리는 것 — 도메인 어휘, 한국어 질의, 이미지와 텍스트가 서로 다른 원뿔에 모이는 모달리티 갭 / 정상·결함 프롬프트의 거리로 하는 제로샷 이상 탐지가 되는 조건과 안 되는 조건
  - 절「데이터와 벤치마크」 — 소절: 표 네 줄이 가르치는 것이 다르다 — 쌍 데이터는 정렬을, 인스트럭션 데이터는 대화를 만든다 / 벤치마크 넷이 재는 것 — MMBench의 객관식 능력, MMMU의 전공 지식, ScienceQA의 과학 추론, OCRBench의 문서·장면 글자 / 객관식 점수와 우리 과제 성능이 갈리는 자리와, 자기 자료로 만든 작은 집합이 따로 필요한 이유

- `multimodal-rag` (지금 4,592자 · 절 9)
  - 절「색인에 넣는 세 가지 길」 — 소절: 텍스트로 옮기기 — 설명문에 안 적힌 것은 안 걸리므로 축 이름·범례·단위·변곡점·열 이름을 적게 하는 프롬프트 / 공동 임베딩 — 사진에는 맞고 글자가 빽빽한 도표에 약한 이유(「무엇이 찍혀 있는가」만 배웠다) / 페이지 통째 — 파싱 단계가 사라지는 대신 인용이 「이 페이지 어딘가」로 거칠어진다 / 문서 종류마다 다른 길로 태우는 배치와 그 판단 기준
  - 절「색인 형태와 생성 형태」 — 소절: 검색은 글끼리 견주고 답은 원본 그림을 보고 쓰게 나누는 구성과 그 이유 / 그러려면 조각마다 파일 경로·페이지 번호·좌표 상자를 남겨야 한다 — 나중에 붙이려면 색인을 다시 만든다 / 원본을 꺼내 VLM에 넣을 때 함께 적어 주는 것 — 그림 번호, 캡션, 그 그림을 언급한 문단
  - 절「그림 문서의 청킹」 — 소절: 그림 하나 + 캡션 + 「그림 3에서 보듯이」로 시작하는 문단을 한 조각으로 묶는 원칙 / 표는 통째로 두고, 잘라야 하면 자를 때마다 열 이름 줄을 복사해 붙인다 / 여러 쪽에 걸친 표를 파싱 단계에서 이어 붙이지 않으면 뒤쪽 조각이 고아가 된다 / 조각마다 문서 제목·절 제목·발행일을 붙여 어느 문서의 무엇인지 남기기
  - 절「두 색인의 결과 합치기」 — 소절: 텍스트 코사인 0.82와 이미지 검색 점수 0.61을 견줄 수 없는 이유 / 값 대신 순위로 합치기(RRF) — 색인을 하나 더 붙일 때 가중치를 다시 조율하지 않아도 된다 / 합친 뒤 재순위를 한 번 더 태우기 — 그림을 볼 수 있는 재순위 모델과 설명문 기준 재순위의 차이
  - 절「새로 생기는 실패 넷」 — 소절: 막대그래프 값을 눈대중으로 읽어 「약 340만 원」이라 답하는 것 — 정확한 수는 표나 원본 데이터로 되돌린다 / 비슷하게 생긴 도표 중 엉뚱한 것을 근거로 드는 것 — 답변에 자산 식별자를 밝히게 해 바로 확인시키기 / 스캔 OCR이 오류가 아니라 그럴듯한 다른 글자로 바뀌는 것 — 색인 시점에 OCR 신뢰도를 함께 저장 / 그림과 그 설명문이 상위를 함께 채우는 중복 — 대표를 정하거나 원본 자산 기준으로 제거
  - 절「비용이 붙는 자리」 — 소절: 네 항목을 색인 한 번짜리와 질의마다짜리로 갈라 보기 / 생성 단계의 이미지 토큰이 가장 크게 는다 — 검색은 상위 20개, 이미지로 넣는 것은 상위 3개로 따로 관리 / 문서 종류마다 해상도 기준을 정해 두기 — 추세만 보면 되는 도표와 작은 글자를 읽어야 하는 스캔은 반대다
  - 절「평가와 착수 순서」 — 소절: 답이 그림에만 있는 질의 30~50개를 따로 모아 검색 재현율 재기 — 전체 평가에 섞으면 텍스트 질의에 묻힌다 / 근거로 든 자산이 정답 자산인지 따로 세기 — 답만 맞았다면 우연이다 / 다섯 걸음 — 지금 색인에서 빠지는 것 세기 → 표부터 → 설명문 텍스트 경로 → 원본 이미지를 생성에 투입 → 그래도 남는 질의에만 공동 임베딩·페이지 통째 색인


### agents-rag

**합치기 — 짝이 남지 않았다(2026-09-08).** 83편 → 68편.


**채우기**

- `rag-agentic-rag` (지금 1,285자 · 절 5)
  - Self-RAG를 소절로 — 검색 필요 여부·근거 충분성·답변 지지도를 모델이 특수 토큰으로 스스로 판정하는 구조와 학습이 필요하다는 제약
  - CRAG(Corrective RAG)를 소절로 — 검색 결과를 정확/애매/틀림 셋으로 판정하고 틀림일 때 웹 검색으로 갈아타는 흐름
  - 질문 라우팅 — 단일 조회·비교·집계·최신 정보 넷을 갈라 다른 경로로 보내는 분류기와, 분류가 틀렸을 때의 회복
  - 반복을 언제 멈추는가 — 최대 홉 수, 근거 점수 문턱, 같은 쿼리 반복 감지, 예산 초과 시의 부분 답변
  - 실패 모드 — 검색 루프에 갇히는 경우, 매 홉마다 컨텍스트가 불어나 뒤쪽 근거가 묻히는 경우, 판정 모델이 자기 검색 결과를 과신하는 경우
  - 비용과 지연 — 고정 RAG 대비 호출 수가 몇 배로 늘어나는지와 홉당 상한을 두는 방법
  - 고정 파이프라인으로 충분한 자리 — 질문 형태가 좁은 사내 FAQ처럼 에이전트를 쓰면 손해인 경우

- `rag-evaluation` (지금 1,340자 · 절 6)
  - 검색 지표와 생성 지표를 갈라서 재는 이유 — Recall@k·MRR·nDCG로 검색을 먼저 재고 그다음에 답변을 재야 어디를 고칠지 나온다는 순서
  - 골든셋 만들기 — 실제 트래픽에서 질문을 뽑는 법, 정답 문서를 붙이는 법, 답할 수 없는 질문(unanswerable)을 일부러 섞는 이유, 몇 건이면 지표가 흔들리지 않는가
  - RAGAS 네 지표를 소절로 쪼개 각각 계산식과 실패 예 붙이기 — Faithfulness가 높은데 Answer Relevance가 낮은 경우, Context Recall만 낮은 경우 각각 무엇을 고치는가
  - LLM-as-Judge의 편향과 보정 — 위치 편향, 길이 편향, 자기 모델 선호, 사람 라벨과의 일치도를 재서 심판을 검증하는 절차
  - 오프라인 평가와 온라인 지표 — 클릭·재질문·상담원 이관율 같은 실사용 신호를 오프라인 점수와 대조하기
  - CI에 붙이는 회귀 평가 — 임베딩 모델·청크 크기·프롬프트를 바꿀 때마다 같은 골든셋을 돌려 점수 변화를 남기는 방식
  - 지표가 안 움직일 때 무엇을 보는가 — 골든셋이 쉬운 경우, 지표가 포화한 경우, 실패가 특정 문서 유형에 몰린 경우의 구분

- `rag-vs-finetuning` (지금 1,355자 · 절 5)
  - 판단을 세 축으로 정리 — 지식의 갱신 주기, 출처를 대야 하는가, 바꾸려는 것이 지식인가 행동·형식인가
  - 비용 모델을 숫자로 비교 — 파인튜닝은 학습 1회 + 서빙, RAG는 요청마다 검색·임베딩·긴 프롬프트. 월 요청 수가 어디를 넘으면 역전되는지
  - 파인튜닝으로 지식을 넣으면 생기는 것 — 환각의 자신감 증가, 갱신 불가, 어느 데이터에서 나온 답인지 추적 불가
  - 파인튜닝이 확실히 이기는 자리 — 출력 형식 고정, 도메인 말투, 분류·추출처럼 짧고 반복되는 작업, 지연·토큰을 줄여야 하는 경우
  - 둘을 겹쳐 쓰는 방식 — 검색 결과를 잘 쓰도록 파인튜닝하기(RAFT), 임베딩 모델만 도메인에 맞추기, 어느 쪽을 먼저 시도할지의 순서
  - 프롬프트만으로 되는지 먼저 확인하는 단계 — 셋 중 가장 싼 수를 건너뛰고 파인튜닝부터 잡는 실수
  - 잘못 고른 사례 두 개 — 매주 바뀌는 사내 규정을 파인튜닝한 경우, 말투 통일을 RAG로 풀려 한 경우

- `agent-llamaindex` (지금 1,444자 · 절 9)
  - 인덱스 유형 선택 기준을 소절로 — VectorStore·Summary·KnowledgeGraph·Tree 각각이 어떤 질문에 강한지와 색인 비용
  - Node와 메타데이터 — Node가 청크와 다른 점, 메타데이터를 임베딩에 넣을지 필터에만 쓸지, 관계(prev/next/parent)를 붙였을 때 얻는 것
  - Retriever와 Query Engine과 Chat Engine의 층위 — 어디까지가 검색이고 어디부터 생성인지, 직접 손댈 지점이 어디인지
  - 후처리(Postprocessor) — 리랭킹, 유사도 컷오프, 앞뒤 문맥 확장을 파이프라인 어디에 끼우는가
  - Workflow의 이벤트 모델 — 스텝과 이벤트로 나눴을 때 얻는 것, LangGraph의 상태 그래프와 무엇이 다른가
  - LlamaIndex 자체 평가 모듈 — Faithfulness·Relevancy 평가기와 합성 질문 생성기를 색인 튜닝에 쓰는 흐름
  - LangChain과 섞어 쓸 때의 경계 — 색인은 LlamaIndex, 에이전트는 LangGraph로 나눌 때 무엇이 겹치고 무엇이 새는가

- `vector-db-pgvector` (지금 1,601자 · 절 9)
  - pgvector를 고르는 진짜 이유 — 벡터와 업무 데이터가 같은 트랜잭션 안에 있다는 것, 조인과 외래키로 권한·테넌트를 거를 수 있다는 것
  - HNSW 파라미터가 재현율에 하는 일 — m과 ef_construction이 색인 시간·메모리에, ef_search가 질의 시간·재현율에 각각 어떻게 걸리는지
  - IVFFlat을 언제 쓰는가 — lists 수를 데이터 규모로 정하는 규칙, 데이터가 늘면 다시 만들어야 하는 이유
  - 하이브리드 검색 — tsvector 전문 검색 점수와 벡터 거리를 한 쿼리에서 합치는 법, RRF로 순위를 섞을 때의 SQL
  - 운영에서 실제로 겪는 것 — 색인 재구축 중의 잠금, 대량 인제스천 때 색인을 나중에 만드는 순서, VACUUM과 색인 팽창
  - 필터와 벡터 색인이 부딪히는 자리 — WHERE 조건이 좁을 때 색인을 안 타고 순차 스캔으로 떨어지는 문제와 부분 색인
  - 언제 전용 벡터 DB로 옮기나 — 벡터 수·QPS·재현율 요구를 기준으로 한 이관 신호

- `project-rag-from-scratch` (지금 1,750자 · 절 11)
  - 여덟 단계가 지금은 코드 사이 한 줄 설명뿐이다 — 단계마다 「왜 이 값인가」를 한 문단씩 붙인다(청크 512·오버랩 50을 그렇게 고른 근거 등)
  - 1~2단계 앞에 「무엇을 만들 것인지 정하기」 절 — 답해야 할 질문 유형, 문서가 몇 건인지, 정답을 어떻게 확인할지를 먼저 적는다
  - 3~5단계를 「색인 만들기」 한 절로 묶고 소절로 — 임베딩 모델 선택, FAISS 인덱스 종류, 메타데이터 붙이기
  - 6~7단계를 「답 만들기」 한 절로 — 프롬프트에 근거를 넣는 형식, 근거가 없을 때 모른다고 답하게 만드는 지시, 출처 표시
  - 8단계를 「고쳐 나가기」로 확장 — 처음 돌렸을 때 나오는 전형적 실패 셋(엉뚱한 청크, 근거는 맞는데 답이 벗어남, 답할 수 없는 질문에 답함)과 각각의 다음 수
  - 프레임워크 없이 만든 이 코드가 LangChain·LlamaIndex의 어느 부분에 해당하는지 대조하는 절 — 다음에 프레임워크로 옮길 때의 지도
  - 「실전 팁」 목록을 절로 승격 — 각 함정마다 증상 → 원인 → 확인 방법 순으로

- `rag-reranking` (지금 1,860자 · 절 7)
  - Bi-Encoder와 Cross-Encoder의 계산량 차이를 수로 — 문서 N건일 때 각각 몇 번의 인코딩이 필요한지, 그래서 왜 2단계로 나누는지
  - 1단계 후보 수(top-k)를 정하는 법 — k를 늘릴 때 재현율과 지연이 어떻게 움직이는지, 리랭커가 살릴 수 있는 상한
  - 리랭커 모델 고르기 — 오픈 모델과 API의 지연·비용·한국어 성능, 문서 길이 상한에 걸릴 때의 자르기
  - 도메인 리랭커 파인튜닝 — 클릭 로그나 골든셋으로 학습 쌍을 만드는 법과, 그만한 값을 하는 시점
  - 후기 상호작용(ColBERT류)과의 관계 — 2026-08의 rag-late-interaction과 어느 지점에서 갈라지는지
  - 지연 예산 배분 — 검색·리랭킹·생성에 각각 몇 ms를 줄지, 스트리밍으로 체감 지연을 줄이는 자리
  - 리랭킹이 효과 없는 경우 — 1단계 검색이 이미 정답을 못 데려온 경우, 문서가 다 비슷한 경우

- `vector-similarity-metrics` (지금 2,024자 · 절 6)
  - 절「지표가 정하는 두 가지」 — 소절: 같은 임베딩인데 순위가 갈리는 이유 / 인덱스가 지표에 묶여 나중에 못 바꾸는 이유 / 임베딩 모델이 어떤 지표로 학습됐는지를 먼저 보는 순서
  - 절「세 지표의 정의와 범위」 — 소절: 코사인 — 각도만 보고 크기를 버린다, −1~1 / 유클리드 — 직선 거리, 0~∞, 크기에 민감 / 내적 — 코사인 × 두 노름, 부호와 상한이 없다 / 같은 벡터 쌍에 셋을 손으로 계산해 나란히 놓기
  - 절「정규화가 만드는 등가」 — 소절: $$\|a-b\|^2 = 2-2\cos$$ 를 전개해 순위가 같아지는 것을 보이기 / 단위 벡터에서 내적 = 코사인 / 등가인데도 속도가 갈리는 이유 — 노름 계산이 남는가
  - 절「거리와 유사도의 부호」 — 소절: ANN 인덱스가 최소화 문제로 짜여 있다는 사실 / pgvector의 `<->`·`<#>`·`<=>`가 각각 무엇을 돌려주고 내적이 왜 음수로 오는지 / 정렬 방향을 뒤집어 상위가 하위로 오는 버그의 증상
  - 절「정규화를 빠뜨린 색인」 — 소절: 코사인 인덱스에 정규화 안 된 벡터를 넣었을 때의 조용한 저하 / 긴 문서의 노름이 커져 내적 검색 상위를 독차지하는 현상 / 색인 전에 노름 분포를 찍어 확인하는 한 줄
  - 절「크기가 의미인 경우」 — 소절: 추천에서 인기도를 노름에 담아 내적으로 함께 반영하기 / k-평균처럼 절대 거리가 목적 함수인 자리 / 그때도 검색용 벡터는 정규화해 따로 두는 이중 저장
  - 절「자기 데이터로 고르는 절차」 — 소절: 골든셋 하나로 세 지표를 돌려 상위 10의 겹침을 세기 / 지표를 바꾸면 재색인이 필요하므로 초기에 정하기 / 차이가 거의 없으면 가장 빠른 것을 고른다는 결론

- `rag-embedding-models` (지금 2,056자 · 절 6)
  - 차원 수가 하는 일 — 저장·검색 비용과 품질의 교환, Matryoshka(MRL) 절단으로 차원을 줄일 때 잃는 것
  - 한국어에서 갈리는 지점 — 토크나이저가 한국어를 어떻게 쪼개는지, 조사·띄어쓰기 흔들림에 얼마나 약한지, 영어 벤치마크 순위를 그대로 믿으면 안 되는 이유
  - 정규화와 거리 함수의 짝 — L2 정규화 후 내적과 코사인이 같아지는 이유, 색인 설정과 어긋났을 때 생기는 조용한 성능 저하
  - 쿼리와 문서에 다른 접두사를 붙이는 모델(E5·BGE) — 안 붙였을 때 얼마나 떨어지는지
  - 도메인 적응 — 임베딩을 파인튜닝할 시점과 그 전에 시도할 것(하이브리드·리랭킹)의 순서
  - 모델을 바꿀 때의 재색인 비용 — 문서 수 × 토큰으로 계산하고, 무중단으로 갈아 끼우는 절차
  - MTEB 읽는 법 — Retrieval 하위 과제만 봐야 하는 이유, 자기 골든셋으로 다시 재야 하는 이유

- `function-calling-reliability` (지금 2,105자 · 절 6)
  - 절「다섯 지점으로 나누기」 — 소절: 「도구 호출이 실패한다」가 뭉뚱그린 다섯 사건 / 모델을 바꿔 나아지는 두 줄과 코드가 고쳐야 하는 세 줄 / 호출마다 지점 태그를 남기고 일주일 뒤 분포표를 보는 절차
  - 절「도구 선택이 갈리는 자리」 — 소절: 도구가 있는데 안 부르고 지어내는 경우 — 설명문에 「언제 쓰는가」가 없다 / 비슷한 도구 둘 사이에서 흔들릴 때 서로를 가리키는 문장 / 반대 방향 실패 — 안 불러도 될 때 부른 오선택률을 함께 세기
  - 절「인자를 지어내는 조건」 — 소절: 모든 필드가 required면 「모른다」는 선택지가 없다 / `pattern`·`enum`으로 형식을 못 박고 `description`에 값의 출처를 적기 / 「값이 없으면 되묻는다」를 도구 설명문에 적기 / 인자 JSON 자체가 깨져 오는 파싱 실패의 처리
  - 절「반환값이 모델에게 하는 말」 — 소절: 스택 트레이스를 그대로 돌려줬을 때의 두 결말 / `ok`·`empty`·`error` 셋과 `empty`에 검색 범위·이유를 붙이기 / `hint` 한 줄이 결과 해석 실패를 줄이는 자리 / 사용자에게 보일 문자열과 모델용 문자열을 나누기
  - 절「듣는 재시도와 안 듣는 재시도」 — 소절: 같은 요청을 그대로 보내면 같은 값이 온다 / 검증 오류 문자열을 대화에 되먹이는 형태 / 상한 2회의 근거와 없을 때의 토큰 폭증 / 재시도해도 되는 실패와 안 되는 실패(부작용 있는 도구)
  - 절「루프를 끝내는 세 제동」 — 소절: 총 호출 수 8~12·벽시계·중복 호출 해시 / 막는 대신 「같은 조회를 이미 했다」를 결과로 돌려주는 편이 나은 이유 / 프롬프트의 「반복하지 마세요」는 경향이고 코드는 보장이라는 대비
  - 절「무엇부터 고칠지의 순서」 — 소절: 분포표에서 가장 큰 지점부터 집기 / 지점마다 다른 처방을 한 줄씩 정리 / 프롬프트 문장을 늘리는 것이 마지막에 오는 이유

- `prompt-zero-few-shot` (지금 2,124자 · 절 8)
  - 절「문맥 안에서 배우기」 — 소절: 파라미터를 안 바꾸고 패턴만 옮겨 가는 구조 / 예시 쌍이 이후 예측의 조건이 된다는 설명 / 파인튜닝·RAG와의 자리 구분 — 바꾸려는 것이 지식인가 형식인가
  - 절「예시 없는 프롬프트」 — 소절: Zero-shot이 잘 되는 세 조건(흔한 과제·익숙한 형식·짧은 지시) / 무너지는 세 조건(특수 출력 형식·도메인 용어·복잡한 추론) / 예시를 붙이기 전에 지시부터 고쳐 보는 순서
  - 절「예시 수와 수확 체감」 — 소절: 0→1에서 가장 크게 오르고 4~8에서 포화한다는 관찰 / 과제 복잡도별로 다른 최적 수 — 단순 분류 2~3, 복잡한 추론 5~8 / 예시가 늘 때 늘어나는 토큰에 월 요청 수를 곱해 본 값
  - 절「예시가 만드는 편향 셋」 — 소절: 다수 레이블 편향 — 긍정 5·부정 0으로 넣었을 때 / 최근성 편향 — 마지막 예시의 레이블로 쏠리는 것과 순서를 섞는 대응 / 형식 과적합 — 예시의 길이와 문체까지 따라 하는 것
  - 절「좋은 예시의 조건」 — 소절: 다양성·대표성·간결성을 각각 나쁜 예와 나란히 / 실제 입력의 잡음을 예시에도 남겨 두기 / 자주 틀리는 경계 사례를 고르고 잘하고 있는 것은 넣지 않기
  - 절「레이블보다 형식이 하는 일」 — 소절: 레이블을 뒤섞어도 성능이 크게 안 떨어졌다는 관찰(Min et al. 2022)과 그 해석 / 입력 분포·레이블 공간·형식 셋이 각각 기여하는 부분 / 「대충 써도 된다」로 읽으면 안 되는 이유 — 과제가 복잡할수록 레이블이 다시 중요해진다
  - 절「동적 선택과 캐시의 충돌」 — 소절: 입력과 가까운 예시를 임베딩으로 골라 넣는 구현과 중복을 피하는 다양성 항 / 예시가 매 요청 달라지면 프리픽스 캐시가 안 듣는다 / 고정 예시는 앞·동적 예시는 뒤에 두는 배치와 그 절충

- `project-agent-from-scratch` (지금 2,127자 · 절 11)
  - 절「무엇을 만들지 정하기」 — 소절: 이 에이전트가 답해야 할 질문 열 개를 먼저 적기 / 도구 셋을 그 열 개에서 거꾸로 뽑는 순서 / 「끝났다」의 정의를 코드 짜기 전에 정하기
  - 절「ReAct 한 바퀴의 실제 모양」 — 소절: Thought·Action·Observation이 API에서는 `tool_use`·`tool_result` 블록으로 나타난다 / 한 바퀴 돌 때 `messages`가 몇 개로 자라는지 손으로 세기 / 루프의 전부가 `while`과 `append`이고 그래서 남는 결정이 무엇인지
  - 절「도구 레지스트리가 드는 셋」 — 소절: 이름·JSON 스키마·실행 함수를 한 자리에 묶는 이유 / 스키마를 함수 시그니처에서 뽑을 때와 손으로 쓸 때의 차이 / 도구 결과를 문자열로 바꾸는 규칙을 한 곳에 두기
  - 절「메모리 둘의 경계」 — 소절: 단기 = `messages` 리스트 자체, 슬라이딩 윈도우로 자를 때 호출·결과 짝이 깨지는 자리 / 장기 = 세션을 넘겨 살아남는 것, 무엇을 승격시킬지의 기준 / 둘을 한 장치로 다루면 생기는 문제
  - 절「멈추는 조건 넷」 — 소절: 걸음 상한·토큰 상한·벽시계·중복 호출 해시 / 상한에 걸렸을 때 끊지 말고 지금까지의 결과를 정리하게 하기 / 상한 값을 실제 작업의 걸음 수 분포에서 정하기
  - 절「처음 돌릴 때의 함정 셋」 — 소절: 같은 도구 반복 — 인자 정규화 해시로 잡기 / 긴 Observation이 창을 먹는 것 — 도구 결과 길이 상한 / 시스템 프롬프트 없이 돌렸을 때의 장황함과 도구 남용
  - 절「히스토리를 읽는 디버깅」 — 소절: 실패한 작업의 `messages`를 그대로 출력해 걸음 순서로 읽는 절차 / 어느 걸음에서 계획이 틀어졌는지 찾는 표시 / 이 코드가 LangChain·LlamaIndex의 어느 부분에 해당하는지 대조

- `context-long-context-reality` (지금 2,143자 · 절 5)
  - 절「창에 넣는 것과 읽히는 것」 — 소절: 「들어간다」와 「쓰인다」가 다른 말이라는 것 / 광고되는 창 크기와 실효 창의 간격 / 예산을 뺀 뒤의 실제 자리 — 출력 상한·시스템 지시·이력을 먼저 뺀다
  - 절「위치에 따른 정확도」 — 소절: 같은 조각을 앞·중간·뒤로 옮겨 재면 나오는 U자 / 「20만 토큰에서 정확도 90%」를 평균으로 말할 수 없는 이유 / 순서가 통제 가능한 변수라면 반드시 통제한다는 결론
  - 절「바늘 찾기가 쉬운 이유」 — 소절: 심어 놓은 문장이 주변과 이질적이라 눈에 띈다는 점 / 바늘을 여럿 두거나 문맥에 녹여 넣으면 점수가 내려가는 것 / 벤더 점수를 참고 이상으로 쓰지 않는 이유
  - 절「먼저 무너지는 작업 유형」 — 소절: 사실 하나 찾기·전체 요약은 버티고, 흩어진 근거 셋 종합·전수 집계·모순 찾기가 먼저 무너진다 / 없는 것을 찾는 일이 특히 어려운 이유 / 이 다섯 유형에 자기 작업을 대응시켜 보는 표
  - 절「길이가 부르는 비용」 — 소절: 프리필이 길이에 민감해 첫 토큰까지의 시간이 먼저 늘어난다 / KV 캐시가 길이에 비례해 메모리를 먹어 동시 처리 수를 깎는 것 / 진짜 비용은 그 요청의 청구서가 아니라 시스템 처리량이라는 관점
  - 절「작업을 쪼개는 구성」 — 소절: 구간별 처리 → 중간 결과 → 마지막 판단의 두 단계 / 구간 처리를 병렬로 돌려 지연까지 줄이는 자리 / 구간을 넘는 관계는 중간 결과에 적어 두지 않으면 사라진다 / 검색과 어느 쪽을 고를지는 `context-retrieval-vs-longcontext`로 넘긴다는 경계
  - 절「자기 데이터로 재기」 — 소절: 길이 4단계(8K·32K·128K·전체) × 정답 위치 3곳의 격자 / 질문을 사실 하나·근거 셋 두 종류로 섞기 / 정확도와 첫 토큰까지의 시간을 같은 표에 남기기

- `prompt-engineering` (지금 2,156자 · 절 7)
  - 「6대 핵심 원칙」과 「10가지 패턴」이 지금은 목록으로만 있다 — 원칙마다 나쁜 프롬프트와 고친 프롬프트를 나란히 놓은 소절로 바꾼다
  - 지시가 무시되는 지점 — 부정형 지시, 서로 어긋나는 지시, 예시와 지시가 다를 때 무엇이 이기는지
  - 출력 형식을 강제하는 층위 — 프롬프트로 부탁하기, 예시로 보이기, 스키마로 못 박기 셋의 신뢰도 차이
  - 길이와 위치의 효과 — 중요한 지시를 앞에 둘 때와 뒤에 둘 때, 긴 프롬프트에서 가운데가 묻히는 현상
  - 이터레이션을 기록하는 법 — 무엇을 바꿨고 무엇이 좋아졌는지 남기는 최소 양식, 한 번에 한 가지만 바꾸는 규칙
  - 프롬프트로 풀 문제와 아닌 문제 — 지식이 없어서 틀리는 것, 데이터 구조가 잘못된 것, 도구가 없어서 못 하는 것은 프롬프트로 안 낫는다
  - 이 사슬 뒤의 글들(Zero/Few-shot·CoT·ToT·시스템 메시지)로 가는 지도를 마지막 절에 두기

- `function-calling-parallel` (지금 2,182자 · 절 5)
  - 절「순차 루프가 쓰는 시간」 — 소절: 도구 대기보다 모델 왕복이 더 큰 항이라는 것 / $$T_{\text{순차}}=(n+1)M+\sum t_i$$ 와 $$T_{\text{병렬}}=2M+\max t_i$$ 를 실제 값으로 비교 / 왕복이 줄면 재전송되는 프리픽스도 함께 준다는 두 번째 이득
  - 절「묶을 수 있는지 가르는 셋」 — 소절: 데이터 의존 — 뒤가 앞의 출력을 인자로 쓴다 / 실패 의존 — 데이터는 독립인데 앞이 실패하면 뒤를 하면 안 된다(결제·적립) / 순서 자체가 의미인 경우(재고 확인 → 주문) / 실무 기준 한 줄 — 읽기끼리 묶고 쓰기는 묶지 않는다
  - 절「실행기가 지는 책임」 — 소절: 부분 실패 — 하나가 죽어도 나머지 성공을 살려 돌려주기 / 결과 순서 — 완료 순이 아니라 요청 순으로 정렬하고 호출 id로 짝짓기 / 동시성 상한 4~6과 레이트리밋 / 개별 타임아웃과 전체 타임아웃을 따로 두기
  - 절「부작용 도구와 멱등성」 — 소절: 재시도와 병렬이 겹쳐 같은 쓰기가 두 번 나가는 경로 / 멱등 키를 인자에 두는 방식 / 데이터가 독립이어도 순차로 두는 편이 나은 경우의 판단
  - 절「모델이 안 묶는 세 원인」 — 소절: 도구 설명문이 순서를 암시하는 문장 / 도구가 지나치게 잘게 쪼개져 한 덩어리 작업으로 읽히는 것 / 시스템 프롬프트의 단계적 사고 지시와의 충돌 / 밀어 주는 한 문장과 그것을 넣기 전에 확인할 것
  - 절「이득이 없는 자리」 — 소절: 도구가 한둘뿐일 때 줄어드는 것과 늘어나는 복잡도 / 호출당 과금이 크거나 초당 요청 수에 민감한 외부 API / 값을 하는 조건 — 독립된 읽기 셋 이상 + 사용자가 기다리는 화면
  - 절「묶였는지 세어 보기」 — 소절: 응답마다 도구 호출 개수를 남겨 분포 보기 / 병렬 묶음의 실제 크기와 기대한 값의 차이 / 부분 실패율을 도구별로 나눠 보기

- `prompt-chain-of-thought` (지금 2,182자 · 절 8)
  - 절「중간 단계를 내보내는 구조」 — 소절: 자기회귀에서 앞서 쓴 추론이 뒤 토큰의 조건이 된다는 것 / 한 번에 답하는 것과 나눠 쓰는 것을 같은 문제로 비교 / 컨텍스트를 작업 메모리로 쓴다는 비유와 그 한계
  - 절「트리거 한 문장의 효과」 — 소절: 추론 유도 → 답 추출 두 단계로 나누는 구성 / 트리거 문구를 바꿔 가며 재는 실험과 문구 의존성 / 답만 뽑아 쓰려고 추론을 태그로 감싸는 형식
  - 절「예시로 추론을 가르치기」 — 소절: 예시에 답만 넣을 때와 풀이를 넣을 때 / 좋은 예시 조건 넷(다양성·명시적 단계·일관된 형식·4~8개) / Auto-CoT — Zero-shot으로 만든 추론을 예시로 재활용할 때 섞이는 오답을 걸러 내기
  - 절「효과가 갈리는 과제」 — 소절: 산술·상식·기호 추론에서 크고 단순 조회·분류에서는 없거나 마이너스 / 모델 크기에 따라 비선형으로 나타난다는 관찰 / 질문 유형을 먼저 갈라 CoT를 붙일지 정하는 라우팅
  - 절「그럴듯한 추론과 틀린 답」 — 소절: 초기 단계의 오류가 뒤로 전파되는 누적 / 쓰인 추론이 실제 계산 경로가 아닐 수 있다는 문제 / 계산은 실행기로, 코드는 실행으로 검증해 붙이는 도구 결합
  - 절「추론에 드는 값」 — 소절: 출력 토큰이 몇 배로 늘어나는 것과 그만큼 늘어나는 지연 / 추론을 사용자에게 보이지 않고 답만 내보낼 때의 처리 / 캐시가 안 듣는 부분 — 추론은 매번 새로 생성된다
  - 절「한 갈래에서 여러 갈래로」 — 소절: 같은 문제를 여러 번 풀어 다수결하는 방향 / 갈래를 펼쳐 탐색하는 방향 / 둘 다 비용이 배수로 붙는다는 공통점과 그래서 어디에만 쓰는가

- `context-system-prompt-design` (지금 2,344자 · 절 6)
  - 절「매 요청에 실려 가는 블록」 — 소절: 길이 × 요청 수로 계산한 고정 비용 / 캐시의 맨 앞이라 여기가 흔들리면 뒤가 전부 무효 / 유출을 전제로 쓴다 — 여기에 비밀을 두지 않는 원칙
  - 절「넣을 것과 넣지 말 것」 — 소절: 기준 하나 — 요청마다 값이 달라지는가 / 자주 어기는 둘 — 오늘 날짜와 사용자 이름 / 그런 값은 뒤쪽 상태 블록으로 옮기면 오히려 더 잘 지켜진다는 관찰
  - 절「변화 빈도 순으로 쌓기」 — 소절: 역할 → 규칙 → 형식 → 도구 원칙 → 개인화 순의 층 / 맨 위 한 줄이 아래 수천 토큰을 자정마다 무효화하는 계산 / 캐시 경계를 어디에 두고 그 아래를 얼마나 작게 유지할지
  - 절「금지가 아니라 대체 행동」 — 소절: 「추측하지 마세요」와 「못 찾으면 이렇게 답하세요」의 차이 / 형용사 대신 관찰 가능한 조건 — 「간결하게」 → 「세 문단 이내」 / 셀 수 없는 규칙은 회귀 평가에도 안 걸린다는 연결
  - 절「규칙 수와 준수율의 꺾임」 — 소절: 규칙이 스무 개를 넘으면 충돌하는 쌍이 생긴다 / 우선순위 한 줄과 예외를 해당 규칙 바로 아래 붙이기 / 열 번 시도해도 안 되는 규칙은 구조화 출력·후처리 검증으로 옮긴다
  - 절「예시 두셋이 하는 일」 — 소절: 문체·형식·거절 방식을 한꺼번에 보이는 짧은 대화 쌍 / 가장 자주 틀리는 경우를 고르고 잘하고 있는 것은 넣지 않기 / 예시가 많을 때의 형식 고착과 매 요청 실리는 토큰
  - 절「배포물로 다루기」 — 소절: 파일로 두고 버전 문자열을 응답 로그에 남기기 / 고정 질문 스무 개짜리 회귀 묶음을 나란히 놓고 비교하기 / 되돌리기 절차 — 한 줄 수정의 영향 범위가 전체라는 사실

- `context-compression` (지금 2,358자 · 절 7)
  - 절「손실 크기로 줄 세우기」 — 소절: 「얼마나 줄어드나」로 고르면 늘 요약이 이긴다는 함정 / 필드 선별·형식 교체·축약·요약 네 단계가 각각 잃는 것 / 위에서부터 쓰고 목표에 닿으면 멈춘다는 규칙
  - 절「쓰지 않는 필드」 — 소절: 화면을 그리려고 만든 응답과 모델이 실제로 보는 서너 개의 간격 / 허용 목록으로 짜야 하는 이유 — 금지 목록은 API가 필드를 늘릴 때 샌다 / 「혹시 필요하면?」의 답은 상세 조회 도구를 하나 더 두는 것
  - 절「같은 사실, 다른 형식」 — 소절: JSON 배열·공백 제거·마크다운 표·탭 구분의 절감폭과 성질 / 값이 짧고 필드가 많은 목록형에서 차이가 커지는 이유 / 값에 줄바꿈·구분자가 섞이면 표가 깨지는 것과 모델이 받은 형식을 흉내 내 출력하는 경로
  - 절「축약과 요약의 갈림」 — 소절: 최근 두셋은 전문, 그보다 오래된 도구 결과는 한 줄로 / 축약문에 「무엇이 있었는지」를 남겨 복구 경로를 여는 형태 / 「결과 생략」이라고만 적었을 때 모델이 조회 실패로 오해하는 것
  - 절「압축하면 안 되는 것」 — 소절: 제약·식별자·숫자·확정 사항 넷을 원문으로 남기는 이유 / 실패 기록을 지웠을 때 같은 시도를 반복하는 고리 / 손대지 않을 목록을 먼저 정하고 압축을 짜는 순서
  - 절「무엇이 사라졌는지 재기」 — 소절: 압축률 $$\rho = 1 - T_{\text{후}}/T_{\text{원}}$$ 만으로는 아무 판단도 못 한다 / 기법마다 따로 재기 — ①②는 차이가 없고 ③부터 작업 성격에 따라 갈린다 / 평균이 아니라 질문 유형별로 — 피해가 특정 유형만 통째로 틀리는 모양으로 나타난다
  - 절「조립 자리 한 곳」 — 소절: 압축이 여러 경로에 흩어지면 경로마다 다르게 줄어든다 / 어느 단계까지 갔는지를 호출부로 돌려주고 로그에 싣기 / 예산 초과 시의 강등 순서와 같은 사다리를 쓴다는 연결

- `vector-db-comparison` (지금 2,376자 · 절 8)
  - 절「벡터 DB를 DB로 만드는 것」 — 소절: 저장·ANN 색인·필터·스케일·API 다섯을 함께 갖춘다는 정의 / 라이브러리(Faiss)와 서비스의 경계 — 지속성·동시성·복제 / 관계형 DB에 벡터 열만 더한 것과 무엇이 다른가
  - 절「비교하기 전에 정할 축」 — 소절: 배포 형태(관리형·자체 운영·임베디드) / 필터링·하이브리드·멀티테넌시 지원 / 비용이 무엇에 붙는가 — 벡터 수·쿼리 수·인스턴스 시간 / 축을 안 정하고 벤치마크부터 보면 생기는 일
  - 절「관리형과 임베디드」 — 소절: Pinecone — 운영 부담이 없는 대신 오픈소스가 아니고 데이터가 밖에 있다 / Chroma — `pip install` 한 줄로 시작하고 수백만 벡터에서 갈리는 지점 / 프로토타입에서 프로덕션으로 넘어갈 때 실제로 바뀌는 것
  - 절「직접 운영하는 셋」 — 소절: Weaviate — 모듈과 멀티모달, GraphQL의 학습 곡선 / Milvus — 구성 요소가 많은 대신 초대규모와 인덱스 선택지 / Qdrant — Rust 기반, 페이로드 필터링과 단일 노드 배포의 단순함
  - 절「기존 인프라로 버티기」 — 소절: pgvector가 이기는 조건 — 벡터가 업무 데이터와 같은 트랜잭션에 있을 때 / 10만 벡터 언저리의 손익 / 옮겨야 할 신호 — 벡터 수·QPS·재현율 요구
  - 절「벤치마크를 읽는 법」 — 소절: 재현율을 고정하지 않은 QPS 비교는 의미가 없다는 것 / `ef`·`nprobe` 설정에 따라 곡선이 통째로 움직이는 것 / 자기 데이터와 자기 필터 조건으로 다시 재야 하는 이유
  - 절「옮길 때 드는 값」 — 소절: 재색인 비용을 문서 수 × 토큰으로 계산하기 / 필터 문법·하이브리드 구현이 제품마다 달라 다시 짜야 하는 부분 / 이중 쓰기로 무중단 이관하는 절차

- `context-multi-turn-management` (지금 2,391자 · 절 6)
  - 절「턴 수의 제곱으로 드는 값」 — 소절: $$T=\sum_{i}\sum_{j\le i} t_j$$ 를 전개해 $$\bar t\,n(n+1)/2$$ 로 보이기 / 턴이 두 배면 비용이 네 배라는 결과를 실제 요금으로 환산 / 캐시가 이 곡선을 누르지만 접는 순간 무효가 된다는 단서
  - 절「대화에서 실제로 부푸는 것」 — 소절: 사용자 메시지·모델 답·도구 인자·도구 응답 원문의 턴당 크기 비교 / 도구 응답이 대부분인데 다시 쓰이는 일은 드물다는 관찰 / 여기만 손봐도 사용자가 한 말은 하나도 안 잃는다는 점
  - 절「접는 시점 정하기」 — 소절: 턴 수 기준이 실제 압박과 어긋나는 이유 / 예산 비율 70% 언저리와 임계를 낮게 잡을 때의 캐시 손해 / 주제가 바뀌는 자리 — 손실은 가장 적지만 판단이 필요하다
  - 절「상태를 대화 밖에 두기」 — 소절: 확정된 사실만 담는 구조화 블록의 필드 설계 / 마지막 사용자 메시지 쪽에 붙이는 이유 — 캐시와 준수율 둘 다 / 추측을 넣으면 그것이 매 턴 다시 실려 사실로 굳는 경로
  - 절「접기에서 깨지는 셋」 — 소절: 도구 호출과 응답을 항상 짝 단위로 다루기 / 버린 자리에 식별자를 남겨 되찾을 길을 열기 / 경계를 위로 올리지 않기 — 요약을 매 턴 갱신하면 캐시가 한 번도 안 맞는다
  - 절「옛 지시와 새 지시의 충돌」 — 소절: 5턴의 「간결하게」와 20턴의 「자세히」가 한 창에 함께 있는 상태 / 요약이 옛 지시를 고정된 선호로 승격시키는 부작용 / 지시성 발화를 상태 블록의 한 필드로 옮겨 덮어쓰기
  - 절「길어지는 대화를 재기」 — 소절: 턴 수 분포와 턴당 증가량 $$g$$ 를 로그에 남기기 / 접기가 몇 턴마다 일어나는지와 캐시 적중률을 나란히 보기 / 스무 턴 뒤에도 답이 유지되는지 재는 고정 질문 묶음

- `vector-ann-algorithms` (지금 2,416자 · 절 9)
  - 절「정확한 답을 포기하는 이유」 — 소절: 100만 × 1536을 완전 탐색할 때의 연산 수와 실측 시간 / 재현율의 정의와 recall@10을 재는 절차 — 정답은 완전 탐색으로 만든다 / 재현율 몇 %를 포기하고 얻는 속도의 크기
  - 절「HNSW의 계층과 이웃」 — 소절: 작은 세계 그래프에서 탐욕 이동으로 답에 닿는 과정 / 상위 층은 거칠게 0층은 촘촘하게 — 층을 나눠 얻는 것 / `M`·`ef_construction`·`ef_search`가 재현율·메모리·질의 시간의 어디에 걸리는지 / 삭제가 어려운 이유와 묘비 누적
  - 절「IVF의 클러스터 분할」 — 소절: k-평균으로 `nlist`개로 나누고 `nprobe`개만 훑는 구조 / 클러스터 경계에 걸린 벡터를 놓치는 이유와 `nprobe`를 올릴 때의 곡선 / 학습이 필요하다는 성질 — 데이터가 늘면 다시 만들어야 한다
  - 절「LSH가 남아 있는 자리」 — 소절: 임의 초평면의 부호로 비트를 만드는 방식 / 재현율이 낮은 대신 학습이 없고 스트리밍에 붙는다는 성질 / 사전 필터 단계로 쓰는 구성
  - 절「메모리를 깎는 양자화」 — 소절: PQ·SQ·이진 양자화의 절감 배수와 재현율 손실 / 양자화로 후보를 뽑고 원본 벡터로 다시 재는 재점수 단계 / 어느 조합이 어디까지 견디는지 자기 데이터로 재기
  - 절「고르는 기준 셋」 — 소절: 삽입·삭제가 잦은가 — 동적 갱신이면 HNSW 쪽 / 메모리 예산 — 벡터 수 × 차원 × 4바이트에 그래프 몫을 더한 값 / 필터가 자주 걸리는가 — 좁은 조건에서 그래프가 끊기는 문제로 이어진다
  - 절「비교를 제대로 하는 법」 — 소절: 재현율을 고정하고 QPS를 비교해야 하는 이유 / 파라미터를 바꿔 가며 곡선을 그리는 측정 절차 / 알고리즘보다 임베딩 품질이 먼저라는 순서

- `context-chunk-ordering` (지금 2,424자 · 절 7)
  - 절「배치가 결정이 되는 조건」 — 소절: 조각이 여덟을 넘거나 하나가 길거나 관련도 편차가 클 때 / 셋 다 아니면 개수를 다섯으로 줄이는 편이 확실하다는 결론 / 검색기 출력을 그대로 잇는 한 줄도 결정이라는 자각
  - 절「세 배치와 그 근거」 — 소절: 순위 그대로 — 1위가 앞머리, 구현이 없다는 장점 / 역순 — 1위를 질문 바로 앞에, 근거 하나로 답하는 질의에 맞음 / 샌드위치 — 홀짝으로 채워 상위를 양 끝에, 근거 여럿을 종합할 때 / $$\hat S=\sum s_i\,w(p_i)$$ 로 셋을 같은 식의 다른 해로 보기
  - 절「조각 사이의 관계」 — 소절: 원문에서 이웃한 조각을 떼어 놓으면 「이 경우」가 엉뚱한 곳에 붙는다 / 문서 단위로 묶은 뒤 묶음끼리 배치하는 구현 / 회의록·로그처럼 시간이 정보인 자료는 고르는 일과 늘어놓는 일을 분리한다
  - 절「경계와 머리말」 — 소절: 구분이 흐릿하면 두 문서의 값이 섞여 나오는 증상 / 머리말에 넣을 셋 — 어느 문서·어느 부분·언제 것인지 / 갱신일이 있으면 새 판을 고르고 없으면 앞엣것을 고른다 / 번호를 붙여 인용과 자동 검증에 쓰기
  - 절「중복을 먼저 걷어 내기」 — 소절: 겹치게 자른 청킹과 여러 문서에 복사된 문단이 만드는 중복 / 같은 말이 여러 번 나오는 것 자체가 그 내용을 무겁게 만든다는 부작용 / 이미 고른 것과의 최대 유사도로 거르되 0.9 언저리에서 시작하기 / 버린 것을 로그로 남겨 「왜 안 들어갔지」를 추적하기
  - 절「재순위와 배치의 분담」 — 소절: 무엇을 넣을지 고르는 일과 넣은 것을 덜 낭비하는 일 / 순서로는 재순위가 먼저라는 이유 / 정답 조각이 애초에 안 뽑혔는데 배치만 몇 주 만지는 상황
  - 절「배치를 재는 절차」 — 소절: 개수를 고정하고 배치만 바꿔 세 벌을 돌리기 / 질문을 근거 하나·근거 여럿으로 나눠 따로 집계 / 정답이 몇 번 조각에 있었는지를 함께 기록해 검색 문제와 배치 문제를 가르기

- `context-retrieval-vs-longcontext` (지금 2,438자 · 절 5)
  - 절「선별의 주체와 시점」 — 소절: 요청 전에 우리 코드가 고르는 것과 요청 후에 모델이 어텐션으로 고르는 것 / 놓치는 방식이 다르다 — 못 찾는 것과 창 안에 있는데 못 보는 것 / 근거 표시가 조각 단위로 가능한 쪽과 아닌 쪽
  - 절「선택을 강제하는 조건」 — 소절: 사용자마다 접근 권한이 다르면 고르는 단계가 반드시 있어야 한다 / 문서가 시시각각 바뀌면 재색인이 늘 뒤처진다 / 감사·인용이 요구되는 도메인에서는 조각 단위 근거가 필요하다
  - 절「비용이 갈리는 지점」 — 소절: $$C_{\text{긴문맥}}=N D c$$ 와 $$C_{\text{검색}}=N R c + C_{\text{색인}}$$ 를 실제 값으로 / 캐시가 붙으면 $$D c_h$$ 와 $$R c$$ 가 비슷해지는 구간과 캐시의 까다로운 조건 / 비용이 뒤집혀도 지연은 안 뒤집힌다는 별개의 축
  - 절「무관한 비율이 정하는 정확도」 — 소절: 관련 문서 한 편 전문·비슷한 문서 서른 편·무관한 문서가 섞인 것 세 경우 / 판단 기준이 「창에 들어가는가」가 아니라 「무관한 비율이 얼마인가」 / 문서 한 편 질의응답에서는 조각내는 것이 오히려 손해인 자리
  - 절「섞어 쓰는 세 방식」 — 소절: 넓게 찾아 통째로 넣기 — 검색의 역할이 정답 찾기에서 무관한 것 버리기로 바뀐다 / 캐시 위에 검색 얹기 — 고정 자료를 앞, 질문별 검색을 뒤 / 두 단계로 나누기 — 관련 위치를 먼저 뽑고 그 부분만 다시 넣기
  - 절「재야 하는 네 값」 — 소절: 질문당 관련 문서 수의 분포와 상위 백분위 / 같은 문서에 오는 질문 수 — 1에 가까우면 검색이 손해 / 캐시 적중률과 검색 상위 $$k$$ 의 재현율 / 재현율이 낮은데 창을 키우면 오히려 나빠지는 순서 오류
  - 절「한 시스템에 세 경로」 — 소절: 질문 유형으로 경로를 가르는 라우팅 / 어느 경로로 갔는지를 로그에 남겨 나중에 다시 판단하기 / 경로별로 비용·지연·정확도를 따로 집계하기

- `embedding-sentence` (지금 2,450자 · 절 8)
  - 절「문장 하나를 벡터 하나로」 — 소절: 토큰마다 벡터가 나오는 것과 문장 하나로 접는 것의 차이 / 크로스 인코더로 N개를 비교할 때의 $$O(N^2)$$ 를 1만 문장에 대입 / 바이 인코더가 바꾸는 것 — 색인은 미리 한 번, 질의는 한 번
  - 절「풀링만 바꿔 쓸 때의 한계」 — 소절: `[CLS]`가 문장 의미를 담도록 학습되지 않았다는 사실 / 평균 풀링이 조금 낫지만 여전히 의미 유사도와 안 맞는 이유 / 학습 없이 얻은 벡터의 유사도가 좁은 구간에 몰리는 현상
  - 절「샴 구조와 학습 목표」 — 소절: 가중치를 공유한 두 인코더가 만드는 제약 / NLI 분류·STS 회귀·트리플렛 셋이 각각 벡터 공간에 넣는 성질 / 레이블 없이 대조 학습으로 가는 갈래(SimCSE)와 그 배치 구성
  - 절「대칭 검색과 비대칭 검색」 — 소절: 문장 쌍 유사도로 학습한 모델을 질문–문서 검색에 쓸 때의 어긋남 / 쿼리와 문서에 다른 접두사를 요구하는 모델과 안 붙였을 때의 손해 / 색인과 질의에 같은 모델·같은 전처리를 쓰는 규칙
  - 절「한국어에서 고르기」 — 소절: 조사가 붙는 언어에서 토크나이저가 갈리는 지점 / 한국어 SBERT 모델들의 학습 데이터 차이(KLUE NLI·STS, 대조 학습) / 영어 벤치마크 순위를 그대로 옮기면 안 되는 이유
  - 절「검색에 얹는 순서」 — 소절: 사전 계산 → 정규화 → 색인 세 걸음과 각 걸음의 함정 / 완전 탐색으로 시작해 근사 탐색으로 옮기는 시점 / 상위 후보만 크로스 인코더로 다시 재는 2단계 — 처음에 버렸던 것이 여기서 돌아온다
  - 절「품질을 재는 두 지표」 — 소절: STS 상관과 검색 재현율이 서로 다른 것을 잰다는 구분 / 중복 탐지·군집처럼 임계값이 필요한 응용에서 임계를 정하는 절차 / 자기 데이터로 만든 골든셋이 공개 점수를 이기는 이유

- `context-window-budgeting` (지금 2,481자 · 절 6)
  - 절「창을 항목으로 나누기」 — 소절: 시스템 지시·도구 스키마·이력·근거 문서·출력 상한·안전 여유 여섯 칸 / 출력 상한을 먼저 빼야 하는 이유 — 답이 중간에 끊기는 실패는 되돌릴 수 없다 / 칸마다 상한을 두고 남는 자리를 어디에 줄지 정하기
  - 절「토큰 추정의 오차」 — 소절: 글자 수 나누기·다른 토크나이저·해당 토크나이저·계산 API 넷의 정확도와 비용 / 한국어에서 편차가 커지는 자리 — 고유명사와 코드가 섞일 때 / 추정과 실제를 함께 로그로 남겨 $$\epsilon$$ 분포를 만들고 안전 여유를 백분위로 정하기
  - 절「강등 순서를 코드에」 — 소절: 되돌리기 쉬운 것부터라는 원칙과 압축 사다리의 재사용 / 조립 자리를 한 곳에 두지 않으면 경로마다 다르게 버린다 / 몇 단계까지 강등했는지를 호출부로 돌려주는 반환값
  - 절「잘리지 않는 집합」 — 소절: 시스템 지시·최초 과제 서술·사용자 제약·마지막 메시지·직전 도구 짝 / 토큰이 큰 순서로 자를 때 호출만 남고 결과가 사라지는 사고 / 불가침 집합의 크기를 미리 재 두고 그것만으로 넘치는 요청을 앞에서 거르기
  - 절「모델별 예산표」 — 소절: 창 크기만이 아니라 출력 상한·토크나이저·캐시 조건이 함께 다르다 / 문서 상한을 창 크기에 비례해 늘리면 안 되는 이유 / 창 큰 모델은 넘침을 막는 수단이지 품질을 올리는 수단이 아니라는 구분
  - 절「강등을 남기는 지표」 — 소절: 어느 단계까지 갔는가·무엇이 빠졌는가·추정과 실제의 차이 셋 / 경로별 강등 단계 분포로 예산 설계가 틀린 경로 찾기 / 전부 ①에서 끝나면 여유가 과하다는 반대 신호
  - 절「나누는 것이 답인 자리」 — 소절: 억지로 밀어 넣으면 근거 없는 답이 나온다는 결말 / 장별로 나눠 처리하고 합치는 구성 / 사용자에게 되묻는 문구를 미리 정해 두기

- `context-summarization-memory` (지금 2,511자 · 절 6)
  - 절「접기와 기억하기」 — 소절: 창을 줄이는 압축과 창 밖에서 살아남는 저장의 차이 / 한 장치로 다루면 「지난주에 정한 배포 정책」이 요약 세 번을 거쳐 사라지는 경로 / 두 장치를 그림 한 장으로 갈라 두기
  - 절「접는 시점과 캐시」 — 소절: 토큰 임계·턴 수·작업 경계 셋의 성질과 조합 / 요약이 이력 앞부분을 갈아 끼우므로 그 순간 프리픽스가 무효가 된다 / $$n=\lfloor B/g \rfloor$$ 로 다음 무효화까지의 턴을 세고 임계를 늦게 잡는 근거
  - 절「보존 목록을 지시로 못 박기」 — 소절: 읽기 좋은 서사가 아니라 이후 대화가 딛고 설 발판이라는 목표 / 수치·식별자·제약·결정과 그 이유를 원문 표현으로 남기라는 지시 / 결론이 안 난 논의를 「~하기로 했다」로 굳히는 매끄러움을 막는 한 줄 / 요약을 넣을 자리 — 시스템 블록 뒤, 최근 턴 앞
  - 절「요약의 요약이 잃는 것」 — 소절: $$S_k \approx r^k$$ — $$r$$ 이 0.9여도 다섯 번이면 0.59 / 잃는 것이 고르지 않다 — 구체적인 값과 예외 조건이 먼저 떨어진다 / 계층 요약만으로 버티는 설계가 수렴하는 상태
  - 절「항목으로 된 메모리」 — 소절: 산문은 부분 수정이 안 되고 목록은 된다는 차이 / `confidence` — 사용자가 말한 것과 모델이 짐작한 것을 섞지 않기 / `expires` — 유효 기간 없는 메모리가 틀린 사실 창고가 되는 것 / 요약과 같은 호출에서 항목을 함께 뽑아 호출을 하나로 끝내기
  - 절「골라 넣는 회상」 — 소절: 제약·선호는 전부, 결정·사실은 관련된 것만, 실패 기록은 같은 도구를 쓸 때만 / 제약을 유사도 검색에 맡기면 안 되는 이유 / 항목이 수백 개가 되면 창을 아끼려던 장치가 창을 먹는다
  - 절「무엇을 잃었는지 확인하기」 — 소절: 접기 전에 답할 수 있던 질문 목록을 만들어 접은 뒤 다시 던지기 / 먼저 무너지는 순서 — 결정의 이유, 실패 기록, 그다음 수치 / 무너지는 유형을 메모리 항목으로 승격시키는 것이 요약 프롬프트를 다듬는 것보다 확실하다

- `agent-failure-modes` (지금 2,524자 · 절 6)
  - 절「한 걸음의 여섯 자리」 — 소절: 이해·계획·도구 선택·인자 채우기·결과 해석·종료 판단 각각이 하는 일 / 처방이 자리마다 다르다는 것 — 프롬프트로 낫는 것은 위쪽 둘뿐이다 / 마지막 걸음을 먼저 보는 습관이 왜 틀리는가
  - 절「증상에서 자리로 거슬러 가기」 — 소절: 여섯 증상과 각각의 확인 절차(트레이스에서 무엇을 열어 보는가) / 같은 증상이 다른 자리에서 오는 경우 — 「절반만 하고 끝냄」의 두 원인 / 짐작하지 말고 확인한 다음 고치는 순서
  - 절「오류 없이 틀리는 실패」 — 소절: 0건을 「없음이 확인됨」으로 읽고 그 위에 쌓는 전형적 경로 / 「찾아봤고 없다」와 「질의가 이상해 못 찾았다」가 같은 값인 도구 / 처방은 프롬프트가 아니라 도구 쪽 — 이유·구분·신뢰도를 함께 돌려주기
  - 절「진척으로 잡는 루프」 — 소절: 오류 횟수로는 안 잡힌다 — 모든 호출이 성공하며 도는 경우 / 작업 기록의 진척, 인자 정규화 해시, 계획 항목의 완료 수 세 신호 / 잡은 뒤 바로 멈추기 전에 스스로 빠져나올 기회를 한 번 주기
  - 절「회복의 다섯 단계」 — 소절: 호출·걸음·도구 선택·계획·되묻기 각각이 어디로 돌아가는가 / 「계획을 다시 세운다」가 없으면 틀린 계획의 다음 항목을 성실히 수행한다 / 되묻는 자리를 미리 정해 두기 — 애매한 요청을 혼자 반복해 푸는 것이 가장 나쁜 조합
  - 절「실패를 자료로 쌓기」 — 소절: 실패마다 자리 태그를 남겨 분포를 만들기 / 분포가 바뀌면 무엇이 바뀐 것인지 — 모델·프롬프트·도구 / 실패 작업을 회귀 시험 묶음으로 옮기는 절차
  - 절「고치기 전에 재현하기」 — 소절: 시작 상태·걸음별 입출력·바깥 응답 셋이 있으면 다시 돌릴 수 있다 / 고친 프롬프트가 그 상황을 푸는지 같은 자료로 확인하기 / `agent-anti-patterns`와의 경계 — 짜기 전에 피하는 것과 돌린 뒤 찾는 것

- `vector-search-basics` (지금 2,538자 · 절 8)
  - 절「낱말이 겹쳐야 찾히는 검색」 — 소절: 역색인이 하는 일과 어휘 불일치가 생기는 자리 / 동의어·의도·언어 세 종류의 실패를 예로 / 동의어 사전으로 메우다 한계에 닿는 이유
  - 절「거리로 바꾼 의미」 — 소절: 의미의 유사성을 벡터 공간의 거리로 옮기는 발상 / 이 대응이 깨지는 자리 — 반의어가 가깝게 놓이고 부정문이 잘 안 잡힌다 / 벡터 검색이 「비슷한 것」을 찾지 「맞는 것」을 찾지 않는다는 성질
  - 절「색인 단계의 네 걸음」 — 소절: 수집 → 청킹 → 임베딩 → 저장과 각 걸음에서 정할 값 / 청크 크기와 겹침을 정하는 기준, 너무 크거나 작을 때의 증상 / 원문과 메타데이터를 벡터 옆에 함께 두어야 하는 이유
  - 절「질의 단계와 지켜야 할 짝」 — 소절: 질의를 같은 모델·같은 전처리로 벡터화해야 하는 규칙과 어겼을 때의 조용한 저하 / 상위 $$k$$ 를 정하는 법과 점수 임계로 자르기 / 모델을 바꾸면 전체 재색인이 필요하다는 사실
  - 절「전부 비교하지 않는 이유」 — 소절: 100만 × 1536을 완전 탐색할 때의 연산 수와 시간 / 근사 탐색이 포기하는 것과 얻는 것 / 처음에는 완전 탐색으로 정답을 만들어 두고 시작하는 순서
  - 절「벡터가 못 하는 것」 — 소절: 제품 코드·고유명사처럼 정확한 문자열이 필요한 질의 / 의료·법률 같은 전문 도메인에서 범용 모델이 무너지는 자리 / 키워드 점수와 벡터 점수를 순위로 섞는 하이브리드
  - 절「도입을 판단하는 자리」 — 소절: 시맨틱 검색·RAG·추천·중복 탐지 넷의 공통 조건 / 정확 일치·구조화 필터·소규모 데이터에서는 기존 방식이 낫다는 반대편 / 붙이기 전에 준비할 것 — 골든셋과 지금 검색의 재현율

- `tool-schema-design` (지금 2,557자 · 절 6)
  - 절「스키마가 곧 프롬프트」 — 소절: 모델이 볼 수 있는 것은 텍스트뿐이고 함수 본문은 안 보인다 / 도구 목록이 매 요청 고정분으로 실려 가는 크기를 실제로 세어 보기 / API 문서를 그대로 옮겨 붙였을 때 생기는 문제
  - 절「이름이 좁히는 후보」 — 소절: 이름을 먼저 보고 설명문은 후보를 좁힌 뒤에 읽는다 / 동사 + 구체적 대상 꼴과 나쁜 이름 다섯 / `do_refund_and_notify`는 이름 문제가 아니라 설계 문제 — 부작용이 다르면 나눈다
  - 절「경계를 적는 설명문」 — 소절: 무엇을 하는지보다 무엇을 못 하는지가 빠진다 / 범위·제외 항목·대안 도구 셋을 넣는 형태 / 서로를 가리켜 주면 비슷한 도구 사이에 경계가 생긴다는 것 / 선택 인자에 「비워도 된다」를 명시해야 실제로 비운다
  - 절「제약으로 옮길 수 있는 말」 — 소절: `enum`·`minimum`·`format`·`pattern`·`default`로 옮기는 대응표 / 옮기면 짧아지면서 오히려 정확해지는 이유 / 설명문에 남길 것 — 값의 출처, 쓰지 말아야 할 상황, 다른 도구와의 관계
  - 절「도구 개수의 구간」 — 소절: 10개 이하·10~30·30개 이상에서 각각 나타나는 증상 / 접두사로 묶기와 겹치는 도구 합치기 / 단계별 노출과 계층 구조 — 한 번에 판단할 선택지 수를 줄인다는 공통점 / 목록을 대화 중간에 바꾸면 그 지점부터 캐시가 깨진다는 대가
  - 절「반환값의 설계」 — 소절: 도구 결과가 이력에 남아 다음 턴에도 계속 실린다는 점 / 필요한 필드만 돌려주기 — 여기서 아낀 토큰은 반복해서 아껴진다 / 빈 결과에 말을 붙여 「없음」과 「조회 실패」를 가르기
  - 절「고친 뒤 재는 두 값」 — 소절: 써야 할 질문 20개와 쓰면 안 될 질문 20개의 묶음 / 선택률과 오선택률을 함께 봐야 하는 이유 — 「적극적으로 사용하세요」는 둘 다 올린다 / 스키마 수정을 프롬프트 수정과 같은 절차로 다루기

- `context-prompt-caching` (지금 2,583자 · 절 6)
  - 절「캐시에 담기는 것」 — 소절: 프리필과 디코드를 나누고 길어질 때 무거워지는 쪽 짚기 / 저장되는 것이 문장이 아니라 키·값 벡터라는 것 / 여기서 따라 나오는 두 성질 — 앞에서부터만, 토큰 단위로 정확히
  - 절「캐시를 깨는 한 줄들」 — 소절: 현재 시각·사용자 id를 앞에 둔 경우 / 도구 목록을 딕셔너리 순회로 만들어 순서가 흔들리는 경우 / 직렬화 옵션이 호출부마다 달라 공백·키 순서가 갈리는 경우 / 여섯 사례가 전부 「바뀌는 것을 앞에 두었다」의 변형이라는 정리
  - 절「배치 순서를 코드로」 — 소절: 메시지를 만드는 자리를 한 함수로 모으고 순서를 못 박기 / `sorted(TOOLS)` 한 줄의 방어 / 렌더링 함수를 한 곳에 두어야 하는 이유
  - 절「손익이 갈리는 지점」 — 소절: 쓰기에 웃돈이 붙고 읽기가 싸지는 구조에서 몇 회 재사용해야 이득인지 / 유효 시간이 짧아 트래픽이 뜸한 경로에서는 매번 쓰기만 하는 상태 / 최소 길이에 못 미치는 프리픽스는 아예 캐시되지 않는다는 조건
  - 절「에이전트 루프의 역설」 — 소절: 뒤에만 붙는 모양이라 캐시에 아주 유리하다는 점 / 앞에서부터 잘라내면 자를 때마다 그 뒤가 전부 미스가 되는 역전 / 자르는 빈도를 낮추기·자른 자리를 요약으로 고정하기·애초에 짧게 받기 셋
  - 절「히트율을 세어 보기」 — 소절: 응답 메타데이터의 캐시 읽기·쓰기 토큰을 로그로 남기기 / $$H=T_r/(T_r+T_w+T_f)$$ 를 경로별로 찍어 새는 자리 찾기 / 0.2에 머무는 경로와 0.8을 넘는 경로에 각각 무엇을 할지
  - 절「캐시를 전제로 한 설계」 — 소절: 시스템 지시·도구 스키마·고정 예시를 앞에 모으는 순서 / 개인화와 검색 결과를 뒤로 보내는 배치 / 캐시를 살리려다 품질을 깎는 자리 — 청크 순서를 고정할 때의 맞바꿈

- `rag-cost-latency-tuning` (지금 2,659자 · 절 6)
  - 절「한 질의를 단계로 쪼개기」 — 소절: 임베딩·검색·재순위·생성 네 구간의 시간 비율 / 검색 전부를 반으로 줄여도 전체가 거의 안 준다는 계산 / 검색 안쪽에서는 재순위가 3분의 2를 쓴다는 것
  - 절「재순위를 빼면 안 되는 이유」 — 소절: 재순위가 있으면 근거를 열에서 셋으로 줄이고도 품질이 유지된다 / 근거 일곱이 빠져 생성에서 돌려받는 시간이 재순위에 쓴 시간보다 크다는 계산 / 한 단계만 보고 판단할 때 이 맞바꿈이 안 보인다는 것
  - 절「근거 토큰의 지분」 — 소절: 질문 30·지침 800·근거 6,000·답 400으로 세어 본 한 질의 / 88%가 근거이므로 개수를 줄이는 것이 가장 큰 절약이라는 결론 / 그냥 줄이면 답이 나빠지므로 재순위가 앞에 필요하다는 순서
  - 절「대가 없는 세 수단」 — 소절: 흘려 보내기 — 총 시간은 그대로인데 체감이 바뀐다 / 캐시 순서 정리 — 고정된 것을 앞, 바뀌는 것을 뒤 / 근거 청크를 문서 식별자순으로 붙여 자리를 고정하기와 그 품질 맞바꿈 / 같은 질문이 반복되는 서비스에서 답 자체를 캐시하는 층
  - 절「품질과 맞바꾸는 수단들」 — 소절: 근거 개수·청크 길이·모델 크기 셋이 각각 치르는 대가 / 평가 없이 손대면 안 되는 이유와 최소 평가 묶음 / 싼 것부터 시도하는 순서
  - 절「두 시간을 나눠 재기」 — 소절: 첫 글자까지의 시간은 입력이, 전체 시간은 출력이 정한다 / 각각에 대응하는 처방 — 근거 토큰·캐시 적중률 대 답 길이 지침·모델 크기 / 한 값만 보고 있을 때 엉뚱한 쪽을 만지게 되는 예
  - 절「꼬리와 단계별 기록」 — 소절: 평균 2.8초가 쓸모없는 이유와 p95 / 꼬리가 길어지는 원인이 평균과 다른 자리들 / 네 단계 시간 + 입력 토큰 + 근거 개수를 함께 남기기 / 목표를 먼저 정해야 위 표의 어느 줄까지 감수할지 정해진다

- `agent-observability` (지금 2,680자 · 절 6)
  - 절「같은 입력에 다른 길」 — 소절: 일반 백엔드는 다시 돌리면 복원되는데 에이전트는 아니라는 차이 / 남은 것이 최종 응답 한 줄일 때 답할 수 없는 질문들 / 무엇을 남길지가 곧 나중에 무엇을 물을 수 있는지라는 성질
  - 절「세 겹으로 쪼개기」 — 소절: 작업 = 트레이스, 걸음 = 구간, 그 안에 모델 호출과 도구 호출 / 걸음 층이 있어야 「몇 번째에서 틀어졌나」를 물을 수 있다는 것 / 길이로 느린 자리가, 위치로 실패와 회복이 보이는 이득
  - 절「구간에 붙일 것」 — 소절: 작업·걸음·모델 호출·도구 호출 각각의 필수 항목 / 모델 버전과 프롬프트 버전이 가장 자주 빠지고 가장 아쉬운 자리 / 도구 인자를 남겨야 「같은 질의를 조금씩 바꿔 반복」이 보인다는 것
  - 절「전문 보존의 절충」 — 소절: 다 남기면 저장 비용과 개인정보, 안 남기면 재현 불가라는 양쪽 / 전문은 짧게·지표는 길게, 실패한 작업만 보존 기간을 따로 길게 / 마스킹하되 무엇이 지워졌는지 표시를 남기기 / 트래픽이 크면 표본만 전문 저장하고 실패는 전량
  - 절「세는 값 여섯」 — 소절: 작업 성공률의 정의를 정하는 것이 절반의 일 / 걸음 수와 작업당 비용은 평균이 아니라 분포 — 95백분위 하나로 잡히는 문제 / 도구별 실패율, 사람 개입률, 상한에 걸려 멈춘 비율
  - 절「변화에 거는 알림」 — 소절: 절대 기준을 걸면 늘 켜져 있고 곧 아무도 안 본다 / 지난주 대비 배수로 거는 네 가지 / 작업당 평균 비용이 이틀 연속 오르는 것이 조용한 사고를 가장 잘 잡는다
  - 절「재현이라는 목표」 — 소절: 시작 상태·걸음별 입출력·바깥 세계의 응답 셋 / 실험 환경에서 그대로 다시 돌려 고친 프롬프트를 확인하는 흐름 / 모은 실패 작업이 그대로 회귀 시험 자료가 된다는 이득

- `rag-metadata-filtering` (지금 2,683자 · 절 6)
  - 절「두 종류의 조건」 — 소절: 뜻으로 찾는 것과 맞거나 틀리는 것이 한 질문에 섞여 있다는 것 / 벡터가 맡을 몫과 필터가 맡을 몫을 가르는 기준 / 순서만 바꿔도 결과가 달라진다는 예고
  - 절「사전 필터링과 사후 필터링」 — 소절: 사후 — 구현이 쉽고 저장소가 필터를 몰라도 되지만 개수가 안 채워진다 / 통과율 2%일 때 상위 50개 중 평균 한 개만 남는다는 계산 / 51등에 있던 좋은 후보를 알 길이 없다는 더 나쁜 성질 / 사전 — 개수는 채워지지만 다른 값을 치른다
  - 절「선택도와 끊긴 그래프」 — 소절: 근사 탐색이 이웃 그래프를 걸어 답에 닿는다는 구조 / 조건을 통과 못 한 노드가 빠지면 그 길목도 함께 사라진다는 것 / 30% 이상·1~30%·1% 미만 세 구간에서 저장소가 갈아타는 방식 / 가운데 구간이 가장 비싼 이유
  - 절「필터로 쓸 값의 설계」 — 소절: 갈래가 적은 값을 쓰고 자유 문장·제목은 검색 대상으로 남긴다 / 시각은 `2026-08`처럼 월로 잘라 따로 넣고 정확한 시각은 정렬·표시에만 / 나중에 쓸지 모르는 값도 지금 넣는다 — 색인이 만들어진 뒤의 재작업 비용
  - 절「조건별 선택도를 재 두기」 — 소절: 사용자 한 명으로 좁히는 조건과 「문서 종류: 계약서」로 좁히는 조건의 자릿수 차이 / 조건 조합의 선택도를 미리 세어 두고 느려지는 구간을 알아 두기 / 사전·사후를 강제 지정하기보다 저장소에 맡기는 편이 나은 이유
  - 절「자연어를 조건으로 옮기기」 — 소절: 질의 재작성 단계에 조건 추출을 함께 붙이는 자리 / 모델이 만든 조건은 후보일 뿐 — 허용 목록에 대조한 뒤 쓰기 / 0건이면 조건을 하나씩 떼며 재시도하고 「부서 조건은 빼고 찾았습니다」라고 밝히기
  - 절「권한을 거는 필터」 — 소절: 빠뜨렸을 때·정하는 주체·0건일 때 셋이 전부 다르다는 표 / 「0건이면 조건을 떼고 다시」를 권한에 적용하면 곧바로 유출 / 권한 조건은 검색 함수 안쪽에서 무조건 덧붙이고 모델은 손대지 않는 통로 분리

- `rag-chunking-strategies` (지금 2,744자 · 절 9)
  - 절「조각 크기가 정하는 것」 — 소절: 컨텍스트 한계와 임베딩 정밀도가 서로 반대 방향으로 크기를 민다 / 400~600 토큰이 권장값이 된 근거 — 임베딩 모델의 학습 길이와 검색 정밀도가 만나는 지점 / 같은 문서를 128·512·1024 토큰으로 잘라 같은 질문을 던졌을 때 무엇이 걸리고 무엇이 빠지는지 한 번 따라가기
  - 절「오버랩이 사는 자리」 — 소절: 경계에서 잘린 문장이 두 청크 어디에도 온전히 없게 되는 예 / 10~20% 권장값의 계산(500자면 50~100자)과 그만큼 늘어나는 색인 크기 / 오버랩으로는 못 고치는 것 — 표지·목차와 수천 자 떨어진 조각은 겹침이 닿지 않는다
  - 절「고정 크기와 재귀 분할」 — 소절: `CharacterTextSplitter`가 기계적으로 자를 때 남는 반쪽 문장 / `RecursiveCharacterTextSplitter`의 구분자 우선순위(`\n\n` → `\n` → `.` → 공백)가 실제로 도는 순서 / 구분자 목록이 영문 기본값 그대로면 한국어 문장부호에서 안 끊기는 문제
  - 절「의미로 자르기」 — 소절: 인접 문장 임베딩의 코사인 유사도가 급락하는 지점을 경계로 삼는 절차 / 임계값을 백분위로 잡는 것과 절대값으로 잡는 것의 차이 / 청크 길이가 들쭉날쭉해져 토큰 한계를 넘는 것에 후처리가 필요한 이유 / 문장 수만큼 임베딩 호출이 붙는 비용과 처리 속도
  - 절「구조가 있는 문서」 — 소절: 마크다운 헤더 분할이 헤딩을 메타데이터로 남긴다 / 헤딩으로 자르면 절 길이가 그대로 청크 길이가 되어 한 문장짜리 절과 3,000자짜리 절이 한 저장소에 섞인다 — 긴 절만 다시 나누고 짧은 절은 위 절에 붙이는 후처리가 따라붙는 이유 / 코드 파일은 함수·클래스 경계로 자른다 / PDF에서 온 문서는 헤딩이 없어 이 전략이 통째로 못 쓰인다 — 헤딩을 살려 내는 일 자체와 그 값은 `rag-document-parsing`이 맡으므로 여기서는 다시 적지 않는다
  - 절「부모-자식 청킹」 — 소절: 검색은 작은 자식으로, 문맥은 큰 부모로 나누는 구조 / 자식과 부모 크기를 어떻게 짝짓는가 — 원고 예제가 자식 300 · 부모 2,000이므로 그 값으로 적는다(「400 대 2,000」이 아니다)와 그 비율이 커질 때 생기는 일 / 부모를 통째로 넣으면 문맥 예산을 얼마나 먹는지 계산해 보기
  - 절「고른 뒤에 재는 것」 — 소절: 청크 길이 표준편차·짧은 청크 비율·긴 청크 비율 세 값이 각각 무엇을 보여 주는가 / 기준을 정확히 적기 — 5% 이하는 `short_chunk_ratio`·`long_chunk_ratio` 두 비율에만 걸리고 `std_length`에는 절대 기준이 없어 같은 저장소 안에서의 비교로 읽는다(원고가 셋 모두에 5%를 건 것처럼 읽히는 자리를 고친다) / 세 값이 다 좋아도 검색이 나쁠 수 있다 — `recall@N`을 함께 본다 / 문서 종류가 섞인 저장소에서는 종류마다 따로 재야 평균에 묻히지 않는다

- `agent-handoff-protocols` (지금 2,753자 · 절 6)
  - 절「전부 넘기는 쪽의 문제」 — 소절: 3만 토큰 대화를 일꾼 셋에게 각각 붙이면 입력이 9만이 되는 계산 / 도구가 뱉은 표와 중간에 고친 실수가 같은 무게로 들어가 「내가 할 일」을 받는 쪽이 스스로 찾아야 한다 / 앞에 남은 오류가 뒤에 붙은 정정보다 강하게 남는 자리 / 반대쪽 극단 — 「이 회사 조사해 줘」 한 줄만 넘기면 이미 한 조사를 다시 하고 이미 실패한 길로 다시 간다
  - 절「봉투의 다섯 칸」 — 소절: 해낼 것에 「됐다」의 조건까지 적는다 / 이미 알아낸 것과 확실하지 않은 것을 두 칸으로 갈라 적는다 — 앞 칸은 확인된 사실만, 뒤 칸은 「매출은 확인했지만 연결 기준인지 별도 기준인지 모른다」처럼 넘기는 쪽이 자신 없는 자리다. 뒤 칸이 없으면 받는 쪽이 앞 칸을 통째로 사실로 믿고, 나중 원인 추적도 그만큼 길어진다 / 하면 안 되는 것에 건드릴 수 없는 대상·쓰지 말 도구·예산 상한을 적는다 / 돌려줄 모양을 필드 이름까지 못박아 관리자가 결과를 파싱하려고 모델을 다시 부르지 않게 한다
  - 절「봉투를 만드는 쪽」 — 소절: 관리자가 봉투를 코드로 짜는 것과 모델에게 짜게 하는 것의 갈림 / 봉투를 스키마로 고정해 두면 빠진 칸이 실행 전에 코드로 걸린다 / 봉투가 원래 대화 기록만큼 길어지면 나눈 이유가 사라진다 — 길이 상한을 두고 넘으면 봉투를 줄이는 것이 아니라 일감 쪼개기를 다시 본다
  - 절「돌아오는 봉투」 — 소절: 결과·못 한 것·확신이 낮은 자리·쓴 값 넷 / 다섯을 시켰는데 셋만 찾았을 때 돌려줄 서식에 다섯 칸만 있으면 나머지 둘이 그럴듯한 값으로 채워진다 / 빈칸을 허용하고 그 옆에 이유를 적게 하면 지어내는 자리가 사라진다 / 호출 수·토큰·걸린 시간을 봉투에 실어 걷으면 관리자가 남은 예산으로 다음 일감을 정한다
  - 절「사람에게 넘길 때」 — 소절: 사람은 3만 토큰 대화 기록을 읽지 않으므로 서식이 더 중요해진다 — 담는 것은 에이전트끼리와 같고 표현만 다르다 / 왜 넘겼는가를 세 갈래로(모르는 질문·권한 없는 요청·고객이 사람을 요구) 적고 고객이 원하는 것을 한 문장으로 적는다 / 이미 시도한 것(안내한 내용과 고객의 반응)과 확인된 정보(주문 번호·계정처럼 다시 묻지 않아도 되는 것) — 원고의 네 칸 중 이 둘이 얼개에서 빠져 있었고, 빠지면 같은 안내를 두 번 하고 같은 것을 두 번 묻는다 / 「처음부터 다시 말씀해 주시겠어요」 한마디가 에이전트 도입으로 얻은 것을 대부분 상쇄하는 자리
  - 절「왕복이 늘어날 때」 — 소절: A가 B에게 묻고 B가 다시 A에게 묻는 왕복이 세 번을 넘으면 일감 쪼개기가 틀린 것이다 / 되돌아온 일감을 손대지 않고 재시도하면 같은 자리에서 다시 막힌다 — 다시 넘길 때 무엇이 달라졌는지 봉투에 적는다 / 시간 상한과 「응답 없음」 경로가 없으면 관리자가 영원히 기다린다
  - 절「봉투를 따라가는 기록」 — 소절: 봉투마다 작업 아이디를 붙여 어느 갈래에서 틀렸는지 한 줄로 따라간다 / 나중에 붙이려면 전부 고쳐야 하므로 첫날에 넣는다 / 되받은 결과를 코드로 검사한다 — 모델 출력은 형식을 지키다가도 어느 날 안 지킨다

- `rag-document-parsing` (지금 2,822자 · 절 7)
  - 절「PDF가 담고 있는 것」 — 소절: 파일 안에 있는 것은 「이 글자를 이 좌표에 이 글꼴로 그려라」의 목록이고 문단·단·표·각주는 하나도 없다 / 추출이 아니라 복원이므로 되짚기가 틀릴 수 있다 / 좌표를 위에서 아래로만 훑으면 2단 편집에서 왼쪽 첫 줄·오른쪽 첫 줄·왼쪽 둘째 줄 순으로 나와 문장 한가운데가 다른 단과 이어 붙는다
  - 절「자주 어긋나는 네 자리」 — 소절: 머리말·꼬리말이 청크 100개에 같은 문장을 심어 벡터를 그쪽으로 끌어당긴다 / 각주 표시 숫자가 본문 낱말에 붙어 「환불은 7일2 이내」가 된다 / 자간을 좌표로 조절한 문서에서 띄어쓰기가 임계값 문제가 되고 한글에서 특히 자주 어긋난다 / 표 셀을 왼쪽 위부터 이어 붙이면 「구분 2024년 2025년 매출 120 138」이 되어 행·열 대응이 사라진다
  - 절「문서마다 다른 파서」 — 소절: 텍스트 추출기·레이아웃 모델 파서·비전 모델 파서 셋의 강한 곳과 약한 곳 / 텍스트 레이어가 멀쩡한 PDF를 이미지로 바꿔 OCR을 돌리면 원래 정확했던 글자에 오류가 새로 생긴다 / 앞의 둘은 틀리면 순서가 어긋나고 비전 모델은 틀리면 원문에 없는 그럴듯한 글자를 적는다 / 금액·기한·수량이 답의 근거인 문서에서 원본 셀과 대조하는 검사를 따로 두는 이유
  - 절「파일 하나씩 고르는 절차」 — 소절: 텍스트 레이어 유무 → 단 수 → 표 비중 순으로 갈라 내는 결정 트리 / 문서 종류를 자동으로 판별해 파서를 라우팅하는 자리와 그 판별이 틀렸을 때의 대응 / 파서를 섞어 쓰면 출력 형식이 갈리므로 뒤 단계 전에 하나로 맞춰 두는 일
  - 절「헤딩을 살리는 값」 — 소절: 헤딩이 남으면 글자 수가 아니라 절 단위로 자르고 긴 절만 나눌 수 있다 / 상위 헤딩을 이어 붙여 「2분기 실적 보고서 > 3. 구독 부문 > 3.2 환불 규정」을 모델 호출 없이 얻는다 / 「3장에서만 찾아라」 같은 메타데이터 조건이 가능해진다 / 헤딩이 평범한 문단으로 내려앉으면 이 셋이 한꺼번에 사라지므로 파서 비교는 헤딩 인식률부터 본다
  - 절「PDF 밖의 형식들」 — 소절: HTML의 네비게이션·광고·푸터와 DOCX의 변경 이력·주석 / 슬라이드는 도형마다 좌표만 있어 읽기 순서 문제가 PDF보다 심하고, 발표자 노트를 함께 담을지 정해야 한다 / 이메일 스레드의 인용된 이전 메일이 같은 문장을 여러 문서에 중복 색인한다 / 중복된 문장이 검색 결과 상위를 자기들끼리 채워 다른 문서를 밀어내는 자리
  - 절「파싱 품질을 재는 신호」 — 소절: 빈 쪽 비율·한 줄 길이 분포·반복 문자열 비율·헤딩 개수 넷을 각각 무엇으로 읽는가 / 자동 신호는 「무너졌다」를 잡고 「조용히 틀렸다」를 못 잡는다 / 문서 종류마다 표본 열 개는 사람이 읽고, 검수한 그 표본이 그대로 파서 교체용 회귀 세트가 된다

- `rag-table-understanding` (지금 2,889자 · 절 7)
  - 절「뜻을 만드는 교차점」 — 소절: 「11」의 뜻은 왼쪽으로 훑어 「영업이익」을, 위로 훑어 「2025년」을 만나야 생긴다 / 줄글로 펴면 숫자 넷의 나열만 남고 대응은 순서로 추측된다 / 열이 다섯을 넘거나 빈 셀로 값 개수가 안 맞으면 추측이 어긋나는데, 결과는 「모르겠습니다」가 아니라 그냥 다른 숫자다
  - 절「청킹이 표를 깨는 두 자리」 — 소절: 200행 표를 800자로 자르면 헤더 행이 첫 조각에만 남아 둘째부터는 열 이름 없는 근거가 모델에 간다 / 「영업이익 8 1」에서 끊기고 다음 조각이 「1 …」로 시작해 11이 두 조각으로 갈린다 / 표는 글자 수가 아니라 행 단위로 자르고 조각마다 헤더를 다시 붙인다 / 헤더 반복으로 늘어나는 색인 크기와, 그래도 그편이 나은 이유
  - 절「색인에 넣는 세 방식」 — 소절: 마크다운 표 그대로 — 교차 관계가 글자에 남지만 수백 행은 문맥 창에도 검색 단위로도 크다 / 행마다 한 문장 — 「2025년 영업이익은 11억 원입니다」가 질의와 모양이 닮아 두 검색기가 다 잡지만 행끼리의 관계는 못 본다 / 테이블로 적재하고 질의문을 지어 실행 — 세는 일을 모델이 아니라 데이터베이스가 해서 집계가 정확해지는 대신 스키마 준비가 든다 / 셋은 배타적이지 않다 — 같은 표를 두 벌로 색인해도 색인이 두 배가 될 뿐 서로 방해하지 않는다
  - 절「질문 유형과 방식의 대응」 — 소절: 한 칸 찾기·행 사이 비교·집계·여러 해 추이·표 자체의 해석 다섯 유형과 각각 맞는 방식 / 질의 로그를 먼저 세어 보는 것이 순서다 — 한 칸 찾기가 대부분이면 행 문장만으로 끝난다 / 집계 질문이 몇 건뿐인데 데이터베이스 적재 파이프라인을 만드는 낭비
  - 절「병합 셀과 다단 헤더」 — 소절: 헤더가 두 줄이고 「2025년」이 두 칸에 걸친 표에서 「72」의 뜻을 만들려면 두 줄을 합쳐 읽어야 한다 / 파싱 단계에서 「2025년 하반기」로 열 이름을 합쳐 격자를 반듯하게 만든 뒤 색인한다 — 모델이 알아서 합쳐 읽기를 기대하지 않는다 / 세로 병합에서 「구분」의 빈칸을 위에서 물려받아 채우지 않으면 「상반기는 72억 원입니다」처럼 주어 없는 문장이 나온다 / 격자를 이미 뭉갠 파서 결과는 되살릴 방법이 없어 파서 선택과 맞물린다
  - 절「표 밖의 글자」 — 소절: 「단위: 억 원」이 표 제목에만 있고 셀에는 「11」만 있는 흔한 구조 / 기준일·「잠정치」 같은 위아래 주석 줄은 표와 같은 청크에 담는다 / 행 문장을 만들 때 단위를 문장에 넣지 않으면 11이 무엇인지 알 수 없게 되는 자리
  - 절「숫자를 답에 담는 규칙」 — 소절: 「합계는 얼마인가」에 근거 표만 주고 더하게 하면 대개 맞지만 틀린 합계는 표에 없는 숫자라 대조할 자리가 없다 / 「11억 원(실적표 · 영업이익 · 2025년)」처럼 행과 열 이름을 붙여 원문에서 확인할 수 있게 한다 / 계산은 코드나 데이터베이스가 하고 모델은 그 결과를 옮긴다

- `rag-retrieval-strategies` (지금 2,902자 · 절 9)
  - 절「두 갈래의 검색 철학」 — 소절: 정확 매칭이 압도적인 자리(제품 코드·법률 조항 번호·오류 메시지)와 못 하는 자리(동의어·부정 의도·오타) / 의미 검색이 「자동차」로 「승용차」를 잡는 원리와 그 대가 / 한쪽만 쓸 때 각각 빈손으로 돌아오는 질의를 짝지어 놓고 보기 — 「연차」와 「유급 휴가」, 제품 코드 `KX-2100`
  - 절「BM25가 세는 것」 — 소절: TF·IDF·문서 길이 정규화 세 항이 각각 무엇을 눌러 주는가 / $$k_1$$은 빈도가 포화하는 속도를, $$b$$는 길이 벌점의 세기를 정한다 / 어휘 10만에 등장 낱말 50개면 벡터의 99.95%가 0이고, 그 희소성이 역색인을 가능하게 한다 / 학습이 필요 없고 점수의 근거를 낱말 단위로 설명할 수 있다는 성질
  - 절「Dense 검색의 구조」 — 소절: 1,024~3,072차원에서 코사인·내적·L2가 각각 무엇을 재고 언제 결과가 갈리는가 / 백만 건에서 정확 최근접이 왜 안 되는지와 근사 탐색으로 넘어가는 지점 / HNSW의 계층 그래프가 $$O(\log n)$$을 만드는 방식과 IVF-PQ의 양자화가 메모리와 재현율을 맞바꾸는 방식
  - 절「RRF가 순위만 쓰는 이유」 — 소절: BM25 점수 0~20과 코사인 −1~1을 같은 자리에 놓을 때 생기는 불공평 / $$1/(k+r)$$ 에서 $$k=60$$이 상위 순위의 과대 가중을 눌러 주는 방식 / 두 검색기에서 각각 2위·1위인 문서가 한쪽에서만 1위인 문서보다 위로 올라오는 계산을 손으로 따라가기 / 순위만 쓰므로 스케일 차이가 사라진다는 점이 이 방법의 전부다
  - 절「구현에서 갈리는 자리」 — 소절: 두 검색기가 각각 몇 건씩 내놓아 융합에 들어가는가 / 한 검색기가 아무것도 못 찾은 질의를 융합이 어떻게 다루는가 / 한 저장소 안에서 하는 융합(pgvector의 SQL 한 덩이)과 밖에서 두 결과를 합치는 융합의 차이 / 도메인별 가중치 출발점 — 법률·기술 문서 0.6:0.4, FAQ·일반 문의 0.3:0.7, 확신 없으면 0.5:0.5
  - 절「재순위를 얹는 자리」 — 소절: 교차 인코더가 질의와 문서를 함께 넣어 얻는 것과 후보 수에 비례하는 대가 / Top-20을 뽑아 Top-5로 줄이는 구조에서 지연이 어디서 늘어나는지 / 재순위로는 못 고치는 것 — 1차 검색이 후보에 안 넣은 문서는 순위를 고칠 대상 자체가 아니다

- `agent-multi-agent-patterns` (지금 2,958자 · 절 5)
  - 절「하나로 두는 기본값」 — 소절: 도구 여섯 개짜리 하나를 「기획·조사·작성·검토」 넷으로 나누면 토큰 세 배·지연 두 배·고장 지점 넷이 된다 / 나누기 전에 「지금 하나로 안 되는 이유」를 한 문장으로 적을 수 있어야 한다 / 나눠 놓고 결과가 오히려 나빠지는 흔한 자리
  - 절「나누는 네 가지 배치」 — 소절: 관리자와 일꾼 — 일꾼마다 도구가 서너 개라 고르기가 정확해지고 동시에 돌릴 수 있다 / 줄지어 넘기기 — 순서가 이미 정해져 있다면 그것은 파이프라인이지 에이전트가 아니다. 매 단계의 「무엇을 할지 고르는」 판단이 비용이기만 하다 / 서로 검토하기 — 계약서 검토·마이그레이션 계획처럼 틀리면 되돌리기 어려운 일에서만 값을 하고, 「글을 더 잘 써 줘」류에서는 서로 칭찬만 하고 끝난다 / 실제 구성은 넷 중 하나가 아니라 둘셋의 조합이다
  - 절「나눌 이유를 묻는 세 질문」 — 소절: 도구 목록이 스물을 넘어 「주문 조회·배송 조회·결제 조회」 셋 중 아무거나 부르는가 / 서로 기다리지 않아도 되는 일이 셋 있어 지연이 3분의 1이 되는가 — 줄어드는 양을 미리 계산할 수 있는 유일한 이유다 / 틀렸을 때의 값이 검토 에이전트 비용보다 큰가 / 셋 다 「아니오」인데 나눈 구조는 얻는 것 없이 비용과 실패 지점만 늘린 것이다
  - 절「스키마 정리가 먼저인 자리」 — 소절: 도구 혼동은 구조가 아니라 이름·설명·인자로 먼저 푼다 / 스키마로 풀리는 혼동을 구조 변경으로 푸는 것이 왜 훨씬 비싼가 / 이름을 갈라 놓아도 여전히 헷갈리는 도구가 남았을 때 비로소 나눈다
  - 절「나눈 뒤의 새 문제」 — 소절: 하나였을 때 「그냥 남아 있던」 것이 사라져 일꾼 B가 A의 발견을 모른다 / 배경을 셋에게 각각 설명하면 그 배경이 세 번 입력 토큰이 되고, 공통 앞부분 캐싱이 그 곱셈을 눌러 준다 / 네 갈래 기록을 맞춰 봐야 해서 「관리자가 잘못 쪼갠 것」과 「일꾼이 잘못한 것」을 가르는 데 시간이 든다 — 각 호출에 같은 작업 아이디를 붙이는 것이 선택이 아니라 필수가 되는 지점
  - 절「관리자 구조의 실무」 — 소절: 목표가 아니라 일감을 준다 — 「이 주제를 알아봐 줘」와 「이 회사의 2025년 매출·영업이익을 사업보고서에서 숫자와 출처로」의 차이 / 일꾼을 한 번 부르고 끝나는 상태 없는 호출로 두면 관리자가 관리할 것이 준다 / 일꾼 수 상한을 코드로 걸고 넘으면 관리자에게 다시 추리게 한다 — 스스로 쪼개게 두면 열두 개를 만드는 날이 온다 / 못 찾은 것을 「없음」으로 조용히 처리하면 관리자는 다 찾은 줄 안다

- `rag-multi-tenant` (지금 3,025자 · 절 6)
  - 절「테넌트를 가르는 세 방법」 — 소절: 전용 색인 — 파일이 아예 달라 조건을 빼먹어도 남의 것이 안 나오고, 고객사별로 임베딩 모델·청크 크기를 다르게 갈 수 있다 / 색인 안의 칸 — 조건으로 거르는 것이 아니라 탐색 범위 자체가 갈리므로 근사 탐색의 선택도 문제가 안 생긴다 / 값 하나로 구분 — 고정 비용이 거의 없어 고객사가 수만이어도 되는 대신 격리가 조건 하나에 달려 있다 / 격리 강도·고객사당 고정 비용·현실적 상한·고객사별 설정·이탈 처리 다섯 축의 비교
  - 절「색인 개수라는 벽」 — 소절: 근사 탐색 색인 하나마다 붙는 고정 비용 — 그래프 구조, 상주 메모리, 연결 자원 / 문서 열 개짜리 고객사도 그 비용을 그대로 쓴다 / 수백까지는 버티고 수천이 되면 대부분이 거의 빈 색인인 채로 메모리를 먹는 상태 / 큰 고객사 몇 곳만 전용 색인으로 떼고 나머지를 한 색인에 담는 혼합 구성이 요금제와 나란히 가는 배치
  - 절「여러 겹의 격리」 — 소절: 테넌트를 요청 본문이 아니라 세션·인증 토큰에서 서버가 정한다 / 조건은 호출하는 쪽이 넘기게 하지 말고 검색 함수가 세션에서 읽어 무조건 덧붙인다 — 새 기능을 만들다 빠뜨릴 여지를 없앤다 / 결과에 다른 테넌트가 섞였으면 버리고 경보를 올린다. 정상이면 절대 안 걸려 비용이 0에 가깝고, 걸리면 조건이 안 먹었거나 적재 때 값이 잘못 들어간 것이다 / 모델이 질의에서 테넌트를 뽑게 두면 프롬프트 주입의 표적이 된다
  - 절「캐시와 로그에서 새는 자리」 — 소절: 질의 결과 캐시의 키를 질의 문자열로만 만들면 같은 질문을 한 다른 고객사에게 앞 사람의 답이 그대로 간다 / 임베딩 캐시는 질의 텍스트만 다루니 공유해도 되고 검색 결과·생성 답 캐시는 안 되는 이유 / 로그 색인이 하나면 근거 문서 내용이 운영 화면에서 다 보인다
  - 절「시끄러운 이웃」 — 소절: 한 고객사가 문서 백만 건을 올리면 색인 작업이 쏠려 남의 갱신이 대기열 뒤에 서고 처리 지연이 몇 시간이 된다 / 큰 대기열 하나 대신 고객사마다 줄을 세우고 돌아가며 처리한다 / 시간당 처리 문서 수에 고객사별 상한을 두고 급한 대량 이관은 별도 경로로 받는다 / 검색 쪽 동시 실행 상한이 한 고객사의 트래픽 급증이 전체 지연으로 번지는 것을 막는다
  - 절「이탈과 삭제 설계」 — 소절: 지울 자리는 색인 밖에 더 많다 — 원문 보관소, 결과·응답 캐시, 질의 로그와 대화 기록, 평가용 데이터셋, 옛 백업 / 삭제 절차를 코드로 만들어 시험 고객사로 정기적으로 돌려 본다. 계약 종료 통보를 받고 처음 짜면 반드시 뭔가 빠진다 / 지움 표시를 먼저 세워 노출을 곧바로 멎게 하고 유예 기간 뒤에 실제로 지운다 / 전용 색인 방식이 여기서 값을 하는 이유 — 증빙이 간단하다
  - 절「고객사별로 나눠 재기」 — 소절: 전체 평균이 좋아도 문서 종류·용어·질문 방식이 달라 특정 고객사에서만 나쁠 수 있다 / 문서가 많은 몇 곳이 평균을 끌어올리고 문서가 적거나 형식이 특이한 곳이 평균 아래에 조용히 깔린다 — 불만은 그 아래쪽에서 나온다 / 문서 스무 건짜리 고객사에서는 어떤 질문에도 그 스무 건 중 열 개가 나온다 / 최소 점수 기준을 두고 못 미치면 「자료에 없습니다」로 답하게 하되, 그 기준은 고객사 규모에 따라 달라야 한다

- `agent-sandboxing` (지금 3,026자 · 절 6)
  - 절「프롬프트가 방어가 아닌 이유」 — 소절: 모델은 악의 없이도 지운다 — 임시 파일 정리 지시에 상위 폴더를 지우는 코드는 사람도 쓴다 / 지시가 웹 페이지·내려받은 문서를 타고 밖에서 들어온다. 브라우저 에이전트에서는 잘못 눌린 버튼 하나였던 것이 코드 실행에서는 임의의 명령이 된다 / 프롬프트는 실행 시점의 방어가 아니다 — 막는 것은 그 코드가 도는 환경이다
  - 절「격리의 네 단계」 — 소절: 같은 프로세스 — 우리 변수·열린 DB 연결·환경 변수의 키가 전부 그 코드 손 안에 있다 / 별도 프로세스와 언어 런타임의 「제한된 실행 모드」 — 같은 런타임 안에서 자기를 가두는 시도는 반복해서 뚫려 왔으므로 신뢰 경계로 삼지 않는다 / 컨테이너 — 0.1~1초에 뜨고 다 쓰면 통째로 버리지만 커널을 공유한다 / 가벼운 가상 머신 — 100밀리초 안팎에 커널까지 가르고, 넷 중에서 고르는 기준은 「누가 그 코드에 영향을 줄 수 있는가」 하나다
  - 절「밖으로 나가는 네 길」 — 소절: 파일 — 소스 폴더를 통째로 붙이면 격리가 무의미해진다. 필요한 것만 읽기 전용으로, 쓸 곳은 작업이 끝나면 버릴 폴더 하나로 / 네트워크 — 컨테이너 기본값이 사내망 관리 화면·데이터베이스·클라우드 메타데이터 주소를 전부 사정권에 넣는다 / 자격 증명 — 환경 변수의 키는 안에서 그대로 읽힌다. 범위가 좁고 짧게 사는 토큰만 넣거나, 바깥 프록시가 붙여 주면 안에서는 본 적이 없다 / 자원과 시간 — 무한 루프와 메모리를 끝없이 먹는 코드는 악의가 아니라 그냥 자주 나온다
  - 절「기본 차단과 예외 허용」 — 소절: 「위험한 것만 막는다」가 실패하는 이유 — 무엇이 위험한지 다 적을 수 있다는 가정이 틀렸다 / 패키지 설치를 열어 두면 네트워크를 막은 의미가 절반 사라진다 / 미리 담긴 이미지를 쓰거나 사내 미러 하나만 여는 구성 / 예외를 하나 열 때마다 남기는 기록
  - 절「돌아오는 길」 — 소절: 10만 줄짜리 로그가 문맥에 통째로 들어오면 그 자리에서 예산이 끝난다 / 앞뒤 몇십 줄만 남기고 「중간 9만 줄 생략」이라 적는 방식 / 실행 결과에도 남이 심어 둔 글자가 섞여 온다 — 파일 목록에 `README_먼저_읽으세요.txt`가 있고 그 안에 지시가 적힌 식 / 결과는 결과라고 표시해 넣는다
  - 절「샌드박스 밖의 위험」 — 소절: 우리가 도구로 열어 준 것은 안에서도 그대로 된다 — 메일이 나가고 DB가 바뀐다. 격리는 그것을 막으라고 만든 것이 아니다 / 막는 것과 안 막는 것을 짝지어 놓은 표(서버 파일 삭제 대 도구로 파일 삭제, 사내망 훑기 대 열어 준 주소 부르기) / 그래서 도구 수·계정 권한·되돌릴 수 없는 동작 앞의 사람이 격리와 항상 함께 간다
  - 절「구축 뒤에 새는 자리」 — 소절: 샌드박스를 재사용해 앞 작업이 남긴 파일과 상태가 다음 작업에 보인다 / 시간 상한만 걸고 호출 횟수를 안 세면 1초짜리 API를 10분 동안 불러 상한 안에서 요금이 쌓인다 / 안에서 나온 출력이 섞인 로그를 밖에 그대로 쌓는다 / 「일단 로컬에서는 그냥 돌리자」가 그대로 배포된다 — 격리는 운영에서 더 필요하다

- `agent-human-in-the-loop` (지금 3,029자 · 절 7)
  - 원고에 이미 있는 것 — 세 자리(계획·행동·결과 검수), 게이트 하나의 값 셋, 판단 순서 셋, 승인 화면 다섯 칸, 승인 피로와 그 대응 셋, 거부율 0건·30%, 거부 세 갈래. 지난 얼개는 이 일곱을 소절로 풀어 적은 것이라 채워도 늘어나는 것이 없었다. 아래는 그 일곱을 앞의 두 절로 눌러 담고 나머지 다섯 절을 새로 쓰는 구성이다
  - 절「게이트 셋과 그 값」 — 소절: 계획 승인·행동 승인·결과 검수가 「작업당 한 번인가 걸음마다인가」로 갈리므로 질문은 「어느 것이 맞나」가 아니라 「몇 개를 켤 것인가」다 / 게이트 하나가 쓰는 셋 — 사람의 시간·작업의 지연·주의력. 로그 조회 걸음에 걸면 셋을 다 쓰고 아무것도 못 막는다 / 판단 순서 — 되돌릴 수 있는가(격리를 넓히면 이쪽이 넓어진다) → 피해가 늦게 드러나는가 → 100번 돌려 실패율을 재 봤는가
  - 절「승인 화면의 다섯 칸」 — 소절: 무엇을 하려는가를 도구 이름이 아니라 사람 말로 — 「`db.exec` 호출」이 아니라 「주문 3건을 취소」 / 왜 하려는가 — 이 걸음에 이르게 한 근거 한두 줄. 원고 표의 다섯 칸 중 지난 얼개에서 빠져 있던 칸이다 / 범위·되돌릴 수 있는가·다른 선택지 — 「메일을 보냅니다」와 「4,200명에게 보냅니다」는 다른 결정인데 도구 호출을 그대로 찍으면 그 4,200이 인자 어딘가에 묻힌다 / 평소 범위를 넘는 값을 눈에 띄게 하는 규칙은 이 절이 아니라 아래 「묶음 승인의 반대편」에서 다룬다 — 그것은 화면의 칸이 아니라 무엇을 사람에게 올릴지의 문제다
  - 절「승인자와 요청의 수명」 — 소절: 승인자를 사람 이름이 아니라 역할로 정한다 — 휴가·퇴사에 게이트가 멎고, 요청자와 승인자가 같아지는 자리도 함께 막는다. 금액·건수 구간마다 승인 단계를 하나 더 두는 이중 승인 / 만료 시각을 붙이고 만료가 취소인지 진행인지 걸음마다 미리 정한다 — 기한 없는 요청은 쌓이기만 하고, 쌓인 목록은 아무도 안 본다 / 기다리는 동안 전제가 바뀐다 — 승인 요청 때 본 재고·잔액·주문 상태를 승인이 떨어진 뒤 다시 확인하고 달라졌으면 다시 묻는 걸음 / 어디로 보내는가 — 사내 메신저·메일·전용 화면의 응답 시간과 오조작률이 다르고, 휴대폰에서 3초에 판단하게 만드는 것이 서식 설계의 실제 제약이다
  - 절「묶음 승인의 반대편」 — 소절: 「같은 걸음 30개를 한 번에 목록으로」가 승인 피로를 줄이는 대신 개별 검토를 없앤다는 맞바꿈 / 묶음 안에서 한 건만 평소와 다를 때를 잡는 법 — 요약 한 줄에 이상치 표시를 얹고, 금액·건수·수신자 수가 평소 범위를 넘는 줄은 묶음에서 빼내 따로 묻는다 / 묶음 크기 상한과 「전체 승인」 버튼을 두지 않는 선택 — 사람은 같은 모양의 화면을 반복해 보면 차이를 못 본다
  - 절「섀도 게이트와 실패율」 — 소절: 「이 도구는 안전하다」를 재려면 게이트를 켠 채 사람 판단을 모아야 하는데 그러면 그동안 작업이 계속 선다 / 섀도 게이트 — 승인 화면을 띄우되 기다리지 않고 진행하고, 나중에 온 사람 판단과 실제 결과를 대조해 오차를 센다. 되돌릴 수 있는 걸음에서만 쓸 수 있다는 조건 / 몇 건이 모이면 게이트를 내릴지 정하는 기준과, 내린 뒤에도 표본을 계속 뽑아 다시 재는 주기
  - 절「승인 대신 되돌리기」 — 소절: 게이트는 앞에서 막고 취소 창은 뒤에서 되돌린다 — 같은 위험에 값이 다르게 붙는다 / 「보내기 전 승인」보다 「5분 안에 취소」가 나은 자리와 안 되는 자리 — 밖으로 나간 메일과 끝난 결제는 되돌아오지 않는다 / 임시 작업 공간과 되돌리기 기록을 넓히는 쪽이 게이트를 늘리는 쪽보다 싸게 먹히는 계산
  - 절「게이트를 재는 지표」 — 소절: 게이트마다 따로 세는 넷 — 요청 수·거부율·승인까지 걸린 시간의 분포·요청당 사람 시간 / 두 문턱 — 거부 0건이면 그 게이트는 아무 판단도 안 하고 있고, 30%를 넘으면 그 걸음이 자동으로 돌 준비가 안 된 것이다 / 대기 시간 분포의 꼬리가 곧 「이 에이전트가 왜 느린가」의 답이라는 것 / 거부를 세 갈래(건너뛰기·고쳐서 재시도·전체 중단)로 받아 이유를 라벨로 쌓고, 같은 조건이 열 번 막히면 묻는 대신 규칙으로 코드에 넣는 경로

- `agent-cost-control` (지금 3,099자 · 절 7)
  - 절「걸음 수와 토큰의 관계」 — 소절: 첫 입력이 $$c$$ 이고 걸음마다 $$d$$ 가 붙으면 $$n$$번째 입력이 $$c+(n-1)d$$ 다 / 총합 $$nc+\frac{n(n-1)}{2}d$$ 에서 뒤쪽 항이 $$n^2$$ 에 비례해 걸음이 5에서 10이 되면 총 입력이 서너 배가 된다 / 도구 결과 $$d$$ 가 클수록 이차항이 지배해 같은 걸음 수에도 비용이 갈린다
  - 절「평균이 아니라 분포」 — 소절: 평균 6걸음이어도 20걸음짜리가 5% 섞이면 그 5%가 전체 비용의 3할을 먹는다 / 작업당 걸음 수 히스토그램에서 오른쪽 꼬리부터 본다 / 비싼 작업 열 개를 뽑아 놓으면 새는 자리와 손댈 순서가 그 열 개에서 나온다
  - 절「새는 여섯 자리」 — 소절: 도구 결과 통째 넣기 — 검색 20건을 본문까지 넣으면 한 걸음에 3만 토큰이고 그것이 이후 모든 걸음에 남는다. 도구 쪽에서 요약·필드 선택·건수 제한을 걸면 한 번의 수정으로 작업 전체가 절반이 된다 / 앞 걸음을 계속 이고 가기와 접는 기준 — 오래됐다고 접는 것이 아니라 이후 걸음에서 참조되지 않는 것을 접는다 / 실패 재시도·안 멈춤·전 구간 큰 모델 셋 / 위쪽 셋이 대체로 8할이고 나머지를 먼저 손대는 것은 순서가 틀린 것이다
  - 절「캐시가 가장 잘 듣는 형태」 — 소절: 에이전트 루프는 같은 앞부분을 걸음마다 다시 보내는 구조라 캐시 적중률이 구조적으로 높다 / 앞부분이 바이트 단위로 같아야 걸리므로 시스템 프롬프트와 도구 정의를 맨 앞에 고정한다 / 앞쪽에 시각·난수·사용자 이름이 끼면 그 뒤가 통째로 캐시를 잃는다 / 캐시 적중 토큰을 따로 기록해야 이 손잡이가 실제로 걸렸는지 알 수 있다
  - 절「세 겹의 상한」 — 소절: 걸음 상한은 도구 결과 크기에 걸고 넘으면 자르되 작업은 안 멈춘다 — 조회 결과가 크다고 여섯 시간짜리가 죽으면 안 된다 / 작업 상한은 걸음 수와 총 토큰에 둘 다 건다. 걸음 20인데 매번 큰 결과를 물어 오면 토큰은 걸음 100개짜리다 / 하루 상한은 새 작업만 안 받고 돌던 것은 마저 끝낸다 — 죽이면 이미 쓴 비용이 버려진다 / 셋이 각각 다른 사고를 막는다. 「여러 작업이 동시에 조금씩 새는 것」은 앞의 둘로 절대 안 잡힌다
  - 절「재시도가 먹는 값」 — 소절: 재시도마다 모델 호출이 새로 일어나고 전체 문맥이 다시 들어가 실패한 걸음 하나가 세 걸음 값이 된다 / 시간 초과·일시적 오류와 인자 오류를 가른다 — 인자가 틀려서 난 것은 다시 해도 같다 / 실패한 응답 전문을 문맥에 남기지 않는다. 스택 트레이스 하나가 그 뒤 모든 걸음을 비싸게 만든다 / 같은 도구가 같은 오류로 세 번 실패하면 재시도가 아니라 보고다
  - 절「걸음마다 다른 모델」 — 소절: 작은 모델로 되는 걸음(분류·형식 변환·「끝났는가」 판단·짧은 요약)과 큰 모델이 필요한 걸음(계획·근거 엮기·최종 산출물) / 모델을 바꾸는 것은 품질에 영향을 주므로 재는 일이 반드시 따라붙는다 / 자르기와 캐싱을 먼저 하는 이유 — 그 둘은 품질을 거의 안 건드리면서 비용만 줄인다 / 작업당 입력·출력 토큰, 걸음 수, 캐시 적중, 사용 모델을 걸음별로 기록하기 전에는 최적화를 시작하지 않는다

- `rag-freshness-updates` (지금 3,138자 · 절 7)
  - 절「세 토막으로 나뉜 지연」 — 소절: 감지 지연은 훑기 주기가 그대로 값이라 코드를 빠르게 해도 안 줄어든다 — 한 시간 주기면 평균 30분, 최악 1시간 / 처리 지연은 대기열에서 기다린 시간까지 포함해 대량 갱신 한 번에 몇 시간으로 튄다 / 반영 지연은 저장소마다 다르다 — 곧바로 보이는 곳과 세그먼트를 주기적으로 정리해 몇십 초 걸리는 곳 / 셋을 나눠 재지 않으면 병목이 훑기 주기인데 임베딩 배치를 최적화하게 된다
  - 절「변경을 알아채는 세 방식」 — 소절: 주기적 훑기·변경 알림 받기·변경 기록 읽기 셋의 감지 지연과 걸리는 조건 / 파일 시스템과 문서 도구의 수정 시각은 권한 변경이나 열어만 봐도 도는 경우가 흔해 그대로 믿으면 매 주기 전체를 다시 임베딩한다 / 시각으로 후보를 좁히고 내용 해시로 확정하는 두 단계 / 알림을 받는 구조에서도 놓친 알림이 있다는 전제로 훑기를 병행하는 이유
  - 절「청크 경계가 밀리는 문제」 — 소절: 두 번째 문단에 두 문장이 늘었을 뿐인데 800자 규칙 아래에서는 그 뒤 모든 조각의 시작점이 함께 밀린다 / 내용이 하나도 안 바뀐 세 번째 조각도 어제와 다른 글자가 되어 재임베딩 대상이 되고, 문서 끝에서는 조각이 하나 더 생긴다 / 경계를 문단·절 제목·표에 맞추면 이 번짐이 사라진다 — 갱신 비용이 여기서 자릿수로 갈린다 / 청크 키를 `문서id:5번째`로 만들면 앞에 하나 끼는 순간 뒤 키가 전부 밀린다. 구획 이름이나 내용 해시로 키를 만든다
  - 절「조용히 남는 삭제」 — 소절: 추가·수정은 안 나오면 누가 알아채지만 삭제는 오류를 안 내고 계속 검색되며 그 근거로 답이 만들어진다 / 훑기는 「있는 것의 목록」을 받으므로 색인에 있는 것과 대조하지 않으면 없어진 사실을 영영 모른다 / 지움 표시를 세워 검색에서 항상 배제하면 노출이 즉시 멎고 되돌리기도 쉽다. 실제 삭제는 나중에 몰아서 / 하루 한 번 전체 식별자 대조 — 돌려 본 적 없는 색인에는 대개 유령 문서가 꽤 있다
  - 절「권한이 낡는 자리」 — 소절: 문서를 볼 수 있는 사람이 줄었는데 색인의 값이 예전 그대로면 필터가 정확해도 옛 기준으로 동작한다 / 권한 회수는 문서 내용 변경으로 안 잡혀 별도 동기화 경로가 필요하다 / 사람이 조직을 옮긴 날과 색인이 그것을 아는 날 사이의 간격을 무엇으로 재는가
  - 절「임베딩 교체와 재색인」 — 소절: 벡터끼리의 거리는 같은 모델이 만든 것 사이에서만 뜻이 있어, 차원 수가 우연히 같아도 섞으면 검색이 망가진다 / 새 색인을 따로 채우고 바라보는 곳을 한 번에 옮긴 뒤 옛 것을 지우는 순서 / 채우는 동안 원본이 계속 바뀌므로 옮기기 직전에 쌓인 변경을 한 번 더 흘려 넣는 단계 / 분기에 한 번 빈 색인에서 전체를 채워 보고 걸리는 시간을 재 둔다 — 모델 교체 때 처음 시도하면 대개 잘 안 된다
  - 절「최신 색인의 남은 문제」 — 소절: 지난해 규정과 올해 규정이 각각 별개 문서로 있으면 검색은 둘을 비슷하게 가깝다고 판단하고, 어느 쪽이 유효한지는 내용만 봐서 모른다 / 「2026-01-01부터 유효」 같은 효력 기간을 청크 값으로 넣어 사람이 판단할 것을 검색에 맡기지 않는다 / 자르기 애매하면 점수 단계에서 최근 문서에 가산점을 준다 / 답에 「2026년 3월 개정본 기준입니다」 한 줄을 적으면 읽는 사람이 낡았는지 판단할 수 있다

- `embedding-multimodal` (지금 3,247자 · 절 8)
  - 절「공유 공간이 바꾼 것」 — 소절: 1,000 클래스를 고정해 두고 새 카테고리가 생기면 전체를 다시 학습하던 분류기 방식 / 이미지 벡터와 텍스트 벡터를 같은 512차원에 놓으면 두 모달리티 비교가 코사인 유사도 하나로 끝난다 / 그 하나에서 zero-shot 분류·크로스모달 검색·캡션 매칭이 갈라져 나오는 구조
  - 절「대조 학습의 구조」 — 소절: 이미지 인코더(ViT의 16×16 패치와 [CLS] 토큰)와 텍스트 인코더([EOS] 토큰)가 각각 요약한 것을 선형 사영과 L2 정규화로 512차원 구 위의 단위 벡터로 올려놓는 두 걸음 / N×N 유사도 행렬에서 대각선을 올리고 나머지 $$N^2-N$$ 을 내리는 손실, 그리고 이미지→텍스트와 텍스트→이미지 두 방향의 평균 / 오답 후보가 배치 안의 나머지 $$N-1$$ 개이므로 배치가 클수록 난이도가 올라간다 — 4억 쌍이라는 규모가 이 방식에 필요했던 이유 / 온도 파라미터를 $$\log(1/\tau)$$ 로 두고 함께 학습해 유사도 분포의 날카로움을 조절한다
  - 절「Zero-Shot 분류」 — 소절: 클래스 목록을 `"a photo of a {class}"` 텍스트로 바꿔 임베딩하고 이미지와 견주는 네 걸음 / ImageNet 레이블을 하나도 안 쓰고 ResNet-50 수준에 닿았다는 결과의 뜻과 한계 / 클래스 이름을 무엇으로 적느냐가 결과를 바꾼다 — 같은 대상을 「car」로 쓸 때와 「automobile」로 쓸 때
  - 절「프롬프트가 바꾸는 것」 — 소절: `"dog"`보다 `"a photo of a dog"`이 나은 이유 — 학습에 쓰인 인터넷 캡션의 문장 분포 / 「big」·「small」 같은 세부 묘사를 더하면 더 오르는 방향 / 여러 프롬프트 텍스트 임베딩의 평균을 쓰는 앙상블과 그 비용
  - 절「크로스모달 검색」 — 소절: 텍스트→이미지와 이미지→텍스트가 같은 색인에서 도는 구조 / 이미지 벡터를 벡터 DB에 넣을 때 정규화를 빼먹으면 코사인이 내적과 어긋나는 자리 / 「붉은 드레스를 입은 여성」처럼 속성이 여럿인 질의에서 결과가 흔들리는 이유
  - 절「이미지를 다리로 삼기」 — 소절: ImageBind가 오디오·깊이 맵·열화상·IMU까지 여섯 모달리티를 한 공간에 놓는 방식 / 이미지-텍스트, 이미지-오디오, 이미지-깊이 쌍을 각각 정렬하면 이미지를 매개로 나머지가 간접 연결된다 / 학습에 쓰지 않은 오디오-텍스트 쌍의 유사도가 계산되는 이유와, 그 간접 연결이 약한 자리
  - 절「CLIP이 못 하는 것」 — 소절: 「두 마리의 개」·「개가 고양이 왼쪽에」 같은 수와 공간 관계 / 조류 종·자동차 모델명 같은 세밀 분류에서 전용 모델에 밀린다 / 인터넷 데이터가 실어 온 편향 — 특히 직업명과 인물 사진을 잇는 자리 / 기본 CLIP은 영어 중심이라 한국어는 koclip이나 다국어 변형이 필요하고, 그 변형이 원본과 다른 점

- `rag-contextual-retrieval` (지금 3,274자 · 절 8)
  - 절「문맥을 잃은 조각」 — 소절: 「이번 분기 매출은 전년 대비 3% 늘었습니다」에 회사도 분기도 부문도 없다 / 키워드 검색은 「2026」·「2분기」·「구독」이 글자로 없어 못 찾고, 벡터 검색도 조각 안에 없는 정보를 벡터에 담을 수 없다 / 겹침과 문단 경계로는 안 풀린다 — 표지·목차는 열두 번째 조각에서 수천 자 떨어져 있다 / 하이브리드 손잡이를 다 돌려도 안 고쳐지는 이유는 두 검색기가 보는 글자 자체에 답이 없어서다
  - 절「색인 때 붙이는 이유」 — 소절: 청크당 딱 한 번 계산하고 저장한다 — 검색 때 하면 질의마다 후보 전체에 다시 해야 한다 / 색인이 끝난 뒤에는 글자가 조금 긴 청크일 뿐이라 검색 경로에 모델 호출이 하나도 안 는다 / 붙인 글자가 BM25 색인과 임베딩에 함께 들어가 두 검색기가 같이 좋아진다 — 한쪽만 고치는 대부분의 방법과 다른 점
  - 절「맥락 생성 프롬프트」 — 소절: 문서 전체와 그 조각을 함께 넣고 한두 문장을 받아 조각 맨 앞에 붙이는 호출 모양 / 「조각 안에 없는 고유 정보를 우선」이 빠지면 모델이 조각에 이미 있는 말을 되풀이한다 / 「요약하지 말라」가 빠지면 조각을 줄여 써서 원문의 숫자와 낱말이 사라진다 / 원본 청크는 따로 보관해 두었다가 답변 생성에 쓴다
  - 절「캐싱이 정하는 비용」 — 소절: 문서 2만 토큰·청크 500·출력 80일 때 그냥 30번 부르면 입력이 61만 5천 토큰이다 / 문서를 프롬프트 맨 앞에 두고 캐시 경계를 그 뒤에 잡으면 첫 호출만 정가이고 29번이 캐시 읽기 값으로 지나간다 / 같은 문서의 청크를 연달아 처리해야 캐시가 살아 있다 — 문서를 섞어 병렬로 돌리면 캐시가 계속 밀려나 이득이 사라진다 / 맥락 한두 문장 뽑기는 어려운 추론이 아니므로 모델은 작은 것으로 충분하다
  - 절「효과가 나는 문서」 — 소절: 보고서·회의록·약관·매뉴얼처럼 앞을 읽었다고 전제하고 쓴 글에서 크게 오르고, FAQ처럼 항목이 자족적인 문서에서는 별로 안 오른다 / 조각과 표지 사이가 멀수록, 즉 문서가 길수록 잃어버린 정보가 많아 이득이 크다 / 리랭커와 겹치지 않는다 — 리랭커는 후보의 순위를 고치고 이쪽은 1차가 건지는 목록 자체를 바꾼다 / 순위가 아니라 「건졌는가」가 바뀌므로 `recall@N`을 먼저 보고 최종 답변 지표를 그다음에 본다
  - 절「어긋나는 자리 넷」 — 소절: 200자 청크에 300자 맥락이 붙으면 임베딩이 맥락 쪽으로 끌려가 모든 청크가 「이 문서는 실적 보고서다」로 몰린다 — 청크가 짧으면 청크를 먼저 키운다 / 프롬프트가 느슨하면 요약으로 바뀌어 원문에만 있던 숫자와 고유명사가 사라진다 / 300쪽 매뉴얼은 통째로 못 넣으므로 「제목 + 목차 + 그 조각이 속한 절의 앞부분」만 넣는다 / 문서에 없는 「2분기」를 지어내면 틀린 글자가 색인에 들어가 엉뚱한 질의에 걸린다 — 표본 100개를 사람이 읽는 것이 유일한 방어다
  - 절「모델 없이 붙이는 법」 — 소절: 문서 제목·폴더 경로·작성일만 붙여도 되는 문서 — 제목이 이미 구체적일 때 / 상위 헤딩 이어 붙이기와 모델 호출을 값으로 견주기 — 앞 절의 캐싱 계산(청크 30개에 입력 61만 5천 토큰)이 헤딩 쪽에서는 아예 0이 되고, 대신 담기는 것은 절 이름뿐이라 표지에만 있는 회사·분기·부문은 못 채운다. 두 방법의 값과 담기는 것을 같은 표에 놓는다 / 앞뒤 문단 요약은 서술형 긴 글에서만 값을 한다 / 네 방법을 비용과 담기는 것으로 줄 세우고, 모델 호출은 앞의 셋으로 안 되는 문서에만 쓴다

- `agent-long-running` (지금 3,308자 · 절 7)
  - 절「상태를 밖에 두기」 — 소절: 걸음마다 도구 결과가 붙어 문맥이 단조롭게 커지고, 창을 넘으면 앞부터 잘리는데 무엇이 잘렸는지 모르는 채로 계속 돈다 / 메모리에만 있는 대화 기록은 재시작 한 번에 사라진다 — 몇 시간짜리 일에서 재시작은 예외가 아니라 전제다 / 작업 기록에 담는 다섯 가지 — 어디까지 했는가, 무엇을 알아냈는가, 무엇이 실패했는가, 지금 어떤 상태인가, 얼마나 썼는가 / 대화 기록은 상태가 아니라 이번 걸음의 작업 공간이 된다
  - 절「체크포인트의 자리」 — 소절: 되돌리기 어려운 일의 직전과 직후에 남긴다 — 그 사이에서 죽는 것이 가장 아프다 / 간격은 체크포인트 값이 아니라 다시 하는 비용이 정한다. 한 걸음 10분짜리면 걸음마다 / 「7번 완료」와 「8번 시작함」은 다른 기록이고, 다음에 무엇을 할 차례인지가 없으면 8번부터인지 7번을 하다 만 것인지 모른다 / 체크포인트 자체는 몇 킬로바이트라 아껴서 이득 볼 것이 없다
  - 절「멱등성의 세 방법」 — 소절: 호출마다 고유 키를 붙여 같은 키로 들어온 요청에 서버가 앞 결과를 그대로 돌려주게 한다 — 결제 API가 쓰는 방식 / 「행을 추가한다」 대신 「이 아이디의 행을 이 값으로 둔다」로 짜면 몇 번을 불러도 한 줄이다 / 보내기 전에 이미 보냈는지 조회한다 — 조회와 실행 사이의 틈은 남지만 앞의 둘이 안 되는 자리에서는 이것이라도 한다 / 응답이 오기 전에 죽은 호출이 서버 쪽에서는 성공했을 수 있다는 전제에서 출발한다
  - 절「멱등으로 못 만드는 동작」 — 소절: 메일 발송처럼 두 번 보내는 위험과 한 번도 안 보내는 위험 중 하나를 골라야 하는 자리 / 「보냄」을 먼저 기록하고 보내는 순서가 어느 쪽 위험을 택하는 것인지 / 안 보낸 것이 남아 있는지 나중에 확인하는 절차를 함께 둔다 / 그 선택을 미리 해 두는 것 자체가 설계다
  - 절「사람을 기다리는 상태」 — 소절: 승인이 세 시간 뒤에 오는데 프로세스를 띄워 두면 그 사이 재시작 한 번에 끝난다 / 「사람 승인 대기」를 작업 기록에 적고 프로세스는 내린다 — 며칠을 기다려도 아무것도 떠 있을 필요가 없다 / 기한이 지나면 취소인지 자동 진행인지 미리 정한다 / 승인 요청 때 본 재고와 승인이 떨어진 뒤의 재고가 다를 수 있어 이어서 하기 전에 전제를 다시 확인하는 걸음을 둔다
  - 절「멈추는 다섯 조건」 — 소절: 전체 걸음 수, 비용과 토큰(예산 8할에서 알리고 넘으면 정지), 같은 오류 세 번 연속, 진척 없음, 벽시계 시간 / 진척 없음이 특히 자주 빠진다 — 에이전트는 오류 없이도 같은 검색을 조금씩 바꿔 가며 제자리를 돈다 / 오류 횟수만 세면 그 상태를 못 잡으므로 「어디까지」가 움직이는지 재는 자를 따로 둔다 / 상한에 걸려 멈춘 작업이 재개 불가능하면 상한을 건 의미가 절반이다
  - 절「밖에서 보이는 진행」 — 소절: 화면에 내놓을 셋 — 「8,412건 중 3,190건 처리 중」 정도의 한 줄, 지금까지의 비용과 남은 예산, 건너뛴 항목의 목록 / 셋 다 작업 기록에 이미 있으므로 화면 하나만 붙이면 된다 / 잘못 시작한 여섯 시간짜리를 멈추는 방법이 서버 재시작뿐이면 사람이 그 작업을 시작하기를 두려워한다

- `agent-reflection` (지금 3,321자 · 절 6)
  - 절「고쳐 쓰는 루프」 — 소절: Generator → Critic → Reviser 세 역할과 한 바퀴가 도는 모양 / 사람이 초안을 쓰고 검토하고 고치는 과정과 같은 구조라는 점, 그리고 다른 점 / 한 번 더 도는 것이 값을 하는 일과 안 하는 일을 가르는 첫 질문 — 좋아졌는지를 무엇으로 아는가
  - 절「혼자 보는 것의 한계」 — 소절: 같은 모델이 생성과 평가를 하면 자기가 놓친 것을 같은 이유로 다시 놓친다 / 그래도 자기 검토가 잘 듣는 자리 — 형식 위반, 빠뜨린 항목, 계산 검산처럼 기준이 글 밖에 있는 것 / 「더 잘 써 줘」류에서 두 번째 시도가 첫 번째보다 나빠지는 경우와 그 신호
  - 절「Critic을 가르는 이득」 — 소절: 평가자를 다른 호출로 떼면 앞의 초안이 든 문맥에 끌리지 않는다 / Critic에게 채점 기준을 항목으로 주고 자유 서술을 못 하게 막는 이유 / 호출이 두 배가 되는 만큼 어떤 일에서 그 거래가 남는가 / 같은 모델을 Critic으로 쓸 때와 다른 모델을 쓸 때의 차이
  - 절「구조화된 피드백」 — 소절: 점수와 이슈 목록을 JSON 스키마로 받아 종료 조건을 모델이 아니라 코드가 판단하게 한다 / 자유 서술 피드백이 Reviser에게 넘어갈 때 드는 해석 비용 / 이슈마다 「어디를 어떻게」가 없으면 Reviser가 전체를 다시 써서 잘돼 있던 부분까지 바뀐다
  - 절「종료 조건과 반복 상한」 — 소절: 임계 점수만 두면 점수가 진동하며 상한까지 도는 자리 / 「점수가 더 안 오르면 멈춘다」는 개선 폭 기준을 함께 둔다 / 반복 2회를 넘겨 좋아지는 경우가 드문 이유와, 그것을 자기 데이터로 확인하는 법 / 비용이 반복 수에 그대로 곱해지므로 상한이 곧 예산이다
  - 절「코드에서의 자기 수정」 — 소절: 실행 오류라는 밖의 신호가 있어 자기 검토가 가장 잘 듣는 자리 / 오류 메시지 전문을 그대로 넣으면 문맥이 스택 트레이스로 채워진다 — 무엇을 잘라 넣을지 / 테스트를 통과할 때까지 도는 루프가 코드 대신 테스트를 고치기 시작하는 실패 / 코드를 돌리는 자리이므로 격리가 함께 필요하다
  - 절「패턴을 고르는 기준」 — 소절: 자기 검토·Critic 분리·자기 디버그 셋의 오버헤드와 맞는 일 / 「밖에서 오는 신호가 있는가」가 첫 기준이다 — 실행 결과·테스트·스키마 검증이 있으면 리플렉션이 값을 하고, 없으면 취향 싸움이 된다 / 리플렉션을 붙이기 전에 프롬프트와 도구를 먼저 손보는 순서

- `rag-ocr-pipeline` (지금 3,375자 · 절 8)
  - 절「OCR 파이프라인의 단계」 — 소절: 기울기 보정·이진화·줄 검출·인식·후처리가 각각 다른 방식으로 틀린다 / 0.5도 남은 기울기가 줄 검출을 흔들면 인식 모델을 더 좋은 것으로 바꿔도 별로 안 오른다 — 인식률이 안 나올 때 먼저 볼 것은 전처리 결과 이미지다 / 격자선의 세로줄을 「1」이나 「l」로 읽으면 그 줄 전체의 셀 대응이 한 칸씩 밀려 숫자가 다른 항목으로 옮겨 붙는다 / 후처리 사전은 「환볼→환불」은 되살려도 「7일→7밀」과 금액 자릿수는 못 고친다 — 판단할 근거가 사전에 없다
  - 절「글자 정확도와 청크 무결률」 — 소절: $$P=p^{100}$$ 으로 계산하면 98%가 100자 청크의 13%만 무결로 만든다 / 92·98·99·99.5% 넷을 나란히 놓으면 계단이 얼마나 가파른지 보인다 / 400자 청크에서 무결 확률이 0.03%로 떨어지는 계산 — 원고 43행의 「0.3%」는 자릿수가 틀렸으므로 0.03%로 고친다($$0.98^{400}=0.031\%$$, 표의 $$0.98^{100}\approx13\%$$는 맞다) / 오류를 없애는 설계가 아니라 오류가 있다는 전제의 설계로 방향이 바뀌는 지점
  - 절「층마다 다른 오류의 얼굴」 — 소절: BM25는 글자가 일치해야 점수를 주므로 「7밀」이 든 청크는 아예 후보에 안 든다 — 조항 번호·금액·제품 코드가 하필 OCR이 가장 자주 틀리는 자리다 / 벡터 검색은 견디는데 그래서 검색 지표가 멀쩡히 나와 문제를 늦게 안다 / 답변은 「7밀」을 그대로 옮기거나 「7일」로 알아서 고쳐 적는다 — 후자가 금액에서 일어나면 조용히 틀린 숫자가 나간다 / OCR 저장소에서 `recall`만 보고 판단하면 안 되는 이유
  - 절「전통 OCR과 비전 모델」 — 소절: 나오는 것·표 처리·틀리는 방식·신뢰도·좌표·비용 여섯 축의 비교 / 신뢰도와 좌표가 남는지가 계약서·의무기록에서는 정확도보다 중요할 때가 많다 — 답변 옆에 원본 이미지의 그 자리를 띄울 수 있다 / 비전 모델은 없는 글자를 지어내므로 숫자 표에서는 전통 OCR 결과와 대조해 다른 셀만 사람이 본다 / 문서 종류로 나눠 쓰는 구성 — 대량의 평범한 스캔본은 전통, 표가 빽빽한 소수는 비전
  - 절「신뢰도 점수의 쓸모」 — 소절: 청크 메타데이터에 평균 신뢰도와 최저 신뢰도를 함께 넣어 「이 답의 근거가 얼마나 믿을 만한가」에 답할 근거를 남긴다 / 기준 아래 청크만 모으면 대개 전체의 몇 퍼센트다 — 만 쪽은 못 봐도 300쪽은 본다 / 근거의 신뢰도가 낮으면 「스캔 인식 결과이며 확인이 필요합니다」를 답과 함께 낸다 / 대부분의 파이프라인이 텍스트만 꺼내면서 이 값을 버린다
  - 절「한국어에서 갈리는 자리」 — 소절: 「갈」과 「길」, 「몸」과 「음」처럼 획 하나가 다른 글자쌍이 많고 해상도가 낮으면 그 획이 뭉개진다 — 300dpi가 실무 하한으로 통하는 이유 / 받침이 작고 빽빽해 이진화 임계값이 조금만 어긋나도 다른 글자가 되는 구조 / 숫자와 단위가 붙은 표기에서 자릿수가 조용히 바뀌는 오류가 사전으로 안 걸리는 자리
  - 절「설계에서 챙길 것」 — 소절: 원본 이미지를 지우면 더 좋은 모델이 나와도 다시 못 돌린다 — 없으면 재스캔이라 사실상 불가능하다 / 쪽 번호와 좌표를 청크에 남겨 「이 문서 43쪽」까지 가리키고 검수할 때 그 자리를 바로 연다 / 문서 하나만 다시 돌려 그 문서의 청크만 갈아 끼울 수 있는 색인 구조 — 전체를 다시 만들어야만 고칠 수 있으면 아무도 안 고친다 / 사람이 정답을 적어 둔 쪽 50장으로 회귀 세트를 만들어 전처리 설정을 바꿀 때마다 잰다

- `agent-computer-use` (지금 3,557자 · 절 7)
  - 절「관측과 행동의 루프」 — 소절: 찍고·정하고·누르고·확인하는 네 걸음이 구조의 전부다 / 한 바퀴 3~8초가 어디서 나오는가 — 스크린샷 생성, 이미지 읽기, 판단, 동작 전송, 화면 반응 대기 / 사람이 3초면 끝낼 「메뉴에서 보고서를 골라 내려받기」가 스무 걸음이면 1분을 넘는다
  - 절「스크린샷이 먹는 토큰」 — 소절: 한 장이 입력 토큰 1,000개 남짓이고 앞 걸음의 화면을 문맥에 남기면 그것도 누적된다 / 화면을 한 번 볼 때 동작을 여러 개 묶어 내게 하는 방식 — 「이 칸에 이걸, 다음 칸에 저걸, 저장」 / 반복되는 앞부분은 정해진 절차로 미리 처리하고 판단이 필요한 지점부터 모델에게 넘긴다 / 앞 걸음 스크린샷을 몇 장까지 남길지 정하는 기준
  - 절「가리키는 세 방법」 — 소절: 픽셀 좌표 — 창 크기·화면 배율·목록에 항목 하나가 늘면 어제 맞던 좌표가 옆 버튼을 누르고, 그 옆이 「삭제」인 경우가 꼭 있다 / 접근성 트리 — 「저장이라는 이름의 버튼」을 가리켜 위치가 바뀌어도 따라가지만 라벨 없이 그림만 얹은 요소는 트리에 안 보인다 / 브라우저 DOM — 선택자로 직접 집고 스크린샷 없이 상태를 텍스트로 읽어 토큰이 훨씬 덜 든다 / 하나로 통일하지 말고 섞어 쓴다 — 실제 작업은 브라우저와 데스크톱 앱을 오간다
  - 절「정해진 실패 여섯」 — 소절: 아직 안 뜬 화면 누르기, 안 눌렸는데 눌린 줄 알고 같은 동작 반복, 팝업에 가림, 알림창이 초점을 가져가 엉뚱한 창 조작, 스크롤 밖을 「없는 것」으로 판단, 조용히 끝없이 돌기 / 여섯 중 넷의 대응이 사실상 하나다 — 행동 뒤 확인 걸음을 빼지 않는 것. 그런데 걸음 수를 아끼려다 가장 먼저 생략되는 것이 그 걸음이다 / 확인은 「스크린샷을 한 장 더」가 아니라 「저장되었습니다가 뜨는가」·「목록에 새 줄이 생겼는가」여야 한다 / 성공 조건을 안 적어 두면 모델은 화면을 보고 「잘된 것 같다」고 판단하고 그 판단은 자주 틀린다
  - 절「되돌릴 수 없는 동작」 — 소절: 읽기·이동·검색과 결재·발송·확정·삭제를 코드가 가른다 — 화면을 보고 모델이 「안전해 보인다」고 판단하게 두지 않는다 / 담당자의 실제 컴퓨터가 아니라 따로 띄운 가상 환경에서, 그 일에 필요한 권한만 준 별도 계정으로 돌린다 — 컴퓨터 사용은 그 화면에서 사람이 할 수 있는 모든 일을 할 수 있다 / 걸음마다 스크린샷과 동작을 남기되 화면에 개인정보가 그대로 찍히므로 보관 기간과 접근 권한을 먼저 정한다
  - 절「화면 글자와 주입」 — 소절: 에이전트가 읽는 화면은 남이 쓴 내용이다 — 웹 페이지, 받은 메일, 협력사가 올린 문서, 그리고 흰 글자로 심어 두면 화면을 보는 사람도 못 알아챈다 / 텍스트를 잘못 생성하는 것이 아니라 실제로 마우스가 움직인다는 점에서 결과가 훨씬 무겁다 / 열어도 되는 주소와 써도 되는 앱을 목록으로 좁혀 지시가 심겨도 실행할 곳을 없앤다 / 화면에서 읽은 것과 사용자가 시킨 것을 프롬프트에서 자리를 갈라 넣는다
  - 절「쓸 만한 자리」 — 소절: 맞는 자리 넷 — API가 없고, 지루하게 반복되고, 틀려도 되돌릴 수 있고, 몇 분 걸려도 되는 일 / 안 맞는 자리 넷과 각각의 이유 — 앞의 셋은 비용과 속도, 마지막(한 번 틀리면 끝인 일)은 신뢰도 / 스무 번 돌려 경로가 안정되면 그 경로를 스크립트로 굳히고 모델은 예외 상황에서만 부른다 / 매번 화면을 보고 새로 판단하는 것은 아직 무엇을 할지 모를 때의 방식이지 아는 일을 반복하는 방식이 아니다

- `rag-hybrid-search-tuning` (지금 3,644자 · 절 9)
  - 절「기본값이 안 맞는 세 이유」 — 소절: 널리 쓰이는 $$k=60$$ 은 영어 웹 문서와 학술 검색 벤치마크에서 잘 나온 값이지 우리 문서에서 잘 나온 값이 아니다 / 문서 종류에 따라 한쪽이 확연히 나은데 융합 기본값은 둘을 대등하게 다룬다 / 1차 검색·융합·리랭커 중 어디가 문제인지 구별을 못 하니 손잡이를 아무거나 돌린다 — 셋째가 가장 크다
  - 절「손잡이 네 자리의 순서」 — 소절: 앞쪽 손잡이가 뒤쪽의 상한을 정한다 — 리랭커는 주어진 후보를 다시 줄 세울 뿐 없는 문서를 만들지 않는다 / 실무의 흔한 순서가 정확히 거꾸로인 이유 — 리랭커 교체가 API 한 줄이라 제일 쉽다 / `recall@N` 하나만 재면 순서가 정해진다. 0.72면 리랭커를 무엇으로 바꿔도 최종 정확도의 천장이 0.72다 / 질의와 정답 문서 쌍 100~200개를 사람이 붙이는 일을 건너뛰면 이후 모든 튜닝이 감으로 하는 일이 된다
  - 절「후보 깊이와 토크나이저」 — 소절: $$N$$을 50→100→200으로 올리며 `recall@N` 곡선이 평평해지는 지점 조금 뒤에서 멈춘다 — 대개 100~200 / 어간 처리 — 「검색하다·검색했다·검색합니다」가 한 토큰으로 안 묶이면 질의와 문서가 아예 안 만난다 / 복합명사와 조사·어미 — 「전자정부표준프레임워크」를 통째로 두면 「표준 프레임워크」로 안 걸리고 너무 잘게 쪼개면 엉뚱한 문서가 걸리며, 조사·어미가 토큰에 남으면 점수가 흐려지고, 제품 코드·버전 문자열은 형태가 온전히 보존되는지 따로 본다 / 공백 분리에서 형태소 분석기로 바꾸는 하나가 뒤쪽 손잡이 전부를 합친 것보다 클 수 있다
  - 절「RRF의 k와 가중치」 — 소절: $$\sum_i w_i/(k+r_i(d))$$ 에서 $$k$$가 크면 「몇 위였는지」보다 「두 검색기가 함께 뽑았는지」가 중요해진다 / 실력이 비슷하고 상보적이면 60 이상, 한쪽이 확연히 정확하면 10~30으로 낮추고 그쪽 가중치를 올린다 / 질의에 따옴표·제품 코드·오류 번호가 들어 있으면 키워드 쪽을 올리는 동적 가중치를 규칙 몇 줄로 / 점수를 정규화해 섞지 않는 이유 셋 — 질의마다 정규화 기준이 달라지고, 아무것도 못 찾은 검색기의 최고 점수가 1.0이 되고, 코퍼스가 바뀌면 어제 맞춘 가중치가 오늘 안 맞는다
  - 절「리랭커와 지연의 교환」 — 소절: 교차 인코더가 질의와 문서를 함께 넣어 정확한 대신 후보 수에 비례해 느린 구조 / $$M$$이 작으면 융합이 아래로 밀어 둔 정답을 못 살리고, 크면 지연이 그만큼 는다. 10~30에서 시작한다 / 여기서 재는 것은 최종 정확도가 아니라 「이득 대비 늘어난 지연」이다 — 20에서 50으로 올려 1%p에 300ms인 교환이 우리 제품에서 맞는지는 검색 품질이 답할 문제가 아니다
  - 절「증상에서 손잡이 찾기」 — 소절: 정답이 아예 안 나온다 → `recall@N`을 먼저 보고 $$N$$과 토크나이저 / 후보엔 있는데 아래에 있다 → 융합 전후 순위를 비교하고 $$k$$와 가중치 / 특정 유형 질의에서만 나쁘다 → 그 유형만 따로 재고 질의별 동적 가중치 / 검색 밖의 두 증상 — 최근 문서를 못 찾으면 색인 문제이고, 순위는 좋은데 답이 나쁘면 청크 크기와 문맥 구성이다. 이 둘을 먼저 배제하고 튜닝을 시작한다
  - 절「검색 층과 응답 층」 — 소절: 검색 층은 정답 문서 쌍만 있으면 되고 모델을 안 불러 싸고 빠르다 — 값 하나 바꿀 때마다 돌린다 / 응답 층은 비싸고 느리므로 검색 층에서 개선이 확인된 것만 올린다 / 세트는 실제 사용자 질의에서 뽑되 잘 안 되는 질의를 일부러 포함한다 — 잘 되는 것만 모으면 어떤 변경을 해도 점수가 안 움직인다

- `agent-evaluation` (지금 3,794자 · 절 7)
  - 절「에이전트 평가가 어려운 이유」 — 소절: 같은 입력에도 매번 다른 도구 호출 순서가 나와 한 번 돌린 결과로는 판단할 수 없다 / 최종 답이 맞아도 스무 걸음 돌아서 온 것과 세 걸음에 온 것이 같지 않다 / 수십 스텝짜리 일에서 「어디까지 성공인가」를 정의하는 문제 / 정확도와 비용이 맞바뀌어 지표 하나로 줄 세울 수 없다 — 그래서 결과·과정·값을 따로 잰다
  - 절「태스크 성공률」 — 소절: 「됐다」의 조건을 코드로 판정할 수 있게 만드는 일이 평가 설계의 절반이다 / 정확 일치·포함 여부·구조화 필드 대조 중 무엇으로 채점할지와 각각이 놓치는 것 / 비결정성 때문에 같은 케이스를 여러 번 돌려 성공률로 보는 이유와 몇 번이면 값이 안정되는가 / 케이스 수가 적으면 한 건 차이가 몇 %p를 흔든다
  - 절「과정을 재는 지표」 — 소절: 도구 선택이 맞았는가·순서가 맞았는가·불필요한 호출이 몇 번인가 세 축 / 정답 궤적과 대조하는 방식과, 정답 궤적을 만들 수 없을 때의 대안(금지 호출 목록·최소 호출 수) / 같은 도구를 세 번 연속 부르는 루프를 자동으로 잡아내기 / 최종 답이 맞았는데 과정이 나쁜 경우가 실제로 위험한 이유 — 다음 입력에서 무너진다
  - 절「LLM 평가자를 쓰는 자리」 — 소절: 정답이 없는 열린 질문에서 레퍼런스 비교와 절대 점수 두 방식 / 채점 기준을 항목으로 쪼개 주지 않으면 점수가 길이와 문체에 끌린다 / 평가자와 피평가자가 같은 모델일 때의 자기 선호 편향 / 사람이 매긴 표본 50건과 대조해 평가자 자체를 먼저 검증하는 단계
  - 절「비용과 지연이라는 축」 — 소절: 작업당 토큰·걸음 수·벽시계 시간을 성공률과 같은 표에 놓는다 / 성공률 2%p를 위해 비용이 두 배가 되는 변경을 받아들일지의 판단을 무엇으로 하는가 / 평균이 아니라 꼬리 지연(p95)이 제품을 정하는 자리
  - 절「회귀 세트 만들기」 — 소절: 실제 실패에서 케이스를 가져오는 것이 합성 케이스보다 나은 이유 / 잘 되는 것만 모으면 어떤 변경에도 점수가 안 움직인다 / 케이스가 낡는다 — 도구 스키마나 원본 데이터가 바뀌면 정답도 함께 바뀐다 / 몇 건에서 시작해 어떤 신호가 올 때 늘리는가
  - 절「평가를 돌리는 자리」 — 소절: 데이터셋·실행·채점·대시보드 넷을 어디에 두는가 / 프롬프트를 고칠 때마다 도는 것과 배포 전에만 도는 것을 가른다 / 운영 로그에서 실패를 자동으로 세트에 넣는 고리 / 평가 파이프라인을 개발 초기에 만드는 것과 나중에 만드는 것의 차이 — 나중에는 「무엇이 좋아졌는지 모르는」 상태에서 출발한다

- `rag-late-interaction` (지금 3,865자 · 절 8)
  - 원고에 이미 있는 것 — 정보 병목의 예, 세 방식의 대조, MaxSim 식과 두 성질, 저장 용량 표(2.0 GB → 614 GB)와 감축 셋(96~128차원·2비트·가지치기 30~40%), 1차/리랭커 표와 PLAID 한 줄, 안 쓰는 경우 넷, 재는 두 숫자. 지난 얼개는 이 전부를 소절로 옮겨 적은 것이었다. 아래는 그것을 첫 절 하나로 눌러 담고 나머지 여섯 절을 새로 쓰는 구성이다
  - 절「벡터 하나의 병목과 세 방식」 — 소절: 환불 기한·교환 기한·배송비·사업자 예외 네 사실이 벡터 하나의 평균으로 뭉개져 알맹이 없는 「환불 정책 안내」 제목 문서가 더 가깝게 나오는 예 / 바이인코더·크로스 인코더·레이트 인터랙션을 「무엇을 미리 계산할 수 있는가」 축에 놓으면 비용 구조가 그대로 보인다 / MaxSim 식과 거기서 따라 나오는 두 성질 — 부분 누락이 평균에 안 묻히고 문서 길이에는 벌점이 없다. 그 뒷면으로 표제어만 나열한 색인 페이지가 전 질의에서 상위에 뜬다
  - 절「토큰 벡터를 만드는 걸음」 — 소절: 질의와 문서에 서로 다른 표시 토큰을 붙여 인코더 하나가 두 역할을 갈라 쓰는 구조 / 짧은 질의를 마스크 토큰으로 채워 길이를 고정하는 질의 확장 — 빈 행이 생기는 것이 아니라 그 자리가 문서에서 걸릴 곳을 찾아 점수를 만든다는 것 / 토큰 벡터를 L2 정규화해야 내적이 코사인이 되고 양자화 눈금이 흔들리지 않는다는 조건 / 구두점·특수 토큰을 색인 단계에서 빼는 자리 — 원고가 「토큰 가지치기」라고만 적은 것이 실제로 어디서 일어나는가
  - 절「잔차 압축과 2단 검색」 — 소절: 토큰 벡터를 중심점 하나와 잔차 몇 비트로 나눠 담는 방식이 원고의 「2비트 양자화」가 실제로 하는 일이라는 것 / 그 중심점이 그대로 후보 생성 장치가 되는 구조 — 같은 자료를 압축에도 후보 축소에도 쓴다 / PLAID의 걸음을 나눠 적기 — 중심점으로 문서 후보 뽑기 → 약한 후보 쳐내기 → 남은 것만 정확히 채점, 각 걸음의 손잡이가 재현율과 지연 중 어디에 걸리는지 / 중심점 개수와 잔차 비트를 자기 데이터에서 정하는 순서
  - 절「저장 용량 계산의 입력값」 — 소절: 원고 표의 300토큰은 가정이므로 자기 저장소의 청크 토큰 수 분포를 먼저 재고 평균이 아니라 상위 백분위로 계산한다 / 차원 축소·양자화·가지치기 셋의 배수를 곱해 실제 총량을 얻고, 셋을 다 해도 바이인코더의 다섯 배라는 결론까지 가기 / 지연 쪽 입력값 — 후보 수 × 질의 토큰 수 × 문서 토큰 수가 그대로 행렬 곱의 크기가 된다 / 메모리에 올릴지 디스크에서 읽을지가 이 두 숫자에서 갈린다
  - 절「한국어에서의 MaxSim」 — 소절: 서브워드가 낱말을 쪼개면 행이 낱말이 아니라 조각 단위가 되어 「환불」이 두 행으로 갈리는 자리 / 조사·구두점 30~40%를 쳐낼 때 의미 있는 조각까지 함께 지웠는지 확인하는 법 — 가지치기 전후로 같은 질의 묶음을 돌려 대조한다 / 한국어로 학습한 레이트 인터랙션 모델이 드물어 영어 모델을 그대로 쓸 때 무엇이 먼저 무너지는지 재 보기
  - 절「이웃 방식과의 자리」 — 소절: SPLADE — 같은 「벡터 하나로 안 뭉친다」를 희소 쪽에서 풀어 역색인을 그대로 쓴다는 차이 / ColPali — 문서를 이미지 패치 벡터로 남겨 파싱 단계를 통째로 건너뛰는 갈래이고, 저장 계산은 여기서도 똑같이 걸린다 / 넷을 「무엇을 미리 계산하나 · 저장이 얼마나 드나 · 무엇을 고치나」 세 축의 한 표로 놓기
  - 절「도입 절차와 두 숫자」 — 소절: 리랭커 자리부터 — 색인을 다시 만들지 않아도 되고 효과가 없으면 뺀다. 후보 수 $$M$$을 10~30에서 시작해 이득 대비 지연으로 정한다 / 판단하는 두 숫자 — 1차의 `recall@50`이 0.95를 넘으면 1차를 바꿔 얻을 것이 없고, 평가 집합을 질의 길이로 나누면 대개 긴 질의 쪽에서만 차이가 난다 / 값을 못 하는 자리 넷(짧은 청크·짧은 질의·정확한 문자열·잦은 모델 교체)을 도입 판단의 반대편에 나란히 두기 / 1차 검색으로 올릴 때의 조건 — 앞 절의 2단 구조가 준비돼 있는가

- `agent-browser-automation` (지금 3,900자 · 절 8)
  - 절「네 층으로 나뉜 구조」 — 소절: CDP가 크롬이 자기 안을 밖에 열어 주는 통로이고, 그 위에 Playwright·Puppeteer가 「요소를 집는다」·「기다린다」 손잡이를 얹었다 / 아래 세 층은 에이전트가 등장하기 훨씬 전부터 QA 자동화가 다듬어 온 것이다 / 새로 짤 것은 맨 위 하나 — 검증된 라이브러리를 도구로 감싸 모델에게 내주는 일 / 그 도구를 몇 개로 쪼개 어떤 이름으로 내줄지가 모델의 고르기 정확도를 정한다
  - 절「페이지를 넣는 세 방법」 — 소절: 원본 HTML은 한 페이지 300KB에 7만 토큰대이고 광고·인라인 스크립트·추적 픽셀이 9할이다 / 스크린샷은 1,000토큰대지만 글자를 다시 읽어야 하고 스크롤 밖은 안 보인다 / 접근성 스냅샷은 보통 2천~8천 토큰에 요소마다 이름·역할·번호가 붙어 오고(라벨 없는 요소는 빠진다), 각 줄의 `ref`를 그대로 돌려주면 모델이 좌표도 선택자도 지어낼 필요가 없다 — 지어낼 자리를 없애는 것이 곧 실패를 없애는 것이다 / 캔버스 차트·지도처럼 트리에 이름이 안 잡히는 자리에서만 스크린샷을 한 장 보조로
  - 절「기다리는 방식」 — 소절: `sleep(2초)`는 두 방향으로 틀린다 — 느린 날엔 없는 버튼을 누르고 빠른 날엔 0.3초면 될 일을 2초 세운다. 걸음 스무 개면 그것만 40초다 / 조건 대기는 시간이 아니라 상태를 기다린다 — 「저장 버튼이 보일 때까지」, 「진행 표시가 사라질 때까지」 / 요즘 라이브러리는 클릭·입력 안에 대기를 이미 넣어 두어 기본 동작만 그대로 써도 절반이 해결된다 / 남는 자리는 「무엇이 보이면 저장된 것인가」 — 토스트인지, 새 행인지, 회색이 된 버튼인지를 절차마다 하나씩 적어 준다
  - 절「요소를 가리키는 다섯 층」 — 소절: 역할과 이름 → `data-testid` → 보이는 글자 → CSS 선택자 → 절대 XPath 순으로 수명이 짧아진다 / 개발자 도구의 「Copy XPath」가 만들어 주는 형태가 아무 생각 없이 쓰기 쉬운 그 형태다 — 요소 하나만 끼어들어도 끝난다 / 우리가 만든 서비스라면 테스트용 표시를 심어 두는 것이 가장 확실하다 / `ref` 번호는 그 스냅샷 안에서만 유효하다 — 페이지가 다시 그려지면 번호가 바뀌므로 동작 하나마다 새로 뜬다
  - 절「로그인과 세션 상태」 — 소절: 매번 아이디와 비밀번호를 넣게 하면 자격 증명이 모델 문맥을 지나가고 2단계 인증이 붙은 곳에서는 아예 막힌다 / 사람이 한 번 로그인해 쿠키·세션 상태를 파일로 남기고 에이전트가 그 상태로 브라우저를 띄운다 / 에이전트용 계정을 따로 파 필요한 권한만 주면 잘못 눌렀을 때 닿는 범위가 그 안으로 묶인다 — 모델을 얌전하게 만드는 것보다 확실하다 / 남의 사이트에서 캡차나 봇 탐지에 걸리는 것은 고칠 버그가 아니라 하지 말라는 뜻이다. 약관과 `robots.txt`를 먼저 보고 공식 API 경로를 찾는다
  - 절「페이지 글자와 주입」 — 소절: 사람 눈에 안 보이게 심어 둔 글자가 접근성 스냅샷에는 그대로 들어온다 — 공격자가 모델에게 직접 말을 거는 대신 모델이 읽을 것이 확실한 자리에 지시를 심는 구조다 / 완전히 막는 대신 닿는 범위를 좁힌다 — 허용한 도메인 밖으로는 이동 자체가 안 되게 한다 / 페이지에서 가져온 내용은 「참고 자료」로 표시해 넣고 할 일은 시스템 쪽에만 둔다 / 결제·전송·삭제·권한 변경은 모델이 아니라 코드가 갈라 사람이 확인한다
  - 절「브라우저를 안 쓰는 선택」 — 소절: 공개 API가 있으면 HTTP 요청 한 번이 100배 싸고 빠르다 — 사내 시스템이면 DB 조회 권한이나 내부 API를 먼저 요청해 본다 / 목록·게시물을 훑는 일은 RSS·사이트맵·공개 데이터셋으로 / 내려받은 파일은 직접 읽고 화면에서 읽지 않는다 / 같은 화면을 매일 도는 일은 경로가 굳었으면 정해진 스크립트로 옮기고 모델은 예외에만 부른다


### llm-core

**합치기 — 짝이 남지 않았다(2026-09-08).** 62편 → 51편.


**채우기**

- `transformer-basics` (지금 896자 · 절 6)
  - RNN·Seq2Seq가 실제로 막혔던 자리 — 순차 처리, 기울기 소실, 고정 길이 컨텍스트 벡터
  - 블록 다이어그램을 텐서 모양으로 따라가기: 임베딩부터 출력 로짓까지
  - d_model·헤드 수·레이어 수·d_ff가 서로 묶여 있는 이유와 원논문의 base/big 설정
  - 원논문의 학습 설정: Adam warmup 스케줄, label smoothing, 잔차 dropout
  - 인코더만·디코더만·둘 다 — 셋으로 갈린 계보와 왜 디코더만이 남았는가
  - 2017년 구조에서 지금 바뀐 것: Pre-Norm, RoPE, GQA, SwiGLU

- `transformer-multi-head` (지금 1,220자 · 절 7)
  - 절「한 헤드로 부족한 이유」 — 소절: 소프트맥스 한 번이 분포 하나만 만든다는 제약과 관계 셋을 동시에 못 담는 근거 / d_k를 d_model/h로 잡아야 총 연산량이 단일 헤드와 같아지는 계산 / W_O 없이 concat만 하면 헤드 사이가 섞이지 않는다
  - 절「헤드를 쪼개는 구현」 — 소절: view·transpose가 메모리를 옮기지 않고 stride만 바꾸는 것과 contiguous()가 필요해지는 지점 / 마스크를 (B,1,T,T)로 두고 브로드캐스트하는 모양 / 단계마다 텐서 모양을 적은 대조표
  - 절「헤드가 나눠 맡는 관계」 — 소절: 어텐션 가중치 행렬을 뽑아 대각·인접·문두 집중 비율로 헤드를 분류하는 절차 / 헤드를 하나씩 0으로 만드는 절제 실험과 성능 변화 / 헤드 수를 늘려 d_k가 16 아래로 내려갔을 때의 표현력 손실
  - 절「비용의 두 축」 — 소절: 파라미터 수는 h와 무관하고 활성값 메모리는 h·T²에 비례한다는 대조 / 시퀀스 512·2048·8192에서 어텐션 행렬 크기를 직접 계산 / FlashAttention이 그 행렬을 물리적으로 만들지 않는 방식
  - 절「K·V를 줄이는 갈래」 — 소절: KV 캐시 크기 식에 h·g·1을 대입한 세 경우 비교 / g를 고를 때 보는 값(품질 하락과 캐시 절감의 교차점) / MQA가 무너지는 작업 유형
  - 절「정리와 연습」 — 소절: d_model·h·T를 바꿔 가며 파라미터와 KV 캐시를 손으로 계산하는 문제 넷 / W_O를 빼면 무엇이 깨지는지 확인하는 문제

- `transformer-encoder` (지금 1,556자 · 절 7)
  - Add & Norm이 없으면 무슨 일이 생기는가 — 잔차 경로와 기울기 전달
  - Pre-Norm과 Post-Norm: 학습 곡선이 갈리는 지점과 warmup 의존성
  - FFN의 d_ff가 4배인 이유와 「지식 저장소」 해석의 근거
  - 레이어를 프로빙하면 무엇이 보이는가 — 얕은 층·중간 층·깊은 층의 표현
  - 패딩 마스크가 인코더에서만 필요한 이유
  - 인코더 전용 모델이 지금 남아 있는 자리 — 임베딩, 리랭커, 분류기

- `llm-instruction-tuning` (지금 1,640자 · 절 6)
  - 채팅 템플릿이 학습과 추론에서 어긋나면 나타나는 증상
  - 손실 마스킹: 프롬프트 토큰을 손실에서 빼는 이유
  - 데이터 규모 대 품질 — LIMA의 1,000건과 InstructGPT의 13,000건
  - 다양성을 어떻게 확보하는가: 태스크 분포와 길이 분포
  - SFT가 만드는 부작용 — 장황함, 정형 문구, 거절 과잉
  - 합성 데이터를 쓸 때의 검수 절차와 라이선스
  - 무엇으로 끝났다고 판단하는가 — 보류 세트와 LLM-as-Judge의 한계
  - RLHF·DPO는 다음 글로 넘기고 SFT 한 단계에 집중하도록 절 재배치

- `transformer-mqa-gqa` (지금 1,708자 · 절 6)
  - KV 캐시 용량 공식을 직접 세워 보기 — 배치·길이·레이어·헤드의 곱
  - MQA의 품질 저하가 실제로 어디서 나타나는가
  - MLA(Multi-head Latent Attention): DeepSeek이 KV를 압축한 방식
  - KV 캐시 양자화(FP8·INT4)를 GQA와 함께 쓸 때
  - PagedAttention·연속 배칭과의 관계 — 메모리 절감이 처리량으로 바뀌는 경로
  - 업사이클링 실전: 평균 풀링으로 초기화한 뒤 얼마나 학습해야 하는가

- `transformer-gpt` (지금 1,725자 · 절 8)
  - GPT-1이 실제로 보인 것: 사전학습 + 태스크별 헤드
  - GPT-2의 zero-shot과 「태스크를 프롬프트로 적는다」는 발상
  - In-Context Learning이란 무엇이고 무엇이 아닌가 — 가중치는 변하지 않는다
  - GPT-3 스케일 표와 few-shot 곡선 읽는 법
  - Decoder-only가 Encoder-Decoder를 밀어낸 이유 — 학습 효율과 태스크 통일
  - InstructGPT: 벤치마크 점수 대신 사람 선호가 기준이 된 지점

- `tokenizer-and-tokens` (지금 1,765자 · 절 6)
  - 토큰이 곧 돈이자 컨텍스트다 — 요금·한도가 토큰으로 매겨지는 구조
  - 한국어 토큰 효율을 직접 재기: 같은 문장을 모델별로 세어 비교
  - 토큰 경계가 만드는 실제 버그 — 숫자 분해, 글자 세기 실패, 스트리밍 중 깨진 한 글자
  - 어휘 크기와 임베딩·출력 행렬이 차지하는 파라미터 비중
  - 특수 토큰과 프롬프트 인젝션: 사용자 입력에서 특수 토큰을 막는 이유
  - 토크나이저를 바꾸면 무엇이 깨지는가 — 어휘 확장과 임베딩 초기화

- `transformer-bert` (지금 1,788자 · 절 8)
  - MLM의 80/10/10 규칙 — 왜 전부 [MASK]로 두지 않는가
  - NSP가 왜 버려졌나: RoBERTa·ALBERT가 확인한 것
  - 세 임베딩(토큰·세그먼트·위치)의 합으로 만드는 입력
  - 파인튜닝 네 패턴: 문장 분류, 문장 쌍, 토큰 분류, 추출형 QA
  - 한국어 BERT의 토크나이저 문제와 KLUE·KoBERT가 고친 것
  - BERT를 생성에 쓸 수 없는 이유 — 양방향성과 자기회귀의 충돌

- `transformer-moe` (지금 1,796자 · 절 7)
  - 라우터 수식과 Top-K 게이팅의 미분 가능성 문제
  - 로드 밸런싱 보조 손실이 없으면 벌어지는 일 — 전문가 붕괴
  - Expert Choice와 Token Choice의 토큰 드롭 동작 차이
  - 전문가 병렬: all-to-all 통신 비용과 배치 크기의 관계
  - 추론 메모리는 왜 줄지 않는가 — 활성 파라미터와 적재 파라미터
  - DeepSeek-V3의 공유 전문가·세밀한 전문가와 최근 설계 흐름
  - MoE가 손해인 자리: 작은 배치, 파인튜닝, 온디바이스

- `llm-pretraining` (지금 1,934자 · 절 5)
  - 데이터 파이프라인 각 단계에서 얼마나 버려지는가 — 단계별 감소 비율
  - 중복 제거: MinHash·SimHash와 문서 간/문서 내 중복
  - 데이터 믹스와 커리큘럼 — 코드·수학 비중이 바꾸는 것
  - 3D 병렬(데이터·텐서·파이프라인)을 언제 무엇으로 조합하는가
  - 손실 스파이크가 났을 때의 대응 절차와 체크포인트 롤백
  - 학습 비용 계산: FLOPs 추정에서 GPU-시간과 금액까지
  - 베이스 모델을 무엇으로 판단하는가 — perplexity가 못 보는 것

- `structured-output-validation` (지금 2,191자 · 절 6)
  - 절「검증의 네 층」 — 소절: 층별 실패율을 실제 로그에서 세는 계측 코드와 그 값이 알려 주는 것 / 층마다 실패를 어디로 보낼지 정하는 분기(즉시 재시도·폴백 큐·사람 검토) / 층 구분이 없는 코드가 같은 검사를 두 번 하게 되는 자리
  - 절「의미 검증 규칙의 구조」 — 소절: 규칙을 함수 대신 선언 목록으로 두고 필드마다 붙이는 형태 / 규칙 id와 발생 건수를 함께 남겨 어느 규칙이 실제로 걸리는지 세는 방식 / 필드 간 일관성 규칙을 조합표로 적어 빠뜨림을 막는 절차
  - 절「되먹임 재시도의 상한」 — 소절: 수리 1회·2회·3회의 성공률을 재서 상한을 정하는 실측 절차 / 재시도마다 컨텍스트가 늘어 비용이 붙는 계산 / 수리해도 안 되는 입력을 분류해 폴백 큐로 보내는 기준
  - 절「근거 인용의 대조 구현」 — 소절: normalize를 어디까지 할지(공백·전각·조사) 정하는 기준과 과한 정규화가 만드는 오탐 / 완전 일치 대신 유사도 임계값을 쓸 때 임계값을 정하는 절차 / 인용 필드가 있는 스키마와 없는 스키마의 환각율을 나란히 재는 실험 설계
  - 절「검증 지표 대시보드」 — 소절: 스키마 버전과 지표를 함께 남겨 회귀 시점을 짚는 방법 / 필드별 수리율이 10%를 넘을 때 description을 고치는 구체적 예 / 폴백 비율 상승과 입력 분포 변화를 구별하는 신호
  - 절「검증을 옮기는 스키마 설계」 — 소절: 널 자리·인용 필드·출처 enum을 넣어 검증 가능한 스키마로 바꾼 전후 비교 / 검증 규칙 중 스키마로 옮길 수 있는 것과 끝까지 코드에 남는 것의 구분

- `slm-overview` (지금 2,245자 · 절 5)
  - 절「크기와 메모리의 관계」 — 소절: 홑 `$`로 적힌 기호를 전부 `$$`로 고친다(P·b·L·H_kv·d_h·S) / 7B를 16·8·4비트로 올렸을 때 가중치·KV·활성값을 각각 더해 실제 소요 메모리를 계산한 예 / 문맥 4K·32K·128K에서 KV가 가중치를 넘어서는 지점
  - 절「같은 크기가 좋아진 이유」 — 소절: 데이터 품질·증류·긴 학습·양자화 넷이 각각 어느 값을 올렸는지 구분 / 학습 1회와 추론 수억 회의 총비용을 식으로 놓고 작은 모델이 이기는 조건 / 한국어 토크나이저 효율이 같은 문장의 토큰 수를 바꾸는 폭
  - 절「능력이 갈리는 축」 — 소절: 「밖에서 주어지는 정보량」과 「답의 형태가 정해진 정도」 두 축으로 작업을 사분면에 놓기 / 검색을 붙였을 때 지식 격차가 얼마나 메워지는지 재는 실험 설계 / 긴 문맥에서 놓치는 폭을 바늘 찾기로 재는 절차
  - 절「양자화 고르기」 — 소절: 4비트 방식들이 무엇을 다르게 하는지(가중치만·활성까지·그룹 크기) / 양자화 전후를 자기 평가 묶음으로 재서 손실을 수치로 확인하는 절차 / 양자화에서 특히 크게 무너지는 작업(긴 계산·도구 호출 형식)
  - 절「작은 모델이 맡는 자리」 — 소절: 전처리·일괄 처리·기기 안·초안 넷의 요청량과 단가를 곱해 절감액을 계산한 예 / 실패를 알아챌 방법이 있는지로 자리를 거르는 기준 / 큰 모델과 나란히 두는 구성(라우팅·초안·검증)의 차이
  - 절「고를 때 확인하는 것」 — 소절: 실행기(llama.cpp·vLLM·MLX)마다 되는 것과 안 되는 것 / 라이선스 조항에서 실제로 걸리는 항목(재배포·파생 모델·사용자 상한) / 바꿔 끼울 수 있는 구조로 두는 코드 형태와 자기 평가 묶음 붙이기

- `slm-vs-llm-tradeoff` (지금 2,317자 · 절 5)
  - 절「폴백을 넣은 비용식」 — 소절: 홑 `$`를 전부 `$$`로 고친다(C·c_s·c_l·p·p*) / 트래픽 규모별로 자체 서빙과 API의 실효 단가가 교차하는 지점을 계산한 표 / 폴백 판정 자체가 드는 비용(검증기 호출·재시도)을 식에 더한 형태
  - 절「지연 분포로 보는 이득」 — 소절: 폴백 비율을 0.1·0.3·0.5로 놓고 p50과 p95를 각각 계산한 대조 / 라우팅 구성과 폴백 구성의 지연 분포를 나란히 놓기 / 기기 안 실행에서 초당 토큰 수와 왕복 지연이 뒤집히는 출력 길이
  - 절「격차를 재는 절차」 — 소절: 실제 요청 200건을 뽑을 때 분포를 닮게 만드는 표본 추출 방법 / 두 모델의 답이 갈린 건만 사람이 보는 채점 흐름과 채점 시간 견적 / 격차를 신뢰구간과 함께 적어 노이즈와 구별하는 계산
  - 절「네 관문의 통과 기준」 — 소절: 관문마다 통과·불통과를 판정할 숫자를 미리 적어 두는 형식 / 실패를 자동으로 가려낼 수단이 없는 작업에서 그 수단을 먼저 만드는 순서 / 통과 못 한 작업을 어디에 기록해 다시 볼지
  - 절「전환과 되돌리기」 — 소절: 그림자 배포로 며칠치 답을 비교하는 구성과 그때 볼 지표 / 롤백을 부르는 조건을 숫자로 적어 두기 / 단계적 전환(10%·50%·100%)에서 각 단계의 관측 기간
  - 절「숨은 비용의 목록」 — 소절: 프롬프트 두 벌을 관리하는 실제 비용(규칙 변경 시 두 곳 수정·평가 두 벌) / 작은 모델의 교체 주기를 견적에 넣는 방법 / 자체 서빙에서 새로 생기는 운영 항목의 인건비 환산

- `llm-korean-models` (지금 2,425자 · 절 5)
  - 한국어 토큰 효율을 모델별로 실제로 세어 비교한 표
  - KMMLU·HAE-RAE·KoBEST가 각각 무엇을 재는가
  - 라이선스와 상업적 이용 조건 비교
  - 온프렘·주권 AI 요구가 선택을 바꾸는 자리
  - 글로벌 모델로 충분한 작업과 그렇지 않은 작업의 경계
  - 한국어 파인튜닝에서 어휘 확장이 필요한 경우와 그 비용

- `reasoning-when-to-use` (지금 2,484자 · 절 5)
  - 절「세 질문의 순서」 — 소절: 질문 1의 「기계적 검증」을 실제 작업에서 만들어 보는 절차(부분 검증도 검증이다) / 질문 2에서 100건이 적을 때의 신뢰구간과 필요한 표본 수 계산 / 질문 3에서 병렬 샘플링과 추론 모델의 지연·비용을 나란히 놓은 대조
  - 절「실패 유형 라벨링」 — 소절: 실패 30건을 다섯 유형으로 가르는 코딩 시트 양식과 두 사람이 갈릴 때의 처리 / 유형별 비중에 처방의 기대 효과를 곱해 순서를 정하는 계산 / 「접근 오류」로 분류한 건을 다시 읽어 오분류를 걸러 내는 검토
  - 절「등급 라우팅의 설계」 — 소절: 규칙을 트래픽 로그로 다듬는 절차(오분류된 요청을 뽑아 규칙에 반영) / 분류 모델을 앞에 붙일 때의 손익분기(그 호출의 지연·비용 대 절감액) / 등급별 트래픽 비중이 예상과 어긋날 때 의심할 자리
  - 절「승급 경로의 조건」 — 소절: 무엇을 실패로 볼지 정의하기(검증 불통과·예산 소진·형식 오류) / 승급 성공률을 재서 승급 자체를 접는 기준 / 승급 대신 사람에게 넘기는 편이 싼 작업 유형
  - 절「붙이면 손해인 자리」 — 소절: 목록의 항목마다 실제로 무엇이 나빠지는지(지연 지배·해석 추가·길어짐) / 이미 붙어 있는 경로를 찾아내는 로그 질의 / 떼어 냈을 때의 절감액을 미리 추정하는 계산
  - 절「결정 기록의 양식」 — 소절: 기록에 남길 항목을 표로 못 박기(문항 집합·정답률·비용·p95·실패 유형 비중·판정) / 재평가 주기를 정하는 기준(모델 교체·트래픽 변화·정답률 하락) / 반년 뒤 재평가를 하루에 끝내는 실행 절차

- `llm-essence` (지금 2,500자 · 절 8)
  - 「크다」가 정확히 무엇을 바꾸는가 — 파라미터·데이터·연산 셋을 나눠 보기
  - 다음 토큰 예측만으로 왜 지식이 생기는가
  - 베이스 모델과 어시스턴트 모델의 차이를 같은 프롬프트로 보이기
  - 창발 논쟁을 한 절로 요약하고 이 글의 입장을 밝히기
  - LLM이 못 하는 것을 먼저 말하기 — 최신성, 계산, 자기 지식의 경계
  - 가문별 지형을 한 장으로: 프런티어·오픈 웨이트·소형

- `llm-emergent-abilities` (지금 2,513자 · 절 6)
  - 절「창발의 정의」 — 소절: 「예측 불가능」의 두 뜻을 가르기(어느 능력이 나타날지 모름 · 언제 나타날지 모름) / 손실은 매끄럽게 내려가는데 정확도만 계단인 상황을 한 그림에 겹쳐 놓기 / 창발이라 부르지 않는 것(점진 향상·파인튜닝으로 생긴 능력)의 경계
  - 절「창발로 불린 능력들」 — 소절: 능력별로 보고된 임계 규모를 한 표에 모으고 그 값이 논문마다 갈리는 폭 / 자릿수 올림 처리 능력을 자릿수별 정확도로 분해해 재는 실험 / 임계값이 데이터 품질·토크나이저에 따라 옮겨 간 사례
  - 절「측정 방식 논쟁」 — 소절: Exact Match와 Partial Credit이 같은 로그에서 다른 곡선을 그리는 것을 직접 재는 코드 / 지표를 바꿔도 계단이 남는 능력과 사라지는 능력의 구분 / 문항 수가 적을 때 계단처럼 보이는 표본 효과
  - 절「임계값 예측의 시도」 — 소절: 작은 모델들의 곡선으로 큰 모델 성능을 외삽하는 방법과 그것이 깨지는 자리 / 능력이 나타나기 전에 미리 재는 대리 지표 / 규모가 아니라 데이터·후훈련이 임계를 옮긴 예
  - 절「안전 쪽의 함의」 — 소절: 사전 평가에서 무엇을 재는지(위험 능력 목록과 그 측정 형태) / 단계별 안전 조치가 실제로 걸리는 지점 / 해석 가능성 연구가 창발 탐지에 쓰이는 방식
  - 절「남은 물음」 — 소절: 창발이 진짜인지가 실무 판단을 바꾸는 자리(모델 교체 주기·평가 설계) / 규모 밖의 축에서 능력이 나타나는 지금의 그림

- `reasoning-cost-latency` (지금 2,526자 · 절 6)
  - 절「요청 단가의 구성」 — 소절: 사고 토큰 분포를 요청 유형별 히스토그램으로 그려 중앙값과 p95를 뽑는 절차 / 월 비용을 분포로 시뮬레이션해 신뢰구간과 함께 내놓는 계산 / 배치 처리 할인과 캐시 적중을 넣은 실효 단가식
  - 절「지연을 만드는 구간」 — 소절: 프리필·사고·답변 각 구간을 실측으로 나눠 재는 계측 코드 / 동시 요청이 늘 때 큐 대기가 붙어 p95가 무너지는 지점 / 사고 요약 스트리밍을 켰을 때 체감 지연이 바뀌는 폭을 재는 방법
  - 절「꼬리를 자르는 두 손잡이」 — 소절: 예산 상한과 정답률의 교환 곡선을 자기 데이터로 그리는 절차 / 타임아웃을 p99에서 정하고 강등 비율을 감시하는 운영 / 강등된 요청의 품질을 따로 재서 강등이 손해인지 보는 검사
  - 절「캐시가 듣는 자리」 — 소절: 응답 캐시 키를 만드는 정규화(공백·대소문자·의미 임베딩)와 각 방식의 적중률 / 캐시 적중이 잘못된 답을 굳히는 위험과 무효화 기준 / 프롬프트 캐시와 응답 캐시를 같이 켰을 때의 절감 합산
  - 절「비용을 줄이는 순서」 — 소절: 1번(사고가 필요 없는 요청 걸러 내기)을 실제로 실행하는 로그 질의와 분류 절차 / 각 단계의 절감폭을 자기 트래픽으로 추정해 순서를 다시 매기는 방법 / 손대면 안 되는 자리(품질 위험이 큰 5·6번)를 판단하는 기준
  - 절「계측 항목의 설계」 — 소절: 다섯 지표를 남길 로그 스키마와 요청 유형 태그를 처음부터 넣는 이유 / 지표를 유형별로 갈라 보는 대시보드 구성 / 지표만 보고 어느 손잡이를 돌릴지 정하는 판단표

- `structured-output-constrained-decoding` (지금 2,572자 · 절 6)
  - 절「로짓 마스킹의 자리」 — 소절: 마스킹이 온도·top-p·반복 페널티와 어느 순서로 겹치는지 / 마스크를 씌운 뒤 확률이 재정규화되면서 남은 후보의 분포가 바뀌는 것 / 로그 확률을 기록할 때 마스킹 전후 중 무엇을 남길지
  - 절「토크나이저 경계의 문제」 — 소절: 어휘 15만에서 상태별 허용 집합을 담는 자료구조(트라이·비트마스크)와 그 메모리 / 같은 문자열을 다르게 쪼갤 수 있는 토큰화가 만드는 경로 중복 / 정규식 `pattern`이 상태를 폭발시키는 예와 그 대응
  - 절「컴파일 비용과 캐시」 — 소절: 스키마 컴파일 시간을 실제로 재는 절차와 복잡도에 따른 값 / 동적 스키마의 캐시 키를 구조 해시로 만드는 방법과 적중률 감시 / 첫 토큰 지연이 튀는 사고를 캐시 미스로 되짚는 진단
  - 절「제약이 지우는 불확실성」 — 소절: 널 자리와 출처 enum을 넣기 전후의 환각율을 같은 입력으로 재는 실험 / 모델이 스스로 모순을 적는 비율을 재서 이 신호를 얼마나 믿을지 정하기 / 국소 선택이 막다른 길로 가는 사례(상한 길이·enum 접두사 충돌)
  - 절「빈 허용 집합의 진단」 — 소절: 마스크가 비었을 때 구현체마다 다른 동작(예외·EOS 강제·무시) / 스키마와 어휘가 어긋나는 자리를 미리 찾는 검사 / 로짓 프로세서를 직접 붙일 때 상태 전이를 검증하는 단위 테스트
  - 절「스트리밍 부분 파싱」 — 소절: 부분 JSON을 안전하게 읽는 파서의 규칙(닫히지 않은 문자열·미완 숫자) / 필드가 완성됐다고 판단하는 시점 / 제약 없이 부분 파싱을 했을 때 실제로 깨지는 경로

- `llm-comparison-benchmarks` (지금 2,621자 · 절 8)
  - 절「벤치마크가 답하는 것」 — 소절: 세 평가 방식(주관·태스크 특화·표준 벤치마크)의 재현성과 비용을 나란히 견주기 / 점수 하나에 뭉쳐 있는 조건(문항·채점·시도 횟수·프롬프트)을 풀어 적기 / 같은 벤치마크를 자기 손으로 재현했을 때 점수가 갈리는 폭
  - 절「지식·코딩 벤치마크」 — 소절: **본문의 모델별 점수를 지금 세대 모델로 다시 채운다** — 출처와 측정 시점을 값마다 함께 적고, 낡은 2024년 수치를 그대로 두지 않는다 / pass@k 정의와 k가 다른 표를 비교하면 안 되는 이유를 식으로 / MMLU 후속 벤치마크들이 무엇을 고쳤는지(오답 정제·난도 상향·객관식 탈피)
  - 절「사람 선호와 LLM 심사」 — 소절: ELO가 표본 수에 따라 갖는 신뢰구간과 순위가 겹칠 때의 해석 / 스타일 편향을 보정한 순위와 원래 순위가 갈리는 자리 / LLM-as-Judge의 자기 편애·순서 편향과 그 완화(위치 교체·다중 심사)
  - 절「점수가 오염되는 경로」 — 소절: 해설이 크롤링을 타고 학습 데이터에 들어가는 경로를 단계로 / 오염과 포화를 가르는 신호 / 수명 곡선의 어느 지점에서 그 벤치마크를 버릴지 정하는 기준
  - 절「자체 평가 묶음」 — 소절: 골든 데이터셋 50~100건을 실제 로그에서 뽑는 표본 설계 / 자동 채점과 사람 채점을 섞는 비율과 채점 기준서 양식 / 정확도 옆에 비용·p95·형식 준수율을 함께 적은 결정표
  - 절「모델을 갈아 끼우는 운영」 — 소절: 새 모델이 나왔을 때 반나절에 답을 내는 실행 절차 / 평가 묶음을 버전으로 관리하고 실패 사례를 계속 더하는 운영 / 벤치마크 카드에 남길 항목(측정일·조건·재현 명령)

- `llm-llama-family` (지금 2,627자 · 절 7)
  - 절「가중치를 여는 선택」 — 소절: 「오픈소스」와 「오픈 웨이트」의 차이(데이터·코드·가중치 중 무엇이 열렸나)를 표로 / 유출이 만든 생태계가 이후 공개 정책에 남긴 것 / 공개가 회사에 돌아오는 이득(생태계 표준·인재·비용 전가)
  - 절「버전별로 바뀐 것」 — 소절: **버전 표를 지금 나와 있는 세대까지 다시 채운다** — 크기·문맥 길이·어휘 크기·라이선스 조건을 열로 두고 값마다 출처와 확인일을 적는다 / 어휘 32K→128K가 한국어 토큰 수를 실제로 얼마나 줄이는지 같은 문장으로 재기 / GQA와 긴 문맥이 추론 비용에 미친 변화를 KV 캐시 식으로
  - 절「로컬에서 돌리는 법」 — 소절: 실행기 셋(Ollama·llama.cpp·vLLM)이 맡는 자리와 고르는 기준 / 같은 모델을 4·8·16비트로 돌렸을 때의 메모리·초당 토큰 실측 절차 / OpenAI 호환 API로 붙일 때 어긋나는 것(도구 호출·스트리밍·토큰 계정)
  - 절「파생 모델의 계보」 — 소절: 파생이 원본에 무엇을 더했는지로 갈래 나누기(지시 학습·도메인·코드·멀티모달) / 파생 모델을 고를 때 확인할 것(베이스 버전·학습 데이터 출처·평가 재현성) / 파생 모델의 라이선스가 원본보다 좁아지는 경우
  - 절「라이선스가 거는 조건」 — 소절: 실제 조항에서 걸리는 항목(사용자 상한·상표·파생 모델 이름 규칙) / 사내 배포·SaaS 제공·재배포 세 경우에서 확인할 자리 / 라이선스가 바뀌었을 때 이미 배포한 것에 미치는 영향
  - 절「열린 쪽과 닫힌 쪽의 선택」 — 소절: 총소유비용을 트래픽 규모별로 계산해 교차점을 찾기 / 데이터가 밖으로 나가면 안 되는 요건을 만족하는 배포 형태 셋 / 둘을 같이 쓰는 구성(민감 요청만 자체 서빙)

- `llm-constitutional-ai` (지금 2,632자 · 절 5)
  - 절「원칙 목록의 구성」 — 소절: 원칙을 어디서 가져오는지와 서로 충돌할 때의 우선순위 / 원칙을 프롬프트로 쓸 때 한 번에 몇 개를 주는지와 그 영향 / 원칙이 추상적일 때와 구체적일 때 비판의 질이 달라지는 폭
  - 절「비판과 수정의 순환」 — 소절: 순환 횟수를 늘렸을 때 개선이 멎는 지점과 그 판정 / 비판 프롬프트가 원칙을 지목하는 방식(무작위 하나·전부·유형별)의 차이 / 수정본이 원 요청의 유용성을 깎아 먹었는지 재는 검사
  - 절「AI 선호 라벨의 품질」 — 소절: AI 라벨과 사람 라벨의 일치율을 재는 절차와 어긋나는 항목의 성격 / 위치 편향·길이 편향을 줄이는 장치(순서 교체·길이 정규화) / 편향 증폭을 감시할 지표
  - 절「무해성과 유용성의 교환」 — 소절: 과잉 거절률과 유해 응답률을 한 좌표에 놓고 운영점을 고르는 방법 / 거절 대신 부분 응답을 내는 설계와 그 경계 / 맥락에 따라 답이 갈리는 요청을 평가 묶음으로 만드는 법
  - 절「원칙을 우리 시스템에 쓰기」 — 소절: 사내 정책을 원칙 목록으로 옮겨 적을 때의 형식과 검증 / 시스템 프롬프트·심사 모델·후처리 필터 중 어디에 둘지 정하는 기준 / 원칙을 고쳤을 때 회귀를 잡는 평가 묶음
  - 절「CAI가 남긴 것」 — 소절: RLHF·DPO·RLAIF를 재료와 절차 두 축으로 정리한 표 / 원칙 공개가 사용자에게 주는 것과 주지 못하는 것

- `llm-scaling-laws` (지금 2,636자 · 절 7)
  - 멱법칙이 말하는 것과 말하지 않는 것
  - Kaplan과 Chinchilla가 갈린 원인 — 학습률 스케줄 처리
  - Chinchilla 최적을 실제 예산에 대입해 보기
  - 추론 비용까지 넣으면 최적점이 어디로 옮겨 가는가 — 과학습 모델이 늘어난 이유
  - 데이터 벽: 고품질 토큰이 모자랄 때의 선택(반복·합성·다국어)
  - MoE와 스케일링 법칙: 활성 파라미터로 다시 세기
  - 법칙이 깨지는 자리 — 다운스트림 성능은 예측되지 않는다

- `llm-gemini-family` (지금 2,651자 · 절 9)
  - 절「전략 재편의 배경」 — 소절: 조직 통합이 모델에 남긴 것 / 이름을 제품과 모델로 통일한 뒤 생긴 혼란(버전 표기·티어 이름)을 읽는 법 / 발표 시점의 벤치마크 주장과 이후 재현 결과의 간격
  - 절「멀티모달 네이티브의 뜻」 — 소절: 입력을 토큰으로 바꾸는 자리가 모달리티마다 어떻게 다른지 / 나중에 붙인 멀티모달과 갈리는 작업(영상 시간축·오디오 억양) / 이미지·영상 토큰이 문맥과 비용을 얼마나 먹는지 계산
  - 절「티어 구조와 지금 세대」 — 소절: **티어 표를 지금 나와 있는 세대로 다시 채운다** — 모델명·문맥 길이·가격·모달리티를 열로 두고 확인일을 함께 적는다 / 티어를 고르는 기준을 작업 유형별로 정리 / 세대가 바뀌며 이름만 남고 성격이 달라진 티어를 다시 재는 절차
  - 절「긴 문맥의 값어치」 — 소절: 1M 문맥에서 실제 회수율을 바늘 찾기로 재는 절차 / 긴 문맥과 검색을 비용·정확도로 견주는 계산 / 문맥이 길어질 때 지연과 캐시가 어떻게 움직이는지
  - 절「두 경로의 API」 — 소절: AI Studio와 Vertex AI가 갈리는 항목(데이터 취급·할당량·지역·SLA) / 프로토타입에서 운영으로 옮길 때 코드에서 바뀌는 부분 / 무료 티어의 데이터 사용 조항에서 확인할 자리
  - 절「생태계 결합의 양면」 — 소절: 개인 데이터 연결이 만드는 이점과 그 대가(권한 범위·감사) / 같은 기능을 다른 벤더에서 구현할 때 드는 것 / 벤더 고착을 줄이는 추상화 층의 형태

- `reasoning-benchmarks` (지금 2,673자 · 절 6)
  - 절「갈래마다 다른 능력」 — 소절: 우리 작업이 네 갈래 중 어디에 가까운지 대응시키는 절차 / 갈래 간 순위 역전을 실제 데이터로 확인하는 방법 / 갈래를 넘겨 해석한 결정이 어떻게 실패하는지
  - 절「채점 조건 맞추기」 — 소절: pass@k 추정량을 n·c로 직접 계산해 k를 바꿔 보는 표 / 사고 예산을 32K·8K·4K로 낮춰 가며 같은 문항의 점수 변화를 재는 실험 / 답 추출기를 바꿨을 때 점수가 움직이는 폭 실측
  - 절「오염 실험의 절차」 — 소절: 공개 문항 30개를 변형하는 규칙(수·이름·단위·순서)과 변형 뒤 정답을 다시 확정하는 과정 / 변형 전후 정답률 차이를 신뢰구간과 함께 판정 / 학습 컷오프 전후 연도로 문항을 나눠 재기
  - 절「자체 평가 셋의 운영」 — 소절: 정답 확정을 사람이 하는 흐름과 이견 조정 규칙 / 반복 측정 횟수와 표준편차에서 유의미한 차이의 하한을 계산 / 실패 사례를 계속 더하면서 난도 분포가 치우치지 않게 관리
  - 절「비교표의 열 설계」 — 소절: 정확도 옆 세 열(비용·p95·형식 준수율)을 각각 어떻게 재는지 / 오답 한 건의 처리 비용을 넣어 결정을 계산으로 바꾸는 식 / 표를 다시 돌릴 수 있게 남기는 항목(명령·버전·문항 집합 해시)
  - 절「점수가 못 알려 주는 것」 — 소절: 공개 점수를 후보 축소에만 쓰는 운영 규칙 / 발표 표에서 조건이 안 적힌 항목을 다룰 때의 기본값

- `structured-output-grammar` (지금 2,686자 · 절 7)
  - 절「표현력의 층」 — 소절: enum·정규식·스키마·문법 각각의 컴파일 시간과 상태 수를 실측해 표에 채우기 / 층을 잘못 골랐을 때의 증상(첫 토큰 지연·상태 폭발) / 층을 올려야 하는 신호를 로그에서 찾는 법
  - 절「GBNF 읽고 쓰기」 — 소절: EBNF·ABNF로 적힌 명세를 GBNF로 옮길 때 바뀌는 표기 / 문법을 단위 테스트하는 방법(문법에서 무작위 문자열을 뽑아 파서에 넣기) / 문법 파일을 버전으로 관리하고 카탈로그와 맞추는 구조
  - 절「문법을 만드는 파이프라인」 — 소절: 카탈로그 변경을 감지해 문법을 다시 만드는 트리거와 캐시 무효화 / 테이블별 문법 분리로 좁힐 수 있는 범위와 조인에서 다시 넓어지는 자리 / 생성된 문법의 크기가 컴파일 비용에 미치는 영향
  - 절「자주 걸리는 세 가지」 — 소절: 좌재귀를 반복으로 바꾸는 변환을 우선순위 층까지 포함해 손으로 해 보기 / 공백 규칙을 어디에 박을지 정하는 원칙과 늘어짐을 막는 상한 / 접두사가 겹치는 대안을 인수분해해 모호성을 없애는 절차
  - 절「JSON과 문법의 결합」 — 소절: 호출을 나누는 구성과 한 문법에 넣는 구성의 비용·지연 대조 / 문법 안에서 JSON 이스케이프를 다룰 때 실제로 틀리는 자리 / 필드가 늘어날 때 어느 지점에서 호출을 나누는 것이 나은지
  - 절「좁힌 문법의 대가」 — 소절: 거절 분기를 넣기 전후로 억지 출력이 얼마나 주는지 재는 실험 / 거절 로그를 모아 문법을 넓힐 항목을 정하는 순환 / 문법으로 못 막는 문맥 의존 규칙을 뒤 검증으로 넘기는 자리 나누기

- `reasoning-vs-cot-prompting` (지금 2,692자 · 절 7)
  - 절「사고가 놓이는 자리」 — 소절: 사고 블록이 다음 턴에 다시 들어가는지에 따라 멀티턴 비용이 달라지는 계산 / 사고 구간이 출력 형식·도구 호출과 자리를 다투는 구조 / 제공자마다 사고를 노출하는 방식(원문·요약·암호화)과 그에 따라 할 수 있는 일
  - 절「프롬프팅으로 되는 범위」 — 소절: zero-shot CoT와 few-shot CoT의 차이를 같은 문항으로 재기 / 문구를 바꿔 가며 정답률 분산을 재서 「문구에 흔들린다」를 수치로 확인 / self-consistency를 얹었을 때의 정확도·비용 변화
  - 절「두 방식을 겹쳤을 때」 — 소절: 겹친 프롬프트와 정리한 프롬프트의 출력 토큰·형식 준수율을 나란히 재는 실험 / 기존 프롬프트에서 지울 문구 목록(사고 형식 지시·단계 번호·「자세히」) / 지운 뒤 품질이 떨어졌다면 무엇이 실제로 필요했는지 되짚기
  - 절「고르는 기준」 — 소절: 「길이 정해져 있는가」를 실패 사례로 판정하는 절차 / 작업 유형별로 어느 쪽이 이겼는지 자기 데이터로 채우는 표 / 같은 작업이 입력에 따라 갈릴 때의 요청 단위 분기
  - 절「경로를 나누는 구현」 — 소절: 두 프롬프트를 각각 버전 관리하고 평가를 따로 도는 구조 / 분기 판정을 어디서 할지(요청 메타·규칙·작은 분류기)와 그 비용 / 한 프롬프트를 재사용했을 때 생기는 손해를 재는 방법
  - 절「프롬프팅을 미는 순서」 — 소절: 소수 예시를 고르는 기준(대표성·난도·형식)과 개수별 효과 / 문제를 쪼갠 두 호출의 총비용·지연이 한 호출보다 나은 조건 / 값을 코드로 계산해 넣었을 때 줄어드는 실패 유형
  - 절「비교표의 네 열」 — 소절: 형식 준수율을 재는 방법과 그것이 전체 성공률에 곱해지는 계산 / 같은 100문항을 두 경로에 흘리는 실행 스크립트 구성 / 표를 다시 돌릴 수 있게 남길 항목

- `reasoning-distillation` (지금 2,739자 · 절 7)
  - 절「데이터 작업으로서의 증류」 — 소절: 고전 KD(로짓 정합)와 궤적 SFT를 학습 신호·필요 접근 권한으로 갈라 정리 / 증류 데이터셋을 자산으로 관리하는 형식(스키마·버전·출처) / 한 번 치르는 비용과 회수 기간을 계산하는 식
  - 절「교사와 문항 고르기」 — 소절: 약관에서 확인할 조항과 확인 기록을 남기는 형식 / 학생이 받을 요청 분포를 로그에서 뽑아 문항 분포와 맞추는 절차 / 교사 정답률로 문항을 층화해 난도를 고르게 퍼뜨리는 방법
  - 절「궤적 수집의 설정」 — 소절: k와 온도를 바꿔 가며 통과 후보 수를 재서 k를 정하는 실험 / 사고·답변·메타를 나눠 저장하는 레코드 스키마 / 대량 호출의 동시성·재시도·비용 상한을 다루는 실행 구조
  - 절「필터 설계」 — 소절: 정답 비교 정규화를 도메인별로 정하는 규칙(수식·코드·구조화 값) / 궤적 길이 상하한을 데이터로 정하는 절차 / 표본 검수로 「도약」을 잡는 채점 시트와 검수 비율
  - 절「학생 학습의 결정」 — 소절: 로스를 사고 구간에만 걸지 답변까지 걸지와 그 마스킹 구현 / LoRA 랭크·학습률·에폭을 정할 때 보는 지표 / 기존 지시 데이터를 섞는 비율과 회귀 검사 묶음
  - 절「기대치의 경계」 — 소절: 증류 전후를 정답률·길이·비용으로 나란히 재는 평가 설계 / 사고가 길어진 것을 향상으로 오인하지 않게 하는 지표 / 증류가 성립하지 않는 영역을 미리 가려내는 질문 셋

- `reasoning-test-time-compute` (지금 2,933자 · 절 9)
  - 절「컴퓨트를 늘리는 세 방향」 — 소절: 세 방식의 지연·비용·필요 부품을 한 표로 견주기 / 트리 탐색을 짜는 최소 형태(빔 폭·부분 해 점수·가지치기 기준) / 방식별로 레이트 리밋과 동시성이 걸리는 자리
  - 절「후보를 알아보는 능력」 — 소절: 다수결 식의 가정(오답이 흩어진다)이 깨지는 것을 자기 데이터로 확인하는 방법 / 오답이 뭉치는 문항 유형을 골라내는 절차 / 검증기가 없을 때 먼저 만들 수 있는 값싼 신호
  - 절「예산 배분의 두 축」 — 소절: 같은 예산을 병렬·순차·절충으로 나눠 정확도를 재는 실험 설계 / 문제 유형별로 어느 쪽이 이겼는지 표에 채우고 기본값을 정하기 / 배분을 바꿀 때 지연 분포가 어떻게 움직이는지
  - 절「병렬 샘플링 운영」 — 소절: 합의율을 신뢰도로 쓸 때의 보정(합의율 구간별 실제 정답률) / 배치 API로 병렬 호출 단가를 낮추는 구성과 그때의 지연 / 동시 호출이 레이트 리밋에 걸릴 때의 큐잉과 재시도
  - 절「순차 수정 운영」 — 소절: 진행이 멎었음을 판정하는 신호를 오류 문자열 밖에서 더 두기(점수 정체·같은 코드 재생성) / 히스토리를 마지막 것만 넣는 결정을 실측으로 확인하는 실험 / 라운드마다 비용이 누적되는 계산과 상한
  - 절「수확 체감과 도입 판단」 — 소절: 자기 데이터로 N별 정확도 표를 그리는 절차와 필요한 문항 수 / 기저 정확도 구간별로 기대 이득을 미리 추정하는 방법 / 적응형 배분을 켰을 때 평균 샘플 수가 얼마나 내려가는지 재기

- `reasoning-models-overview` (지금 2,968자 · 절 8)
  - 절「사고 구간의 구조」 — 소절: 사고가 컨텍스트에 남는 방식과 멀티턴에서 되돌려 보낼지의 판단 / 사고가 출력 토큰으로 계산되는 것을 청구서에서 확인하는 절차 / 제공자별 사고 노출 형태(원문·요약·암호화)와 그에 따라 로그에 남길 수 있는 것
  - 절「정확도가 오르는 조건」 — 소절: 식의 전제(맞은 것을 골라낼 수 있다)를 자기 작업에서 만족시키는 방법 / 자기 검증이 가능한 작업과 아닌 작업을 가르는 질문 셋 / 과잉 사고로 오히려 나빠지는 사례를 재현해 보기
  - 절「작업별 차이 재기」 — 소절: 표를 자기 트래픽으로 다시 채우는 실행 절차(같은 입력 집합·같은 채점기) / 정확도 델타의 신뢰구간을 계산해 유의미한지 판정 / 델타가 사라졌는지 주기적으로 다시 재는 운영
  - 절「요청 단위 분기」 — 소절: 두 축(단계 깊이·검증 가능성)을 실제 요청에 붙이는 라벨링 절차 / 규칙이 놓치는 요청을 찾아 규칙을 갱신하는 순환 / 분기 결과를 로그에 남겨 나중에 되짚는 형식
  - 절「호출 파라미터」 — 소절: 예산을 안 줬을 때 모델이 고르는 값의 분포를 재 보기 / max_tokens·예산·타임아웃 셋을 함께 정하는 계산 / 도구 호출·구조화 출력과 사고를 같이 켤 때 어긋나는 자리
  - 절「계측과 오해」 — 소절: 네 지표를 남길 로그 스키마와 대시보드 구성 / 「항상 더 똑똑하다」·「프롬프트로 흉내 낸다」·「비쌀 뿐」 셋을 각각 반증하는 측정 / 지연이 손해가 되는 경로를 미리 표시해 두기

- `reasoning-budget-control` (지금 2,978자 · 절 7)
  - 절「예산과 출력 상한」 — 소절: 답변 길이 분포를 재서 계수 2를 자기 데이터로 다시 정하는 절차 / 잘림을 잡는 검사와 잘린 응답을 복구하는 경로(이어 쓰기·재요청) / 상한을 넉넉히 잡았을 때 실제 비용이 얼마나 오르는지 실측
  - 절「등급을 나누는 신호」 — 소절: 신호별 기여도를 로그로 재서 규칙의 가중치를 다시 잡기 / 규칙과 작은 분류기의 정확도·비용을 견주는 실험 / 규칙이 오분류한 요청을 모아 규칙을 갱신하는 주기
  - 절「승급 사다리의 조건」 — 소절: exhausted 판정 임계값을 데이터로 정하는 절차 / 승급 대신 다른 처방으로 보내는 분기(형식 오류는 재시도, 지식 부족은 검색) / 승급된 요청의 지연이 사용자 경로에서 감당되는지 보는 계산
  - 절「지연 체감의 설계」 — 소절: 실측 p50·p90으로 안내 문구를 만드는 절차와 갱신 주기 / 사고 요약 스트리밍을 붙일 때의 이벤트 처리와 실패 시 폴백 / 취소를 허용할 때 서버에서 실제로 호출을 끊는 구현
  - 절「운영 지표와 임계」 — 소절: 네 지표를 요청 유형별로 갈라 보는 대시보드 구성 / 정상 범위를 자기 트래픽으로 다시 정하는 절차 / 지표가 어긋났을 때 어느 손잡이를 먼저 돌리는지 판단표
  - 절「자주 나오는 실수」 — 소절: 예산을 늘리기 전에 확인할 세 가지를 순서로 못 박기 / 전역 설정을 바꾸기 전에 유형별로 쪼개 보는 분석 절차 / 사고 로그 보관 정책(표본 비율·보존 기간·마스킹)

- `structured-output-json-schema` (지금 2,982자 · 절 6)
  - 절「강제되는 키워드」 — 소절: 자기 제공자에서 실제로 강제되는지 확인하는 시험 스키마와 판정 절차 / 강제되지 않는 키워드를 뒤 검증 코드로 옮기는 대응표 / 라이브러리가 조용히 프롬프트 힌트로 바꿔치기하는 것을 알아채는 법
  - 절「description 쓰는 법」 — 소절: 애매한 입력 유형을 모아 경계 조건 문장을 만드는 절차 / description을 고쳤을 때의 효과를 같은 입력 묶음으로 재는 대조 실험 / 길이와 토큰 비용의 균형(어느 필드에 길게 쓸지)
  - 절「선택 필드와 널 자리」 — 소절: 닫힌 스키마로 옮길 때 기존 파싱 코드에서 고칠 자리 / 「없음」의 종류를 나누는 설계(찾지 못함·해당 없음·거절) / 널 허용이 환각을 줄이는 폭을 재는 실험
  - 절「중첩과 재귀의 한계」 — 소절: 깊이별 정확도를 같은 데이터로 재서 자기 상한을 찾는 절차 / 평평한 스키마 여럿으로 나누고 코드에서 조립할 때의 호출 수·비용 / `$ref` 재사용은 쓰고 재귀는 피하는 경계와 깊이 절단 코드
  - 절「배열 설계」 — 소절: 개수를 목적 문장으로 유도했을 때와 minItems를 적었을 때의 결과 비교 / 원소 필드 순서를 바꿔 근거를 앞에 두었을 때 원소 수만큼 곱해지는 효과 재기 / 빈 배열이 정상인 경우와 실패인 경우를 구별하는 신호
  - 절「스키마의 버전 관리」 — 소절: 타입 정의(Pydantic·TypeScript)에서 스키마를 생성해 코드와 어긋나지 않게 두는 구조 / 스키마 변경을 프롬프트 변경과 같은 절차로 다루는 릴리스 흐름(버전·평가 재실행·롤백) / 스키마 토큰 비용을 재고 필드를 덜어 내는 기준

- `llm-claude-family` (지금 3,017자 · 절 9)
  - 절「안전을 앞세운 출발」 — 소절: 「능력과 안전을 함께」가 제품 결정으로 나타난 자리(거절 방식·시스템 카드·배포 정책) / 회사가 내건 철학을 모델 행동으로 검증하는 방법 / 다른 벤더와 갈리는 지점을 사용자 관점에서 확인하기
  - 절「Constitutional AI 요약」 — 소절: HHH 세 축이 실제 응답에서 부딪히는 예 / 원칙 기반 정렬을 자기 시스템 프롬프트 설계에 옮겨 쓰는 방법 / 자세한 절차는 별도 글로 넘기고 여기서는 결과만 다룬다는 경계 표시
  - 절「세대별로 바뀐 것」 — 소절: **세대 표를 지금 나와 있는 모델까지 다시 채운다** — 모델명·문맥 길이·가격·사고 지원·확인일을 열로 둔다 / 세대마다 실제로 달라진 축을 셋으로 정리(문맥·코딩·에이전트) / 이름이 같은 티어의 성격이 세대마다 바뀌는 것을 다시 재는 절차
  - 절「티어를 고르는 기준」 — 소절: 작업 유형별로 티어를 정하고 근거를 숫자로 남기는 결정표 / 티어를 내렸을 때 무엇이 먼저 무너지는지 확인하는 회귀 묶음 / 가격 개편이 났을 때 결정을 다시 도는 절차
  - 절「확장 사고와 도구」 — 소절: 사고 예산·도구 호출·구조화 출력을 같이 켤 때 확인할 자리 / SDK에서 응답 블록을 다루는 안전한 형태(블록 순회·타입 분기·stop_reason) / 모델 id를 코드에 박지 않고 설정으로 빼는 구조
  - 절「정직성이 만드는 차이」 — 소절: 「모른다」를 말하는 빈도를 재는 평가 묶음 / 불확실성 표현을 후속 코드가 쓰는 방식(임계값·기권) / 거절이 과할 때 프롬프트로 조정할 수 있는 범위와 없는 범위

- `llm-gpt-family` (지금 3,021자 · 절 8)
  - 절「사전학습이라는 발상」 — 소절: 태스크별 학습과 사전학습+파인튜닝의 데이터·비용을 견주기 / Decoder-only를 고른 결과가 이후 무엇을 가능하게 했는지 / 지금 기준으로 GPT-1 크기의 모델이 무엇을 하는지
  - 절「규모가 만든 전환」 — 소절: 파라미터·데이터·컴퓨트를 함께 늘린 각 세대의 값을 한 표로 / few-shot이 파인튜닝을 대체한 자리와 대체하지 못한 자리 / 「위험해서 공개 불가」 판단이 이후 공개 관행에 남긴 것
  - 절「정렬이 만든 대중화」 — 소절: RLHF 세 단계를 데이터·모델·비용 관점에서 정리 / 같은 베이스 모델이 정렬 전후로 달라지는 지점을 예로 / 정렬이 능력을 깎는 부분과 그 측정
  - 절「세대별 표 갱신」 — 소절: **모델 표를 지금 나와 있는 세대까지 다시 채운다** — 모델명·문맥 길이·모달리티·가격·확인일을 열로 두고 공개된 값만 적는다 / 파라미터 비공개 이후 무엇으로 모델을 비교하는지 / 음성·이미지 통합이 지연과 비용에 미친 변화
  - 절「공통 아키텍처 원리」 — 소절: 자기회귀 생성이 만드는 제약 셋(되돌아가기 없음·순차 지연·오류 누적) / 스케일링 법칙이 실무 결정에 쓰이는 자리 / 같은 원리를 쓰는 다른 계열과 갈리는 축
  - 절「생태계에 남긴 것」 — 소절: API 경제가 만든 의존과 그 위험(가격·정책·모델 폐기) / 프롬프트 엔지니어링이 지금 어디까지 남았는지 / 모델 폐기 공지에 대비하는 코드 구조

- `reasoning-verifier-models` (지금 3,102자 · 절 7)
  - 절「회수율이라는 지표」 — 소절: pass@N과 실제 정확도의 간격을 자기 데이터로 재는 절차 / 회수율이 낮을 때 생성 개선이 헛도는 것을 수치로 보이기 / 표의 값들을 자기 작업에서 다시 채우는 실행 계획
  - 절「네 가지 검증 방식」 — 소절: 규칙 검증기를 만들 여지를 찾는 질문 목록(원문 대조·합계·참조 존재·형식) / 방식별 비용과 지연을 한 표로 견주기 / 여러 방식을 어떤 순서로 쌓을지 정하는 기준
  - 절「ORM 만들기」 — 소절: 학습 데이터를 정답 라벨에서 만드는 절차와 필요한 규모 / 검증자 모델 크기를 고를 때의 손익 / 생성 모델과 같은 계열을 쓸 때의 편향 공유
  - 절「PRM의 라벨 문제」 — 소절: 단계 라벨을 롤아웃으로 자동 생성하는 절차와 그 비용 / 단계를 무엇으로 자를지 정하는 기준 / PRM 없이 트리 탐색을 흉내 내는 값싼 대안
  - 절「검증 계층의 구현」 — 소절: 규칙 필터가 걸러 내는 비율을 재서 ORM 호출을 얼마나 아끼는지 계산 / 합의와 모델 점수의 가중치를 자기 데이터로 맞추는 절차 / 기권 임계값을 넘긴 요청을 어디로 보낼지 설계
  - 절「자기 검증의 배치」 — 소절: 세 장치(컨텍스트 분리·반증 요구·독립 재풀이)를 각각 켜고 끄며 효과를 재기 / 온도와 투표 수를 정하는 실험 / 자기 검증을 최종 판정이 아니라 가중치로만 쓰는 형태
  - 절「검증자 평가와 보정」 — 소절: 선택 정확도·회수율·보정 셋을 재는 실행 코드와 읽는 법 / 온도 스케일링으로 점수를 보정하는 계산 / 운영 데이터가 바뀔 때 임계값을 다시 잡는 주기

- `llm-limits-and-hallucination` (지금 3,108자 · 절 9)
  - 절「환각의 정의와 유형」 — 소절: 세 유형을 실제 로그에서 가르는 판정 규칙과 애매한 경계 / 유형별 비중을 세어 어느 완화책이 값을 하는지 정하기 / 「단순 오류」와 「환각」을 나누는 실익
  - 절「생기는 원리」 — 소절: 다음 토큰 확률과 사실성이 갈라지는 자리를 예로 짚기 / 학습 데이터의 빈도가 답을 끌어당기는 현상 / 「모른다」를 학습시키기 어려운 이유(그 정답 라벨을 만들 수 없음)
  - 절「불확실성 측정」 — 소절: 자가 일관성 밖의 신호(토큰 확률·엔트로피·의미 클러스터링)와 각각의 비용 / 임계값을 보정 곡선으로 정하는 절차 / 측정 자체가 비용이므로 어느 요청에만 켤지 정하는 기준
  - 절「완화책의 순서」 — 소절: 세 완화책이 각각 없애는 환각 유형을 대응표로 / 근거 인용을 코드로 대조하는 구현과 그 통과율 / 완화책을 겹쳤을 때 남는 실패와 사람 검토 큐 설계
  - 절「환각 밖의 한계」 — 소절: 한계별로 지금 쓸 수 있는 우회책과 그 대가를 표로 / Lost in the Middle을 자기 문서로 재현해 보는 절차 / 편향을 자기 도메인 입력으로 재는 방법
  - 절「환각을 전제한 설계」 — 소절: 출력을 그대로 내보내는 자리와 검증을 끼우는 자리를 가르는 기준 / 실패가 눈에 보이게 만드는 화면 장치(출처·확신도·기권) / 사고가 났을 때 되짚을 수 있게 남길 로그

- `llm-decoding-methods` (지금 3,154자 · 절 8)
  - 절「탐색 공간의 크기」 — 소절: 50,000^100을 로그로 세면 10^470이다 — 본문의 10^480을 고친다 / 한 스텝 최적과 시퀀스 최적이 갈리는 지점 / 완전 탐색·Greedy·Beam을 폭 k 하나로 잇는 축
  - 절「Greedy가 놓치는 것」 — 소절: store 0.45와 bank 0.35를 두 스텝까지 전개해 누적 점수가 뒤집히는 것 보이기 / 반복된 구절이 다시 입력이 되어 스스로를 강화하는 구조 / Greedy가 오히려 맞는 자리(추출·분류·낮은 온도 코드 생성)
  - 절「빔 한 스텝의 계산」 — 소절: k×|V|=25만 후보에서 상위 5를 다시 고르는 순서 / 누적 로그확률 점수를 3스텝 손계산으로 따라가기 / EOS로 끝난 가설을 완료 풀로 빼고 빈 빔 자리를 채우는 처리 / early_stopping이 무엇을 기준으로 멈추라는 뜻인가
  - 절「길이 정규화」 — 소절: 로그확률 합이 길수록 반드시 작아진다는 것 / 같은 후보 둘에 합/L^α를 α=0·1·2로 적용해 순위가 뒤집히는 것 보이기 / 번역에서 0.9~1.2를 쓰는 근거와 요약에서 값이 달라지는 이유 / 정규화를 반대로 걸어 한 문장으로 끝나 버리는 출력
  - 절「빔을 넓혔을 때의 역전」 — 소절: 폭을 5에서 50으로 키우면 오히려 나빠진다는 보고와 그 해석 / 최대 확률 문장이 사람이 쓴 문장과 다른 이유 / no_repeat_ngram_size가 고유명사·수치처럼 정당한 반복까지 막는 자리
  - 절「다양성을 만드는 그룹」 — 소절: 그룹을 차례로 처리하며 앞 그룹이 고른 토큰의 점수를 깎는 절차 / num_beams=num_beam_groups인 극단과 diversity_penalty 값 감각 / num_return_sequences가 빔 폭에 묶이는 제약 / 재순위화용 후보를 만들 때 다양 빔과 샘플링이 갈리는 기준
  - 절「서빙에서 치르는 값」 — 소절: 빔마다 KV 캐시를 따로 들어 메모리가 빔 폭배가 되는 계산 / 최종 가설이 마지막에 바뀔 수 있어 토큰 스트리밍과 맞지 않는 점 / 대화 모델이 빔을 쓰지 않게 된 경위 / 투기적 디코딩은 「무엇을 고르나」가 아니라 「얼마나 빨리」의 문제이므로 한 문단으로 줄이고 `/articles/speculative-decoding`으로 넘긴다

- `structured-output-basics` (지금 3,164자 · 절 6)
  - 절「세 방식의 보장」 — 소절: 자기 제공자에서 셋 중 무엇이 되는지 확인하는 시험과 판정 / 프롬프트 요청으로 버티는 코드가 늘어나는 순서를 커밋 이력처럼 늘어놓기 / 방식을 올릴 때 드는 비용(토큰·지연·구현)
  - 절「남는 실패의 성격」 — 소절: 의미 오류가 조용히 통과하는 사고를 실제 형태로 재현 / 형태 실패와 의미 실패의 비용 차이를 계산 / 뒤 검증을 이 글에서 어디까지 다루고 어디부터 다른 글로 넘길지 경계 긋기
  - 절「도구 호출과 응답 형식」 — 소절: 도구 정의로 스키마를 넘기는 방식과 응답 형식 파라미터의 차이 / tool_choice를 빼먹었을 때 생기는 회귀와 그것을 잡는 검사 / 응답 블록 순회를 안전하게 짜는 형태와 stop_reason 처리
  - 절「필드 설계의 세 축」 — 소절: 널 자리·enum·순서 각각을 켜고 끄며 정확도를 재는 실험 설계 / enum 값의 이름을 정하는 규칙(모호한 라벨이 만드는 오분류) / 순서 효과가 큰 작업과 작은 작업의 구분
  - 절「쓰지 않는 자리」 — 소절: 긴 자유 서술을 스키마에 넣었을 때의 이스케이프 비용을 토큰으로 재기 / 상한에 걸려 JSON이 잘렸을 때의 복구 경로 / 메타데이터만 구조화하고 본문을 따로 받는 구성
  - 절「탐색에서 고정까지」 — 소절: 자유 형식으로 뽑은 몇십 건에서 반복되는 필드를 세어 스키마를 만드는 절차 / 스키마를 고정한 뒤 놓치는 정보를 되찾는 자유 텍스트 여유 필드 / 스키마를 처음 붙일 때의 전환 순서와 롤백

- `reasoning-rl-training` (지금 3,205자 · 절 8)
  - 절「모방으로 안 되는 자리」 — 소절: 교과서 풀이에 막혔다가 돌아간 흔적이 남지 않는다는 것 / 정답 경로가 모델마다 다르다는 문제 / 순수 RL만 돌렸을 때 보고되는 언어 혼용·가독성 저하와 앞에 소량 SFT를 두는 이유
  - 절「검증 가능한 보상의 조건」 — 소절: 채점기가 서는 네 갈래(수치 대조·단위 테스트·스키마 검증·증명 검증) / 답 정규화가 깨지는 자리(1/2와 0.5와 \frac12, 단위, 후행 공백) / 형식 보상과 정답 보상을 나눠 걸고 형식만 맞은 응답을 0으로 두는 이유 / 코드 채점을 샌드박스·타임아웃 뒤에 두는 이유
  - 절「그룹 상대 우위」 — 소절: 가치 모델을 지우면 사라지는 모델 한 벌의 메모리와 계산 / G=8에서 3승 5패일 때 A_i를 실제 수로 계산 / 8개가 전부 맞거나 전부 틀리면 표준편차가 0에 붙어 신호가 사라지는 것 / 통과율 0.2~0.8인 문제만 남겨 배치를 채우는 데이터 필터
  - 절「한 스텝이 도는 순서」 — 소절: 롤아웃·채점·우위·갱신 네 단계와 각 단계가 먹는 시간 비중 / 한 배치를 미니배치로 쪼개 쓰는 동안 정책이 이미 움직이므로 롤아웃 시점 로그확률을 따로 들고 있어야 한다 / clip_eps와 kl_coef가 각각 막는 것, KL을 e^(-δ)+δ-1로 추정해 항상 0 이상이 되게 하는 이유 / 손실을 토큰 수로 나눌지 샘플 수로 나눌지가 만드는 길이 편향
  - 절「저절로 길어지는 사고」 — 소절: 아무도 지시하지 않았는데 평균 응답 길이가 느는 경위 / 「잠깐, 부호를 잘못 봤다」류 표현이 강화되는 자리 / 정확도 곡선과 길이 곡선을 겹쳐 놓고 길이만 느는 구간을 짚기 / 길이 패널티와 짧은 정답 가산점이 각각 만드는 부작용
  - 절「보상 해킹의 세 형태」 — 소절: 테스트 입력을 하드코딩하는 우회와 비공개 테스트를 섞는 방어 / 답 태그 남발과 「정확히 하나일 때만 채점」 규칙 / 판정 모델이 길이·확신 어투·목록 형식을 보상하게 되는 경로 / 학습 보상만 오르고 보류 정확도가 정체할 때 먼저 의심할 것
  - 절「학습을 읽는 지표」 — 소절: 형식 위반율·평균 길이·기준 모델 KL·보류 정확도 넷을 한 화면에 놓기 / 정책 엔트로피가 죽어 롤아웃 8개가 서로 같아지는 신호 / 다단계 논리에는 전이되고 문체·창작에는 이득이 없다는 경험칙 / 롤아웃 생성이 학습 시간의 대부분을 먹는 구조와 그것이 실험 설계에 주는 제약

- `slm-finetuning` (지금 3,332자 · 절 6)
  - 절「고쳐지는 것과 안 되는 것」 — 소절: 형식·라벨·말투가 몇 백 건에 잡히는 이유 / 새 사실이 내용 대신 문장 형식만 남기고 흘러가는 구조 / 프롬프트·예시·검색을 먼저 소진하는 순서와 증상별 판단표 / 프롬프트 3,000토큰을 가중치로 옮겼을 때 요청당 비용·지연이 얼마나 줄어드는지 계산
  - 절「데이터의 양과 품질」 — 소절: 라벨 15개짜리 분류에서 라벨당 30~50건이라는 출발점 / 틀린 예시 하나가 옳은 예시 열 개를 상쇄한다는 것을 실제로 확인하는 방법 / 세 경로(기존 기록·큰 모델 생성·운영 로그)의 검수 단계와 약관 확인 / 학습 입력과 운영 입력의 형식이 같은지 실제로 대조하는 절차
  - 절「모델 형식에 맞춰 넣기」 — 소절: 템플릿 이론은 `/articles/llm-instruction-tuning`에 두고 여기서는 apply_chat_template로 찍어 눈으로 확인하는 절차만 / 프롬프트 토큰을 손실에서 빼는 마스킹과 안 뺐을 때 나타나는 차이 / EOS를 안 붙여 생성이 멈추지 않는 함정 / 손실이 처음부터 0에 가까우면 학습률이 아니라 라벨·마스킹을 먼저 의심한다
  - 절「LoRA가 여는 옆길」 — 소절: 7B 풀 파인튜닝의 80GB를 항목별로 나눠 적기(가중치 14·기울기 14·모멘트 둘 56) / r=16·d=4,096에서 나온 0.78%와 코드가 찍은 0.29%가 왜 다른지(한 층 대 전 층) / 붙일 자리 — 질의·값 투영만과 all-linear이 갈리는 작업의 성격 / QLoRA가 원본을 4비트로 눌러 8~12GB로 내리는 대신 내주는 것
  - 절「돌릴 때 만지는 값 셋」 — 소절: LoRA 학습률 1e-4~2e-4가 풀 파인튜닝보다 한 자릿수 큰 이유 / 에폭 1~3과 4를 넘겼을 때 학습 데이터 문구를 그대로 복창하는 증상 / 99분위로 최대 길이를 자를 때 아끼는 패딩 비용을 수로 계산 / 과적합이 검증 손실보다 출력의 단조로움으로 먼저 보이는 것
  - 절「학습 전에 만드는 평가」 — 소절: 홀드아웃 100~300건과 같은 원본에서 잘려 나온 누출을 확인하는 법 / 파인튜닝 전 작은 모델과 지금 쓰는 큰 모델 둘을 기준선으로 먼저 재는 이유 / 일반 능력 회귀 묶음 50문항이 잡는 것(지시 따르기·한국어 문장력·거절) / 결론을 「좋아졌다」 대신 격차 몇 퍼센트포인트로 적기
  - 절「어댑터로 배포하기」 — 소절: 원본에 합치기와 얹은 채 서빙하기의 득실 / 4비트 원본 위에서 학습한 어댑터를 fp16 원본에 합칠 때 값이 미세하게 달라지는 것 / 재학습 주기를 데이터가 바뀌는 속도로 정하고, 자주 바뀌는 것은 프롬프트·검색 색인에 남긴다 / 등록·버전·되돌리기 절차는 `/articles/finetuning-serving-adapters`로 넘긴다

- `llm-context-window` (지금 3,397자 · 절 7)
  - 절「윈도우가 정하는 상한」 — 소절: 입력과 출력이 한 예산을 나눠 쓴다는 뜻 / 상한이 생기는 세 근거(학습 때 본 길이·위치 인코딩의 정의역·메모리와 연산 예산) / 1,024에서 100만까지 늘어난 경위를 모델 표 한 장으로
  - 절「토큰으로 세는 예산」 — 소절: 같은 문단을 한국어·영어·코드·표로 넣어 토큰 수 비교 / 시스템 프롬프트·도구 스키마·첨부처럼 눈에 안 보이는 몫 / 128K를 시스템·문서·대화 기록·출력 여유로 쪼개는 예산표 / 출력 자리를 안 남겨 답이 중간에서 잘리는 실패
  - 절「대화가 쌓이는 모양」 — 소절: 매 턴 기록 전체를 다시 보내므로 누적 전송 토큰이 턴 수의 제곱으로 자라는 것 / 넘쳤을 때 자르는 주체가 우리인지 라이브러리인지 먼저 정해 두는 이유 / 앞부터 자르기·오래된 구간 요약·중요도 유지 셋이 각각 잃는 것
  - 절「KV 캐시가 먹는 메모리」 — 소절: 토큰당 캐시 바이트 공식 세우기(층×KV 헤드×헤드 차원×2×정밀도) / 본문의 0.5·4·16GB는 KV 헤드 8개 기준이고 코드는 헤드 32개라 값이 4배 어긋난다 — 설정을 하나로 통일해 다시 적는다 / LLaMA-2 70B는 80층·KV 헤드 8개라 토큰당 320KiB로 따로 계산해야 한다 / 같은 문단 60행의 `**제곱(O**(n²))`은 여는 `**`와 닫는 `**`가 어긋나 「제곱(O」까지만 굵어진다 — `**제곱**(O(n²))`으로 고친다. 강조는 닫혀 있어 `emphasis.test.ts`가 못 잡으므로 눈으로 확인한다 / 프리필 O(n²)와 토큰당 디코딩 O(n)이 다르게 자라 긴 입력이 첫 토큰 지연을 지배하는 것
  - 절「창을 늘리는 방식」 — 소절: 슬라이딩 윈도가 포기하는 것과 O(n·w)로 내려가는 계산 / RoPE 회전 주파수를 조정하는 위치 보간·YaRN이 학습 없이 늘리는 범위 / 8K에서 128K로 늘린 모델이 그 뒤에 추가 학습을 붙이는 이유 / 희소 어텐션과 FlashAttention은 한 문단으로 줄이고 `/articles/transformer-efficient`로 넘긴다
  - 절「광고된 길이와 유효 길이」 — 소절: 시작과 끝에 주의가 몰리는 분포 / 바늘 하나 찾기는 통과하는데 여러 사실을 엮는 과제에서 무너지는 것 / 중요한 것을 앞뒤로 보내는 배치를 같은 프롬프트 둘로 비교 / 자기 데이터로 재는 절차는 `/articles/context-long-context-reality`로 넘긴다
  - 절「검색과 나누는 경계」 — 소절: 문서 수·갱신 빈도·지연·비용으로 가르는 표 / 같은 작업을 두 방식으로 놓고 요청당 토큰 비용까지 계산 / 압축·요약·계층 메모리를 섞어 쓰는 순서 / 시스템 프롬프트 값을 한 번만 내는 접두사 캐싱은 `/articles/context-prompt-caching`으로 넘긴다


### ml-ops

**합치기 — 짝이 남지 않았다(2026-09-08).** 104편 → 92편.


**채우기**

- `llmops-cache` (지금 786자 · 절 7)
  - 무엇을 「같은 질문」으로 볼 것인가 — 정확 일치 키에 모델·온도·시스템 프롬프트 버전을 넣어야 하는 이유
  - 임계값을 곡선으로 고르기 — 0.85와 0.95 사이에서 히트율과 오답률이 어떻게 갈리는가
  - 잘못 맞힌 캐시의 값 — 오탐 한 건의 비용이 절감액을 넘기는 지점 계산
  - 무효화 세 갈래 — TTL, 프롬프트 버전 키, 원본 문서 변경 시 태그 삭제
  - 캐시하면 안 되는 응답 — 개인화된 답, 시각·재고 같은 시점 의존 답, 개인정보가 섞인 답
  - 프롬프트 캐시는 다른 물건이다 — 접두사 정렬, 5분 TTL, 캐시 쓰기 할증과 손익분기 호출 수
  - 히트율을 재는 자리와 낮을 때 먼저 볼 것

- `finetuning-vs-prompt-vs-rag` (지금 855자 · 절 6)
  - 세 전략이 각각 고치는 문제를 한 표로 — 지식 부족·행동 패턴·출력 형식
  - 결정 기준 넷 — 손에 있는 데이터 양, 지연 예산, 지식 갱신 주기, 돌볼 사람 수
  - 선택이 뒤집히는 임계 — 예시를 몇 개까지 프롬프트에 넣을 수 있나, 문서가 며칠마다 바뀌면 RAG인가
  - 세 전략을 섞은 실제 구성 — RAG 위에 형식만 파인튜닝한 소형 모델
  - 자주 하는 오판 — 형식 문제를 지식 문제로 오진하기, 파인튜닝으로 최신 정보를 넣으려는 시도
  - 되돌아가는 비용 — 파인튜닝을 걷어낼 때 남는 것(어댑터·평가셋·재학습 주기)

- `data-augmentation` (지금 901자 · 절 6)
  - 증강이 듣는 조건과 안 듣는 조건 — 데이터가 이미 충분할 때 생기는 역효과
  - 레이블 보존이 깨지는 자리 — OCR 뒤집기, 의료 영상 색 반전, 감성 분석의 동의어 치환
  - 증강 강도를 검증셋으로 튜닝하는 절차 — 강도 축을 놓고 성능 곡선 그리기
  - 온라인 증강과 오프라인 증강 — 학습 속도와 저장 비용의 맞바꿈
  - 클래스 불균형과 SMOTE — 보간이 만드는 가짜 경계
  - 텍스트 증강이 이미지만큼 안 듣는 이유 — 의미가 쉽게 깨진다

- `data-labeling` (지금 1,056자 · 절 7)
  - 가이드라인 쓰는 법 — 경계 사례를 먼저 모아 예시로 못 박는다
  - IAA가 0.7 아래로 나왔을 때의 복구 절차 — 가이드 개정 → 재교육 → 재작업 범위 산정
  - 능동 학습 루프 — 다음에 무엇을 레이블링할지 고르는 기준(불확실도·다양성)
  - LLM 자동 레이블의 검수 표본 설계 — 몇 %를 어떤 층에서 뽑나
  - 레이블 비용 산정 — 건당 단가·재작업률·품질 관리 인건비를 한 식에
  - 선호 데이터는 다르게 모은다 — eval-preference-collection으로 잇는 한 절

- `data-collection` (지금 1,109자 · 절 4)
  - 소스별 라이선스를 확인하는 절차 — robots.txt, 이용약관, CC 조건 구분
  - 언어·도메인 구성비를 정하는 법과 그것이 모델에 남기는 흔적
  - 필터 임계값을 정하는 실험 — 남는 양과 품질이 그리는 곡선
  - 벤치마크 오염 검사를 수집 단계에 넣기 — eval-contamination과 이어 붙이는 자리
  - 저장 포맷·샤딩·재현 가능한 스냅숏(수집 시점을 어떻게 고정하나)
  - PII 제거 파이프라인 — 정규식이 놓치는 것과 표본 검수 설계

- `finetuning-full-vs-peft` (지금 1,257자 · 절 6)
  - 절「전체 학습의 메모리 계산」 — 소절: 7B bf16에서 파라미터 14GB·그래디언트 14GB·Adam 상태 28GB를 자리마다 세기 / 활성값이 배치 크기와 시퀀스 길이에 비례해 따로 남는 몫 / 같은 계산을 70B에 대입해 A100 80GB 몇 장인지 세어 보기
  - 절「저랭크 분해의 근거」 — 소절: ΔW를 B(d×r)·A(r×d)로 쓰면 파라미터가 d²에서 2rd로 주는 계산 / r=16·d=4096에서 16,777,216 → 131,072(0.78%) / A는 가우시안, B는 0으로 초기화해 학습 시작 시 ΔW=0이 되게 하는 이유 / α/r 스케일이 랭크를 바꿔도 학습률을 다시 안 잡게 해 주는 자리
  - 절「줄어드는 것과 안 줄어드는 것」 — 소절: 학습 대상이 1%로 줄면 그래디언트·Adam 상태도 함께 1%가 되는 것 / 활성값 메모리는 학습 대상과 무관해 그대로 남는다는 사실 / 그래서 LoRA로도 OOM이 나는 자리와 gradient checkpointing이 필요해지는 지점
  - 절「4비트 위의 학습」 — 소절: NF4가 가중치의 정규분포 가정으로 눈금을 배치하는 것과 균등 int4의 차이 / 이중 양자화가 양자화 상수까지 눌러 얻는 몫 / 저장만 4비트이고 계산은 bf16으로 되돌리는 구조 / 24GB 카드에서 7B가 도는 이유를 첫 절의 표에 다시 대입하기
  - 절「어댑터를 붙이는 층」 — 소절: q_proj·v_proj만으로 성능 대부분이 나온다는 관측과 그 해석 / gate·up·down을 더할 때 늘어나는 것 — 학습 파라미터 수와 지식 흡수 / target_modules를 늘렸을 때 메모리와 스텝 시간이 어떻게 움직이는지 재는 법
  - 절「병합과 그 대가」 — 소절: 어댑터를 얹은 채 쓰면 매 순전파에 B·A 연산이 붙고 합치면 그것이 사라지는 대신 그 가중치가 특정 어댑터 전용이 된다는 맞바꿈 / 어느 쪽을 고르는가 — 어댑터 하나로 끝나는 서비스와 과제마다 갈아 끼우는 서빙에서 답이 갈린다 / 절차와 정밀도 이야기는 여기서 하지 않는다 — merge_and_unload의 실제 순서와 4비트 정밀도 누수는 `project-finetune-pipeline`의 「병합에서 새는 정밀도」가 맡는다
  - 절「GPU 예산별 선택 기준」 — 소절: 24GB 미만·80GB 미만·80GB 이상 세 구간에서 갈리는 것 / QLoRA로 시작해 부족하면 LoRA, 그래도 부족하면 Full로 올라가는 순서의 근거 / 「모델의 근본 행동을 바꿔야 할 때」가 실제로 무엇을 뜻하는가

- `data-deduplication` (지금 1,288자 · 절 4)
  - MinHash + LSH — 밴드 수와 행 수가 임계 유사도를 정하는 계산
  - 임계값 정하기 — 재현율과 정밀도의 맞바꿈을 표본으로 재는 절차
  - SemDeDup — 임베딩 클러스터 안에서 중심에 가까운 것부터 버린다
  - 억 단위 문서에서 도는 파이프라인 — 샤딩·메모리 상한·중간 산출물 관리
  - 중복 제거가 벤치마크 오염과 만나는 자리 — 학습셋에서 평가 문항 지우기
  - 지우면 안 되는 중복 — 법령 조항·FAQ처럼 의도된 반복

- `inference-tgi` (지금 1,380자 · 절 8)
  - 절「라우터와 워커의 분업」 — 소절: Rust 라우터가 입력 검증과 SSE 스트리밍을, 파이썬 워커가 순전파를 맡는 이유 / 스케줄러가 반복(iteration)마다 끝난 요청을 빼고 대기 요청을 넣는 자리 / 둘 사이가 프로세스 경계라 워커가 죽어도 연결이 남는 것과 그때 클라이언트가 받는 것
  - 절「토큰 상한 세 개의 관계」 — 소절: max-input-length·max-total-tokens·max-batch-prefill-tokens가 각각 무엇을 막는가 / max-batch-total-tokens가 KV 캐시 크기와 직결되는 계산 / 셋을 잘못 잡았을 때 나오는 증상 셋 — 요청 거절, 프리필 OOM, 처리량 정체
  - 절「양자화 옵션 다섯」 — 소절: awq·gptq·eetq·fp8·bitsandbytes가 각각 어느 자리에 쓰이는가 / 사전 양자화된 가중치를 받는 것과 로드 시점에 누르는 것의 기동 시간 차이 / --quantize와 --num-shard를 함께 줄 때 무엇이 먼저 적용되는가
  - 절「문법 제약과 구조화 출력」 — 소절: JSON Schema를 grammar로 넘겨 디코딩 단계에서 형식을 강제하는 원리 / 정규식 제약이 값을 하는 자리 — 분류 라벨, 날짜, 코드 블록 / 스키마가 복잡할수록 첫 토큰 지연이 늘어나는 대가와 그것을 재는 법
  - 절「텐서 병렬과 샤드 수」 — 소절: --num-shard가 층 안의 행렬을 카드로 나누는 것이지 층을 나누는 것이 아니라는 점 / 어텐션 헤드 수가 샤드 수로 나눠떨어져야 하는 제약 / 카드 간 연결이 NVLink냐 PCIe냐로 갈리는 실측 차이
  - 절「vLLM과 갈리는 자리」 — 소절: HF Hub 게이트 모델과 토큰 처리가 이미 붙어 있는 점 / Inference Endpoints가 같은 엔진이라 로컬 설정이 그대로 넘어가는 것 / OpenAI 호환 API를 기준으로 고를 때 무엇이 남는가

- `finetuning-overview` (지금 1,484자 · 절 4)
  - 사전 학습·SFT·선호 학습 세 단계가 각각 바꾸는 것
  - 세 유형을 GPU 예산으로 가르는 표 — 7B·13B·70B × Full·LoRA·QLoRA
  - 학습 데이터가 몇 건 필요한가 — 과제 유형별 하한과 그 근거
  - 파인튜닝이 실패하는 전형적인 자리 넷 — 데이터 형식, 과적합, 기저 모델 오선택, 평가 부재
  - 각 단계에서 남겨야 하는 산출물 — 체크포인트·어댑터·평가 리포트·학습 설정
  - 이어지는 글들의 지도 — 어느 글로 가면 무엇이 있는가

- `llmops-overview` (지금 1,630자 · 절 7)
  - MLOps와 다른 점을 다섯 축으로 — 버전 대상, 평가 방식, 비용 구조, 지연 특성, 실패 양상
  - 프롬프트·모델·인덱스가 각각 다른 주기로 바뀐다는 사실이 만드는 문제
  - 무엇을 버전 관리하는가 — 프롬프트, 도구 정의, 인덱스 스냅숏, 모델 별칭
  - 배포 단위가 컨테이너가 아니라 프롬프트일 때의 롤백 절차
  - 팀에 처음 들일 때의 순서 — 트레이스 → 골든셋 → 비용 대시보드 → 승격 게이트
  - 도구 생태계가 자주 바뀌는 이유와 갈아탈 수 있게 붙이는 법

- `data-quality` (지금 1,650자 · 절 7)
  - 6대 차원을 실제 검사로 옮기기 — 차원마다 어떤 쿼리를 돌리는가
  - 스키마 변경 감지 — 상류가 컬럼 하나를 바꿨을 때 무엇이 먼저 깨지는가
  - 결측·이상치를 지울 것인가 표시할 것인가 — 학습과 서빙에서 답이 다르다
  - 품질 게이트를 파이프라인 어디에 두나 — 수집 직후와 학습 직전의 차이
  - 검사가 실패하면 파이프라인을 세울 것인가 통과시킬 것인가
  - 입력 품질과 레이블 품질을 따로 재기

- `mlops-pipeline` (지금 1,652자 · 절 6)
  - 오케스트레이터 셋의 실제 차이 — 스케줄링 모델, 실패 재개, 로컬 실행 가능 여부
  - 단계를 어디서 자르나 — 캐시 재사용 단위와 같아야 하는 이유
  - 멱등성과 부분 재실행 — 같은 날짜를 두 번 돌려도 같은 결과가 나오게
  - 실패 복원 — 재시도·백오프·수동 개입 지점을 어디에 두나
  - 파이프라인 코드의 테스트 — 단계 단위 테스트와 스모크 실행
  - 스케줄 트리거와 이벤트 트리거를 섞을 때 생기는 중복 실행

- `inference-engines` (지금 1,797자 · 절 8)
  - 고르기 전에 답할 질문 넷 — 동시 사용자 수, 모델 크기, 하드웨어, 출력 형식 제약
  - 엔진마다 다른 것은 스케줄러다 — 무엇을 언제 배치에 넣는가
  - 같은 모델·같은 카드에서 재는 법 — serving-benchmarking으로 잇는 비교 절차
  - 양자화 포맷 지원 차이가 선택을 뒤집는 자리
  - 구조화 출력·함수 호출 지원 차이
  - 갈아탈 때 드는 비용 — API 표면, 배포 형태, 운영 지표가 함께 바뀐다

- `quantization-awq-gptq` (지금 1,898자 · 절 6)
  - 절「단순 반올림이 무너지는 곳」 — 소절: LLaMA-2-7B RTN INT4에서 PPL 5.47 → 8.12, MMLU 45.2% → 38.4% / absmax 스케일이 이상치 채널 하나에 끌려가 나머지 채널이 0과 1로 뭉개지는 과정 / −8~7이라는 표현 범위가 그룹 크기와 함께 정하는 실제 해상도
  - 절「그룹 크기와 스케일 저장」 — 소절: g=128일 때 128개마다 스케일·영점을 따로 두어 실효 비트가 4.2 안팎이 되는 계산 / 그룹을 줄일수록 품질이 오르고 파일이 커지는 곡선 / 「4비트」라고 적힌 파일의 실제 크기를 세어 보고 표와 맞추기
  - 절「AWQ의 등가 변환」 — 소절: |Xⱼ|가 큰 채널을 중요 채널로 보는 근거와 「1%가 성능 대부분」이라는 관측 / W·s와 X/s로 나눠 곱해도 결과가 같다는 등가성 / α를 격자 탐색해 s를 정하는 절차 / 보정이 몇 분에 끝나는 이유 — 최적화가 아니라 탐색이다
  - 절「GPTQ의 오차 보상」 — 소절: ||WX − Q(W)X||²를 열 단위로 최소화한다는 목표식 / H=2XXᵀ의 역행렬로 이미 양자화한 열의 오차를 남은 열에 흘려보내는 절차 / desc_act를 켜면 정확도가 오르고 커널이 느려지는 맞바꿈 / 보정에 수십 분~수 시간이 드는 자리
  - 절「보정 데이터가 정하는 것」 — 소절: C4 128 샘플이 사실상 표준이 된 자리와 그 한계 / 한국어·도메인 모델에서 보정셋을 바꿔야 하는 이유와 바꾼 뒤의 확인법 / 보정 길이 512 토큰이 긴 문맥 성능에 남기는 흔적
  - 절「서빙에서 갈리는 자리」 — 소절: AWQ의 GEMM·GEMV 커널이 배치 크기에 따라 갈리는 것 / vLLM에서 quantization 인자만 바꿔 같은 코드가 도는 것과 그때 실제로 달라지는 커널 / 7B에서 3.5~4.0GB로 줄어든 몫이 KV 캐시로 되돌아오는 계산 / llama.cpp로 가려면 GGUF 변환이 한 번 더 필요한 것

- `quantization-gguf` (지금 1,961자 · 절 8)
  - GGUF 파일 구조 — 헤더·메타데이터·텐서 블록을 실제로 열어 확인하기
  - K-quant가 블록마다 하는 일과 Q4_K_M의 K와 M이 뜻하는 것
  - imatrix — 보정 데이터로 중요한 채널을 지키는 방식(quantization-calibration으로 잇기)
  - 레벨별 품질 손실을 직접 재는 절차 — 퍼플렉시티와 과제 정확도를 함께
  - 변환에서 밟는 함정 — 토크나이저 메타데이터, 채팅 템플릿 누락, 어댑터 병합 순서
  - 언제 GGUF가 아니라 AWQ·GPTQ인가

- `distillation` (지금 2,063자 · 절 7)
  - 절「원-핫이 버리는 정보」 — 소절: [0,0,1,0]과 [0.02,0.15,0.80,0.03]이 담는 정보량의 차이 / 「고양이와 강아지가 자동차보다 가깝다」가 학습 신호가 되는 자리 / 교사가 잘 훈련될수록 정답 확률이 0.999에 붙어 그 정보가 사라지는 역설
  - 절「온도가 하는 일」 — 소절: 로짓을 T로 나눌 때 분포가 평평해지는 과정을 숫자 넷으로 따라가기 / T=1·3·5에서 같은 로짓이 어떻게 갈리는가 / 온도로 나누면 그래디언트가 T²만큼 작아져 손실에 T²를 곱해 보정하는 계산 / T를 키울수록 틀린 클래스의 잡음까지 배우는 경계
  - 절「두 손실의 균형」 — 소절: α가 0.3일 때와 0.7일 때 학생이 무엇을 따라가는가 / 교사가 틀린 문제에서 hard 항이 안전망이 되는 자리 / 교사와 학생의 어휘가 다르면 KL을 못 재는 문제와 그때의 우회
  - 절「출력 매칭과 중간층 매칭」 — 소절: DistilBERT — 110M → 66M에서 성능 97%, 속도 60% 향상이 나온 구성 / TinyBERT가 어텐션 행렬과 은닉 상태를 층 단위로 맞추는 방식 / 층 수가 다를 때 어느 층과 어느 층을 짝지을지 정하는 문제 / 차원이 다를 때 끼우는 투영 행렬과 그 파라미터를 어디에 두는가
  - 절「LLM 증류의 두 갈래」 — 소절: SFT 증류 — 교사 응답만 받아 학생을 지도학습(Alpaca·Vicuna)하는 구성 / 토큰 단위 KD — 위치마다 수만 어휘 분포를 맞추므로 교사를 계속 돌려야 하는 비용 / 상위 k개 로짓만 저장해 비용을 줄이는 절충 / 학생이 만든 문장 위에서 교사를 부르는 것과 교사 문장을 그대로 쓰는 것의 차이
  - 절「증류로 못 넘는 선」 — 소절: 학생이 교사를 넘지 못하는 이유와 검증을 끼웠을 때 생기는 예외 / 상용 API 출력으로 학습할 때 걸리는 이용약관과 출처 기록의 필요 / TinyLLaMA가 이름과 달리 사실상 대규모 사전학습에 가깝다는 점 — 「증류」라는 말이 덮고 있는 범위

- `project-finetune-pipeline` (지금 2,100자 · 절 14)
  - 절「단계마다의 산출물」 — 소절: 여덟 단계를 파일 이름으로 이어 붙이기(raw.jsonl → clean.jsonl → chatml.jsonl → adapter → merged → gguf) / 어느 단계가 실패했을 때 어디부터 다시 도는가 / 지우면 안 되는 중간 산출물 셋과 그 이유
  - 절「정제가 병목인 이유」 — 소절: 중복·길이 이상치·형식 깨짐 세 갈래를 각각 세어 보고 몇 %가 빠지는지 기록하기 / 1,000건 아래에서는 few-shot이 낫다는 판단의 근거 / 정제 규칙을 코드가 아니라 분포로 정하기 — 길이 분포의 상하위를 잘라 내는 절차
  - 절「세 가지 대화 포맷」 — 소절: Alpaca·ShareGPT·ChatML이 각각 무엇을 담고 무엇을 버리는가 / 토크나이저의 chat_template과 어긋나면 조용히 성능이 떨어지는 자리 / 손실을 답 토큰에만 거는 설정과 안 걸었을 때 학습되는 것의 차이
  - 절「학습 설정과 체크포인트」 — 소절: 24GB에서 배치 2로 도는 조합 — 4비트 로드·gradient checkpointing·누적 스텝 / save_total_limit과 load_best_model_at_end가 함께 고르는 것 / eval loss가 train loss와 갈라지는 지점을 곡선에서 읽고 그 스텝으로 되돌리기
  - 절「병합에서 새는 정밀도」 — 소절: merge_and_unload가 W+BA를 실제로 더하는 절차와 그 앞뒤로 무엇을 로드해 두어야 하는가(개념 쪽 맞바꿈은 `finetuning-full-vs-peft`에 두고 여기서는 절차와 숫자만 다룬다) / 4비트 베이스 위에서 학습한 어댑터를 4비트에 병합하면 반올림 오차가 겹치는 이유 / CPU·bf16으로 병합하고 다시 눌러 내보내는 순서 / 병합 전후를 같은 문제로 비교해 정밀도가 실제로 샜는지 확인하는 단계
  - 절「GGUF와 Ollama 배포」 — 소절: convert_hf_to_gguf.py와 transformers 버전이 어긋날 때의 증상 / Q4_K_M·Q5_K_M·Q8_0의 크기와 손실을 베이스가 아니라 파인튜닝한 모델 기준으로 다시 재기 / Modelfile의 템플릿을 학습 때 쓴 형식과 한 글자까지 맞춰야 하는 이유
  - 절「막히는 자리 넷」 — 소절: OOM, 학습 가능 파라미터 0(prepare_model_for_kbit_training 누락), 병합 후 성능 저하, 변환 오류 / 각각을 한 줄로 확인하는 명령과 그 출력에서 볼 값 / 넷 중 셋이 학습 시작 전에 잡히는 것이라는 정리

- `data-synthetic` (지금 2,168자 · 절 5)
  - 절「합성이 답이 되는 자리」 — 소절: 수집에 수개월 걸리는 도메인(의료·자율주행)에서 병목을 옮기는 것 / 원본을 못 내보내는 규제 아래에서 통계만 옮기는 방식 / 사기 거래·장애 로그처럼 희귀한 것을 의도적으로 만드는 자리 / 실제 분포를 아직 모를 때는 만들 수 없다는 경계
  - 절「규칙·통계·학습 세 갈래」 — 소절: Faker가 결정론적이고 변수 간 상관을 못 담는다는 한계 / 가우시안 코퓰라가 주변 분포와 의존 구조를 나눠 모델링하는 방식 / CTGAN이 표 데이터의 이산 열을 조건부로 다루는 이유 / 셋의 학습 비용과 필요한 원본 양을 한 표로
  - 절「SMOTE가 만드는 가짜 경계」 — 소절: 소수 클래스의 k-최근접 이웃 사이를 보간해 새 점을 만드는 절차 / 클래스가 겹치는 자리에서 보간점이 다수 클래스 영역을 침범하는 그림 / 테스트셋에는 절대 적용하지 않는다는 규칙과 어겼을 때 생기는 점수 착시
  - 절「TSTR로 재는 쓸모」 — 소절: 합성으로 학습해 실제로 시험(TSTR)과 실제로 학습해 실제로 시험(TRTR)을 나란히 놓기 / 두 F1의 격차를 몇 %p까지 허용할지 미리 정하는 기준 / 통계적 유사성 점수는 높은데 TSTR이 낮은 경우가 뜻하는 것
  - 절「프라이버시가 새는 세 갈래」 — 소절: 멤버십 추론·속성 추론·재구성 공격이 각각 노리는 것 / 에포크를 늘려 원본에 가까워질수록 유사도 점수와 프라이버시가 반대로 가는 곡선 / 차분 프라이버시를 끼웠을 때 잃는 품질의 크기와 그것을 재는 자리
  - 절「LLM으로 만드는 텍스트」 — 소절: 시드 몇 개에서 시작할 때 표현이 몰리는 자리와 그것을 눈으로 확인하는 법 / 만든 것을 다시 거르는 순서 — 형식·중복·난이도 / 벤치마크 문항이 섞여 들어오는 오염 검사를 이 단계에 넣기

- `pruning` (지금 2,171자 · 절 8)
  - 절「지우는 단위의 두 갈래」 — 소절: 개별 가중치를 0으로 만들면 행렬 크기가 그대로라 일반 GPU에서 속도가 안 나는 이유 / 행·채널·헤드를 통째로 지우면 크기 자체가 줄어드는 것 / 같은 희소율에서 구조적 쪽이 더 많이 잃는다는 관측과 그 해석
  - 절「절댓값 기준의 한계」 — 소절: |W|가 작아도 활성값이 크면 출력에 크게 기여한다는 반례 / Wanda가 |W_ij|·‖Xⱼ‖₂로 둘을 곱해 보는 이유 / 층마다 희소율을 다르게 줄 것인가 전체를 한 임계로 자를 것인가
  - 절「2:4 패턴과 실제 속도」 — 소절: 연속한 넷 중 둘을 0으로 두는 규칙과 Sparse Tensor Core가 그 패턴을 읽는 방식 / 정확히 50%로 고정이라 더 눌러도 이득이 없다는 제약 / A100 이후 카드에서만 되는 것과 FP16 대비 2배가 어디까지나 이론값인 자리
  - 절「오차를 나머지에 흘리기」 — 소절: SparseGPT가 GPTQ와 같은 OBC 틀을 프루닝에 쓰는 구조 / 열 하나를 0으로 만든 뒤 남은 열을 Hessian 역행렬로 조정하는 절차 / Wanda가 Hessian을 안 세워 훨씬 빠른 대신 포기하는 것 / 보정 데이터 수를 늘렸을 때 결과가 얼마나 달라지는가
  - 절「층을 통째로 빼기」 — 소절: LLM-Pruner가 어텐션 헤드와 FFN 차원을 중요도로 고르는 방식 / ShortGPT — LLaMA-2-13B에서 층 25%를 빼도 손실이 미미했다는 결과 / 층을 빼면 KV 캐시와 지연이 함께 주는 것 / 어느 층이 덜 중요한지 재는 지표와 그 지표가 놓치는 것
  - 절「프루닝 뒤의 회복」 — 소절: 소량 파인튜닝이 되돌리는 폭과 걸리는 시간 / LoRA로 회복시킬 때 마스크가 다시 채워지지 않게 하는 처리 / 반복 프루닝이 원샷보다 나은 조건과 그때 드는 시간 계산
  - 절「양자화와 겹쳐 쓰기」 — 소절: SparseGPT 50% 뒤에 GPTQ INT4를 거는 순서와 반대로 했을 때 / 두 압축의 손실이 더해지는지 곱해지는지 같은 세트로 재 보는 절차 / 프루닝·양자화·증류 셋 중 무엇을 먼저 볼지 정하는 기준

- `speculative-decoding` (지금 2,202자 · 절 7)
  - 절「대역폭이 정하는 상한」 — 소절: 배치 1로 70B를 돌릴 때 계산 자원의 10~20%만 쓰이는 이유 / 토큰 하나마다 가중치 전체를 읽는 비용을 모델 크기 ÷ 대역폭으로 세어 보기 / 그래서 K+1 자리를 한 번에 검증해도 비용이 K배가 아니라는 결론
  - 절「초안과 검증의 분업」 — 소절: 초안 모델이 K개를 자기회귀로 만들고 목표 모델이 한 번의 순전파로 검증하는 순서 / K를 키울 때 기대 이득과 버려지는 초안이 갈리는 지점 / 초안 모델을 목표의 몇 분의 일 크기로 잡을 것인가 — 같은 토크나이저를 써야 하는 제약
  - 절「거부 샘플링의 보장」 — 소절: min(1, p_target/p_draft)로 수락하는 규칙 / 거부했을 때 p_target − p_draft의 잔여 분포에서 다시 뽑는 처리 / 출력 분포가 목표 모델과 같아진다는 것이 실무에서 뜻하는 것 — 품질 평가를 다시 안 해도 되는 자리
  - 절「수락률이 정하는 이득」 — 소절: 수락률 α와 K로 걸음당 기대 토큰 수를 세는 식 / 초안과 목표의 분포가 벌어질수록 이득이 사라지는 곡선 / 온도를 올리면 수락률이 떨어지는 이유와 그리디 디코딩에서 가장 잘 듣는 것
  - 절「초안 모델 없는 변형」 — 소절: Medusa — 마지막 층 위에 예측 헤드 K개를 붙여 별도 모델을 없애는 구성 / Prompt Lookup — 입력에서 그대로 베껴 오므로 요약·RAG·코드 편집에서 잘 듣는 자리 / EAGLE — 은닉 상태를 써서 수락률을 올리는 방식과 Vicuna-7B 3~4배 보고
  - 절「이득이 사라지는 자리」 — 소절: 배치가 8을 넘으면 이미 계산이 차서 메울 빈자리가 없다는 것 / 서빙 엔진에서 켤 때 처리량과 지연이 반대로 움직이는 관측 / 양자화된 목표 모델과 짧은 생성에서 오버헤드만 남는 경우

- `eval-rubric-design` (지금 2,311자 · 절 5)
  - 절「앵커가 없는 척도」 — 소절: 「4점 = 좋음」이 4점을 다시 말한 것뿐이라는 점 / 좋은 앵커의 조건 셋 — 답을 보고 확인 가능, 이웃 눈금과 갈림, 답 안의 것만 봄 / 「신뢰가 간다」·「사용자가 만족할 것이다」가 앵커가 못 되는 이유
  - 절「눈금 수를 정하는 법」 — 소절: 통과·실패 2단계가 맞는 자리 — 규정 위반, 형식 준수, 도구 호출 정확성 / 3단계로 또렷한 것이 5단계로 애매한 것보다 나은 이유 / 5단계를 쓰려면 앵커 다섯을 실제로 다르게 쓸 수 있는지 먼저 시험하기
  - 절「축을 나눈 채점」 — 소절: 사실 정확성·지시 준수·형식·안전 넷을 출발점으로 삼기 / 평균 3.5점은 아무것도 가리키지 않지만 「지시 준수 2점」은 프롬프트의 어디를 볼지 알려 주는 것 / 안전만 점수가 아니라 통과·실패로 세고 하나라도 걸리면 전체를 실패로 치는 구성 / 가중치를 붙이는 순간 따라오는 「그 숫자의 근거」 질문
  - 절「앵커를 벼리는 절차」 — 소절: 문제 스무 개를 두 사람이 각자 채점해 갈린 자리만 고치는 반복을 두세 번 / 채점자 일치도를 무엇으로 셀 것인가 / 갈린 문제를 버리지 않고 모아 두는 이유 — 앵커를 고쳐야 할 자리가 거기 있다 / 「애매해서 3점 줬다」 메모를 받을 칸을 만들어 두기
  - 절「모델 채점기 만들기」 — 소절: 근거를 먼저 쓰게 하고 점수를 나중에 내게 하는 순서 / 출력 필드를 고정해 파싱이 매번 깨지지 않게 하기 / 사람 채점 100~200건과의 일치율을 재고 채점기에도 버전을 붙이는 것 / 채점 프롬프트를 고칠 때마다 그 세트로 다시 확인하는 절차
  - 절「채점기의 네 치우침」 — 소절: 길이·자기 편애·위치·중앙 쏠림이 각각 무엇을 하는가 / 순서를 바꿔 두 번 채점해 평균하는 대응 / 앵커에 길이를 넣지 않고 형식 축을 따로 두는 처리 / 채점 모델을 평가 대상과 다른 계열로 두는 이유
  - 절「루브릭의 버전 관리」 — 소절: 앵커 한 줄을 고치면 그 전후의 점수를 이어 못 읽는 이유 / 고친 날 직전 버전으로도 한 번 더 채점해 두 점수를 나란히 남기기 / 점수 기록에 루브릭 버전을 함께 박아 두어 몇 달 뒤 되짚을 수 있게 하기

- `eval-regression-testing` (지금 2,376자 · 절 7)
  - 절「평균이 감추는 뒤집힘」 — 소절: 같은 72점 안에서 회귀 5건과 개선 5건이 상쇄된 네 칸 표 / 대각선 밖 두 칸만이 이번 변경이 실제로 한 일이라는 읽기 / 깨진 다섯이 잡담이냐 결제 질문이냐에 따라 판단이 갈리는 자리
  - 절「코드 테스트와 다른 셋」 — 소절: temperature를 0으로 두어도 배치 구성과 커널 선택으로 토큰이 달라지는 것 / 채점이 참·거짓이 아니라 정도라서 채점기에도 버전이 필요한 것 / 만점이 목표가 아니라 어제보다 안 나빠지는 것이 목표라는 차이 — 100점 세트는 이미 쉬운 세트다
  - 절「고정해야 하는 다섯」 — 소절: 모델 버전·디코딩 설정·평가 세트·채점기·실행 회수 / 별칭 뒤 모델이 갈린 것을 프롬프트 효과로 읽는 흔한 사고 / 둘을 한꺼번에 바꿔야 할 때 기록에 남길 한 문장
  - 절「흔들리는 문제 가르기」 — 소절: 떨어진 문제를 바로 회귀로 세지 않고 몇 번 더 돌리는 순서 / 세 번 중 한두 번만 통과하는 것을 격리 목록으로 옮기고 따로 세기 / 격리 수가 늘면 대개 정답이 여럿인데 채점기가 하나만 인정하는 자리 / 세 번씩 돌려 다수결로 채점할 때 드는 비용 세 배와 그것이 값을 하는 세트 크기
  - 절「기준선 파일의 모양」 — 소절: 점수 하나가 아니라 문제별 결과 목록이어야 짝이 맞는 이유 / case_id를 순번으로 두면 문제 하나를 끼우는 순간 뒤가 전부 회귀로 보이는 사고 / 갱신 시점을 「회귀를 확인하고 받아들이기로 한 순간」으로 못 박기 / 매 실행마다 덮어쓰면 하루 1점씩 열흘을 떨어져도 경고가 안 뜨는 것
  - 절「CI에 붙이는 두 단계」 — 소절: 막는 것과 알리는 것을 나누는 기준 / 절대 깨지면 안 되는 문제 열 개를 따로 표시하기 — 안전 거절, 가격 지어내기, 사내 규정 / 커밋마다 부분 집합·배포 전에 전체로 나눌 때 부분 집합을 층화로 뽑아야 짝이 맞는 것

- `eval-contamination` (지금 2,418자 · 절 5)
  - 절「오염이 들어오는 네 갈래」 — 소절: 웹 크롤·합성 데이터·사람을 거친 과적합·평가 중 검색 / ③은 학습 데이터에 문제가 한 줄도 안 들어가도 생긴다는 것 — 모델이 아니라 절차가 오염된 경우 / ④만이 평가하는 쪽에서 막을 수 있는 갈래라는 점
  - 절「겹침을 세는 방법」 — 소절: 13-그램 일치로 세는 절차와 그 길이를 정하는 근거 / 학습 데이터에 접근할 수 있을 때만 되는 한계 — 남의 모델에는 못 쓴다 / 표현을 조금 바꾼 합성 경로를 못 잡는 이유
  - 절「변형 대조로 재기」 — 소절: 숫자만 바꾸기·보기 순서 섞기·새로 만든 문제 셋이 각각 확인하는 것 / 깨끗한 모델이라면 넷이 비슷하고 원본만 솟으면 그 격차가 곧 기억의 크기라는 읽기 / 변형을 만들 때 난이도가 함께 바뀌지 않게 하는 주의점
  - 절「시점 가르기와 카나리아」 — 소절: 학습 마감 시점 뒤 문제만 골라 재고 앞 문제와 견주는 절차 / 최근 사건일수록 문제 자체가 어려운 경향과 오염을 갈라 보는 법 / 무작위 문자열을 심어 두고 이어 쓰게 해 학습 포함 여부를 확인하는 방식 / 그 문자열이 크롤 제외 표시이기도 하다는 점
  - 절「우리 평가를 지키는 규칙」 — 소절: 로그에서 뽑기·정답 비공개·홀드아웃 분리·주기적 교체·도구 접근 통제 / 공개 저장소에 정답과 함께 올린 세트가 몇 달 뒤 크롤에 들어가는 경로 / 매일 보는 세트와 가끔 여는 세트의 점수가 벌어지는 것이 절차 오염의 신호라는 읽기
  - 절「순위표를 읽는 다섯 규칙」 — 소절: 한 벤치마크에서만 유난히 높은 모양을 의심하기 / 최근에 만들어진 벤치마크와 비공개 채점 벤치마크를 우선하기 / 1~2점 차이는 프롬프트 형식이나 채점 방식만 바꿔도 뒤집힌다는 것 / 순위표는 후보를 좁히는 데 쓰고 고르는 것은 우리 평가로 하는 순서

- `eval-preference-collection` (지금 2,421자 · 절 6)
  - 절「기준선이 흔들리는 자리」 — 소절: 점수 매기기가 답과 자기 머릿속 기준선을 함께 재는 일이라는 점 / 후하게·짜게 주는 사람과 앞에 본 답이 만드는 대비 효과 / 쌍 비교에서 기준선이 두 답에 똑같이 걸려 상쇄되는 이유 / 대신 치르는 값 — 판단 시간이 길고 짝이 제곱으로 는다
  - 절「브래들리–테리 모형」 — 소절: P(i≻j)=σ(s_i − s_j)라는 가정과 관측한 승패로 s를 맞추는 절차 / 차이만 뜻이 있다는 성질 — 모든 점수에 100을 더해도 확률이 그대로다 / 0.63 차이가 약 65%, 1.4 차이가 약 80%라는 대응 / 전체 승률을 로그 오즈로 바꿔 감을 잡는 간이 계산
  - 절「질문과 화면의 설계」 — 소절: 좌우를 무작위로 섞고 어느 쪽이 어느 모델인지 알리지 않기 / 「더 좋은 답」 대신 축을 정해 묻기 — 사실·지시 준수·읽기 편함이 서로 다른 답을 낸다 / 동점을 막으면 잡음이 늘고 허용하면 몰리는 양쪽 / 「조금 낫다」와 「확실히 낫다」를 나눠 승점 0.75·1.0으로 세는 표
  - 절「길이 편향 걷어내기」 — 소절: 사람이 긴 답을 더 좋게 보는 경향의 크기 / 길이가 비슷한 짝만 따로 모아 점수를 다시 내 보는 검사 / 모델 판정에서 이 편향이 사람보다 더 크되 방향이 일정해 보정하기 쉬운 것
  - 절「비교 횟수 줄이기」 — 소절: 후보 10개면 45짝, 짝마다 20회면 900회 — 한 판단 1분이면 15시간이라는 계산 / 기준 후보 하나를 두고 그것하고만 비교해 n−1짝으로 줄이기 — 「지금보다 나은가」에 바로 답하는 것 / 점수가 붙어 있는 짝에 비교를 몰아 주는 배분 / 모델 판정으로 대체할 때 사람 판정 200짝과의 일치율을 먼저 재 두기
  - 절「학습 재료로 남기기」 — 소절: 선호 쌍이 그대로 선호 학습의 입력이 되는 자리 / 최종 선택 말고 함께 남길 것 — 보인 짝, 평가자, 확신도, 고른 근거 / 나중에 평가자별 편향을 보정하거나 확신 높은 짝만 골라 쓰려 할 때 그 기록이 없으면 다시 못 모으는 것

- `eval-statistical-significance` (지금 2,462자 · 절 7)
  - 절「정답률의 오차 폭」 — 소절: SE=√(p(1−p)/n)에 100문제·72%를 넣어 ±8.8%p를 얻는 계산 / 신뢰구간이 뜻하는 것 — 같은 방식으로 계속 뽑아 그리면 95%가 참값을 품는 범위 / n이 제곱근 안에 있어 문제를 네 배로 늘려야 폭이 절반이 되는 성질
  - 절「차이의 오차 폭」 — 소절: 흔들리는 것이 둘이라 차이의 구간이 각각보다 넓어지는 이유 / 100문제로 잰 72%와 74%의 차이가 −10~+14%p에 걸린다는 계산 / 이 2%p를 말하려면 4,000문제가 필요하다는 결론과 순위표의 1점 차이를 읽는 법
  - 절「짝지어 잴 때의 이득」 — 소절: 문제 난이도의 흔들림이 두 모델에 똑같이 걸려 차이에서 상쇄되는 구조 / 갈린 문제만 남는다는 것 — 2 대 12로 갈린 표에서 χ²=(12−2)²/14≈7.14 / 맥니마 검정이 100문제로 결론을 내는 것과 따로 뽑았을 때 ±11.6%p로 아무 말도 못 하는 대비 / 그래서 평가 세트를 두 후보에 똑같이 돌리는 것이 공짜로 얻는 예민함이라는 정리
  - 절「부트스트랩으로 재기」 — 소절: 루브릭·선호 점수처럼 0과 1 사이 값에 이항 공식이 안 맞는 이유 / 복원 추출로 표본을 여러 벌 다시 만들어 그 흔들림을 오차 폭으로 쓰는 절차 / 문제별 점수가 아니라 문제별 차이를 넣어야 짝지은 이점이 사는 것 / 구간이 0을 품는지만 보면 판정이 끝나는 자리
  - 절「실용적 유의성의 문턱」 — 소절: n을 키우면 0.2%p도 유의해지는 것과 그것으로 비용 세 배를 낼 수는 없는 사정 / 「정답률이 3%p 이상 오르면 갈아탄다」를 재기 전에 적어 두는 이유 / 결과를 보고 문턱을 정하면 결국 원하던 결론이 나오는 것
  - 절「여러 번 견줄 때의 우연」 — 소절: 유의수준 5%로 스무 번 견주면 전부 같은 후보라도 하나쯤 유의해지는 계산 / 문턱을 나누는 대응(5%÷20=0.25%)과 그 보수성 / 1등 후보를 홀드아웃 세트에 한 번 더 돌리는 방식이 가장 든든한 이유 / 보고서에 「후보 스무 개 중 1등」이라고 적어 두기

- `finetuning-data-synthesis` (지금 2,557자 · 절 5)
  - 절「생성과 검증의 난이도 차」 — 소절: 한 번에 맞힐 확률은 낮아도 열 번 중 맞은 것을 고르는 일은 검증기가 한다는 비대칭 / 「스무 번에 한 번 나오는 좋은 답을 항상 나오게 만드는 일」로 이해하기 / 검증기를 만들 수 없는 과제에서 이 논리가 성립하지 않는 자리
  - 절「프롬프트를 늘리는 법」 — 소절: 씨앗 확장·난이도 진화·페르소나 조합·역생성이 각각 늘리는 것 / 가진 문서에서 「이 문서가 답이 되는 질문」을 거꾸로 만드는 절차 / 화자·분야·난이도·길이 축을 정해 조합을 훑는 것이 무작위성에 맡기는 것보다 넓게 퍼지는 이유
  - 절「거부 샘플링의 두 조건」 — 소절: 하나도 못 맞힌 문제는 답이 없어 버리고 전부 맞힌 문제는 이미 아는 것이라 버린다 / 남는 「가끔 맞히는」 구간이 학습이 일어나는 곳이라는 판단 / 맞은 답 중 가장 짧은 것을 고르면 군더더기 없는 풀이가 학습되는 것 / 통과율을 몇 회 표본으로 셀 것인가
  - 절「거르는 다섯 단계」 — 소절: 검증 통과 → 형식 → 중복 제거 → 난이도 → 오염 순서와 단계마다 남는 비율 기록하기 / 글자 중복(n-gram)과 뜻 중복(임베딩)이 둘 다 필요한 이유 — 표현만 바꾼 같은 문제는 글자 비교로 안 잡힌다 / 오염 검사를 빠뜨리면 이후 모든 숫자가 거짓이 되는 자리
  - 절「다양성이 무너지는 자리」 — 소절: 모델이 확률 높은 영역으로 몰려 그 밖이 아무리 만들어도 안 채워지는 과정 / 그 데이터로 학습하면 좁은 영역만 더 잘하게 되는 순환 / 씨앗 20개와 200개의 차이가 온도·기법 차이보다 큰 것 / 임베딩을 2차원으로 줄여 눈으로 보기 — 유사 제거에서 절반 이상이 사라지는 것은 정상
  - 절「증류로 채울 때의 제약」 — 소절: 상용 API 출력으로 경쟁 모델을 학습하는 것을 막는 약관 조항과 그것을 확인하는 시점 / 행마다 어느 모델·어느 버전이 언제 만들었는지 남기기 — 섞이고 나면 복원 불가 / 증류의 상한은 원본 모델이고 넘으려면 검증이 필요하다는 결론 — 그래서 방법 선택의 갈림길이 여기다

- `project-deploying-llm` (지금 2,558자 · 절 12)
  - 절「상태를 밖으로 빼는 구조」 — 소절: FastAPI 인스턴스를 무상태로 두고 카운터·세션·캐시를 Redis에 두는 이유 / lifespan으로 httpx·Redis 연결을 앱 수명에 묶어 매 요청 연결 비용을 없애기 / 인스턴스마다 카운터가 따로 놀 때 레이트 리밋에 생기는 실제 오차
  - 절「스트리밍이 끊기는 자리」 — 소절: 전체 10초짜리 응답에서 첫 토큰 0.3초가 체감을 정하는 것 / X-Accel-Buffering: no를 빠뜨리면 nginx가 토큰을 모아 보내 스트리밍이 사라지는 것 / 클라이언트가 중간에 끊었을 때 GPU 작업을 함께 취소하지 않으면 남는 낭비 / 이미 200을 보낸 뒤에 생긴 오류를 어떻게 전달할 것인가
  - 절「동시성 상한과 대기열」 — 소절: KV 캐시 크기가 정하는 최대 동시 요청 수를 세마포어 값으로 옮겨 적기 / 큐 깊이가 상한의 세 배를 넘으면 즉시 503으로 거절하는 규칙과 그 근거 / 무한정 대기시켰을 때 오는 클라이언트 타임아웃과 메모리 누수
  - 절「레이트 리밋과 재시도」 — 소절: Redis 슬라이딩 윈도우가 토큰 버킷보다 단순한 자리 / 429에 Retry-After가 없으면 클라이언트가 즉시 재시도해 한꺼번에 몰리는 것 / 티어별 한도를 요청 수로 걸 것인가 토큰 수로 걸 것인가
  - 절「살아 있음과 받을 준비」 — 소절: /health와 /ready를 나누는 이유 — 큐가 넘쳐도 프로세스는 살아 있어야 한다 / 헬스체크가 Redis 연결과 GPU 백엔드 도달까지 확인해야 하는 자리 / 종료 신호를 받은 뒤 준비 신호를 먼저 내리고 진행 중 생성을 끝까지 보내는 순서 / terminationGracePeriodSeconds 기본 30초가 긴 생성을 끊는 사고
  - 절「지표 넷과 임계값」 — 소절: TTFT·처리량·오류율·큐 깊이와 각각의 알림 기준(p95 5초, 오류율 1%, 큐 20) / p50·p95·p99를 함께 봐야 하는 이유 / 구조화 로그에 요청 id를 심어 지표에서 로그로 건너가는 경로 / 토큰 수·큐 깊이 같은 LLM 고유 지표는 직접 정의해야 하는 것
  - 절「컨테이너와 로드 밸런서」 — 소절: --workers 1로 두는 이유 — 세마포어가 한 프로세스 안에서만 도는 것 / nginx least_conn이 처리 시간이 불균일한 LLM에 맞고 라운드로빈이 긴 요청을 한쪽에 쌓는 것 / GPU 컴포즈에 NVIDIA Container Toolkit이 필요한 것과 확인 순서

- `gpu-cuda` (지금 2,623자 · 절 9)
  - 절「지연과 처리량의 설계」 — 소절: CPU가 분기 예측·비순서 실행·큰 캐시로 한 흐름을 빨리 끝내는 구조 / GPU가 작은 코어 수천 개로 같은 연산을 동시에 미는 구조 / 1000×1000 행렬 곱이 10억 번의 독립 연산이라는 계산 / A100의 FP16 312 TFLOPS가 어떤 조건에서만 나오는 값인가
  - 절「SM과 워프의 실행 단위」 — 소절: SM 하나에 CUDA 코어·텐서 코어·공유 메모리·워프 스케줄러가 함께 들어 있는 것 / 워프 32스레드가 같은 명령을 함께 실행하는 SIMT / if/else로 갈리면 두 갈래가 차례로 도는 워프 발산 / H100의 132 SM × 128 코어를 실제 점유율로 환산해 보기
  - 절「메모리 계층과 접근 패턴」 — 소절: 레지스터·공유 메모리·L2·HBM·호스트 램의 대역폭 자릿수 차이 / 인접한 스레드가 인접한 주소를 읽어야 한 번에 묶이는 병합 접근 / PCIe가 HBM보다 자릿수 느려 H2D·D2H 복사를 줄여야 하는 이유 / 행렬 타일링이 공유 메모리를 쓰는 자리
  - 절「비동기 실행과 시간 재기」 — 소절: 파이썬 줄이 끝나도 GPU 작업은 큐에 올라간 것뿐이라는 사실 / synchronize 없이 time.time()으로 재면 커널 시간이 아니라 큐잉 시간을 재는 것 / torch.cuda.Event로 재는 방법과 워밍업 몇 회가 필요한 이유 / pin_memory와 non_blocking 복사가 겹쳐 도는 자리
  - 절「혼합 정밀도의 두 부품」 — 소절: autocast가 연산마다 FP16과 FP32를 골라 주는 규칙 / GradScaler가 언더플로를 막으려 손실을 키웠다 되돌리는 절차 / bf16이 지수부가 넓어 스케일러 없이 되는 자리 / 텐서 코어가 실제로 붙으려면 차원이 8의 배수여야 하는 제약
  - 절「메모리 지표 읽기」 — 소절: allocated와 reserved가 다른 것과 캐싱 할당자가 잡아 두는 몫 / empty_cache가 파편화에는 듣고 누수에는 안 듣는 것 / 그래디언트 누적으로 작은 배치에서 큰 유효 배치를 흉내 내는 계산 / OOM 메시지에서 어느 텐서가 범인인지 좁히는 순서
  - 절「자주 만나는 CUDA 오류」 — 소절: device-side assert — 인덱스 범위 초과와 CUDA_LAUNCH_BLOCKING=1로 스택을 되찾기 / 텐서와 모델의 디바이스가 갈릴 때 나오는 메시지와 .to(device)를 어디에 둘 것인가 / DataParallel이 마스터 GPU에 출력을 모아 한쪽만 터지는 것과 DDP가 그것을 없애는 구조

- `eval-golden-dataset` (지금 2,628자 · 절 7)
  - 절「세 가지 출처의 값」 — 소절: 운영 로그가 진짜 분포이면서 인터넷에 없어 오염되지 않은 이유 / 사람이 쓴 문제가 드문 상황을 담는 대신 실제로 안 오는 질문을 지어내기 쉬운 것 / 모델이 만든 문제는 세트를 처음 만들 때가 아니라 있는 갈래를 늘릴 때 쓰는 것
  - 절「로그를 거르는 두 단계」 — 소절: 개인정보와 평가에 못 쓰는 대화(한 줄 인사·오타 재질문·중도 이탈) 걸러 내기 / 원본을 지우지 않고 거른 결과를 따로 쓰는 이유 — 거르는 조건은 반드시 한 번 이상 바뀐다 / 중복을 안 지우면 300건 중 200건이 「배송 언제 와요」의 변형이 되는 것
  - 절「층화 표집의 세 기준」 — 소절: 빈도·틀렸을 때의 비용·지금의 정답률 셋을 함께 보기 / 로그의 3%인 규정 문의에 20%를 주는 판단의 근거 / 이미 100점인 갈래에 60자리를 주면 그 60자리가 아무것도 못 재는 것 / 층화한 세트의 평균은 서비스 품질이 아니므로 비율을 안 건드린 표본을 하나 더 두기
  - 절「정답의 네 가지 모양」 — 소절: 분류·추출은 값 하나, 사실 질의는 반드시 들어갈 사실 목록 / 긴 답변은 모범 답안과 함께 「무엇이 있어야 맞는 답인가」를 항목으로 적기 / 도구 호출은 불러야 할 도구와 인자를 호출 기록과 비교 / 정답에 근거(어느 규정 문서 몇 조)를 함께 저장해 규정이 바뀔 때 고칠 문제를 찾아내기
  - 절「두 사람이 붙여 보기」 — 소절: 일부를 두 사람이 각자 붙여 얼마나 갈리는지 세는 절차 / 30%에서 갈리면 모델이 못 맞히는 것이 아니라 애초에 답이 안 정해진 것이라는 판정 / 그런 문제를 기준을 다시 쓸 것인가 세트에서 뺄 것인가
  - 절「갈래당 필요한 건수」 — 소절: 30건 아래면 점수가 튀어 회귀와 잡음을 못 가르는 것 / 갈래당 50~100건, 갈래 여덟이면 전체가 400~800건이 되는 계산 / 갈래 다섯 × 40건으로 시작해 갈래별 점수를 보고 어디에 자리를 더 줄지 정하기
  - 절「동결과 교체 주기」 — 소절: 문제를 조용히 더하거나 빼면 어제 점수와 오늘 점수를 못 견주는 이유 / 분기마다 30%를 최근 로그에서 갈아 끼우고 버전 번호를 올리기 / 갈아 끼우는 날 두 버전을 함께 돌려 점수 차를 기록해 예전 숫자와 이어 읽기

- `serving-gpu-sharing` (지금 2,642자 · 절 6)
  - 절「기본값인 시분할」 — 소절: 드라이버가 프로세스를 번갈아 실행하고 전환마다 문맥을 갈아 끼우는 것 / 처리량은 거의 안 늘고 지연의 예측만 나빠지는 이유 / 서빙 엔진이 시작할 때 남은 메모리의 90%를 잡아 두어 두 번째 프로세스가 아예 못 뜨는 자리
  - 절「MPS가 메우는 빈자리」 — 소절: 여러 프로세스의 커널을 한 스케줄러 아래 모아 동시에 올리는 구조 / 작은 커널이 연산 유닛을 못 채울 때 옆 커널이 그 자리에 들어가는 이득 / CUDA_MPS_ACTIVE_THREAD_PERCENTAGE가 상한일 뿐 최소 보장이 아닌 것 / 한 프로세스의 메모리 오류가 같은 서버에 붙은 다른 프로세스까지 넘어뜨리는 것
  - 절「MIG의 하드웨어 칸막이」 — 소절: 연산 유닛·메모리·L2·대역폭까지 갈라 조각에 붙이는 방식 / A100 80GB의 1g.10gb~7g.80gb 프로파일과 각각에 맞는 모델 크기 / 조각이 놀아도 옆칸이 못 쓰는 것 — 이용률로는 오히려 손해가 나는 자리 / 프로파일을 바꾸려면 그 카드의 작업을 전부 내려야 하는 것과 조각끼리 못 묶는 제약
  - 절「격리와 이용률의 축」 — 소절: 동시 실행·메모리 칸막이·오류 격벽·성능 예측·되빌려주기 다섯 칸으로 셋을 나란히 놓기 / 지연 시간 약속을 걸 수 있는 것이 MIG뿐인 이유 / 「무엇이 제일 좋은가」로는 답이 안 나오고 무엇이 급한지가 정한다는 정리
  - 절「쿠버네티스의 함정」 — 소절: 디바이스 플러그인의 time-slicing replicas가 자원을 나눈 것이 아니라 스케줄러를 속인 것이라는 점 / 넷이 각자 90%를 잡으려 들어 셋이 못 뜨는 것과 gpu_memory_utilization을 0.22쯤으로 손수 나누는 대응 / MIG가 nvidia.com/mig-3g.40gb로 정직하게 노출되는 대비
  - 절「나누기 전에 볼 것」 — 소절: 이용률이 낮은 이유가 동시 요청 부족인지 배치가 안 차서인지 가르기 / 연속 배칭이 제대로 도는지부터 확인하면 엔진 설정 한 줄로 끝나는 경우 / 같은 베이스의 파생 모델이면 나누지 말고 어댑터를 함께 태우는 쪽이 이용률에 유리한 것

- `serving-multi-lora` (지금 2,661자 · 절 7)
  - 절「어댑터와 베이스의 크기 차」 — 소절: 2·r·d·L·s에 r=16, d=4096, L=32, s=4를 넣어 1,680만 개·fp16 34MB를 얻는 계산 / 8B 베이스 16GB 대비 0.21%라는 비율 / 어댑터마다 서버를 띄우면 그 0.21%를 위해 99.79%를 복사하는 것 / 트래픽이 고를 리 없어 하나가 붐빌 때 나머지가 노는 낭비
  - 절「병합을 포기한 계산」 — 소절: h = Wx + (α/r)BAx에서 앞항은 모두가 공유하고 뒷항만 요청마다 갈리는 구조 / 병합하는 순간 그 가중치가 특정 어댑터 전용이 되는 것 / 같은 어댑터를 부른 요청끼리 묶어 어댑터 수만큼만 작은 곱을 돌리는 그룹 행렬곱 / 연속 배칭과 맞물려 걸음마다 어댑터 구성을 다시 보는 것
  - 절「세 가지 대가」 — 소절: 걸음마다 5~15% 느려지는 계산 오버헤드 / 어댑터 종류가 많을수록 그룹이 잘게 쪼개져 커널이 헐거워지는 것 — 배치 64에 어댑터 32면 두 개씩 32그룹 / 어댑터 수 × 34MB가 KV 캐시에서 빠져나가 동시 요청 수가 주는데 지표에는 원인이 안 드러나는 것
  - 절「상주와 스왑」 — 소절: 자주 쓰는 것만 GPU에 붙박이로 두고 나머지를 CPU 메모리·디스크에 두기 / 34MB를 PCIe로 올리는 데 몇 ms, 디스크에서 수십 ms인 것과 카드를 새로 띄우는 몇 분의 대비 / 하루에 한 번 쓰는 어댑터는 그 한 번이 매번 느린 것과 LRU·예열 대응
  - 절「랭크가 섞일 때」 — 소절: 엔진이 가장 큰 랭크에 맞춰 자리를 잡는 구현이 흔한 것 / 랭크 8짜리 스물에 64짜리 하나를 섞으면 스물하나 전부가 64로 자리를 먹는 계산 / 큰 랭크 하나만 따로 서버를 두는 편이 전체로는 싼 자리 / 그래서 어댑터를 만들 때부터 랭크를 맞춰 두는 것
  - 절「이 방식이 안 맞는 자리」 — 소절: 베이스가 다르면 아예 안 되는 것 — 같은 크기라도 버전이 다르면 다른 모델 / 어댑터 하나가 카드를 꽉 채울 만큼 붐비면 격리만 잃고 계산 대가만 남는 것 / 고객사 격리 요건이 있으면 같은 프로세스에 태우는 것 자체가 문제인 자리 / 꼬리가 긴 분포일수록 이득이 커지는 이유

- `serving-autoscaling` (지금 2,669자 · 절 6)
  - 절「콜드 스타트 7분의 내역」 — 소절: 지표 감지·판단 지연·노드 확보·이미지 내려받기·가중치 로드·워밍업 여섯 조각으로 나누기 / 설정으로 줄어드는 것이 앞의 둘뿐이고 3분의 2가 뒤에 있다는 점 / 웜 풀·노드 이미지 사전 적재·로컬 NVMe 캐시가 각각 줄이는 초 / 준비 신호를 워밍업 뒤로 미루면 시간은 그대로인데 사용자가 겪는 것이 달라지는 자리
  - 절「이용률이 말을 멈추는 곳」 — 소절: nvidia-smi 이용률이 「최근 구간에 커널이 하나라도 돌던 시간의 비율」이라는 정의 / 배칭이 도는 서버가 어중간한 부하에서 이미 90%를 넘고 그 뒤로 안 움직이는 것 / 임계를 80%로 잡으면 일찍 늘고 90%로 잡으면 이미 밀린 뒤에 느는 딜레마
  - 절「밀린 정도를 재는 지표」 — 소절: 대기 요청 수는 복제본 수로 나눠야 비교가 되는 것 / KV 캐시 사용률이 90%를 넘으면 엔진이 요청을 밀어내기 시작하는 자리 / TTFT가 요청 길이에 흔들려 잡음이 많은 것과 도착률이 지연보다 먼저 움직이는 것 / vllm:num_requests_waiting과 vllm:gpu_cache_usage_perc를 함께 보는 구성
  - 절「목표 대수 계산」 — 소절: N = ⌈λs/(cu)⌉에 λ=12, s=8, c=24, u=0.7을 넣어 5.7 → 여섯 대를 얻기 / u를 1에 가깝게 두면 도착이 조금만 몰려도 대기열이 폭발하는 것 / 여유분이 콜드 스타트 7분을 견디는 완충재라는 읽기 — 스케일이 느릴수록 u를 낮게 / 한 대씩 늘리면 그 7분 동안 대기열이 더 자라 과하게 늘어나는 것
  - 절「최소 복제본 수의 선택」 — 소절: 0으로 내리면 첫 요청이 7분을 그대로 맞는 것과 개발·사내 도구에서는 그것이 맞는 이유 / 최소 1대 유지·시간표 기반 사전 확장·작은 모델로 받아 두기 세 절충 / 라우팅 계층이 이미 있으면 세 번째가 규칙 한 줄인 것
  - 절「줄일 때 깨지는 것」 — 소절: 진행 중인 생성을 끊으면 이미 절반을 스트리밍으로 보낸 뒤라 재시도도 어려운 것 / KV 캐시와 프리픽스 캐시가 그 복제본에만 있어 오래 산 복제본을 내리면 남은 쪽의 적중률까지 떨어지는 것 / 종료 신호 뒤 준비 신호를 내리고 가장 긴 생성만큼(5~10분) 유예를 주기 — 기본값 30초가 흔한 실수 / 빨리 늘리고 천천히 줄이는 비대칭의 근거

- `ondevice-mobile` (지금 2,692자 · 절 7)
  - 절「지속 처리량과 발열」 — 소절: 초당 32토큰이 1분쯤 뒤 14토큰이 되는 곡선과 그것이 앱이 아니라 OS의 결정이라는 점 / 설계에 쓸 숫자는 최고 속도가 아니라 평평해진 뒤의 값이라는 기준 / 짧게 쓰고 쉬는 기능(자동완성·알림 요약·분류)이 거의 항상 최고 속도로 도는 이유 / 대량 처리를 충전 중·화면 꺼짐 조건으로 미루는 구성
  - 절「배터리라는 청구서」 — 소절: 지속 추론이 동영상 재생과 비슷하거나 더 무거운 수준이라는 감 / 사용자가 설정 화면에서 앱별 소모를 정확히 볼 수 있다는 사실 / 사용자가 눌러 시작한 작업과 모르는 사이 도는 작업이 갈리는 지점
  - 절「CPU·GPU·NPU의 갈림」 — 소절: 세 유닛의 와트당 성능과 이식성 맞바꿈 / 안드로이드 NPU가 칩 제조사마다 런타임과 모델 포맷이 다른 파편화 / 일부 연산이 NPU에서 안 돌아 CPU로 떨어질 때 데이터 왕복 때문에 오히려 느려지는 것 / 이식성 있는 경로를 기본으로 깔고 상위 몇 종에만 NPU를 더하기 — 어느 경로로 돌았는지 반드시 기록
  - 절「메모리 상한과 강제 종료」 — 소절: iOS가 앱별 상한을 넘으면 즉시 죽이고 안드로이드가 경고를 먼저 보내는 차이 / 백그라운드로 갈 때 모델을 놓을 것인가 — 재로드 시간과 종료 확률의 맞바꿈 / KV 캐시가 대화 길이에 비례해 자라므로 문맥 상한과 요약 접기를 처음부터 넣기 / 램 4GB 기기에서는 켜지 말고 서버로 보내는 판단과 그 목록을 어디에 두는가
  - 절「첫 글자까지의 침묵」 — 소절: 그 시간의 대부분이 계산이 아니라 파일 읽기와 준비라는 분해 / 미리 올려 두기·파일 매핑·프롬프트 앞부분 재사용 셋으로 4초를 1초 아래로 내리기 / 첫 토큰이 나오는 즉시 흘리기 시작하면 같은 총 시간도 다르게 견뎌지는 것
  - 절「모델을 배달하는 문제」 — 소절: 앱에 넣기와 받아 오기의 맞바꿈 — 설치 크기 대 첫 실행 대기 / 다운로드 중에도 서버 경로로 동작하게 두기와 저장 공간이 모자랄 때 무엇을 지우라고 할지 / 기기 등급별로 다른 크기를 내려 주는 구성(상위 3B·중저가 1B·그 아래 서버) / 기기의 모델은 앱 업데이트로만 바뀌므로 원격 스위치로 기기 경로를 끌 수 있게 하기
  - 절「기기와 서버의 분담」 — 소절: 서버로 넘기는 조건 다섯 — 기기가 목록에 없음, 램 부족, 저전력 모드, 길이 초과, 기기 답의 검증 실패 / 넘어갈 때 사용자가 눈치채지 못하게 하는 처리 / 두 경로의 품질 차이를 무엇으로 재고 어디에 기록할 것인가

- `serving-benchmarking` (지금 2,698자 · 절 7)
  - 절「지표 넷과 보는 사람」 — 소절: TTFT·ITL·전체 응답 시간·처리량이 각각 누구의 관심인가 / 스트리밍이 아니면 TTFT가 뜻을 잃는 것 / 넷이 서로 반대로 움직여 하나만 좋게 만들기는 쉬운 자리
  - 절「무릎을 찾는 측정」 — 소절: 부하를 단계로 올리며 곡선을 그리고 단계마다 3~5분을 두는 절차 / 대기열이 자라기 시작하는 꺾임을 무릎으로 부르고 운영 지점을 그 앞에 두는 것 / 처리량은 최대 부하에서, 지연은 최소 부하에서 가져와 나란히 싣는 흔한 사고 — 둘 다 참이지만 함께 성립하지 않는다
  - 절「열린 루프와 닫힌 루프」 — 소절: 가상 사용자가 응답을 받고 다음을 보내면 서버가 느려질 때 부하도 함께 줄어드는 것 / 시계에 맞춰 보내야 감당 못 하는 구간을 만들 수 있는 이유 / 지연을 「실제로 보낸 시각」이 아니라 「보내려던 시각」부터 세야 하는 것 — 협조적 누락
  - 절「워크로드를 적어 두기」 — 소절: 입력 길이가 TTFT에 거의 비례하고 출력 길이가 배치 점유 시간을 정하는 것 / 평균이 같아도 편차가 크면 지연 꼬리가 두꺼워지는 자리 / 앞부분 공유율을 안 밝히면 캐시 적중 100%의 숫자를 싣게 되는 것 / 로그에서 길이 분포를 뽑아 그대로 쓰는 방법과 그럴 수 없을 때 적어 둘 것
  - 절「자주 새는 여섯 자리」 — 소절: 워밍업 없음·평균만 봄·실행이 짧음·클라이언트 병목·토크나이저 차이·실패를 안 셈 / 과부하에서 거절이 시작되면 남은 요청이 빨라져 지연 그래프가 오히려 좋아지는 착시 / 생성기의 CPU와 응답 대기 수를 함께 기록해 병목이 어느 쪽인지 가르기
  - 절「결과와 함께 남길 것」 — 소절: 모델·정밀도, 엔진 버전과 최대 배치·KV 비율·텐서 병렬 수, 카드 종류와 장수 / 부하 방식과 지연 기산 시점, 워밍업 제외 구간과 퍼센타일, 오류율 / 손으로 적으면 반드시 빠지므로 결과 파일에 설정을 함께 써 넣는 습관

- `edge-inference-hardware` (지금 2,745자 · 절 8)
  - 절「현장에 두는 네 이유」 — 소절: 데이터 반출 금지·회선 불안·지연이 공정에 들어감·올려 보낼 양이 과다 / 어느 이유냐에 따라 사양보다 이중화나 감사 기록이 먼저인 자리 / 「큰 모델을 쓰고 싶다」와는 다른 동기라 작은 모델로 되는 일만 내리게 되는 결론
  - 절「메모리로 거르기」 — 소절: M ≈ P·b + N·M_KV + 여유에 7B 4비트(4GB)와 세션 16개(0.4GB씩)를 넣어 12GB급을 얻는 계산 / 평균이 아니라 최대 동시 사용자로 세야 하는 이유 — 점심시간과 교대 시각 / 넘칠 때 대기열에 세울지 중앙으로 보낼지를 장비 고르기와 함께 정하기
  - 절「TOPS가 말하지 않는 것」 — 소절: 카탈로그 숫자가 INT8 피크라 토큰을 하나씩 만드는 구간과 거의 무관한 것 / 4GB 모델을 100GB/s에서 돌리면 토큰당 40ms·초당 25토큰이 상한이고 실제는 그 6~7할 / TOPS가 실제로 결과를 정하는 자리 — 프리필과 영상·문서의 묶음 처리 / 우리 일이 대화형인지 배치형인지 정하고 나서 카탈로그를 보는 순서
  - 절「동시 사용자와 묶음」 — 소절: 하나를 처리하든 여덟을 묶든 가중치를 읽는 비용은 한 번이라는 성질 / 대신 세션마다 KV 캐시가 자리를 먹어 메모리 관문으로 되돌아가는 것 / 엣지에서는 묶을 것이 애초에 없어 한 사람의 체감 속도로 골라야 하는 경우
  - 절「런타임 지원이라는 관문」 — 소절: 우리 런타임이 이 칩을 공식 지원하는가, 그 지원이 지금도 갱신되는가 / 모델 변환이 필요한가, 변환에서 정확도가 얼마나 떨어지는가, 커널·드라이버 버전을 우리가 고를 수 있는가 / 벤더 전용 가속기가 새 모델 구조 지원까지 몇 달 걸리거나 안 오는 것 / 3~5년 쓰는 동안 모델을 몇 번 바꾼다는 전제로 고르기
  - 절「전력과 지속 성능」 — 소절: 정격만이 아니라 순간 최대치·콘센트 종류·무정전 장치 용량을 미리 확인하기 / 함체 안에서 온도가 올라 클럭이 내려가는 것 — 용량 산정을 한 시간 돌린 뒤의 값으로 / 무팬 장비에서 그 폭이 특히 크게 갈리는 자리
  - 절「고른 뒤의 운영」 — 소절: 원격으로 모델을 내려 주고 검증하고 되돌리는 경로를 처음부터 만들기 — 사람이 가야 하면 사실상 안 바뀐다 / 한 대만 놓는 구성이 못 쓰는 자리와 중앙 자동 전환이 회선에 기대는 한계 / 초당 토큰·온도·대기열 길이·실패율을 중앙에 모아 두지 않으면 사용자 불만이 첫 신호가 되는 것

- `finetuning-hyperparameters` (지금 2,779자 · 절 8)
  - 절「사전학습 지식과의 균형」 — 소절: 사전학습 1e-4~3e-3 대비 파인튜닝이 한 자릿수 낮은 학습률을 쓰는 이유 / 너무 높으면 파괴적 망각, 너무 낮으면 수렴이 안 되는 양쪽 끝 / LoRA 1e-4~3e-4, Full 1e-5~5e-5, 분류 헤드 1e-3~1e-2로 갈리는 근거 — 학습 대상 파라미터가 사전학습된 것인가 새로 만든 것인가
  - 절「학습률을 찾는 절차」 — 소절: LR Range Test — 아주 작은 값에서 올리며 손실이 내려가는 구간을 찾기 / 손실이 튀기 시작하는 지점보다 한참 아래를 고르는 관례와 그 근거 / cosine·linear 스케줄이 뒤쪽에서 하는 일과 최종 LR을 0으로 둘 것인가
  - 절「워밍업이 막는 것」 — 소절: LoRA의 B가 0으로 초기화돼 초반 몇 스텝에 그래디언트가 튀는 자리 / 전체 스텝의 3~10%를 쓰고 데이터가 적을수록·LR이 높을수록 길게 잡는 규칙 / 워밍업을 뺐을 때 손실 곡선 앞머리에 남는 모양
  - 절「실효 배치를 만드는 법」 — 소절: per_device × accumulation × GPU 수로 실효 배치를 세는 식과 per_device=4·accumulation=8 → 32 예시 / 권장 32~128의 양쪽 끝 — 1~4의 그래디언트 잡음과 256 이상의 일반화 저하 / 누적 구간에서 손실을 누적 횟수로 나눠야 하는 이유 / 실효 배치를 키울 때 학습률도 함께 올릴 것인가
  - 절「에폭과 조기 종료」 — 소절: 데이터 규모별 권장(1,000 미만 5~10, 1만 이하 3~5, 10만 초과 1~2)과 그 근거 / train loss는 내려가는데 eval loss가 올라가는 지점을 찾는 절차 / load_best_model_at_end와 save_total_limit이 함께 고르는 것 / 데이터가 수백 건일 때 1에폭 만에 과적합이 오는 자리
  - 절「클리핑·감쇠·길이」 — 소절: max_grad_norm=1.0이 거의 모든 자리에서 권장되는 이유와 노름 스파이크가 뜻하는 것(LR 과대·배치 과소) / AdamW가 weight decay를 그래디언트에서 분리해 적용하는 것과 0.01이라는 출발점 / max_seq_length를 실제 데이터의 95 백분위수에 맞추기 — 8192로 잡았을 때 낭비되는 메모리 계산
  - 절「탐색 순서와 기록」 — 소절: LR → 배치 → 에폭 → 워밍업 → weight decay 순서로 하나씩 고정해 실험 수를 줄이는 이유 / 실험마다 설정·데이터 버전·시드를 함께 남기지 않으면 무엇이 좋았는지 못 되찾는 것 / 기본값 한 벌에서 출발해 eval loss만 보고 조정하는 실용적 접근

- `eval-cost-quality-tradeoff` (지금 2,791자 · 절 6)
  - 절「파레토 프론티어 그리기」 — 소절: 평가를 돌릴 때 점수 옆에 요청당 토큰과 지연을 함께 적어 두는 한 줄 / 더 싸고 더 좋은 대안이 있는 열등한 구성을 먼저 지워 후보를 줄이기 / 기울기가 눕는 지점 — 중형에서 중형+자기검증까지 비용 2.1배에 4점, 거기서 대형까지 두 배 더 쓰고 3점
  - 절「하한선을 먼저 못 박기」 — 소절: 예산을 먼저 정하면 빠듯한 시기에 품질이 조용히 무너지는 것 / 하한선을 평균으로 정하면 안 되는 이유 — 평균 82점 둘이 60점 미만 비율에서 2%와 12%로 갈릴 수 있다 / 「하위 5% 점수」나 「60점 미만 비율」처럼 꼬리 쪽으로 적기 / 실험마다 새로 정하지 말고 서비스 단위로 한 번 정해 두기
  - 절「값싼 레버 셋」 — 소절: 프롬프트 캐시가 품질을 안 건드리는 유일한 레버인 것과 앞부분이 매번 같아야 하는 조건 / 출력 길이 제한의 함정 — 잘린 답은 품질이 낮은 게 아니라 못 쓰는 답이라 잘린 비율을 따로 세기 / 컨텍스트 문서 수 k와 추론 예산이 각각 어떤 문제에서만 품질을 깎는가
  - 절「계단식의 손익분기」 — 소절: 전부 대형 600 대 계단식 268이 나오는 계산과 28건이 두 번 돈을 내는 구조 / p > c_s/c_l 에 1/6을 넣어 통과율 17%라는 분기점을 얻기 / 검증을 모델로 하면 그 비용이 c_s에 더해져 분기점이 올라가는 것 / 형식 검사·필수 필드·근거 인용·확신도 같은 거의 공짜인 검증기부터 쓰기
  - 절「검증기 임계값의 손잡이」 — 소절: 통과시키면 안 될 것을 통과시키는 오류와 막아도 될 것을 막는 오류의 무게 차이 / 그래서 임계값을 막는 쪽으로 치우치게 잡는 관례 / 임계값을 몇 단계로 바꿔 가며 프론티어를 다시 그려 자리를 눈으로 고르기
  - 절「비용을 세는 단위」 — 소절: 요청당으로 세면 실패를 늘리며 싸지는 변경이 좋아 보이는 것 — 성공한 요청당으로 세기 / 에이전트는 과제당으로 세지 않으면 걸음을 잘게 쪼개는 변경이 저렴해 보이는 것 / 사람 검수 비용을 빼고 세면 검수 비율을 절반으로 줄이는 큰 개선이 안 보이는 것

- `finetuning-lora-merging` (지금 2,791자 · 절 6)
  - 절「병합이라는 덧셈」 — 소절: $$W' = W + (\alpha/r)BA$$ 를 한 번 전개해 어댑터 값이 어디로 들어가는지 / 어댑터 하나 병합이 근사가 아니라 정확한 연산인 이유 — 합친 모델과 얹은 모델의 출력이 같다 / 4비트 베이스에 그대로 합칠 때 반올림 오차가 쌓이는 경로, QLoRA로 학습했으면 원본 정밀도 베이스를 다시 받아 합치고 그 결과를 새로 양자화하는 순서
  - 절「합칠 자리와 얹을 자리」 — 소절: 어댑터 하나 · 고객사별 · 잦은 갱신 · 엣지 내보내기 네 상황의 판단 / 고객사 50곳을 통째 모델 50개로 띄우면 GPU 50대, 베이스 하나 + 어댑터 50개면 한 대가 되는 산수 / 얹어 두는 쪽이 무는 대가 — 어댑터 커널이 요청마다 붙고 적재분만큼 KV 캐시에서 자리를 빼 온다
  - 절「둘 이상을 더할 때의 상쇄」 — 소절: 가중 평균 $$\sum_k \lambda_k (\alpha_k/r_k) B_kA_k$$ 가 셋을 넘기며 무뎌지는 이유 — 부호가 반대인 값이 서로를 깎는다 / 랭크와 $$\alpha$$ 가 다른 어댑터의 실제 배율 $$\alpha/r$$ 을 각각 계산해 놓고 계수를 잡는 절차 / 상쇄가 얼마나 일어났는지 보는 법 — 병합 전 각 $$\Delta W$$ 의 노름 합과 병합 후 노름을 층별로 비교
  - 절「TIES·DARE·SLERP」 — 소절: TIES의 세 단계 — 작은 값 버리기 → 부호 다수결 → 남은 것만 평균 / DARE가 변화량 대부분을 무작위로 버리고 남은 것을 그 비율만큼 키워도 되는 이유(변화량의 중복)와 DARE→TIES 조합 / SLERP과 모델 수프는 다른 문제를 푼다 — 정확히 둘을 보간하는 자리, 같은 과제의 체크포인트를 평균하는 자리
  - 절「병합 전에 맞춰 둘 것」 — 소절: `target_modules`가 어긋난 둘을 합치면 겹치는 자리만 섞이고 나머지는 그대로 들어간다 / 어댑터 학습 중에 추가한 특수 토큰과 토크나이저를 함께 옮기기 / 베이스 리비전 고정 — 같은 이름의 다른 커밋 위에 합치면 오류 없이 조용히 이상해진다
  - 절「합친 뒤의 검증」 — 소절: 어댑터별 원래 과제를 따로 재기 — 「평균은 괜찮은데 하나가 무너진」 경우는 총점에 안 보인다 / 계수 0.3·0.5·0.7을 각각 만들어 재는 탐색이 학습보다 훨씬 싼 이유 / 하나짜리 병합을 얹은 채로 돌린 결과와 대조 — 출력이 달라지면 그 자체가 양자화·리비전 오류의 증거

- `model-routing-cascade` (지금 2,870자 · 절 7)
  - 절「사전 라우팅과 캐스케이드」 — 소절: 판단 시점·근거·추가 비용·지연·만들기 난이도 다섯 축 대조 / 캐스케이드 로그가 라우터의 학습 라벨이 되는 순서 / 라우터를 먼저 만들려다 라벨이 없어 막히는 자리
  - 절「비용식과 지연식」 — 소절: $$C = c_s + p c_l$$ 에서 $$p < 1 - c_s/c_l$$ 유도 — 20분의 1 모델이면 승격률 95%까지 이득 / 그래서 비용으로 판단하면 답이 언제나 「도입」이 되고 그건 판단이 아니라는 지적 / $$L = l_s + p(l_v + l_l)$$ 에 0.4·0.1·2초와 승격률 15%를 넣어 평균 0.72초, 꼬리 2.5초 / 승격되는 요청이 곧 어려운 요청이라 꼬리가 가장 중요한 사용자에게 떨어지는 구조
  - 절「승격 신호 네 갈래」 — 소절: 확실한 신호 — 스키마를 못 맞춘 JSON, 없는 도구 호출, 필수 필드 누락, 라벨 집합 밖의 값, 빈 응답 / 도메인 규칙 — 인용 문서 번호를 검색 결과와 대조, 계산 검산, 입력에 없는 고유명사 / 작은 판정 모델과 그 판정의 정확도를 라벨로 따로 재 두기 / 자기 확신도와 로그 확률이 마지막 수단인 이유 — 유창함에 가깝지 사실성과 멀다
  - 절「임계값 곡선 그리기」 — 소절: 평가 묶음 몇백 건을 두 모델에 다 돌려 두면 임계값 훑기가 추가 호출 없이 표 계산으로 끝나는 이유 / 승격률 10~25%에서 품질 이득의 대부분이 나오는 무릎 찾기 / 무릎이 아주 오른쪽에 있으면 임계값이 아니라 작은 모델이 이 작업에 안 맞는다는 신호
  - 절「스트리밍과의 충돌」 — 소절: 다 만들어야 검사할 수 있으니 작은 모델의 출력을 흘려보낼 수 없다 / 선택지 셋 — 검사 후 한 번에 보이기, 흘려보내고 승격 시 교체, 스트리밍 기능은 캐스케이드에서 빼고 사전 라우팅만 / 긴 생성에서 작은 모델 비용이 통째로 버려지는 계산
  - 절「승격률이라는 지표」 — 소절: 서서히 오른다 · 갑자기 0에 가까워진다 · 그대로인데 비용이 오른다 세 신호의 해석 / 승격된 요청에서 큰 모델의 답이 실제로 더 나았는지 표본으로 대조하기 / 프롬프트 캐시가 모델마다 따로 잡히므로 승격이 잦으면 양쪽 적중률이 함께 떨어지는 상호작용
  - 절「도입하지 않을 자리」 — 소절: 하루 몇천 건에서 아끼는 금액이 두 모델 관리 비용에 못 미치는 계산 / 지연 상한이 엄격한 실시간 기능 — 꼬리가 두 배가 되는 구조를 감당할 수 없다 / 작은 모델 품질이 애초에 모자라면 승격률만 올라 아무것도 못 아끼고 복잡도만 는다

- `ondevice-inference-basics` (지금 2,870자 · 절 7)
  - 절「대역폭이 정하는 속도」 — 소절: $$t \approx P b / BW$$ 를 3B·4비트·60GB/s로 끝까지 계산해 초당 30~50토큰 상한을 얻기 / 실측이 그 값의 50~70%에 그치는 이유 셋 — 읽기가 이어지지 않고, 어텐션이 따로 붙고, 다른 앱이 같은 버스를 쓴다 / 계산 결과가 초당 5토큰이면 최적화가 아니라 모델을 바꿔야 하는 자리라는 판단
  - 절「프리필과 디코드」 — 소절: 두 국면의 병목·체감·줄이는 법 대조 / 「초당 30토큰인데 느리다」는 신고가 대개 첫 토큰까지의 2초인 이유 / 긴 문서를 통째로 넣는 설계가 기기에서 특히 비싼 이유 — 프리필이 입력 길이에 정직하게 비례한다
  - 절「메모리 예산 짜기」 — 소절: 램 용량이 아니라 OS·다른 앱·우리 앱을 뺀 남는 자리부터 시작하는 순서 / $$M_{KV} = 2Ln_{kv}d_hTb$$ 를 4k와 16k로 각각 넣어 0.44GB와 1.8GB — 문맥 길이가 기능이 아니라 지출 항목인 이유 / 캐시를 8비트·4비트로 눌러 문맥을 여는 거래 / 서버는 요청을 큐에 세우지만 휴대폰 OS는 앱을 죽인다 — 여유분을 넉넉히 잡는 근거
  - 절「양자화라는 전제」 — 소절: 4비트가 자리와 읽을 바이트를 함께 4분의 1로 만들어 속도까지 바뀌는 구조 / 층별로 비트를 달리 주는 방식이 품질을 가장 잘 지키는 자리 / 3비트 아래에서 무너지기 시작하는 것과, 눌러 놓고 평가 묶음으로 다시 재는 단계를 건너뛰면 안 되는 이유
  - 절「런타임 네 갈래」 — 소절: llama.cpp 계열 · 애플 계열 · 모바일 런타임 · 브라우저의 성격과 잘 맞는 자리 / 고르는 기준이 성능 수치가 아니라 「어느 기기까지 지원할 것인가」인 이유 / 벤더 NPU와 이식성을 함께 넣고 기기에 따라 고르는 분기 코드가 프로젝트에서 차지하는 몫
  - 절「배포에서 부딪히는 것」 — 소절: 앱에 넣기와 받아 오기 — 설치 이탈과 첫 실행 대기의 교환, 그리고 받아 오는 쪽이 요구하는 실패·재시도·저장 공간 부족 코드 / 1.5GB 첫 로드 1~5초를 매핑과 예열로 감추기 / 기기 모델은 사용자가 갱신할 때까지 남는다 — 몇 달 전 버전을 전제로 한 프롬프트와 서버로 되돌릴 스위치
  - 절「기기와 서버 나누기」 — 소절: 분류·추출·자동완성·짧은 요약은 기기, 긴 추론과 복잡한 계획은 서버 / 경계를 긋는 한 문장 — 틀렸을 때 사용자가 바로 알아채고 넘어갈 수 있는가 / 경계를 먼저 그으면 모델 크기와 메모리 예산이 따라오는 숫자가 되는 순서

- `eval-agent-trajectories` (지금 2,906자 · 절 7)
  - 절「정답률이 감추는 것」 — 소절: 같은 정답에 걸음 셋과 다섯 — 토큰 2.8배, 부르지 않아도 될 도구, 오류 덕에 방향을 잡은 실행 / 틀린 100건이 한 가지 이유인지 열 가지 이유인지 정답률은 답하지 않는다 — 그 둘이 완전히 다른 작업인 이유 / 운과 실력을 가르려면 무엇을 더 기록해야 하는가
  - 절「지표의 세 층」 — 소절: 결과층 — 과제 성공률, 걸음 수·토큰·지연 / 과정층 — 도구 선택 정확도, 인자 정확도, 불필요 걸음 비율, 복구율 / 과정 지표가 결과 지표보다 먼저 움직여 회귀를 빨리 잡는 이유 / 안전 축은 점수로 세지 않고 통과·실패로 센다 — 삭제 API를 한 번 부른 실행은 나머지가 좋아도 실패
  - 절「실패의 다섯 갈래」 — 소절: 계획·도구 오선택·인자·결과 오독·멈춤 실패가 한 걸음을 밟는 순서에서 나오는 구조 / 갈래마다 고치는 자리가 다르다 — 계획은 시스템 프롬프트, 오선택은 도구 설명의 「언제 쓰지 않는가」, 인자는 필드 설명·예시·열거형, 결과 오독은 도구 응답 모양, 멈춤 실패는 최대 걸음 수와 종료 조건 / 분류기 자체를 검증하기 — 사람이 나눈 50건과 맞춰 보면 계획과 오선택 경계에서 갈린다
  - 절「대조하는 세 방법」 — 소절: 정답 궤적 대조와 「정답 궤적이 하나가 아니다」 문제 — 순서를 무시한 집합 비교, 도구 이름만 보고 인자는 따로 / 규칙 검사 — 확인 없는 삭제 호출, 같은 인자로 세 번 이상, 빈 검색 결과 위에 답 쌓기. 안전 축은 거의 전부 이쪽 / 모델 채점은 궤적을 통째로 던지지 말고 걸음마다 「목표에 가까워졌는가」를 묻고 모아야 흔들리지 않는다
  - 절「걸음에 남길 것」 — 소절: `run_id`·`step`·`tool`·`arguments`·`status`·`result_size`·`latency_ms`·`tokens`·`parent_step`이 각각 나중에 무엇을 답해 주는가 / `status`와 `result_size`가 없으면 「성공했지만 빈 결과」를 못 찾고, 결과 오독은 정확히 그 자리에서 생긴다 / 실행 단위 식별자가 없으면 걸음 수 평균이 실행 경계를 넘나들며 계산된다
  - 절「재현과 채점 비용」 — 소절: 도구 호출에 실제 부작용이 있어 궤적 평가를 마음대로 다시 못 돌리는 문제 / 도구 응답을 녹화해 되돌려 주는 고정 응답으로 결정성 확보하기 / 모델 버전·온도·도구 스키마 버전을 결과에 함께 적어 두지 않으면 지난 점수와 이어 읽을 수 없다
  - 절「회귀 세트로 굳히기」 — 소절: 갈래마다 실패했던 실행을 몇 건씩 골라 고정 세트로 / 통과한 실행도 넣는 이유 — 도구 설명을 고쳐 인자 오류를 잡으면 도구 선택이 나빠지는 일이 흔하다 / 지표를 갈래별로 나란히 보기 — 전체 성공률로 합치면 좋아진 것과 나빠진 것이 상쇄된다 / 첫 세 걸음까지만 돌려 도구 선택과 인자만 보는 짧은 세트

- `mlops-ci-cd` (지금 2,919자 · 절 8)
  - 절「ML에서 CI가 어려운 이유」 — 소절: 코드가 그대로여도 데이터가 바뀌면 결과가 바뀐다 — 트리거를 둘로 두어야 하는 구조 / 비결정성 — 같은 코드·데이터로 0.3% 흔들릴 때 단일 임계값 대신 범위, 시드 고정, 3회 평균 / 학습 시간 — 모든 PR에 전체 재학습을 걸었을 때의 비용과 「무엇이 재학습을 부르는 변경인가」의 분류
  - 절「CI·CD·CT 세 요소」 — 소절: 각 요소가 무엇에 반응해 무엇을 돌리는가 / CT의 세 방아쇠 — 스케줄·드리프트·성능 하락과 각각이 오작동하는 방식(드리프트 오탐이 재학습을 매일 부르는 자리) / 재학습을 러너 안에서 돌리지 않고 클라우드 잡으로 넘기는 이유와 그때 필요한 완료 대기·타임아웃
  - 절「테스트 네 층」 — 소절: 단위 — 피처 변환 함수와 스키마, 수백 개가 수십 초 안에 / 통합 — 소규모 데이터로 파이프라인 완주와 서빙 응답 스키마 / 모델 테스트 — 베이스라인 대비 회귀, 슬라이스별 공정성, 신규 사용자 세그먼트 / 섀도 — 실 트래픽을 새 모델에도 보내고 응답은 쓰지 않는 구조, 층마다 몇 개를 두고 어디까지 자동으로 막을 것인가
  - 절「성능 게이트 설계」 — 소절: 고정 임계값이 몇 달 뒤 무의미해지는 방식 / 챔피언 대비 델타로 바꾸면 달라지는 것 — 하락 허용폭과 개선 판정선을 각각 정하는 근거 / 다중 메트릭 — 정확도만 보면 추론 p99가 뚫린다, 최소·최대 방향을 지표마다 나눠 적기 / 메트릭이 기록되지 않았을 때를 통과로 처리하지 않는 규칙
  - 절「트리거 세분화」 — 소절: 경로별로 다른 파이프라인 — 피처 코드, 모델 코드, 데이터 스키마, 설정이 각각 부르는 것 / 캐시와 아티팩트로 반복 구간을 건너뛰기 / 실패했을 때 파이프라인이 무엇을 남기고 죽는가 — 로그·부분 산출물·정리되지 않은 클라우드 잡
  - 절「배포와 되돌리기」 — 소절: 카나리 5% → 50% → 100%를 시간이 아니라 지표로 넘기기 / GitOps — 배포 매니페스트를 파이프라인이 커밋하고 클러스터가 따라오는 구조, 그리고 사람이 손으로 고쳤을 때 벌어지는 되돌림 / 자동 롤백 조건을 배포 전에 코드로 적어 두기
  - 절「사람이 남는 자리」 — 소절: 전부 자동으로 돌리면 안 되는 승격 단계와 그 근거 / 실패 알림이 아무도 안 보는 채널로 가는 흔한 결말 / 파이프라인 자체를 회귀 테스트하기 — 일부러 나쁜 모델을 흘려 게이트가 실제로 막는지 확인한다

- `eval-online-ab` (지금 2,947자 · 절 6)
  - 절「대리 지표의 한계」 — 소절: 오프라인 점수가 원하는 것이 아니라 잴 수 있는 것을 재는 구조 / 답을 길게 만들면 채점 모델 점수는 오르고 사용자는 필요한 문장을 못 찾아 재질문하는 어긋남 / A/B가 대신할 수 없는 것과 A/B로만 답할 수 있는 것의 경계
  - 절「지표의 세 층」 — 소절: 핵심 지표는 하나여야 한다 — 둘이면 결정 규칙이 사라지고 그 자리를 취향이 채운다, 둘 다 중요하면 합치는 법을 실험 전에 정한다 / 가드레일(오류율·p95·요청당 비용·안전 위반율·이탈률)을 실험마다 새로 정하지 않고 공통으로 붙이는 이유 / 진단 지표를 판단에 안 쓴다고 못 박기 — 어느 지표로 결정할지 미리 적는 것이 유일한 방어
  - 절「표본 크기와 기간」 — 소절: MDE를 사업적으로 의미 있는 최소 크기로 정하는 절차(4.0%를 4.2%로면 0.2%p) / $$n \approx 2(z_{\alpha/2}+z_\beta)^2 p(1-p)/\text{MDE}^2$$ 에 $$p=0.04$$, MDE 0.002를 넣어 무리당 약 30만 명 / 그 숫자가 안 되면 실험이 불가능한 것이 아니라 그만큼 작은 차이를 못 본다는 뜻 / 이탈률 대신 「답을 복사한 비율」처럼 자주 일어나는 지표를 찾는 쪽이 현실적인 이유, 그리고 최소 한 주 규칙
  - 절「중간 확인이 만드는 오탐」 — 소절: A와 B가 실제로 같은데도 p값이 매일 흔들려 14번 중 어느 날은 0.05를 뚫는 구조 / 유의수준 5%는 「한 번에」의 확률이지 「열네 번 봐도」가 아니다 — 실제 오탐률 20~30% / 막는 셋 — 멈출 날을 미리 정하고 안 보기, 가드레일만 매일 보고 핵심은 가려 두기, 여러 번 보기를 전제한 방법 쓰기
  - 절「배정에서 새는 것」 — 소절: 사용자 단위 배정과, 식별자가 없을 때 세션이 끝날 때까지 같은 쪽에 머물게 하기 / 캐시 키에 실험 배정을 넣지 않으면 A가 만든 결과를 B가 받는다 / 실험 식별자와 배정을 요청 로그에 함께 남기지 않으면 분석 자체가 불가능해진다 / 신기함 효과 — 첫 이틀을 빼고 다시 계산해 크기를 재기
  - 절「A/A 테스트와 기반 점검」 — 소절: 양쪽에 똑같은 것을 보여 주는 실험이 무엇을 잡아내는가 / 여기서 지표가 유의하게 갈리면 배정·로깅·집계 중 어디가 새는지 좁혀 가는 순서 / 실험 기반을 처음 세울 때 반드시 한 번 거치는 이유
  - 절「트래픽이 적을 때」 — 소절: 섀도 배포 — 행동은 못 재도 오류율·지연·비용을 실제 분포로 재고 위험이 없다 / 인터리빙 — 같은 사람 안에서 비교해 사람 사이 차이가 상쇄되므로 표본이 훨씬 적게 들지만 목록형 결과에만 붙는다 / 전환점 분석은 계절성과 다른 변경이 섞이는 약한 증거이고, 약하다는 것을 보고에 함께 적는 것이 사용 조건

- `finetuning-dpo-practice` (지금 2,984자 · 절 6)
  - 절「RLHF에서 지운 단계」 — 소절: 보상 모델 학습 → 강화학습 두 단계와 DPO 한 단계의 대조 / 학습 중 메모리에 함께 올라가는 모델 수가 줄어드는 계산 / 지도학습과 같은 모양으로 돈다는 것이 실무에서 뜻하는 것 — 안정성과 디버깅
  - 절「손실식 읽는 법」 — 소절: 괄호 안 각 항이 「참조 대비 이 답을 얼마나 더 좋아하게 됐는가」임을 항별로 짚기 / 참조 모델이 기준으로 들어가지 않으면 선호 데이터에 없던 능력이 통째로 무너지는 경로 / $$\beta$$ 0.05~0.5 구간의 성격과, $$\beta$$ 를 학습률보다 먼저 정하는 순서
  - 절「선호 쌍 만들기」 — 소절: 사람 비교·모델 심판·규칙·기존 대비 네 방법이 각각 데이터에 심는 성질 / 「같은 모델·같은 프롬프트」 규칙 — 좋은 답을 큰 모델에서 가져오면 판단이 아니라 문체를 배운다 / 길이 편향 — 길이 차 상한, 차이 큰 쌍 버리기, 길이 벌점 셋 / 사람 둘이 갈리는 애매한 쌍은 정보가 아니라 잡음, 수천 쌍이면 도는 방법이라 덜어 내는 편이 낫다
  - 절「학습 설정과 순서」 — 소절: 학습률 $$10^{-7}$$ 대 — SFT의 $$10^{-5}$$ 를 그대로 가져오는 것이 가장 흔한 실수 / 에포크 1에서 시작하고 2를 넘기지 않는 근거 / SFT가 선택이 아니라 순서인 이유 — 참조가 베이스가 되면 선호가 가리키는 방향과 멀어 학습이 불안정해진다 / LoRA로 정책을 학습하고 어댑터를 끈 상태를 참조로 써 사본을 없애는 구성
  - 절「내려가는 손실과 나빠지는 답」 — 소절: 손실이 두 로그확률의 차이라서 둘 다 내려가도 잘 내려가는 구조 / 함께 볼 세 곡선 — chosen 로그확률(크게 내려가면 멈춘다), rejected 로그확률(내려가는 것이 정상), 정확도 / 정확도 0.9를 넘으면 데이터를 외운 것이라 더 돌릴 이유가 없다 / 대응 셋 — $$\beta$$ 키우기, 학습률 낮추기, SFT 손실을 조금 섞어 좋은 답의 확률을 직접 올리기
  - 절「밖에서 재는 회귀」 — 소절: 선호 쌍에 없던 능력(코드 생성·수식 계산)을 학습 전후로 재 두기 / 답 길이 분포가 학습 전후로 얼마나 밀렸는지 함께 보기 / 실제 서비스 프롬프트로 몇십 건을 눈으로 훑는 마지막 단계
  - 절「IPO·KTO·ORPO·SimPO」 — 소절: 고르는 기준이 알고리즘이 아니라 가진 데이터의 모양이라는 점 / KTO가 실무에서 쓸모 있는 이유 — 운영에서 모이는 것은 👍👎 단답이지 두 답의 비교가 아니고, 억지로 짝지으면 「같은 모델·같은 프롬프트」가 깨진다 / 참조 사본을 못 들 때의 SimPO, SFT를 따로 못 돌릴 때의 ORPO / 방법을 바꿔 풀리는 문제보다 데이터를 고쳐 풀리는 문제가 훨씬 많다

- `finetuning-reward-modeling` (지금 3,020자 · 절 6)
  - 절「점수 헤드와 손실」 — 소절: 다음 토큰 분포를 내던 마지막 층을 스칼라 하나로 바꾸는 구조 / 브래들리-테리 손실 $$-\log\sigma(r_w - r_l)$$ 이 실제로 최적화하는 것 / 손실에 차이만 들어가므로 점수의 절댓값에 뜻이 없다 — 프롬프트가 다른 답끼리 비교하면 안 되고, GRPO가 무리를 같은 프롬프트에서 뽑는 이유가 여기에도 걸린다
  - 절「사람 일치도라는 상한」 — 소절: 같은 쌍 200개를 두 사람에게 보여 겹치는 비율을 재는 절차 / 0.85 이상 · 0.70~0.85 · 0.70 미만 세 구간의 대응 / 일치도가 낮으면 데이터를 열 배로 늘려도 안 되는 이유 — 사람도 못 가르는 것을 모델이 가를 수 없다 / 가이드라인을 고치고 일치도를 다시 재는 순환이 곧 수집 작업이라는 관점
  - 절「데이터에 새는 신호」 — 소절: 「같은 모델·같은 프롬프트」와 길이 맞추기가 여기서도 그대로 걸리는 이유 / 정책이 그 점수를 최대화하므로, 길이에 준 점수는 학습이 끝난 뒤 정확히 긴 답으로 돌아온다 / 「긴 쪽을 고른다」 규칙만의 정확도를 대조군으로 계산해 두기 — 그 값이 0.72인데 모델이 0.74면 모델은 길이만 보고 있다
  - 절「학습 설정」 — 소절: 베이스는 SFT를 끝낸 것 — 정책이 낼 법한 답을 이해해야 그 답들을 가른다 / 정책보다 작아도 되지만 너무 작으면 미묘한 차이를 못 가르고 그 자리를 표면 신호가 채운다 / 에포크 1, 학습률 $$10^{-6}$$ 대 / 지표는 검증 쌍 정확도이고 사람 일치도가 천장 — 일치도 0.8에 정확도 0.95면 잘된 것이 아니라 새는 것
  - 절「범위 밖으로 나가는 정책」 — 소절: 보상 모델은 학습에서 본 답의 범위에서만 믿을 수 있다는 성질 / 강화학습이 그 범위 밖에서 우연히 높은 점수를 받는 지점을 찾아내는 과정 / 곡선 하나만 보면 성공으로 보인다는 점이 이 실패를 위험하게 만드는 이유
  - 절「보상 해킹을 막는 네 겹」 — 소절: KL 항을 살리는 것이 곧 학습 범위 안에 머무는 것 — RLVR과 달리 여기서 $$\beta$$ 를 0으로 두면 안 된다 / 체크포인트마다 답 100개를 사람이 훑기 — 두 곡선을 실제로 그려 보는 유일한 방법 / 시드가 다른 보상 모델 셋의 최솟값 쓰기, 빈틈의 위치가 서로 다르다는 근거 / 지금 정책의 답으로 새 쌍을 모아 다시 학습하는 순환을 몇 바퀴 도는 실무 구성
  - 절「네 갈래 보상 모델」 — 소절: 결과 · 과정 · 생성형 심판 · 규칙 혼합의 채점 대상과 어울리는 자리 / 과정 보상 모델의 촘촘한 신호와 단계마다 라벨이 필요해 몇 배가 되는 데이터 비용 / 생성형 심판이 채점 한 번 = 생성 한 번이라 학습 루프에 비싸고 평가 쪽으로 밀리는 이유 / 규칙이 울타리를 치고 그 안에서 모델이 품질을 재는 혼합 구성이 실무에서 가장 흔한 이유

- `finetuning-eval-during-training` (지금 3,039자 · 절 7)
  - 절「교사 강요와 자기 출력」 — 소절: 검증 손실 식에서 $$y_{<i}$$ 가 정답으로 주어진다는 조건이 무엇을 뜻하는가 / 서비스에서는 100번째 토큰의 앞 99개가 자기 출력이라는 차이 / 두 숫자가 어긋나는 세 모양 — 손실은 내려가는데 긴 출력이 무너진다, 형식이 깨진다, 손실은 그대로인데 점수가 오른다
  - 절「고르는 지표 정하기」 — 소절: 체크포인트를 고르는 지표는 배포를 결정하는 지표와 같아야 한다는 원칙 / 손실로 고른 뒤 골든셋으로 검사하는 순서가 뒤집힌 이유 — 손실이 이미 후보를 좁혀 버렸다 / 단일 최대 · 문턱 + 최대 · 가중합 세 규칙과 가운데가 기본값인 근거(가중치를 정할 근거가 대개 없다) / 고른 것이 마지막 체크포인트면 한 번 의심하는 습관
  - 절「평가의 세 층」 — 소절: 매 50~100스텝 검증 손실 — 거르는 용도이지 고르는 용도가 아니다, 발산·NaN에서 학습을 즉시 죽여 GPU 시간을 아낀다 / 매 500스텝 골든셋 30~50문제 — 매번 같은 고정 부분집합을 쓰는 이유(표본 교체와 모델 변화가 안 갈린다) / 에폭 끝 전체 스위트 — 보존·안전·사람 검수와 배포 판단
  - 절「평가 비용 계산」 — 소절: 500스텝 15분에 평가 4분이면 학습 시간의 21%, 8시간이 10시간이 되는 산수 / 체크포인트만 저장하고 별도 프로세스가 집어 가 평가하고 결과만 되돌려 주는 구조 / 평가 생성을 배치로 묶기, 최대 생성 길이를 256토큰으로 자르기 / GPU가 한 장뿐일 때 4분을 1분 근처까지 줄이고 주기를 늘리는 차선
  - 절「조용히 틀리는 자리」 — 소절: 검증셋이 학습셋과 겹친다 — 무작위 분할 대신 문서·고객 단위로 나누기 / 평가 프롬프트가 서비스의 시스템 프롬프트·채팅 템플릿과 한 글자만 달라도 점수가 움직인다 / 샘플링이 켜져 있으면 같은 체크포인트가 두 번 다르게 나온다 — 학습 중 비교는 탐욕 디코딩으로 고정 / 30문제에서의 2점 차이를 추세로만 읽고 최종 선택은 표본과 유의성을 따진 뒤에
  - 절「조기 종료 걸기」 — 소절: `metric_for_best_model`을 손실이 아니라 고를 때 쓰는 지표로 두고 `greater_is_better`까지 함께 뒤집기 / 생성 기반 점수가 손실보다 울퉁불퉁해 인내 구간을 평가 3~5회로 넉넉히 잡는 이유 / 끊긴 지점이 아니라 가장 좋았던 지점을 저장하려면 인내 구간 동안 계속 저장하고 있어야 한다
  - 절「기록으로 남기는 것」 — 소절: 체크포인트마다 함께 적을 것 — 데이터 커밋, 시드, 평가 세트 버전, 프롬프트 템플릿 / 두 번째 학습에서 「그때 그 곡선」을 다시 그릴 수 있게 로그를 한자리에 모으기 / 같은 설정을 두 번 돌려 점수 차이의 잡음 폭을 먼저 알아 두는 준비 작업

- `serving-pipeline-parallel` (지금 3,127자 · 절 9)
  - 절「스테이지로 자르기」 — 소절: 80층을 카드 넷이 20층씩 나눠 들고 요청이 GPU 0에서 3까지 흐르는 경로 / 통신량 대조 — 층마다 All-Reduce 160번 대 스테이지 경계에서 3번 / 한 방향으로 한 번만 보내면 되므로 서버 경계를 넘길 수 있는 이유, All-Reduce는 가장 느린 링크가 전체를 잡아먹는다
  - 절「파이프라인 버블」 — 소절: 요청 묶음을 통째로 밀면 어느 순간에도 한 장만 일해 이용률이 25%가 되는 그림 / 버블 비율 $$(p-1)/(m+p-1)$$ 을 $$m=4,8,16$$ 으로 계산해 43%·27%·16% / 잘게 썰어도 0이 되지 않는 이유 — 파이프라인을 채우는 시작과 비우는 끝
  - 절「추론에서 달라지는 것」 — 소절: 디코드는 요청당 토큰 하나라 더 쪼개면 조각이 GPU를 못 채운다 — 학습에서 통하던 방식이 여기서 뒤집히는 지점 / 연속 배칭의 요청 64개를 16개씩 네 묶음으로 나눠 마이크로배치로 쓰는 방식 / 그래서 동시 요청이 적으면 이득이 거의 없다는 실무 결론
  - 절「지연에서 치르는 대가」 — 소절: 텐서 병렬은 층 하나의 계산을 나눠 요청 하나를 빠르게 하고, 파이프라인은 여전히 모든 층을 순서대로 지나므로 빨라지지 않는다 / 스테이지 경계를 넘는 시간이 붙어 오히려 조금 느려지는 자리 / 파이프라인이 벌어 주는 것은 담을 수 있는 모델 크기와 총 처리량이라는 정리
  - 절「스테이지 균형 맞추기」 — 소절: 가장 느린 스테이지가 전체 속도를 정하는 컨베이어 성질 / 층 수를 똑같이 나눠도 부하가 다른 세 자리 — 임베딩을 함께 든 첫 스테이지, 어휘 12만짜리 출력 층을 든 마지막, 나눠떨어지지 않는 나머지 / 스테이지별 처리 시간을 실측해 층을 한둘 옮기는 절차
  - 절「텐서 병렬과 섞기」 — 소절: 연결이 빠른 경계일수록 통신이 잦은 방식을 둔다는 규칙 하나 — 서버 안은 텐서, 서버 사이는 파이프라인 / 8장 서버 두 대에서 TP 8 × PP 2 배치와 그때 `--max-num-seqs`를 넉넉히 두는 이유 / 서버 사이에 텐서 병렬을 걸면 되기는 하지만 통신에 잡아먹혀 카드를 늘린 보람이 없는 이유
  - 절「나누기 전에 볼 것」 — 소절: 양자화로 파라미터당 바이트를 절반으로 줄이면 아예 안 나눠도 되는 경우가 많다 / 모델이 한 장에 들어가면 카드마다 독립 인스턴스가 통신 0이라 거의 항상 낫다 / 동시성 1로 부하 시험을 해 이용률 25% 그림을 보고 「파이프라인은 느리다」로 결론 내리는 흔한 오판

- `finetuning-catastrophic-forgetting` (지금 3,151자 · 절 7)
  - 절「손실에 없는 항」 — 소절: $$\theta_{t+1} = \theta_t - \eta\nabla\mathcal{L}_{\text{new}}$$ 에 예전 능력에 대한 항이 아예 없다는 사실부터 / 「분류 담당 파라미터」와 「요약 담당 파라미터」가 따로 없다는 구조 / 「망각」이 삭제가 아니라 경로 위 값의 이동이라는 근거 — 예전 데이터를 조금만 다시 보여 줘도 빠르게 회복한다
  - 절「보존 세트 만들기」 — 소절: 일반 능력 · 형식과 지시 이행 · 안전 정렬 세 갈래로 100~300문제를 채우는 법 / 안전 축이 가장 자주 빠지고 가장 늦게 발견되는 이유 — 정렬은 사후 학습으로 얻은 성질이라 무관한 도메인 학습에도 흔들린다 / 목표 곡선과 보존 곡선을 겹쳐 그리면 「둘의 합이 최대인 자리」가 목표 최고점보다 앞에 온다는 결론
  - 절「건드리는 범위」 — 소절: LoRA가 망각에 강한 이유 — 베이스가 얼어 있어 크게 배울 수 없는 대신 크게 잊을 수도 없다 / 랭크 128에 MLP까지 붙이면 보호막이 얇아져 전체 미세조정 쪽 성질이 되는 지점 / 「LoRA를 썼으니 망각은 없다」가 아니라 「랭크가 낮은 LoRA는 망각이 적다」가 맞는 문장인 이유
  - 절「리허설 비율 잡기」 — 소절: 배치의 10~30%를 예전 분포로 고정하는 계산과 5%·50%에서 각각 벌어지는 일 / 섞을 데이터를 구하는 세 길 — 공개 지시 데이터셋, 베이스 모델로 직접 만들기, 출력 분포 자체 붙잡기 / 「미세조정 전 모델처럼 답하라」를 데이터로 적는 쪽이 보존 목적에 가장 정확한 이유
  - 절「KL 항으로 묶기」 — 소절: 손실에 $$\lambda\,\mathrm{KL}(p_{\theta_0}\|p_\theta)$$ 를 더해 출력 분포를 붙잡는 방식 / $$\lambda$$ 가 고삐의 세기이고, 선호 학습이 참조 모델을 옆에 두는 것과 같은 구조라는 연결 / 학습 중 모델을 두 벌 들어야 하는 메모리·시간 비용과 그것을 감당할지 정하는 기준
  - 절「학습률·에폭·데이터 폭」 — 소절: 전체 미세조정 1e-5, LoRA 1e-4 언저리에서 시작하는 이유 — 큰 걸음이 원래 값에서 멀리 데려간다 / 소규모 데이터에 3에폭을 넘기면 목표는 거의 안 오르고 보존만 내려가는 구간 / 데이터가 좁을수록 심한 이유 — 한 형식 짧은 응답 2,000건이 「나는 항상 이렇게 답하는 존재」를 가르치고, 길이·형식을 섞어 두면 이 쏠림이 줄어든다
  - 절「순서대로 시도하기」 — 소절: 보존 세트 → 낮은 랭크 LoRA → 학습률·에폭 → 리허설 15% 순서와 각 단계에서 멈춰도 되는 조건 / 여기까지 안 되면 그 과제가 검색이나 프롬프트로 갈 자리였는지 되돌아보기 / 안전 축을 회귀 테스트의 상시 항목으로 남겨 다음 학습에서 다시 재게 만들기

- `serving-continuous-batching` (지금 3,154자 · 절 8)
  - 절「묶어서 도는 이유」 — 소절: 토큰 하나를 만드는 데 가중치 전체를 읽어야 한다 — 계산이 아니라 대역폭에 막힌 구조 / 한 번 읽는 김에 여러 요청의 토큰을 같이 만들면 읽는 비용은 그대로인데 뽑는 양이 배가 되는 관계 / 자리를 계속 갈아 끼우려면 메모리를 유연하게 빌리고 돌려줄 수 있어야 한다는 짝 관계
  - 절「고정 배치의 빈자리」 — 소절: 답 길이가 제각각이라 두 걸음에 끝난 요청의 자리가 비어 있는 채로 여덟 걸음을 함께 도는 그림 / 낭비가 두 겹 — GPU 시간과, 대기 중인 요청이 첫 글자를 못 받는 시간 / 짧은 요청에 패딩을 채워 계산을 버리던 옛 구현과 가변 길이를 그대로 이어 붙이는 지금의 커널
  - 절「걸음마다 다시 짜기」 — 소절: 토큰 한 번마다 끝난 요청을 빼고 대기 요청을 메모리가 허락하는 만큼 넣는 판단 / 414번째 토큰을 만드는 요청과 첫 토큰을 만드는 요청이 한 배치에 있을 수 있는 이유 — 디코드에서 각자 넣는 토큰이 하나뿐이다 / 각자 다른 길이의 캐시를 블록 테이블이 이어 준다는 연결
  - 절「프리필의 끼어듦」 — 소절: 프리필과 디코드의 한 번에 처리하는 토큰 수 · 병목 · 걸리는 시간 대조 / 4,000토큰 프리필 동안 디코드 중인 요청 전부가 멈춰 스트리밍이 반 초 끊기는 체감 / 청크 프리필 — 512토큰씩 잘라 디코드 사이에 섞으면 끊김이 줄고 첫 토큰이 늦어지며 이용률은 오히려 오르는 세 결과 / `--max-num-batched-tokens`를 줄일 부하(대화형)와 키울 부하(야간 일괄)
  - 절「스케줄러의 세 판단」 — 소절: 빈 블록이 있는가 · 동시 요청 상한에 걸리는가 · 이번 걸음의 토큰 예산이 남았는가 / 프리필 조각과 디코드 토큰이 같은 예산을 나눠 쓰는 관계 / 블록이 떨어질 때의 선점 — 재프리필과 CPU로 옮겨 둔 캐시 되돌리기, 로그에 자주 찍히면 용량 문제라는 신호
  - 절「세 지표 함께 보기」 — 소절: TTFT·ITL·초당 총 출력 토큰이 각각 무엇에 반응하는가 / 사용자가 화면 앞에 있으면 앞의 둘에 상한을 걸고 그 안에서 셋째를 최대로 / 평균 지연은 멀쩡한데 99백분위가 무너지는 전형적 증상과 그 원인(프리필에 밀린 요청이 꼬리에 몰린다) / 평균 500토큰인데 가끔 30,000토큰이 섞이는 입력 분포의 처리
  - 절「부하를 나누는 판단」 — 소절: 대화형과 일괄 처리를 같은 엔진에 섞으면 긴 프리필이 대화형 흐름을 계속 끊는 이유 / `--max-num-seqs`를 무한대로 두었을 때 처리량 그래프와 실제 응답 속도의 어긋남, 요구사항에서 역산해 상한을 거는 법 / 동시성 1로 부하 시험을 하면 아무것도 측정하지 못한 것이라는 결론

- `eval-rag-metrics-deep` (지금 3,166자 · 절 6)
  - 절「구간이 둘인 시스템」 — 소절: 검색과 생성이 직렬로 붙어 하나만 고장 나도 답이 틀리는 구조 / 두 구간이 쓰는 지표·필요한 라벨·나빠졌을 때 손대는 자리가 각각 다르다 / 한 점수로 합치는 순간 그 구별이 사라진다는 결론
  - 절「검색 지표 네 개」 — 소절: Recall@k가 RAG의 천장인 이유 — 0.6이면 질문의 40%는 근거 자체가 프롬프트에 없고 생성이 할 수 있는 최선은 「모른다」다 / Precision@k와 $$k$$ 의 저울질, 그리고 관련 없는 문서가 토큰을 쓰고 모델을 헷갈리게 하는 두 번째 손해 / MRR은 첫 정답의 순위만 본다 — 정답이 사실상 하나인 질문에 맞고 근거를 여럿 모으는 질문에 안 맞는 이유 / nDCG@k는 관련도를 등급으로 매겼을 때만 이득이 생긴다
  - 절「정답 문서 라벨」 — 소절: 풀링 — 여러 검색 방식의 상위 20개 합집합만 라벨링하고 그 밖을 포기하는 거래 / 역방향 생성 — 라벨이 저절로 붙는 대신 질문이 문서 표현을 그대로 따라 해 검색이 쉬워지는 함정과 사람이 다시 쓰는 단계 / 운영 로그 — 가장 정확하고 가장 느리다 / 어느 방법으로 만든 라벨인지 질문마다 기록해 두면 「우리 지표가 실제보다 얼마나 낙관적인가」가 숫자로 나온다
  - 절「생성 구간 지표」 — 소절: Faithfulness는 답을 주장 단위로 쪼개고 근거로 뒷받침되는 비율을 낸다 — 통째로 판정하면 다섯 중 하나 지어낸 답이 「대체로 맞음」으로 넘어간다 / 쪼개기를 모델에게 시키면 그 단계도 채점 대상이라는 점 / Answer relevance — 근거에는 충실하지만 질문에 답하지 않는 요약형 실패 / Context precision·recall이 이름과 달리 정답 문서 라벨 없이 재는 값이라는 구별
  - 절「두 구간을 맞댄 표」 — 소절: 검색 낮음·생성 높음 → 청킹 크기·임베딩 모델·$$k$$·질의 재작성 / 높음·낮음 → 프롬프트·생성 모델·근거 인용 강제 / 둘 다 낮음 → 데이터 자체, 답할 수 없는 질문을 일부러 섞고 「모른다」로 답하는지 따로 세기 / 엉뚱한 문서를 충실히 요약하면 Faithfulness가 높게 나오는 오른쪽 위 칸 — 근거 충실도만 보면 안 보인다
  - 절「운영에 붙이는 지표」 — 소절: 뒤 구간 지표는 라벨이 없어 실 트래픽에 그대로 붙는다 / 표본을 뽑아 매일 재는 구성과 그 비용(채점 호출 수와 지연) / 값이 흔들릴 때 모델 변화인지 트래픽 변화인지 가르는 법 — 같은 고정 세트를 나란히 돌려 둔다
  - 절「재기 전에 고정할 것」 — 소절: $$k$$ 를 하나로 고정하고 나머지는 참고로 두기 — Recall@5와 Recall@10을 섞어 보고하면 개선인지 $$k$$ 를 키운 것인지 모른다 / 관련도 등급 수를 바꾸면 nDCG가 통째로 움직인다 / 재순위 전후를 나눠 재기 — 1차 검색의 Recall이 상한이고 재순위는 없는 문서를 만들지 못한다 / 채점 모델에 버전 붙이기

- `finetuning-rlvr` (지금 3,175자 · 절 7)
  - 절「코드가 매기는 보상」 — 소절: 이름에 알고리즘이 아니라 보상의 출처가 들어간 구성 — GRPO여도 PPO여도 된다 / 학습된 보상 모델의 빈틈을 정책이 몇천 스텝 안에 찾아내는 보상 해킹 / 검증기에는 그 빈틈이 적어 보상이 무너지기 전까지 훨씬 오래 돌릴 수 있다는 교환
  - 절「검증 가능한 과제」 — 소절: 수학 정답 · 코드 · 형식 준수 · 구조화 추출 · 도구 호출 각각의 채점 방법과 조심할 곳 / 번역·요약이 경계 밖인 이유와 그때 DPO·보상 모델로 넘기는 판단 / 과제 전체가 아니라 「과제가 만족해야 할 제약」을 검증기로 옮기는 방식과 그 한계 — 제약을 다 지키면서 형편없는 답이 만점을 받는다
  - 절「추출과 정규화」 — 소절: `answer in completion` 한 줄이 두 방향으로 다 틀리는 예 — 정답 2에 12가 통과하고, 분수로 답하면 떨어진다 / 추출 — `<answer>`나 `\boxed{}`를 프롬프트에서 요구하고 없으면 마지막 줄 / 정규화 — 공백·쉼표·단위 제거, 분수와 소수 통일, 수식은 기호 계산으로 동치 판정 / 비교 — 정수는 그대로, 실수는 허용 오차
  - 절「거짓 음성이 비싼 이유」 — 소절: 맞은 답을 틀렸다고 하면 잡음이 아니라 잘못된 방향의 신호가 된다 / 학습이 끝난 모델이 검증기가 알아보는 표기법으로만 경직되게 답하는 증상의 원인 / 0점 받은 답을 주기적으로 파일에 남겨 사람이 읽는 습관 / 싼 문자열 비교를 먼저 하고 실패한 것만 기호 계산으로 넘기는 이유 — 무리 8 × 배치 64면 스텝당 512회 채점이고 한 번이 100ms면 학습 속도가 절반이 된다
  - 절「코드 채점의 격리」 — 소절: 타임아웃과 자원 상한 — 무한 루프를 도는 답이 반드시 나오고, 상한이 없으면 답 하나가 학습 노드를 멈춘다 / 네트워크 차단이 속도이자 정답 유출 차단인 두 겹 / 테스트 파일을 답과 같은 디렉터리에 두지 않기 — 정책은 테스트를 통과시키는 가장 짧은 길을 찾고, 테스트를 지우는 것이 그 길일 때가 있다 / 격리는 보안 조치처럼 보이지만 실제로는 보상의 정확도를 지키는 조치라는 관점
  - 절「0과 1 사이 채우기」 — 소절: 무리 안의 답이 전부 0점이거나 전부 1점이면 학습 신호가 없다는 구조 / 부분 점수의 판단 기준 하나 — 그 점수가 오르는 것이 과제를 더 잘 푸는 것과 같은 뜻인가 / 테스트 통과 비율은 넣고, 길이·풀이 단계 가점은 넣지 않으며, 형식 점수 0.1은 초반에만 두고 빼는 이유 / 프롬프트별 통과율을 기록해 0이나 1로 굳은 것을 빼고 중간에 있는 것을 채워 넣는 데이터 운영
  - 절「밖으로 새는 이득」 — 소절: 수학·코드만 학습했는데 다른 과제도 좋아지는 관찰과 그 해석 — 길게 생각하고 스스로 검토하는 습관은 과제를 가리지 않는다 / 답이 길어지는 대가 — 짧게 답해야 할 질문에서의 비용과 품질 저하 / 학습에 없던 능력을 전후로 재 두기 / 검증 가능한 쪽은 RLVR, 아닌 쪽은 선호 데이터로 섞는 실무 구성

- `gpu-memory-tuning` (지금 3,216자 · 절 9)
  - 절「VRAM을 채우는 다섯」 — 소절: 파라미터 · 기울기 · 옵티마이저 상태 · 활성값 · 배치 데이터가 각각 어떤 크기로 잡히는가 / 7B를 FP32로 올리면 28 + 28 + 56 = 112GB, A100 80GB 한 장으로도 모자란 산수 / 앞의 셋은 파라미터 수만으로 정해지고 활성값만 배치·시퀀스 길이에 비례한다는 구별 / 혼합 정밀도 기준 파라미터당 약 16바이트라는 어림식과 그 안에 무엇이 들어 있는지
  - 절「메모리 프로파일링」 — 소절: `memory_allocated`와 `memory_reserved`가 갈리는 이유와 둘의 차이가 뜻하는 것 / `reset_peak_memory_stats`로 피크만 뽑아 순전파·손실·역전파 각 단계에 찍기 / 프로파일러로 연산 단위 타임라인을 보고 어느 층이 활성값을 쥐고 있는지 찾기 / 최적화를 한 번에 하나씩 적용하고 다시 재는 순서가 왜 규칙인가
  - 절「활성값 재계산」 — 소절: 중간 활성값을 저장하지 않고 역전파 때 다시 계산하는 거래 — 메모리는 $$O(\sqrt{N})$$, 연산은 20~30% 증가 / 세그먼트 수를 정하는 감각과 층 단위로 감싸는 자리 / 재계산 구간 안의 드롭아웃·난수가 두 번 도는 문제와 그것을 다루는 옵션 / 배치를 못 늘리던 자리가 이 하나로 풀리는 경우와 안 풀리는 경우
  - 절「FP16과 BF16」 — 소절: 절반이 되는 것(파라미터·기울기)과 그대로 남는 것(FP32 옵티마이저 상태)의 구별 / FP16의 언더플로와 GradScaler가 하는 일 — 스케일업 → 역전파 → 되돌리기 / BF16은 지수 범위가 FP32와 같아 스케일러가 필요 없다는 차이 / 기울기 클리핑을 하려면 `unscale_`이 먼저라는 순서
  - 절「가상 배치 만들기」 — 소절: micro_batch × accumulation_steps = 유효 배치라는 관계와 32×8로 256을 만드는 예 / 손실을 누적 스텝 수로 나누지 않으면 기울기 크기가 달라지는 자리 / 스케줄러를 옵티마이저 스텝에 맞춰 부르기 / 배치를 줄였는데 메모리가 안 줄면 병목이 활성값이 아니라 다른 항목이라는 진단
  - 절「샤딩과 오프로딩」 — 소절: ZeRO 1·2·3이 각각 무엇을 GPU 사이에 나누는가와 FSDP가 대응하는 자리 / CPU 오프로드가 VRAM을 크게 줄이는 대신 PCIe 왕복으로 학습 속도를 30~60% 깎는 거래 / 데이터 병렬·텐서 병렬·파이프라인 병렬·파라미터 샤딩을 각각 언제 고르는가
  - 절「OOM 대응 순서」 — 소절: BF16 → 체크포인팅 → 배치 축소와 누적 → FSDP·ZeRO-3 → CPU 오프로드 순서와 각 단계에서 멈춰도 되는 조건 / `empty_cache`가 실제로 해 주는 것과 안 해 주는 것 / 총량은 남는데 못 잡는 단편화형 OOM을 알아보는 법과 그때의 대응

- `pruning-structured` (지금 3,224자 · 절 6)
  - 절「희소도와 실제 속도」 — 소절: GPU가 정해진 크기의 타일로 값을 읽어 곱셈-누산을 도는 방식 — 0이 몇 개 섞이든 읽는 양도 연산 횟수도 같다 / 비구조적 가지치기가 같은 희소도에서 정확도를 가장 잘 지키는 대신 행렬 모양이 8×8 그대로 남는 이유 / 구조적 가지치기는 희소 행렬이 아니라 더 작은 조밀 행렬을 만든다는 차이 / 반구조적 절충안이 요구하는 하드웨어·커널 조합과 학습 쪽 제약
  - 절「덩어리의 네 층위」 — 소절: 채널·뉴런, 어텐션 헤드, FFN 중간 차원, 층 전체 네 층위의 이득과 손실을 나란히 대조 / 트랜스포머에서 FFN 중간 차원이 손대기 좋은 이유 — 파라미터 비중이 크고 앞뒤 모양만 맞추면 된다 / 헤드를 지울 때 층별 최소 개수를 두는 이유 / 층 전체 제거는 효과도 위험도 가장 크고, 앞쪽 몇 층과 마지막 층은 대체로 못 건드린다
  - 절「중요도 점수 세 가지」 — 소절: 절댓값 노름 — 거의 공짜지만 뒤에서 크게 증폭되는 경로가 있으면 틀린다 / 활성화 기반 — 보정 데이터를 흘려 그 채널의 출력이 얼마나 움직이는지 본다 / 손실 변화 근사 — 기울기로 「지우면 손실이 얼마나 오를까」를 추정, 가장 정확하고 가장 비싸다 / 보정 데이터의 크기와 분포가 결과를 바꾸는 자리
  - 절「층 안과 층 사이」 — 소절: 층별로 같은 비율씩 지우기 — 균형은 유지되지만 여유 없는 층에서도 억지로 덜어 낸다 / 전체에서 하위 N% 지우기 — 여유 있는 층에서 많이 가져오지만 어떤 층이 통째로 비어 모델이 끊긴다 / 층별 하한을 함께 거는 이유와 그 값을 정하는 감각 / 층마다 점수 스케일이 달라 정규화 없이 전체 비교하면 스케일 큰 층만 살아남는다
  - 절「지우고 회복하기」 — 소절: 목표까지 한 번에 가지 않고 조금 지우고 재학습하기를 반복하는 형태 / `ratio`를 3제곱으로 올려 초반에 크게, 후반에 천천히 접근하는 이유 — 처음 몇 %는 거의 손실 없이 사라지고 마지막 몇 %가 어렵다 / 매 단계 평가하고 바닥값을 넘으면 멈추기 — 목표 희소도가 아니라 허용 손실을 기준으로 삼는다 / 회복 학습률을 처음 학습보다 훨씬 작게 잡는 근거
  - 절「양자화·증류와의 순서」 — 소절: 가지치기 → 회복·증류 → 양자화 순서와 그 이유 — 보정이 최종 구조 위에서 이뤄져야 한다 / 양자화를 먼저 하면 보정 통계를 다시 잡아야 하고 두 손실이 겹쳐 원인 분리가 안 되는 자리 / 회복 단계에 증류를 넣어 원래 모델을 교사로 쓰는 구성
  - 절「가지치기가 지는 자리」 — 소절: 같은 크기의 모델이 이미 공개돼 있으면 잘라 만든 것보다 대개 낫다는 관찰 / 가지치기가 이기는 자리 — 내 데이터로 파인튜닝한 모델을 배포 제약에 맞춰야 하는데 그 크기의 대체품이 없을 때 / 줄인 모델을 실제 서빙 스택에 올려 지연과 처리량을 다시 재는 마지막 확인

- `mlops-model-registry` (지금 3,225자 · 절 6)
  - 절「없을 때 생기는 일」 — 소절: `model_final_v2_use_this.pkl`이 쌓이고 「지금 서비스 중인 것이 무엇인가」에 아무도 확신 못 하는 상태 / 롤백 불가 · 책임 추적 불가 · 무단 배포 세 증상이 같은 원인에서 나온다는 정리 / Git이 코드에 해 주는 것과 레지스트리가 모델에 해 주는 것의 대응, 그리고 대응되지 않는 것 — 가중치는 diff가 안 되고 리뷰도 안 된다
  - 절「등록에 함께 담는 것」 — 소절: 버전 · 실험 ID · 데이터셋 버전 · 메트릭 · 아티팩트 · 감사 로그 여섯 / 학습 데이터 커밋과 코드 커밋을 같이 적어야 재현이 되는 이유 / 태그와 설명이 반년 뒤 「왜 이걸 배포했나」에 답하는 방식 / 이름 규칙을 팀-용도-알고리즘으로 굳혀 두면 레지스트리가 커져도 찾을 수 있는 이유
  - 절「네 상태의 뜻」 — 소절: None · Staging · Production · Archived가 각각 무엇을 보장하는 상태인가 / Staging에서 실제로 돌리는 넷 — 입출력 스키마, 서빙 파이프라인 통합, 챔피언 대비 성능·지연·메모리, 소량 트래픽 / Production에서 지속적으로 보는 것과 SLA / Archived를 지우지 않는 두 이유 — 감사 재현과 비상 롤백
  - 절「자동 승격 파이프라인」 — 소절: 검증 스크립트가 하는 일 — 모델 로드, 입력 스키마, 반복 호출로 잰 p99, 그리고 통과 시 상태 전환 / 성능 기준을 문서나 메신저가 아니라 코드로 강제하면 사라지는 논쟁 / 이전 Production을 자동으로 Archived로 보내 Production을 항상 하나로 유지하기 / 그래도 사람 승인을 한 단계 남기는 이유
  - 절「서빙이 참조하는 방식」 — 소절: 버전 번호를 하드코딩하지 않고 스테이지 별칭으로 부르기 / 별칭이 바뀌는 순간 이미 떠 있는 프로세스가 무엇을 보는가 — 재적재 시점을 정해 두지 않으면 인스턴스마다 다른 모델이 돈다 / 별칭 하나에만 의존하면 되돌리기가 곧 전체 전환이 되는 위험과 카나리 비율의 자리
  - 절「챔피언과 챌린저」 — 소절: 섀도 모드 — 챌린저 예측을 로깅만 하고 사용자 응답에는 쓰지 않는 구조 / 판정에 필요한 표본이 쌓이는 속도와, 무엇으로 판정할지를 시작 전에 적어 두기 / 앙상블에서 모델마다 독립 버전 관리가 주는 이득 — 하나만 갱신해도 나머지가 안 흔들린다 / 챌린저가 자원을 두 배로 먹는다는 비용과 비율을 정하는 기준
  - 절「운영에서 굳히는 것」 — 소절: 롤백 절차를 배포 전에 문서와 스크립트로 준비해 두기 / 오래된 모델을 지우는 대신 콜드 스토리지로 옮기는 보관 정책 / 레지스트리에 없는 모델이 서빙에 올라가지 못하게 막는 마지막 관문을 어디에 두는가

- `finetuning-continual-learning` (지금 3,239자 · 절 6)
  - 절「이어 붙이기와 다시 만들기」 — 소절: 학습 비용 · 소요 시간 · 재현성 · 망각 · 실수의 수명 다섯 축 대조 / 회차마다 「허용 범위 안」이던 하락이 여섯 번 쌓이면 안 괜찮아지는 산수 / 3회차 라벨 오류를 4·5·6회차가 그대로 물려받는 구조와 되돌리는 비용 / 기본값을 다시 만들기로 두고 여섯 회차마다 한 번 전체 재학습으로 청산하는 절충
  - 절「데이터를 쌓는 세 방식」 — 소절: 전체 누적 · 슬라이딩 창 · 누적 + 표본이 각각 맞는 자리 / 최근 3개월 전부 + 그 이전 10~20% 표본이 무난한 기본값인 이유 / 「최근에 안 나온 유형」과 「잘 처리해서 신고가 안 올라온 유형」을 가르는 법, 회귀 세트가 그 구별을 대신해 주는 방식 / 회차 간 중복을 문서 해시·사례 ID로 걷어내지 않으면 같은 사례가 두 번 학습된다
  - 절「데이터가 들어오는 길」 — 소절: 운영 로그에서 학습 후보를 뽑는 규칙 — 낮은 만족도, 재질문, 사람이 개입한 대화 / 개인정보와 데이터 사용 동의가 파이프라인의 어느 지점에서 걸리는가 / 라벨을 붙이는 사람과 기준 문서를 회차마다 같게 유지하지 않으면 회차 간 비교가 흔들리는 이유
  - 절「고리 한 바퀴의 시간」 — 소절: 한 바퀴가 두 주면 늘 두 주 전 세상에 맞춰진 모델을 쓰고 있다는 뜻과 도메인별 허용치 / 병목이 학습 40분이 아니라 라벨 검수 5일인 자리 — GPU를 두 배로 늘려도 고리는 거의 그대로다 / 자동화로 검수 시간을 줄이는 것과 검수를 건너뛰는 것의 차이, 그리고 지속 학습에서 후자가 손해인 이유 / 사람이 남는 두 자리 — 새 학습 데이터 표본 검수와 배포 직전 최종 판단
  - 절「고정 세트와 현행 세트」 — 소절: 골든셋이 만든 시점의 분포를 반영하는데 그 분포가 변하는 것이 지속 학습의 이유라는 모순 / 매번 갈아 끼우면 3회차 82점과 6회차 85점이 다른 시험지가 되어 3점이 뜻을 잃는 문제 / 두 세트를 나란히 보면 분포 이동과 과적합이 갈리는 해석 — 「고정 유지·현행 낮음」은 분포가 옮겨 간 것이고 「현행 좋음·고정 하락」은 새 데이터에 과적합된 것이다 / 매 회차 몇 문제를 더하되 빼지 않는 운영과 두 해쯤 뒤 한 번 정리하는 시점
  - 절「회차마다 남기는 기록」 — 소절: 데이터 커밋 · 베이스 리비전 · 학습 설정 · 두 세트 점수를 한 줄로 적는 회차 대장 / 그 대장이 있어야 「몇 회차부터 나빠졌나」에 답할 수 있는 이유 / 되돌릴 때 어느 회차 모델로 갈지 미리 정해 두기
  - 절「지속 학습을 미룰 자리」 — 소절: 바뀌는 것이 지식뿐이면 검색 문제다 — 갱신 주기가 며칠인 사실을 가중치에 굽는 것이 최악인 이유 / 회차마다 수백 건뿐이면 학습으로 움직이기에 적고 프롬프트·예시가 빠르다 / 평가가 자동화되기 전에는 고리가 돌지 않는다 — 순서가 평가 자동화 먼저인 이유

- `model-selection-strategy` (지금 3,245자 · 절 7)
  - 절「자리의 요구 다섯」 — 소절: 「우리는 어떤 모델을 쓸까」가 답할 수 없는 질문인 이유 — 자리마다 요구가 다르다 / 적을 다섯 — 입출력 모양, 실패했을 때 벌어지는 일, 하루 물량과 몰림, 기다려 줄 수 있는 시간, 데이터가 어디까지 나가도 되는가 / 「실패하면 잘못된 값이 결제 시스템으로 간다」 한 줄이 후보를 절반으로 줄이는 방식
  - 절「하드 제약으로 자르기」 — 소절: 여덟 제약 — 데이터 위치·규정, 실효 컨텍스트, 모달리티, 구조화 출력, 도구 호출, 배포 형태, 라이선스, 가용성 / 최대 컨텍스트와 실효 컨텍스트를 나눠 보는 이유 — 최대치는 큰데 뒤쪽을 놓치는 경우 / 폐기 예고 기간이 1년짜리 기능에서 품질 몇 퍼센트보다 큰 문제가 되는 자리 / 이 단계는 모델을 한 번도 호출하지 않고 문서만 읽어 끝난다
  - 절「골든셋의 구성」 — 소절: 평범한 요청 절반, 어려운 요청, 과거에 틀렸던 요청, 안 해야 하는 요청 네 갈래 / 100건에서 시작하고 300건이면 대부분의 자리에서 충분한 이유, 개수보다 구성이 중요하다는 점 / 공개 리더보드가 재는 것과 우리가 알고 싶은 것의 어긋남 / 사고가 났던 입력을 모아 둔 부분이 가장 값이 나가는 이유
  - 절「공평하게 재기」 — 소절: 지금 모델에 맞춰 오래 다듬어진 프롬프트를 그대로 옮기면 새 모델이 제 실력을 못 낸다 / 후보마다 같은 손질 예산(시간이나 수정 횟수)을 정해 두는 절차 — 정하지 않으면 손질 시간이 곧 편애가 된다 / 채점 자동화 — 문자열 비교, 스키마 검사, 기준을 적은 판정 모델 / 판정 모델을 쓴다면 사람이 채점한 30건과의 일치를 한 번은 확인해 두기
  - 절「비용과 지연의 산수」 — 소절: 입력 단가만 보는 실수와 $$C = (n_{in}/10^6)p_{in} + (n_{out}/10^6)p_{out}$$ / 추론 모델은 보이는 답이 짧아도 $$n_{out}$$ 이 몇 배로 뛴다 — 응답의 사용량 필드를 골든셋 실행 때 그대로 기록해 두기 / 캐시 적중 단가를 따로 넣으면 총액 순위가 바뀌는 자리 / 지연은 평균이 아니라 p95, 스트리밍이면 첫 토큰까지를 따로 재기
  - 절「상자 안에서 고르기」 — 소절: 품질 하한과 비용 상한을 점 찍기 전에 정해 두는 이유 — 나중에 정하면 마음에 드는 점이 들어오도록 선을 옮기게 된다 / 상자 안의 점 중 가장 싼 것을 고르는 규칙 / 상자가 비어 있는 것도 결과다 — 후보를 늘리는 대신 작업을 쪼개거나 문맥을 줄이거나 파인튜닝을 볼 신호
  - 절「결정과 재평가 조건」 — 소절: 자리 · 결정 · 탈락 사유 · 근거 수치를 파일 한 장으로 저장소에 남기기 / 재평가 트리거를 미리 적어 두면 새 모델 출시가 회의의 시작 신호가 되지 않는 이유 / 자주 보는 오판 넷 — 인상적인 실패 한 건으로 교체, 최신 모델 반사 채택, 모든 자리를 한 모델로 통일, 표본 크기 없이 0.90과 0.93을 구별하기

- `finetuning-data-prep` (지금 3,300자 · 절 8)
  - 절「형식 세 가지」 — 소절: Completion — 도메인 적응에 맞고 지시 이행을 못 가르치는 이유 / Instruction(Alpaca) 세 필드와 하나의 템플릿으로 잇는 자리, `input`이 비는 경우 / Chat·ChatML — 멀티턴과 역할 구분이 필요한 자리 / 한 데이터셋에 형식을 섞으면 모델이 추론 때 어느 형식으로 답할지 못 정하는 결과
  - 절「손실 마스킹」 — 소절: 응답 부분에만 손실을 거는 이유 — 안 하면 프롬프트를 외우는 학습이 된다 / 멀티턴에서 어시스턴트 턴만 골라 마스킹할 때 자주 틀리는 경계 / `apply_chat_template`의 `add_generation_prompt`를 학습에서 False, 추론에서 True로 두는 이유 / 마스킹이 제대로 걸렸는지 한 건을 토큰 단위로 찍어 확인하는 절차
  - 절「과제별 최소 데이터 양」 — 소절: 분류 100~500, 생성·요약 500~2,000, 복잡 추론·코드 2,000~10,000, 도메인 적응 10,000+ 네 기준과 각각의 근거 / 데이터가 적을수록 에폭을 늘리고 학습률을 낮추는 방향과 그때 생기는 과적합 위험 / Alpaca 52,000건이 보여 준 것 — 수백만 저품질보다 나은 이유 / 공개 한국어 데이터셋으로 양을 메울 때 확인할 것 — 라이선스와, 번역투가 그대로 학습에 옮는 문제
  - 절「거르는 다섯 단계」 — 소절: 중복 제거 — 정확 일치와 MinHash 퍼지 중복, 임계값 0.85가 뜻하는 것 / 길이 필터 — 의미 없는 단답과 컨텍스트 초과를 양끝에서 자르기 / 언어 일관성과 독성·개인정보 — 자동화가 안 되는 부분과 표본 검토 / 퍼플렉시티 양끝 자르기 — 너무 낮은 것(이미 아는 내용)과 너무 높은 것(잡음)을 함께 버리는 이유
  - 절「합성 데이터의 자리」 — 소절: 시드 지시문에서 변주를 만드는 방식과, 시드가 적으면 다양성이 무너지는 지점 / 만든 데이터에도 할루시네이션이 들어오므로 사람 검토가 조건이라는 점 / 합성 데이터가 평가셋과 겹치지 않게 격리하는 절차
  - 절「나누고 검사하기」 — 소절: 80:20 분할에서 무작위로 나누면 안 되는 자리 — 같은 원본·같은 고객의 사례가 양쪽에 들어간다 / 태스크 유형별 분포를 맞추는 층화 분할 / 검증셋이 과적합 감지와 조기 종료에서 실제로 하는 일
  - 절「학습 전 최종 점검」 — 소절: 형식 일관성 · 특수 토큰 · UTF-8 · JSONL 유효성 · 손실 마스킹 · 분포 · 최소 수량 일곱 / 토크나이저에 통과시켜 길이 분포와 잘림 비율을 미리 보기 / 무작위 20건을 렌더된 프롬프트 그대로 눈으로 읽는 마지막 단계

- `finetuning-serving-adapters` (지금 3,302자 · 절 7)
  - 절「학습이 남기는 것」 — 소절: `adapter_config.json`의 `base_model_name_or_path`가 문자열 하나뿐이라는 사실 / 같은 이름이 다른 가중치를 가리켰을 때 아무 오류 없이 올라가 이상한 답을 내는 경로 / 리비전 해시까지 박아 두기 — `...@0e9e39f`가 반년 뒤 재현을 살린다 / `r`·`lora_alpha`·`target_modules`가 서빙 쪽에서도 읽히는 값이라는 점
  - 절「어댑터 레지스트리」 — 소절: 담을 여섯 — 이름, 버전, 베이스 리비전, 학습 데이터 커밋, 평가 결과, 학습 설정과 각각이 없을 때 생기는 일 / 평가 결과를 함께 넣으면 회귀를 통과하지 않은 버전을 배포 API가 거절하게 만들 수 있다는 구조 / 모델 레지스트리가 이미 있으면 얹고, 없으면 객체 저장소 위 매니페스트 한 장으로 시작하기
  - 절「적재 전략 셋」 — 소절: 미리 전부 · 지연 · LRU가 각각 맞는 어댑터 개수와 트래픽 모양, 그리고 치르는 대가 / 랭크 16짜리 8B 어댑터 34MB × 30개 = 1GB, 그 1GB가 KV 캐시에서 빠져 동시 요청 수를 깎는 계산 / 지표에는 「처리량 하락」으로만 보이므로 적재 수와 캐시 여유를 같은 대시보드에 나란히 두기 / 지연 적재의 첫 요청 수백 ms가 평균이 아니라 p99에 찍히는 이유와 야간 예열
  - 절「불변 이름과 버전 지정」 — 소절: 같은 이름으로 덮어쓰면 진행 중 요청이 중간에 다른 가중치를 만나고 되돌릴 대상이 사라진다 / 요청이 버전까지 지정해 서버 상태에 의존하지 않게 하기 / 라우팅 규칙을 추론 서버가 아니라 게이트웨이에 두는 이유 — 되돌릴 순간은 대개 재시작이 가장 하기 싫은 순간이다
  - 절「비율로 옮기기」 — 소절: 새 버전과 옛 버전이 같은 서버에 함께 있으므로 되돌리기가 재배포가 아니라 라우팅 비율 변경이 되는 성질 / 5%에서 잡히는 것(형식 붕괴·빈 응답·지연 튐)과 50%가 되어야 보이는 것(품질) / 넘어가는 조건을 시간이 아니라 지표로 적기 / 이전 버전을 며칠 남겨 두는 규칙 — 34MB를 아끼려고 되돌릴 길을 없애지 않는다
  - 절「어댑터별로 갈라 보기」 — 소절: 전체 평균이 붐비는 어댑터에 눌려 한산한 어댑터의 붕괴를 감추는 구조 / 이름·버전을 라벨로 붙일 넷 — 요청 수와 오류율, p50·p95(첫 토큰과 전체를 나눠), 적재 여부와 마지막 사용 시각, 출력 길이 분포 / 출력 길이 분포가 골든셋이 놓친 형식 붕괴를 운영 중에 잡아 주는 이유
  - 절「베이스를 갈아 끼울 때」 — 소절: 베이스를 올리면 어댑터를 전부 다시 학습해야 하는가 — 레지스트리에 재현 정보가 있어야 답할 수 있는 질문 / 옛 베이스와 새 베이스를 한동안 함께 띄우는 이행 기간의 메모리 비용 / 합쳐 내보낸 것과 얹은 것이 섞여 있을 때 버전 관리를 하나로 유지하는 법

- `inference-cpu-only` (지금 3,334자 · 절 7)
  - 절「대역폭이 정하는 천장」 — 소절: 토큰 하나에 모델 가중치 전부를 읽는다 — 8B를 4비트로 담은 4.5GB를 초당 몇 번 읽는가가 곧 상한 / DDR5 듀얼 채널 100GB/s에서 $$100 \div 4.5 \approx 22$$, 실측 12~16 / 사람이 읽는 속도 초당 5~8과 비교해 「한 명은 되고 다섯 명은 안 된다」 / 코어를 두 배로 늘려도 이 선이 안 움직이는 이유
  - 절「메모리 채널과 장비 선택」 — 소절: 8채널 서버 CPU와 듀얼 채널 데스크톱이 코어 수가 같아도 두 배 갈리는 자리 / 통합 메모리를 쓰는 장비가 CPU 추론에서 유독 잘 나오는 근거 / 장비를 사기 전에 「필요한 초당 토큰 × 모델 파일 크기 > 대역폭」인지 계산해 가능 여부를 먼저 내는 절차
  - 절「CPU에서의 양자화」 — 소절: GPU에서는 메모리를 아끼는 일이고 CPU에서는 그대로 속도가 되는 이유 / FP16 16GB → 4비트 4.5GB에서 초당 토큰이 3~4에서 12~16으로 가는 표를 정밀도별로 / 4비트가 사실상 기본값이고 3비트는 긴 추론·코드 생성에서 무너지기 시작한다 / 가중치를 매번 부동소수점으로 되돌려 곱하는 형식과 정수 명령으로 바로 곱하는 형식의 차이 — GPU에서 좋았던 양자화가 CPU에서도 좋을 것이라 가정하지 않기
  - 절「런타임 세 갈래」 — 소절: llama.cpp/GGUF · ONNX Runtime · OpenVINO의 강점과 약한 자리 / 인텔 장비면 OpenVINO, 아니면 llama.cpp로 시작해 부족할 때 옮기는 현실적 순서 / 셋 다 같은 천장 아래 있어 차이는 「천장의 몇 할까지 가느냐」라는 점 / `n_ctx`·`n_threads`·`n_batch`가 각각 무엇을 정하는가
  - 절「프리필과 디코드의 반응」 — 소절: 프리필은 프롬프트 전체를 한꺼번에 처리하는 큰 행렬 곱이라 계산 병목, 코어를 늘리면 거의 비례해 빨라진다 / 디코드는 대역폭 병목이라 코어 4개쯤에서 평평해진다 / 스레드를 물리 코어 수까지만 잡는 이유 — 두 논리 코어가 같은 물리 코어의 메모리 포트를 다툰다 / RAG처럼 컨텍스트가 긴 트래픽인지 짧은 질문에 긴 답인지가 튜닝 대상을 정한다
  - 절「동시 요청과 큐」 — 소절: CPU에서 배치가 GPU만큼 벌어 주지 못하는 이유 — 계산 여유가 애초에 없어 금방 계산 쪽이 막힌다 / 동시 4~8개에서 요청당 속도가 사람이 못 기다릴 수준으로 내려가는 지점 / 큐를 앞에 두고 동시 실행 수를 명시적으로 제한하는 구성, 그리고 대기 시간을 응답에 알려 주는 편이 나은 이유
  - 절「가동률로 보는 비용」 — 소절: 토큰당 단가로만 비교하면 CPU가 거의 항상 지는 이유 / 하루 200건이면 GPU 인스턴스가 99% 놀면서 요금을 만드는 산수, 이미 도는 앱 서버에 얹으면 추가 비용이 사실상 0인 자리 / 네 갈래 판단 — 드문드문하고 지연에 여유가 있으면 CPU, 꾸준하고 동시성이 높으면 GPU, 데이터 반출 금지면 어차피 CPU, 대역폭 초과라 코드로 해결이 안 되면 GPU

- `finetuning-on-cloud` (지금 3,362자 · 절 10)
  - 절「직접 사는 것과 빌리는 것」 — 소절: 즉시 확장 · 쓴 만큼 지불 · 관리 부담 없음 · 스토리지 연동 넷과 각각의 뒷면 / 데이터 전송 비용과 거버넌스가 클라우드를 아예 막는 자리 / 온프레미스가 유리해지는 가동률 — 하루 몇 시간을 넘겨 계속 돌리는가로 갈리는 계산
  - 절「학습 잡의 공통 모양」 — 소절: 벤더가 달라도 같은 다섯 — 컨테이너 이미지, 진입 스크립트, 하이퍼파라미터, 입력 경로, 출력 경로 / 데이터를 스토리지에서 읽고 결과를 다시 스토리지에 쓰는 구조가 재시작을 가능하게 만드는 이유 / 로컬 코드를 그대로 못 올리게 만드는 흔한 자리 — 상대 경로, 로컬 캐시, 인터넷 접근을 전제한 다운로드
  - 절「권한과 접근 경로」 — 소절: 학습 잡이 스토리지와 이미지 저장소에 접근하려면 필요한 최소 권한의 모양 / 전체 권한 정책으로 시작해 좁혀 가는 순서와, 프로덕션에 남기면 안 되는 것 / 모델 가중치 접근 토큰을 잡에 넘기는 방법과 로그에 찍히지 않게 하는 주의
  - 절「세 플랫폼의 갈림길」 — 소절: 관리형 컨테이너에서 기존 HuggingFace 코드를 얼마나 그대로 쓰는가 / TPU를 쓰려면 JAX나 PyTorch/XLA가 필요하다는 조건과 그때 얻는 것·잃는 것 / 스토리지를 파일시스템처럼 마운트해 주는 편의가 스크립트를 얼마나 덜 바꾸게 하는가 / 사내 인증·컴플라이언스 요구가 선택을 먼저 정해 버리는 자리
  - 절「중단을 전제로 한 설계」 — 소절: 스팟·프리엠티블·저우선순위가 크게 싸지는 대신 언제든 끊긴다는 계약 / `save_steps`를 50~100으로 두는 근거 — 스토리지 비용과 중단 시 잃는 시간의 저울 / `save_total_limit`과 재개 플래그가 함께 있어야 재개가 실제로 되는 이유 / 중단·재개 횟수를 로그에 세어 두면 스팟이 정말 싼지 사후에 계산할 수 있다
  - 절「분산으로 넘어가는 시점」 — 소절: 단일 GPU에 안 올라감 · 속도가 너무 느림 · 70B 이상 세 신호 / ZeRO 세 단계와 CPU 오프로드가 각각 무엇을 나누고 무엇을 대가로 내는가 / QLoRA로 필요한 VRAM을 낮춰 분산 자체를 피하는 자리 / 카드를 늘렸는데 속도가 비례하지 않을 때 먼저 볼 곳 — 데이터 적재와 카드 간 통신
  - 절「로컬 예행과 비용 예측」 — 소절: 작은 모델 · 100건 · 10스텝으로 코드가 끝까지 도는지 먼저 확인하는 이유 — 클라우드에서 발견한 오류는 그 자체가 비용 / 스토리지 연동만 따로 소규모로 시험하는 두 번째 단계 / 스텝당 시간을 재서 예상 시간과 비용을 계산하고 예산 알림을 걸어 두기 / 학습이 끝난 뒤 체크포인트를 정리하고 최종 산출물만 남기는 절차

- `serving-vllm` (지금 3,387자 · 절 7)
  - 절「GPU가 노는 두 자리」 — 소절: 가중치를 한 번 읽는 김에 여러 요청의 토큰을 뽑는 것이 이득인 구조 / 고정 묶음에서 답 길이가 제각각이라 생기는 빈자리 — 800토큰 하나가 도는 동안 30토큰에 끝난 아홉 자리 / 최대 길이만큼 캐시를 미리 잡아 동시 요청 수가 깎이는 자리 / 두 낭비가 서로 다른 자원(시간과 메모리)이라는 구별
  - 절「두 아이디어의 짝」 — 소절: 연속 배칭이 매 걸음 자리를 갈아 끼우는 방식과, PagedAttention이 그 자리를 만들어 주는 관계 — 하나만 있으면 이득이 반토막인 이유 / 지금은 거의 모든 엔진이 형태를 바꿔 쓰므로 vLLM을 고르는 이유가 아이디어가 아니라 운영 편의라는 점 / 더 깊은 설명은 각각의 글로 넘기고 여기서는 설정과 운영에 집중한다는 경계
  - 절「OpenAI 호환이 주는 것」 — 소절: 클라이언트가 주소 한 줄만 바뀐다는 이득 — 상용 API와 자체 서빙 사이를 오가는 이행 경로가 열린다 / 호환되지 않는 자리를 미리 확인하기 — 일부 파라미터, 사용량 필드, 오류 형식 / 게이트웨이를 앞에 두어 모델 이름과 실제 인스턴스를 분리해 두는 구성
  - 절「여섯 설정과 부작용」 — 소절: `--gpu-memory-utilization`은 캐시 몫이 아니라 총량이라는 오해와 0.85~0.90에서 시작해 실측하며 올리는 이유 / `--max-model-len`을 모델 최대치로 두면 요청당 예약 상한이 커져 동시 처리량이 깎인다 — 로그의 길이 분포로 정하기 / `--max-num-seqs`와 `--tensor-parallel-size`가 각각 무엇을 사고 무엇을 파는가 / `--enable-prefix-caching`이 이득인 부하(긴 시스템 프롬프트·에이전트 루프)와 관리 비용만 붙는 부하
  - 절「띄운 뒤 확인할 로그」 — 소절: 기동 로그의 KV 캐시 블록 수로 동시에 담을 수 있는 총 토큰을 역산하고 평균 요청 길이로 나눠 실제 동시 처리 수를 얻기 / 선점 횟수가 계속 찍히면 튜닝이 아니라 용량 문제라는 신호 / 대기열 길이와 실행 중 요청 수를 함께 보며 상한을 정하는 절차
  - 절「부하 시험의 조건」 — 소절: 동시성 1로 재면 연속 배칭의 이득을 하나도 못 재는 이유 / 실제 트래픽의 입력·출력 길이 분포를 흉내 낸 부하를 만드는 법 / TTFT · ITL · 초당 총 출력 토큰 셋을 함께 기록하고 앞의 둘에 상한을 먼저 정하기 / 대화형과 일괄 처리를 같은 엔진에 섞으면 둘 다 어중간해지므로 인스턴스를 나누는 판단
  - 절「vLLM이 아닌 자리」 — 소절: 동시 요청이 거의 없으면 연속 배칭이 벌어 줄 것이 없다 — 단일 사용자 환경에서는 더 가벼운 엔진 / GPU가 없거나 모델이 아주 작아 메모리가 병목이 아닐 때 / 물량이 적으면 자체 서빙 자체가 총비용에서 손해인 계산과, 그것을 엔진 고르기보다 먼저 하는 순서

- `serving-paged-attention` (지금 3,448자 · 절 8)
  - 절「캐시의 규모 감각」 — 소절: $$2 \times L \times H_{kv} \times d_h \times n \times b$$ 각 항이 무엇인지 짚고 층 32·KV 헤드 8·헤드 차원 128·fp16을 넣어 토큰당 128KB / 4,096토큰 요청 하나가 512MB, 80GB 카드에 16GB 가중치를 올리면 128개가 상한이 되는 계산 / 그 상한이 곧 동시에 받을 수 있는 요청 수라 처리량 이야기가 메모리 이야기가 되는 이유 / KV 헤드를 줄이는 구조가 이 식의 어느 항을 얼마나 깎는가
  - 절「미리 잡는 방식의 낭비」 — 소절: 답이 30토큰에서 끝날지 900토큰까지 갈지 모르니 최대 길이만큼 잡는 소박한 구현 / 내부 단편화 — 잡아 둔 자리의 4분의 3이 아무것도 없는 채로 다른 요청이 못 쓰게 묶인다 / 외부 단편화 — 총합은 충분한데 연속된 큰 덩어리가 없어 새 요청을 못 받는다 / 공유 불가 — 같은 앞부분이 메모리에 두 벌 올라간다, 셋의 뿌리가 「요청당 연속된 큰 덩어리」 하나라는 결론
  - 절「블록과 블록 테이블」 — 소절: 16토큰 고정 블록을 필요할 때마다 하나씩 빌려주는 방식 / 블록 테이블이 「이 요청의 몇 번째 블록 = 물리적으로 몇 번 블록」을 적어 흩어진 자리를 이어 주는 구조 / 운영체제 가상 메모리와의 대응 — 요청/프로세스, 16토큰 블록/페이지, 블록 테이블/페이지 테이블 / 남는 낭비가 마지막 블록의 빈칸뿐이고 요청당 최대 15토큰이라는 결과
  - 절「표를 베끼는 공유」 — 소절: 같은 시스템 프롬프트로 시작하는 요청 100개가 블록 한 벌을 가리키는 그림 / 채워지는 중인 블록에 쓸 때만 그 블록을 복사해 갈라지는 copy-on-write / 값을 하는 세 자리 — 한 입력에서 답 여럿 뽑기, 빔 서치, 에이전트의 누적 대화 / 블록 참조 수를 세어 언제 반납할지 정하는 관리
  - 절「블록 크기 고르기」 — 소절: 32·64로 키우면 표가 짧아지고 읽는 오버헤드가 줄지만 마지막 블록의 빈칸이 커져 내부 단편화가 다시 는다 / 8로 줄이면 낭비는 더 줄고 커널이 블록을 찾아다니는 비용이 는다 / 답이 수십 토큰인 분류·추출 부하와 긴 문서를 생성하는 부하에서 방향이 갈리는 이유 / 이 값을 만져 얻는 이득이 다른 설정보다 작다는 우선순위
  - 절「넘칠 때의 선점」 — 소절: 재계산 — 블록을 통째로 반납하고 차례가 오면 프리필부터 다시, 버리는 것은 계산이고 아끼는 것은 메모리 / 스왑 — CPU 메모리로 옮겼다 도로 올리며 PCIe 왕복 시간을 문다 / 짧은 요청은 재계산이, 긴 요청은 스왑이 싼 이유 / 선점이 잦으면 튜닝할 자리가 아니라 용량이 모자란 것이라는 신호와 그때의 대응
  - 절「실무에서 만지는 값」 — 소절: `--gpu-memory-utilization`은 총량이라 큰 모델일수록 같은 비율에서 실제 블록 수가 적다 / `--max-model-len`이 요청당 예약 상한처럼 작동해 스케줄러를 보수적으로 만드는 자리 / 기동 로그의 블록 수 × 블록당 토큰 ÷ 평균 요청 길이로 동시 처리 가능 요청 수를 역산하기 / KV 캐시를 8비트로 두면 식의 $$b$$ 가 절반이 되어 담을 수 있는 요청이 두 배가 되는 거래와 품질을 따로 재는 조건

- `distillation-task-specific` (지금 3,450자 · 절 7)
  - 절「증류 파이프라인의 네 칸」 — 소절: 교사 가중치가 필요 없다는 것의 뜻(API로만 열리는 상용 모델도 교사가 된다) / 시작 전에 제공자 약관을 읽어야 하는 이유 — 자사 출력으로 경쟁 모델을 학습시키는 것을 막는 조항 / 가지치기·양자화와 갈리는 지점 — 큰 모델을 줄이는 대신 작은 모델을 따로 세운다
  - 절「입력 데이터의 출처」 — 소절: 실제 서비스 트래픽이 배포 뒤 마주칠 분포를 그대로 갖는다 / 생성으로 보충할 때 실제 입력 몇십 건을 씨앗으로 주는 이유(조건 없이 만들면 교과서 문장만 나온다) / 전체를 균등하게 늘리지 않고 드문 유형만 지목해 채우기 / 중복 제거를 건너뛰면 흔한 유형에만 과하게 맞춰진다
  - 절「교사에게서 받는 신호」 — 소절: 정답 라벨·확률 분포·추론 과정·다수결 넷의 필요 조건과 효과 / 확률 분포가 교사 접근 권한과 같은 어휘 집합을 요구해 상용 API에서는 대개 못 쓰이는 자리 / 추론 과정을 받는 방식이 작은 모델에서 특히 듣는 이유 — 한 번에 정답으로 점프하지 못하는 문제를 중간 단계가 푼다
  - 절「데이터를 거르는 네 겹」 — 소절: 형식 검증이 가장 싸고 가장 많이 걸러진다(유형 목록 밖 값·깨진 JSON·비정상 길이) / 자기 일관성 — 교사가 헷갈리는 것을 학생에게 시키지 않는다 / 검증 가능한 과제는 실행·재계산으로 확인한다 / 200건 표본 검수가 오류율 3%와 25%를 가르는 자리
  - 절「학생 모델과 비교 대상」 — 소절: 출발점을 지시 학습이 끝난 체크포인트로 잡는 이유 / 데이터를 한 번 만들면 학생 크기를 바꿔 다시 돌리는 비용이 작다 / 비교는 셋이다 — 교사 대비, 같은 크기 원본 대비, 기존 운영 방식 대비 / 같은 크기 모델을 프롬프트로만 쓴 것과 비슷하면 파이프라인 전체가 헛돈 것
  - 절「손익 분기와 폴백」 — 소절: 교사 호출·학습·서빙 고정비와 호출당 절감이 만나는 지점 / 과제가 자주 바뀌면 프롬프트 한 줄로 하던 대응이 데이터 재생성과 재학습이 된다 / 학생이 자신 없어 하는 입력만 교사로 되돌려 하한을 교사 수준으로 잡기 / 형식 검증 실패를 그대로 폴백 트리거로 쓰는 구성

- `mlops-experiment-tracking` (지금 3,485자 · 절 7)
  - 절「기록이 없을 때의 비용」 — 소절: 「지난달 그 조합」을 노트북·깃 로그·슬랙에서 찾는 실제 절차 / 재현에 필요한 넷 — 데이터셋 버전, 코드 커밋, 하이퍼파라미터, 환경(라이브러리·하드웨어) / 넷 중 환경이 가장 자주 빠지고 반년 뒤에 그것 때문에 못 되살린다
  - 절「파라미터·메트릭·아티팩트」 — 소절: 학습 전에 정하는 값과 도중에 재는 값의 경계 / 메트릭을 step과 함께 남겨야 학습 곡선이 그려진다 / 시스템 메트릭(GPU 사용률·학습 시간·메모리)이 원인 규명에 쓰이는 자리 / 아티팩트에 무엇까지 넣을지 — 용량과 재현 가능성의 맞바꿈
  - 절「실험과 Run의 단위」 — 소절: 재시작·이어 학습·스윕을 각각 몇 개의 Run으로 볼 것인가 / 실패한 Run을 지우지 않는 이유와 FAILED 상태가 남기는 정보 / run_name과 태그 규칙을 처음에 정해 두기 — 모델·버전·주요 변경점
  - 절「자동 로깅의 경계」 — 소절: autolog가 잡아 주는 것(프레임워크 파라미터·메트릭)과 못 잡는 것 / 자동 로깅만 믿었을 때 빠지는 값 — 데이터셋 버전, 전처리 설정, 실행 환경 / 커밋 해시를 손으로 심어 두는 자리
  - 절「실험을 고르는 절차」 — 소절: 지표 정렬과 태그 필터로 후보를 좁히는 순서 / 챔피언·챌린저 — 교체 임계를 숫자로 미리 못 박기 / 검증셋 하나로 수백 Run 중 최고를 고르면 그 셋에 과적합된다 / 승격을 사람 리뷰에 거는 지점
  - 절「도구 셋의 갈림길」 — 소절: 자체 호스팅이 요구사항인가 — 보안·규정이 정하는 경우 / 실시간 시각화와 하이퍼파라미터 자동 탐색이 필요한가 / 나중에 갈아탈 수 있게 로깅 호출을 얇은 래퍼 뒤에 두기

- `ondevice-webgpu` (지금 3,491자 · 절 6)
  - 절「WebGL과 갈리는 지점」 — 소절: 행렬 곱을 텍스처인 척 포장해 픽셀 셰이더로 통과시키던 우회 / 컴퓨트 셰이더가 정식 경로로 들어오면서 데이터를 그래픽으로 위장할 필요가 없어진 것 / 큰 저장 버퍼 지원이 수 GB 가중치를 처음으로 현실화한 자리
  - 절「도구 셋의 쓰임」 — 소절: WebLLM·Transformers.js·ONNX Runtime Web이 각각 맞는 자리 / OpenAI 호환 형태의 API가 기존 클라이언트 코드를 얼마나 살리는가 / WebGPU 경로와 WASM 폴백이 갈리는 지점
  - 절「첫 방문에 드는 시간」 — 소절: 내려받기·캐시·셰이더 컴파일 셋으로 나눠 세기 / 셰이더 컴파일이 5~20초인 이유 — 계산 커널 수십 개를 그 장치의 GPU용으로 만든다 / 두 번째 방문에서 무엇이 사라지고 무엇이 남는가
  - 절「기다리게 만드는 법」 — 소절: initProgressCallback의 값을 「620MB / 1.9GB」처럼 그대로 적기 / 페이지를 열자마자 받지 않고 사용자가 기능을 처음 누를 때 용량과 이유를 먼저 알리기 / 재방문이 전제인 서비스에서만 첫 방문 비용이 회수된다
  - 절「버퍼 한계라는 천장」 — 소절: 브라우저가 장치 메모리 전부를 페이지에 안 내주는 이유(다른 탭과 화면 합성) / 모델을 받기 전에 adapter.limits로 maxStorageBufferBindingSize를 확인하는 순서 / 4비트 1B가 0.7GB, 3B가 1.9GB — 실용 구간이 여기인 근거 / 8B에서 컨텍스트를 길게 잡으면 KV 캐시가 한계를 넘긴다
  - 절「얻는 것과 잃는 것」 — 소절: 입력이 기기를 안 떠나는 것이 편의가 아니라 요건인 경우(의료 초안·법률 검토·사내 회의록) / 같은 3B가 장치마다 초당 30토큰과 5토큰으로 갈리는 문제와 첫 응답에서 속도를 재는 대응 / 모델 버전을 올릴 때마다 모든 사용자가 GB를 다시 받는다 / navigator.gpu가 없는 사용자를 위한 서버 경로를 함께 두기

- `serving-tensor-parallel` (지금 3,491자 · 절 7)
  - 절「가중치가 넘치는 자리」 — 소절: 파라미터 수 × 값의 바이트 수로 어림하기 — 700억 fp16이면 140GB / 80GB 카드에서 KV 캐시 몫까지 빼고 남는 여유 / 양자화로 먼저 줄이는 길과 그것으로도 부족한 크기
  - 절「나누는 축 셋」 — 소절: 텐서·파이프라인·데이터 병렬이 각각 무엇을 나누는가 / 데이터 병렬은 통신이 없는 대신 메모리 문제를 못 푼다 / 셋을 섞는 실제 구성 — 서버 안은 텐서, 서버 경계는 파이프라인
  - 절「세로·가로로 자르기」 — 소절: W₁을 열로, W₂를 행으로 자르면 카드마다 완결된 항이 나온다 / GELU가 원소별 함수라 중간에 합칠 필요가 없는 이유 / 방향을 반대로 잡으면 GELU 앞에서 한 번 더 합쳐 통신이 두 배가 되는 자리 / All-Reduce가 하는 일 — 모아 합치고 전부에게 돌려준다
  - 절「어텐션과 헤드 수」 — 소절: 헤드마다 독립 계산이라 나눠 맡기면 그대로 병렬이 된다 / 32개 헤드를 2·4·8·16·32장까지만 나눌 수 있고 3·5는 안 되는 제약 / GQA에서 KV 헤드 8개를 카드 16장이 나눌 수 없어 엔진이 복제하는 자리와 그만큼 줄어드는 캐시 이득
  - 절「통신이 붙는 양」 — 소절: 블록마다 두 번, 층이 80개면 토큰 한 걸음에 160번 / 한 번의 크기 = 토큰 수 × 은닉 차원 × 바이트(동시 32건·8,192·fp16이면 512KB) / 양보다 중요한 것은 각 All-Reduce가 계산을 멈춰 세운다는 사실 / NVLink는 묻히고 PCIe는 붙고 이더넷은 쓸 자리가 아니다
  - 절「장수를 정하는 판단」 — 소절: 2장 1.7~1.9배, 4장 3배 언저리, 8장 5배를 넘기기 어려운 세 가지 이유 / 한 장에 들어가고 처리량만 필요하면 독립 인스턴스가 거의 항상 낫다 / 가중치만 겨우 들어가는 장수는 KV 캐시 자리를 안 남겨 동시 처리량이 안 나온다 / 부분합 더하는 순서가 바뀌어 출력이 미세하게 달라진다 — 문자열 일치 회귀 테스트가 깨지는 자리

- `edge-npu-runtime` (지금 3,501자 · 절 7)
  - 절「미리 컴파일하는 구조」 — 소절: 런타임이 커널을 만들어 주는 GPU 경로와 무엇이 다른가 / 빌드가 개발 기계에서 끝나고 배포가 앱 업데이트 절차를 타는 결과 / 퀄컴용 바이너리가 미디어텍에서 안 도는 것 — 지원 기기 수만큼 산출물이 는다
  - 절「연산자 지원의 벽」 — 소절: 대응 없는 연산자에서 컴파일 실패와 CPU 분할 둘로 갈리고 후자가 더 위험한 이유 / 경계마다 메모리 복사와 동기화가 붙어 다섯 군데만 생겨도 CPU 전부보다 느려지는 자리 / 잘 도는 것(합성곱·완전연결·풀링)과 막히는 것(커스텀 층·조건 분기·동적 shape) / 시퀀스 길이를 고정값 몇 개로 나눠 각각 컴파일하는 우회와 늘어나는 산출물
  - 절「INT8이 전제인 이유」 — 소절: GPU에서 최적화였던 것이 여기서는 올라가느냐 마느냐가 된다 / 보정 데이터를 배포될 현장에서 뽑는 이유 — 공장 조명 아래 이미지를 스튜디오 샘플로 보정했을 때 검증셋과 실측이 갈린다 / 층 단위와 채널 단위, 대칭만 지원하는 칩의 차이가 스펙 시트에 안 적혀 있어 작은 모델로 먼저 재는 수밖에 없다
  - 절「지속 부하에서의 성능」 — 소절: 짧은 벤치마크에서 NPU가 모바일 GPU에 지는 것으로 나오는 자리 / 발열이 쌓이면 GPU는 클럭을 내리고 NPU는 거의 안 깎인다 / 간헐 추론과 상시 추론이 갈리는 판단 — 카메라 프리뷰 검출·상시 음성 인식·24시간 검사 장비 / 배터리 기기와 팬 없는 밀폐 기기에서 다른 선택지가 없는 이유
  - 절「벤더마다 갈린 도구」 — 소절: 퀄컴·애플·인텔·엔비디아·안드로이드 공통 경로가 각각 서는 자리 / 공통 경로는 코드 하나로 여러 칩을 덮는 대신 최적화가 얕고 최신 기능이 늦다 / 기기 종류가 많으면 공통 경로로 시작하고 주력 한두 종만 벤더 SDK로 다듬기 / NPU 초기화가 실패하는 기기가 반드시 나온다 — 드라이버 버전, 제조사 차단, 다른 앱의 점유
  - 절「프로젝트 순서」 — 소절: 목표 기기 확정 → 무작위 가중치로 컴파일 통과 → INT8 전제 학습 설계 / 학습을 다 마치고 컴파일러를 처음 돌렸을 때 되돌리는 비용 / 실기기에서 지속 부하로 재고, NPU·CPU 어느 경로로 돌았는지를 기록에 남긴다

- `finetuning-grpo` (지금 3,581자 · 절 8)
  - 절「어드밴티지와 기준선」 — 소절: 보상을 그대로 쓰면 쉬운 프롬프트의 평범한 답이 어려운 프롬프트의 훌륭한 답을 이기는 자리 / PPO의 가치 모델이 정책과 비슷한 크기라 GPU에 모델이 하나 더 올라간다 / 무리 평균을 빼고 표준편차로 나누는 식이 각각 하는 일 / 형제 답끼리 비교해 프롬프트 난이도가 자동으로 상쇄되는 구조
  - 절「무리 크기 G」 — 소절: 4·8~16·32 이상에서 얻는 것과 잃는 것 / G를 늘릴 것인가 배치를 늘릴 것인가는 과제 성공률이 정한다 / 성공률 0.05인 과제에서 G=8이면 여덟 개가 모두 실패해 그 프롬프트가 학습에 기여하지 않는다
  - 절「기울기가 사라지는 자리」 — 소절: 무리의 보상이 전부 같으면 어드밴티지가 전부 0이 된다 / 전부 성공하는 프롬프트도 전부 실패와 똑같이 쓸모없고, 학습이 진행될수록 그 비율이 는다 / 「보상이 갈린 프롬프트의 비율」을 매 스텝 찍어 진행을 정직하게 보기 / 난이도 교체(커리큘럼)와 부분 점수 둘의 대응과 각각의 부작용
  - 절「참조 모델과 KL 계수」 — 소절: 학습 시작 시점 사본을 얼려 두고 KL 항으로 거리를 재는 이유 — 사람이 못 읽는 문자열이 만점을 받는 상태를 막는다 / β가 크면 못 움직이고 작으면 다른 능력을 잃는 맞바꿈 / 테스트 통과처럼 기계가 정확히 매기는 보상에서 β를 아주 작게 두는 근거 / 모델이 매기는 점수를 쓸 때는 반드시 살려 둔다
  - 절「보상 함수의 사고」 — 소절: 길이 부분 점수·태그 가점·테스트 통과·심판 점수 넷에서 모델이 찾아낸 지름길 / 테스트 파일을 고치거나 예외를 삼키는 일이 실제로 자주 일어나는 자리 / 채점 환경을 모델이 못 건드리게 격리하는 것이 보상 설계의 절반 / 보상 함수를 여러 개로 나눠 어느 쪽이 오르고 어느 쪽이 정체하는지 따로 보기
  - 절「돌릴 때의 값 넷」 — 소절: per_device_train_batch_size가 G로 나눠떨어져야 하는 이유 — 무리가 쪼개지면 평균 낼 대상이 사라진다 / 학습률이 DPO보다도 작은 근거 — 한 번 크게 밀리면 다음 생성이 통째로 오염된다 / 생성이 전체 시간의 대부분이라 추론 엔진을 학습 루프에 붙이는 구성 / DPO·PPO와 고르는 기준 — 채점 기준을 코드로 적을 수 있는가

- `quantization-kv-cache` (지금 3,741자 · 절 7)
  - 절「캐시 크기 세기」 — 소절: 2·L·H_kv·d_h·p·B·S 식에서 각 항이 뜻하는 것 / 층 32·KV 헤드 8·헤드 차원 128·FP16이면 토큰 하나가 128 KiB인 계산 / 32k 문맥 16건이면 64 GiB, 가중치 16 GiB와 합쳐 80 GiB가 꽉 차는 자리 / B와 S가 곱으로 들어가고 가중치는 이 식에 아예 없다
  - 절「가중치 양자화와의 차이」 — 소절: 배포 전 한 번 대 토큰마다 실시간 / 보정 데이터가 대개 필요 없는 이유 — 그 자리에서 잰다 / 오차가 누적된다 — 3번 토큰을 만들 때의 오차를 100번 토큰이 다시 읽는다 / 설정 한 줄로 껐다 켤 수 있어 비교가 쉽다는 이점
  - 절「줄이면 빨라지는 이유」 — 소절: 디코딩이 계산이 아니라 메모리 대역폭에 막혀 있는 구간이라는 것 / 매 토큰 읽는 캐시가 절반이면 읽는 시간도 절반 / 역양자화 커널이 그 이득을 까먹지 않을 만큼 가벼워야 한다는 조건
  - 절「K와 V의 축 처리」 — 소절: K에는 토큰이 무엇이든 절댓값이 큰 채널이 따로 있다 / 토큰 축으로 묶으면 그 채널이 범위를 혼자 정하고 나머지가 몇 칸에 뭉개진다 / V는 편향이 약해 토큰 축이어도 손해가 적고, 새 토큰만 보고 바로 정할 수 있어 구현이 쉽다 / 잔여 창 — 채널 축이 토큰을 모아야 하는 사정과 최근 토큰에 어텐션이 몰린다는 두 이유
  - 절「비트 폭의 안전 구간」 — 소절: FP8·INT8은 설정 한 줄로 켜는 안전 구간 / INT4는 K의 축 처리와 잔여 창을 지원하는 구현에서만, 토큰 축으로 통째 묶으면 무너진다 / 2비트가 긴 생성과 정확한 검색에서 깨지는 자리 / 가중치 양자화와 캐시 양자화는 독립된 선택이고 어느 쪽이 메모리를 더 쓰는지 세어 정한다
  - 절「다른 감축 수단과 검증」 — 소절: GQA·MLA·슬라이딩 윈도우·토큰 축출·프리픽스 캐싱·CPU 이관이 각각 식의 어느 항을 줄이나 / 양자화는 흐리게 하고 축출·윈도우는 버린다 — 정확한 인용이 필요한 과제에서 갈리는 지점 / 품질은 긴 생성 뒷부분·긴 문맥 인용·여러 차례 대화 셋으로 재고 원본과 나란히 놓는다 / 도입의 실제 이유는 동시 요청 상한이고 지연 개선은 덤이다

- `speculative-draft-models` (지금 3,787자 · 절 6)
  - 절「디코드의 빈틈」 — 소절: 토큰 하나를 뽑으려고 가중치 전부를 읽고 행렬 곱 몇 번만 하는 구간 / 가중치를 한 번 읽은 김에 여러 토큰을 처리해도 시간이 거의 안 는다 / 자기회귀 생성이 그것을 원리상 막는 이유
  - 절「거절 샘플링의 보정」 — 소절: p ≥ q면 무조건 수용, 아니면 p/q 확률로 수용 / 거절한 자리는 max(0, p−q)를 정규화한 분포에서 다시 뽑는 보정 항 / 그리디 디코딩이면 초안이 타깃의 argmax와 같은지만 보면 되는 단순화 / 품질 회귀가 없어 도입할 때 평가셋을 다시 안 돌려도 되는 이점
  - 절「이득을 정하는 두 값」 — 소절: 기대 토큰 수 (1−α^{k+1})/(1−α)에서 분자의 k+1이 뜻하는 것 / 초안 비용 c가 붙은 실제 배수 = 기대 토큰 ÷ (1 + kc) / α=0.5에서 k를 2에서 7로 늘려도 1.75 → 1.99뿐이라 분모만 커지는 계산 / 초안 모델을 줄이면 c와 α가 함께 내려가고 최적점이 조합마다 다르다
  - 절「초안을 만드는 네 길」 — 소절: 같은 계열의 작은 체크포인트 — 학습이 필요 없는 대신 토크나이저가 같아야 한다 / n-gram 룩업 — 비용이 사실상 0이고 요약·코드 편집처럼 입력 문자열이 출력에 다시 나오는 작업에서 강하다 / 다중 헤드 — c가 작은 대신 헤드끼리 독립이라 뒤로 갈수록 정확도가 떨어진다 / 특징 예측 — 토큰이 아니라 타깃의 은닉 특징을 맞혀 정보가 진하지만 학습 파이프라인을 따로 갖춰야 한다
  - 절「배치 크기의 임계」 — 소절: 「계산 유닛이 논다」는 전제가 동시 요청 32개에서 무너지는 자리 / 벤치마크는 배치 1이고 프로덕션은 아니라는 흔한 어긋남 / 대기 큐 길이를 보고 투기 경로를 켜고 끄는 전환 / 줄이는 것은 한 사용자의 토큰 간 지연이지 서버 처리량이 아니다
  - 절「도입 전 확인 넷」 — 소절: 실제 프롬프트 로그 몇백 건으로 α를 직접 재기 — 같은 초안 모델이 코드와 한국어 대화에서 다르게 나온다 / k를 고정하지 않고 직전 수용 결과로 올리고 내리기 / KV 캐시가 두 벌이고 거절 시 초안 쪽을 되돌려야 한다 / 되돌리기 버그는 출력이 여전히 정확해 품질 지표로 안 잡히므로 수용률을 지표로 내보낸다

- `finetuning-qlora` (지금 3,794자 · 절 10)
  - 절「LoRA가 못 넘는 벽」 — 소절: 동결된 베이스도 bf16으로 올라가야 해서 7B가 14GB를 먼저 먹는 자리 / 24GB에서 배치 1과 그래디언트 누적으로 버티는 상태 / 「학습하지 않는 가중치를 왜 16비트로 두는가」라는 물음 / 4비트로 눌렀을 때 열리는 조합 — 24GB에서 8B, 48GB에서 13B, 80GB에서 70B
  - 절「NF4 코드북」 — 소절: 균등 분할이 정규분포 데이터에 최적이 아닌 이유 / 각 4비트 코드가 같은 확률 질량을 갖도록 경계를 배치해 0 근처에 코드를 몰아 주기 / 저장은 4비트, 계산은 bf16 — compute_dtype을 따로 두지 않으면 정밀도가 무너지는 자리
  - 절「이중 양자화와 페이징」 — 소절: 블록 64마다 fp32 상수 하나가 파라미터당 0.5비트인 계산 / 그 상수를 int8로 다시 눌러 0.375비트를 더 아끼는 이중 양자화 / 페이지드 옵티마이저가 OOM 대신 CPU 스왑을 고르는 거래 / 스왑이 잦아지면 무엇을 먼저 줄이나 — 시퀀스 길이와 배치
  - 절「빠뜨리기 쉬운 준비」 — 소절: prepare_model_for_kbit_training이 대신 해 주는 일 / 4비트 상태에서 그래디언트 체크포인팅을 안전하게 켜는 순서 / LayerNorm을 fp32로 남기는 이유
  - 절「학습 불안정의 원인」 — 소절: 4비트 상태의 역전파에서 그래디언트 스케일이 흔들리는 자리 / max_grad_norm과 낮은 학습률로 잡는 범위 / bf16 대비 처리량이 느려질 수 있다는 사실과 그 대가를 받아들일 조건
  - 절「병합과 선택」 — 소절: merge_and_unload로 병합한 뒤 bf16으로 저장하면 양자화 오차가 그대로 남는다 / 양자화 상태로 서빙할 것인가 병합하고 손실을 감수할 것인가 / VRAM이 충분하면 LoRA, 부족하면 QLoRA라는 단순한 갈림 / 학습 처리량이 목표면 LoRA가 나은 자리

- `mlops-feature-store` (지금 3,820자 · 절 6)
  - 절「학습-서빙 스큐」 — 소절: 학습은 배치 집계, 서빙은 실시간 변환이라 타임존·NULL 처리에서 값이 갈리는 자리 / 같은 피처를 추천·검색·리스크 팀이 각자 구현할 때 생기는 미묘한 차이 / 서빙에서 현재 시각 기준으로 집계하면 미래 데이터가 새어 든다 / 오프라인 평가는 좋은데 실서비스만 나쁜 증상의 전형적인 원인
  - 절「저장소 넷의 역할」 — 소절: 레지스트리가 답하는 질문 — 어디서 왔나, 누가 고쳤나, 어느 모델이 쓰나 / 오프라인 스토어와 point-in-time 조회 — 그 시점의 값만 쓰게 만드는 구조 / 온라인 스토어가 KV여야 하는 지연 요구와 대표 기술 / 같은 변환 로직이 양쪽에 함께 적용되는 것이 일관성의 실체
  - 절「적재의 지연」 — 소절: 배치 materialize 주기가 곧 온라인 피처의 나이 / 증분 적재가 마지막 시점 이후만 처리하는 구조 / 이 지연이 모델 성능에 영향을 주는지 재는 법 / 스트리밍으로 넘어갈지 정하는 지점
  - 절「TTL과 빈 값」 — 소절: TTL이 짧으면 서빙에서 값 대신 None이 돌아온다 / 피처 성격마다 유효 기간을 다르게 잡는 기준 / 빈 값이 왔을 때의 기본값 정책이 학습 시 처리와 어긋나면 스큐가 다시 생긴다
  - 절「도입 순서」 — 소절: 기존 파이프라인을 한 번에 옮기지 않고 신규 피처부터 태우기 / 처음 옮길 피처를 고르는 기준 — 여러 팀이 이미 중복 계산하는 것 / 학습 코드와 서빙 코드가 같은 피처 이름으로 조회하게 만드는 것이 목표 / 도입해도 안 사라지는 일 — 피처 분포 변화 감시
  - 절「도구를 고르는 축」 — 소절: 운영 부담을 팀이 질 것인가 구독료로 넘길 것인가 / 스트리밍 피처가 핵심인가 배치로 충분한가 / 이미 쓰는 클라우드에 묶을 때 얻는 것과 잃는 것 / 어느 쪽이든 남는 합의 — 피처 정의와 소유자를 누가 정하는가

- `serving-disaggregated-prefill` (지금 3,861자 · 절 7)
  - 절「두 계산의 병목」 — 소절: 프롬프트 4,000토큰을 한 번에 넣는 행렬×행렬과 토큰 하나를 넣는 행렬×벡터 / 산술 강도 = 연산 횟수 ÷ 읽은 바이트 / 가중치를 읽는 양은 같은데 계산량만 4,000배 갈린다 / 배치를 키우면 디코드만 이득을 보고 프리필은 이미 꽉 차 있다
  - 절「섞였을 때의 증상」 — 소절: 걸음에 새 요청의 프리필이 끼면 같은 배치의 디코드가 그동안 토큰을 못 낸다 / 사용자 화면에서 글자가 흐르다 멈추는데 그 이유가 자기 요청과 무관한 자리 / p50은 멀쩡하고 p99만 나빠져 토큰 사이 간격의 분포를 따로 안 보면 지나친다
  - 절「청크 프리필의 한계」 — 소절: 긴 프리필을 512토큰 조각으로 쪼개 한 걸음이 길어지는 정도를 줄인다 / 없애는 것은 튐의 크기이지 자원 경쟁 자체가 아니다 / 조각으로 쪼갠 만큼 행렬이 작아져 프리필 효율도 조금 떨어진다 / 배치 정책과 병렬화를 두 단계가 여전히 공유한다는 사실은 그대로다
  - 절「나눠서 얻는 셋」 — 소절: 디코드 노드에 프리필이 안 들어와 토큰 간격이 고르게 되는 것 / 배치 정책을 처리량과 첫 토큰 시간에 각각 맞추기 / 대역폭 좋은 카드와 연산 좋은 카드를 따로 고르고 텐서 병렬 크기도 다르게 잡기 / 세 번째의 값이 규모가 커질수록 커지는 이유
  - 절「KV 캐시 전송 비용」 — 소절: 2 × h_kv × d_head × b로 토큰당 층별 크기를 세고 층 수를 곱하기(8B에서 토큰당 128KB) / 프롬프트 4,000토큰이면 약 500MB — NVLink 0.6ms, RDMA 10ms, 25G 이더넷 160ms / 층별로 완성되는 대로 먼저 보내 프리필 계산 뒤에 전송을 숨기기 / 블록 단위 KV 구조가 부분 전송과 재사용에 다시 쓰이는 자리
  - 절「두 무리의 비율」 — 소절: 입력이 길고 출력이 짧으면 프리필 쪽, 반대면 디코드 쪽으로 일이 쏠린다 / 시간대마다 비율이 바뀌어 고정 비율이 어느 쪽이든 놀리는 문제 / 한쪽만 막히면 다른 쪽이 놀면서 전체가 느려지는 새 실패 모드 / 도입 판단표 — 대부분의 서비스는 「청크 프리필로 충분하다」 줄에 있다

- `finetuning-lora` (지금 3,910자 · 절 10)
  - 절「저랭크 가정」 — 소절: 파인튜닝 중 생기는 ΔW가 저랭크라는 관찰이 뜻하는 것 / d=4096, r=16에서 1,677만 개가 13만 개가 되는 계산 / B를 0으로 초기화해 ΔW=0에서 출발시켜 초기 불안정을 없애기 / 실제로 더하는 것은 (α/r)·B·A라는 점
  - 절「rank를 고르는 법」 — 소절: 4·8·16·32·64가 각각 더하는 파라미터와 어울리는 과제 / rank를 올려도 성능이 수렴하는 구간이 있다 / rank를 올리는 것보다 target_modules를 늘리는 편이 나은 자리
  - 절「alpha와 학습률」 — 소절: 실효 스케일 α/r이 학습률과 곱해져 작동하는 구조 / alpha = 2 × rank가 관행이 된 근거와 α=r로 두었을 때의 보수적 학습 / LoRA가 Full FT의 열 배 학습률을 견디는 이유 — 동결된 W에 비해 B·A의 스케일이 작다
  - 절「어댑터를 붙일 모듈」 — 소절: q·v만, 전체 어텐션, 어텐션+FFN 세 전략의 파라미터 차이 / 전체 어텐션이 기본인 이유 — Q·K·V를 다 조정해야 어텐션 패턴 전체가 재구성된다 / FFN까지 더할 때 값을 하는 과제(수학·코드·전문 도메인)와 늘어나는 비용 / dropout은 데이터 양이 정한다 — 수천~수만 건과 수십만 건의 갈림
  - 절「병합과 어댑터 교체」 — 소절: 어댑터를 붙인 채 쓰면 매 forward마다 B·A 연산이 붙는다 / merge_and_unload로 추론 지연을 원본과 같게 만들기 / 태스크마다 어댑터를 갈아 끼우는 서빙에서는 병합이 오히려 손해인 자리
  - 절「막혔을 때의 순서」 — 소절: rank=16·alpha=32·전체 어텐션에서 시작하는 이유 / 성능이 모자라면 데이터 품질 → target_modules → rank 순서로 손대기 / 학습 곡선이 안 내려갈 때 학습률보다 먼저 보는 것 / 어댑터만 저장되므로 설정을 바꿔 가며 실험 이력을 쌓기 쉽다

- `quantization-basics` (지금 4,038자 · 절 8)
  - 절「줄여서 얻는 셋」 — 소절: 메모리 — 7B가 FP32 28GB에서 INT4 3.5GB까지 내려가는 자리 / 속도 — 정수 텐서 코어 처리량과 절반이 된 메모리 대역폭 / 비용 — 같은 카드로 몇 배를 받는가 / 목표가 셋 중 무엇이냐에 따라 고르는 형식이 달라진다
  - 절「지수와 가수의 몫」 — 소절: 부호·지수·가수가 각각 정하는 것(범위와 정밀도) / FP16의 지수 5비트가 만드는 65504 천장과 Loss Scaling / BF16이 지수 8비트로 FP32와 같은 범위를 갖고 학습에서 FP16을 대체한 이유 / INT8·INT4에는 지수 개념이 없어 표현 범위가 고정된다는 것
  - 절「스케일과 영점」 — 소절: absmax 대칭 — scale = max|x| ÷ 127과 양자화·역양자화 식 / 비대칭 — min·max를 함께 쓰고 zero-point를 하나 더 두는 자리 / ReLU 뒤처럼 0 이상만 있는 분포에서 대칭이 절반을 버리는 계산 / NF4가 분위수로 코드를 배치해 정규분포에 맞추는 아이디어
  - 절「이상치가 깨뜨리는 것」 — 소절: 6.7B를 넘으면 일부 차원에서만 절댓값이 커지는 현상 / 대부분이 ±1인데 한 차원이 100이면 absmax 스케일에서 나머지가 몇 칸에 몰리는 계산 / LLM.int8()의 혼합 정밀도 — 이상치 차원만 FP16으로 계산 / 채널·그룹 단위로 쪼개 피해를 가두는 다른 길
  - 절「PTQ와 QAT」 — 소절: 재학습 없이 보정만 하는 절차와 걸리는 시간 / QAT의 가짜 양자화와 미분 불가능한 지점을 STE로 통과시키는 근사 / INT8은 PTQ로 충분하고 INT4에서 손실이 기준을 넘으면 QAT로 가는 갈림 / 재학습 비용이 전체 파인튜닝에 준한다는 사실
  - 절「적용하면 안 되는 자리」 — 소절: 세밀한 수치 계산이 필요한 과제에서 INT4가 실수를 늘리는 자리 / 1B 이하 소형 모델에서 오차의 영향이 큰 이유 / 계속 파인튜닝할 모델과 QLoRA가 예외인 까닭 / 남의 표를 믿지 않고 자기 과제로 원본과 나란히 재는 절차

- `finetuning-on-prem` (지금 4,089자 · 절 11)
  - 절「자체 서버가 이기는 조건」 — 소절: 주당 사용 시간으로 회수 시점을 어림하는 경험칙 / 데이터를 외부로 못 보내는 조직에서는 선택지가 아니라 전제인 자리 / 초기 비용·장기 비용·확장성·실험 빈도 네 축의 맞바꿈
  - 절「카드와 연결」 — 소절: A100 80GB·RTX 4090·H100이 각각 감당하는 모델 크기와 가격대 / NVLink 600GB/s와 PCIe 4.0 ×16의 32GB/s가 step 시간에 만드는 차이 / AllReduce가 필수라 통신 대역폭이 곧 GPU 가동률인 이유 / 서버를 넘을 때 InfiniBand가 필요하고 10GbE는 GPU를 절반 이상 놀린다
  - 절「버전을 맞추는 순서」 — 소절: 드라이버 확인 → CUDA Toolkit → cuDNN → PyTorch 순서와 어겼을 때 나는 증상 / 팀마다 다른 CUDA·PyTorch 조합을 한 서버에서 공존시키는 환경 격리 / 설치 확인을 한 줄로 끝내는 지점
  - 절「ZeRO 세 단계」 — 소절: 7B Full FT의 56GB를 파라미터 14·그래디언트 14·옵티마이저 28로 나눠 세기 / Stage 1·2·3이 각각 무엇을 나누고 통신이 얼마나 느는가 / 대부분의 파인튜닝에서 Stage 2가 균형점인 이유와 Stage 3이 필요한 크기 / overlap_comm과 contiguous_gradients가 실제로 하는 일
  - 절「스토리지와 데이터 공급」 — 소절: NVMe와 RAID 0의 읽기 대역폭을 GPU 처리 속도에 맞추는 계산 / 토크나이즈를 미리 해 두지 않으면 CPU가 병목이 되는 자리 / num_workers·pin_memory로 CPU→GPU 전송을 겹치기 / GPU 사용률이 80% 아래일 때 데이터 로딩과 통신 중 무엇을 먼저 의심하나
  - 절「돌아가는 동안 보는 것」 — 소절: 70B 체크포인트 한 개가 130~140GB — 보관 개수와 저장 주기를 정하는 계산 / 온도·전력·ECC 오류를 보는 이유와 각각이 알려 주는 것 / ZeRO Stage 3의 분산 체크포인트를 단일 파일로 병합하는 자리 / 시드·커밋 해시·데이터셋 버전을 학습 로그에 함께 남기기

- `serving-sglang` (지금 4,093자 · 절 9)
  - 절「요청이 공유하는 앞부분」 — 소절: 챗봇의 시스템 프롬프트·에이전트의 누적 대화·한 문서에 붙는 여러 질문이 겹치는 모양 / 프리필이 입력 토큰 수에 비례해 드는 비용이라 1,200토큰 앞부분을 매번 다시 내는 것의 뜻 / 서버가 요청을 독립된 문자열로 볼 때 그 관계를 모른다는 것
  - 절「접두 트리의 동작」 — 소절: 처리한 앞부분의 KV를 버리지 않고 가지로 남긴다 / 새 요청이 가장 길게 겹치는 가지를 찾아 거기서부터만 계산하기 / 가득 차면 잎사귀부터 LRU로 버려야 하는 이유 — 중간을 버리면 아래 가지가 통째로 못 쓰게 된다 / 캐시 히트가 긴 요청을 먼저 실행해 그 노드가 축출되지 않게 하는 스케줄링
  - 절「vLLM 접두 캐싱과의 차이」 — 소절: 블록 해시 테이블과 접두 트리의 저장 단위 / 블록 경계에 맞아야 하는 판정과 토큰 단위 최장 공통 접두사 / 여러 갈래가 갈라지는 지점까지 한 노드를 공유하는 경우 / 앞부분이 통째로 같은 부하에서는 둘의 결과가 크게 다르지 않다
  - 절「이득이 0인 부하」 — 소절: 사용자마다 다른 문서를 통째로 넣는 부하, 매번 새 이미지가 앞에 오는 작업 / 겹치지 않으면 트리 유지 비용만 붙는다 / 엔진을 바꾸기 전에 로그에서 공통 앞부분 길이를 재는 절차
  - 절「점프 포워드 디코딩」 — 소절: 제약 디코딩이 매 토큰 문법상 허용되는 후보만 남기는 방식 / 후보가 하나뿐인 구간을 모델 호출 없이 붙여 버리기 / 필드 이름이 길고 값이 짧은 추출 작업에서 이득이 커지는 이유
  - 절「고를 때와 함정」 — 소절: 자체 서빙 판단 → 접두사 겹침 실측 → 내 부하로 둘 다 측정 → 운영 조건 순서 / mem-fraction-static을 올렸는데 히트율이 떨어지는 자리 — 정적 할당이 트리 자리를 먹는다 / 히트율을 안 보면 튜닝이 아니라 추측이다 / fork 없이 OpenAI 호환 엔드포인트만 써도 트리는 동작한다는 점과 결정론을 기대하면 안 되는 이유

- `finetuning-prefix-tuning` (지금 4,274자 · 절 8)
  - 절「하드 프롬프트의 한계」 — 소절: 자연어 어휘가 불연속이라 미분으로 최적화할 수 없다 / 좋은 프롬프트를 사람이 찾아야 하고 문구에 따라 결과가 흔들려 재현성이 낮은 자리 / 프롬프트를 임베딩 공간의 연속 벡터로 옮기고 역전파로 직접 미는 발상
  - 절「KV 앞에 붙는 벡터」 — 소절: 층마다 P_k, P_v를 K·V 앞에 이어 붙이는 구조 / 쿼리가 입력 토큰뿐 아니라 접두사를 참조하게 된다는 것의 뜻 / 파라미터 수 2 × L × m × d와 GPT-2 medium에서 m=10일 때의 0.14% / 직접 최적화하면 불안정해 MLP로 재매개변수화하고 학습 후 버리는 이유
  - 절「입력층과 전 층의 차이」 — 소절: P-Tuning v1이 입력 임베딩에만 넣고 LSTM·MLP로 프롬프트를 생성하는 방식 / 모델이 깊어지면 입력층 신호가 희석되던 문제 / v2가 모든 층으로 확장하며 겨냥한 과제군 — NLU·NER·QA 같은 시퀀스 레이블링
  - 절「가상 토큰 수 m」 — 소절: m이 늘면 표현력과 함께 각 층의 KV 길이가 함께 는다 / m=20이면 모든 어텐션 연산에 20개가 얹히는 비용 / 긴 시퀀스에서 이 비용이 추론 속도와 메모리로 드러나는 자리
  - 절「LoRA와 갈리는 지점」 — 소절: 병합이 되는 쪽과 잔차·KV 구조라 안 되는 쪽 / 기본 모델을 공유하고 태스크마다 접두사만 갈아 끼우는 서빙 / 같은 접두사를 미리 계산해 KV 캐시에 넣어 두고 여러 요청이 재사용하기 / 대규모 생성 과제에서 LoRA가 앞서는 경향
  - 절「이 계열을 고르는 자리」 — 소절: 모델 인스턴스 하나로 여러 태스크를 서빙해야 할 때 / 데이터가 적은 NLU 분류에서 Full FT보다 과적합이 덜한 이유 / 저장할 것이 접두사 가중치뿐이라 실험 반복이 싼 자리 / 단일 태스크이고 생성 품질이 최우선이면 LoRA·QLoRA로 간다

- `finetuning-adapter` (지금 4,470자 · 절 10)
  - 절「태스크마다 늘어나는 사본」 — 소절: BERT 110M을 태스크마다 통째로 저장할 때의 비용 / 기본 모델 하나를 공유하고 소형 모듈만 따로 두는 발상 / 태스크당 0.5~4%가 실제 용량으로 뜻하는 것
  - 절「병목 모듈의 구조」 — 소절: d → r → d와 잔차 연결이 각각 하는 일 / 파라미터 2 × d × r × L × N 계산 — BERT-base, r=64, 층마다 둘이면 약 2.36M / up-projection을 0으로 초기화해 학습 초기에 항등 함수로 두기 / r을 정할 때 보는 것과 늘렸을 때 늘어나는 지연
  - 절「직렬과 병렬 삽입」 — 소절: 어텐션 뒤와 FFN 뒤 두 번 넣는 Houlsby 배치 / FFN 뒤 하나만 넣어도 성능이 유지되는 자리 — 파라미터가 절반이 된다 / 병렬 배치가 지연을 줄이려 한 이유와 모듈이 작아 실제 이득이 크지 않은 경우
  - 절「지연이 남는 이유」 — 소절: 잔차 구조라 원본 가중치에 병합할 수 없다는 것이 LoRA와 갈리는 결정적 지점 / 매 forward에 추가 행렬 연산이 필수로 붙는다 / AdapterDrop — 하위 층 어댑터를 빼는 절충과 그 대가 / Compacter처럼 어댑터 자체를 더 누르는 방향
  - 절「여러 어댑터를 조합하기」 — 소절: 소스 태스크마다 따로 학습하는 1단계와 Fusion 층만 새로 학습하는 2단계 / 각 어댑터가 독립이라 소스 사이에 파국적 망각이 없는 구조적 이유 / 공개 어댑터를 받아 조합할 때 확인할 것 — 기반 모델과 삽입 위치가 같은가 / IA³처럼 더 가벼운 변형이 서는 자리
  - 절「LoRA 시대의 자리」 — 소절: 주요 파인튜닝 프레임워크가 LoRA를 기본으로 두고 어댑터를 선택 옵션으로 남긴 현재 / 수십~수백 태스크를 한 인스턴스에서 서빙할 때 남는 이점 / 배치 추론이 지배적이라 개별 요청 지연이 덜 중요한 환경 / 모듈성 연구 — 능력을 따로 학습해 동적으로 조합한다는 맥락에서 계속 쓰이는 이유

- `mlops-overview` (지금 4,607자 · 절 8)
  - 절「DevOps와 갈리는 다섯 축」 — 소절: 버전 대상이 코드에서 코드+데이터+모델로 는다 / 테스트가 기능 단위가 아니라 데이터 유효성과 모델 성능이다 / 모니터링이 지연·에러율에서 예측 분포·드리프트로 바뀐다 / 실패 원인이 코드 버그가 아니라 데이터 변화라는 것
  - 절「데이터에 매인 시스템」 — 소절: 코드가 같아도 학습 데이터가 다르면 다른 모델이 된다 / 「3개월 전 모델을 다시 만들어 달라」에 답하려면 무엇이 남아 있어야 하나 / 드리프트가 알람 없이 성능을 갉아 내려가는 방식 / 피처 계산 로직이 학습과 서빙에서 갈리는 자리
  - 절「생명주기 여섯 칸」 — 소절: 수집·정제·레이블링·검증까지가 한 덩어리라는 것 / 학습과 평가에서 남겨야 하는 산출물 — 체크포인트·평가 리포트·승격 판단 근거 / 배포 전략 셋(블루-그린·카나리·쉐도우)이 각각 감당하는 위험 / 모니터링에서 재학습으로 되돌아오는 트리거 — 스케줄인가 드리프트인가
  - 절「성숙도 세 단계」 — 소절: 레벨 0의 증상 — 수동 배포, 스프레드시트 실험 기록, 배포 공포증 / 레벨 1의 도달 조건 — 파이프라인 코드가 버전 관리되고 새 데이터에 자동으로 재학습이 돈다 / 레벨 2에서 소프트웨어 CD와 ML 파이프라인이 합쳐지는 자리 / 0에서 2로 한 번에 뛰려다 포기하는 흔한 실패
  - 절「스택을 고르는 축」 — 소절: 데이터 버전 관리와 피처 스토어가 각각 막는 문제가 다르다는 것 / 오케스트레이터를 고르는 질문 — 로컬에서 돌릴 수 있나, 실패한 단계부터 재개되나 / 서빙과 모니터링의 최소 구성 / 도구 과잉 — 다 들이면 유지 부담만 남는다
  - 절「첫 석 달의 순서」 — 소절: 실험 트래킹이 첫 칸인 이유 — 도입 비용 대비 회수가 가장 빠르다 / 다음이 데이터 버전 관리인 근거 / 모델 레지스트리와 스테이징→프로덕션 승격 절차를 정의하는 시점 / 모니터링을 마지막에 두는 것이 맞는 자리와 그 전에도 최소한 남겨야 할 로그

- `finetuning-evaluation` (지금 4,648자 · 절 13)
  - 절「손실이 못 말해 주는 것」 — 소절: 훈련 손실은 줄었는데 과제 성능이 그대로이거나 떨어지는 자리 / 정답이 하나가 아닌 생성 과제의 채점 문제 — 같은 뜻의 다른 문장 / perplexity는 유창함을 재지 사실 정확성을 안 잰다 / 그래서 어떤 단일 지표도 못 쓰고 조합해야 하는 이유
  - 절「겹침 기반 지표」 — 소절: ROUGE-1·2·L이 각각 어휘·구문·순서를 보는 방식 / BLEU가 정밀도 중심이고 짧은 출력에 페널티를 두는 구조 / 형태소가 다양한 한국어에서 n-gram 겹침이 과소평가되는 자리 / BERTScore가 임베딩 유사도로 그 자리를 메우는 방식과 한국어 모델을 지정해야 하는 이유
  - 절「과제별 전용 지표」 — 소절: 분류 — 99%가 정상인 데이터에서 Accuracy 99%가 쓸모없는 계산과 macro F1 / QA — Exact Match와 토큰 F1을 함께 보고하는 이유 / 코드 — Pass@k에서 k를 두는 뜻 / 지표를 고르기 전에 과제 정의를 한 줄로 먼저 적는다
  - 절「모델이 매기는 점수」 — 소절: 척도와 평가 기준을 프롬프트에 못 박는 형태 / 자기 선호 — 심판이 자기 계열 문체의 답을 높게 주는 경향 / temperature와 재현성, 그리고 호출 비용 / 규칙 기반 점수와 섞어 쓰고 사람 표본으로 대조하는 구성
  - 절「평가셋을 만드는 법」 — 소절: 최소 200~500건과 훈련 데이터 미포함이라는 두 조건 / 쉬운 예제만 모으면 점수가 과하게 높게 나오는 이유 / 실패할 만한 엣지 케이스를 일부러 넣는 자리 / 데이터 유출을 실제로 확인하는 절차
  - 절「기준선 셋과 망각」 — 소절: 파인튜닝 전 모델·프롬프트만 쓴 결과·상용 모델 참조점 셋을 모두 재기 / 프롬프트만으로 비슷하면 데이터 품질이나 하이퍼파라미터를 다시 보는 판단 / 파국적 망각을 도메인 밖 일반 과제로 재는 절차 / 훈련·검증 곡선이 갈리는 세 모양과 각각이 가리키는 원인

- `quantization-calibration` (지금 4,798자 · 절 9)
  - 절「보정이 하는 일」 — 소절: 역전파도 라벨도 없는 순전파라 GPU 한 장에 몇 분이면 끝난다 / 가중치는 파일에 숫자로 있고 활성값은 돌려야 존재한다는 차이 / AWQ·GPTQ가 데이터를 쓰는 다른 이유 — 어떤 가중치가 중요한지 판단하고 헤시안을 추정한다
  - 절「스케일과 영점」 — 소절: 대칭 s = α/127과 양자화·역양자화 두 식 / 0을 정수 0에 그대로 두는 방식이 맞는 분포와 안 맞는 분포 / 비대칭에서 영점을 하나 더 두는 이유 / 산출물은 층마다의 두 숫자이고 배포된 뒤 다시 재지 않는다는 사실
  - 절「자를 자리를 고르기」 — 소절: 값의 99.9%가 ±0.5인데 한 채널만 8.2인 층을 숫자로 따라가기 / absmax면 칸 폭 0.064로 ±0.5 구간이 16칸, 99.9 백분위면 0.0047로 213칸 / absmax·백분위·MSE·KL·이동 평균 다섯이 각각 정하는 방식과 성격 / 백분위 하나를 모든 층에 똑같이 적용하면 어떤 층은 과하고 어떤 층은 모자란다
  - 절「데이터의 개수와 길이」 — 소절: 128~512면 충분한 이유 — 재는 것이 파라미터가 아니라 분포의 요약값이다 / 시퀀스 2,048짜리 입력 하나로 이미 수백만 개의 활성값이 나온다는 계산 / 짧은 문장 512개보다 긴 문서 128개가 나은 자리 — 긴 문맥에서만 나오는 범위 / 패딩 토큰을 통계에 넣으면 분포가 0 쪽으로 끌린다
  - 절「데이터의 출처」 — 소절: 예제의 영어 웹 코퍼스를 그대로 쓸 때 어긋나는 둘 — 언어와 형식 / 시스템 프롬프트가 항상 붙는 모델이면 그것을 붙인 상태가 실제 분포다 / 요청 유형이 열인데 가장 흔한 하나에서만 뽑으면 드문 유형이 범위를 넘어 잘린다 / 어떤 문서 128개를 썼는지 시드나 해시로 남겨 두기
  - 절「세분화 단위의 선택」 — 소절: 텐서·채널·그룹·토큰 단위가 이상치 피해를 가두는 정도 / 그룹 크기를 128에서 64로 줄이면 품질이 오르는 이유 / 4비트에 그룹 128이면 스케일과 영점을 더해 실효 4.25비트가 되는 계산 / 토큰 단위 동적 양자화가 활성 보정을 아예 없애는 대신 커널이 매번 최댓값을 구한다
  - 절「새어 나오는 모양과 검증」 — 소절: 긴 입력·드문 유형·다른 언어·코드에서만 나빠지는 네 증상과 각각이 가리키는 원인 / 같은 설정으로 두 번 양자화했는데 결과가 다른 자리 — 시드를 안 고정했다 / 위키텍스트 perplexity 하나로는 못 잡는 이유 — 보정도 평가도 같은 분포면 당연히 좋게 나온다 / 과제 평가·층별 코사인 유사도·최악 사례 셋을 원본과 나란히 놓기


### ai-guide

**끝났다 — 16편 전부(2026-09-09).** 하루 열 편씩 집어 가는 대신 한 번에 다 채웠고,
`AI-ROUTINE.md`의 요일 표에서도 이 칸을 뺐다(일곱 칸 → 여섯 칸).

산문 853~4,039자였던 것이 6,399~10,942자가 됐다. 같은 작업에서 **절 제목 규칙이
바뀌었다** — 「명사구」에서 **「그 절이 다루는 것의 이름」**으로 좁혔다. 이 칸 16편이
새 규칙으로 지은 첫 글이라 본보기로 쓸 수 있다.

| 전 | 후 |
| --- | --- |
| EU AI Act: 세계 첫 포괄적 AI 법률 | EU AI Act |
| 실무자를 위한 규정 준수 체크리스트 | 적용 범위 판정 |
| 원문이 남는 자리를 먼저 세어 본다 | 마스킹 지점 |
| 지웠다는 것을 어떻게 아는가 | 검증 |

### math-for-ai

**합치기 — 없다.** 아래 「수학은 합치지 않는다」를 본다.

**채우기**

- `math-basics-reading-order-of-expressions` (지금 2,836자 · 절 7)
  - 새 절 「같은 식을 코드 한 줄로 옮겨 적기」 — 이 트랙에서 코드 대조가 허용된 세 편 중 하나다. 분수선·근호·첨자를 파이썬으로 옮기며 괄호가 어디에 붙는지, (a+b)/2와 a+b/2가 코드에서 갈리는 자리
  - 「(라) 위첨자와 아래첨자」를 소절 둘로 — 「−2²와 (−2)²」 「x_i와 x^i」. 앞의 것은 우선순위 문제이고 뒤의 것은 표기 문제라 성격이 다르다
  - 「무엇을 먼저 하는가」에 소절 「왜 이 순서여야 했는가」 — 곱셈이 덧셈보다 먼저인 것이 약속인지 필연인지. 3+4×2를 두 순서로 계산해 분배법칙 쪽이 살아남는 것을 보인다
  - 「연습 4 — 소리 내어 읽기」 추가 — 식을 말로 옮기는 5문항. 지금 연습이 전부 '괄호를 채우기'와 '옮겨 적기'라 읽는 훈련이 없다

- `math-basics-working-by-hand` (지금 2,871자 · 절 5)
  - 새 절 「틀린 풀이를 실제로 고쳐 보기」 — 이 글의 본체인데 지금 연습 1이 두 문항뿐이다. 부호 실수·이항 실수·괄호 실수·약분 실수 다섯 개를 줄 단위로 짚어 고치는 절을 본문에 세운다
  - 「검산 세 가지」에 소절 「(라) 단위와 차원을 맞춰 본다」 — 넓이를 구했는데 길이 차원이 나오면 틀렸다는 검산. 트랙 뒤쪽 물리량 계산에서 계속 쓴다
  - 새 절 「계산기를 어디서 쓰는가」 — 이 트랙이 손으로 하는 이유와 기계에 맡겨도 되는 자리를 가른다. 지금 규약만 있고 경계가 없다
  - 「틀렸을 때 의심할 순서」를 소절 셋으로 — 「부호」 「이항」 「괄호」. 각 소절에 실제로 틀린 줄을 하나씩 놓는다

- `math-basics-integral-as-area` (지금 2,878자 · 절 8)
  - 절「직사각형으로 덮기」 — 소절: 칸의 가운데에서 높이를 재는 셋째 방식 — 네 칸에서 0.328125 / 위아래 두 값의 평균 0.34375가 사다리꼴로 잰 것과 같다는 것 / 그림: 같은 네 칸을 왼끝·오른끝·중점 세 방식으로 덮어 나란히
  - 절「칸을 늘리는 일」 — 소절: 위아래 차이가 정확히 (f(1)−f(0))/n이라 네 칸에 0.25, 열 칸에 0.1인 오차 상한 / 참값을 몰라도 그 폭으로 소수 몇째 자리까지 믿을 수 있는지 정해지는 것 / 오른끝은 n을 열 배 해야 오차가 십분의 일인데 중점은 백분의 일이라는 대비
  - 절「시그마로 접어 극한 씌우기」 — 소절: 왼끝으로 접으면 k=0부터 n−1까지라 같은 극한에 닿는 것 / 폭이 같지 않아도, 높이를 칸 안 어디서 재도 답이 같다는 조건 / 위끝을 문자로 남겨 ∫₀^b x²dx = b³/3 까지 가기
  - 절「적분 기호 읽기」 — 소절: 적분변수도 더미라 ∫f(x)dx와 ∫f(t)dt가 같다는 것 / dx가 어느 문자로 적분하는지를 밝히는 자리라 변수가 둘이면 반드시 필요하다는 것 / Σ의 k와 ∫의 x가 같은 일을 한다는 대조표
  - 절「부호 있는 넓이」 — 소절: 넓이 자체를 구하려면 축을 지나는 자리에서 구간을 갈라 절댓값을 씌운다는 것 / 원점 대칭인 함수는 ∫_{−a}^{a}가 0이라는 것을 y=x³로 확인 / f가 g보다 크면 적분도 크다는 순서 규칙
  - 절「그림에서 읽히는 규칙 셋」 — 소절: 정적분을 폭으로 나눈 값이 그 구간의 평균 높이라는 읽기 / y=x²의 [0,1] 평균이 1/3이고 그 높이의 직사각형이 같은 넓이라는 그림 / 1/x을 1부터 2까지 재면 내려가는 함수라 오른끝이 참값보다 작은 것

- `math-basics-fundamental-theorem-of-calculus` (지금 2,920자 · 절 9)
  - 절「누적 넓이 함수」 — 소절: 왼쪽 끝 a를 다른 값으로 바꾸면 A가 상수만큼 통째로 오르내리는 것 / f의 부호가 A의 증감을, f=0인 자리가 A의 꼭짓점을 정한다는 대응 / 그림: f의 그래프와 그 아래에 A의 그래프를 세로로 나란히
  - 절「기본정리의 유도」 — 소절: 띠의 넓이를 구간의 최소·최대 높이로 위아래에서 조여 근사를 부등식으로 바꾸기 / f가 이어져 있어야 그 두 값이 같은 곳으로 모인다는 조건 / 위끝이 x²처럼 함수면 연쇄법칙이 하나 붙는 것
  - 절「F(b)−F(a)로 넓이 내기」 — 소절: 정리가 둘이라는 구별 — 「미분하면 f」와 「F로 넓이를 낸다」 / 값이 음수로 나오는 정적분을 F(b)−F(a)로 그대로 확인 / 아무 F나 골라도 답이 같다는 것을 x³/3과 x³/3+5로 나란히
  - 절「부정적분과 적분상수」 — 소절: 조건 하나가 주어지면 C가 정해지는 것 — F(0)=2 / 부정적분은 함수 무리이고 정적분은 수 하나라 기호가 닮아도 종류가 다르다는 것 / 상수배와 합 규칙이 표를 그대로 늘려 준다는 확인
  - 절「기본 적분 표」 — 소절: 표의 둘을 곱한 것은 표에 없다는 못 박기 — x·e^x은 여기서 안 된다 / 미분은 규칙으로 늘 되지만 적분은 초등함수로 안 되는 것이 있다는 경계 — e^{−x²} / 구간 안에 정의되지 않는 자리가 있으면 표를 그대로 쓰면 안 되는 것 — ∫_{−1}^{1}dx/x
  - 절「치환적분」 — 소절: 정적분의 치환은 끝도 함께 바꾼다는 것 — ∫₀¹2xe^{x²}dx = ∫₀¹e^u du / u=ax+b인 선형 치환이 상수 하나로 끝나는 것 — ∫e^{3x}dx / 분자가 분모의 도함수이면 답이 ln|f(x)|가 되는 꼴
  - 절「부분적분」 — 소절: 두 번 적용해야 끝나는 ∫x²e^x dx / 원래 적분이 오른쪽에 다시 나와 이항으로 푸는 ∫e^x sin x dx / 정적분이면 uv 자리에도 끝점을 넣는다는 표기

- `math-basics-variables-and-expressions` (지금 2,970자 · 절 7)
  - 절「글자가 하는 네 가지 일」 — 소절: 문제 안에서는 상수인데 문제 밖에서는 변하는 다섯째 자리 — y=kx의 k / 같은 글자가 모델의 파라미터와 입력으로 갈리는 대응 / 글자를 고르는 관례 — x·y는 변수, a·b·c는 상수, n·k는 정수
  - 절「항·차수·동류항」 — 소절: 문자가 둘인 항의 차수는 지수의 합이라는 것 — x²y는 3차 / 차수가 높은 것부터 적는 내림차순 관례 / 항의 수로 부르는 이름과 차수로 부르는 이름이 다르다는 정리
  - 절「대입의 손계산」 — 소절: 문자 자리에 문자를 넣기 — x 자리에 2t를 넣어 식을 다시 적기 / 문자 둘에 한꺼번에 넣을 때 각각 괄호를 치는 것 — a=3, b=−2로 a²−b² / 대입한 값에 단위를 함께 적어 두는 습관
  - 절「여러 값을 넣은 표」 — 소절: 이웃 칸끼리 뺀 차를 한 줄 더 적어 변화를 보는 것 / 그 차가 일정하면 일차, 차의 차가 일정하면 이차라는 판정 / 표 하나로는 칸 사이의 값을 알 수 없다는 한계
  - 절「관계를 적는 식 한 줄」 — 소절: 문자가 셋인 관계를 하나에 대해 풀어 적기 — S=ab에서 b=S/a / 정비례 그래프가 원점을 지나는 직선이고 k가 기울기라는 것 / 값 몇 쌍이 맞는다고 정비례가 아닌 자리 — 표만 보고 단정하지 않기
  - 절「문장을 식으로 옮기기」 — 소절: 단위가 어긋나는 자리 — 시속 60km로 t분 간 거리는 60×(t/60) / 「~마다」와 「~당」을 곱으로 옮기는 요령 / 같은 문장을 다른 글자로 적어도 같은 식이라 글자 고르기는 자유라는 것

- `math-basics-quadratic-and-parabola` (지금 2,978자 · 절 8)
  - 절「a가 정하는 두 가지」 — 소절: 같은 높이에 닿는 x가 1/√a배가 되어 폭이 좁아진다는 계산 / 그림: y=x²·2x²·½x²·−x²를 한 좌표평면에 겹치기 / a가 0이면 이차가 아니라 직선이 되어 이 글 밖으로 나가는 것
  - 절「완전제곱과 꼭짓점」 — 소절: 세 꼴에서 각각 즉시 읽히는 것을 한 표로 — 일반형·표준형·인수분해형 / 대칭축이 두 근의 평균이라 −b/2a가 근의 공식의 가운데라는 것 / 지나는 점 셋이 주어지면 계수 셋이 연립으로 정해지는 자리
  - 절「이차방정식 푸는 두 길」 — 소절: 근호 안이 완전제곱수일 때만 정수·유리수로 인수분해된다는 판정 / x²=k 꼴로 바로 벗기는 가장 짧은 길 / 근을 소수로 어림해 그래프의 x절편과 맞춰 보는 검산
  - 절「판별식이 세는 것」 — 소절: 꼭짓점의 y좌표가 −D/4a라 판별식과 같은 것을 말한다는 것 / D가 완전제곱수·양수·0·음수 네 갈래로 근의 종류까지 갈리는 표 / 계수에 문자가 든 식에서 D를 조건으로 두어 그 문자를 정하는 자리
  - 절「근과 계수의 관계」 — 소절: α²+β²을 (α+β)²−2αβ로 근을 안 구하고 계산 / 합과 곱의 부호만으로 두 근의 부호를 판정하는 표 / 삼차로 넓혀도 같은 얼개라는 것 — 이름만 짚기
  - 절「이차부등식과 그래프 읽기」 — 소절: 판별식 셋과 부등호 넷을 한 표로 묶어 해가 전부·한 점·없음이 되는 자리 / 이차식이 곧 손실이 되는 자리 — 최솟값의 위치가 −b/2a로 식에 나온다 / 두 이차부등식을 함께 만족하는 구간을 수직선에서 겹쳐 읽기

- `math-basics-average-rate-of-change` (지금 2,998자 · 절 6)
  - 절「할선과 평균변화율」 — 소절: Δ 기호와 변화량 표기를 함께 세우기 / 두 점의 순서를 바꿔도 값이 같은 것 / 단위가 붙으면 속도·연비·단가로 읽히는 자리
  - 절「구간마다 다른 답」 — 소절: 전체 평균이 구간 평균들을 폭으로 가중해 평균 낸 값이라는 계산 / 그 20 m/s를 구간 안 어디선가 실제로 지난다는 관찰 / 거꾸로 평균변화율과 한쪽 끝만 주고 나머지 끝을 묻는 문제
  - 절「h를 줄이는 표」 — 소절: a를 수로 고정하지 않고 남겨 두면 a마다 답이 하나씩 나와 새 함수가 되는 것 / 그 새 함수 2a를 원래 x²와 나란히 그린 그림 / h를 절반씩 줄일 때 답의 어긋남도 절반씩 줄어드는 것
  - 절「약분해서 확정하기」 — 소절: 켤레를 곱해야 약분되는 √x — a=4에서 1/4까지 한 줄씩 / x², x³, 1/x, √x 넷의 결과를 한 표에 모아 2a, 3a², −1/a², 1/(2√a)를 나란히 / 규칙이 눈치채이지만 확정은 31번의 몫이라는 경계
  - 절「좁히기가 실패하는 자리」(새 절) — 소절: |x|의 원점에서 오른쪽은 1, 왼쪽은 −1이라 한 곳에 안 붙는 것 / 그림: 원점에서 꺾인 |x|와 양쪽에서 온 두 할선 / h를 1e−12까지 줄이면 표가 오히려 무너지는 것 — 거의 같은 두 수를 빼며 자릿수가 날아간다 / 손으로 약분해 두면 그 손실을 통째로 피한다는 이유

- `math-basics-fractions` (지금 2,999자 · 절 8)
  - 절「끝내지 않은 나눗셈」 — 소절: 나눗셈을 끝내지 않는 이유 — 1/3을 0.333으로 적으면 세 배가 1이 안 된다 / 분수를 「몇 개 중 몇」과 「나눗셈의 답」 두 가지로 읽는 자리 / 분모가 같은 분수끼리는 수직선 위에서 자연수처럼 늘어선다는 것
  - 절「약분과 통분」 — 소절: 분모가 서로소면 최소공배수가 그냥 곱이고 배수 관계면 큰 쪽 그대로라는 지름길 / 분자와 분모를 함께 소인수분해해 한 번에 지우기 / 약분을 끝냈는지 확인하는 법 — 남은 분자와 분모의 공약수가 1
  - 절「덧셈과 뺄셈」 — 소절: 대분수끼리는 정수끼리·분수끼리 나눠 더하는 것 / 분수 부분이 모자라 정수에서 1을 빌려 오는 받아내림 — 3¼ − 1¾ / 세 분수 이상은 분모를 한꺼번에 맞추는 편이 짧다는 것
  - 절「두 번 자르는 곱셈」 — 소절: 1보다 작은 수를 곱하면 값이 줄어든다는 것 — 나눗셈 절의 짝 / 자연수를 분모 1인 분수로 보아 같은 규칙에 넣기 / 「전체의 2/3의 1/2」처럼 「~의」가 곱으로 옮겨지는 읽기
  - 절「나눗셈과 역수」 — 소절: 나눗셈의 두 뜻 — 「몇 개 들어가는가」와 「똑같이 나누면 하나가 얼마인가」 / 분수를 자연수로 나누면 조각을 다시 잘라 분모가 커지는 것 / 나눗셈을 분수 한 줄로 겹쳐 적으면 번분수가 된다는 것
  - 절「대소 비교와 검산」 — 소절: 분자를 맞춰 비교하면 분모가 큰 쪽이 작다는 다른 길 / 1에서 얼마나 모자란가로 견주기 — 3/4은 1/4, 5/7은 2/7 모자라다 / 소수로 어림해 답이 말이 되는지 보는 검산 — 0.75와 0.714

- `math-basics-linear-systems-and-elimination` (지금 3,005자 · 절 8)
  - 절「첨가행렬로 옮겨 적기」 — 소절: 빠진 미지수 자리에 0을 반드시 적어야 하는 것 / 행이 방정식, 열이 미지수라 크기 m×n이 곧 문제의 모양이라는 읽기 / 우변이 여럿이면 막대 오른쪽에 열을 여러 개 붙여 한꺼번에 소거하는 것
  - 절「해를 바꾸지 않는 세 연산」 — 소절: 셋 다 되돌릴 수 있어 해집합이 양쪽으로 보존된다는 근거 / 열을 맞바꾸면 미지수의 순서가 섞여 답이 어긋나는 것 / 두 연산을 한 줄에 몰아 쓰다 같은 행을 두 번 쓰는 흔한 실수
  - 절「가우스 소거와 사다리꼴」 — 소절: 피벗 자리가 0이면 아래 행과 맞바꾸는 걸음 / 맞바꿀 행도 없으면 그 열을 건너뛰고 다음 열로 가는 것 / 소거의 연산 횟수가 n³/3쯤이라 미지수가 열 배면 계산이 천 배라는 규모 감각
  - 절「후진 대입」 — 소절: 후진 대입은 n²쯤이라 전체 비용을 소거가 지배한다는 것 / 검산이 잡아 주는 것과 못 잡는 것 — 넣어 맞으면 그 해는 맞지만 다른 해가 없다는 보장은 아니다 / 소수로 나누면 자릿수가 흔들리므로 분수로 끌고 가는 손계산 요령
  - 절「마지막 줄이 정하는 세 갈래」 — 소절: 자유롭게 정할 수 있는 미지수의 개수가 미지수 수에서 피벗 수를 뺀 값이라는 셈 / 식이 미지수보다 적으면 해가 하나일 수 없다는 결론 / 그림: 피벗 수와 미지수 수를 견주는 세 경우
  - 절「이름만 짚는 둘」 — 소절: 계수를 0.001만 흔들어도 해가 크게 튀는 2×2를 손으로 한 번 / 그 흔들림의 크기를 재는 값에 조건수라는 이름이 붙는다는 것 / LU가 소거의 기록이라 우변만 바뀌면 다시 소거하지 않아도 되는 자리

- `math-basics-determinant-and-inverse` (지금 3,007자 · 절 10)
  - 절「행렬식의 정의」 — 소절: y도 끝까지 풀어 같은 분모가 나오는 것을 확인하고 두 해를 한 줄로 적어 두기 / 대각행렬과 삼각행렬은 대각 성분의 곱이라 즉답이라는 것 / det(kA)가 k배가 아니라 kⁿ배인 이유 — 두 열이 함께 늘어난다
  - 절「행렬식이 0이라는 것」 — 소절: 3×3에서는 세 열이 한 평면에 있어 부피가 0이라는 대응 / 행으로 읽어도 같은 결론이 나오는 것과 det Aᵀ = det A의 연결 / 성분이 멀쩡한데 det가 0에 아주 가까운 예 — 거의 특이한 자리
  - 절「여인수 전개」 — 소절: 행 연산이 det를 어떻게 바꾸는가 — 맞바꾸면 부호, 상수배는 그 배, 다른 행을 더하면 그대로 / 소거로 삼각꼴을 만든 뒤 대각을 곱해 det를 구하는 더 빠른 길 / 여인수 전개는 n!에 비례해 커져 5×5만 넘어도 못 쓴다는 규모
  - 절「짚고 넘어갈 성질 둘」 — 소절: det(A⁻¹)=1/det A를 곱 성질에서 한 줄로 / det(A+B)에 대응하는 성질은 없다는 못 박기 — 2×2 반례 하나 / 곱의 성질을 넓이 배율의 곱으로 읽기
  - 절「역행렬과 2×2 공식」 — 소절: 역행렬이 하나뿐임을 둘을 가정해 한 줄로 보이기 / 수반행렬이라는 이름과 2×2 공식이 그 가장 작은 경우라는 것 / 역행렬이 있으면 해가 유일하다는 것을 양변에 곱해 확인
  - 절「가우스-조던과 x=A⁻¹b」 — 소절: 왼쪽이 I가 안 되고 0인 행이 나오면 역행렬이 없다는 판정 / (A⁻¹)⁻¹=A와 (kA)⁻¹=A⁻¹/k / 역행렬을 실제로 만들어 두는 것이 이득인 드문 자리 — 같은 A에 우변이 아주 많을 때

- `math-orthogonality-and-projection` (지금 3,018자 · 절 8)
  - 절「내적이 0이라는 정의」 — 소절: 영벡터가 모든 것과 직교한다는 관례와 그것이 정의를 깨지 않는 이유 / 서로 직교한 0 아닌 벡터들은 자동으로 일차독립이라는 한 줄 증명 / 차원이 높아지면 무작위 두 벡터가 거의 직교한다는 것 — 코사인이 1/√d 규모
  - 절「한 벡터 위로의 사영」 — 소절: 스칼라 사영 ‖a‖cosθ와 벡터 사영을 갈라 두기 / 잔차의 노름이 곧 점과 직선 사이 거리라는 대응 / b의 길이를 바꾸거나 −b로 뒤집어도 사영이 그대로인 것 — 쓰이는 것은 방향뿐이다
  - 절「부분공간 위로의 사영」 — 소절: AᵀA가 가역일 조건이 A의 열이 일차독립인 것 / 열이 종속이면 x̂은 하나로 안 정해지는데 사영 Ax̂은 그대로라는 구별 / I−P도 사영 행렬이고 그것이 직교여공간으로 내린다는 것 / 그람-슈미트가 사영을 되풀이해 정규직교기저를 만드는 절차이고 그것을 행렬로 적으면 QR이 되는 자리 — QᵀQ=I인데 QQᵀ는 I가 아닐 수 있다
  - 절「최소제곱의 기하」 — 소절: 최소 잔차제곱합이 ‖b‖²−‖Pb‖²라 피타고라스 한 줄로 나오는 것 / 그 비를 설명된 몫으로 읽으면 결정계수가 되는 자리 / 정규방정식을 그대로 푸는 것이 조건수를 제곱하므로 QR로 가는 이유
  - 절「코드로 확인하기」 — 소절: 차원을 올려 가며 무작위 벡터 쌍의 코사인을 재어 1/√d와 맞춰 보기 / A와 AᵀA의 조건수를 나란히 찍어 제곱을 확인 / lstsq와 정규방정식 풀이의 답을 견주기
  - 절「직교 초기화가 강제하는 것」 — 소절: 정사각이 아닌 W에서는 한쪽 방향만 보존된다는 것 / 활성함수가 크기를 줄이므로 gain을 곱해 보정하는 자리 — ReLU에 √2 / 학습이 진행되며 특이값이 1에서 흩어지는 것을 무엇으로 재는가
  - 절「멀티헤드와의 구별」 — 소절: 헤드를 지워도 성능이 크게 안 떨어진다는 관찰이 겹침의 증거로 읽히는 자리 / 겹치는 것이 늘 손해는 아니라는 것 — 같은 것을 여럿이 보면 잡음에 강해진다 / 직교를 손실 항으로 강제한 변형이 있고 그때 무엇을 잃는가

- `math-taylor-hessian-and-curvature` (지금 3,077자 · 절 7)
  - 「연습 문제」 절 — 2변수 함수의 헤세 손계산 3문항, 고윳값 부호로 최소·최대·안장 판정 3문항, 조건수 계산과 등고선 축비 잇기 2문항
  - 「고윳값은 그 방향의 곡률이다」를 소절 셋으로 — 「양정치면 그릇」 「부호가 섞이면 안장」 「0이 끼면 2차로는 판정이 안 된다」
  - 새 소절 「안장점이 고차원에서 왜 흔한가」 — d개 부호가 전부 같아야 최소인데 그 확률이 작다는 셈. '국소 최소에 갇힌다'는 통념을 이 셈으로 고쳐 놓기
  - 「근가 얼마나 맞는가」 절에 소절 「신뢰 구간을 어떻게 잡는가」 — 2차 근사가 깨지는 거리와 그것이 신뢰영역 방법의 근거라는 연결

- `math-dot-product-and-cosine` (지금 3,142자 · 절 8)
  - 「연습 문제」 절 — 세 지표(내적·코사인·L2)로 순위를 매겨 갈리는 예 직접 만들기 2문항, 코시-슈바르츠 등호 조건 1문항, ‖a−b‖² 전개 손계산 2문항, 정규화 뒤 셋의 순위가 같아짐을 수로 확인 2문항
  - 「내적의 두 얼굴」을 소절 셋으로 — 「성분끼리 곱해 더한다」 「‖a‖‖b‖cosθ」 「둘이 같은 이유」(이미 있는 소절 하나를 살려 셋으로 맞춘다)
  - 새 소절 「내적이 음수라는 것」 — 사잇각이 90도를 넘는 자리, 임베딩에서 '반대 뜻'이 아니라 '방향이 반대'라는 구별
  - 새 소절 「차원이 커지면 코사인이 어디로 모이는가」 — 무작위 두 벡터가 거의 직교한다는 관찰을 수치 한 줄로. 중급 66번 고차원 기하로 넘길 씨앗

- `math-gradient-descent-and-lr-bound` (지금 3,152자 · 절 7)
  - 절「최급강하 방향의 유도」 — 소절: 「가장 가파른」이 노름을 유클리드로 쟀을 때만 성립한다는 단서 / 최대 노름으로 재면 답이 부호 함수가 되어 sign-SGD가 나오는 자리 / 좌표의 축척을 바꾸면 방향이 함께 바뀌는 것 — 방향은 좌표계에 딸린 값이다
  - 절「L-매끄러움」 — 소절: L이 아예 없는 손실 — ReLU와 절댓값처럼 기울기가 튀는 자리 / 실제 손실은 구간마다 L이 크게 달라 전역 L이 쓸모없이 크다는 것 / L을 재는 손쉬운 방법 — 두 점의 그래디언트 차 노름을 거리로 나눈 값의 최대
  - 절「학습률 상한 2/L」 — 소절: 손실이 매 스텝 준다는 것과 최솟값에 닿는다는 것이 다른 진술이라는 구별 / 감소량을 T스텝 더하면 가장 작은 그래디언트 노름이 1/√T 규모로 줄어든다는 결론 / 미니배치에서는 분산 항이 더 붙어 이 상한이 그대로 가지 않는다는 단서
  - 절「이차함수에서의 경계」 — 소절: x는 부호를 오가는데 f(x)는 단조로 줄어드는 구간 — 1/L과 2/L 사이 / 표에 f값 열을 하나 더 붙여 그 갈림을 눈으로 확인 / 실제 학습에서 손실이 이 선 근처를 오래 맴돈다는 관찰에 붙은 이름
  - 절「조건수와 반복 수」 — 소절: κ를 낮추는 쪽의 처방 — 입력을 표준화하면 헤세의 축이 고르게 된다 / 좌표마다 다른 학습률을 주는 대각 전처리가 Adam이 하는 일과 겹치는 자리 / 같은 문제를 다시 매개화해 골짜기를 펴는 다른 길
  - 절「코드로 확인하기」 — 소절: 유한차분으로 헤세를 만들고 λmax를 재어 2/L을 직접 구하기 / 이차가 아닌 손실에서 그 경계가 어디까지 맞는지 확인 / 그래디언트에 잡음을 섞어 같은 η에서 손실이 튀는 것을 보기

- `math-basics-surfaces-and-contours` (지금 3,218자 · 절 7)
  - 절「같은 함수를 보는 세 언어」 — 소절: 등고선 값을 일정한 간격으로 잡는 관례라야 촘촘함이 기울기를 뜻한다는 것 / 색으로 높이를 칠하는 넷째 방법과 그때 색 눈금이 하는 일 / 단면을 y=0이 아니라 y=1에서 잘라 z=x²+1을 얻는 것
  - 절「네 함수의 등고선」 — 소절: 등고선끼리 절대 만나지 않는 이유 — 한 점에 높이가 둘일 수 없다 / 가장 단순한 등고선인 z=2x+3y — 나란한 직선 무리 / 그림: 네 함수의 단면을 x=0과 y=0으로 잘라 나란히
  - 절「편미분」 — 소절: 한 변수만 h만큼 밀어 적는 극한 정의 / ∂와 d를 갈라 쓰는 표기의 뜻 / 등고선이 촘촘한 방향에서 그 변수의 편도함수가 크다는 대응과, 그릇·봉우리·안장의 원점이 모두 둘 다 0인데 안장만 최소도 최대도 아니라는 것 / 이차 편도함수와 미분 순서를 바꿔도 같아지는 자리
  - 절「이중적분」 — 소절: 격자로 잘라 기둥을 더하는 리만합으로 부피를 정의하는 자리 / f(x,y)=g(x)h(y)로 갈리면 적분도 두 곱으로 갈리는 것 / 부피를 밑넓이로 나누면 그 구간의 평균 높이라는 읽기
  - 절「그림이 끝나는 자리」 — 소절: 세 변수는 등위면으로 본다는 것 — w=c가 선이 아니라 면이 된다 / 한 번에 두 축만 흔들어 단면을 보는 실무 방식 / 계산은 그대로 확장되지만 눈으로 고르는 일이 없어진다는 경계

- `math-variance-and-sampling-error` (지금 3,221자 · 절 8)
  - 절이 여덟이다. 「분산과 공분산」 한 절에 소절 둘로 접고, 「σ²/n과 대수의 법칙」을 한 절에 소절 둘로 접어 여섯으로 줄인다
  - 「연습 문제」 절 — 주어진 표본으로 분산·표준편차·공분산 손계산 3문항, 독립 합의 분산 가법성 2문항, 배치 크기를 4배로 늘리면 표준오차가 얼마가 되는지 1문항, n−1로 나눈 값과 n으로 나눈 값 비교 1문항
  - 새 소절 「상관계수는 공분산을 정규화한 것」 — 공분산에 단위가 붙어 크기를 못 읽는다는 문제와 ρ=Cov/(σ_xσ_y). 지금 이름이 안 붙어 있다
  - 새 소절 「독립이 아니면 어떻게 되는가」 — 가법성이 깨지고 2Cov 항이 남는 자리를 수로. 미니배치 표본이 상관될 때 σ²/n이 낙관적이라는 결론까지

- `math-basics-inequalities-and-signs` (지금 3,223자 · 절 10)
  - 절「부등호 넷」 — 소절: 이상·이하·초과·미만이 각각 어느 기호인지의 대응표 / ≠는 순서를 말하지 않으므로 부등호가 아니라는 것 / 두 부등호를 한 줄에 겹쳐 적는 a<x≤b의 읽기
  - 절「구간으로 나오는 해」 — 소절: 해를 집합 기호 {x | x<4}로 적는 다른 표기 / 구간의 교집합과 합집합 기호 / 해가 한 점뿐이거나 아예 없는 구간을 적는 법
  - 절「방향이 뒤집히는 한 자리」 — 소절: 둘 다 양수일 때 역수를 취하면 순서가 뒤집히는 것 — 2<5인데 1/2>1/5 / 두 부등식은 더할 수 있지만 뺄 수는 없다는 것 / 곱하기는 양쪽이 음이 아닐 때만 그대로 간다는 조건
  - 절「일차부등식과 연립」 — 소절: 분모에 미지수가 있으면 부호를 몰라 그대로 곱할 수 없는 자리 — 1/x>2 / 「또는」으로 묶인 연립은 합집합이라 칠한 데를 다 읽는 것 / 세 부등식이 겹치는 구간을 한 수직선에서
  - 절「거리로 읽는 절댓값 부등식」 — 소절: 안에 계수가 붙으면 먼저 묶어 내는 것 — |2x−6|<4 / 경우를 갈라 풀어도 같은 답이 나온다는 확인 / 벡터에서도 「가깝다」를 같은 꼴로 적는다는 예고
  - 절「제곱이 만드는 가짜 해」 — 소절: 근호가 든 부등식을 끝까지 푸는 한 예 — √(x+2)<x / 제곱하기 전에 근호 안이 0 이상인 조건을 먼저 적어 두는 순서 / 제곱으로 답이 늘 수도 줄 수도 있다는 두 방향
  - 절「부등식을 세우는 밑천」 — 소절: 두 식의 차를 만들어 부호를 보는 가장 기본 수단 / 합이 일정하면 곱이 언제 가장 큰가 — 둘레가 정해진 직사각형의 넓이 / 등호가 성립하는 자리를 못 찾으면 그 부등식이 아직 조여지지 않았다는 신호

- `math-basics-natural-numbers-and-place-value` (지금 3,231자 · 절 8)
  - 절「자릿값과 위치 기수법」 — 소절: 10을 밑으로 쓰는 것이 약속이라 2진법·16진법도 같은 얼개라는 것 / 13을 1101로 적어 자리마다 값을 세기 / 그림: 같은 수 13을 십진 자리와 이진 자리로 나란히 펴기
  - 절「받아올림과 받아내림」 — 소절: 받아올림이 연달아 나는 999+1과 그것을 거꾸로 한 1000−1 / 계산하기 전에 어림으로 답의 자릿수를 잡아 두는 습관 — 476+258은 700 언저리 / 뺄셈의 검산은 답에 뺀 수를 도로 더해 보는 것
  - 절「자리를 맞춰 쓰는 이유」 — 소절: 소수점이 들어오면 맞추는 기준이 오른쪽 끝이 아니라 소수점이라는 것 / 세 수 이상을 더할 때 받아올림이 2가 되는 자리 / 자리를 한 칸 잘못 맞춘 답이 얼마나 어긋나는지 한 번
  - 절「곱셈 세로셈의 두 줄」 — 소절: 세 자리 × 두 자리면 줄이 몇 개이고 자리별 곱이 몇 개인지 세기 / 가운데 자리가 0인 203×45에서 한 줄이 통째로 비는 것 / 곱의 자릿수 어림 — 세 자리 × 두 자리는 네 자리 아니면 다섯 자리
  - 절「몫과 나머지」 — 소절: 나머지가 같은 것끼리 묶는 셈 — 요일과 시계가 그 계산이라는 것 / 나누는 수가 더 크면 몫이 0이고 나머지가 그대로인 자리 / 나머지만 필요할 때 자리별로 줄여 가며 구하는 요령
  - 절「순서와 큰 수 읽기」 — 소절: 묶는 순서를 바꾸는 것과 자리를 바꾸는 것이 다른 성질이라는 구별 / 세 자리마다 콤마를 찍는 표기와 네 자리로 끊어 읽는 우리말이 어긋나는 자리 / 조와 경까지 넷씩 올라가는 이름표

- `math-basics-what-equals-means` (지금 3,233자 · 절 6)
  - 절「명령과 주장」 — 소절: 코드가 =와 ==를 갈라 둔 것이 이 차이를 이미 표기로 인정한 것이라는 읽기 / 코드에도 주장의 등호가 있다는 것 — assert가 참·거짓을 묻는 자리 / 이름을 한 번만 붙이는 언어에서는 =가 곧 정의라는 것
  - 절「세 갈래로 가려 읽기」 — 소절: 항등식임을 보이는 일과 방정식을 푸는 일이 서로 다른 작업이라는 대비 / 정의를 :=나 ≡로 적어 갈래를 기호로 밝히는 관례 / 갈래를 잘못 읽으면 무엇이 어긋나는지 한 예
  - 절「등호 사슬」 — 소절: 부등호가 섞인 사슬에서 결론이 가장 약한 쪽을 따른다는 것 — a=b≤c=d면 a≤d / 여러 줄로 적을 때 등호를 줄 앞에 맞춰 세우는 관례 / 사슬이 길면 가운데를 잘라 두 사슬로 나눠 확인하는 것
  - 절「양변에 같은 일 하기」 — 소절: 되돌릴 수 있는 조작과 없는 조작의 갈림 — 제곱은 되돌릴 수 없다 / 양변에 0을 곱하면 등호는 지켜지는데 조건이 사라지는 것 / 양변에 같은 함수를 씌우는 것도 같은 성질이라는 것 — 로그를 취하기
  - 절「등호를 닮은 기호 넷」(새 절) — 소절: 근사 ≈와 비례 ∝가 각각 무엇을 주장하는가 / 정의 기호 :=와 ≡를 쓰는 자리 / 따름 ⇒와 동치 ⟺는 값이 아니라 문장 사이의 관계라는 것 / 「따라서」를 =로 적는 흔한 오류와 그때 무엇이 무너지는가

- `math-chain-rule` (지금 3,247자 · 절 6)
  - 「연습 문제」 절 — 3층 합성 손미분 3문항, 갈림길 있는 그래프에서 경로별 곱·경로합 2문항, 0.9와 1.1을 50번 곱해 소멸·폭발을 수로 확인 2문항
  - 「계산 그래프 위에서 손으로 끝까지」를 소절 셋으로 — 「순전파로 값 채우기」 「역방향으로 비율 곱하기」 「같은 노드가 두 번 쓰이면 더한다」
  - 새 소절 「왜 역방향으로 도는가」 — 순방향 곱과 역방향 곱의 비용이 왜 다른지(출력 하나·입력 수십억이면 역방향이 싸다). 34번 VJP 글에 넘기기 전 한 문단
  - 「곱이 길어지면 값이 무너진다」에 소절 「그래서 무엇으로 막는가」 — 잔차 연결·정규화·클리핑이 각각 이 곱의 어디에 개입하는지 이름만 붙이기

- `math-basics-exponential-function` (지금 3,257자 · 절 9)
  - 절「지수 자리에 앉는 변수」 — 소절: 거듭제곱함수와 지수함수를 이름으로 갈라 부르는 것 / 밑이 음수면 값이 부호까지 튄다는 것 — (−2)¹과 (−2)² / 밑을 정하면 곡선이 하나로 정해져 밑이 곧 함수의 이름표가 되는 자리
  - 절「표를 채워 그리기」 — 소절: 세로 눈금을 1·2·4·8처럼 곱으로 매기면 점이 고르게 서는 것 / 그림: 같은 2ˣ을 보통 눈금과 곱 눈금에 나란히 그리기 / 그 눈금에 붙는 이름은 22번의 몫이라는 경계
  - 절「밑이 1보다 작을 때」 — 소절: 밑이 클수록 오른쪽에서 더 가파르고 모든 곡선이 (0,1)에서 만나는 것 / 밑 a와 밑 1/a의 곡선이 y축을 거울로 마주 보는 짝 / 밑이 1에 가까우면 거의 가로선이 되는 자리
  - 절「옮긴 곡선 읽기」 — 소절: 세로로 3배 늘인 3·2ˣ이 사실 가로로 옮긴 것과 같다는 것 / 그래서 지수함수는 세로 확대와 가로 이동을 구별할 수 없다는 결론 / 점근선이 y=0이 아닌 곡선에서 값의 범위를 읽는 순서
  - 절「한 칸당 배율」 — 소절: 배율이 정해지면 두 배가 되는 데 걸리는 칸수도 정해진다는 것 / 「70의 법칙」 — 해마다 r%면 대략 70/r년에 두 배 / 에폭마다 학습률을 γ배로 줄이는 스케줄이 이 함수 그대로라는 것
  - 절「x²과 2ˣ의 성장 경주」 — 소절: 밑이 1보다 크기만 하면 어떤 거듭제곱도 결국 진다는 것 — 1.01ˣ과 x¹⁰⁰ / 지수보다 더 빠른 n!과 계산 비용의 자리 / 줄어드는 쪽의 경주 — 2^{−x}와 1/x²
  - 절「e가 나오는 자리」 — 소절: (1−1/n)ⁿ이 1/e로 가는 짝 계산 / n개에서 중복을 허용해 n개를 뽑으면 대략 63%만 뽑히는 것이 그 값이라는 자리 / e의 다른 얼굴들은 뒤로 넘기고 지금은 수 하나로 두는 경계

- `math-baseline-and-advantage` (지금 3,282자 · 절 6)
  - 절「점수 함수의 기댓값 0」 — 소절: 답이 연속이면 합이 적분이 되고 미분과 순서를 바꿀 수 있어야 한다는 단서 / 이 벡터의 공분산이 피셔 정보라는 이름을 얻는 자리 / 세 답짜리 예에서 ∇logπ 셋을 확률로 가중해 실제로 더해 0을 보기
  - 절「빼도 되는 것의 조건」 — 소절: b가 θ에 딸린 값이면 미분이 하나 더 붙어 코드의 detach가 필요해지는 자리 / 학습으로 얻은 V(x)가 부정확해도 편향이 안 생기는 이유 — 조건은 정확함이 아니라 y와 무관함이다 / b를 스텝마다 바꿔도 되는 것
  - 절「최적 베이스라인」 — 소절: 총분산이 아니라 성분마다 최적화하면 파라미터별로 다른 b가 나온다는 것 / 가중치 ‖∇logπ‖²가 고르면 b*가 그냥 평균과 같아지는 자리 / 베이스라인으로는 못 내리는 분산의 바닥이 무엇인가
  - 절「그냥 평균을 쓰는 이유」 — 소절: 표본으로 추정한 평균 자체가 흔들려 그만큼 이득이 깎이는 것 / 그룹 크기가 작으면 그 흔들림이 커지는 대응 / 표본 평균을 쓰면 b가 y에 딸리게 되어 다음 절의 어긋남이 생기는 자리
  - 절「GRPO의 그룹 평균」 — 소절: 그룹의 점수가 전부 같으면 어드밴티지가 통째로 0이라 그 프롬프트가 아무것도 안 보태는 것 / 채점이 맞고 틀림 둘뿐일 때 그 일이 자주 나는 자리 / G를 키우면 어긋남과 잡음은 줄지만 표본 비용이 그만큼 든다는 맞바꿈
  - 절「점수의 축척이 하는 일」(새 절) — 소절: 점수를 100배 하면 어드밴티지도 100배라 학습률을 100배 한 것과 같다는 것 / 상수를 더하는 것은 정리가 지켜 주고 곱하는 것은 안 지켜 준다는 갈림 / 표준편차가 0에 가까운 그룹에서 분모의 1e−4가 하는 일 / 어드밴티지를 잘라 두는 변형이 같은 축척을 다른 손잡이로 잡는다는 것

- `math-basics-functions-and-graphs` (지금 3,284자 · 절 11)
  - 절「입력 하나에 출력 하나」 — 소절: 같은 함수를 말·표·식·그래프 네 가지로 줄 수 있다는 것 / 입력이 둘 이상이거나 출력이 벡터인 대응도 함수라는 경계 / 표에서 함수인지 가리는 연습 — 같은 입력 줄이 둘인가
  - 절「f(x) 표기 읽기」 — 소절: 이름을 f 말고 다른 글자로 두는 것 — g, h, σ, ReLU / x ↦ 3x+1 화살표 표기 / 함수를 함수에 넣는 f(g(x))와 순서가 결과를 바꾸는 것
  - 절「정의역·공역·치역」 — 소절: 식이 아니라 문제가 정의역을 자르는 자리 — 사람 수는 자연수 / 치역을 그래프에서 y축으로 그림자를 내려 읽는 방법 / 같은 식이라도 정의역이 다르면 다른 함수라는 것을 그래프로
  - 절「f(x+1)과 f(x)+1의 갈림」 — 소절: f(a+b)와 f(a)+f(b)가 같아지는 함수가 드물다는 것 — 그것이 곧 일차함수다 / √(a+b)와 √a+√b를 수로 갈라 보기 / 넣기 전에 손대는 것이 가로 방향, 나온 뒤에 손대는 것이 세로 방향이라는 정리
  - 절「좌표평면과 세로선 판정」 — 소절: 사분면 넷과 각 칸의 부호 / 축의 눈금을 다르게 잡으면 같은 함수가 달라 보이는 것 / 가로선 판정 — 한 번씩만 만나면 거꾸로 돌릴 수 있다는 예고
  - 절「다섯 곡선 손으로 그리기」 — 소절: 여섯째로 상수함수 y=2를 넣어 세로선과 대비시키기 / 각 곡선이 지나는 점 둘씩만 기억해 개형을 세우는 요령 / 다섯 곡선의 정의역과 치역을 한 표로 모으기
  - 절「조각별로 정의된 함수」 — 소절: max(0,x)로 적는 ReLU가 조각별 함수라는 것 / 끊긴 자리와 꺾인 자리를 갈라 보는 눈 / 조건이 겹치거나 비면 함수가 아니게 되는 자리

- `math-eigenvalues` (지금 3,289자 · 절 7)
  - 절「방향이 변하지 않는 축」 — 소절: λ=0인 고유벡터가 곧 0으로 뭉개지는 방향이고 det=0과 같은 말이라는 것 / λ=1이면 그 방향이 통째로 고정된다는 읽기 / 고유벡터를 노름 1로 정규화하는 관례와 부호가 정해지지 않는 것
  - 절「특성방정식」 — 소절: n×n이면 n차 다항식이라 중복까지 세면 근이 n개라는 것 / 삼각행렬은 대각 성분이 그대로 고윳값이라 즉답이라는 자리 / 수치 계산은 이 다항식을 풀지 않는다는 것 — 근 찾기가 불안정해 다른 알고리즘을 쓴다
  - 절「고유공간이라는 부분공간」 — 소절: 서로 다른 고윳값의 고유벡터는 자동으로 일차독립이라는 것 / 중근이면 고유공간의 차원이 1일 수도 2일 수도 있다는 갈림 / 대표 벡터를 어떻게 골라도 부분공간은 같다는 확인
  - 절「풀지 않고 갈래 읽기」 — 소절: n×n에서도 대각합이 고윳값의 합, 행렬식이 곱이라는 것 / 그 두 값이 기저를 바꿔도 안 변해 갈래 읽기가 좌표계와 무관하다는 것 / 각 행에서 고윳값이 어디쯤에 있는지 대략 가두는 눈어림
  - 절「대각화」 — 소절: A^k 말고 다른 것도 대각에 그대로 먹인다는 것 — √A와 exp(A) / 가장 큰 |λ|가 A^k의 운명을 정해 그 값에 스펙트럼 반지름이라는 이름이 붙는 자리 / P가 정규직교이면 P⁻¹이 전치라 계산이 통째로 가벼워지는 것
  - 절「대각화가 안 되는 두 경우」 — 소절: 두 고유 방향이 거의 평행하면 P의 조건수가 커져 대각화가 수치로 무너지는 것 / 중근이라고 늘 안 되는 것은 아니라는 확인 — 단위행렬 / 대칭이 아니어도 고유벡터가 n개면 되므로 대칭은 충분조건일 뿐이라는 것
  - 절「코드로 확인하기」 — 소절: 대칭 행렬에는 eigh를 쓰는 이유 — 실수와 직교가 보장된다 / 고유벡터의 부호와 순서가 실행마다 달라질 수 있다는 것 / 전단처럼 대각화가 안 되는 행렬에서 eig가 무엇을 돌려주는지

- `math-matmul-and-shape-arithmetic` (지금 3,323자 · 절 6)
  - 절「곱 하나, 시선 넷」 — 소절: 네 시선이 ∑_k∑_i∑_j 세 합의 순서를 바꾼 것뿐이라 값이 같을 수밖에 없다는 확인 / 어느 시선이든 곱셈 횟수는 m·n·p로 같고 2mnp FLOPs인 것 / 외적 시선을 부분합으로 쌓으면 k를 조각내 따로 더할 수 있어 큰 축약을 나누는 근거가 되는 자리
  - 절「shape 산수의 세 규칙」 — 소절: 브로드캐스트가 크기 1인 축을 늘리는 구체적 자리 — 어텐션 마스크 (B,1,T,T)가 (B,H,T,T)에 맞춰지는 예 / 축이 하나뿐인 벡터에는 앞뒤로 1을 임시로 붙였다 떼므로 `x @ W`와 `W @ x`가 둘 다 통하는 것 / 규칙이 조용히 통과하는 자리 — (T,1)과 (1,T)가 만나 오류 없이 (T,T)가 되는 브로드캐스트
  - 절「멀티헤드의 축 흐름」 — 소절: view(B,T,12,64)와 view(B,T,64,12)가 둘 다 shape은 맞지만 뒤쪽은 오류 없이 다른 값을 내는 자리 / d_h가 손잡이가 아니라 d_model/H로 정해지고 H가 768을 나누어떨어져야 하는 제약 / 텐서가 데이터 한 벌과 축마다의 보폭(stride) 두 벌이라 transpose는 보폭 순서만 바꿔 두고, 그래서 view가 거부하고 contiguous()·reshape이 복사 비용을 무는 것 / GQA·MQA에서 K·V의 헤드 수가 Q보다 작아 규칙 3의 브로드캐스트로 맞춰지는 자리
  - 절「점수 표의 메모리와 연산량」 — 소절: (B,H,T,T)를 원소 수로 세기 — B=8·H=12·T=2048이면 4.03억 개, fp16으로 805MB / T가 두 배면 네 배가 되는 이유와 그것이 문맥 길이의 벽인 자리 / 같은 shape 규칙으로 FLOPs 세기 — QKᵀ가 2·B·H·T²·d_h이고 가중치@V가 같은 값 / 그림: T를 512·1024·2048·4096으로 올릴 때 활성값 메모리와 FLOPs가 각각 몇 배로 뛰는지 두 벌 막대
  - 절「shape 오류 읽기」 — 소절: 오류가 안 나고 지나가는 어긋남 — 배치 축이 1이라 브로드캐스트로 맞아 버리는 자리 / 메시지에 안 적히는 앞 축까지 확인하는 순서(뒤 두 축 → 앞 축 → dtype) / 실행 전에 모양만 흘려 보는 방법 — 실제 값을 만들지 않고 shape만 굴려 보기
  - 절「einsum과 축 이름」 — 소절: 문자열 하나로 전치·대각·축 합까지 적히는 것('ij->ji', 'ii->i', 'ij->i') / 텐서가 셋 이상이면 계산 순서에 따라 중간 결과 크기가 달라져 비용이 갈리는 자리 / 축 이름을 먼저 정하고 식을 적으면 transpose를 넣을 일이 없어지는 습관

- `math-matrix-powers-and-power-iteration` (지금 3,324자 · 절 7)
  - 절「고유기저에서 펼친 A^k」 — 소절: 고유벡터가 기저를 못 이루면 이 전개 자체가 성립하지 않는다는 단서와 [[1,1],[0,1]]^k=[[1,k],[0,1]] / ρ=1인데 크기가 k에 비례해 자라는 그 예에서 「크기를 정하는 것은 고윳값뿐」이 깨지는 것 / 무작위 행렬에서는 대각화가 거의 언제나 되지만 두 고윳값이 가까우면 수치적으로 그 경계에 붙는 자리 / 그림: 조르당 블록에서 k에 비례해 자라는 성분과 지수로 자라는 성분을 로그 눈금에 겹쳐 그리기
  - 절「스펙트럼 반지름」 — 소절: ρ(A)=lim‖A^k‖^{1/k}는 극한이라 유한 k에서는 ‖A^k‖가 ρ^k를 넘을 수 있다는 것 / 그래서 「ρ<1이면 언제나 줄어든다」가 아니라 「충분히 큰 k에서 줄어든다」로 읽어야 하는 자리 / 성분이 전부 양수인 행렬에서 행합의 최솟값과 최댓값 사이에 ρ가 갇힌다는 손쉬운 상하한
  - 절「순환망 기울기의 자릿수」 — 소절: 실제 역전파에는 W만이 아니라 diag(σ'(z))W가 곱해져 활성함수의 도함수가 이득을 한 번 더 깎는 것 / tanh의 |σ'|≤1이 그 곱의 ρ를 눌러 폭발보다 소실 쪽이 흔한 이유 / 스텝마다 diag가 달라 같은 행렬의 거듭제곱이 아니라 곱이므로 ρ 하나로는 못 읽고 상한만 남는 자리
  - 절「거듭제곱법」 — 소절: 나눈 값 대신 레일리 몫 xᵀAx/xᵀx로 고윳값을 추정하면 — 벡터가 얼마나 A 방향으로 늘어났는지를 재는 값이다 — 대칭 행렬에서 오차가 제곱으로 작아지는 것 / 첫 성분으로 나누는 정규화가 그 성분이 0에 가까우면 터지고 노름으로 나누면 안전한 것 / 멈출 때를 정하는 잔차 ‖Ax−λx‖와 그 값이 오차와 어떻게 이어지는지
  - 절「수렴 속도와 스펙트럼 갭」 — 소절: A−μI로 시프트해 노리는 쪽 갭을 벌리는 것과 μ를 고르는 감각 / A⁻¹에 같은 절차를 돌려 최소 고윳값을 얻는 역거듭제곱법 / 고윳값이 켤레쌍이라 |λ₁|=|λ₂|인 자리에서는 한 스텝이 아니라 두 스텝을 묶어야 무엇이 도는지 보이는 것
  - 절「코드로 확인하기」 — 소절: 512×512 무작위 대칭 행렬에서 거듭제곱법과 전체 고윳값 분해의 시간·정확도 대조 / 레일리 몫을 함께 찍어 수렴이 한 자릿수 빨라지는 것 / 시프트 μ를 넣었을 때 같은 정확도에 드는 스텝 수가 줄어드는 표

- `math-random-projection-and-jl` (지금 3,358자 · 절 7)
  - 절「무작위 사영의 정의」 — 소절: 1/k를 분산에 넣는 것과 표준정규로 뽑은 뒤 √k로 나누는 것이 같다는 확인 / 편극 항등식 ⟨x,z⟩=(‖x+z‖²−‖x−z‖²)/4로 내적 보존을 거리 보존에서 끌어내기 / 그 오차가 상대오차가 아니라 ε‖x‖‖z‖에 비례하는 덧셈 오차라 내적이 작은 쌍일수록 상대오차가 커지는 것
  - 절「흔들림의 크기」 — 소절: 카이제곱의 평균 k·분산 2k를 표준정규 제곱 하나의 평균 1·분산 2에서 세우기 / 길이 제곱의 상대표준편차에서 제곱근을 취하면 절반이 되는 한 줄 유도 — √(1+u)≈1+u/2 / 표의 「±0.1 안에 든 비율」 열이 정규근사로 예측되는지 여덟 줄 전부 맞대 보기
  - 절「모든 쌍에 대한 보장」 — 소절: 카이제곱 꼬리 한계를 마르코프 부등식과 모멘트생성함수로 얻는 세 줄 / 지수의 ε²−ε³에서 ε³이 log(1+ε)를 2차까지 편 나머지에서 오는 것 / 「그런 사영이 존재한다」와 「무작위 R이 확률 1/2로 만족한다」가 같은 말인 자리와, 몇 번 뽑아 확인하는 비용
  - 절「d와 n에 대한 의존성」 — 소절: 실험의 점을 정규분포에서 뽑은 것이 결과를 유리하게 만들지 않는지 — 정리에 분포 가정이 없다는 것으로 답하기 / 실측 최악 왜곡이 정리가 허락하는 ε보다 훨씬 작은 이유 — 합집합 한계가 느슨하다 / 그림: 같은 k에서 R을 100번 다시 뽑아 그린 최악 왜곡의 히스토그램
  - 절「필요한 차원의 크기」 — 소절: 정리 대신 원하는 recall에서 k를 역산하는 절차 — 표본 쌍으로 왜곡 분포를 재고 필요한 k를 이분 탐색 / 식의 상수 8이 증명 기법에서 나온 값이라 실무 설정에는 뜻이 없다는 것 / ε을 쌍마다 다르게 요구할 수 있는지 — 가까운 쌍만 정확하면 되는 검색에서는 조건이 훨씬 약해지는 자리
  - 절「주성분 분석과의 대비」 — 소절: 원소를 ±1/√k로 두면 곱셈이 부호 뒤집기와 덧셈이 되는 것 / {+√3, 0, −√3}을 1/6·2/3·1/6로 뽑아 3분의 2를 0으로 비우는 희소 사영 / 사영 비용이 O(dk)라 d가 클수록 이 절약이 커지고 구조화 사영은 이름만 적어 두는 자리

- `math-lsh-collision-probability` (지금 3,359자 · 절 6)
  - 절「부호 한 비트」 — 소절: 벡터의 크기가 식에 안 들어가 각도만 재므로 크기가 뜻을 갖는 검색에는 그대로 못 쓰는 자리 / 초평면이 원점을 지나므로 데이터를 평행이동하면 결과가 바뀌는 것 — 중심화가 무해하지 않다 / r·x가 정확히 0인 경계의 처리와 그 확률이 0인 것
  - 절「AND와 OR로 만드는 S자」 — 소절: 비트 k개를 정수 하나로 묶어 사전 키로 쓰는 구현과 버킷 수 2^k / S자의 기울기를 문턱에서 재어 k가 문턱의 위치를, L이 기울기를 움직인다는 것을 수로 / p*=L^{−1/k}가 p^k≈1/L의 어림이라 정확한 문턱은 이분법으로 구하는 자리
  - 절「예측 recall과 실측의 대조」 — 소절: 후보로 뽑은 뒤 정확한 거리로 다시 정렬하는 단계가 있어야 recall이 정확도로 이어지는 것 / 재정렬 비용이 후보 수에 비례하므로 후보 수 표가 곧 질의 시간인 자리 / 이웃이 여럿일 때의 recall@10과 이웃 하나짜리 확률의 관계
  - 절「후보 수와 메모리 비용」 — 소절: 버킷이 비면 조회가 거의 공짜라 후보 수 기댓값이 곧 비용인 것 / 질의가 떨어진 버킷만 보지 않고 비트 하나 뒤집은 이웃 버킷까지 훑는 다중 프로브 / 같은 recall을 더 적은 L로 얻어 메모리 L배를 깎는 대신 질의당 조회 수가 느는 맞바꿈
  - 절「유사도마다 다른 해시족」 — 소절: 자카드 유사도의 MinHash — 충돌 확률이 그대로 자카드값이 되는 성질 / 유클리드 거리의 p-stable 해시 — floor((a·x+b)/w)와 버킷 폭 w라는 손잡이 / 「충돌 확률이 유사도의 함수로 적히는 해시족」이라는 공통 틀과, 그 함수만 갈아 끼우면 k·L 계산이 그대로 따라오는 것 / 그림: 세 해시족의 충돌 확률 곡선을 유사도 축 위에 겹쳐 그리기
  - 절「16과 50이 정하는 것」 — 소절: 데이터의 각도 분포를 실제로 재는 절차 — 무작위 쌍과 진짜 이웃 쌍의 각도 히스토그램 둘을 겹쳐 그리기 / 두 분포가 겹치는 폭이 좁아야 문턱이 설 자리가 생긴다는 읽기 / 그림: 그 두 히스토그램 위에 39°짜리 문턱선을 그은 그림

- `math-jensen-inequality` (지금 3,366자 · 절 8)
  - 절「볼록함수의 정의」 — 소절: λ와 1−λ를 「두 점 사이를 나눈 비율」로 읽고 λ=1/3에서 실제로 두 값을 재 보기 / 그래프 위쪽 영역이 볼록집합이라는 것이 정의와 같은 말인 자리 / 엄격 볼록과 그냥 볼록의 구별 — 직선은 뒤쪽에만 든다
  - 절「볼록성의 판정과 조립」 — 소절: 합·양수배·최댓값이 볼록을 보존한다는 규칙과 ReLU가 max(0,x)라 볼록인 자리 / 볼록함수에 아핀사상을 합성해도 볼록한 것과, 여러 변수에서는 헤세가 반양정치라는 것 / logsumexp가 볼록이라 softmax 교차엔트로피가 로짓에 대해 볼록하고, 그 앞에 신경망을 태우면 그 성질이 사라지는 대비
  - 절「젠센 부등식」 — 소절: 값이 n개일 때와 연속 분포까지 그대로 가는 이유 — 가중치가 확률이기만 하면 된다 / 갭의 크기가 대략 ½|f''(μ)|Var(X)라는 2차 어림 / 49와 52에서 근사 4.41×10⁻⁴와 실제 4.4×10⁻⁴가 맞는데 1과 100에서는 근사 0.48이 실제 1.619와 어긋나는 경계 / 그림: 분산을 가로축에 두고 젠센 갭과 2차 어림을 겹쳐 그린 곡선
  - 절「교차엔트로피 초과분의 부호」 — 소절: 같은 결론을 접선 부등식 log t ≤ t−1 하나로 얻는 다른 길 / 그 부등식이 젠센 증명에서 쓴 접선과 같은 것이라는 확인
  - 절「하한을 대신 올리는 논법」 — 소절: 하한이 얼마나 팽팽한지가 곧 젠센 갭이고 q를 고르는 자유가 그 갭을 줄이는 손잡이인 것 / 표본을 여러 개 뽑아 안쪽에서 먼저 평균 낸 뒤 로그를 씌우면 갭이 줄어드는 것 / 같은 논법이 서는 다른 자리 — 중요도 샘플링의 로그와 대조 손실의 분모
  - 절「코드로 확인하기」 — 소절: E[1/X]≠1/E[X]를 지연 시간 표본으로 재 보기 — 역수의 평균을 처리량이라 부르는 자리 / 배치별 perplexity를 산술평균하면 전체보다 크게 나오는 것을 수로 / 어느 쪽으로 치우치는지는 f의 볼록·오목만 보면 정해진다는 마무리

- `math-kl-divergence` (지금 3,371자 · 절 7)
  - 「연습 문제」 절 — 이산 세 값 분포로 KL(p‖q)와 KL(q‖p)를 양쪽 손계산 3문항, 교차엔트로피−엔트로피 분해 확인 1문항, 삼각부등식 반례 직접 만들기 1문항, JS가 대칭임을 보이기 1문항
  - 「방향의 선택」을 소절 셋으로 — 「정방향은 넓게 덮는다」 「역방향은 한 봉우리에 몰린다」 「어느 쪽이 어디에 쓰이는가」(RLHF 페널티·증류 손실·ELBO가 각각 어느 방향인지 표)
  - 새 소절 「q에 0이 들어가면」 — 지지집합 조건과 KL이 발산하는 자리, 실무의 ε 스무딩·라벨 스무딩이 그 자리를 어떻게 피하는지
  - 「세 자리에 같은 양이 있다」에 소절 「같은 양인데 왜 이름이 셋인가」 — 최소화 대상이 p인지 q인지가 다르다는 정리

- `math-momentum-and-ema` (지금 3,377자 · 절 7)
  - 절「지수이동평균」 — 소절: 세 번째 셈 — 유효 표본 수 (∑w)²/∑w²로 재면 창이 (1+β)/(1−β)라 두 배가 되는 것과 그 차이의 뜻 / 초기 편향이 세 자리에서 문제가 되는 정도가 다른 것 — 수천 스텝 도는 EMA 체크포인트는 β^t가 0에 붙어 저절로 사라진다 / v₀을 0이 아니라 첫 관측 g₁로 두면 편향이 없어지는 대신 초반이 흔들리는 맞바꿈
  - 절「유효 평균 창 1/(1−β)」 — 소절: 들어오는 값이 흔들릴 때 EMA의 잡음이 (1−β)/(1+β)배로 주는 계산 — 평활의 이득을 수로 / 그래서 β를 올리면 잡음은 줄고 지연은 늘어 둘이 같은 손잡이의 양 끝인 것 / 그림: β 셋에서 계단 입력에 대한 EMA의 반응 곡선
  - 절「EMA된 그래디언트로 걷기」 — 소절: 부호가 매 스텝 뒤집히는 성분이 (1−β)/(1+β)배로 눌리는 계산 — 「상쇄된다」를 수로 바꾸기 / 일정한 성분의 1/(1−β)배와 나란히 놓으면 두 성분의 비가 (1+β)/(1−β)²로 벌어지는 것 / 잡음이 섞이면 걸음이 1/(1−β)배로 커져 유효 학습률이 η/(1−β)가 되는 자리
  - 절「네스테로프의 미리보기」 — 소절: 미리보기 지점을 따로 저장하지 않도록 좌표를 옮겨 적은 형태(PyTorch의 `nesterov=True`가 쓰는 것) / 이차함수에서 네스테로프의 최적 β와 수렴률을 폴랴크와 나란히 놓기 / 볼록·매끄러운 함수에서 O(1/k²)와 O(1/k)라는 이론 차이, 그리고 폴랴크가 이차함수 밖에서는 보장이 없는 자리 / 그림: 같은 골짜기에서 두 방법의 궤적을 겹쳐 그리기
  - 절「조건수와 √κ 가속」 — 소절: β를 올리다 M의 고윳값이 복소수가 되는 문턱 β>(1−√(ηλ))²와 그 뒤로 수렴이 진동하며 붙는 것 / 최적 β*가 정확히 그 문턱 위에 있어 두 방향의 ρ가 0.8182로 같아지는 것 / β가 최적보다 크면 ρ=√β라 β 하나로만 정해지는 구간
  - 절「코드로 확인하기」 — 소절: 네스테로프를 세 번째 줄로 함께 돌려 스텝 수를 나란히 찍기 / 그래디언트에 잡음을 섞어 모멘텀의 잡음 바닥이 학습률 η/(1−β)짜리 경사하강과 비슷한 자리에 서는 것을 재기 / β를 최적값 위로 올려 진동이 생기는 것을 궤적으로 보기

- `math-expected-reward-objective` (지금 3,383자 · 절 6)
  - 절「정책과 궤적」 — 소절: 궤적의 확률이 곱이라 길이가 길수록 지수적으로 작아지고 그래서 언제나 로그로 다루는 것 / 같은 답을 여러 토큰 열로 적을 수 있는 자리 — π가 답이 아니라 토큰 열에 확률을 준다는 구별 / 채점자가 결정적이 아니라 잡음을 갖는 경우에도 J의 형식이 그대로인 것
  - 절「목적식」 — 소절: r에 상수를 더해도 ∇J가 안 변한다는 것을 ∑∇π=0에서 한 줄로 — 뒤에 나올 베이스라인의 씨앗 / J가 π에 대해서는 선형인데 θ에 대해서는 선형이 아닌 자리 / 그림: 답이 셋인 확률 단체 위에 J의 등고선을 그려 같은 값을 주는 정책이 여럿인 것
  - 절「기댓값 안으로 못 들어가는 미분」 — 소절: softmax 정책에서 ∂p_i/∂θ_j=p_i(δ_ij−p_j)를 넣어 ∇J=p⊙(r−E[r])를 끝까지 전개하기 / 그 식이 「평균보다 나은 답의 확률을 올리고 못한 답을 내린다」로 읽히는 것 / θ=(0.3,0.1,−0.2)에서 손으로 얻은 (0.3454,−0.0548,−0.2907)이 뒤의 실측과 맞댈 기준이 되는 자리
  - 절「유한차분의 표본 비용」 — 소절: 두 항에 같은 난수를 쓰면 분산이 크게 주는 것과 이산 표집에서 그 짝을 맞추는 방법 / 중심차분으로 바꿔도 잡음 항은 그대로라 ε 증폭이 안 없어지는 것 / 무작위 방향 하나로 재면 반복 횟수는 줄지만 분산이 차원에 비례해 남는 것
  - 절「같은 벽을 만나는 다른 문제들」 — 소절: 이산 잠재변수 VAE — 코드를 뽑는 순간 θ와의 연결이 끊기는 같은 자리 / 하드 어텐션과 구조 탐색이 전부 ∑r∇π 꼴로 적히는 것 / 연속 잠재변수에서는 재매개변수화가 되는데 이산에서만 막히는 이유를 세 문제에 걸쳐 한 줄로 / 다음 글의 항등식이 셋 모두에 그대로 쓰인다는 예고
  - 절「필요한 항등식의 모양」 — 소절: 그 항등식이 만족해야 할 조건 셋을 먼저 적어 두기 — 모든 π에 대해 성립할 것, 표본평균으로 계산될 것, 뽑은 표본에서 π를 미분할 수 있을 것 / 조건 셋을 만족하는 것이 사실상 하나라는 예고와 대가로 따라오는 큰 분산

- `math-basics-numbers-and-number-line` (지금 3,392자 · 절 5)
  - 절「수를 넓혀 온 순서」 — 소절: 0이 정수 단계에서 함께 들어온 것과, 0으로 나눌 수 없는 이유 — 나눗셈은 곱셈을 되돌리는 계산이라 답이 없거나 하나로 안 정해진다 / 「나누기가 늘 된다」의 정확한 뜻은 0을 뺀 모든 수에 역수가 있다는 것 / 단계마다 사칙연산이 그 자리를 안 벗어나는지 확인해 보기
  - 절「수직선 위에 앉히기」 — 소절: 부등식의 양변에 음수를 곱하면 부호가 뒤집히는 것을 수직선의 뒤집기로 읽기 / |x|<3이 −3과 3 사이의 구간을 뜻하는 것과 그것을 수직선에 색칠하기 / 삼각부등식 |a+b|≤|a|+|b|를 이동 두 번으로 확인하기 / 그림: |x−1|≤2를 구간으로 색칠한 수직선
  - 절「유리수 사이의 빈틈」 — 소절: 두 유리수 사이에는 늘 또 다른 유리수가 있다는 것 — 둘의 한가운데를 잡으면 되고, 그것을 몇 번 되풀이해 보기 / 그렇게 촘촘한데도 √2 자리가 비어 있다는 것 — 촘촘한 것과 빈틈없는 것이 다르다 / 실수가 그 빈틈까지 메워 수직선의 점과 하나씩 짝지어진다는 정리 / 그림: 1과 2 사이를 반씩 쪼개 가며 √2를 좁히는 구간 다섯 개
  - 절「음수가 섞인 계산」 — 소절: ×(−1)이 수직선에서 0을 중심으로 뒤집는 일이라는 그림 쪽 설명 / 같은 분배법칙 논법을 (−5)×(−7)에 되풀이해 보는 자리 / 항이 셋 이상 섞인 식을 이동 여러 번으로 읽어 괄호를 풀지 않고 답을 내기
  - 절「연습 문제」 — 소절: 절댓값이 든 거리 다섯 문제 — |a−b|를 계산하고 수직선에서 칸을 세어 두 번 확인 / |x|<4와 |x−2|≤3을 구간으로 적는 두 문제 / 분배법칙 논법을 (−5)×(−7)에 스스로 되풀이해 보기 / 0으로 나눌 수 없는 이유를 곱셈으로 설명해 보는 한 문제

- `math-softmax-cross-entropy-gradient` (지금 3,405자 · 절 6)
  - 절「softmax의 야코비안」 — 소절: 열의 합이 0인 것과 대칭이라 행의 합도 0인 자리를 함께 짚기 / J가 반양정치이고 고윳값 하나가 정확히 0이며 그 방향이 상수 이동 방향인 것 / p가 한쪽으로 쏠릴수록 성분이 전부 0에 가까워지는 포화 — z=(10,1,0.1)로 재 보기
  - 절「합성에서 소거되는 야코비안」 — 소절: 같은 소거가 sigmoid+BCE에서도 일어나 기울기가 p−y가 되는 것 — C=2인 특수 경우로 읽기 / 소거가 우연이 아닌 이유를 로그와 지수가 서로를 되돌린다는 구조로 / y의 합이 1이 아닌 경우(멀티라벨)에는 마지막 등호가 깨져 p_j∑y_i−y_j가 남는 자리
  - 절「p−y를 성분마다 읽기」 — 소절: 배치에서 reduction이 mean이면 1/B가 곱해져 실제로 흐르는 값이 (p−y)/B인 것 / 패딩 자리를 뺀 경우 그 자리의 기울기가 0이고 분모의 개수에서도 빠지는 것 / 그림: 잘 맞힌 표본과 크게 틀린 표본의 p−y 막대를 나란히
  - 절「온도가 바꾸는 눈금」 — 소절: p=softmax(z/T)의 야코비안이 (1/T)(diag(p)−ppᵀ)로 모양은 같고 눈금만 바뀌는 계산 / 그래서 기울기가 (p−y)/T가 되고 T가 클수록 로짓으로 흘러가는 신호가 작아지는 것 / 증류 손실에 T²를 곱해 그 1/T를 되돌리는 관행이 여기서 나오는 자리 / 어텐션의 1/√d_h도 같은 자리의 T라는 읽기
  - 절「붙여 구현하는 세 이유」 — 소절: 어텐션의 backward에는 −log가 없어 diag(p)−ppᵀ가 소거되지 않고 그대로 남는다는 것 / 그래도 행렬을 만들 필요는 없다 — VJP가 p⊙(u−p·u) 한 줄로 적히는 계산 / (B,H,T,T)짜리 확률마다 T×T 야코비안을 세우면 안 되는 이유를 원소 수로
  - 절「코드로 확인하기」 — 소절: 온도 셋에서 (p−y)/T를 유한차분과 맞대 보기 / sigmoid+BCE가 같은 p−y를 주는지 두 줄로 확인 / 라벨의 합이 1이 아닐 때 p−y가 깨지는 것을 수로

- `math-basics-derivative-rules` (지금 3,421자 · 절 11)
  - 절「기본 도함수 표」 — 소절: tan을 몫의 규칙으로 미분해 1/cos²x를 얻고 표에 한 줄 더하는 자리 / a^x의 계수 ln a를 a=e^{ln a}로 바꿔 연쇄법칙 한 번에 얻기 / log_a x 줄이 밑변환 log_a x=ln x/ln a에서 상수배로 따라 나오는 것
  - 절「합·상수배·곱·몫」 — 소절: 몫의 규칙을 곱의 규칙과 연쇄법칙으로 유도하기 — f·g^{−1}로 보면 두 줄이다 / 곱이 셋이면 항이 셋이 되는 확장 (fgh)'=f'gh+fg'h+fgh' / 분수를 나눠 적어 몫의 규칙을 피하는 요령 — (x²+1)/x를 x+1/x로 먼저 고치기
  - 절「합성함수와 연쇄법칙」 — 소절: 왜 곱인가 — Δy/Δx=(Δy/Δu)(Δu/Δx)로 변화율 둘을 이어 붙이는 읽기 / 라이프니츠 표기에서 du가 약분되는 것처럼 보이는 자리와 그것이 증명이 아닌 이유 / 그림: 눈금자 셋을 나란히 놓고 x가 1 움직일 때 u와 y가 각각 몇 배로 움직이는지
  - 절「고계도함수와 볼록」 — 소절: f'과 f''의 부호 네 조합이 만드는 곡선 모양 네 가지 / x⁴에서 f''(0)=0인데도 극소인 자리 — f''=0은 변곡점의 필요조건일 뿐이다 / f'이 속도, f''이 가속도인 대응을 한 줄로
  - 절「음함수·로그·역함수 미분」 — 소절: 로그 미분법 — 곱과 거듭제곱이 겹친 식에 양변 로그를 씌워 y=x^x를 x^x(ln x+1)로 / 역함수의 미분 dy/dx=1/(dx/dy)를 y=x 대칭 그림으로 읽기 / (ln x)'=1/x를 x=e^y에서 역함수 미분으로 되찾기
  - 절「연습 문제」 — 소절: 로그 미분법 두 문제(x^x, (x²+1)³(x−1)⁴) / 역함수 미분 두 문제 / 한 점에서 유한차분과 대조해 검산하는 두 문제 — h를 10⁻³·10⁻⁶·10⁻¹⁰으로 바꿔 가며 / 틀린 답 다섯을 주고 부호·속미분 누락·음의 지수 중 무엇을 놓친 것인지 고르기

- `math-basics-pythagorean-theorem` (지금 3,443자 · 절 7)
  - 절「세 변에 이름 붙이기」 — 소절: 직각과 마주 보는 변이 왜 가장 긴지 한 문장으로 — 나머지 두 각이 예각이라 그 맞은편 변이 짧다 / 세 변이 아무 값이나 될 수 없다는 것 — 두 변의 합이 나머지보다 커야 하고 a²+b²=c²이 그 조건을 저절로 만족하는 확인
  - 절「두 가지로 채우는 정사각형」 — 소절: 한 변이 c인 정사각형 안에 같은 삼각형 넷을 돌려 넣으면 가운데 한 변 b−a인 정사각형이 남는 두 번째 배치와 c²=4·(ab/2)+(b−a)² / 두 증명이 「큰 정사각형에서 삼각형 넷을 뺀다」는 같은 뺄셈이라는 확인 / 그림을 덮고 복원할 때 가장 자주 빠지는 두 문장이 어느 것인지 / 그림: 안쪽으로 접은 배치 한 장
  - 절「정리의 역과 3·4·5」 — 소절: a²+b²이 c²보다 크면 예각, 작으면 둔각이라는 갈래로 삼각형 모양을 판정하기 / 정수 세 쌍을 만드는 법 (m²−n², 2mn, m²+n²)과 (m,n)=(2,1)·(3,2)·(4,1)에서 3·4·5, 5·12·13, 8·15·17이 나오는 것 / 세 쌍에 같은 수를 곱해도 여전히 직각이라 서로소인 것만 세면 되는 자리
  - 절「모눈 위와 상자 안의 길이」 — 소절: 직육면체의 대각선은 바닥 대각선을 다시 한 변으로 쓰는 두 번 적용 — 3·4·12에서 √(9+16+144)=13 / 두 번 쓰면 세 변이 제곱합으로 모이는 것과 그것이 차원을 하나 늘린 꼴인 자리 / 그림: 직육면체에서 바닥 대각선과 공간 대각선이 만드는 직각삼각형
  - 절「한 변이 1인 정사각형의 대각선」 — 소절: √2가 1과 2 사이 어디인지 후보를 제곱해 좁히는 절차 — 1.4²=1.96, 1.5²=2.25 / 대각선이 한 변의 몇 배인지는 정사각형 크기와 무관하다는 것 / A4 종이의 가로세로비가 그 값이라 반으로 접어도 비가 유지되는 계산
  - 절「연습 문제」 — 소절: 예각·직각·둔각을 세 변에서 판정하는 다섯 문제 / (m²−n², 2mn, m²+n²)으로 정수 세 쌍을 셋 만들어 검산하기 / 직육면체 대각선 세 문제 / 증명 그림을 안쪽 배치로도 복원해 보기

- `math-basics-trigonometry-and-unit-circle` (지금 3,443자 · 절 8)
  - 절「크기와 무관한 변의 비」 — 소절: 30°와 60°의 값이 서로 뒤바뀐 것을 sin(90°−θ)=cosθ로 일반화하고 이름 붙이기 / tan을 sin/cos으로 적으면 외울 값이 셋에서 둘로 주는 것 / sin과 cos이 0과 1 사이에 갇히는 이유는 빗변이 가장 길기 때문이고 tan만 그 제한이 없는 자리
  - 절「단위원 위의 좌표」 — 소절: 값의 범위가 −1과 1 사이인 것이 「원 위의 점」이라는 사실에서 바로 나오는 것 / 사분면별 부호를 외우지 않고 점의 좌표에서 매번 읽는 절차 / cos²θ+sin²θ=1로 나머지 값을 구할 때 부호를 사분면이 정하는 자리
  - 절「라디안」 — 소절: 라디안으로 재야 작은 각에서 sinθ≈θ가 성립하는 것과 표로 확인하기(0.1에서 0.0998) / 도로 재면 그 근사에 π/180이 붙어 지저분해지는 대비 / 그림: 각이 작을 때 호의 길이·현·높이가 거의 겹치는 확대 그림
  - 절「한 바퀴가 그리는 물결」 — 소절: 주기가 같은 두 물결을 더하면 다시 같은 주기의 물결 하나가 되고 진폭과 위상만 바뀌는 것 — sin x+cos x가 √2 sin(x+π/4)인 확인 / tan의 점근선 자리를 단위원에서 읽기 — cos이 0이 되는 두 자리 / 그림: sin x+cos x와 √2 sin(x+π/4)를 겹쳐 그려 같은 곡선임을 보이기
  - 절「각으로 재는 높이와 넓이」 — 소절: 밑변과 올려본각으로 높이를 구하기 — 20m 떨어져 30°면 20 tan30°≈11.5m / 두 변과 낀 각으로 삼각형 넓이 ½ab sinC를 얻고 그것이 밑변×높이÷2와 같은 식인 확인 / 덧셈정리는 결과만 적어 두고 26번 복소수 글이 곱셈 한 번을 전개해 증명한다는 예고 / 그림: 올려본각으로 나무 높이를 재는 그림
  - 절「연습 문제」 — 소절: 올려본각으로 높이를 재는 세 문제 / ½ab sinC로 넓이를 구하는 두 문제 / sin(90°−θ)=cosθ를 특수각으로 확인하는 두 문제

- `math-sgd-noise-batch-and-schedule` (지금 3,450자 · 절 6)
  - 절「미니배치 그래디언트의 분산」 — 소절: σ²가 파라미터마다 다르고 그 합이 곧 벡터의 노름이라 한 식으로 다뤄도 되는 확인 / 복원 없이 뽑으면 유한모집단 보정 (N−B)/(N−1)이 붙지만 B≪N에서 무시되는 자리 / σ²를 실제로 재는 법 — 같은 위치에서 배치를 여러 번 뽑아 그래디언트의 분산을 직접 세기
  - 절「선형 스케일링 법칙」 — 소절: 법칙이 맞추는 것은 「같은 데이터를 본 뒤의 분포」이지 매 스텝의 위치가 아니라는 구별 / η를 한 번에 k배로 올리지 않고 워밍업으로 올리는 관행이 여기서 나오는 자리 / 그림: 작은 배치 k스텝의 궤적과 큰 배치 한 스텝을 같은 평면에 겹쳐 그리기
  - 절「Adam의 √k 스케일링」 — 소절: 갱신이 g/√v̂라 그래디언트의 크기가 약분되어 잡음에 대한 반응이 SGD와 다르다는 것 / 분자의 평균이 √B에 비례해 자라고 분모가 그 절반만 되돌려 √k가 남는 계산 / 두 규칙 중 어느 쪽을 쓸지는 옵티마이저의 갱신 식을 보고 정한다는 정리 / 그림: 배치를 8배로 할 때 SGD와 Adam이 각각 필요로 하는 학습률 배수
  - 절「임계 배치 크기」 — 소절: B_crit를 실제로 재는 법 — 큰 배치와 작은 배치의 ‖g‖² 두 값으로 ‖∇L‖²과 σ²를 갈라내는 두 식 두 미지수 / 학습 초반에 작고 후반에 커지는 것과 배치를 도중에 키우는 관행 / gradient accumulation이 같은 B를 만드는 다른 방법일 뿐이라는 확인 — 스텝 수와 통신량만 달라진다
  - 절「워밍업이 지나가는 구간」 — 소절: 워밍업 길이를 β₂의 창과 맞추는 감각과 선형·지수 두 방식의 차이 / 워밍업 없이 Adam을 돌렸을 때 무엇이 먼저 터지는지 — 첫 몇 스텝의 √v̂가 작아 걸음이 크게 나가는 자리 / 워밍업이 필요 없어지는 설정(β₂를 낮추거나 ε을 키우는 것)과 그 대가
  - 절「감쇠와 잡음 바닥」 — 소절: 바닥이 η/B 하나로 정해져 학습률과 배치가 같은 손잡이의 두 이름인 것 / 그래서 배치와 학습률을 함께 두 배로 하면 바닥이 그대로인 확인 / 코사인은 T를 알아야 하고 역제곱근은 몰라도 되며, 상수 구간을 두고 끝에서만 내리는 스케줄이 그 T 문제를 피하는 자리

- `math-kl-constrained-optimum-and-dpo` (지금 3,453자 · 절 6)
  - 절「최적 정책의 닫힌 해」 — 소절: 미분하는 것이 함수가 아니라 값 π(y) 하나씩이라 보통 미분으로 끝나는 자리 / 부등식 제약 π(y)≥0을 따로 안 걸어도 되는 이유 — 지수라 저절로 양수다 / β→0과 β→∞ 두 극한에서 π*가 각각 무엇이 되는지
  - 절「β가 정하는 것」 — 소절: 같은 KL 예산을 제약으로 적었을 때의 승수와 여기 β가 같은 값이라는 대응 / β를 고정하는 대신 KL을 목표로 두고 β를 자동으로 조절하는 방식의 자리 / 그림: β를 가로축에 두고 E[r]과 KL을 나란히 그린 두 곡선
  - 절「보상을 정책으로 적기」 — 소절: 로그비를 토큰 단위 합으로 풀면 답이 길수록 값이 작아지는 것 — 길이 편향이 여기서 들어오는 자리 / 로그비를 길이로 나누는 변형이 그 편향을 다루는 방법이라는 한 줄
  - 절「분배함수의 소거」 — 소절: 소거가 「같은 프롬프트」에 기대므로 프롬프트가 다른 두 답은 비교할 수 없다는 것 / 그래서 데이터가 프롬프트마다 쌍으로 묶여 있어야 하는 자리
  - 절「DPO 손실의 기울기」 — 소절: L=−logσ(Δ)를 θ로 미분해 −βσ(−Δ)(∇log π_θ(y_w)−∇log π_θ(y_l))를 얻기 / 가중치 σ(−Δ)가 「지금 얼마나 틀렸는가」라 이미 잘 갈라진 쌍에서 0에 가까워지는 것 / 손실이 차이만 보므로 chosen과 rejected의 로그확률이 함께 내려가도 손실은 줄어드는 자리 / 그림: 두 로그확률이 나란히 내려가는데 Δ만 벌어지는 궤적
  - 절「유도가 기대는 것과 맞바꿈」 — 소절: 데이터의 답이 π_ref에서 나온 것이 아닐 때 생기는 어긋남과, 먼저 지도 미세조정으로 π_ref를 데이터 쪽으로 옮겨 두는 대처 / 모델 둘과 샘플링 루프를 지운 대신 학습 중 새 답을 채점받을 기회를 잃었다는 맞바꿈 — 두 방법이 나란히 쓰이는 이유

- `math-matrix-as-linear-map` (지금 3,455자 · 절 8)
  - 절「선형사상의 두 조건」 — 소절: c=−1을 넣으면 T(−x)=−T(x)라 원점 대칭이어야 하고 ReLU가 바로 그 자리에서 걸리는 확인 / 편향·ReLU·softmax·LayerNorm이 각각 어느 조건에 걸리는지 다섯 줄 표 / 아핀사상과 동차좌표 — 한 차원을 늘려 [W b; 0 1]로 편향을 행렬 안에 넣는 요령 / 그림: 편향이 붙으면 원점이 옮겨 가 격자가 통째로 밀리는 그림
  - 절「기저의 상이 적힌 장부」 — 소절: 상은 열들이 만드는 span이고 그 차원이 계수 — 도착 공간을 얼마나 채우는가 / 핵은 0으로 가는 입력들이 이루는 부분공간이고, 768→64에서 계수가 최대 64이므로 핵이 적어도 704차원인 계산 / 계수와 핵의 차원을 더하면 입력 차원이라는 한 줄과 그것이 「버린 만큼 남는다」로 읽히는 것 / 그림: 입력 공간이 핵과 나머지로 갈리고 나머지만 상으로 가는 그림
  - 절「단위 정사각형으로 읽기」 — 소절: 행렬식의 부호가 방향이 뒤집혔는지를 말하는 것 — 회전은 +1, 거울 대칭은 −1 / 3차원에서는 부피 배율이고 그 부호가 오른손·왼손을 가르는 자리 / 행렬식이 0에 가까운 행렬은 되돌리기는 되지만 오차가 크게 늘어나는 것
  - 절「합성사상과 행렬곱」 — 소절: 결합법칙 (AB)C=A(BC)이 「세 번 보내기」에서 당연한 것과, 그런데도 계산 비용은 순서에 따라 달라지는 자리 / 항등사상과 단위행렬, 역사상과 역행렬의 대응 / 전치가 무엇을 뜻하는지는 내적을 지나야 보인다는 경계
  - 절「코드로 확인하기」 — 소절: 열 768개 중 몇 개가 일차독립인지 계수로 세어 보기 / 서로 다른 두 입력이 같은 출력으로 가는 예를 핵에서 실제로 만들어 보기 / 무작위 다각형을 통과시켜 넓이 배율이 행렬식과 맞는지 재기
  - 절「W_Q가 하는 일」 — 소절: q·k=xᵀW_QᵀW_K x'로 두 사상이 표 하나로 접히고 그 표의 계수가 64를 못 넘는다는 계산 / 그래서 점수 표가 저계수라는 것과 그것이 헤드를 여럿 두는 이유와 이어지는 자리 / 접힌 표 하나만 학습하면 되는지 — 되지만 파라미터가 768×768로 늘어나는 대비

- `math-basics-decimals-percent-ratio` (지금 3,466자 · 절 7)
  - 절「소수 표기」 — 소절: 분모가 2^a·5^b이면 소수 자릿수가 max(a,b)라는 것 — 1/8은 2³이라 세 자리다 / 나눗셈의 나머지가 되풀이되는 순간이 곧 순환이 시작되는 자리이고, 그래서 순환마디가 분모보다 짧은 것 / 1/7을 손으로 나눠 여섯 자리 마디를 확인하기
  - 절「백분율」 — 소절: %가 단위가 아니라 「÷100」이라 0.75와 75%를 등호로 이어 적어도 되는 자리 / 「무엇의 몇 %인가」에서 기준이 되는 양을 문장에서 찾아 밑줄 긋는 훈련 / 퍼센트를 곱셈으로 바꾸는 두 꼴 — 늘리면 ×(1+r), 줄이면 ×(1−r)
  - 절「비를 수로 옮기기」 — 소절: 세 항의 비 a:b:c를 전체로 나눠 셋을 한꺼번에 받는 것과, 같은 수로 곱하거나 나눠 정수비로 고치기(0.5:1.5 → 1:3) / 속력·농도·밀도처럼 서로 다른 양을 나눈 값에는 단위가 남고, 같은 양끼리 나눈 비율은 단위가 지워져 순수한 수가 되는 대비 / 단위가 붙은 비는 그냥 평균 내면 안 된다는 것 — 60km/h로 갔다가 30km/h로 같은 거리를 돌아오면 평균이 45가 아니라 40인 계산
  - 절「구성비」 — 소절: 반올림하면 합이 100%가 안 되는 자리 — 33.3%가 셋이면 99.9%다 / 어느 칸을 조정해 100으로 맞출지 정하는 규칙 / 구성비만 보고는 원래 값을 되돌릴 수 없다는 것 — 합이 하나 더 있어야 한다
  - 절「퍼센트에서 틀리는 네 자리」 — 소절: 20% 올린 것을 원래로 되돌리려면 몇 %를 내려야 하는지 역산하기(16.7%) / 두 해의 성장률을 「평균」 낼 때 산술평균이 아니라 곱의 제곱근이어야 하는 것 — 1.1과 1.3에서 1.196 / 그림: 두 해의 성장률을 곱으로 이어 붙인 막대와 산술평균으로 잘못 이은 막대
  - 절「연습 문제」 — 소절: 왕복 평균 속력 한 문제와 농도를 섞는 두 문제 / 구성비를 반올림해 100%가 안 되는 예를 직접 만들어 보는 한 문제 / 20% 올린 것을 되돌리는 비율을 구하는 한 문제 / 1/7을 손으로 나눠 순환마디 여섯 자리를 확인하기

- `math-basics-complex-numbers-and-euler` (지금 3,467자 · 절 10)
  - 절「허수단위와 사칙연산」 — 소절: i의 거듭제곱을 4로 나눈 나머지로 읽는 절차 / √(−4)를 2i로 적을 때 √a√b=√(ab)가 음수에서 깨지는 자리 — √(−1)√(−1)은 1이 아니다 / 켤레가 덧셈과 곱셈을 통과한다는 성질을 두 줄로 확인하기
  - 절「복소평면」 — 소절: 두 점 사이의 거리 |z−w|와 그것으로 원 |z−c|=r을 적기 / 켤레가 곱셈을 통과하므로 실계수 다항식의 근이 언제나 켤레쌍인 것 — 이차식만이 아니라 차수가 몇이든 / 그림: 켤레쌍 두 근이 가로축을 사이에 두고 마주 보는 그림
  - 절「극형식」 — 소절: 편각이 한 바퀴만큼 달라도 같은 점이라는 것과 −180°<θ≤180°로 잡는 관례 / |zw|=|z||w|가 극형식에서 한 줄로 보이는 것과, 성분으로 적으면 (a²+b²)(c²+d²)도 두 제곱의 합이 되는 부산물(2×5=10=1²+3²) / 나눗셈이 극형식에서 크기를 나누고 각을 빼는 일이 되는 것
  - 절「회전과 드무아브르 정리」 — 소절: z와 iz가 언제나 수직이라는 것과 그것으로 수직 조건을 복소수 한 줄로 적기 / 1의 n제곱근 중 하나를 ω라 두면 나머지가 전부 ω의 거듭제곱인 것 / n제곱근의 합이 0인 것과 그것을 정n각형의 무게중심으로 읽기
  - 절「오일러 표기」 — 소절: e^{iθ}의 켤레가 e^{−iθ}이고 둘을 더하면 2cosθ가 되는 것 — 삼각함수를 지수로 되받는 자리 / e^{i(α+β)}=e^{iα}e^{iβ} 한 줄이 덧셈정리 두 개를 한꺼번에 적는 것 / 밑이 왜 하필 e인지는 35번 테일러가 급수로 갚는다는 예고
  - 절「연습 문제」 — 소절: |z−1|=2를 만족하는 점을 복소평면에 그리는 한 문제 / 1의 여섯제곱근을 구하고 합이 0인지 확인하기 / 켤레가 곱셈을 통과한다는 것을 두 복소수로 직접 확인하는 한 문제 / 덧셈정리를 곱셈 전개로 스스로 한 번 더 유도해 보기

- `math-matrix-calculus` (지금 3,478자 · 절 6)
  - 절「규약이 둘인 이유」 — 소절: 연쇄법칙을 적을 때 두 규약에서 곱하는 순서가 뒤집히는 것 — 분자 배치는 왼쪽에서, 분모 배치는 오른쪽에서 / 규약을 안 밝힌 글에서 shape 하나로 되짚는 절차 / 두 규약이 섞인 식을 코드로 옮길 때 전치를 어디에 넣을지 정하는 기준
  - 절「성분에서 공식 만들기」 — 소절: 미분 형식으로 한 번에 읽기 — dL을 적고 dL=tr(Gᵀ dW) 꼴로 묶으면 그 자리의 G가 곧 ∂L/∂W인 규칙 / 앞의 δxᵀ를 그 방법으로 두 줄에 다시 얻기 / 첨자가 셋 이상 얽히면 성분보다 이쪽이 짧고, tr의 순환 성질이 그 정리를 맡는 자리
  - 절「shape로 복원하는 법」 — 소절: 후보가 둘 남는 자리를 미리 알아보는 법 — 정사각행렬이 끼거나 두 재료의 모양이 같을 때다 / shape가 맞아도 값이 틀리는 다른 예 — 브로드캐스트가 조용히 축을 늘려 주는 자리
  - 절「원소별 함수와 브로드캐스트」 — 소절: y=σ(z)의 야코비안이 대각행렬이라 VJP가 δ⊙σ'(z)라는 원소곱 한 줄로 줄어드는 것 / 순전파에서 늘린 축은 역전파에서 더한다는 규칙 — 편향이 배치 축 합인 것이 그 한 예 / 나눗셈·거듭제곱이 원소별로 붙은 경우도 같은 규칙인 것 / 그림: 순전파의 브로드캐스트와 역전파의 합이 서로 짝인 그림
  - 절「배치 차원이 끼는 자리」 — 소절: 손실을 합으로 정의했는지 평균으로 정의했는지가 1/B로 나타나는 자리 / 축이 셋 이상인 텐서 (B,T,n)에서는 앞의 두 축을 하나로 접어 같은 식을 쓰는 것 / 마스크가 있는 표본을 0으로 만들어 그 자리가 합에서 빠지게 하는 구현
  - 절「코드로 확인하기」 — 소절: 중심차분이 한쪽 차분보다 정확한 이유 — 오차가 ε 대신 ε²이다 / ε을 너무 작게 잡으면 반올림이, 크게 잡으면 절단이 이기는 자리와 float64에서 1e-6 언저리가 되는 셈 / 절대오차가 아니라 상대오차로 판정하고 0에 가까운 성분은 따로 보는 요령

- `math-weight-init-variance` (지금 3,491자 · 절 6)
  - 절「한 층의 이득」 — 소절: 편향을 0으로 두는 관행과 그것이 이득 계산을 바꾸지 않는 확인 / tanh의 이득이 정확히 1이 아니라 입력 크기에 따라 줄어드는 것과 계수 5/3이 붙는 자리 / 가정 셋(평균 0·서로 독립·가중치와 활성값의 독립)을 나란히 적어 두고 어느 것이 먼저 깨지는지
  - 절「이득의 거듭제곱과 깊이」 — 소절: 이득을 1에 맞춰도 유한한 폭에서는 층마다 상대오차 √(2/n)이 남는 것 / 그 오차가 랜덤워크라 L층 뒤에 √L에 비례하는 흔들림으로 쌓이는 계산 / 그림: 같은 초기화로 씨앗을 스무 개 바꿔 돌린 층별 크기의 띠
  - 절「순전파와 역전파의 두 조건」 — 소절: 두 조건의 절충이 산술평균이 아니라 조화평균 꼴로 적히는 이유 / n_in과 n_out이 크게 다른 층에서는 그 절충값이 양쪽 다 크게 어긋나는 자리 / mode를 fan_out으로 바꾸는 것이 맞는 구조는 어떤 것인지 — 역전파 쪽이 먼저 죽는 경우
  - 절「뽑는 분포와 층마다의 예외」 — 소절: 균등분포는 분산이 a²/3이라 ±√(6/(n_in+n_out))으로 적히는 환산과 정규분포와의 실제 차이 / 직교 초기화 — 이득을 근사가 아니라 정확히 1로 맞추는 방법과 정사각이 아닐 때의 제약 / 합성곱에서 팬인이 k_h·k_w·C_in이라는 것 / 임베딩처럼 팬인이 뜻을 갖지 않는 자리와, 출력 투영·잔차 마지막 층을 0에 가깝게 두는 관행
  - 절「잔차 연결과 정규화」 — 소절: LayerNorm의 γ를 1, β를 0으로 두는 것이 「처음에는 아무것도 안 한다」는 뜻인 자리 / 정규화가 블록 앞이냐 뒤냐에 따라 분산이 쌓이는 방식이 달라지는 것 / 분기를 1/√L로 줄이는 것과 분기 끝에 학습 가능한 계수를 0으로 두고 시작하는 것이 같은 자리를 노리는 두 방법인 것
  - 절「폭이 커질 때의 갱신 크기」 — 소절: Adam에서는 갱신 크기가 그래디언트 크기와 무관해 폭에 대한 지수가 SGD와 달라지는 것 / muP가 층 종류(입력·은닉·출력)마다 초기화·학습률·출력 배율 셋을 따로 정하는 이유 / 가중치와 활성값이 독립이라는 가정이 학습이 진행되면 깨져 이 계산이 t=0 한 점의 이야기인 자리 / 작은 모델에서 고른 학습률을 큰 모델에 옮겨 쓰는 것이 실제로 아끼는 비용

- `math-sampling-from-categorical` (지금 3,503자 · 절 6)
  - 절「역변환 표집」 — 소절: 누적합을 매번 만들면 O(n)이라 같은 분포에서 여러 번 뽑을 때는 별칭법으로 O(1)이 되는 것 / 부동소수점에서 누적합의 끝이 정확히 1이 아니라 u가 그 끝을 넘는 경우의 처리 / 확률이 아주 작은 토큰이 float32에서 0으로 내려앉아 영영 안 뽑히는 자리
  - 절「온도가 하는 일」 — 소절: T를 낮추면 두 확률의 비가 (p_i/p_j)^{1/T}로 지수적으로 벌어지는 계산 / 같은 로짓에 T=0.5·1·2를 넣은 확률 벡터와 엔트로피를 나란히 놓아 「뾰족해진다」를 수로 바꾸기 / T→0이 argmax, T→∞가 균등분포인 두 극한 / 온도는 순위를 바꾸지 않고 아무것도 0으로 만들지 않는다는 것 / 그림: T 셋에서 같은 로짓이 만드는 확률 막대 세 벌
  - 절「개수로 자르기와 질량으로 자르기」 — 소절: min-p — 1등 확률의 일정 비율 아래를 버려 문턱이 모델의 확신 정도를 따라 움직이는 것 / 겹치는 순서가 결과를 바꾸는 자리 — 온도는 순위를 안 바꿔 top-k 집합은 그대로지만 질량을 옮기므로 top-p가 남기는 개수는 달라진다 / 반복 벌점처럼 이미 나온 토큰의 로짓을 직접 깎는 조작이 확률이 아니라 점수 쪽에 손대는 자리
  - 절「Gumbel-max 트릭」 — 소절: Gumbel 분포가 「최댓값의 분포」로 나오는 것이라는 이름의 뿌리 한 줄 / 잡음을 더한 값의 최댓값 자체도 분포를 갖고 그 평균이 logsumexp 언저리인 것 / 로짓에 상수를 더해도 결과가 안 바뀌는 것 — softmax의 상수 이동 불변성이 여기서도 그대로 / 같은 잡음으로 상위 k개를 고르면 비복원 추출과 같아지는 Gumbel top-k
  - 절「Gumbel-softmax」 — 소절: 순전파는 원-핫으로 반올림하고 역전파는 부드러운 값으로 흘리는 직통 추정량과 그것이 편향된 기울기라는 것 / τ를 학습 중 줄여 가는 스케줄과 너무 빨리 줄이면 기울기가 죽는 자리 / 표집이 아니라 기댓값만 필요하면 Gumbel 없이 softmax(z/τ)를 쓰는 다른 길
  - 절「코드로 확인하기」 — 소절: 온도 셋에서 엔트로피와 상위 다섯 개의 누적 질량을 찍어 표로 / 잡음을 고정하고 온도만 바꿔 뽑아 결과가 어디서 갈리는지 / Gumbel top-k가 정말 비복원 추출과 같은 분포를 주는지 세어 보기

- `math-basics-graph-transformations` (지금 3,526자 · 절 11)
  - 절「옮기기 — 위아래와 좌우」 — 소절: 옮기기를 「점 (a,b)가 (a+h, b+k)로 간다」로 적으면 두 방향이 한 줄이 되는 것 / 식을 y−k=f(x−h)로 나란히 고쳐 적으면 부호가 헷갈리지 않는 자리 / 그림: 같은 곡선을 네 방향으로 옮긴 네 그림에 기준점을 하나씩 찍어 두기
  - 절「늘이기와 뒤집기」 — 소절: 뒤집기가 c=−1인 늘이기라는 통합 — 규칙 넷이 둘로 줄어드는 자리 / |c|<1과 |c|>1의 갈래를 세로·가로 각각에서 표로 / y=|f(x)|는 음수 부분만 접어 올린 것이고 y=f(|x|)는 오른쪽을 왼쪽에 비친 것이라는 대비 / 우함수·기함수를 「뒤집어도 그대로인가」로 판정하고 x²·|x|와 x·1/x를 갈라 보기
  - 절「겹칠 때의 순서」 — 소절: 가로 변형끼리 겹칠 때는 안에서 밖으로 읽는 것 — f(2x−4)는 f(2(x−2))라 왼쪽으로 4가 아니라 오른쪽으로 2다 / 세로 변형과 가로 변형은 서로 순서를 바꿔도 결과가 같다는 것 / 그림: 같은 식을 두 순서로 그려 갈리는 것과 안 갈리는 것을 나란히
  - 절「역함수」 — 소절: 원래 함수의 정의역과 치역이 역함수에서 맞바뀐다는 것 / f(f⁻¹(x))=x와 f⁻¹(f(x))=x가 성립하는 범위가 다를 수 있는 자리 / f⁻¹(x)를 1/f(x)와 헷갈리지 않기
  - 절「정의역·치역·점근선」 — 소절: √(x+3)처럼 옮기면 정의역이 함께 옮겨 가는 것(x≥−3) / y=−2|x|+5처럼 늘이고 뒤집고 옮기면 치역이 그만큼 따라가는 것(y≤5) / 1/x를 오른쪽 2·위로 3 옮기면 점근선도 x=2와 y=3으로 함께 가는 것 / 그림: 변형 세 단계마다 정의역과 치역을 두 축 위에 색칠해 따라가는 그림
  - 절「연습 문제」 — 소절: 변형된 식에서 정의역과 치역을 먼저 적는 네 문제 / f(2x−4) 꼴을 묶어 읽는 세 문제 / y=|f(x)|와 y=f(|x|)를 같은 곡선에서 그려 보는 두 문제 / 역함수의 정의역·치역을 원래 함수에서 옮겨 적는 두 문제

- `math-mutual-information` (지금 3,557자 · 절 8)
  - 절「조건부 엔트로피」 — 소절: 사슬 규칙 H(X,Y)=H(Y)+H(X|Y)를 정의에서 한 줄로 얻기 / H(X|Y)≤H(X)이고 등호가 독립일 때뿐이라는 것 — 「알면 놀람이 줄지 늘지는 않는다」 / 그런데 특정 y 하나에서는 H(X|Y=y)가 H(X)보다 클 수 있다는 것 — 평균에서만 서는 부등식
  - 절「상호정보량의 세 얼굴」 — 소절: I(X;X)=H(X)라 자기 자신과의 상호정보량이 엔트로피인 확인 / 결정적 함수 Y=g(X)에서 I=H(Y)가 되는 것 / 연속 변수에서 엔트로피 자체는 눈금을 바꾸면 달라지는데 상호정보량은 그대로인 자리
  - 절「2×2 표로 세어 보기」 — 소절: 상관계수가 0인데 정보량이 0이 아닌 예 — X가 −1·0·1이고 Y=X²일 때를 표로 세어 상관과 정보를 가르기 / 표의 0.4를 0.5로 밀어 완전히 결정적으로 만들면 I가 1비트로 차는 것 / 세 정의가 같은 수를 주는지 코드로 확인하고 독립 표에서 0이 나오는지 보기
  - 절「고차원에서 재기 어려운 이유」 — 소절: 데이터 처리 부등식 — X→Y→Z이면 I(X;Z)≤I(X;Y)라 인코더를 지나며 정보가 늘지 않고 표현학습이 「고르는 일」인 이유 / 벤 그림이 두 변수에서만 통하고 셋이 되면 가운데 겹치는 자리가 음수가 될 수 있어 깨지는 것 / 하한 말고 상한을 만드는 길도 있지만 그쪽은 참조 분포가 필요하다는 한 줄
  - 절「InfoNCE 하한」 — 소절: 온도 τ가 점수의 눈금을 바꿔 하한이 얼마나 팽팽한지를 정하고 CLIP이 그것을 학습 파라미터로 두는 것 / 최선의 점수함수가 아니라 실제로 학습된 점수함수를 넣으면 하한이 더 느슨해지는 것 — 손실을 하한으로 읽을 때의 단서 / 배치 안의 음성 표본이 서로 독립이 아니라는 것과 그때 부등식이 흔들리는 자리
  - 절「log N이라는 천장」 — 소절: 메모리 뱅크·큐로 음성 표본을 모아 두는 우회와 그때 표현이 낡는다는 대가 / 천장이 걸려도 학습이 되는 이유 — 하한을 「재는 값」이 아니라 「미는 힘」으로 쓰는 자리 / 그림: N을 키울 때 천장과 실제 손실이 각각 어떻게 움직이는지 두 곡선

- `math-maximum-likelihood` (지금 3,558자 · 절 7)
  - 절「확률과 가능도의 구별」 — 소절: 가능도의 절대값에는 뜻이 없고 두 θ에서 잰 비만 뜻이 있다는 것 / 연속 변수에서는 확률이 아니라 밀도라 값이 1을 넘을 수도 있는 자리 / 같은 데이터에 모델 둘을 대 보는 일이 가능도비 하나로 적히는 것
  - 절「로그가능도」 — 소절: 로그가 곱을 합으로 바꾸면서 미니배치 평균이 전체 로그가능도의 불편 추정이 되는 것 — 학습이 이 성질 위에 서는 자리 / 언더플로를 수로 확인하기 — 0.5를 2,000번 곱하면 float64에서 0이다 / 최댓값의 자리는 안 바뀌는데 값 자체는 달라진다는 구별
  - 절「가능도 곡면」 — 소절: μ̂이 σ에 안 걸려 두 손잡이가 따로 풀리는 것이 우연인지 — 식의 어느 자리가 그것을 정하는지 / n으로 나눈 σ̂²이 참값을 (n−1)/n배 낮잡는 것을 n=8에서 수로 보기 / 가정이 틀렸을 때 E[log p_θ]를 최대화하는 것이 KL(p_data‖p_θ)를 최소화하는 것과 같다는 한 줄과, 정방향 KL이라 데이터가 있는 자리를 비우지 않으려 넓게 덮는 쪽으로 답이 잡히는 성질
  - 절「가정에서 나오는 손실」 — 소절: 두 유도에서 한 일이 똑같다는 정리와 가정별 손실 표 — 라플라스→L1, 베르누이→BCE, 포아송→λ−k log λ / 이분산 정규 — σ까지 예측하게 두면 손실에 log σ 항이 붙어 모델이 「자신 없는 자리」를 스스로 넓히는 것 / 그림: 같은 자료에 정규와 라플라스를 각각 맞췄을 때 이상치 하나가 답을 얼마나 끄는지
  - 절「사전분포와 정규화 항」 — 소절: 가능도에 사전분포를 곱하고 로그를 씌우면 항이 하나 더 붙는 MAP / 가중치에 N(0,τ²) 사전을 두면 ‖w‖²/2τ²가 떨어져 나와 L2 정규화가 되는 계산 / 라플라스 사전이 L1이 되는 것과, 「정규화를 얼마나 줄까」가 「가중치가 얼마나 퍼져 있다고 볼까」라는 물음이 되는 자리 / n이 커지면 사전의 몫이 상대적으로 작아져 MAP이 최대가능도로 수렴하는 것
  - 절「코드로 확인하기」 — 소절: 같은 자료에 라플라스를 가정해 최대가능도를 풀면 중앙값이 나오는 것을 격자로 확인 / L2 정규화를 넣은 최대화와 MAP이 같은 답을 주는지 맞대기 / 이상치를 하나 넣고 정규와 라플라스의 답이 갈리는 것을 수로

- `math-guidance-as-extrapolation` (지금 3,566자 · 절 7)
  - 절「코드가 실제로 하는 한 줄」 — 소절: 매 스텝 모델을 두 번 돌리므로 샘플링 비용이 두 배이고, 두 조건을 배치로 묶어 한 번에 돌리는 구현 / w를 노이즈가 아니라 x₀ 예측 쪽에 적용해도 같은 식이 되는 확인 / 매 스텝 기울인 것을 이어 붙인 결과가 「기울인 분포에서 한 번 뽑은 것」과 정확히 같지는 않다는 단서
  - 절「조건 없는 예측의 출처」 — 소절: 학습 때 조건을 10%쯤의 확률로 빈 임베딩으로 바꿔 한 모델이 두 가지를 다 배우게 하는 것 / 그래서 별도 분류기를 학습시키지 않는다는 것이 이름의 'classifier-free'인 자리 / 원래의 분류기 가이던스 — 잡음 낀 이미지에 대한 분류기를 따로 학습해 ∇log p(y|x)를 얻던 방법과 그것을 버린 이유 / 그림: 학습 때 조건이 무작위로 빠지는 것과 추론 때 둘을 다 부르는 것을 나란히
  - 절「스코어로 바꿔 읽기」 — 소절: 두 스코어의 차가 「조건이 가리키는 방향」이라는 읽기와, 그 방향으로 얼마나 더 가느냐가 w인 것 / 기하평균 기울이기가 지수족에서 자연 파라미터의 선형결합이라 정규분포에서 정확히 풀리는 자리 / 0.5^{7.5}≈0.0055로 어중간하게 맞는 것이 사라지는 계산
  - 절「정확히 풀리는 두 경우」 — 소절: 두 경우가 각각 무엇을 대표하는지 — 하나는 폭, 하나는 모드 / 두 봉우리의 간격을 좁혀 가며 같은 격자 적분을 되풀이하면 언제부터 두 봉우리가 하나로 합쳐지는지 / 실제 이미지에서 「봉우리 둘」에 해당하는 것이 프롬프트의 두 가지 해석이라는 대응 / w를 축마다 다르게 주는 것이 왜 안 되는지 — 모델이 축을 알려 주지 않는다
  - 절「같은 식에서 나오는 변형들」 — 소절: 부정 프롬프트 — 조건 없는 자리에 다른 조건을 넣으면 기울인 분포가 p(x|y_neg)^{1−w}p(x|y)^w가 되는 것 / w를 시각에 따라 바꾸는 스케줄 — 초반에 낮게 두어 다양성을 남기고 후반에 올리는 이유 / 값 범위를 넘어간 것을 되돌리는 동적 임계와, 그것이 화면은 살려도 평균 이동 자체를 없애지는 않는다는 한계 / 그림: 세 변형이 같은 한 식의 어느 자리를 건드리는지 표시한 도식
  - 절「7.5가 정하는 것」 — 소절: w를 고르기 전에 무엇을 재야 하는지 — 한 프롬프트로 여러 장을 뽑아 폭과 모드 소실을 따로 보기 / 모델마다 「좋은 w」가 다른 이유는 학습 때의 조건 드롭아웃 비율과 조건부 스코어의 정확도라는 것

- `math-basics-taylor-first-steps` (지금 3,574자 · 절 9)
  - 절「접선이라는 1차 근사」 — 소절: 근사의 오차가 h가 아니라 h²에 비례한다는 것을 e^{0.1}과 e^{0.05}로 재서 확인하기 — 넷으로 준다 / 1차 근사가 도함수의 정의를 뒤집어 적은 것이라는 읽기 — f(a+h)−f(a)≈f'(a)h / 그림: h를 반으로 줄일 때 접선과 곡선 사이의 간격이 넷으로 주는 것을 두 눈금으로
  - 절「2차 항과 일반 항」 — 소절: 계수를 정하는 절차가 「n번 미분해 a를 넣으면 n!c_n만 남는다」 한 줄인 것 / 0!=1로 두는 것이 약속이 아니라 그 절차가 n=0에서도 성립하게 하는 값인 자리 / 다항식은 자기 자신이 유한 항의 테일러 전개라는 확인과, 중심을 옮기면 계수가 달라지는 것
  - 절「자주 쓰는 전개 여섯」 — 소절: sin에 홀수 차수만, cos에 짝수 차수만 있는 것이 기함수·우함수와 맞는 것 / 이미 아는 전개에서 새 전개를 만드는 요령 — e^{−x²}은 eˣ에 −x²을 넣고, ln(1+x)는 1/(1+x)를 항마다 적분한 것 / (1+x)^α가 α가 자연수면 유한 항에서 끊겨 이항정리가 되는 자리 / 그림: 차수를 1·3·5·7로 올릴 때 sin 곡선에 점점 붙어 가는 네 다항식
  - 절「손으로 재는 오차와 범위」 — 소절: n차에서 자르면 버린 첫 항이 곧 오차의 크기라는 어림을 네 예에서 확인 / 부호가 번갈아 바뀌는 급수에서는 그 어림이 상한이 되는 것(sin, cos, ln(1+x)) / 같은 x에서도 중심을 옮기면 오차가 달라지는 것 — √4.1을 a=0과 a=4에서 각각 펴 보기 / 1/(1−x)가 |x|<1 밖에서는 아예 다른 값이 되는 자리
  - 절「세 급수와 오일러 공식」 — 소절: x 대신 ix를 넣는 것이 허용되는 조건 한 줄 — 급수가 절대수렴하면 항을 갈라 모아도 된다 / cosh와 sinh를 같은 자리에서 얻어 eˣ=cosh x+sinh x로 적기 / 오일러 공식을 얻고 나면 덧셈정리가 지수법칙 한 줄이 되는 것을 되짚기
  - 절「연습 문제」 — 소절: 버린 첫 항으로 오차를 미리 어림하고 실제와 맞대는 네 문제 / a를 0이 아닌 곳에 잡아야 하는 두 문제(√9.1, ln 1.1을 a=1 근처에서) / 이미 아는 전개에서 새 전개를 만드는 세 문제(e^{−x²}, 1/(1+x²), ln(1−x)) / cosh와 sinh의 전개를 eˣ에서 직접 만들어 보기

- `math-gradient-checking` (지금 3,574자 · 절 8)
  - 절「두 차분과 절단오차의 차수」(원고 「두 가지 차분」과 「절단오차: 테일러로 세어 보기」를 한 절로) — 소절: 중심차분이 함수를 두 번 새로 부른다는 대가 — 전진차분은 f(x)를 재활용해 호출이 한 번뿐 / f'''이 0에 가까운 자리에서는 중심차분의 실측 오차가 예측식보다 훨씬 아래로 내려간다는 예외
  - 절「반올림오차와 자릿수 소실」 — 소절: x가 10⁶이면 절대 h=10⁻⁵가 유효숫자에 안 잡혀 상대 스텝 h·max(1,|x|)를 써야 한다는 것 / 손실을 float64로 캐스팅할 때 중간 계산 하나라도 float32면 ε가 그쪽 값으로 결정된다는 자리
  - 절「최적 스텝 h」 — 소절: 비례가 아니라 상수까지 넣은 h_*=(3ε|f|/|f'''|)^{1/3} / |f|/|f'''|가 100배 갈려도 세제곱근이라 h_*는 4.6배만 움직인다 — 1e-5 하나를 기본값으로 굳혀도 되는 근거 / 복소 스텝 Im f(x+ih)/h는 뺄셈이 없어 V자의 오른쪽 절반이 통째로 사라지지만 abs·max가 끼면 못 쓴다는 조건
  - 절「상대오차와 통과 기준」(원고 「무엇과 비교할 것인가」) — 소절: 출력이 벡터면 무작위 u와 내적해 스칼라로 만든 뒤 재는 것 — 야코비안 m×n칸을 다 재지 않아도 된다 / 무작위 방향 v 하나로 ∇f·v를 재면 좌표 백만 개가 한 번에 덮인다는 계산 / gradcheck의 eps·atol·rtol이 이 글의 h와 판정 대역 중 어느 자리인가
  - 절「검사하지 말아야 할 자리」 — 소절: 드롭아웃·데이터 증강이 있으면 f(x+h)와 f(x−h)가 다른 마스크를 써서 검사가 무조건 실패한다 — 좌표마다 시드를 되돌린다 / BatchNorm 학습 모드는 배치 전체에 의존해 좌표 하나를 흔들면 다른 좌표의 출력까지 바뀐다는 것
  - 절「검사 절차와 기본값」(원고 「절차로 굳히기」) — 소절: 좌표를 전부 도는 대신 무작위 몇 개만 뽑아도 되는 근거 — 유도 실수는 대개 좌표 전체에 걸린다 / 통과했는데도 틀리는 유일한 자리 — 유도와 검사가 같은 잘못된 순전파를 공유하는 경우

- `math-cross-entropy-and-nll` (지금 3,579자 · 절 7)
  - 절「교차엔트로피의 정의」 — 소절: 이진 분류의 −[y log q+(1−y)log(1−q)]가 V=2를 넣은 같은 식이라는 확인 / 클래스 가중치 weight=는 목표 분포가 아니라 항마다 저울을 다는 조작이라 p를 바꾸는 스무딩과 자리가 다르다는 대비
  - 절「원-핫에서의 항 접힘」 — 소절: 프레임워크가 확률이 아니라 로짓을 받는 이유 — log_softmax와 nll_loss를 한 연산으로 붙여야 log 0이 안 나온다 / ignore_index는 분포를 안 건드리고 합에서 항을 빼고, mean이 그때 유효 토큰 수로 나눈다는 것 / 패딩을 안 빼면 배치의 패딩 비율만큼 손실이 희석되어 같은 모델의 값이 배치마다 달라지는 자리
  - 절「라벨 스무딩과 목표 분포」 — 소절: 손실 바닥 H(p')을 직접 계산해 V=4·ε=0.1에서 0.575비트 / 어휘 50,257에서는 ε/V=2×10⁻⁶이라 바닥이 0.06내트 남짓이고 로그에서는 안 보이는 크기 / 지식 증류에서 교사 분포를 p 자리에 놓고 온도 T를 쓰면 그래디언트가 1/T²로 줄어 손실에 T²를 곱하는 자리
  - 절「배치 손실의 추정 오차」 — 소절: reduction이 'sum'이면 배치를 두 배로 늘릴 때 그래디언트도 두 배라 학습률과 얽힌다 / 'mean'이 표준이 된 이유 — 배치 크기와 학습률을 따로 고를 수 있다
  - 절「손실·perplexity·bpt」 — 소절: 어휘 크기가 정하는 상한 log V — 아무것도 모르는 모델의 손실이 10.8내트이고 그것이 눈금의 0점이라는 읽기 / 토큰 단위 perplexity와 문서 단위가 다른 값이 되는 자리
  - 절「코드로 확인하기」 — 소절: ignore_index를 켠 것과 끈 것의 손실 차이를 실제로 재기 / weight=와 label_smoothing을 함께 켰을 때 바닥이 어디로 가는지

- `math-vjp-and-jvp` (지금 3,579자 · 절 7)
  - 절「곱하는 순서와 비용」 — 소절: 역전파가 순전파의 두 배인 이유 — y=Wx의 VJP가 입력 쪽 uᵀW와 가중치 쪽 uxᵀ 둘이라 곱셈이 두 벌 / 그 「두 배」가 층 종류에 따라 1.5배에서 3배까지 갈리는 자리
  - 절「JVP와 VJP의 정의」 — 소절: 이중수 a+bε(ε²=0)로 JVP를 구현하는 방식 — 값과 미분이 한 번의 순전파로 함께 흐른다 / v를 여러 개 묶어 한 번에 굴리면 열 여러 개가 동시에 채워진다는 것
  - 절「층으로 나눈 곱」 — 소절: 갈래가 진 그래프에서 VJP가 더해지는 규칙과 그것이 .grad 누적·zero_grad()로 나타나는 자리 / 브로드캐스팅의 VJP는 편 축을 따라 합이라 편향의 그래디언트가 배치 축 합이 되는 이유
  - 절「전체 야코비안까지의 횟수」 — 소절: 두 모드를 겹쳐 Hv를 얻는 조합(안쪽 VJP, 바깥 JVP)과 그때 헤세가 만들어지지 않는 자리 / m=n이면 횟수는 같은데 메모리가 갈린다는 것
  - 절「손실이 스칼라라는 조건」 — 소절: 손실이 둘 이상인 다중 목적 학습이면 m>1이라 역방향을 m번 굴려야 하고, 가중합으로 스칼라를 먼저 만드는 관행이 그 비용을 피한다 / 체크포인팅을 √L 구간으로 나눌 때 메모리가 O(√L)이고 계산이 1.33배라는 셈
  - 절「코드로 확인하기」 — 소절: 갈래 진 그래프에서 VJP가 더해지는 것을 손으로 확인 / 이중수 열 줄짜리 구현으로 JVP와 손 미분을 대조

- `math-lagrange-and-kkt` (지금 3,601자 · 절 6)
  - 절「제약 위의 정지 조건」 — 소절: 최소화로 적으면 ∇f=λ∇g의 부호가 뒤집혀 교재마다 식이 달라 보이는 자리 / 제약 집합이 곡선이 아니라 점 하나로 오그라드는 예 — 접선 방향이 없어 논증 자체가 시작되지 않는다
  - 절「단위원 위의 손계산」 — 소절: λ=−1/√2인 둘째 해가 최소점이라는 것 — 라그랑주는 최대와 최소를 함께 내놓고 어느 쪽인지는 값을 넣어 가른다 / 그림: 등고선 x+y=c가 원에 접하는 두 자리와 각각의 승수 부호
  - 절「부등식 제약과 KKT」 — 소절: 상보 여유의 두 인수가 동시에 0이 되는 경계 경우(λ=0이면서 g=c)와 그 자리가 불안정한 이유 / 최소화·최대화·제약 방향 세 가지 부호를 한 표로 정리해 λ≥0이 어느 조합에서 나오는지
  - **새 절**「제약이 여럿인 문제」 — 소절: 등식 제약 m개면 ∇f=Σλᵢ∇gᵢ이고 승수도 m개 / 부등식이 섞이면 활성 제약만 살고 나머지는 λ=0으로 빠지는 활성 집합 / ∇gᵢ가 일차종속이면 승수가 유일하지 않거나 아예 없다는 제약 자격조건과, 같은 제약을 두 번 적었을 때가 그 예 / 그림: 두 제약이 접하는 자리와 가로지르는 자리에서 ∇g 둘이 만드는 원뿔
  - 절「승수의 그림자 가격 해석」 — 소절: 활성 집합이 바뀌는 c에서 f*(c)가 꺾여 미분이 좌우로 갈리는 자리 / 그래서 예산 방식이 학습 중에 튀고, 가격을 직접 정하면 그 꺾임을 안 밟는다는 것
  - 절「KL 예산 문제의 라그랑지안」 — 소절: π가 확률분포라 Σπ=1 제약이 하나 더 있고 승수가 둘이라는 것 — 다음 글의 닫힌 해가 그 둘째 승수의 정규화에서 나온다 / 쌍대 함수 d(β)=max_π L(π,β)가 β에 대해 볼록이고 그래서 β를 올리면 KL이 단조로 줄어든다는 근거

- `math-loss-landscape-and-convexity` (지금 3,608자 · 절 6)
  - 절「볼록집합과 볼록함수」 — 소절: 강볼록 μI ⪯ ∇²f가 주는 것 — 수렴이 (1−μ/L)^k로 지수적이라는 속도 보장 / L2 항 λ‖θ‖²/2를 더하면 강볼록이 되는가 — 신경망에서는 국소적으로만 참인 이유
  - 절「볼록성이 보장하는 것」 — 소절: 볼록이 보장하지 않는 것 둘 — 최소점의 유일함도, 최소점의 존재도 아니다 / 선형분리 가능한 데이터에서 로지스틱 회귀의 최소가 무한대에 있다는 예 / 그림: 바닥이 평평한 볼록함수와 한없이 내려가는 볼록함수
  - 절「치환 대칭성과 비볼록성」 — 소절: 중점을 잇는 직선 대신 곡선으로 이으면 손실이 낮게 이어진다는 관찰 / 반례가 부순 것은 「직선」이지 「길」이 아니라는 정리
  - **새 절**「ReLU의 스케일 대칭」 — 소절: relu(αz)=α·relu(z)라 w←αw·v←v/α가 글자 그대로 같은 함수를 준다는 확인 / 손실이 정확히 같은 점이 곡선으로 이어져 최소점이 점이 아니라는 것 / 그 곡선 방향의 헤세 고윳값이 0이므로 신경망의 헤세는 양정치일 수 없고 양반정치가 최선이라는 결론 / 가중치 감쇠가 이 곡선 위에서 ‖w‖=‖v‖인 자리를 고른다 / 그림: α를 바꾸며 파라미터가 옮겨 가도 손실이 평평한 골짜기
  - 절「임계점 네 갈래」 — 소절: 위 대칭이 0 고윳값을 강제하므로 실제 헤세의 스펙트럼에 0 근처 덩어리가 크다는 것 — 표의 「평지」 줄이 예외가 아니라 기본 / 안장점의 지수(음의 고윳값 개수)가 손실이 낮을수록 작아진다는 반복 관찰
  - 절「코드로 확인하기」 — 소절: α를 바꿔 가며 손실이 그대로임을 재고 그 방향의 수치 곡률이 0에 붙는 것 / 두 최소를 직선과 이차 곡선으로 각각 이어 손실 단면을 뽑아 비교

- `math-high-dimensional-geometry` (지금 3,639자 · 절 6)
  - 절「껍질로 쏠리는 부피」 — 소절: 구면 위에서 아무 적도 띠나 폭 1/√d 안에 넓이의 대부분이 담긴다는 측도 집중 / 부피가 껍질에 있다는 것과 코사인이 0에 몰린다는 것이 같은 현상의 두 판이라는 연결
  - 절「무작위 두 벡터의 각도」 — 소절: 길이를 1로 맞추면 ‖a−b‖²=2−2cos이라 코사인 순위와 유클리드 순위가 정확히 같아진다는 계산 / 정규화하지 않으면 두 순위가 갈리고 노름이 큰 벡터가 내적 상위를 독차지하는 자리
  - 절「거리 집중과 상대 대비」 — 소절: 공간을 격자로 나누는 색인이 2^d 칸을 요구해 d=30에서 이미 십억 칸이라는 셈 — 색인이 격자에서 거리 기반으로 옮겨 간 이유
  - 절「내재 차원이 정하는 것」 — 소절: 내재 차원을 실제로 재는 두 방법 — 특잇값의 참여 비율 (Σσ²)²/Σσ⁴, 그리고 두 최근접 거리의 비로 재는 방식 / 실제 임베딩에서 그 값이 주변 차원의 몇 %로 나오는지 / 평균 벡터를 빼면 무관 쌍의 코사인이 0.7~0.8에서 0 근처로 돌아오는 실험
  - **새 절**「최근접 목록의 허브」 — 소절: 자기가 남의 k-최근접 목록에 몇 번 등장하는지를 세면 분포가 오른쪽으로 길게 늘어진다는 것 / 원인은 데이터 중심에 조금 가까운 점이 모두에게 조금씩 가깝다는 거리 집중의 부산물 / 「어떤 질의를 넣어도 같은 문서가 뜬다」는 증상이 이 셈으로 설명되는 자리 / 상호 근접과 중심화가 허브를 줄이는 두 손잡이 / 그림: 등장 횟수 히스토그램을 d=10과 d=1000에서 나란히
  - 절「되는 자리와 안 되는 자리」 — 소절: 임계값을 절대값 대신 무관 쌍 분포의 분위수로 잡는 실무 / 그 분포를 데이터에서 뽑는 절차 세 줄

- `math-spectral-theorem-and-quadratic-forms` (지금 3,662자 · 절 7)
  - 절「대칭 행렬이 나오는 세 자리」 — 소절: 대칭이 아닌 자리도 있다 — 야코비안과 층 가중치 W는 대칭이 아니고 그래서 다음 글의 특잇값이 필요하다는 경계 / 비대칭 A를 (A+Aᵀ)/2와 (A−Aᵀ)/2로 갈라도 이차형식에는 앞쪽만 남는다는 계산
  - 절「스펙트럼 정리」 — 소절: 고윳값이 실수라는 부분의 증명 — v̄ᵀAv를 두 방향으로 계산해 λ=λ̄를 얻는 세 줄 / 증명이 다루지 않은 자리 — λ₁=λ₂인 중근에서는 고유공간이 평면이라 직교하는 둘을 골라 잡아야 한다는 것 / QΛQᵀ에서 대각만 손보면 A^{1/2}와 A^{−1/2}가 나오고, 공분산의 A^{−1/2}가 화이트닝이라는 자리
  - 절「이차형식의 정의」 — 소절: 등고선 xᵀAx=1이 타원이고 축 길이가 1/√λᵢ라는 것 / 고윳값 비가 클수록 길쭉해지고 그 비가 다음 단원의 조건수라는 예고 / 그림: 양정치와 부정부호 두 행렬의 등고선을 나란히 — 타원과 쌍곡선
  - 절「고유기저에서의 대각화」 — 소절: 길이 1인 x에서 xᵀAx가 λ_min과 λ_max 사이에 놓이고 양 끝이 고유벡터에서 도달된다는 레일리 몫 / 그래서 이차형식의 최댓값을 구하는 일이 최대 고윳값을 구하는 일과 같다는 정리 / 제약 최적화로 적으면 라그랑주 승수가 곧 고윳값으로 떨어지는 대응
  - 절「부호 판정의 다섯 이름」 — 소절: 3×3 이상에서 쓰는 실베스터 판정법 — 왼쪽 위부터 잘라 낸 부분행렬의 행렬식이 전부 양수면 양정치 / 실무에서는 촐레스키 분해가 도는지로 판정하고 그것이 O(n³/3)로 고윳값보다 싸다는 이유 / 판정 보류(고윳값 0)가 딥러닝에서 예외가 아니라 기본인 자리
  - 절「코드로 확인하기」 — 소절: 대칭 행렬에 eig와 eigh를 나란히 걸어 앞쪽에 허수부가 붙는 것을 보기 / 촐레스키가 실패하는 문턱과 eigvalsh의 1e-12 문턱이 어긋나는 예

- `math-basics-limits` (지금 3,684자 · 절 9)
  - 절「표로 보는 수렴」 — 소절: 표만 보면 속는 자리 — n=10까지로는 붙는 곳이 안 보이는 (1+10⁻⁶)ⁿ 같은 예 / 그래서 표는 짐작이고 확정은 대수라는 규약이 필요한 것
  - 절「rⁿ의 네 갈래」 — 소절: |r|<1을 「1보다 작은 수를 거듭 곱한다」가 아니라 로그로 확인하기 — n log|r|이 −∞로 간다 / r이 1에 아주 가까울 때(0.999) 0.001 아래로 내려가는 데 걸리는 n을 세어 「결국 0」과 「금방 0」을 가르기
  - 절「좌극한과 우극한」 — 소절: 좌우극한이 달라 극한이 없는 것과, 좌극한조차 없는 것의 구별 — sin(1/x)를 x→0에서 표로 채우면 값이 안 정해진다 / x→∞에는 다가가는 길이 하나뿐이라 좌우를 안 나눈다는 것
  - 절「부정형을 푸는 세 조작」(원고 「커지는 속도에도 서열이 있다」를 이 절의 소절로 흡수) — 소절: 부정형 네 꼴 표 — 0/0, ∞/∞, ∞−∞, 0·∞ / ∞−∞ 꼴 √(n²+n)−n을 유리화로 1/2로 만들기 — 유리화가 세 조작 중 하나가 아니라 두 자리에서 쓰인다는 것 / 커지는 속도의 서열(로그<다항<지수)이 ∞/∞를 눈으로 가르는 도구라는 자리 / 0·∞를 분수로 고쳐 앞의 셋으로 되돌리는 요령
  - 절「연속의 세 조건」(원고 「여기서 멈추는 것」을 이 절의 마지막 문단으로) — 소절: 연속인 것끼리 더하고 곱하고 합성해도 연속이라는 표 — 그래서 다항·유리·지수·로그가 정의역에서 연속 / 중간값 정리 — 닫힌 구간에서 연속이면 두 값 사이를 전부 지나고, x³−x−1=0의 해가 1과 2 사이에 있다는 확인 / 샌드위치 정리 — 위아래로 낀 수열은 같은 곳에 붙고 (−1)ⁿ/n이 0으로 가는 것을 그것으로 / 그림: 위아래 두 곡선 사이에 낀 수열이 함께 한 값으로 오므라드는 모양
  - 절「연습 문제」 — 소절: ∞−∞ 세 문제를 새로 — √(n²+n)−n, √(n²+3n)−n, n−√(n²−4n) / 샌드위치로 푸는 두 문제 / 중간값 정리로 해의 존재를 보이는 두 문제

- `math-log-sum-exp-and-online-softmax` (지금 3,694자 · 절 5)
  - 절「최댓값 빼기의 항등성」 — 소절: m을 최댓값이 아니라 평균으로 잡아도 되는가 — 점수 폭이 좁으면 되고 넓으면 안 되는 경계 / 두 항짜리 LSE를 max+log1p(e^{−|a−b|})로 적으면 차이가 클 때 자릿수가 안 깎이는 이유
  - **새 절**「매끄러운 최댓값으로서의 LSE」 — 소절: max(x) ≤ LSE(x) ≤ max(x)+log n이라는 두 줄 부등식과 n=50,257에서 그 폭이 10.8 / ∂LSE/∂xᵢ가 정확히 softmax라는 계산 — 분모를 미분하면 분자가 나오는 자리 / T·LSE(x/T)가 T→0에서 max로, T→∞에서 평균으로 가는 두 극단 / 그림: 두 점수에서 max의 꺾인 선과 LSE의 둥근 선을 겹쳐 그리기
  - 절「−inf 마스킹」 — 소절: 큰 음수를 쓸 때 얼마나 커야 하는가 — float16에서 −1e9는 −inf로 넘쳐 NaN이 되돌아오므로 −65504 언저리를 쓴다 / 마스크를 점수에 더하는 것과 softmax 뒤에 곱하는 것이 역전파에서도 갈리는 자리
  - 절「온라인 softmax 점화식」 — 소절: 블록이 수천 개면 ℓ에 부동소수점 오차가 쌓인다는 것과 그 크기를 재는 셈 / 블록 크기를 키우면 오차는 줄고 빠른 메모리는 더 드는 맞바꿈
  - **새 절**「역전파가 다시 쓰는 LSE」 — 소절: 순전파에서 행마다 LSE 하나만 남기면 P를 저장하지 않고 P=e^{S−LSE}로 되살릴 수 있다는 계산 / n×m짜리 P 대신 길이 n짜리 벡터 하나라 저장이 m배 줄어드는 자리 / 되살리는 데 드는 재계산이 순전파 한 번어치라 메모리를 계산으로 되사는 같은 거래 / 그림: 저장하는 것과 되살리는 것을 순전파·역전파 축에 나란히
  - 절「코드로 확인하기」 — 소절: LSE의 미분을 유한차분으로 재어 softmax와 맞대기 / 블록 천 개로 쪼갠 온라인 합과 한 번에 계산한 값의 마지막 자릿수 차이

- `math-svd-and-rank` (지금 3,694자 · 절 9)
  - 절「AᵀA의 대칭성과 양반정치」(원고 「고윳값으로는 접근조차 안 되는 자리」와 「AᵀA는 언제나 대칭이다」를 한 절로) — 소절: AAᵀ로 해도 같은 고윳값이 나오고 0이 |m−n|개 더 붙는다는 것 — m≫n이면 작은 쪽으로 계산하는 실무 / σᵢ>0인 자리만 uᵢ가 정의되고 나머지 열은 직교여공간에서 채워 넣는다는 것
  - 절「회전·늘림·회전」 — 소절: A=Σσᵢuᵢvᵢᵀ로 랭크-1 행렬의 합으로 적기 — 같은 분해를 곱으로도 합으로도 읽는 자리 / (uᵢ,vᵢ) 쌍을 함께 뒤집어도 같은 분해라 라이브러리마다 부호가 달라 보인다는 것 / 축소 SVD와 전체 SVD가 U의 몇 열을 남기느냐로만 갈린다는 것
  - 절「3×2의 손계산」 — 소절: 남은 u₃를 직교여공간에서 채워 전체 U를 완성하기 / V의 두 열과 U의 두 열이 각각 어느 부분공간의 기저인지 이름 붙이기
  - 절「전단에서 갈리는 두 분해」 — 소절: 회전행렬은 실수 고윳값이 아예 없는데 특잇값은 둘 다 1이라 「길이를 안 바꾼다」를 정확히 말한다는 것 / 대칭이 아니면 σ₁이 |λ|의 최댓값보다 클 수 있다는 부등식과 전단의 1 대 1.618
  - 절「랭크와 수치 랭크」(원고 「랭크」와 「수치 랭크」를 한 절로) — 소절: U의 앞 r열이 열공간의 정규직교기저이고 V의 뒤 n−r열이 영공간의 기저라는 네 부분공간 대응 / 조건수 σ₁/σ_r과 그것이 「이 행렬을 뒤집으면 오차가 몇 배로 커지는가」라는 읽기 / 유사역행렬 A⁺=VΣ⁺Uᵀ가 랭크가 모자라도 정의되고 최소제곱의 답을 준다는 것 / 그림: 특잇값 막대 위에 문턱선을 긋고 살아남는 축과 뭉개지는 축을 갈라 표시
  - 절「코드로 확인하기」 — 소절: 부호를 뒤집어도 UΣVᵀ가 같은 A를 준다는 확인 / pinv와 lstsq가 랭크가 모자란 행렬에서 같은 답을 내는지 재기

- `math-basics-linear-equations` (지금 3,701자 · 절 11)
  - 절「해와 등식의 성질 넷」(원고 「해」와 「등식의 성질 넷」을 한 절로) — 소절: 성질 넷이 전부 되돌릴 수 있는 조작이라는 것이 해가 안 바뀌는 진짜 이유 / 0을 곱하면 되돌릴 수 없어 2x=4가 0=0이 되고 해가 모든 수로 늘어난다 — 「0으로 나눌 수 없다」의 반대쪽 금지 / 양변을 제곱하는 것도 되돌릴 수 없어 없던 해가 생긴다는 예고
  - 절「푸는 절차 다섯 걸음」(원고 「이항」·「푸는 절차」·「검산」을 한 절로) — 소절: 소수 계수는 10이나 100을 곱해 정수로 만든다 — 0.3x+0.5=1.4를 3x+5=14로 / 분수와 소수가 섞이면 소수를 분수로 먼저 고치는 순서 / 검산이 틀렸을 때 어느 걸음에서 새었는지 좁히는 법 — 이항을 한 번 되돌려 중간 줄부터 다시 넣어 보기
  - 절「해가 없거나 무수한 경우」 — 소절: ax=b 한 줄로 세 갈래를 통일 — a≠0이면 해 하나, a=0·b≠0이면 없음, a=0·b=0이면 모든 수 / 앞의 두 예가 그 표의 두 줄이라는 확인 / 그림: a와 b의 영 여부로 갈리는 세 갈래 표
  - 절「특정 문자에 대해 풀기」(원고 「특정 문자에 대해 풀기」와 「비례식은 이미 푼 것이다」를 한 절로) — 소절: 남길 문자가 여러 항에 흩어져 있으면 먼저 그 문자로 묶는다 — ax+b=cx+d를 x에 대해 풀며 a≠c 단서 달기 / 비례식이 이 절의 한 경우라는 것 — a:b=c:d를 d에 대해 푼 것이 d=bc/a
  - 절「연립방정식의 두 소거법」 — 소절: 연립도 세 갈래라는 것 — x+y=1과 x+y=3, x+y=1과 2x+2y=2를 각각 소거해 보기 / 소거 뒤 남은 것이 0=수인지 0=0인지로 가르는 판정이 앞 절과 같다는 것 / 세 갈래가 두 직선이 만나는 세 모양이라는 대응은 24번 · 좌표평면과 직선으로 넘긴다
  - 절「연습 문제」 — 소절: 소수 계수 다섯 문제를 새로 / ax=b에서 a에 따라 갈래를 답하는 세 문제 / 연립의 갈래를 판정하는 세 문제 / 답이 조건에 안 맞아 버려야 하는 문장제 두 문제

- `math-basics-counting-and-binomial` (지금 3,724자 · 절 9)
  - 절「합의 법칙과 곱의 법칙」(원고 「포함배제 원리」를 이 절로 옮김) — 소절: 되풀이해 고르는 것에 이름 붙이기 — 중복순열 nʳ이고 네 자리 비밀번호 10⁴가 그 예 / 포함배제가 합의 법칙의 보정이라는 자리 — 겹치면 한 번 뺀다 / 세 집합의 포함배제 — 더하고 빼고 다시 더하는 부호 규칙과 1~100의 2·3·5의 배수
  - 절「팩토리얼과 순열」 — 소절: 같은 것이 있는 순열 n!/(p!q!) — 같은 것끼리 자리를 바꾼 p!·q!가 한 묶음이라 나눈다 / 격자에서 오른쪽 3칸·위 2칸으로 가는 최단경로가 5!/(3!2!)=10이고 그것이 C(5,2)와 같은 수라는 것 / 원순열 (n−1)! — 돌려서 같아지는 n가지가 한 묶음
  - 절「조합 C(n,k)」 — 소절: 같은 것이 있는 순열과 조합이 사실 한 식이라는 확인 — ○ k개와 × n−k개를 늘어놓는 방법이 C(n,k) / 큰 수에서 계산하는 요령 — 분자를 k개만 곱하되 한 번 곱할 때마다 나눠 정수를 유지하기
  - 절「파스칼 삼각형과 이항정리」(원고 두 절을 한 절로) — 소절: 한 줄의 합이 2ⁿ이라는 것 — (1+1)ⁿ을 이항정리에 넣은 결과이자 부분집합의 개수 / 부호를 번갈아 더하면 0이라는 것과 (1−1)ⁿ / 한 줄에서 가운데가 가장 크고 양 끝으로 갈수록 급히 줄어드는 모양 / 그림: 파스칼 여덟 줄 오른쪽에 줄마다 합 2ⁿ을 적어 나란히
  - 절「손으로 세 가지」 — 소절: 생일 문제를 짝의 수로 어림하기 — C(30,2)=435쌍에 각각 1/365이면 기대 겹침이 1.19쌍 / 그 어림값과 정확한 0.7063이 얼마나 가까운지 / 로또를 두 장 사면 확률이 두 배가 되는가 — 서로 배반이라 그냥 더한다는 확인
  - 절「연습 문제」 — 소절: 같은 것이 있는 순열 세 문제를 새로 / 격자 최단경로 두 문제 / 한 줄의 합이 2ⁿ임을 쓰는 두 문제 / 세 집합 포함배제 두 문제

- `math-softmax-jacobian-and-attention-gradient` (지금 3,726자 · 절 8)
  - 절「기호·shape와 바깥 두 미분」(원고 「기호와 shape를 먼저 못 박는다」와 「∂L/∂V와 ∂L/∂P」를 한 절로) — 소절: 배치와 헤드 축을 붙이면 같은 다섯 줄이 einsum 한 줄씩으로 그대로 옮겨진다는 것 / 헤드가 서로 독립이라 W_Q의 그래디언트가 헤드별 기여의 단순 합이 되는 자리
  - 절「∂L/∂S와 행별 규칙」 — 소절: 마스크가 있으면 가린 자리의 P가 0이라 Δ도 0이 되어 따로 마스킹할 필요가 없다는 확인 / −∞ 대신 −10⁹을 쓰면 P가 정확히 0이 아니라 Δ에 미세한 값이 남는다는 것과 그 크기 / 드롭아웃이 P에 걸릴 때 마스크가 U 쪽으로 들어가고 r도 함께 바뀌는 자리
  - 절「∂L/∂Q와 ∂L/∂K」 — 소절: 1/√d_k가 순전파와 역전파에서 각각 한 번씩만 곱해진다는 확인 — 두 번 곱하는 흔한 구현 실수 / Δ의 행 합이 0이라는 성질이 「한 행의 점수에 상수를 더하는 어떤 구현도 그래디언트를 안 바꾼다」로 읽히는 자리
  - 절「뾰족한 분포에서의 기울기」 — 소절: 뾰족함을 미리 재는 값 — 행의 엔트로피나 최대 P를 로그에 찍어 두면 조용한 붕괴가 눈에 보인다 / 배수를 키운 표에서 ‖∂L/∂Q‖가 오르다 꺾이는 봉우리가 어느 배수이고 그때 최대 P가 얼마인지
  - 절「잔차 연결이 살리는 경로」 — 소절: LayerNorm이 사이에 끼면 항등 경로가 그대로가 아니라는 것 — pre-LN과 post-LN에서 I가 남는 자리가 다르다 / 잔차·1/√d_k·정규화 셋이 각각 막는 자리를 한 표로
  - 절「코드로 확인하기」 — 소절: 마스크를 −inf와 −1e9로 각각 두고 Δ를 재어 차이 보기 / P를 저장하지 않고 LSE로 되살려 같은 Δ가 나오는지 확인 — 다음 글의 온라인 softmax가 쓰는 그 저장 방식

- `math-matrix-norms` (지금 3,727자 · 절 9)
  - 절「유도 노름과 σ_max」(원고 「벡터의 자를 행렬로 옮기기」와 「‖A‖₂ = σ_max」를 한 절로) — 소절: 노름이 갖춰야 할 세 조건(0일 때만 0, 상수배, 삼각부등식)과 유도 노름이 그것을 만족하는 확인 / L¹과 L∞가 유도한 노름은 최대 열합·최대 행합이라 SVD 없이 곧바로 나온다는 것 / 두 값으로 ‖A‖₂ ≤ √(‖A‖₁‖A‖∞)라는 상한을 공짜로 얻는 자리
  - 절「프로베니우스 노름과 손계산」(원고 「프로베니우스 노름」과 「손으로 한 번」을 한 절로) — 소절: 프로베니우스가 유도 노름이 아니라는 근거 — ‖I‖_F=√n인데 유도 노름이면 반드시 1 / 그런데도 부분곱셈성은 만족한다는 것과 코시-슈바르츠 두 줄 / 특잇값을 전부 더한 핵 노름 Σσᵢ가 세 번째 요약이고 저계수 근사의 볼록 완화라는 예고
  - 절「부분곱셈성」 — 소절: 실제 층들의 노름 곱과 전체 립시츠 상수를 재어 상한이 몇 배 헐거운지 / 층이 열이면 그 여유가 곱해져 상한이 수천 배가 되는 자리
  - 절「립시츠 상수」 — 소절: 클리핑의 식 g←g·min(1, c/‖g‖)와 그것이 방향을 보존하고 크기만 자른다는 확인 / 스펙트럼 정규화가 치르는 대가 — 모든 특잇값이 1 이하라 층이 절대 늘리지 못하고 표현력이 줄어든다 / 편향과 정규화 층은 이 상한 밖에 있다는 경계
  - 절「거듭제곱법으로 재기」 — 소절: 조건수 σ₁/σ_min이 ‖A‖₂‖A⁻¹‖₂와 같다는 계산 / 거듭제곱법은 최대만 주므로 최소를 얻으려면 역행렬 쪽에 같은 방법을 쓴다는 것 / σ₁≈σ₂이면 갭이 없어 수렴이 느려지는 자리와 그때 시작 벡터를 여러 개 잡는 대응
  - 절「코드로 확인하기」 — 소절: 세 유도 노름(1·2·∞)과 프로베니우스를 한 행렬에서 나란히 재기 / ‖I‖_F=√n으로 프로베니우스가 유도 노름이 아님을 눈으로

- `math-jacobian` (지금 3,732자 · 절 7)
  - 절「야코비안의 정의와 배치」 — 소절: m=1이면 행이 하나뿐이고 그것을 세운 것이 그래디언트라는 것 — 분자 배치와 분모 배치가 전치 하나로 갈린다 / 그래디언트 장 ∇f의 야코비안이 헤세이고, 그래서 헤세가 대칭이라는 대응 / 입력이 배치·채널처럼 다차원이면 야코비안이 4차 텐서가 되고 축을 펴서 행렬로 되돌린다는 것
  - 절「점에서의 선형사상」 — 소절: 근사가 정확해지는 속도 — h를 절반으로 줄이면 오차가 넷으로 준다는 것을 0.0005에서 확인 / 야코비안이 상수인 함수는 그 선형사상 자체라 근사가 아니라 등식이 되는 자리
  - 절「합성과 야코비안의 곱」 — 소절: 표에 없는 두 층을 채우기 — softmax는 diag(p)−ppᵀ, LayerNorm은 (I−11ᵀ/n−ẑẑᵀ/n)/σ / LayerNorm의 야코비안이 「평균과 방향을 빼는 사영」이라 어떤 벡터를 넣어도 성분 합이 0이 되는 것 / 둘 다 대각도 항등도 아니라 앞의 네 줄과 다른 갈래라는 정리
  - 절「행렬식과 부피 배율」 — 소절: det≠0이면 그 점 근처에서 되돌릴 수 있다는 역함수 정리에 이름 붙이기 / 「국소적으로」가 붙는 이유 — 극좌표처럼 전체로는 여러 점이 한 점으로 가는 예 / 로그행렬식을 싸게 만드는 층 셋 — 대각·삼각·직교가 각각 얼마에 끝나는지
  - 절「명시적으로 만들 때의 비용」 — 소절: 야코비안 노름을 손실에 더하는 정칙화가 왜 비싼가와, 전체를 만들지 않고 무작위 벡터 하나의 JVP로 추정하는 방법 / 원하는 것이 행 하나뿐이라는 사실이 다음 글의 순서 바꾸기로 이어지는 자리
  - 절「코드로 확인하기」 — 소절: LayerNorm 야코비안의 행 합이 0인 것을 수치로 / det J가 0에 가까운 자리에서 유한차분 넓이 배율이 무너지는 것

- `math-random-variables-and-expectation` (지금 3,732자 · 절 8)
  - 절「확률변수의 정의」 — 소절: 사건마다 0과 1을 붙이는 지시확률변수와 E[1_A]=P(A) — 확률과 기댓값을 한 언어로 묶는 자리 / 확률변수 여럿을 한 표본공간 위에 함께 두는 것이 곧 결합분포라는 것
  - 절「pmf·pdf·cdf」 — 소절: 이산도 연속도 아닌 것 — ReLU의 출력은 0에 확률 덩어리가 있고 나머지는 연속인 혼합 / cdf로 적으면 그 자리가 수직으로 뛰는 계단 하나로 보인다는 것 / 중앙값과 분위수를 cdf의 역으로 읽고 기댓값과 다른 값이 되는 자리 / 그림: 혼합 분포의 cdf에서 점프 하나와 매끄러운 구간
  - 절「베르누이·범주형·이항」 — 소절: 이항의 기댓값 nq를 지시확률변수 n개의 합으로 두 줄에 얻기 — 정공법의 Σk·p(k)와 대조 / 마스크가 서로 독립이 아니어도 그 두 줄이 그대로 성립하는 이유
  - 절「기댓값과 선형성」(원고 「기댓값」과 「선형성」을 한 절로) — 소절: 곱의 기댓값은 독립일 때만 E[XY]=E[X]E[Y]이고 Y=X가 그 반례라는 것 / 덧셈에는 독립이 필요 없고 곱에는 필요한 이유를 결합분포에서 갈라 보기 / 전체기댓값 법칙 E[X]=E[E[X|Y]] — 배치마다 다른 조건에서 잰 평균을 다시 평균 내는 것이 왜 맞는가
  - 절「LOTUS」 — 소절: LOTUS가 안 통하는 자리 — 분포 자체가 θ에 의존하면 E_{p_θ}[g]의 미분을 기댓값 안으로 못 넣는다 / 그 한 가지 차이가 정책 그래디언트와 재파라미터화를 부르는 자리라는 예고 / g가 볼록이면 E[g(X)] ≥ g(E[X])로 방향이 정해진다는 것
  - 절「손실과 불편추정량」 — 소절: 불편이 아닌 추정량의 예 — 배치 안에서 정규화하는 손실(대조학습의 분모)은 배치 크기에 따라 기댓값이 달라진다 / 그래서 배치 크기를 바꾸면 손실 값을 나란히 비교할 수 없는 자리

- `math-basics-divisors-and-prime-factorization` (지금 3,762자 · 절 7)
  - 절「약수와 배수」 — 소절: 짝짓기가 홀수 개로 끝나는 수 — 제곱수만 가운데 짝이 하나라 약수 개수가 홀수 / 36과 100에서 확인하고 왜 제곱수뿐인지 한 줄로
  - 절「소수의 정의」 — 소절: 나눗셈 판정법 — 2·5는 끝자리, 4는 끝 두 자리, 3·9는 각 자리의 합, 11은 번갈아 더한 차 / 판정법이 있는 이유는 자릿값이 10의 거듭제곱이기 때문이라는 것 / 그림: 자릿수 합으로 3의 배수를 가르는 절차를 한 수에서 따라가기
  - 절「에라토스테네스의 체」 — 소절: √n까지만 지우면 되는 이유를 「합성수는 √n 이하의 소인수를 반드시 갖는다」 한 줄로 일반화 / 1000까지 걸러려면 31까지만 지우면 된다는 확인
  - 절「소인수분해」 — 소절: 큰 수에서 분해가 어렵다는 것 — 여섯 자리만 되어도 손으로는 시험 나눗셈이 길어진다 / 그래서 두 수의 최대공약수를 분해 없이 구하는 길이 따로 있다는 예고
  - 절「최대공약수와 최소공배수」 — 소절: 지수로 세는 약수의 개수 — 36=2²×3²에서 (2+1)(2+1)=9로 앞에서 짝지어 센 아홉과 같다 / 360=2³×3²×5에서 4×3×2=24개이고 하나씩 안 적어도 되는 자리 / 유클리드 호제법 — 큰 수를 작은 수로 나눈 나머지로 바꿔 가며 gcd(72,120)을 분해 없이 세 줄에 24로 / 서로소에 이름 붙이기 — gcd가 1인 두 수이고 그때 lcm이 두 수의 곱 / 그림: 호제법 세 걸음을 두 막대의 길이가 줄어드는 그림으로
  - 절「연습 문제」 — 소절: 지수로 약수의 개수를 세는 다섯 문제를 새로 — 84·100·144·512·630 / 나눗셈 판정법으로 가르는 다섯 문제 / 호제법으로 gcd를 구하고 분해로 맞춰 보는 다섯 쌍

- `math-newton-and-second-order` (지금 3,778자 · 절 5)
  - 절「뉴턴 갱신의 유도」 — 소절: 좌표를 선형변환해도 뉴턴법이 같은 점들을 지난다는 아핀 불변성 — 학습률과 달리 스케일을 손볼 필요가 없는 근거 / 그 성질이 「조건수에 둔감하다」의 정확한 뜻이라는 것
  - 절「저장과 역행렬의 비용」(원고 「비용」) — 소절: 양정치가 아닌 문제를 λ 하나로 고치는 자리가 있다는 예고 — 다음 절로 넘김
  - **새 절**「스텝을 줄이는 두 장치」 — 소절: 뉴턴법이 발산하거나 두 점을 오가는 1차원 예(arctan)와, 초기값이 멀면 2차 근사가 거짓말을 한다는 것 / 감쇠 뉴턴 x←x−αH⁻¹∇f와 α를 고르는 선탐색 한 줄 조건 / 신뢰영역의 (H+λI)⁻¹ — λ가 0이면 뉴턴, 크면 경사하강이고 그 사이를 미끄러진다 / λ가 음의 고윳값을 위로 밀어 양정치로 만드는 계산 / 그림: 같은 함수에서 λ를 키우며 갱신 방향이 뉴턴에서 경사하강으로 도는 부채꼴
  - 절「헤세-벡터 곱」 — 소절: 가우스-뉴턴과 GGN에 이름 붙이기 — 잔차의 야코비안으로 만든 JᵀJ가 정의상 양반정치라는 것 / F와 GGN이 손실에 따라 같아지는 자리(교차엔트로피와 제곱오차)
  - **새 절**「준뉴턴법과 L-BFGS」 — 소절: 헤세를 풀지 말고 스텝마다 (s,y)=(Δθ, Δ∇f) 한 쌍으로 H⁻¹을 고쳐 쌓는다는 발상 / 곡률 조건 sᵀy>0이 지켜져야 양정치가 유지된다는 것 / L-BFGS는 최근 m쌍만 들고 두 겹 루프로 방향을 얻어 메모리가 2mn — 7B에 m=10이면 140GB라 여전히 크다는 셈 / 딥러닝에서 안 쓰이는 진짜 이유는 메모리가 아니라 잡음 — 배치마다 다른 그래디언트로 만든 (s,y)가 곡률 대신 잡음을 담는다
  - 절「요즘 방법들이 남기는 칸」 — 소절: 위 표에 L-BFGS 줄을 넣어 메모리와 연산을 나란히 / 준뉴턴이 이력으로, K-FAC·Shampoo가 구조로 곡률을 얻는다는 두 갈래 정리

- `math-softmax-derivation` (지금 3,807자 · 절 8)
  - 절「함수가 지켜야 할 조건 넷」 — 소절: 조건 ②를 정확히 적으면 순열 동변성 — 좌표를 섞으면 출력도 같이 섞인다 / 조건 ③을 단조성 대신 연속성으로 바꿔도 같은 답이 나온다는 것과, 코시 방정식이 요구하는 것이 그 둘 중 하나뿐이라는 자리
  - 절「exp가 강제되는 과정」 — 소절: 조건 ④를 빼면 답이 하나가 아니게 된다 — 정규화된 ReLU와 sparsemax는 정확히 0을 줄 수 있는 대신 평행이동 불변을 버린다 / 그 둘이 실제로 쓰이는 자리와 무엇을 얻고 무엇을 잃는가 / 조건 넷 없이도 같은 식에 닿는 두 번째 길 — Σp=1과 E_p[z]가 주어졌을 때 엔트로피를 최대화하면 승수 둘에서 softmax가 떨어진다
  - 절「남은 상수와 온도」 — 소절: 온도가 출력 분포의 엔트로피를 어떻게 바꾸는지 — z=(2,1,−1)에서 T=0.5·1·2의 엔트로피를 재어 표에 한 줄 더하기 / T가 로짓 전체를 상수배 하는 것과 같으므로 어텐션의 1/√d_k와 같은 자리라는 확인
  - 절「평행이동 불변과 구현」 — 소절: 평행이동 불변은 곧 파라미터가 하나 남는다는 뜻 — 로짓이 n개인데 자유도는 n−1 / 마지막 층의 편향을 빼도 표현력이 안 줄어드는 이유와 그래도 두는 관행 / 최댓값 대신 다른 상수를 빼면 어디까지 되는지의 경계
  - 절「로짓·로그 확률·sigmoid」(원고 「로짓은 로그 확률이다」와 「선택지가 둘이면 sigmoid가 된다」를 한 절로) — 소절: sigmoid의 출력이 0과 1에 닿지 않는 것이 조건 ①의 결과라는 정리 / 그래서 원-핫 목표는 도달할 수 없고 라벨 스무딩이 필요해진다는 다른 글로의 연결 / 이진 분류에서 로짓 하나를 쓰는 것과 둘을 쓰는 것이 같은 모델이라는 확인
  - 절「코드로 확인하기」 — 소절: 조건 넷을 하나씩 깨는 함수 넷을 만들어 각각 어느 조건에서 걸리는지 표로 / 제약 둘을 걸고 엔트로피를 수치로 최대화해 softmax가 나오는 것을 확인

- `math-positional-encoding-math` (지금 3,824자 · 절 7)
  - 절「소박한 세 방법」 — 소절: 네 번째 방법인 학습된 절대 위치 임베딩 — 표를 한 줄 늘려 무엇이 되고 무엇이 안 되는지(최대 길이 밖에서는 값이 아예 없다) / 세 요구(유계·길이 무관·가깝되 구별)를 네 방법에 대조한 표
  - 절「주파수 사다리」 — 소절: 기저 10000이 정하는 것 — 가장 느린 쌍의 파장이 2π·10⁴≈62,832이고 문맥 길이보다 한참 길다는 계산 / 기저를 키우면 사다리 전체가 느려져 먼 거리를 가르는 대신 이웃 해상도를 잃는다는 맞바꿈 / 그림: 기저 10000과 500,000에서 파장 사다리가 어떻게 벌어지는지 나란히
  - 절「더한 위치가 남기는 것」 — 소절: 더하면 벡터의 노름이 바뀐다는 것 — ‖x+PE(p)‖가 p마다 달라져 점수의 규모가 위치에 따라 흔들리는 자리 / 위치항 내적 Σcos((n−m)θᵢ)를 δ에 대해 그리면 멀어질수록 줄어드는 포락선이 나온다는 것
  - 절「RoPE의 회전」 — 소절: 회전이 직교라 ‖R(mθ)q‖=‖q‖ — 위치를 실어도 벡터의 크기가 안 변한다는 것이 더하기와 갈리는 두 번째 자리 / 그래서 RoPE를 Q와 K에만 걸고 V에는 안 거는 설계와 그 이유
  - 절「손계산과 대조」 — 소절: δ가 클 때 점수가 어떻게 줄어드는지 사다리 전체에서 재기 — δ=1·10·100·1000의 값 / 세 번째 방법인 ALiBi를 한 줄로 — 점수에 −m|i−j|를 더하면 상대 위치가 곧바로 들어가고 회전도 사다리도 필요 없다는 대비
  - 절「코드로 확인하기」 — 소절: 노름이 보존되는 것과 더하기에서 안 되는 것을 나란히 재기 / 기저를 바꿔 가며 δ에 따른 점수 포락선을 뽑기

- `math-probability-rules-and-chain-rule` (지금 3,850자 · 절 7)
  - 절「확률의 공리 셋」 — 소절: 조건부확률 P(·|B)도 세 공리를 만족한다는 확인 — 그래서 조건을 걸어 놓고 그 위에서 다시 확률을 다룰 수 있다 / 그 사실이 아래의 모든 규칙이 조건부 판에서도 그대로 성립하는 근거라는 것
  - 절「결합·주변·조건부」 — 소절: 주변화가 방향을 뒤집는 예 — 두 표에서 각각 A가 유리한데 합치면 B가 유리해지는 심슨의 역설 / 그 일이 생기는 조건 — 두 표의 크기가 다르고 그 크기가 결과와 얽혀 있을 때 / 그림: 두 부분표와 합친 표를 나란히 놓고 비율이 뒤집히는 자리 표시
  - 절「독립」 — 소절: 조건부독립 p(x,z|y)=p(x|y)p(z|y)의 정의와 그것이 독립과 어느 쪽도 함축하지 않는다는 것 / 마르코프 가정을 이 말로 정확히 적으면 「먼 과거는 최근 n−1개가 주어지면 조건부독립」 / 그림: 세 변수를 이은 사슬과 조건을 건 자리에서 길이 끊기는 모양
  - 절「베이즈 규칙」 — 소절: p(y)=0이면 조건부가 정의되지 않는다는 것과 그것이 실제로 걸리는 자리(어휘에 없는 토큰) / 사후를 다시 사전으로 삼아 관측을 하나씩 반영하는 순차 갱신과, 관측 순서를 바꿔도 같은 답이 나온다는 확인
  - 절「연쇄법칙」 — 소절: 순서를 뒤에서 앞으로 바꿔도 등식이 참임을 세 자리 표에서 직접 확인 / 등식은 같은데 모델링 난이도는 다르다는 것 — 사람이 쓰는 순서라 조건이 자연스럽다 / 조건을 자르는 것과 자르지 않는 것의 파라미터 수 대비
  - 절「자기회귀 분해」 — 소절: 마스크드 언어모델은 이 분해를 하지 않아 문장 확률을 곱으로 못 준다는 것 / 그래서 두 모델의 「점수」를 나란히 비교할 수 없는 자리 / 생성 순서를 바꾼 모델도 여전히 연쇄법칙 위에 있다는 확인

- `math-basics-probability-and-sample-space` (지금 3,857자 · 절 8)
  - 절「집합과 세 연산」 — 소절: 차집합 A−B와 그것이 A∩Bᶜ와 같다는 확인 / 드모르간 법칙 (A∪B)ᶜ=Aᶜ∩Bᶜ를 벤 다이어그램으로 칠해 보기 / 그 법칙이 「둘 다 안 일어난다」를 「하나라도 일어난다의 반대」로 바꿔 주는 자리
  - 절「시행·표본공간·사건」 — 소절: 같은 시행에서 표본공간을 다르게 잡을 수 있다는 것 — 동전 두 개를 「앞앞·앞뒤·뒤뒤」 셋으로 잡으면 답이 틀리는 이유 / 「똑같이 잘 나온다」가 성립하도록 잡아야 세어서 나눌 수 있다는 조건 / 그림: 같은 시행의 두 표본공간을 나란히 놓고 어느 쪽이 등확률인지 표시
  - 절「확률을 정하는 두 관점」 — 소절: 상대도수가 참값에 다가가는 것을 실제 횟수로 — 10·100·10,000번 던진 짝수 비율 / 몇 번이면 소수 둘째 자리가 안정되는지
  - 절「확률의 기본 성질 셋」 — 소절: 배반과 여사건은 다르다는 것 — 여사건은 배반이면서 합집합이 전체인 특별한 경우 / 셋 이상이 서로 배반일 때 덧셈이 그대로 늘어난다는 것
  - 절「표본공간을 다 적는 두 예」(원고 「주사위 두 개」와 「동전 세 번」을 한 절로) — 소절: 나무그림의 가지에 확률을 적어 곱하는 방식 — 앞면이 0.6인 동전 세 번에서 잎마다 확률이 달라지는 계산 / 그 곱셈이 다음 글 곱셈정리의 미리보기라는 자리 / 잎의 확률을 전부 더하면 1이 되는 검산
  - 절「연습 문제」 — 소절: 드모르간을 쓰는 세 문제를 새로 / 표본공간을 잘못 잡아 나온 답을 고치는 두 문제 / 공평하지 않은 동전의 나무그림 세 문제

- `math-exp-and-log` (지금 3,865자 · 절 7)
  - 절「받아 쓰는 두 성질」 — 소절: 밑을 바꾸는 한 줄 log_b x = ln x / ln b와 내트·비트 환산 / 라이브러리의 log가 자연로그이고 log2·log10이 따로 있다는 것
  - 절「곱의 언더플로」 — 소절: bfloat16은 지수부가 fp32와 같아 범위가 같고 정밀도만 낮다 — 언더플로 관점에서는 fp32와 같은 자리에서 죽는다 / fp16은 반대로 지수부가 5비트라 6×10⁻⁵에서 이미 끝난다는 것 / 네 형식의 지수부·가수부와 하한을 한 표로
  - 절「로그 공간의 덧셈」 — 소절: 로그 확률 500개를 더할 때도 부동소수점 오차가 쌓인다는 것과 그 크기 / 크기가 비슷한 것끼리 짝지어 더하면 오차가 줄어드는 이유 / 로그 확률의 평균이 확률의 기하평균의 로그라는 것 — 길이 정규화가 왜 하필 평균인가
  - 절「단조성과 순서 보존」 — 소절: 순서는 지키지만 차이는 안 지킨다는 것 — 0.42와 0.35의 차 0.07이 로그에서는 0.18 / 그래서 「확률 차이」로 하던 판단을 로그값에 그대로 옮기면 안 되는 자리
  - 절「log-sum-exp 트릭」 — 소절: 두 항만 더하는 logaddexp를 max+log1p(e^{−|a−b|})로 적으면 차이가 클 때 자릿수가 안 깎이는 이유 / log1p와 expm1이 있는 자리 — log(1+x)를 그냥 계산하면 x가 10⁻¹⁶일 때 0이 된다 / 그림: log(1+x)와 log1p(x)의 상대오차를 x가 작아질 때 나란히
  - 절「코드에서 만나는 이름」 — 소절: log(p+1e-8) 관행이 막는 것과 망치는 것 — −inf는 사라지지만 p가 작을 때 기울기가 왜곡된다 / clamp를 걸면 그 자리의 기울기가 0이 되어 학습이 멎는 자리 / 그래서 확률에 바닥을 까는 대신 로그 공간을 안 떠나는 쪽이 답이라는 정리

- `math-adam-from-moments` (지금 3,874자 · 절 5)
  - 절「방향마다 다른 걸음의 재료」 — 소절: 제곱 대신 절댓값을 쓰면 부호는 지워져도 스케일 불변이 깨진다는 확인 / AdaGrad와 RMSProp의 차이 — 합이냐 EMA냐 하나이고, 합이면 분모가 단조증가해 걸음이 0으로 죽는 자리
  - 절「편향 보정」(원고 「초반에 걸음이 부푸는 문제」) — 소절: 보정 대신 v₀=g₁²로 시작하는 방법과 그것이 첫 스텝의 잡음을 통째로 믿는 위험 / 워밍업과 편향 보정이 같은 구간을 덮지만 서로를 대신하지 못하는 이유
  - **새 절**「한 스텝이 움직이는 거리」 — 소절: |m̂/√v̂|가 대체로 1 안팎이라 한 스텝의 이동이 대략 η라는 읽기 / 그래서 Adam의 학습률이 SGD의 것보다 두 자릿수 작다는 것(3×10⁻⁴ 대 10⁻¹) / 그래디언트가 백 배 커져도 걸음이 안 커지므로 손실 스파이크에 강하고, 대신 쓸모 있는 큰 그래디언트도 함께 눌린다는 대가 / 그림: 그래디언트 규모를 가로축에 두고 SGD와 Adam의 걸음 크기를 나란히
  - 절「조립한 갱신식」 — 소절: ε가 √v̂ 밖에 있는 것과 √(v̂+ε) 안에 있는 것이 프레임워크마다 다르고 결과도 다르다는 것 / 상태가 m·v 두 벌이라 fp32에서 8n 바이트이고, Adafactor가 v를 행·열 두 벡터로 분해해 m+n으로 줄이는 자리
  - **새 절**「수렴하지 않는 반례」 — 소절: 큰 그래디언트가 가끔만 오는 문제에서 v의 EMA가 그것을 잊어 잘못된 곳으로 수렴하는 예 / v의 최댓값을 들고 있는 한 줄 수정(AMSGrad)이 그것을 막는 방식 / 그런데도 실무에서 안 쓰이는 이유 — 분모가 다시 내려가지 못해 후반에 걸음이 죽는다
  - 절「AdamW의 분리형 감쇠」 — 소절: 편향과 정규화 파라미터를 감쇠에서 빼는 관행과 그 이유 — 그것들은 크기 자체가 뜻이라 0으로 당기면 표현이 망가진다 / 감쇠가 ηλ로 들어가므로 학습률 스케줄을 바꾸면 실제 규제 세기도 함께 바뀐다는 것

- `math-basics-conditional-probability-and-expectation` (지금 3,880자 · 절 7)
  - 절「좁아지는 표본공간」 — 소절: 칸이 등확률이 아니면 개수 대신 확률을 더해야 한다는 것 — 나무그림 가지의 확률을 곱해 잎마다 값을 매기고 그 위에서 조건 걸기 / 앞 글에서 갈래만 세던 나무그림에 확률이 붙는 자리
  - 절「독립과 배반」(원고 「독립」) — 소절: 곱셈정리를 두 번 써서 공을 되돌리지 않고 둘 꺼내는 경우를 세기 — 조건이 매번 바뀌는 자리 / 되돌리면 독립이고 안 되돌리면 아니라는 것을 같은 문제의 두 값으로 대조 / 그림: 되돌리는 경우와 아닌 경우의 나무그림을 나란히
  - 절「베이즈 정리」 — 소절: 분모를 식으로 적으면 전확률 법칙 P(양성)=P(양성|병)P(병)+P(양성|병 아님)P(병 아님) — 만 명 표의 아래 줄이 그 식이라는 확인 / 몬티 홀 — 표본공간을 다시 적으면 바꾸는 쪽이 2/3이고, 사회자가 무엇을 알고 문을 열었느냐가 답을 정한다는 자리
  - 절「확률변수와 분포표」 — 소절: 같은 시행에 확률변수를 다르게 붙일 수 있다는 것 — 동전 세 번에서 「앞면 개수」와 「처음 앞면이 나온 차례」 / 분포표가 확률변수마다 따로 생긴다는 것
  - 절「기댓값」 — 소절: 기대 이득으로 읽기 — 1,000원 내고 주사위 눈만큼 받는 게임의 기댓값이 3,500원이라 유리하다는 판단 / 기댓값이 음수인 게임과, 그래도 사는 이유는 기댓값 밖에 있다는 것 / 기댓값이 같아도 흩어진 정도가 다르면 다른 게임이라는 것과 그 흩어짐을 재는 값은 40번 · 기술통계의 몫이라는 경계
  - 절「연습 문제」 — 소절: 전확률 법칙으로 분모를 구하는 세 문제를 새로 / 되돌리지 않고 두 번 뽑는 세 문제 / 기대 이득을 판단하는 세 문제

- `math-span-basis-and-coordinates` (지금 3,939자 · 절 8)
  - 절「생성과 부분공간」 — 소절: span이 무한집합인데 유한한 목록으로 적힌다는 것 / 합과 스칼라배가 다시 선형결합이 됨을 두 줄로 확인해 「부분공간」이라는 말의 근거 대기 / 아무 벡터도 안 주면 span이 {0}이고 영벡터를 보태도 안 커지는 구석
  - 절「일차독립과 자명한 해」 — 소절: 판정을 열로 세운 행렬의 $$A\mathbf{c}=\mathbf{0}$$ 으로 바꾸는 절차 / 영벡터가 하나라도 끼면 무조건 종속 / 「서로 배수인가」로 끝나는 것은 둘일 때뿐 — (1,2)·(2,1)·(4,5)는 쌍쌍이 독립인데 셋이 종속
  - 절「기저와 차원」 — 소절: 기저를 실제로 만드는 두 방향 — 생성집합에서 군더더기를 빼기와 독립집합에 보태기 / 차원을 알면 「독립인 n개」만 확인해도 기저라는 지름길 / 부분공간의 기저를 쓰면 좌표의 개수가 n이 아니라 dim이라는 것
  - 절「기저에 붙는 좌표」 — 소절: 좌표를 바꾸면 길이와 각도가 안 지켜진다 — (3,1)의 길이 √10인데 좌표 (2,1)의 길이는 √5 / 그래서 좌표 위에서 코사인 유사도를 재면 다른 순위가 나온다는 실전 함의 / 기저가 정규직교일 때만 그 어긋남이 사라진다는 것으로 Qᵀ의 값어치 다시 읽기 / 그림: 같은 두 점이 두 격자에서 이루는 각이 달라 보이는 것
  - 절「축을 바꾸는 조작들」 — 소절: $$B^{-1}AB$$ 가 대각합과 행렬식을 바꾸지 않는다 — 「같은 조작을 다른 축에서 본 것」이라 부를 수 있는 근거 / W_Q는 기저변환이 아니라는 구별 — 정사각도 아니고 가역도 아니다
  - 절「랭크로 세는 실제 차원」(원고 「코드로 확인하기」를 흡수) — 소절: matrix_rank의 tol이 무엇을 자르고 기본값이 무엇에 비례하는지 / inv 대신 solve를 쓰는 이유 / 무작위 벡터를 모으면 랭크가 언제나 꽉 차는 것과 「퍼져 있다」가 다른 말이라는 것 / 이방성을 실제로 재는 지표 하나 — 무작위 두 문장의 평균 코사인 유사도
  - 절「정리」

- `math-low-rank-approximation-and-lora` (지금 3,940자 · 절 9)
  - 절「랭크 1 층의 합」 — 소절: 층들이 서로 직교한다는 것(프로베니우스 내적이 0)과 그것이 뒤의 오차 공식의 진짜 근거라는 연결 / 랭크 1 층 하나를 저장하는 비용이 mn이 아니라 m+n이라는 셈 / 특잇값을 내림차순으로 늘어놓는 것이 SVD의 관례라 「앞에서 자른다」가 말이 되는 자리
  - 절「에카르트-영 정리」 — 소절: 왜 σ_{k+1}보다 작게 만들 수 없는가 — 랭크 k 행렬의 영공간(차원 n−k)과 상위 k+1개 특이벡터가 만드는 공간이 반드시 만난다 / 유니터리 불변 노름 전체에서 성립하고 원소별 최대 노름 같은 자에서는 아니라는 경계
  - 절「잘라 낸 뒤의 오차」(원고 「손으로 한 번」을 흡수) — 소절: 에너지가 σ 합의 비가 아니라 σ² 합의 비라는 것 — 10·6·3·1에서 σ 합으로 세면 k=2가 80%라는 다른 답이 나온다 / 오차를 상대값 ‖A−A_k‖_F/‖A‖_F 로 적으면 √(1−에너지)와 같아진다는 관계
  - 절「LoRA의 랭크 제약」 — 소절: A와 B 중 하나를 0으로 초기화해 학습 시작에서 ΔW=0을 만드는 장치 / 파라미터는 0.39%인데 병합 전 추론에서는 곱이 두 번이라 지연은 오히려 는다는 구별 / 어느 모듈(q·k·v·o·MLP)에 붙일지가 r과 별개의 손잡이라는 것
  - 절「r을 고르는 절차」(원고 「α/r 은 무엇을 하는가」를 흡수) — 소절: 학습이 랭크 r 안에서 최적 근사를 찾아 준다는 보장은 없다는 경계 — 에카르트-영은 ΔW를 이미 알 때의 이야기다 / 층마다 나온 k를 하나로 합칠 때 최대와 중앙값 중 무엇을 쓰는가 / α를 학습률 배수로 읽으면 옵티마이저 설정과 겹쳐 같은 것을 두 번 조이게 된다는 주의
  - 절「코드로 확인하기」 — 소절: compute_uv=False로 특잇값만 먼저 뽑아 표를 만들고 근사는 나중에 만드는 순서 / 감쇠하는 ΔW를 지어낼 때 QR로 직교행렬을 만드는 이유 — 특잇값을 우리가 정한 값으로 심으려면 U와 V가 직교여야 한다
  - 절「정리」

- `math-importance-ratio-and-clipping` (지금 3,946자 · 절 5)
  - 절「중요도 표집과 불편성」 — 소절: 자기 정규화 추정량 Σwf/Σw — 편향이 생기지만 분산이 작아 실제 구현이 쓰는 쪽 / 비율을 확률의 나눗셈이 아니라 로그 차의 지수로 계산하는 이유 / E_q[w]=1이라는 사실을 표본으로 재 보는 것 자체가 진단이 되는 자리
  - 절「무게의 분산과 유효 표본 수」 — 소절: 분산이 유한하려면 ∫p²/q 가 유한해야 하고 그것은 q의 꼬리가 p보다 두꺼워야 한다는 조건 / ESS를 로그에 안 찍고 approx_kl이 그 자리를 대신하는 까닭 — 배치마다 계산은 되는데 목표값을 정할 근거가 없다 / 「지표가 조용한데 무너진다」를 잡는 다른 지표 — 무게의 최대 비중과 ratio_max
  - (새 절)「토큰 단위와 시퀀스 단위」 — 소절: 시퀀스 전체의 비율은 토큰 비율의 곱이라 200토큰이면 로그 비율이 200개 항의 합이 되어 폭발한다는 계산 / PPO가 토큰마다 r을 따로 두는 것이 그 폭발을 피한 선택이고 대신 시퀀스 단위의 불편성을 잃는다는 대가 / 길이가 다른 답이 한 배치에 섞이면 평균이 긴 답 쪽으로 기우는 자리 / 그림: 곱으로 쌓는 시퀀스 비율과 토큰마다 끊는 비율
  - 절「clip이 바꾸는 목적함수」 — 소절: clip을 넣는 순간 추정량이 더 이상 불편이 아니라는 것 — 앞 절에서 공들여 세운 성질을 스스로 내려놓는 자리 / [1−ε, 1+ε]가 로그 비율에서는 비대칭이라 올리는 쪽과 내리는 쪽의 여유가 애초에 다르다는 것
  - 절「clip_frac을 읽는 법」 — 소절: KL 추정량 셋(−log r, ½(log r)², (r−1)−log r)을 나란히 놓고 왜 마지막인지 — 앞의 둘은 음수가 나오거나 분산이 크다 / clip_frac이 0.5를 넘었을 때 재사용 횟수를 줄일지 학습률을 내릴지 고르는 기준
  - 절「KL 조기 중단」 — 소절: 예산을 넘겨 버린 반복이 계산 낭비가 아니라는 것 — 이미 걸은 걸음은 남고 버리는 것은 남은 반복뿐 / 재사용 횟수 K와 ε과 KL 예산 셋이 같은 하나를 서로 다른 자리에서 조인다는 정리
  - 절「정리」

- `math-basics-symbolic-checking` (지금 3,992자 · 절 6)
  - 절「기호 계산의 관용구」 — 소절: `==` 가 SymPy에서 구조 비교라 `x*(x+1) == x**2+x` 가 False라는 것 — simplify를 쓰는 진짜 이유가 여기 있다 / sp.Rational(1,2)와 0.5의 차이 — 부동소수점을 한 번 섞으면 차가 0으로 정리되지 않는다 / subs로 특수값을 넣어 같은 두 식을 값으로도 한 번 더 견주는 이중 확인
  - 절「두 답이 갈릴 때의 순서」 — 소절: simplify가 0으로 못 줄이는데 사실은 같은 경우 — 무작위 수를 여러 번 대입해 보강하는 다섯째 걸음 / 검산이 못 잡는 자리 — 문제 자체를 잘못 옮겨 적었으면 기호도 수치도 나란히 통과한다
  - 절「수치 검산의 세 도구」 — 소절: sp.lambdify로 기호 식을 수치 함수로 바꿔 기호 길과 수치 길을 잇기 / h를 무한정 줄이면 오히려 나빠진다는 것 — 47번의 자릿수 소거가 여기서 일어나고 그래서 최적 h가 따로 있다 / 시드를 고정해 난수 검산을 재현 가능하게 만드는 습관 / 그림: h를 줄일수록 내려가다 어느 지점부터 다시 올라가는 오차의 V자
  - 절「난수 검산의 한계」 — 소절: 확률이 아니라 기댓값을 잴 때의 오차는 표준편차/√N이라 흩어진 값일수록 더 나쁘다는 것 / 난수 검산은 「어긋난다」만 보일 수 있고 「맞다」는 못 보인다는 비대칭
  - 절「Σ를 옮길 때의 습관」 — 소절: 첨자를 1부터 적어 놓고 코드에서 0부터 도는 자리의 오프바이원 / 빈 합이 0이라는 약속이 코드의 초기값 `total = 0` 과 같은 것이라는 확인
  - 절「연습 문제」 — 소절: 기존 셋(기호 검산 읽기·수치로 확인하기·난수 검산과 이중합) 유지 / 새 연습 4 「기호로 안 되면 수치로」 3문항 — 닫힌 꼴이 안 나오는 적분을 중점법으로 재기, 해석해 없는 방정식을 수치로 풀고 대입해 검산하기, 기호 극한과 h를 줄여 만든 표를 대조하기
  - 절「정리」

- `math-bradley-terry-preference` (지금 3,992자 · 절 6)
  - 절「순서를 확률로 바꾸는 모델」 — 소절: 특징이 「A쪽 +1, B쪽 −1」인 로지스틱 회귀와 같은 모형이라는 대응 / 정규 잡음을 가정한 서스톤 모형(프로빗)과의 차이 — 순위는 거의 같고 눈금만 다르다 / 무승부를 담을 자리가 모형에 없다는 것과 실제 선호 데이터에는 그 라벨이 있다는 어긋남
  - 절「최대가능도와 손실 한 줄」 — 소절: log(sigmoid())가 아니라 logsigmoid를 쓰는 이유 — Δ가 크게 음수일 때 안쪽이 0으로 언더플로한다 / 마진을 넣은 −logσ(Δ−m)이 모형에 무엇을 더하는가 / 기울기가 σ(−Δ)라서 라벨이 뒤집힌 쌍을 가장 세게 민다는 부작용 — 잡음 라벨이 학습을 끌고 간다
  - 절「보상의 평행이동 불변」 — 소절: 배율은 자유가 아니라는 대비 — 2r은 다른 확률을 주고 그 배율이 곧 온도다 / 한 답이 모든 비교를 이겨 해가 발산할 때 정규화 항이 실제로 고정해 주는 것이 무엇인지
  - (새 절)「이 모형이 못 담는 것」 — 소절: 순환 선호(A≻B≻C≻A)를 실수 축 하나로는 표현할 수 없다는 것 / 평가자마다 기준이 다르면 같은 쌍의 라벨이 갈리는데 모형은 그것을 강도 차이로 읽는다 / 프롬프트가 다르면 강도의 뜻도 다른데 r 하나로 묶고 있다는 것 / 그림: 세 답의 순환 선호 화살표와 그것을 한 축에 세우려다 실패하는 그림
  - 절「점수차의 표준오차」 — 소절: w가 0이거나 n이면 로짓이 발산해 추정이 안 되고 ½씩 더해 보정하는 관례 / 라벨링 예산을 어디에 쓸 것인가 — 같은 쌍을 더 볼 것인가 새 쌍을 볼 것인가를 SE 식으로 답하기
  - 절「비교 그래프의 연결」 — 소절: K개 항목의 순위를 세우려면 간선이 적어도 K−1개여야 한다는 것이 「연결」의 뜻 / 비교가 몰린 항목과 헐거운 항목의 오차가 다르므로 순위표에 오차 막대가 필요하다는 결론
  - 절「정리」 — 소절: 원고의 「다시 그 한 줄로」는 절을 없애고 정리 뒤 맺음 문단으로 내려 절 수를 일곱에 맞춘다

- `math-basics-orders-of-magnitude-and-error` (지금 4,000자 · 절 8)
  - 절「과학적 표기와 유효숫자」 — 소절: 「자릿수」라는 말 자체 — 크기의 자릿수는 log₁₀을 정수로 반올림한 것이고 「몇 배인가」는 지수의 차라는 것 / 제목의 물음에 직접 답하는 사다리 — 학습률 1e−3, eps 1e−8, 파라미터 7e9, 토큰 1e12를 한 줄에 늘어놓고 자릿수로 견주기 / 유효숫자 연산 규칙 — 더할 때는 소수 자릿수가 짧은 쪽, 곱할 때는 유효숫자가 적은 쪽에 맞춘다
  - 절「절대오차와 상대오차」 — 소절: 상대오차의 분모를 참값으로 둘지 근삿값으로 둘지 — 참값을 모를 때의 관례 / 오차의 부호를 남기는 것과 절댓값으로 지우는 것의 쓰임 차이
  - 절「연산이 오차를 불리는 법」(원고 「자리가 유한하면 생기는 일」을 흡수) — 소절: 나눗셈도 곱과 같아 상대오차가 더해진다는 것 — 원고에 곱만 있다 / 거듭제곱은 상대오차가 n배, 근호는 1/n배 / 최악 경우와 전형 경우의 갈림 — 오차가 독립이면 합이 아니라 √(δx²+δy²)로 자란다 / 0.1+0.2가 0.3이 아닌 자리와 등호 대신 허용오차로 견주는 습관, 큰 수에 작은 수를 더하면 통째로 삼켜지는 자리
  - 절「로그 축의 두 종류」 — 소절: 로그 축에 0과 음수를 올릴 수 없다는 것 — 손실 곡선을 로그로 그릴 때 걸리는 자리 / 눈금 사이가 고르지 않은 격자를 읽는 법(1·2·5·10)
  - 절「오차의 차수와 O(·)」 — 소절: 알고리즘의 O(n)과 오차의 O(h)가 방향이 반대라는 것 — 앞은 n이 커질 때, 뒤는 h가 0으로 갈 때이고 같은 기호를 두 뜻으로 쓴다 / 차수를 실측 표에서 거꾸로 읽을 때 앞 줄과의 비가 흔들리면 아직 「충분히 작지」 않다는 신호
  - 절「연습 문제」 — 소절: 기존 셋(크기와 오차 재기·오차가 불어나는 법·로그 축과 차수) 유지 / 새 연습 4 「기계의 자릿수」 3문항 — 0.1+0.2와 0.3을 허용오차로 견주는 판정, 큰 수에 1을 더해도 그대로인 까닭을 유효숫자로 설명하기, 자릿수 소거가 일어나는 식 하나를 고쳐 자릿수 되찾기
  - 절「정리」

- `math-basics-similarity-and-proportion` (지금 4,006자 · 절 8)
  - 절「닮음과 합동」 — 소절: 닮음비 k와 1/k — 어느 쪽에서 어느 쪽으로 보느냐로 뒤집히므로 먼저 방향을 정한다 / 「대응하는」이라는 말이 실제로 하는 일 — 꼭짓점 순서를 맞춰 △ABC ∽ △DEF로 적는 관습이고 순서를 어기면 답이 틀린다
  - 절「삼각형의 닮음 조건」 — 소절: 왜 삼각형만 두 각으로 끝나는가 — 사각형은 네 각이 다 같아도(직사각형끼리) 안 닮는다는 반례 / 「비슷해 보인다」와 「닮았다」를 가르는 것 — 무엇을 실제로 재야 판정이 끝나는가
  - 절「대응하는 변의 비」(원고 「비를 적는 표기」를 흡수) — 소절: 두 비를 구별하기 — 한 삼각형 안의 두 변의 비와 두 삼각형 사이의 대응변의 비는 다른 값이고 헷갈리면 식이 뒤집힌다 / 각이 커지면 계단이 어떻게 변하는가 — 45도에서 1:1이고 각이 커질수록 세로가 는다는 것으로 25번에 다리 놓기 / 그림: 같은 두 변의 비를 「한 삼각형 안」과 「두 삼각형 사이」로 각각 화살표를 그어 구별하기
  - 절「닮음비와 넓이·부피」 — 소절: 넓이비에서 닮음비를 거꾸로 구하기 — 넓이비 16이면 닮음비 4 / 제곱-세제곱 법칙 — 커질수록 겉넓이/부피가 1/k로 줄고 그래서 큰 동물이 열을 못 버리는 것
  - 절「평행선이 만드는 비례」 — 소절: 지도의 축척이 곧 닮음비라는 것과 축척 1:25,000에서 거리 읽기 / 핀홀 카메라 — 상의 크기가 거리에 반비례하는 것도 같은 두 삼각형 / 땅이 기울어 있으면 그림자 방법이 깨진다는 조건(원고는 시각만 짚는다)
  - 절「연습 문제」 — 소절: 기존 넷(닮음비로 변 구하기 열·모눈 위 계단 재기·넓이비와 부피비 다섯·그림자로 높이 재기 셋) 유지 / 새 연습 5 「닮았는가 아닌가」 4문항 — 정사각형 둘은 언제나 닮음, 원 둘도 언제나 닮음, 직사각형 둘은 아닐 수 있음(3×4와 3×5), 이등변삼각형 둘이 안 닮는 예 짓기 / 새 연습 6 「거꾸로 구하기」 2문항 — 넓이비에서 닮음비, 부피비에서 닮음비
  - 절「정리」

- `math-covariance-and-multivariate-gaussian` (지금 4,081자 · 절 6)
  - 절「축 방향 분산의 한계」 — 소절: 계산용 꼴 Cov(X,Y) = E[XY] − E[X]E[Y] 를 다섯 점으로 확인하기 / 공분산이 0인데 독립이 아닌 예 — Y=X² 에서 대칭 때문에 0이 나온다 / 독립이면 공분산이 0이지만 거꾸로는 아니고, 다만 다변량 정규분포에서는 거꾸로도 참이라는 대비(뒤 절의 복선)
  - 절「공분산 행렬」 — 소절: 표본 공분산을 데이터 행렬로 적기 — 중심화한 뒤 XᵀX/(n−1) / Σ가 특이일 때 — 한 좌표가 다른 좌표의 상수배면 고윳값 0이 나오고 Σ⁻¹이 없어 밀도가 정의되지 않는다, 「양반정치」의 「반」이 그 경계 / 실무에서 Σ + εI 를 더하는 것이 정확히 그 경계를 밀어내는 조작
  - 절「가장 크게 퍼진 방향」 — 소절: 고윳값 목록 자체를 지표로 읽기 — 유효 차원과 참여비 / 대각합이 전체 분산의 합이라 「앞 두 축이 몇 %」를 고윳값 비로 바로 적을 수 있다는 것
  - 절「다변량 정규분포의 밀도」 — 소절: 1σ 타원 안에 들어가는 확률이 1차원의 68%가 아니라 2차원에서 39%라는 계산 / 차원이 늘수록 확률질량이 중심에서 껍질로 밀려나 「가장 흔한 반지름」이 0이 아니게 되는 것 / 그림: 차원 2·10·100에서 반지름의 분포가 껍질로 옮겨 가는 그림
  - (새 절)「주변분포와 조건부분포」 — 소절: 좌표 일부만 떼어도 정규분포이고 Σ에서 그 칸만 오려 내면 된다는 것 / 조건부 평균 μ₁ + Σ₁₂Σ₂₂⁻¹(x₂−μ₂) 가 선형이고 그것이 곧 회귀식의 꼴이라는 대응 / 조건부 분산이 관측한 값에 의존하지 않는다는 놀라운 성질 — 무엇을 봤든 남은 불확실성은 같다 / 다섯 점 예제의 Σ로 「x=15일 때 y의 조건부 평균」을 손으로 구하기
  - 절「촐레스키 분해」 — 소절: LLᵀ=Σ 를 만족하는 L이 유일하지 않다는 것 — 임의의 직교 Q에 대해 LQ도 된다, 「아래삼각」이라는 조건이 유일성을 만든다 / 고유분해로 만든 제곱근 QΛ^{1/2}Qᵀ 와의 비교 — 값은 같고 비용과 쓰임이 다르다
  - 절「정리」(원고 「코드로 확인하기」를 흡수) — 소절: np.cov의 rowvar 축 함정 — 행이 표본인지 변수인지 / multivariate_normal이 촐레스키가 아니라 SVD를 기본으로 쓰는 이유 — 특이한 Σ도 받는다

- `math-entropy-and-perplexity` (지금 4,117자 · 절 8)
  - 절「정보량 −log p」 — 소절: 「유일한 모양」이라는 주장의 근거 — f(pq)=f(p)+f(q)를 만족하는 연속함수가 로그의 상수배뿐이라는 것 / p=0인 사건의 정보량이 무한대이고 0log0=0으로 약속하는 자리 / 모델이 정답 토큰에 0에 가까운 확률을 주면 손실이 폭발하는 실제 자리와 그 처방(라벨 스무딩·로짓 클리핑)
  - 절「엔트로피」(원고 「균등분포에서 최대인 이유」를 흡수) — 소절: 엔트로피는 분포의 값이지 결과의 값이 아니다 — 「이 문장의 엔트로피」라는 말이 틀린 자리 / 조건부 엔트로피와 사슬 규칙 H(X,Y)=H(X)+H(Y|X) — 언어모델이 문장의 −log p를 토큰마다 쪼개 더하는 것이 정확히 이 규칙 / 하한도 있다 — H≥0이고 등호는 확정 분포에서만
  - 절「perplexity」 — 소절: PPL이 로그의 밑에 무관하다는 것 — exp(H_nat) = 2^{H_bit} 를 1.75비트 예로 확인 / 「후보 몇 개」로 읽는 것이 언제 오독인가 — 후보 수가 실제로 그만큼이라는 뜻이 아니다
  - 절「loss와 ppl의 관계」(원고 「bits-per-byte와 언어모델은 압축기다」를 흡수) — 소절: 문장 단위 perplexity와 토큰 단위의 구별 — 길이로 정규화하지 않으면 짧은 문장의 값이 크게 흔들린다 / 토크나이저가 다르면 토큰 수가 달라 ppl이 갈리는 것을 두 토크나이저의 토큰 수로 실제로 보이기 / bpb의 분모에 들어가는 「바이트 수」가 UTF-8이라 한국어에서 3바이트라는 것 — 영어 4바이트 가정이 그대로 안 통하는 자리
  - (새 절)「ppl 비교가 깨지는 자리」 — 소절: 평가 코퍼스가 다르면 두 ppl은 비교값이 아니라는 것 / 패딩과 마스크가 평균에 섞이면 ppl이 실제보다 낮게 나오는 함정 — 마스크된 자리의 손실이 0으로 들어간다 / 문맥 길이를 늘리면 같은 모델의 ppl도 내려가므로 평가 창을 함께 적어야 한다는 것 / 학습 데이터 오염 — 봤던 문장의 ppl은 낮게 나오는 것이 당연하다
  - 절「코드로 확인하기」 — 소절: 온도 T로 로짓을 나누면 엔트로피가 어디로 가는가 — T→0에서 0, T→∞에서 log n을 실제로 재 보기 / 로짓에서 바로 엔트로피를 계산할 때 logsumexp를 쓰는 이유
  - 절「정리」

- `math-basics-ode-first-steps` (지금 4,123자 · 절 8)
  - 절「해가 무리로 나오는 자리」 — 소절: 「계」라는 말 — 가장 높은 도함수의 차수이고 이 글은 1계만 다룬다는 경계 / 해를 확인하는 법 — 구한 함수를 방정식에 대입해 양변이 같은지 보는 것이 방정식 때와 같은 습관 / 계가 1이라 초기조건도 하나면 되고 적분상수도 하나라는 대응
  - 절「방향장과 평형해」 — 소절: 평형해가 안정한가 불안정한가 — y′=y의 y=0은 밀려나고 y′=−y의 y=0은 되돌아온다 / 로지스틱에서 y=0은 불안정, y=M은 안정이고 그것이 S자 그림의 두 점선이 다른 이유 / 해곡선이 서로 교차하지 않는다는 것 — 뒤 절 유일성의 그림판 / 그림: 평형해 위아래에 화살표를 그려 안정·불안정을 가르는 수직선
  - 절「변수분리형」 — 소절: h(y)=0인 자리에서 나누면 안 된다는 것 — 그 자리가 정확히 평형해이고 나누면서 잃은 해를 마지막에 되찾는다 / dy/y = dx 라는 조작이 왜 정당한가 — 31번이 「dy/dx는 통째로 하나의 기호」라 했던 것과 부딪히는 자리이고 실제로는 치환적분을 줄여 쓴 것
  - 절「손으로 푸는 세 모형」 — 소절: 세 모형의 오른쪽 식을 나란히 놓고 무엇이 같아서 같은 절차로 풀리는지 — 전부 y만의 함수다 / 로지스틱을 실제로 변수분리로 풀기 — 1/(y(1−y/M))을 부분분수로 쪼개는 15번의 인수분해가 그 자리에 쓰인다
  - 절「오일러 방법」(원고 「대개는 풀리지 않는다」를 흡수) — 소절: 꺾은선이 아래로 처지는가 위로 넘치는가는 곡선이 볼록인가 오목인가가 정한다는 규칙 / h를 줄이면 오차가 줄지만 무한정은 아니라는 것 — 걸음이 늘수록 반올림 오차가 쌓여 47번의 이야기와 만난다
  - 절「연습 문제」 — 소절: 기존 셋(적분하고 초기조건 맞추기·변수분리와 지수 모형·평형해와 오일러) 유지 / 새 연습 4 「방향장과 안정성」 3문항 — 그림에서 평형해 짚기, 그 평형해가 안정인지 불안정인지 답하기, 오른쪽 식 셋과 방향장 그림 셋 짝짓기
  - 절「정리」

- `math-basics-what-is-a-derivative` (지금 4,130자 · 절 8)
  - 절「미분계수와 접선의 기울기」(원고 「그 값은 접선의 기울기다」를 흡수하고 문장형 제목을 이름으로) — 소절: 같은 값을 x→a 꼴 (f(x)−f(a))/(x−a) 로 적는 다른 정의식과 어느 쪽이 언제 편한지 / h→0이 양쪽에서 다가가는 것이라는 명시 — 뒤의 |x| 절이 쓰는 조건을 정의 자리에서 미리 세운다 / 「곡선에 닿기만 하고 지나가지 않는 직선」이라는 접선 설명이 틀리는 경우 — x³의 x=0에서 접선이 곡선을 가로지른다 / 접선 근사의 오차가 h²에 비례한다는 것 — 원고가 적어 둔 0.01과 0.0001이 이미 그 표다
  - 절「기준점을 변수로」 — 소절: 이계도함수 f″의 이름을 여기서 붙이고 부호가 볼록·오목을 말한다는 것만 예고 / f′의 그래프를 보고 f를 되짚는 반대 방향 — 부호와 0이 되는 자리만으로 원래 모양이 잡힌다
  - 절「정의식으로 넷 유도」 — 소절: 가장 쉬운 둘이 빠져 있다 — 상수함수 f′=0과 일차함수 f′=m을 정의식으로 / 합과 상수배가 따로따로 미분된다는 것을 정의식으로 확인 — 「규칙 없이 정의만으로 끝까지 간다」는 이 글의 목적에 맞는 자리
  - 절「부호로 읽는 오르내림」 — 소절: 닫힌 구간에서 최대·최소를 찾는 절차 — f′=0인 자리와 끝점을 함께 후보에 넣는다 / 부호표를 실제로 그려 한 줄로 판정하는 형식
  - 절「미분이 안 되는 자리」 — 소절: ReLU가 0에서 미분이 안 되는데 딥러닝이 그냥 쓰는 자리 — |x|와 같은 모양이고 그 한 점을 0이나 1로 정해 두고 넘어간다 / 미분가능한 점이 하나도 없는 함수도 있다는 것은 이름만 적고 지나가기
  - 절「연습 문제」 — 소절: 기존 셋(정의식으로 도함수·접선의 방정식·부호와 예외) 유지 / 새 연습 4 「그래프로 답하기」 3문항 — f의 그래프를 보고 f′의 그래프 고르기, f′=0이 되는 x를 그래프에서 짚기, 미분되지 않는 점을 찾고 셋 중 어느 모양인지 답하기
  - 절「정리」

- `math-basics-descriptive-statistics` (지금 4,153자 · 절 7)
  - 절「평균 넷」 — 소절: 산술평균이 제곱합을 가장 작게 하는 값이고 중앙값은 절댓값 합을 가장 작게 한다는 것 — MSE와 MAE가 갈리는 자리가 여기서 나온다 / 가중평균이 산술평균의 일반형이라는 것(가중치가 전부 1/n) / 조화평균은 「같은 거리」일 때이고 「같은 시간」이면 산술평균이라는 조건의 대비
  - 절「중앙값과 최빈값」 — 소절: 짝수 개일 때 가운데 둘의 평균으로 정하는 것은 규약이고 다른 규약도 있다는 것 / 평균이 자료에 없는 값일 수 있다는 것 — 자녀 수 1.8명 / 연속 자료에서는 최빈값이 뜻을 갖지 못하고 히스토그램의 봉우리로 대신한다는 예고
  - 절「분산과 표준편차」 — 소절: 모든 값에 상수를 더하면 분산이 그대로이고 배수를 곱하면 제곱배가 된다는 것 — Var(aX+b)=a²Var(X) / 표준화(z점수)로 평균 0·표준편차 1로 옮기기 — 뒤의 글들이 계속 쓰는 조작이라 여기서 이름을 붙인다 / E[X²]−μ² 로 계산하면 크기가 비슷한 둘을 빼는 자리라 자릿수 소거가 난다는 47번과의 연결
  - 절「사분위수와 상자그림」 — 소절: Q₁을 구하는 방법이 하나가 아니라는 것 — 중앙값을 아래 절반에 넣느냐로 값이 갈려 손계산과 라이브러리가 다를 수 있다 / 수염이 최솟값까지가 아니라 「1.5 IQR 안의 가장 먼 점」까지라는 정의
  - 절「히스토그램의 칸」 — 소절: 칸 개수의 경험 규칙(√n, 스터지스) 이름만 적고 지나가기 / 칸 폭이 다르면 세로축을 개수가 아니라 밀도로 놓고 면적으로 읽어야 한다는 것 / 평균과 분산이 같은데 그림이 전혀 다른 자료 두 벌을 직접 지어 보기 — 요약값과 그림을 늘 함께 내는 이유
  - 절「연습 문제」 — 소절: 기존 셋(여러 평균·분산과 사분위수·확률변수의 분산) 유지 / 새 연습 4 「같은 요약, 다른 자료」 3문항 — 평균이 같고 분산이 다른 다섯 수 짓기, 중앙값이 같고 평균이 크게 다른 자료 짓기, 자료 전체에 3을 더하고 2를 곱했을 때 평균·분산·표준편차가 각각 어떻게 되는지
  - 절「정리」

- `math-gaussian-and-clt` (지금 4,230자 · 절 9)
  - 절「밀도와 두 손잡이」 — 소절: 밀도는 확률이 아니라는 것 — σ가 작으면 f(x)가 1을 넘고 한 점의 확률은 0이다 / 평균·중앙값·최빈값이 모두 μ로 겹치는 유일한 자리 / 손잡이가 둘뿐이라는 말의 다른 얼굴 — 분산이 정해진 분포 중 엔트로피가 최대인 것이 정규분포다
  - 절「표준화와 68-95-99.7」(원고 「68-95-99.7」을 흡수) — 소절: 표준화가 분포의 모양을 바꾸지 않는다는 것 — 정규가 아닌 것을 표준화해도 정규가 되지 않고, LayerNorm이 활성값을 정규분포로 만들어 주는 것이 아니라는 흔한 오해 / 자주 쓰는 두 분위수 1.96과 2.58을 표에 함께 두기
  - 절「독립 합의 재생성」 — 소절: 차 X−Y도 정규이고 선형결합 Σa_iX_i가 통째로 정규라는 일반형 — 어텐션의 가중합에 곧장 쓰인다 / 독립이 아니면 성립하지 않는다는 것 — X와 −X를 더하면 정규가 아니라 상수 0
  - 절「중심극한정리」(원고 「얼마나 빨리 가까워지는가」를 흡수) — 소절: 정리가 말하는 것은 표준화한 것의 분포 수렴이지 X̄ 자체가 정규로 「간다」가 아니라는 정밀화 — X̄의 분산은 0으로 줄어든다 / 같은 정리의 두 읽기 — 합은 √n으로 퍼지고 평균은 1/√n으로 좁아진다 / 왜도가 0인데도 정규가 아닌 경우 — 대칭이면서 꼬리가 두꺼운 t분포, 그래서 첨도가 따로 필요하다
  - 절「기대면 안 되는 자리」 — 소절: 표본이 독립이 아닐 때 — 같은 프롬프트에서 뽑은 여러 답, 시계열, 같은 문서에서 온 토큰들이 전부 여기 걸린다 / 표본 수가 아니라 「독립인 표본 수」가 √n의 n이라는 것
  - 절「GELU와 Φ」 — 소절: Φ에 닫힌 꼴이 없어 쓰는 두 근사(tanh 세제곱꼴과 σ(1.702x))와 둘의 차이 / SiLU가 Φ 대신 시그모이드를 쓴 같은 구조라는 대응
  - 절「정리」

- `math-basics-symbol-glossary` (지금 4,249자 · 절 6)
  - 절「이 글을 쓰는 법」 — 소절: 표를 훑는 순서 — 앞에서부터 읽지 말고 목차에서 필요한 표로 바로 가라는 사용법
  - 절「자주 오독하는 열둘」 — 소절: log의 밑 생략 — 수학책은 10, 정보이론은 2, 딥러닝은 e라서 같은 글자가 다른 값을 뜻한다 / 쉼표와 세미콜론 — f(x, y)와 f(x; θ)에서 세미콜론 뒤는 변수가 아니라 고정된 값 / 괄호의 네 얼굴 — 함수 적용, 곱셈의 묶음, 순서쌍, 열린구간 / 틸데 ~ 의 두 뜻 — 「분포를 따른다」와 「대략 같다」 / 가운뎃점 · 의 두 뜻 — 곱셈과 자리 표시(f(·))
  - 절「표로 지나가는 나머지」 — 소절: 화살표 셋을 한 표로 — → (집합 사이), ↦ (원소 사이), ⇒ (논리) / 곱셈 기호 셋(×, ·, 생략)과 코드의 * 가 서로 다른 자리에 쓰인다는 것
  - (새 절)「읽을 수 없는 식을 만났을 때」 — 소절: 절차 넷 — 기호마다 이름을 소리 내어 부르기, 각 기호의 안에 든 것이 무엇인지 보기, 무엇이 자유이고 무엇이 묶였는지 가르기, 정의가 적힌 문장을 거슬러 찾기 / 뜻을 몰라도 읽기는 끝낼 수 있다는 것 — 이 글의 연습이 하려던 일을 본문으로 올린다 / 그림: 한 줄짜리 식 위에 네 걸음을 화살표로 얹은 그림
  - 절「이 트랙에서 안 쓰는 것」 — 소절: 읽는 법만 챙기는 목록에 ↦·≜·⊕를 더하기
  - 절「연습 문제」 — 소절: 기존 「다섯 줄을 소리 내어 읽기」 유지 / 새 연습 2 「가려 읽기」 5문항 — 세로줄이 든 다섯 식에서 절댓값·원소 개수·행렬식 중 무엇인지 고르기 / 새 연습 3 「이름으로 받아쓰기」 3문항 — 읽은 말을 듣고 기호로 다시 적기(argmax·∀·:= 를 섞는다)
  - 절「정리」

- `math-elbo` (지금 4,268자 · 절 8)
  - 절「구할 수 없는 값」 — 소절: p(z)에서 뽑은 표본이 왜 쓸모없는지를 숫자로 — 차원이 늘수록 사후분포가 차지하는 부피 비가 지수로 줄어든다 / q(z)와 q(z|x)의 표기 미끄러짐 — 이 글은 x 하나를 고정해 두고 q(z)로 적지만 VAE의 q는 x마다 다르고, 그 차이가 뒤의 「인코더로 때운다」로 되돌아온다
  - 절「젠센 경로」 — 소절: 젠센의 등호 조건이 p(x,z)/q(z)가 상수일 때이고 그것이 정확히 q=p(z|x)라는 것 — 경로 A도 등호 조건까지는 준다 / 간극의 크기는 못 주지만 「언제 0인가」는 준다는 정확한 진술로 원고의 단정을 다듬기
  - 절「KL 경로」(원고 「숫자로 확인한다」를 흡수) — 소절: KL의 방향이 KL(q‖p)이지 KL(p‖q)가 아니라는 것 / 그 방향이 q를 사후분포보다 좁게 만든다(mode-seeking) — VAE의 사후분포가 좁아지는 이유가 손실 안에 이미 적혀 있다 / 그림: 봉우리 둘짜리 사후분포에 두 방향의 KL이 각각 맞춘 q
  - 절「재구성과 KL」 — 소절: KL 항의 닫힌 꼴을 실제로 유도해 코드의 −0.5·Σ(1+logvar−μ²−exp(logvar)) 한 줄과 글자마다 맞추기 — 맨 앞에 던진 코드를 회수하는 자리 / reduction='sum'과 'mean'의 차이가 β를 암묵적으로 바꾼다는 것 — 실무에서 가장 흔한 버그 / 사후분포 붕괴 — KL이 0으로 가고 디코더가 z를 무시하는 자리가 줄다리기의 한쪽 끝이라는 것
  - 절「하한이 헐거운 세 원인」 — 소절: 표본 K개를 써서 하한을 더 조이는 길(IWAE)과 K가 커질수록 log p(x)에 가까워진다는 것 / ELBO를 한 표본으로 추정해도 불편이라는 확인 — 학습이 그 위에서 도는 근거
  - 절「VAE·확산·EM」 — 소절: 평균장 근사도 같은 식의 또 다른 조달 방식이라는 것 / 셋을 「q를 어떻게 얻는가」 한 열로 세운 표 — 최적화·인코더·손으로 고정
  - 절「정리」

- `math-log-derivative-trick` (지금 4,284자 · 절 7)
  - 절「한 줄짜리 항등식」 — 소절: f=1을 넣으면 E[∇log p]=0 이 나온다는 것 — 다음 글 베이스라인의 근거가 이 항등식 안에 이미 있다 / 「점수 함수」라는 이름의 나머지 절반 — 그 분산이 피셔 정보라는 것만 이름으로
  - 절「성립 조건 둘」 — 소절: 조건이 깨졌을 때 빠지는 것이 무엇인지 — 라이프니츠 규칙의 경계항이고 U(0,θ) 예에서 참값 1/2와 −1/2의 차 1이 정확히 그 항 / 소프트맥스가 조건을 만족한다는 것과 마스킹으로 −∞를 넣은 자리는 지지집합에서 빠진다는 구별
  - 절「불편성 확인」 — 소절: 불편이지만 분산이 무한할 수도 있다는 것 — 불편성은 쓸 만함을 보장하지 않는다 / 표본 하나로도 불편이라는 것과 실제 RLHF가 프롬프트마다 답 하나만 쓰는 자리
  - 절「분산이라는 대가」(문장형 「대가: 분산이 크다」를 이름으로) — 소절: 분산이 크면 걸음을 작게 잡아야 하므로 비용이 표본 수가 아니라 걸음 수로도 나타난다는 것 / 시퀀스가 길 때 분산을 줄이는 자리가 셋 있고(베이스라인·미래 보상만 세기·길이 정규화) 다음 글이 첫째를 맡는다는 예고
  - 절「막힌 다른 길」(물음형 제목을 이름으로) — 소절: 이산인데도 재매개변수화를 쓰는 세 번째 길 — 검벨-소프트맥스로 연속 완화하기 / 그 길이 언어모델에 안 쓰이는 이유 — 완화가 편향을 만들고 토큰마다 쌓여 시퀀스에서 커진다
  - 절「다시 그 한 줄로」 — 소절: detach()를 빼면 실제로 어떤 잘못된 그래디언트가 흐르는지 — 점수 쪽으로도 미분이 흘러 목적식이 달라진다 / policy_loss 값 자체는 최소화 대상이 아니라 그래디언트를 만들기 위한 대리식이라 크기를 읽으면 안 된다는 것 — 로그를 보는 사람이 반드시 걸리는 자리
  - 절「정리」

- `math-basics-area-and-volume` (지금 4,287자 · 절 8)
  - 절「글자로 이름 붙이기」 — 소절: 글자에 값을 넣어 보는 것으로 식이 맞는지 확인하는 습관(가로 5·세로 3을 ab에 넣어 15) / 곱셈 기호를 생략하면 두 글자가 한 이름처럼 보이는 자리 — ab와 「a 곱하기 b」를 소리로 가르기
  - 절「넓이의 정의와 단위 정사각형」(물음형 제목을 이름으로) — 소절: 한 칸이 딱 안 떨어질 때 — 가로 2.5면 반 칸을 세는 것이고 이것이 33번 적분으로 이어지는 씨앗 / 넓이의 가법성 — 겹치지 않게 나눠 더해도 된다는 성질이 뒤의 「잘라 붙이기」 전부를 떠받친다 / 둘레와 넓이는 따로 논다 — 둘레가 20으로 같은 1×9와 5×5의 넓이가 9와 25
  - 절「잘라 붙여 직사각형으로」 — 소절: 어느 변을 밑변으로 잡아도 넓이가 같다는 것 — 밑변을 바꾸면 높이도 바뀌고 3·4·5로 확인 / 둔각삼각형의 높이는 도형 밖에 있어 밑변을 연장해 재야 한다는 것 / 마름모도 같은 절차에서 나온다 — 대각선 둘의 곱을 2로 나누기 / 그림: 같은 삼각형을 세 밑변으로 각각 잡아 높이가 달라지는 세 칸
  - 절「원주율과 원의 넓이」(문장형 제목을 이름으로) — 소절: π가 끝나지 않고 되풀이도 하지 않는 수라는 것과 3.14, 22/7 같은 근사 — 10번 수의 종류로 넘기는 다리 / 조각을 8·16·32로 늘릴 때 밑변이 πr에 얼마나 가까워지는지를 수로 적기
  - 절「부피와 단위」(원고 「단위 — 1 m²는 10,000 cm²다」를 흡수) — 소절: 겉넓이와 부피의 구별 — 포장지와 담기는 양, 직육면체 겉넓이를 실제로 세어 보기 / 리터와 cc — 1 L = 1,000 cm³ 이고 우유갑을 세로·가로·높이로 확인 / 밑면이 비스듬해도 밑넓이 곱하기 높이라는 것은 이름만 적고 지나가기
  - 절「연습 문제」 — 소절: 기존 넷(넓이 열·그림 셋 그려 공식 복원·부피 다섯·단위 환산 다섯) 유지 / 새 연습 5 「복합 도형」 4문항 — 직사각형에서 원 하나를 오려 낸 넓이, ㄱ자 도형을 두 직사각형으로 잘라 구하기, 사다리꼴을 삼각형 둘로 잘라 구해 공식과 맞추기, 같은 도형을 두 방법으로 잘라 답이 같은지 검산
  - 절「정리」

- `math-vector-as-meaning` (지금 4,300자 · 절 8)
  - 절「평균을 내도 되는 배열」 — 소절: 별점 1~5처럼 순서는 있고 간격은 없는 값 — 토큰 ID와 같은 문제의 덜 극단적인 판이고 평균이 애매한 이유 / 판정 절차 셋 — 더해 보기, 배수 해 보기, 결과를 원래 쓰던 함수에 다시 넣어 보기
  - 절「두 연산에 닫힘」 — 소절: 벡터공간에는 벡터끼리의 곱셈이 없다는 것 — v*w(원소별)는 나중에 따로 얹는 연산이지 정의에 든 것이 아니다 / 스칼라가 실수 아닌 것이어도 된다는 것 — 회전 위치 인코딩이 복소수로 적히는 자리는 이름만
  - 절「선형결합」 — 소절: 계수의 합이 1인지 아닌지가 가르는 것 — 아핀결합과 볼록결합에 여기서 이름을 붙이면 뒤의 확률분포 절이 그 말을 쓴다 / 어텐션 계수는 음이 아니고 합이 1이라 출력이 값 벡터들의 볼록껍질 안에 있다는 것 — 「공간을 못 벗어난다」보다 훨씬 좁은 사실
  - 절「king − man + woman」 — 소절: 실제 구현이 질의에 쓴 세 단어를 후보에서 빼는 관행 — 원고 코드가 man·king·woman까지 후보에 두고 거리를 찍는 자리에 붙는다 / 차이 벡터가 단어쌍마다 달라 여러 쌍을 평균 내야 방향이 잡힌다는 것 / 정규화를 하느냐 마느냐가 최근접 결과를 바꾼다는 것
  - 절「화살표와 좌표」(원고 「브로드캐스팅은 무엇을 대신하는가」를 흡수) — 소절: 위치 벡터와 자유 벡터의 구별 — 「시작점을 갖지 않는다」와 「점」이 다른 말이고 유추 절이 그것을 쓰고 있었다 / 축을 정하지 않으면 좌표가 없다는 것 — np.array를 적는 순간 이미 기저를 고른 것
  - 절「벡터가 아닌 것들」 — 소절: 비음수 제약 — ReLU 출력에 −1을 곱하면 나가 버린다 / 양자화된 정수 가중치도 닫히지 않는다는 것 — 두 int8을 더하면 격자 위가 아닐 수 있다 / 정수 격자와 실수 공간의 차이가 임베딩 층이 하는 일과 같은 종류라는 정리
  - 절「정리」 — 소절: 원고에는 정리 절이 없다 — 다른 수학 글처럼 요약 절을 세우고 「다시 그 API 응답으로」의 회수는 그 뒤 맺음 문단으로 내린다

- `math-basics-hypothesis-testing-logic` (지금 4,318자 · 절 10)
  - 절「귀무가설에서 시작하는 이유」 — 소절: 「기각하지 못했다」와 「참이라고 받아들였다」가 다른 말이라는 것 — 무죄추정과 같은 구조이고 이 글에서 가장 자주 어긋나는 자리 / 단측과 양측을 언제 고르는가 — 자료를 보고 정하면 안 되고 가설을 세울 때 함께 정한다
  - 절「검정통계량과 p값」(원고 「검정통계량과 기각역」과 「유의수준과 p값」을 합침) — 소절: 제목에만 있고 본문에 없는 기각역을 실제로 정의하기 — α를 먼저 정해 「어느 값부터 기각인가」를 자료를 보기 전에 긋는 것 / 이산 분포라 α를 정확히 0.05에 맞출 수 없다는 것 — X≥15는 0.021이고 X≥14는 0.058이라 실제 유의수준이 0.05가 아니다
  - 절「p값이 아닌 것 셋」(원고 「통계적 유의성은 크기가 아니다」를 흡수 — 셋 중 둘째와 같은 내용이다) — 소절: p=0.049와 p=0.051이 다른 세계가 아니라는 것 — 문턱은 규약이고 값 자체를 적어야 한다 / 멈출 때를 정해 두지 않고 자료를 더 모으면 p값이 저절로 내려간다는 것(p-해킹의 가장 단순한 꼴)
  - 절「두 가지 오류」(원고 「여러 번 재면 우연히 통과하는 것이 생긴다」를 흡수) — 소절: 검정력을 실제로 세어 보기 — p=0.7, n=20에서 기각역에 들어갈 확률 / α·효과 크기·n·검정력 넷 중 셋을 정하면 나머지가 정해진다는 관계 / 검정을 스무 번 하면 1−0.95²⁰≈0.64로 하나쯤은 우연히 걸린다는 계산
  - 절「기각과 구간」 — 소절: 신뢰구간이 「참값이 그 안에 있을 확률 95%」가 아니라는 것 — 흔들리는 것은 구간이지 참값이 아니고, p값의 첫째 오해와 같은 종류 / 구간의 폭이 √n으로 좁아진다는 것과 그것이 앞의 「표본을 키우면 기각된다」와 같은 사실의 두 얼굴
  - 절「연습 문제」 — 소절: 기존 셋(이항계수로 p값·정의 확인·다른 귀무가설 아래에서 계산) 유지 / 새 연습 4 「기각역과 검정력」 3문항 — α=0.05에서 기각역을 실제로 긋고 그때의 참 유의수준 구하기, p=0.7일 때 그 기각역에 들어갈 확률 구하기, n을 40으로 늘리면 그 값이 어떻게 되는지
  - 절「정리」

- `math-sde-ode-and-discretization` (지금 4,328자 · 절 7)
  - 절「이산 사슬에서 SDE로」(조건절 제목을 이름으로) — 소절: dw가 무엇인지 정의하기 — 평균 0, 분산 dt인 정규 증분이고 원고는 이름만 부르고 지나간다 / 왜 √Δt 인가 — 독립 증분의 분산이 더해지므로 표준편차는 제곱근으로 자란다, 가우시안 글의 재생성성이 그대로 쓰이는 자리 / dw²가 dt와 같아져 보통 미분과 계산법이 갈린다는 것은 이름만
  - 절「확률 흐름 ODE」(문장형 「잡음을 뺀 쌍둥이」를 이름으로) — 소절: 잡음을 빼도 주변분포가 같은 이유 — 연속방정식에서 확산항을 드리프트로 옮겨 적으면 같은 밀도의 흐름이 된다 / SDE 쪽이 유리한 자리 — 잡음이 중간에 쌓인 오차를 지워 주므로 스텝이 많으면 SDE 쪽 품질이 낫다는 관찰
  - 절「이산화 오차의 차수」 — 소절: 차수와 별개로 h가 크면 발산한다는 것 — β가 20까지 가는 구간의 안정성 문제 / 하이운이 두 번 부르는 것을 「같은 h에서 두 배 비싸다」로 세는 것과 「같은 비용에서 h가 두 배」로 세는 것의 구별
  - 절「시간 격자 고르기」(원고 「실제로 재 본다」를 이 이름으로 바꾸고 실측 표를 그 안에 둔다) — 소절: t를 균등하게 자르는 것이 최선이 아니라는 것 — σ 균등이나 로그 균등으로 자르면 같은 스텝 수에서 오차가 준다 / 스텝을 어디에 몰아 줄 것인가 — 속도가 빨리 변하는 구간이 답이고 그것이 확산에서는 t가 0에 가까운 쪽 / 그림: 같은 스텝 수를 균등 격자와 σ 균등 격자로 놓았을 때 경로 위 점의 배치
  - 절「DDIM의 정체」(물음형 제목을 이름으로) — 소절: η 파라미터가 0이면 ODE, 1이면 DDPM이라 둘 사이가 이어진 연속체라는 것 / 좌표를 바꾸는 것이 왜 이득인가 — 같은 방정식이라도 변수를 잘 고르면 오일러의 국소 오차가 줄어든다는 일반 원리
  - 절「비용을 맞춘 비교」(물음형 제목을 이름으로) — 소절: CFG를 켜면 한 스텝에 신경망을 두 번 부르므로 NFE 계산이 통째로 달라진다는 것 / 여기서 잰 오차는 x의 L2이고 사람이 보는 품질과 다르다는 것 — 이산화 오차가 작다고 그림이 좋은 것이 아니다
  - 절「정리」

- `math-flow-matching-and-optimal-transport` (지금 4,332자 · 절 9)
  - 절「확률 경로와 속도장」 — 소절: 「직선성 지표」를 여기서 정의하기 — 정리에서 4.79라는 값으로 쓰는데 본문에 정의가 없다 / 같은 양끝을 잇는 확률 경로가 무수히 많고 확산의 경로도 그중 하나라는 것 — 「경로를 고른다」가 무엇을 고르는 일인지
  - 절「조건부에서 주변으로」(원고 「알 수 없는 것을 알 수 있는 것으로 바꾸기」를 이름으로) — 소절: t를 균등하게 뽑는 것이 사실상의 시각 가중치라는 것 — 「가중치도 스케줄도 없다」는 말이 t의 분포를 고르는 순간 반쯤 되돌아온다 / v-예측·ε-예측·x₀-예측이 서로 선형변환이라 같은 것을 다르게 적은 것이라는 대응표
  - 절「직선 경로가 휘는 자리」(물음형 제목을 이름으로) — 소절: 곡률이 어디서 가장 큰가 — 경로 가운데(t≈0.5)이고 그래서 스텝을 거기 몰아 준다 / 휘는 정도를 짝짓기의 교차 횟수로 세어 보는 것
  - 절「최적수송과 수송 비용」(원고 「최적수송: 어떻게 짝지을 것인가」와 「왜 KL이 아니라 수송 비용인가」를 합침) — 소절: 몽주와 칸토로비치의 차이 — 사상으로 풀면 해가 없을 수 있어 커플링으로 완화한다는 것 / W₂는 거리의 공리를 만족하지만 KL은 대칭도 아니고 삼각부등식도 없다는 대비 — 「거리」라고 부를 수 있는 근거 / 최적 커플링이 결정적 사상이 되는 조건(브레니어 정리)은 이름만
  - 절「스텝 수와 곡률」(원고 「최적수송으로 짝지으면 얼마나 나아지나」를 이름으로) — 소절: 바닥값 0.2519가 왜 바닥인지 — 같은 분포에서 2만 개를 두 번 뽑았을 때의 표본 오차라는 계산 / 직선성 지표가 0이면 오일러의 국소 오차가 0이라는 것을 지난 글의 테일러 항으로 잇기
  - 절「고차원에서의 근사」(문장형 제목을 이름으로) — 소절: 미니배치 최적수송이 편향을 만든다는 것 — 배치 안 최적이 전역 최적과 다르고 배치가 작을수록 어긋남이 크다 / 재정류를 반복할수록 모델 오차가 누적돼 분포가 조금씩 밀린다는 대가 / 조건부 경로를 직선 말고 다르게 고르는 자리 — 데이터에 잡음을 조금 섞는 σ_min
  - 절「정리」 — 소절: 원고의 「그래서 4스텝은 어디서 오나」는 절을 없애고 정리 뒤 맺음 문단으로 내려 절 수를 일곱에 맞춘다

- `math-notation-conventions` (지금 4,349자 · 절 8)
  - 절「글꼴과 자료형」(문장형 「글꼴이 곧 자료형이다」를 이름으로) — 소절: Notation 절이 없는 논문에서 하는 일 — 그 기호가 처음 나온 문장을 거슬러 찾는 절차 / 딥러닝 논문의 다른 관행 — 글꼴로 안 가르고 셰이프를 통째로 적는 스타일(h ∈ ℝ^{B×T×d})이고 그때는 축 이름이 곧 자료형
  - 절「열벡터 관례」 — 소절: 세 번째 관례가 있다는 것 — 행벡터를 왼쪽에 두는 xᵀW 꼴을 쓰는 논문들 / 배치 축은 식에서 통째로 생략된다는 것 — 논문의 식은 거의 언제나 표본 하나짜리이고 B는 코드에만 있다 / nn.Linear의 bias가 어느 축으로 브로드캐스팅되는지
  - 절「첨자의 두 역할」(원고 「원소와 슬라이스 — 콜론의 자리」를 흡수) — 소절: 0-기반과 1-기반 — 논문은 1부터, 코드는 0부터라 옮길 때마다 오프바이원이 생기는 자리 / 첨자가 겹쳐 붙는 꼴 h_t^{(l)} — 시각과 층이 함께 붙었을 때 어느 것이 무엇인지 가르는 순서
  - 절「연산 기호 다섯」 — 소절: 대괄호 연결 [a; b]와 ⊕ — 「붙이기」이지 더하기가 아니라는 것 / √· 가 벡터·행렬에 붙었을 때 원소별인지 행렬 제곱근인지 — Adam의 √v는 원소별이다 / ∘ (함수 합성)과 ⊙ (원소별 곱)이 비슷하게 생겨 갈리는 자리
  - 절「사상 표기 읽기」(제목을 이름으로) — 소절: →와 ↦의 구별 — 앞은 집합 사이, 뒤는 원소 사이 / 층을 이어 붙일 때 앞의 공역과 뒤의 정의역을 맞춰 보는 검사가 코드의 셰이프 검사와 같은 일이라는 것
  - 절「LayerNorm을 셰이프까지」(제목을 이름으로) — 소절: ε이 √ 안에 있는지 밖에 있는지가 구현마다 다르다는 것 — 식을 코드로 옮길 때 실제로 갈리는 자리 / 분산의 분모가 d인지 d−1인지 — PyTorch의 LayerNorm은 d이고 그것이 식의 1/d와 맞는다
  - 절「정리」 — 소절: 원고에는 정리 절이 없다 — 요약 절을 세우고 「다시 그 에러로」의 회수는 그 뒤 맺음 문단으로 내린다

- `math-scaling-by-sqrt-dk` (지금 4,449자 · 절 7)
  - 절「내적의 분산과 √d_k」(문장형 제목을 이름으로) — 소절: 성분의 분산이 1이 아니라 σ²이면 Var(s)=d_kσ⁴ 이라 스케일이 네제곱으로 어긋난다는 것 / 왜 d_k가 아니라 √d_k 로 나누는가 — d_k로 나누면 분산이 1/d_k가 되어 이번엔 너무 평평해진다는 계산 / 성분이 독립이 아니면 분산이 어느 쪽으로 어긋나는지 — q와 k가 같은 표현에서 나온 투영이라 상관이 양수이면 더 커진다
  - 절「스케일과 뾰족함」(문장형 제목을 이름으로) — 소절: softmax가 이동에는 불변인데 배율에는 불변이 아니라는 대비 — 「차이의 지수를 본다」의 정확한 진술이고 구현의 max 빼기가 앞쪽 성질을 쓰는 것 / 온도 τ로 나누는 것과 1/√d_k 가 식에서 같은 자리라는 것을 여기서 미리 짚기
  - 절「야코비안이 죽는 자리」(문장형 「그래서 기울기가 죽는다」를 이름으로) — 소절: 반대쪽도 있다는 것 — 너무 크게 나누면 분포가 균등에 붙어 어텐션이 평균 풀링이 되고 야코비안 표의 배수 1보다 왼쪽이 그 구간 / 학습 도중에 다시 뾰족해지는 것(엔트로피 붕괴)과 그 처방으로 q·k에 LayerNorm을 한 번 더 거는 자리
  - 절「스케일링을 뺀 실험」(제목을 이름으로) — 소절: 실패가 초기값에 달렸다는 것 — 시드를 바꾸면 우연히 맞는 자리에 뾰족해져 학습이 되기도 하고 그때도 훨씬 느리다 / 여러 시드로 돌려 「멈춘 비율」로 적어야 하는 이유
  - 절「세 장치의 역할 분담」(물음형 제목을 이름으로) — 소절: 잔차 연결과 층 수 — 깊어질수록 표현의 노름이 커져 이 계산의 가정이 층마다 조금씩 달라진다는 것 / Post-LN과 Pre-LN이 그 지점에서 갈린다는 것은 이름만
  - 절「코드로 확인하기」 — 소절: float16에서 나누지 않은 점수가 exp에서 넘쳐 NaN이 되는 자리 — 실무에서 실제로 나는 오류이고 스케일링이 그것도 막고 있다 / 분산 실측을 할 때 표본 수를 얼마로 잡아야 √d_k 와 셋째 자리까지 맞는지
  - 절「정리」

- `math-sigma-index-and-einsum` (지금 4,588자 · 절 9)
  - 절「Σ와 Π」(문장형 제목을 이름으로) — 소절: 빈 합이 0이고 빈 곱이 1이라는 약속 — 범위가 비는 경우가 코드에서 실제로 걸린다 / Σ의 선형성 — 상수를 밖으로 빼고 합을 쪼개는 두 규칙이 이후 유도의 대부분이라는 것
  - 절「자유 첨자와 더미 첨자」 — 소절: 더미 이름을 바꿔도 된다는 규칙 1의 단서 — 그 이름이 바깥에서 이미 자유 첨자로 쓰이고 있으면 값이 달라진다 / 자유 첨자의 순서가 결과 축의 순서를 정한다는 것 — 식에서는 안 보이고 einsum 문자열에서만 드러나는 자리
  - 절「이중합과 순서 교환」 — 소절: Σ와 바꿔도 되는 것과 안 되는 것 — 상수배와 다른 Σ는 되고 max와 log는 안 된다(Σmax ≠ maxΣ), 그것이 logsumexp가 따로 있는 이유 / 조건부 합 Σ_{i≠j} 의 읽기 — 어텐션 마스크와 대조학습이 정확히 이 꼴이다 / 그림: 격자에서 대각선을 뺀 칸만 훑는 조건부 합
  - 절「아인슈타인 합 규약」 — 소절: 원래 규약은 위 첨자와 아래 첨자의 쌍에만 합을 걸었고 딥러닝은 그 구별을 버렸다는 것 / 같은 첨자가 세 번 나오면 규약으로는 애매하고 einsum은 정해 준다는 차이
  - 절「einsum 문자열 만들기」(원고 「손으로 따라가기 — QKᵀ」를 흡수) — 소절: 생략 부호 `...` — 앞쪽 배치 축을 통째로 넘기는 `'...id,...jd->...ij'` 가 실무에서 가장 많이 쓰는 꼴 / `->` 오른쪽을 생략하면 반복 안 된 첨자를 알파벳 순으로 남긴다는 암묵 규칙과 그래서 언제나 적으라는 권고
  - 절「합성곱 한 칸」(문장형 제목을 이름으로) — 소절: stride와 dilation이 첨자의 어디에 들어가는가 — y·s + k_y·d 로 적히고 원고는 stride 1만 다룬다 / sliding_window_view가 복사를 안 하는 뷰인데 einsum이 그 자리에서 복사를 일으킨다는 것 — im2col의 메모리 값이 여기서 나온다
  - 절「정리」(원고 「einsum을 쓸 때 알아 둘 것」과 「다시 그 코드로」를 흡수) — 소절: 세 주의(축 순서 바꾸기의 비용, 셋 이상 곱할 때의 결합 순서와 optimize, 반복 첨자가 합인지 추출인지)를 정리 안의 한 묶음으로 옮기고 회수는 그 뒤 맺음 문단으로

- `math-quantization-error` (지금 4,632자 · 절 7)
  - 절「격자 사상과 스텝 크기」 — 소절: 자르기를 쓰면 오차가 [−Δ, 0]이고 평균이 −Δ/2로 치우친다는 것 — 왜 반올림인가의 답 / 확률적 반올림 — 편향을 없애 누적을 막고 학습 중 양자화에서 실제로 쓰인다 / int8의 표현 범위가 −128~127로 비대칭이라 대칭 양자화에서 한 칸을 버리고 −127~127을 쓰는 실무 / 범위 밖 값의 클램프 오차는 톱니가 아니라 한쪽으로만 커진다는 구별
  - 절「오차의 분산 Δ²/12」(문장형 제목을 이름으로) — 소절: 오차가 입력과 독립이라는 가정 — 실제로는 x의 함수라 결정적이고, 「잡음」이라 부르는 것 자체가 근사라는 것 / 여러 값을 더할 때 오차가 √n으로 자라는가 n으로 자라는가 — 뒤의 예산 식이 앞쪽을 쓴다
  - 절「비트 하나와 6.02dB」(문장형 제목을 이름으로) — 소절: 꼬리 길이 k를 분포마다 재 보기 — 균등 1.73, 정규 5.5, 가중치에 더 가까운 라플라스는 그보다 길다 / 최적 클리핑 지점을 실제로 구하는 절차 — 잘린 값의 오차와 남은 값이 되찾는 Δ² 이득을 더해 최소로 만드는 지점, 원고가 「감이 아니라 k로 정한다」고 해 놓고 절차를 안 준 자리 / 그림: 클리핑 지점을 옮길 때 두 오차의 합이 U자를 그리는 곡선
  - 절「대칭 격자와 비대칭 격자」(물음형 제목을 이름으로) — 소절: 영점이 정수여야 한다는 제약 — 0을 정확히 표현해야 패딩과 마스크가 정확히 0이 된다 / 비대칭에서 정수 커널에 남는 교차항 — 「커널이 단순해진다」를 식으로 보이는 자리
  - 절「이상치가 먹는 비트」(문장형 제목을 이름으로) — 소절: 그룹 크기 128·64·32에서 유효 비트와 추가 저장량의 표 — 채널별과 텐서 하나 사이의 눈금 / 가중치의 이상치는 고정이고 활성값의 이상치는 입력마다 달라 캘리브레이션이 필요하다는 구별 / 활성값에서는 채널이 아니라 토큰마다 스케일을 두는 쪽이 맞는 자리
  - 절「오차 예산」(제목을 이름으로) — 소절: 층을 여러 개 지날 때 오차가 곱해지는가 더해지는가 — 잔차 연결이 더하기 쪽으로 만든다는 것 / 같은 상대오차라도 층마다 영향이 다르다는 것 — 첫 층·마지막 층·어텐션 점수가 특히 민감한 이유 / 활성값까지 양자화하면 예산이 두 배가 된다는 것(W4A16과 W4A4의 갈림)
  - 절「정리」

- `math-activation-derivatives` (지금 4,657자 · 절 8)
  - 절「도함수가 곱해지는 자리」 — 소절: 역전파가 무엇을 들고 있어야 하는가 — sigmoid는 출력 하나, ReLU는 부호 한 비트, GELU·SiLU는 입력 z 전체라 활성 메모리가 갈린다 / z~N(0,1)에서 E[ReLU′]=0.5이고 E[ReLU(z)²]=0.5라 층마다 분산이 절반이 되는 것, He 초기화의 2배 계수가 그 0.5를 되돌리는 자리
  - 절「ReLU 계열의 1과 0」 — 소절: 음수 경로의 α는 지수로 죽는다 — 0.01을 두 층만 곱해도 1e−4라 「끊지 않는다」가 「살린다」는 아니다 / z가 정확히 0.0인 입력이 실제로 흔한 자리(패딩 뒤의 0, 0으로 초기화한 편향, 양자화된 활성)와 그때 0을 고르는지 1을 고르는지가 실제로 갈리는 성분 수
  - 절「sigmoid와 tanh의 포화」 — 소절: 출력층의 sigmoid는 왜 괜찮은가 — 교차엔트로피와 짝지으면 σ′가 약분되어 기울기가 p−y가 되고 0.25가 식에서 사라진다 / 포화를 정하는 것은 출력이 아니라 입력의 크기 — |z|>4에서 σ′<0.018이고, 앞에 선 LayerNorm이 z를 O(1)로 눌러 두는 것이 포화를 막는 실제 장치
  - 절「GELU의 확률 게이트」 — 소절: GELU는 단조가 아니다 — z=−0.7518에서 −0.1700까지 내려갔다 올라오고, 그 골이 「작은 음수를 조금 살려 둔다」의 정확한 크기 / GELU″의 최댓값이 2φ(0)=0.7979라 도함수가 그보다 빨리 변하지 못한다, 매끄러움이 최적화에 주는 것 / z~N(0,1)에서 E[GELU′(Z)]=0.5로 ReLU와 정확히 같다 — 초기화 시점의 배율이 같다는 뜻 / 그림: ReLU·GELU·SiLU의 값 곡선을 −3~1 구간으로 확대해 음수 쪽 골 셋을 맞대는 그림
  - 절「SiLU와 세 함수의 대조」 — 소절: SiLU의 최솟값 −0.2785(z=−1.2785)가 GELU의 −0.1700보다 깊다 — 표의 「음수를 지난다」 칸에 크기를 붙이기 / z·σ(βz)의 β 하나가 셋을 잇는다 — β를 키우면 ReLU로, 0으로 보내면 z/2로 간다 / 게이트를 다른 선형층에 맡기면 SwiGLU가 되어 활성함수가 아니라 층이 된다, 그래서 d_ff를 3분의 2로 줄이는 관례
  - 절「코드로 확인하기」 — 소절: 도함수의 기댓값을 표본 40만 개로 재어 세 함수가 전부 0.5로 모이는 것 확인 / 층을 쌓아 곱해 보기 — 무작위 z에서 50층의 도함수 곱을 네 함수로 재면 0.25ⁿ 표보다 훨씬 빨리 죽는다(그 표는 최댓값 가정이다)

- `math-norms-and-distance` (지금 4,786자 · 절 10)
  - 절「노름의 세 공리」 — 소절: p<1이면 노름이 아니다 — (1,0)과 (0,1)을 p=1/2로 재면 합의 노름이 4가 되어 1+1을 넘는다 / 세 공리가 함께 주는 것은 볼록성 — 단위구가 볼록집합이고, 정규화 항이 볼록이라 최솟값이 하나뿐인 것이 여기서 온다 / 0이 아닌 성분의 개수는 노름이 아니다 — 2배 해도 값이 그대로라 둘째 공리가 깨지고, 희소성을 그것으로 직접 벌하지 못하는 이유가 그것
  - 절「L1·L2·L∞와 단위구」 — 소절: 남은 부등식 ‖x‖∞ ≤ ‖x‖₂ ≤ √d‖x‖∞ 와 두 등호 자리(한 성분에 몰릴 때·모든 성분이 같을 때) / 벡터가 아니라 행렬을 재는 자리 — clip_grad_norm_이 재는 것은 층마다의 프로베니우스 노름을 다시 제곱합한 값이고, 가장 크게 늘이는 배율을 재는 스펙트럴 노름은 다른 것
  - 절「노름이 만드는 거리」 — 소절: 길이를 1로 맞춘 벡터에서는 ‖a−b‖²=2−2cos가 성립한다 — 코사인이 거리는 아니지만 이때만 순위가 L2와 같아진다 / 차원이 커지면 가장 가까운 점과 가장 먼 점의 거리 비가 1로 가는 현상, 그 무너짐이 L1에서 더 늦게 온다는 관찰
  - 절「클리핑이 자르는 것」 — 소절: 클리핑은 편향을 만든다 — E[clip(g)]는 clip(E[g])와 다르므로, 문턱을 낮게 잡으면 방향은 남아도 기대 갱신이 참 그래디언트에서 어긋난다 / 문턱을 고르는 법 — 서두 로그의 1.84·2.07처럼 초반 몇백 스텝의 grad_norm 중앙값을 재고 그 언저리에 두는 것, 1.0이 하늘에서 온 값이 아닌 자리
  - 절「감쇠와 희소성」 — 소절: soft(w,λ)가 어디서 나오는가 — ½(w−v)²+λ|w|를 최소화하면 그 식이 그대로 떨어지고, 그 해법의 이름이 근접 연산자 / λ가 정하는 것은 세기가 아니라 개수 — 절댓값이 λ보다 작은 성분이 전부 0이 되므로 λ는 「몇 개를 지울지」의 눈금 / L1과 L2를 함께 쓰면 꼭짓점은 남기고 상관 있는 가중치는 함께 살아남는다
  - 절「그 로그를 다시 읽기」 — 소절: (「같은 L2, 다른 L1」 표를 이 절의 첫 소절로 흡수) / 세 줄의 기본값이 같은 「크게 하지 마라」가 아니다 — max_norm=1.0은 그래디언트 길이의 상한이고 weight_decay=0.01은 스텝마다 곱하는 비율이라 단위부터 다르다 / 편향과 LayerNorm 파라미터를 감쇠 대상에서 빼는 관례, 그 둘은 크기를 줄일 이유가 없다는 것

- `math-basics-reading-proofs` (지금 4,797자 · 절 13)
  - 절「문서의 낱말과 문장 가르기」 — 소절: 제목이 약속한 「따라서」의 정체 — 따라서·그러므로는 바로 앞에서 논리로 따라 나온다는 표시, 즉·다시 말해는 같은 것을 다른 말로 적은 것, 한편은 새 갈래의 시작, 이때·그러면은 조건을 이어 붙이는 자리 / 「따라서」를 만나면 근거가 바로 위 한 줄인지 여러 줄인지부터 짚는 읽기, 근거가 안 보이면 그 줄은 정의로 되돌아가라는 표시라는 것 / 필요조건과 충분조건 — P⇒Q에서 P가 충분조건이고 Q가 필요조건이라, 「필요하다」가 결론 쪽에 붙는 말이라는 방향 / 그림: 따라서·즉·한편이 앞 줄과 잇는 방식을 화살표 셋으로 구별한 그림
  - 절「역·이·대우와 반례」 — 소절: 부정을 만드는 법 — 「모든 x에서 P」의 부정은 「어떤 x에서 P가 아니다」이고 그 반대도 마찬가지, 귀류법의 첫 줄이 하는 일이 이 뒤집기 / 「모든」과 「어떤」이 겹치면 순서가 뜻을 바꾼다 — 「모든 학생에게 좋아하는 과목이 있다」와 「모든 학생이 좋아하는 한 과목이 있다」 / 예를 몇 개 들어 보는 것은 증명이 아니다 — n²+n+41이 n=0부터 39까지 소수인데 40에서 41의 배수로 깨지는 자리
  - 절「증명 뼈대 여섯」 — 소절: 뼈대를 고르는 세 물음 — 직접이 막히는가, 결론이 존재 주장인가, 자연수 전체에 대한 주장인가 / 순환논법 — 보이려는 것을 근거로 쓰는 것이 가장 흔한 오류이고, 결론의 부정에서 출발하는 귀류법이 그것과 어떻게 다른지
  - 절「뼈대 넷의 적용」 — 소절: 귀납법에서 출발을 빼먹으면 무너지는 예 — 「n=n+1」은 이음만 놓고 보면 성립한다 / 강한 귀납법 — k 하나가 아니라 k까지 전부를 참으로 놓아야 넘어가는 자리(모든 자연수는 소수의 곱이다)와 보통 귀납법의 차이
  - 절「관용구와 읽는 순서」 — 소절: 관용구 넷에 둘을 더한다 — 「비슷하게」는 같은 계산의 되풀이라 직접 해 보라는 뜻이고, 「연습으로 남긴다」는 안 어렵다는 뜻이 아니다 / 막히는 자리의 갈래 셋 — 낱말의 정의를 모르는가, 앞 줄과의 연결을 모르는가, 왜 그 방향으로 가는지를 모르는가, 셋의 처방이 각각 다르다
  - 절「연습 문제」 — 소절: 접속어 3문항 — 어떤 줄의 「따라서」가 근거로 삼는 줄 번호 찾기, 「즉」과 「한편」이 놓일 자리 갈라 적기 / 부정과 양화사 2문항 — 「모든 …은 …이다」의 부정 만들기, 두 양화사의 순서를 바꾸면 뜻이 어떻게 갈리는지 / 필요조건·충분조건 고르기 2문항과, 순환논법이 든 가짜 증명에서 그 줄 찾기

- `math-attention-formula-anatomy` (지금 4,992자 · 절 6)
  - 절「왼쪽부터 오른쪽으로 읽기」 — 소절: 식에 적히지 않은 Q·K·V의 출처 — Q=XW_Q이고 학습되는 것은 W들이라, 왼쪽 세 글자가 이미 행렬곱 세 번을 감추고 있다 / n이 두 번 쓰였지만 같은 수가 아닐 수 있다 — 크로스 어텐션과 KV 캐시에서는 Q가 n_q행, K·V가 n_kv행이라 점수 행렬이 직사각형이 된다
  - 절「식에 안 보이는 넷」 — 소절: 멀티헤드 — 식은 헤드 하나짜리이고 실제로는 d를 h로 쪼개 h번 돌린 뒤 이어 붙여 W_O를 곱한다, d_k=d/h가 √dₖ의 값을 정하는 자리 / softmax가 걸리는 축 — 코드의 dim=−1이 「행마다」에 해당하고, 축을 잘못 잡으면 열의 합이 1이 되어 오류 없이 다른 것이 계산된다 / 그림: 한 줄짜리 식 위에 감춰진 넷(W들·헤드·마스크·미분)을 얹어 표시한 그림
  - 절「기호와 글의 대응표」 — 소절: 표에 없던 자리 셋 더하기 — W_Q·W_K·W_O(선형사상), 헤드 쪼개고 다시 붙이기(셰이프 산술), 학습 때만 붙는 dropout(확률) / 표를 거꾸로 읽기 — 한 글이 여러 자리를 맡는 경우와 한 자리가 여러 글을 부르는 경우를 갈라 두면 읽는 순서가 정해진다
  - 절「연습 문제」(새 절) — 소절: 층 정규화 식을 다섯 자리로 쪼개고 자리마다 커리큘럼 번호 붙이기 / Adam 갱신식과 KL 발산 식에서 막히는 자리를 각각 세어 내고 두 지도가 겹치는 칸 찾기 / 셰이프 3문항 — 크로스 어텐션 점수 행렬의 모양, 헤드가 h개일 때 W_Q의 모양, 마스크가 더해지는 자리의 모양

- `math-rotation-and-2d-linear-maps` (지금 5,139자 · 절 7)
  - 절「2×2 행렬 셋이 원에 하는 일」 — 소절: 전단을 세 조각으로 쪼개 보기 — k=1인 전단의 특잇값이 1.618과 0.618이라 「넓이는 1인데 한 방향은 1.6배 늘고 다른 방향은 0.6배 줄었다」로 읽힌다, 마지막 절의 분해를 미리 손에 쥐여 주는 자리
  - 절「두 번 돌리면 더해지는 각」 — 소절: 곱을 여러 번 하면 직교성이 새는가 — fp64에서 R(1°)를 360번 곱하면 단위행렬에서 1.07e−14 어긋나고, 그래서 구현은 각을 누적하지 않고 위치마다 새로 계산한다
  - 절「회전행렬의 직교성」 — 소절: 반사 둘을 이으면 회전이다 — 두 직선에 대한 반사를 잇달아 하면 사잇각의 두 배만큼 도는 회전이고, 행렬식도 (−1)(−1)=1로 맞는다 / 아주 작은 각의 회전 — R(θ)≈I+θJ이고 J=[[0,−1],[1,0]]은 J²=−I라, 복소수를 안 쓰고도 「−1의 제곱근」 노릇을 하는 자리
  - 절「RoPE로 돌아가기」 — 소절: θᵢ를 정하는 식과 파장 — 가장 느린 평면이 한 바퀴 도는 데 2π·10000 ≈ 62,832 토큰이 걸려 문맥 길이 안에서는 절대 감기지 않는다 / 더하기와 돌리기의 차이 — 위치 벡터를 더하면 노름이 바뀌지만 돌리면 그대로라, 정규화 뒤에 맞춰 둔 크기 설계가 유지된다
  - 절「연습 문제」 — 소절: 반사 두 번 3문항 — x축 반사와 y=x 반사를 이어 얻는 회전의 각도, 순서를 바꾸면 무엇이 달라지는가 / det(R−λI)=0을 풀어 실수 고유벡터가 없음을 확인하고 다음 글로 넘기기 / 위치 둘을 정해 (Rq)·(Rk)를 수로 계산하고 n−m만 남는 것을 직접 확인

- `math-derivative-and-gradient` (지금 5,229자 · 절 6)
  - 절「민감도로 읽는 도함수」 — 소절: 미분되면 연속이고 거꾸로는 아니다 — |x|가 그 반례이고, 「선형근사가 있다」가 「값이 안 튄다」보다 강한 요구라는 순서
  - 절「손잡이가 여럿일 때」 — 소절: 편도함수가 다 있어도 미분 가능이 아니다 — xy/(x²+y²)는 원점에서 두 편도함수가 0인데 대각선을 따라가면 값이 0.5로 굳어 대신할 평면이 없다 / 좌표마다 단위가 다른 문제 — 임베딩과 편향과 스케일을 한 벡터에 담으면 ‖∇L‖이 무엇의 크기인지 흐려지고, 좌표마다 눈금을 다시 잡는 옵티마이저가 손대는 자리가 거기
  - 절「−∇f가 가장 가파른 내리막인 이유」 — 소절: 「가장 가파르다」는 노름을 바꾸면 답이 바뀐다 — 걸음의 길이를 L∞로 재면 최급강하 방향이 부호 벡터가 되고, 부호만 쓰는 갱신이 그 자리에 선다
  - 절「∇f가 0인 자리」(새 절) — 소절: 임계점 셋을 등고선으로 가르기 — 극소·극대·안장이 같은 「기울기 0」이다 / 차원이 커지면 안장이 압도적이 되는 어림 — d개 방향이 전부 위로 굽어야 극소라, 부호가 무작위라면 2의 d제곱분의 1 / 크기가 0에 가까운 것만으로는 고원과 극값이 안 갈린다는 것과, 그 판정에 필요한 2차 정보의 이름 / 그림: 안장점의 등고선과 그 위에서 −∇f가 그리는 경로
  - 절「연습 문제」 — 소절: xy/(x²+y²)의 원점에서 두 편도함수를 구하고 y=x를 따라간 극한과 맞대기 / 같은 그래디언트에서 L∞ 기준 최급강하 방향을 손으로 구해 L2 쪽과 비교 / 임계점 판정 2문항 — x²−y²와 x²+y²의 원점에서 무엇이 갈리는가

- `math-score-matching` (지금 5,662자 · 절 7)
  - 절「디노이징 스코어 매칭」 — 소절: 정답을 안 쓰고도 최소화하는 원래 방법 — 부분적분으로 목적식을 ½‖s‖²과 ∇·s의 합으로 바꾸면 모르는 항이 사라진다 / 그 대가는 발산 항의 비용 — 성분마다 한 번씩 역전파해야 해서 차원에 비례하고, 무작위 방향으로 투영해 그 값을 추정하면 한 번으로 줄어든다 / 잡음을 섞는 두 번째 이유 — 데이터가 저차원 다양체 위에만 있으면 그 밖에서는 스코어가 정의되지도 학습되지도 않는다, 잡음이 공간을 채워 화살표장을 잇는다
  - 절「세 목표가 하나인 이유」 — 소절: 조건부 평균을 되돌리는 그 공식에 이름이 있다 — E[x₀|x_t]=(x_t+(1−ᾱ)s)/√ᾱ 가 Tweedie 공식이고 x̂₀ 예측이 곧 그것 / 상수배가 손실에서는 가중치가 된다 — 스코어 손실과 ε 손실을 잇는 λ(t)=1−ᾱ_t를 적어 두면 「어느 t에 노력이 몰리는가」를 그 곡선으로 읽을 수 있다
  - 절「스코어로 표본 만들기」 — 소절: √(2ε)가 맞추는 비율의 정체 — 봉우리를 건너는 빈도가 두 봉우리의 높이 비에 맞춰지는 것이고, 계수를 키우면 분포가 평평해지고 줄이면 최빈값 쪽으로 쏠린다 / 이산화가 남기는 어긋남 — ε이 0이 아니면 수렴한 분포가 p에서 조금 빗나가고, 한 걸음마다 받아들일지 되돌릴지를 정해 그 어긋남을 지우는 보정이 따로 있다
  - 절「연습 문제」 — 소절: Tweedie 공식으로 x̂₀를 구해 앞의 두 공식과 세 번째로 맞대기 / 두 봉우리 밀도에서 스코어가 0인 자리 셋을 찾아 봉우리와 골짜기로 가르기 / 부분적분 한 줄로 E[s_θ·∇log p]가 −E[∇·s_θ]와 같아지는 것 확인

- `math-floating-point-and-cancellation` (지금 5,690자 · 절 7)
  - 절「비트 배치와 머신 엡실론」 — 소절: 눈금 간격에 이름 붙이기 — ULP(마지막 자리의 단위)이고, 그 이름을 세워 두면 오차를 「몇 ULP」로 말할 수 있다 / 반올림이 가는 쪽 — 정확히 가운데면 가수의 마지막 비트가 0인 쪽으로 가고, 그 규칙이 오차가 한 방향으로 쏠리는 것을 막는다 / 정수는 어디까지 정확한가 — fp32는 16,777,216까지, bf16은 256까지라 토큰 id나 카운터를 실수형에 담으면 조용히 뭉개진다 / ==를 대신할 것 — 상대 허용치와 절대 허용치가 둘 다 필요하고, 0 근처에서는 상대만으로 판정이 안 된다
  - 절「같은 16비트, 다른 고장」 — 소절: 정규수 아래의 눈금 — fp16의 가장 작은 정규수는 6.10e−5이고 그 아래 5.96e−8까지 비정규수로 버티는데, 그것을 통째로 0으로 미는 설정이 GPU에 따로 있다 / fp16 학습이 쓰는 손실 스케일링 — 손실에 큰 수를 곱해 기울기를 표현 가능한 구간으로 올리고 갱신 직전에 되나눈다, 지수가 넓은 bf16에 그 장치가 필요 없는 이유 / 8비트까지 내려가면 — E4M3의 상한이 448, E5M2가 57,344라 「범위냐 정밀도냐」의 같은 가르기가 한 단계 더 되풀이된다
  - 절「파괴적 상쇄」 — 소절: 곱셈과 나눗셈은 상쇄를 만들지 않는다 — 상대오차가 더해질 뿐이라, 자릿수를 통째로 날리는 연산은 뺄셈 하나뿐이라는 것
  - 절「더하는 순서와 흡수」 — 소절: 오차가 쌓이는 속도의 두 얼굴 — 최악은 항의 개수에 비례하지만 반올림이 고르게 흩어지면 그 제곱근에 비례하고, 정렬은 그 고름을 깨서 최악 쪽으로 민다 / 벡터 길이를 재는 자리도 같은 고장 — 성분이 256만 넘어도 fp16에서는 제곱에서 오버플로하므로 최댓값으로 나눠 두고 재는 구현이 표준이다
  - 절「두 현상의 답」 — 소절: nan을 만났을 때 짚는 순서 — nan은 자기 자신과도 같지 않다는 성질로 자리를 찾고, inf−inf와 0×inf 두 자리를 먼저 본다
  - 절「연습 문제」(새 절) — 소절: 눈금 간격 3문항 — fp32에서 간격이 1이 되기 시작하는 값, 1,024 근처의 ULP, 1에 더해도 1인 x의 상한 / e^z가 fp16 상한을 넘는 가장 작은 정수 z를 구하고 최댓값 빼기가 그 자리에서 하는 일 적기 / 같은 수 100개를 두 순서로 더해 결과가 갈리는 예를 만들고 fp64로 판정, 그리고 16,777,217이 fp32에서 무엇이 되는지

- `math-numerical-stability-patterns` (지금 5,781자 · 절 10)
  - 절「좌표를 옮겨 막는 것」 — 소절: 같은 조작의 세 번째 자리 — 벡터 길이를 잴 때 최댓값으로 나눠 두고 제곱하면 오버플로가 사라진다, 이것도 「값이 안 변하는 변환으로 안전한 구간에 옮기기」다 / 옮기면 무엇을 잃는가 — 최댓값을 찾느라 한 번 더 훑어야 해서 패스가 늘고, 그 비용을 한 번으로 줄인 것이 온라인 softmax
  - 절「식을 바꿔 막는 것」 — 소절: (log1p·expm1과 분산 두 공식·Welford를 이 절의 소절로 흡수) / 로그 공간의 뺄셈 — log(a−b)는 log a에 log1p(−e^{log b−log a})를 더해 적어야 하고, 두 값이 가까우면 여기서도 상쇄가 남는다 / 0·log0을 0으로 두는 자리 — 확률이 0인 항이 섞인 엔트로피와 KL에서 nan이 나오고, 그 예외를 담당하는 전용 함수가 따로 있다
  - 절「분모에 천장을 씌우는 ε」 — 소절: ε의 자리가 라이브러리마다 다르다 — 정규화는 제곱근 안에 넣고 Adam은 제곱근 밖에 붙이는데, 앞은 분산의 하한이고 뒤는 나눗셈 이득의 상한이라 뜻이 다르다 / 값을 고르는 눈금 — 키우면 정규화가 약해지고 줄이면 증폭이 살아나므로, 활성값의 실제 분산 범위와 그 형식의 머신 엡실론 사이에 둔다
  - 절「정밀도를 두는 자리」 — 소절: 확률적 반올림 — 갱신이 눈금보다 작아도 그 크기에 비례한 확률로 한 칸 올려 두면 기댓값이 보존되어, 형식을 안 바꾸고도 갱신이 통째로 사라지는 고장을 막는다 / 누산기를 넓혀야 하는 자리 목록 넷 — 손실 합, 정규화의 평균과 분산, 어텐션 점수의 합, 옵티마이저가 들고 있는 모멘트
  - 절「역행렬 대신 solve」 — 소절: 대칭이고 양반정치면 한 단계 더 싸다 — 콜레스키가 LU의 절반 비용이고, 앞 글의 커널 행렬과 공분산이 정확히 그 꼴 / 그래도 안 풀리면 대각선에 상수를 더한다 — 조건수가 (σ_max+λ)/(σ_min+λ)로 눌리고, 같은 한 줄이 정규화이자 수치 방어라는 두 얼굴
  - 절「일곱을 셋으로 묶기」 — 소절: (「목록으로」의 표와 「직접 짜면 안 되나」의 여섯 물음을 이 절의 소절로 흡수) / 여섯 물음에 하나를 더한다 — 「값을 비교하거나 문턱을 넘는지 보는가」, ==와 절대 허용치가 걸리는 자리 / 프레임워크를 믿을 수 없는 세 자리 — 직접 짠 커널, 손실에 덧붙인 항, 논문 수식을 그대로 옮긴 한 줄

- `math-anisotropy-and-whitening` (지금 5,852자 · 절 8)
  - 절「원점에서 재는 각」 — 소절: 이방성이 어디서 생기는가 — 자주 나오는 토큰 쪽으로 쏠린 공통 평균과 마지막 정규화가 남기는 하나의 반지름, 둘이 겹쳐 좁은 원뿔이 된다
  - 절「공분산으로 재는 쏠림」 — 소절: (원인 둘과 처방 둘의 표를 이 절의 소절로 흡수) / 재는 비용 — 공분산 한 번이면 평균 길이도 유효 차원도 같이 나오고, 768차원이면 59만 칸이라 한 번 재는 값이 싸다 / 두 지표가 어긋나는 경우 — 꼬리가 긴 스펙트럼에서는 상위 k축 누적 설명 분산비가 높은데도 유효 차원이 작게 나온다
  - 절「whitening의 세 걸음」 — 소절: 조건을 만족하는 W가 하나가 아니다 — 왼쪽에 아무 직교행렬이나 곱해도 여전히 해이고, 그중 원래 좌표에서 가장 덜 움직이는 것이 대칭 제곱근이라는 선택 / 콜레스키로 세우는 세 번째 판 — 아래삼각행렬의 역도 whitening이고, 대신 축이 좌표 순서에 묶인다 / 변환 뒤의 거리에 이름이 있다 — ‖W(x−y)‖가 마할라노비스 거리이고, 그 뒤의 코사인은 그 계량에서 잰 각이다
  - 절「4,000개로 재 본 결과」 — 소절: 질의에도 같은 W를 씌워야 한다 — 문서로 적합한 변환을 질의에 안 걸면 둘이 다른 좌표계에 놓여 순위가 무너진다 / 인덱스는 다시 세워야 한다 — 변환이 선형이라 순위 자체는 보존되지만 저장된 벡터가 통째로 바뀌므로 근사 최근접 인덱스는 새로 만든다
  - 절「치르는 대가 셋」 — 소절: 축소 추정의 눈금 — 공분산에 평균 분산짜리 단위행렬을 섞는 비율 하나가 작은 고윳값에 하한을 두는 것과 같은 일이고, 그 비율이 소표본 절벽의 안전판 / 지배 축에 신호가 있는지 미리 재는 법 — 상위 몇 축만 남긴 표현으로 recall을 한 번 재 보면 whitening을 걸기 전에 갈린다
  - 절「문턱을 다시 쓰기」 — 소절: 문턱을 표준편차 눈금으로 다시 적기 — 무작위 쌍의 평균과 표준편차를 재어 (cos−평균)/표준편차로 바꾸면 코퍼스가 바뀌어도 같은 값을 쓸 수 있다 / 문턱은 변환에 딸린 값이다 — 후처리를 고를 때마다 무작위 쌍 분포가 달라지므로 문턱도 같이 다시 잰다

- `math-kernels-and-gram-matrices` (지금 5,863자 · 절 9)
  - 절「그람 행렬과 양반정치」 — 소절: (PSD 판정 두 가지를 이 절의 소절로 흡수) / 중심화한 그람 행렬 — 양옆에 I−11ᵀ/n을 곱하면 평균을 뺀 벡터들의 그람 행렬이 되고, 앞 글의 평균 빼기를 φ를 만들지 않고 하는 방법이 그것 / 랭크가 말해 주는 것 — G의 랭크가 그 점들이 실제로 차지하는 차원이고, 표본이 차원보다 많으면 반드시 특이행렬이 된다
  - 절「커널과 특징 사상」 — 소절: (다항 커널의 φ 전개와 특징 차원 표를 이 절의 소절로 흡수) / 커널을 만드는 규칙 넷 — 합·양수배·곱·양쪽에 같은 함수 곱하기가 다시 커널이고, 지수도 급수의 계수가 전부 양수라 커널을 보존한다 / 그 규칙으로 RBF를 세 줄에 세우기 — 세 조각의 곱으로 쪼개면 급수를 펴지 않고도 커널임이 나온다 / 정규화한 커널 — 자기 자신과의 값으로 나눠 두면 특징공간에서의 코사인이 되고 대각선이 1이 된다
  - 절「RBF와 유효 랭크」 — 소절: 이동 불변이라는 성질 — RBF는 거리만 쓰므로 좌표를 통째로 옮겨도 값이 같고, 다항 커널은 원점에 매여 있어 그때 값이 바뀐다 / 무한 차원을 유한으로 되돌리기 — 무작위 방향 D개로 만든 코사인 특징이 RBF의 근사가 되어, n×n 행렬 대신 n×D로 돌아온다 / 표본이 많아지면 커널이 밀리는 자리 — 그람 행렬이 표본 수의 제곱이라 100만 개면 fp64로 8TB이고, 열 몇 개만 골라 나머지를 메우는 저랭크 근사가 그 자리의 답
  - 절「PSD를 못 넘는 함수」 — 소절: 그람 행렬만 쓰는 알고리즘이 왜 그것으로 충분한가 — 최적해가 k(xᵢ,·)들의 선형결합으로 적힌다는 대표자 정리가 있어서이고, 그 정리가 안 서는 자리가 곧 커널 트릭이 안 되는 자리
  - 절「어텐션 점수 행렬의 자리」 — 소절: M을 대칭으로 묶으면 무엇이 오는가 — W_Q와 W_K를 같게 두면 점수 행렬이 대칭이 되어 그람 행렬의 성질을 전부 얻지만 「가리키는 방향」이 사라진다, 그 맞바꿈을 표의 마지막 줄로 / 커널로 다시 읽은 어텐션 — 지수 안의 내적을 커널로 보고 특징을 근사하면 표본 수의 제곱이 표본 수로 내려간다, 선형 어텐션이 서 있는 자리
  - 절「고르는 자리 셋」 — 소절: 최소 고윳값을 재는 절차 — 대표 표본 몇백 개, 대칭 전용 루틴 한 줄, 그리고 음수가 나왔을 때 대각선에 얼마를 더할지 정하는 눈금


