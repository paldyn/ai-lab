# 리서치 계획

`src/content/articles/`의 리서치 글(카테고리 `lab-notes`·`paper-notes`·`tools`)이
무엇을 다뤄야 하는지 적어 둔다. 자동 작성 루틴이 이 파일의 `evidence`를 근거로 삼는다.

## 왜 이렇게 짰는가

지운 9편의 결함은 분량이 아니라 "직접 확인한 것이 하나도 없다"였다. 그래서 세 안 중 유일하게 자기 숫자를 실측으로 재현해 낸 run-it을 뼈대로 삼았다. 검증에서 scifact nDCG@10 0.6451, 차원 스윕 0.6370/0.6107/0.5404/0.4530/0.2960, KorQuAD 0.7867/0.9567/0.9767이 소수점 넷째 자리까지 재현됐다 — 실험대가 실재한다는 증거다. 여기에 read-papers의 대질 구조를 얹었다. 논문을 요약만 하면 다시 독후감이 되므로, CPU에서 주장을 다시 풀 수 있는 논문만 골라 정독 글 뒤에 재계산 글을 붙여 짝으로 배치했다. lab-chinchilla-recompute가 0.27초에 낸 발견(원논문 계수는 D*/N*=59.1, 재적합 계수는 18.4 — '20토큰/파라미터'가 어느 쪽에서 나오는지가 갈린다)이 이 구조의 값을 증명한다. decide-with-data에서는 근거가 전부 살아 있는 축인 가격·조항 대조 8편만 가져왔다. 세 벤더 가격 문서가 캐시 배수·배치 할인·토크나이저 +30% 문구까지 그대로 반환하는 것을 확인했고, 이는 다른 두 안에 없는 축이다. 불가·오류로 확인된 것은 전부 뺐다 — 게이트된 meta-llama·google/gemma, 존재하지 않는 HyperCLOVAX ID, 그리고 전제가 측정으로 뒤집힌 e5 접두사 글은 제목과 결론을 반대로 다시 썼다.

## 넘지 말아야 할 선

리서치에 있던 9편을 2026-08-04에 전부 지웠다. 결함은 분량이 아니라 **직접 확인한 것이
하나도 없다**는 것이었다. `eval-bias-and-toxicity`는 `return [0.1] * len(texts)  # placeholder`를
다음 줄에서 측정 결과처럼 출력했고, `eval-task-specific`의 BLEU 예제는 실제로 돌리면
0.0000이 나오는데 본문에 그 사실이 없었다.

**리서치 글은 자기 터미널 출력을 가져야 한다.** 없으면 그건 학습 글이다.

## 검증된 환경 제약

설계 단계에서 19건을 실제로 돌려 확인한 것들이다.

| 항목 | 실측 |
|---|---|
| 임베딩 모델 최초 로드 | 5~9초 |
| causal LM 최초 다운로드 | **372.6초** (Qwen2.5-0.5B, 494M 파라미터) |
| BEIR scifact 5,183문서 인코딩 | 9.5초 (all-MiniLM-L6-v2, 668문장/초) |
| KorQuAD 960문단 인코딩 | 5.1초 (multilingual-e5-small) |
| 글 한 편의 실행 시간 상한 | 5분 |

함정 넷:

1. **게이트된 repo는 토큰 없이 무조건 실패한다.** `meta-llama/*`, `google/gemma-*`는
   `OSError: gated repo`로 죽는다. 쓰지 않는다.
2. **`transformers`의 기본 sdpa 어텐션에서 `output_attentions=True`는 경고만 찍고
   빈 튜플을 조용히 반환한다.** `attn_implementation="eager"`가 필요하다. 이걸 모르면
   placeholder 사건과 같은 은폐가 그대로 재현된다.
3. **faiss-cpu + hnswlib + torch를 한 프로세스에 올리면 `OMP: Error #15`가 난다.**
   `KMP_DUPLICATE_LIB_OK=TRUE`가 필요하다.
4. **절대 시간을 결론으로 쓰지 않는다.** 위 수치는 macOS arm64 14코어에서 잰 것이고
   루틴은 리눅스 컨테이너에서 돈다. 상대 비교만 결론으로 삼는다.

## 유형별로 반드시 갖출 것

### 실험 노트 — 변수 하나를 흔들어 꺾이는 지점을 찾는다  — `lab-notes`

(1) 복붙하면 도는 재현 블록 — pip install 한 줄 + 50줄 이내 스크립트 전문 + 실행 명령. (2) 내가 실제로 돌린 터미널 출력 블록. 숫자를 손으로 옮겨 적지 않고 출력을 그대로 붙인다. (3) 실행 환경과 소요 시간 명시(CPU, 초). (4) 시드 고정 + 반복 측정의 산포. 단일 실행 숫자를 결론으로 쓰지 않는다. (5) **꺾이는 지점 한 줄** — '여기까지는 공짜, 여기서부터 손해'를 숫자로. 이게 없으면 실험이 아니라 표다. (6) 한계 절 — 코퍼스 1개·모델 1개·질의 300개라서 말할 수 없는 것. (7) 대응하는 학습 글로 가는 내부 링크. 개념 설명은 반복하지 않는다.

### 논문 축소 재현 — 핵심 주장 하나를 CPU에서 다시 재 본다  — `paper-notes`

(1) 논문 식별자(arXiv ID·연도·저자)와 **재현하려는 주장을 한 문장으로**. 논문 전체 요약이 아니라 주장 하나만 잡는다. (2) 축소 재현 코드 + 실제 출력. (3) 논문이 보고한 수치와 우리 수치를 나란히 둔 표, 그리고 차이의 원인(규모·데이터·구현). (4) **재현되지 않으면 그것을 결론으로 쓴다.** 실패도 결과이고, 실패를 감추는 순간 이 유형은 다시 독후감이 된다. (5) 축소했기 때문에 검증되지 않은 부분을 명시 — 5천 벡터로 백만 벡터 주장을 확인했다고 쓰지 않는다. (6) 논문 본문을 통째로 번역해 싣지 않는다. 주장과 수치만 인용하고 나머지는 우리 실험이다.

### 도구 비교 — 같은 자로 재서 임계값이 있는 결정 규칙을 만든다  — `tools`

(1) 최소 3개 후보를 **같은 실험대·같은 데이터**로 잰 표. 각자 공개한 벤치마크를 모아 놓는 것은 이 유형이 아니다. (2) 축은 품질뿐 아니라 속도(초당 처리량), 메모리, 디스크, 설치 시간까지 최소 4개. (3) 표 아래에 각 축의 측정 명령을 그대로 적는다. (4) **'이럴 때 A, 이럴 때 B'가 숫자 임계값으로** 적힌 결정 규칙. '상황에 따라 다르다'로 끝나면 실패다. (5) 버전 고정 — 패키지 버전, 모델 리비전, 측정 날짜. (6) 후보에서 뺀 것과 뺀 이유(게이트 repo, 크기 초과)를 실패 메시지와 함께 적는다.

## 목록 — 42편

### 실험 노트 — `lab-notes` 19편

| # | 슬러그 | 제목 | 무엇을 다루는가 | 무엇을 직접 확인하는가 |
|---:|---|---|---|---|
| 1 | `lab-retrieval-testbed` | CPU만으로 40초 만에 세우는 검색 실험대 | 이후 20여 편이 전부 올라탈 기준선을 만든다. 영어(BEIR scifact)와 한국어(KorQuAD) 두 코퍼스를 내려받아 인코딩하고 nDCG@10·Recall@k를 재는 스크립트 한 벌을 싣는다. 실험대가 믿을 만한지를 공개 수치와의 일치로 검증하는 것까지가 이 글이다. 이후 글들은 이 코드를 다시 싣지 않고 바뀐 줄만 diff로 보인다. /articles/rag-evaluation이 평가 지표의 개념을 맡으므로 이 글은 실제로 도는 코드와 그 출력만 맡는다. | 실측 재현 완료. BeIR/scifact 5,183문서·1,109질의·qrels 339에서 test 질의 300개. datasets 로드 8.4초, all-MiniLM-L6-v2 로드 8.4초, 인코딩 9.8초(530문장/초), 검색+채점 0.1초, nDCG@10 = 0.6451, 전체 33.9초. squad_kor_v1 validation 5,774행에서 중복 제거한 960문단(평균 538자), multilingual-e5-small 인코딩 4.8초, Recall@1 0.7867 / @5 0.9567 / @10 0.9767, 전체 31.1초. MiniLM의 scifact 공개 수치가 0.645 근방이라 두 자리까지 맞는다 — 이 일치가 실험대의 합격 판정이고 이후 모든 글의 신뢰 근거다. |
| 2 | `lab-embedding-dimension-cliff` | 임베딩 차원을 줄이면 검색은 어디서 무너지는가 | 384차원을 PCA로 256·128·64·32·16까지 줄이면서 nDCG@10이 어디서 꺾이는지 본다. '차원을 줄이면 성능이 떨어진다'는 방향이 아니라, 몇 차원까지가 사실상 공짜이고 어디부터 손해로 돌아서는지를 숫자로 못 박는다. /articles/ml-pca가 PCA 자체를 맡으므로 이 글은 무릎의 위치만 맡는다. | 실측 재현 완료. 스윕 전체가 0.6초(실험대 인코딩 재사용). scifact nDCG@10: 384 → 0.6451, 256 → 0.6370(98.7%), 128 → 0.6107(94.7%), 64 → 0.5404(83.8%), 32 → 0.4530(70.2%), 16 → 0.2960(45.9%). 여섯 값 전부 자릿수까지 재현됐다. 메모리는 각각 67%/33%/17%/8%/4%. 128차원이 메모리 1/3에 품질 95%를 지키는 무릎이고 64에서 처음 두 자리 손실이 난다. 같은 곡선을 KorQuAD 960문단·e5-small에서도 그려 한국어에서 무릎이 같은 자리인지 교차 확인한다. |
| 3 | `lab-int8-quantization-ranking` | int8로 줄인 벡터는 순위를 얼마나 흔드는가 — 난수로 재면 왜 틀리는가 | fp32·fp16·int8·1비트로 같은 인덱스를 만들고 top-10이 원본과 몇 개나 겹치는지 센다. 난수 벡터와 실제 임베딩에서 결과가 크게 갈리는데, 그 차이가 이 글의 핵심이다 — 벡터 벤치마크를 난수로 하면 왜 틀린 결론이 나오는지. /articles/quantization-basics는 모델 가중치 양자화를 맡고 이 글은 임베딩 벡터만 맡는다. | 실측 재현 완료. 가우시안 난수 2만×384, 200질의: fp16 recall@10 = 0.9995, 1비트 = 0.1770(둘 다 자릿수까지 재현). int8은 스케일링 방식에 따라 0.92~0.99로 갈리므로 스케일 방식을 코드에 명시하고 두 방식을 함께 낸다. 핵심 대조: 같은 코드를 scifact 실제 임베딩으로 돌리면 1비트가 0.6765(난수의 3.8배), int8이 0.9690으로 완전히 다른 결론이 나온다. 브루트포스 2만×384 검색이 0.20초라 반복 측정이 싸다. |
| 4 | `lab-binary-embedding-rescoring` | 1비트 임베딩 + 재채점, 후보 몇 개부터 본전인가 | 부호만 남긴 32배 압축 인덱스로 후보를 넓게 뽑고 원본 fp32로 다시 채점하는 2단 검색. 후보 수를 10·50·100·500으로 늘리며 원본 품질을 언제 따라잡는지, 그 대가로 지연이 얼마나 늘어나는지를 같은 표에 놓는다. 회복 곡선이 데이터 성격에 따라 갈리는 것 자체가 이 글의 발견이다. | 실측 완료. 해밍 계산은 numpy packbits로 0.1~0.4초. 실제 scifact 임베딩: 재채점 없음 0.6765 → top-50 0.9420 → top-100 0.9825 → top-500 1.0000. 같은 후보 수로 가우시안 난수에서는 0.3185/0.4155/0.6870에 그쳐 회복이 안 된다. 메모리는 384차원 fp32 1,536바이트 → 48바이트. 재채점 비용은 질의당 ms로 재고, 앞 글의 '난수로 재면 틀린다'는 결론이 여기서 한 번 더 확인된다. |
| 5 | `lab-title-prefix-effect` | 문단 앞에 제목을 붙이면 검색이 오르는가 | 청크에 문서 제목이나 섹션 헤더를 앞에 붙이는 관행이 실제로 얼마를 버는지 잰다. 두 코퍼스 모두 title 필드가 따로 있어 붙인 경우와 뺀 경우를 정확히 갈라 측정할 수 있다. /articles/rag-chunking-strategies가 전략 분류를 맡으므로 이 글은 제목 한 줄의 기여분이라는 숫자 하나만 맡는다. | 기준선 nDCG@10 0.6451은 scifact의 title과 text를 합쳐 인코딩한 값이다(실험대 코드에서 확인). title을 빼고 text만 인코딩해 같은 300질의로 재면 제목 한 줄의 기여분이 바로 나온다. 재인코딩 10초. KorQuAD도 title 필드가 있어 같은 실험을 한국어로 반복한다(재인코딩 5초, 기준 Recall@1 0.7867). 문단 길이 대비 제목 길이 비율과 이득의 상관도 함께 본다. |
| 6 | `lab-chunk-size-recall-curve` | 청크 크기와 recall — 한국어 문단 960개 실측 | 평균 538자짜리 문단을 128·256·512·1024자로 다시 잘라 넣고 Recall@1/5/10 곡선을 그린다. 청크를 줄이면 문단 수가 늘어 인덱스가 커지므로 '저장 비용 대비 recall'로 정규화해야 순위가 뒤집히는지 확인한다. /articles/rag-chunking-strategies가 전략을 맡고 이 글은 곡선만 맡는다. | 기준선 실측: 원본 538자 문단 960개에서 e5-small Recall@1 0.7867 / @5 0.9567 / @10 0.9767. 여기서 재분할해 각 크기의 Recall과 문단 수·인덱스 바이트를 함께 표로 낸다. 정답 문단이 쪼개졌을 때의 매칭 규칙(원문 문단 id 보존)을 코드에 명시해 측정이 흐려지지 않게 한다. 재인코딩은 크기당 5~15초라 전체가 1분 예산 안에 들어간다. |
| 7 | `lab-e5-prefix-ablation` | e5의 query:/passage: 접두사를 빼면 정말 손해인가 — 통념이 뒤집힌 자리 | E5 계열은 질의에 'query: ', 문서에 'passage: '를 붙이도록 학습됐고, 이걸 빼는 것은 실무에서 흔한 실수로 알려져 있다. 실제로 재 보니 방향이 달랐다 — 이 글은 그 뒤집힘을 그대로 싣고 왜 그런지를 임베딩 분포로 파고든다. 기대에 맞는 숫자가 나올 때까지 조건을 바꾸지 않는다는 규칙이 실제로 적용되는 첫 글이다. | 실측 완료(각 조건 재인코딩 5초, 전체 30초). KorQuAD 300질의 Recall@1: 접두사 정상 사용 0.7867, 양쪽 완전 생략 0.8033, 질의/문서 접두사 뒤바꿈 0.7900. 즉 생략이 오히려 @1에서 1.7%p 높다. Recall@5는 0.9567 / 0.9600 / 0.9367로 뒤바꿈만 손해다. 원 가설('생략은 큰 손실')은 이 코퍼스에서 성립하지 않는다. 임베딩 코사인 분포 변화를 함께 재서 '왜'를 연결하고, 300질의 표본에서 1.7%p가 부트스트랩 신뢰구간 안인지 밖인지까지 판정한다. |
| 8 | `lab-bm25-vs-dense-korean` | 한국어에서 BM25가 임베딩을 이기는 질의 | 형태소 분석 없이 공백 토큰화만 한 BM25가 한국어에서 어디까지 버티는지 본다. 전체 평균 비교로 끝내지 않고 BM25만 맞힌 질의를 전부 뽑아 유형을 센다 — 고유명사, 연도, 숫자, 드문 단어. 사람이 눈으로 고른 예시가 아니라 전수 분류다. /articles/rag-retrieval-strategies가 전략 소개를 맡고 이 글은 한국어 실측만 맡는다. | bm25s와 kiwipiepy 설치·구동을 확인했다(kiwipiepy init 0.83초, 한국어 형태소 분해 정상 — '한국어/형태소/분석기/로/BM/25/색인/을/만들/ᆫ다'). dense 기준선 Recall@5 0.9567. 공백 토큰화 BM25와 형태소 토큰화 BM25를 같은 300질의로 돌려 dense와 3자 비교하고, 승패가 갈린 질의를 교집합/차집합으로 자동 분류한다. 질의에 포함된 숫자·영문·고유명사 유무를 정규식으로 세어 유형별 승률표를 만든다. |
| 9 | `lab-hybrid-rrf-sweep` | RRF의 k를 흔들어 보기 — 기본값 60은 한국어에서도 맞는가 | BM25와 dense를 RRF로 합칠 때 상수 k와 가중치를 격자로 스윕해 Recall@5 표면을 그린다. 최적점이 뾰족한지 넓은 고원인지가 실무 결론을 바꾼다 — 고원이면 튜닝할 필요가 없다는 뜻이다. 앞 글에서 만든 두 순위를 그대로 재사용하므로 융합만 반복하면 된다. | k ∈ {1,10,60,200} × 가중치 11단계(0:1 ~ 1:0)로 44개 조합을 KorQuAD 300질의에 돌린다. 두 시스템의 순위는 앞 글에서 이미 계산돼 있어 융합만 반복하므로 전체가 수 초. dense 단독 기준 Recall@5 0.9567이 비교점이다. 원논문 기본값 60이 44개 중 몇 등인지, 최적점과 기본값의 차이가 부트스트랩 신뢰구간 안인지 밖인지까지 판정한다. |
| 10 | `lab-long-document-truncation` | 512토큰에서 잘리는 문서는 얼마를 잃는가 | 임베딩 모델의 최대 길이는 토큰으로 정해져 있고 한국어는 토큰을 많이 먹는다. 두 사실을 곱하면 한국어 문서가 영어보다 훨씬 자주 잘린다. 실제로 몇 %가 잘리고 그 결과 Recall이 얼마나 떨어지는지 잰다. | max_seq_length를 128/256/512로 바꿔 같은 960문단을 인코딩(각 5초 내외). 먼저 토크나이저로 문단별 토큰 수 분포를 재서 각 설정에서 잘리는 문단 비율을 낸다. multilingual-e5-small의 한국어 1.69자/토큰 실측값으로 512토큰이 몇 자인지 환산하고, 평균 538자 문단이 실제로 걸리는지 확인한다. 기준 Recall@1 0.7867 대비 하락폭을 표로 낸다. |
| 11 | `lab-normalization-bug-cost` | 정규화를 빠뜨리면 얼마나 손해인가 | 정규화·거리 함수·모델 접두사를 잘못 조합하는 네 가지 흔한 실수의 대가를 하나의 표로 만든다. 각 실수가 조용히 지나가는 이유(에러가 안 나고 결과도 그럴듯하다)와 숫자로 드러나는 크기를 나란히 놓는다. /articles/vector-similarity-metrics가 지표 선택을 맡으므로 이 글은 실수의 가격표만 맡는다. | 조합 4종 — (정규화 O, 코사인)=기준 0.6451, (정규화 X, 내적), (정규화 X, 코사인 함수), (정규화 O, L2) — 의 nDCG@10을 같은 실험대에서 낸다. normalize_embeddings=False로 다시 인코딩해 노름 분포부터 재고(문서 길이와의 상관계수), 정규화를 뺐을 때 긴 문서가 위로 올라오는 편향을 내적이 올린 문서의 평균 길이로 수치화한다. 재인코딩 10초, 나머지는 1초 미만. |
| 12 | `lab-transformer-cost-recompute` | Transformer 논문의 학습 비용을 다시 계산했다 | 앞 글에서 옮긴 학습 비용 FLOPs를 논문에 적힌 GPU 수·학습 시간·하드웨어 처리량으로 역산해 맞는지 본다. 덤으로 6ND 근사(파라미터당 6 FLOPs)를 base/big 설정에 적용해 논문값과 비교한다. 불일치가 나오면 논문이 아니라 우리 쪽 원인 후보를 먼저 나열하고, 그래도 남으면 가설로만 쓴다. | 표준 라이브러리만 쓴다(추가 설치 없음, 실행 1초 미만). 논문 §5.2의 GPU 개수·학습 스텝·step time과 Table 2의 FLOPs(3.3·10^18 / 2.3·10^19)를 입력으로 역산 스크립트를 돌리고 실제 표준출력을 붙인다. '논문값 / 재계산값 / 상대오차' 표를 내고 판정을 일치·표기차·불일치+가설 셋 중 하나로 명시한다. 파라미터 수(65M/213M)도 shape 산술로 다시 세어 대조한다. |
| 13 | `lab-chinchilla-recompute` | Chinchilla의 계수로 최적 배분을 직접 풀어 봤다 — 20토큰/파라미터는 어느 계수에서 나오는가 | 앞 두 글에서 채취한 파라메트릭 손실 함수의 계수를 넣고 컴퓨트 예산별 최적 (N*, D*)를 직접 최적화한다. 원논문 계수와 재현 논문 계수 두 벌로 각각 풀어 '20토큰/파라미터'가 어느 계수에서 나오고 어디서 흔들리는지 본다. 이 계획에서 가장 확실한 발견을 담는 글이다. | 실측 완료, 전체 0.27초(numpy+scipy). C = 6ND 제약 아래 L(N,D) = E + A/N^α + B/D^β를 최소화. Hoffmann 계수로 Gopher 예산 5.76e23에서 N* = 40.3B가 나와 논문의 40B와 일치 — 최적화 코드가 먼저 검증된다. 그런데 D*/N* = 59.1이다. Besiroglu 재적합 계수로 같은 계산을 하면 18.4. 즉 유명한 '20토큰/파라미터'는 원논문 Approach 3이 아니라 재적합 쪽에서 나온다. 추가로 논문 Table 3의 D*(4.1T)는 6·40e9·4.1e12 = 9.84e23으로 명시 예산 5.76e23과 맞지 않는다 — 우리 쪽 원인(단위, 반올림, 제약식 해석)을 먼저 배제한 과정을 쓰고 남으면 가설로만 적는다. |
| 14 | `lab-superposition-replication` | Toy Models of Superposition 재현: 희소성을 올리면 특징이 겹친다 | arXiv 2209.10652의 핵심 실험을 컨테이너에서 다시 돌린다. 은닉 차원보다 많은 특징을 압축했을 때 희소성이 오르면 특징이 겹치기 시작하는 상(相) 전이를 직접 학습시켜 확인한다. 논문의 그림을 실제로 재현한 글이 되는 것이 목적이다. /articles/ai-explainability-xai와 연결한다. | 실측 재현 완료. 전체 5.4초, 설정당 0.8~1.4초(numpy/torch CPU). 특징 20개를 은닉 5차원에 중요도 가중 MSE로 압축, Adam 4,000스텝. 희소성 0.0에서 표현된 특징 수가 정확히 5(=은닉 차원, 중첩 없음)이고 최대 off-diagonal 0.0085. 희소성을 올리면 0.5→7개, 0.9→9개, 0.99→12개로 늘고 off-diagonal이 0.93→1.13으로 커진다. 논문 그림의 상 구분과 대조하고, 장난감 모델의 결론을 실제 LLM으로 확장할 수 없다는 점을 명시한다. |
| 15 | `lab-kv-cache-formula-check` | KV 캐시 메모리 공식을 실측으로 검증한다 | 2 × layers × kv_heads × head_dim × seq × dtype 공식으로 예측한 값과, 실제로 캐시를 채워 잰 바이트 수를 대조한다. GQA가 몇 배를 줄이는지 config 값으로 계산해 확인한다. /articles/inference-kv-cache가 개념을, /articles/transformer-mqa-gqa가 GQA 구조를 맡으므로 이 글은 검산만 맡는다. | 실측 완료(캐시된 모델로 6.8초). Qwen/Qwen2.5-0.5B-Instruct: L=24, kv_heads=2, attn_heads=14, head_dim=64. seq=29에서 past_key_values 실측 712,704바이트, 공식 2·L·kv_heads·head_dim·S·4로 계산한 값도 712,704바이트 — ratio 1.0000으로 정확히 일치. GQA가 7배(14/2)를 줄이는 것도 config에서 바로 확인된다. 시퀀스 128~4096에서 (예측 MB, 실측 MB, 오차 %) 표를 내고 MHA 모델과 나란히 놓는다. 주의: causal LM 최초 다운로드가 372.6초이므로 다음 글과 같은 날에 써서 캐시를 공유한다. |
| 16 | `lab-attention-sink-probe` | 첫 토큰이 어텐션을 다 먹는다: 소형 모델에서 직접 뽑은 싱크 | 0.5B급 모델을 CPU에 올려 실제 어텐션 행렬을 꺼내, 첫 토큰이 층·헤드별로 가져가는 어텐션 질량 비율을 잰다. 한국어 입력과 영어 입력에서 싱크 강도가 다른지도 본다. /articles/transformer-self-attention이 원리를 맡으므로 이 글은 실제 가중치 측정만 맡는다. 앞 글과 같은 모델을 써서 다운로드를 한 번만 한다. | 실측 완료 — 다만 조용히 실패하는 함정 위에 있다. transformers 기본 sdpa에서 output_attentions=True를 주면 경고만 찍고 out.attentions가 빈 튜플로 온다(예외가 안 나서 그대로 쓰면 가짜 결과가 된다 — 지운 eval-bias-and-toxicity의 placeholder와 같은 종류의 은폐). attn_implementation="eager"를 주면 정상: seq=29에서 attentions 24개, shape (1,14,29,29), forward 0.39초. 마지막 토큰이 0번 토큰에 주는 어텐션 평균 0.413, 최대 1.000, 층별로 1~3층은 0.04~0.07인데 4층에서 0.579로 튄다. 프롬프트 30개·시드 고정·평균과 표준편차를 함께 적고, eager 지정 필요를 본문에 명시한다. |
| 17 | `lab-temperature-entropy` | 온도와 top-p가 실제로 자르는 것: 분포 엔트로피와 유효 어휘 수 | 실제 로짓을 꺼내 온도·top-k·top-p를 적용한 뒤 분포의 엔트로피와 누적 질량 90%를 차지하는 토큰 수를 위치별로 잰다. /articles/llm-temperature-top-k-top-p가 튜닝 가이드를 맡으므로 이 글은 분포 측정만 맡는다. 한국어와 영어 프롬프트에서 유효 어휘 수가 다르면 그 차이도 싣는다. | 실측 완료(forward 한 번이면 되므로 초 단위). 같은 로짓에서 온도별 엔트로피 / 누적 90% 유효 어휘 수 / 상위 1토큰 확률: temp=0.2 → 0.754 nats / 2개 / 0.674, temp=0.7 → 2.489 / 12개 / 0.300, temp=1.0 → 4.552 / 329개 / 0.154, temp=1.5 → 8.551 / 24,979개 / 0.031. 온도 0.7과 1.0 사이에서 유효 어휘가 27배 뛰는 구간이 눈에 보인다. 문장 시작·중간·끝 위치에서 값이 어떻게 달라지는지 위치별로 나눈다. |
| 18 | `lab-near-duplicate-news-archive` | MinHash로 우리 뉴스 아카이브의 중복 발표를 찾았다 | CLAUDE.md가 스스로 적어 둔 구멍 — 같은 발표를 두 사이트가 각각 실으면 id도 URL도 달라 아무 검사에도 안 걸린다 — 를 수치로 확인한다. 다운로드도 라이선스 문제도 없는 우리 저장소 데이터만 쓰는 유일한 글이다. /articles/data-deduplication이 개념을 맡으므로 이 글은 우리 데이터의 실제 중복 수만 맡는다. | 표준 라이브러리만 쓴다(설치 0, 네트워크 0). src/data/news.ts의 387건에 MinHash+LSH를 순수 파이썬으로 돌리고 실제 표준출력을 붙인다. n-gram 크기와 Jaccard 임계값을 바꿔 후보쌍 개수 표를 내고, 실제로 같은 발표인 쌍을 눈으로 확인해 정탐/오탐을 세어 적는다. 결과가 0쌍이면 0쌍이라고 그대로 쓴다. 387건 표본의 한계와, 이 검사를 수집 루틴에 넣을 때 쓸 임계값 근거를 함께 남긴다. |
| 19 | `lab-dimension-vs-corpus-size` | 코퍼스가 커지면 저차원은 더 빨리 무너진다 | 차원 축소의 안전선은 고정된 숫자가 아니라 코퍼스 크기의 함수다. 문서 수를 500에서 5,183까지 늘리며 각 크기에서 차원별 곡선을 다시 그려, 무릎이 오른쪽으로 밀리는지 확인한다. 앞선 글들이 낸 '128차원이 무릎'이 코퍼스 크기에 얼마나 의존하는지를 재는, 계획 전체의 자기 감사 글이다. | scifact를 500/1,000/2,500/5,183문서로 잘라(정답 문서는 항상 포함) 각각에서 384/128/64/32차원 nDCG@10을 잰다. 전체 코퍼스 기준값은 실측 0.6451/0.6107/0.5404/0.4530. 임베딩을 재사용하므로 전체가 수 초. 문서 10배당 같은 차원의 손실이 몇 배가 되는지를 기울기로 내고, 여기서 나온 규칙을 '내 코퍼스가 100만이면 몇 차원'으로 외삽할 때의 위험을 한계 절에 적는다. |

### 논문 노트 — `paper-notes` 11편

| # | 슬러그 | 제목 | 무엇을 다루는가 | 무엇을 직접 확인하는가 |
|---:|---|---|---|---|
| 20 | `paper-bm25-saturation` | BM25의 k1·b는 정말 포화하는가 — 한국어에서 기본값 검증 | BM25의 tf 포화 함수와 길이 정규화 항이 어떤 모양인지 식에서 직접 그리고, k1·b 격자를 한국어 문단에 돌려 기본값(1.2~1.5, 0.75)이 한국어에서도 최적 근처인지 확인한다. 논문의 주장을 우리 코퍼스에서 다시 재는 형태이므로 재계산이 본체다. | 앞 글에서 만든 BM25 인덱스를 재사용한다. k1 ∈ {0.5,0.9,1.2,1.5,2.0,3.0} × b ∈ {0,0.25,0.5,0.75,1.0} 30조합을 KorQuAD 300질의에 돌리면 전체 수십 초. 포화 곡선은 수식을 직접 그려 tf가 늘 때 점수가 어디서 평평해지는지 보인다. 실제 최적값과 기본값의 Recall 차이를 부트스트랩 신뢰구간과 함께 판정하고, 차이가 구간 안이면 '기본값을 바꿀 이유가 없다'를 결론으로 쓴다. |
| 21 | `paper-beir-zero-shot` | BEIR 정독: 제로샷에서 BM25가 이긴 자리, 그리고 우리 실험대의 재확인 | arXiv 2104.08663은 18개 데이터셋에서 밀집 검색과 어휘 검색을 제로샷으로 비교한 벤치마크 논문이다. '임베딩이 BM25보다 낫다'는 통념이 어떤 조건에서 깨지는지 논문 표로 확인하고, 논문이 scifact에 대해 보고한 값을 우리 실험대의 0.6451과 나란히 놓는다. /articles/rag-retrieval-strategies·/articles/rag-embedding-models가 개념을 맡으므로 이 글은 표 읽기와 대조만 맡는다. | 논문 HTML 접근 경로를 확인했다 — 2023년 이전 논문은 ar5iv.labs.arxiv.org/html/<id>, 2023년 이후는 arxiv.org/html/<id>. WebFetch의 마크다운 변환이 MathML로 렌더된 수치까지 복원하는 것도 확인했다. 모델군별 nDCG@10을 데이터셋별로 옮기고 BM25가 이긴 데이터셋 수를 직접 세어 적는다. 논문의 scifact 행과 우리 실측 0.6451을 같은 표에 놓고 모델·조건 차이를 적는다. 한국어 데이터셋이 BEIR에 있는지 없는지를 확인해 우리가 KorQuAD를 따로 세운 이유의 근거로 삼는다. |
| 22 | `paper-curse-of-dimensionality` | 고차원에서 거리는 무의미해진다는데 임베딩은 왜 멀쩡한가 | Beyer 등(1999)의 거리 집중 정리를 384차원 균등난수에서 재현해 최근접과 최원접의 차이가 실제로 사라지는 것을 보인 뒤, 같은 계산을 실제 임베딩에 돌려 왜 예외가 되는지를 내재 차원으로 설명한다. 논문 가정(독립 균등 분포)이 임베딩에서 깨지는 지점을 명시하는 것이 이 글의 목적이다. | 재현 지표는 (max_dist − min_dist)/min_dist. numpy만으로 즉시 계산된다. 384차원 균등난수 2만개에서 이 값이 0에 붙는 것을 확인하고, scifact 실제 임베딩에서 같은 값을 재서 자릿수 차이를 본다. 이유는 PCA 설명분산 95% 지점의 차원 수로 잇는다 — 128차원이 품질 94.7%(0.6107/0.6451)를 지킨 실측이 곧 내재 차원이 384보다 훨씬 낮다는 증거다. 앞선 int8 글에서 난수와 실임베딩의 recall이 3.8배 갈린 것도 같은 원인으로 설명된다. |
| 23 | `paper-rrf-original-claim` | RRF는 정말 학습 없이 이기는가 | Cormack 등(2009)의 '순위 융합이 개별 시스템과 학습 기반 융합을 이긴다'는 주장을 BM25와 dense 두 시스템으로 재현한다. 두 시스템의 품질 차가 클 때도 성립하는지가 재현의 진짜 시험대다. 재현되지 않으면 그것을 결론으로 쓴다. | 앞서 만든 BM25·dense 순위를 그대로 쓴다(dense 기준 Recall@5 0.9567). RRF 융합과 개별 시스템, 그리고 단순 점수 가중합을 같은 표에 놓는다. 그다음 dense 쪽을 일부러 저차원(32차원, 실측 nDCG 0.4530)으로 망가뜨려 품질 차를 벌린 뒤 다시 재서, 약한 시스템이 융합을 끌어내리는 지점을 찾는다. 융합 계산만 반복하므로 전체가 수 초. |
| 24 | `paper-matryoshka-representation` | 앞에서 잘라도 되는 임베딩 — Matryoshka 주장 확인 | MRL(arXiv 2205.13147)의 '앞 k차원만 써도 성능이 유지된다'는 주장을, MRL로 학습되지 않은 모델을 그냥 잘랐을 때와 비교한다. 절단 내성이 훈련으로 만들어지는 성질인지 아니면 원래 그런지를 가른다. 세 번째 열(MRL 학습 모델)은 게이트 없는 모델을 받을 수 있을 때만 채우고, 못 받으면 실패 메시지를 그대로 싣고 두 열로 결론 낸다. | MRL 미학습 모델(all-MiniLM-L6-v2)의 단순 절단 384→256/128/64를, 같은 모델의 PCA 실측값(0.6370/0.6107/0.5404)과 나란히 놓는다. 세 열 표: 단순 절단 / PCA / MRL 학습 모델. 임베딩을 재사용하므로 절단 계산은 1초 미만이고 추가 비용이 사실상 0이다. 없는 모델 ID로 재현 명령을 죽이는 일은 반복하지 않는다 — 세 번째 열의 모델은 집필 시작 전에 실제 로드를 확인하고, 실패하면 그 사실을 본문에 적는다. |
| 25 | `paper-hnsw-graph-structure` | HNSW 논문의 M-recall 관계를 5천 벡터로 확인 | Malkov & Yashunin(arXiv 1603.09320)이 보고한 연결 차수 M과 recall·메모리의 관계를 직접 격자로 재현한다. 논문은 수백만 벡터 규모라 5천 벡터에서 재현되지 않는 부분이 나오는데, 그 불일치를 결론에 남기는 것이 이 글의 목적이다. /articles/vector-ann-algorithms가 알고리즘 원리를 맡는다. | hnswlib 0.8.0 구동을 확인했다. M ∈ {4,8,16,32,64} × efConstruction ∈ {50,200} 격자를 scifact 5,183 벡터에 돌리고 각 설정의 recall@10(브루트포스 기준), 인덱스 메모리, 빌드 시간, 질의당 ms를 낸다. 실측 참고값: 5만×384에서 M=16/ef_c=100 빌드 6.4초, 200질의 k=10에 9ms. faiss 완전탐색이 5,183개에서 0.00초, 2만개에서 0.02초라 이 규모에선 ANN이 이길 자리가 없다는 것 자체가 재현 한계로 기록된다. 주의: faiss+hnswlib+torch 동거 시 OMP Error #15가 나므로 KMP_DUPLICATE_LIB_OK=TRUE를 쓰거나 프로세스를 나눈다. |
| 26 | `paper-attention-is-all-you-need` | Attention Is All You Need 정독: 표에 적힌 수치와 적히지 않은 조건 | 리서치 섹션에서 논문을 표까지 읽는 첫 글. 구조 설명은 /articles/transformer-basics와 /articles/transformer-self-attention으로 넘기고, 이 글은 WMT14 실험 설계·base와 big의 하이퍼파라미터·ablation이 무엇을 보여 주고 무엇을 안 보여 주는지에 집중한다. 논문에서 옮긴 모든 수치에 표 번호를 단다. | ar5iv.labs.arxiv.org/html/1706.03762 접근을 확인했고 표 수치를 실제로 뽑았다. Table 2: base EN-DE 27.3 / EN-FR 38.1 / 학습비용 3.3·10^18, big 28.4 / 41.8 / 2.3·10^19. Table 3 (A)행: h=1 PPL 5.29 BLEU 24.9, h=4 5.00/25.5, h=16 4.91/25.8, h=32 5.01/25.4. 중요한 정정 — MathML로 렌더된 3.3·10^18은 raw HTML을 grep하면 안 걸리지만 WebFetch의 마크다운 변환은 복원한다. 초록의 28.4/41.8이 본문 표의 어느 행인지 대조한 결과를 표로 낸다. |
| 27 | `paper-chinchilla-compute-optimal` | Chinchilla 정독: 20토큰/파라미터는 어디서 나왔는가 | arXiv 2203.15556. 세 가지 접근법(고정 모델 크기, IsoFLOP, 파라메트릭 손실 적합)이 각각 어떤 최적 배분을 내놓았는지와 세 결과가 서로 얼마나 일치했는지를 본다. 파라메트릭 적합의 계수를 정확히 채취해 두 글 뒤 재계산의 입력으로 넘긴다. /articles/llm-scaling-laws가 결론 소개를 맡으므로 이 글은 계수가 나온 자리만 맡는다. | ar5iv.labs.arxiv.org/html/2203.15556 접근을 확인했고 수치를 뽑았다. Gopher 예산에서 세 접근법의 최적점: Approach 1 → 70B·1.5T, Approach 2 → 67B·1.7T, Approach 3 → 40B·4.1T. Approach 3 계수: E=1.6934, A=406.4, B=410.7, α=0.3392, β=0.2849(신뢰구간 없음). 주의: arxiv.org/abs/2203.15556에는 HTML 전문 링크가 없으므로(2022년 논문) 반드시 ar5iv 경로를 쓴다. 세 접근법이 4.1T와 1.5T로 2.7배 갈린 것 자체가 이 글의 관찰 지점이다. |
| 28 | `paper-chinchilla-replication` | Chinchilla 재현 시도 논문이 찾아낸 불일치 | 대질 노트. arXiv 2404.10102는 Chinchilla의 파라메트릭 적합을 원논문 그림에서 데이터를 복원해 다시 맞춰 본 논문이다. 재적합 결과가 원논문 계수와 어디서 갈렸는지, 원논문에 신뢰구간이 없는 것이 왜 문제로 지적됐는지를 원문 그대로 확인한다. | arxiv.org/html/2404.10102v2 접근을 확인했고 Table 1에서 양쪽 계수를 다 뽑았다. Hoffmann Approach 3: L = 1.6934 + 406.4/N^0.3392 + 410.7/D^0.2849(신뢰구간 없음). Besiroglu 재적합: 1.8172(0.03) + 482.01(124.58)/N^0.3478(0.02) + 2085.43(1293.23)/D^0.3658(0.02), 240개 점 4,000회 부트스트랩. B의 신뢰구간이 추정값보다 넓은 것(2085 ± 1293)이 이 대질의 핵심 관찰이다. 재현 논문이 제기한 문제를 셋 이내로 정리하고 각각이 어느 절의 주장인지 지목한다. |
| 29 | `paper-gsm1k-contamination` | GSM1k 논문 정독: 새로 만든 문제에서 점수가 떨어진 모델들 | arXiv 2405.00332. GSM8K와 같은 분포로 새 문제 1,000개를 만들어 재본 논문이다. 어떤 모델군이 얼마나 떨어졌고 어떤 모델은 안 떨어졌는지, 저자가 오염을 어떻게 추정했는지 확인한다. 예전 eval 글들이 남의 벤치마크 수치를 옮기다 무너진 자리를, 벤치마크 수치를 의심하는 논문으로 대신한다. | arxiv.org/html/2405.00332v3이 Table 1을 정상 렌더하는 것을 확인했다. 모델별 GSM8K 정확도와 GSM1k 정확도, 그 차이를 표에서 옮기고 하락폭 순으로 정렬한다. 저자의 데이터 생성·검수 절차와 오염 지표(생성 확률 기반)를 확인한다. '하락 = 부정행위'가 아니라는 저자의 유보를 인용하고, 논문 시점 모델만 다뤘다는 한계를 적는다. 하락폭 분포를 우리가 직접 다시 계산해 논문의 서술과 대조하는 것이 재계산 부분이다. |
| 30 | `paper-judge-agreement-recompute` | LLM 심판의 일치도를 공개 인간 평가 데이터로 다시 계산한다 | 공개된 인간 선호 판정 데이터로 심판 일치도를 직접 계산한다. 단순 일치율 대신 우연 일치를 보정한 값과, 무승부 처리 방식·순서 뒤집기에 따라 숫자가 얼마나 달라지는지 보인다. 지운 eval-llm-as-judge가 하지 못한 자리가 정확히 여기다. /articles/prompt-evaluation·/articles/llmops-eval-pipelines가 파이프라인을 맡는다. | 데이터 접근을 확인했다. lmsys/mt_bench_human_judgments가 10.6초에 로드되고 human 3,355건 / gpt4_pair 2,400건, 컬럼은 question_id·model_a·model_b·winner·judge·conversation_a·conversation_b·turn. 판정이 데이터 안에 이미 들어 있어 LLM 호출이 전혀 필요 없다. 일치율·코헨 카파·위치 편향(순서를 바꿨을 때 판정이 뒤집히는 비율)을 직접 계산하고 95% 부트스트랩 구간을 붙인다. 무승부를 버릴 때와 절반으로 셀 때 숫자가 얼마나 벌어지는지 대조하는 것이 이 글의 핵심 표다. |

### 도구 비교 — `tools` 12편

| # | 슬러그 | 제목 | 무엇을 다루는가 | 무엇을 직접 확인하는가 |
|---:|---|---|---|---|
| 31 | `cost-korean-token-tax` | 한국어 토큰세: 같은 문장이 토크나이저마다 몇 배로 갈리는가 | 게이트 없이 받을 수 있는 토크나이저만 모아 한국어 문서 세 종류(설명문·한국어 주석 코드·수식 섞인 글)로 자/토큰 표를 만든다. 어휘가 클수록 한국어에 유리하다는 통념이 어디서 깨지는지 보고, 이 비율을 이후 cost- 글 세 편이 그대로 재사용한다. /articles/tokenizer-bpe·/articles/tokenizer-tiktoken이 알고리즘을 맡으므로 이 글은 측정과 비용 함의만 맡는다. | 토크나이저 접근 실측 완료. 성공(1.5~4.6초): cl100k_base 한국어 1.05자/토큰, o200k_base 1.72, Qwen/Qwen2.5-7B-Instruct 1.34, mistralai/Mistral-7B-Instruct-v0.3 0.90, deepseek-ai/DeepSeek-V3 1.39, skt/A.X-4.0-Light 2.46, klue/roberta-base 1.79, bert-base-multilingual-cased 1.59, intfloat/multilingual-e5-small 1.69. 실패: meta-llama/Llama-3.1-8B-Instruct와 google/gemma-2-9b-it는 gated repo 오류 — 실패 메시지를 그대로 싣는다. naver-hyperclovax/HyperCLOVAX-SEED-Text-Base-1.5B는 존재하지 않는 ID(`is not a valid model identifier`)이므로 쓰지 않는다. 어휘 32,000의 klue/roberta가 어휘 151,000급보다 한국어에서 나은 것이 통념이 깨지는 자리다. |
| 32 | `cost-price-per-work-not-per-token` | MTok 단가로는 못 고른다: 단가 × 토큰 팽창률로 다시 계산한 실비 | 세 벤더 가격 문서에서 단가표를 받아 앞 글의 한국어 팽창률을 곱해 '한국어 문서 1만 건 처리' 실비를 원화까지 낸다. 단가가 더 싼 모델이 한국어에서 더 비싸지는 자리를 지목한다. /articles/project-cost-optimization·/articles/serving-cost-optimization이 절감 기법을 맡으므로 이 글은 청구서 계산만 맡는다. | 가격 문서 세 곳 전부 완전한 수치 표를 반환하는 것을 확인했다. platform.claude.com/docs/en/about-claude/pricing(모델별 입출력 단가 전체), ai.google.dev/gemini-api/docs/pricing(200k 토큰 경계로 갈리는 티어 가격, 임베딩 $0.15~0.20/MTok), developers.openai.com/api/docs/pricing(표준·배치 표). 함정 둘도 본문에 적는다 — claude.com/pricing은 JS 렌더라 API 단가가 안 나오고, www.anthropic.com/pricing과 platform.openai.com/docs/pricing은 301로 호스트가 바뀐다. Anthropic 문서의 '4.7 이후 모델은 같은 텍스트에 약 30% 더 많은 토큰을 만드는 새 토크나이저를 쓴다'는 문장을 인용해 단가와 팽창률을 분리한다. 표 머리에 취득일 필수. |
| 33 | `spec-sdk-defaults` | SDK 기본값 실측: 타임아웃·재시도는 무엇으로 잡혀 있는가 | openai·anthropic·google-genai 파이썬 SDK를 설치해 클라이언트 기본 타임아웃, 최대 재시도 횟수, 재시도 대상 상태 코드, 백오프 방식, 스트리밍 기본값을 소스와 시그니처에서 직접 뽑아 표로 만든다. /articles/openai-sdk·/articles/anthropic-sdk·/articles/gemini-sdk가 사용법을 맡으므로 이 글은 기본값 대조만 맡는다. 문서에 적힌 값과 코드의 값이 어긋나면 양쪽을 나란히 싣고 어긋남 자체를 결론에 넣는다. | API 키 없이 성립하는 것을 확인했다. openai 2.53.0, anthropic 0.120.2, google-genai 2.16.0 설치 후 introspection으로 확인 — 앞 두 SDK 모두 DEFAULT_TIMEOUT = Timeout(connect=5.0, read=600, write=600, pool=600), DEFAULT_MAX_RETRIES = 2로 동일하게 잡혀 있다. 네트워크도 키도 필요 없어 계획 전체에서 실행 위험이 가장 낮은 항목이다. 재현 스크립트는 inspect.signature와 모듈 상수를 출력하는 20줄이면 된다. |
| 34 | `cost-prompt-cache-breakeven` | 프롬프트 캐시는 몇 번째 호출부터 이득인가: 3사 규칙으로 푼 손익분기 | 캐시 쓰기 배수·읽기 배수·TTL·최소 토큰 조건을 세 벤더 문서에서 뽑아 같은 표에 놓고, 손익분기 읽기 횟수를 닫힌 식으로 유도한다. 실제 시스템 프롬프트를 토크나이저로 재서 대입해 월 절감액까지 낸다. /articles/llmops-cache가 캐시 계층 설계를 맡으므로 이 글은 벤더 과금 규칙의 산수만 맡는다. | Anthropic 문서에서 5분 쓰기 1.25x, 1시간 쓰기 2x, 읽기 0.1x를 확인했다. 부등식 (w + 0.1k) < (k + 1)을 풀면 5분 캐시는 읽기 1회, 1시간 캐시는 읽기 2회부터 이득 — 유도 과정을 싣고 문서 서술과 대조한다. 도구별 시스템 프롬프트 토큰 수 표도 같은 문서에 있다. OpenAI·Gemini의 캐시 조건(자동/명시, 최소 토큰 수, TTL)을 조항 인용으로 채우고, 근거를 못 대는 칸은 '문서에 없음'으로 비운다. 우리 RAG 시스템 프롬프트의 실제 토큰 수는 tiktoken으로 직접 잰다. |
| 35 | `spec-context-window-conditions` | 컨텍스트 창 숫자에 붙어 있는 조건들 | 1M 같은 숫자가 어떤 조건에서 유효한지 — 베타 헤더, 티어 제한, 장문 구간 할증, 출력 토큰 상한, 캐시·배치와의 상호작용 — 를 조항 인용으로 대조한다. 출력 상한이 입력 상한보다 훨씬 작다는 점을 실제 숫자로 대조하는 것이 결론이다. /articles/llm-context-window가 개념을 맡으므로 이 글은 계약 조건만 맡는다. | Anthropic 문서에서 '4.6 이후 모델은 1M 창을 표준 가격으로 포함하며 900k 요청도 9k와 같은 단가'를 확인했다. 반면 Gemini는 2.5/3.1 Pro 입력 단가가 200k 토큰 경계에서 갈린다(3.1 Pro $2/$4). 세 벤더의 입력 상한·출력 상한·경계 할증·필요 헤더를 표로 만들고 각 칸에 URL과 확인일을 단다. 1번 글의 한국어 팽창률을 곱해 '이 창이 한국어로 몇 자인가'를 함께 낸다. |
| 36 | `cost-batch-and-tier-discounts` | 배치 50% 할인을 못 받는 자리 찾기: 티어·모드별 조건 대조 | 배치 API, 저지연 모드, 데이터 레지던시 할증을 세 벤더에서 대조한다. 할인율만이 아니라 '언제 못 쓰는가'(다른 기능과 배타, 완료 지연 상한, 스트리밍 불가)를 조항으로 같이 표에 넣어 할인이 실제로 적용되는 워크로드를 가른다. 앞 글의 캐시 배수와 곱셈 순서가 어떻게 되는지 검산까지 한다. | Anthropic 문서에서 확인 — 배치 입출력 모두 50% 할인, Fast mode는 배치와 병용 불가, inference_geo="us"는 모든 토큰 항목에 1.1x, 관리형 에이전트 세션에는 배치·Fast·레지던시 배수가 전부 적용되지 않고 세션 런타임이 $0.08/시간. OpenAI·Gemini의 대응 조항(배치 50%, 무료 티어 한도)을 같은 표에 조항 인용으로 채운다. 산출: (모드, 할인율, 배타 조건, 지연 상한, 적용 가능 워크로드) 표. |
| 37 | `spec-model-deprecation-calendar` | 지금 쓰는 모델은 언제 사라지는가: 은퇴 일정과 이전 비용 | 세 벤더의 모델 폐기·은퇴 문서를 대조해 달력을 만들고, 후속 모델로 옮길 때 단가와 토크나이저 변화로 생기는 비용 차이를 계산한다. 선택 기준에 '이 모델이 얼마나 오래 살아 있는가'를 수치로 넣는다. 확인일 없는 표는 몇 달 뒤 거짓말이 되므로 표 머리에 확인일을 크게 박는다. | Anthropic 문서에서 상태 표시를 확인했다 — Opus 4.1 deprecated, Opus 4·Sonnet 4·Haiku 3.5는 일부 클라우드 외 retired, Sonnet 5는 2026-08-31까지 도입가 $2/$10이고 09-01부터 $3/$15로 오른다. 이런 날짜와 상태를 세 벤더에서 모아 달력 표로 만들고, 각 이전 경로마다 앞선 cost- 글의 팽창률과 단가를 곱해 '같은 워크로드의 월 비용이 얼마나 변하는가'를 낸다. |
| 38 | `spec-open-model-licenses` | 오픈 가중치 모델 라이선스 대조: 어디까지 상업적으로 쓸 수 있는가 | 토큰 없이 원문을 받을 수 있는 모델의 라이선스만 골라 상업 이용 범위, 사용자 수 임계, 파생 모델 명명 의무, 출력물로 다른 모델을 학습시키는 것의 허용 여부, 재배포 조건을 조항 단위로 대조한다. 접근 자체가 막힌 모델은 그 사실을 표의 한 열로 만든다. 법률 자문이 아님을 명시한다. | 접근성을 실측했다. 토큰 없이 열림: Qwen/*, deepseek-ai/*, mistralai/Mistral-7B-Instruct-v0.3, skt/A.X-4.0-Light, openai/gpt-oss-*. 토큰 없이 실패: meta-llama/*, google/gemma-* (gated repo 오류). 존재하지 않는 ID로 확인된 것: naver-hyperclovax/HyperCLOVAX-SEED-Text-Base-1.5B — 표에 넣지 않는다. LGAI-EXAONE는 trust_remote_code가 필요하고 최신 transformers에서 config 로딩이 깨진다. 라이선스 원문은 통째 번역하지 않고 조항 번호와 함께 요지만 인용한다. |
| 39 | `bench-korean-embedding-models` | 한국어 임베딩 모델 CPU 실측: 크기·처리량·STS 상관 | CPU에서 돌릴 수 있는 한국어 지원 임베딩 모델만 골라 차원·파일 크기·문장당 인코딩 시간·한국어 STS 상관을 한 표에 놓고, 지연 예산별로 무엇을 고를지 숫자 임계값으로 규칙을 만든다. /articles/rag-embedding-models가 모델 소개를 맡으므로 이 글은 측정치와 선택 규칙만 맡는다. 후보에서 뺀 것과 뺀 이유(게이트, 크기 초과)를 실패 메시지와 함께 적는다. | 평가 데이터 접근을 확인했다 — klue/klue의 sts 설정이 8.5초에 로드(train 11,668 / validation 519), mteb/sts17-crosslingual-sts의 ko-ko가 6.1초에 로드(test 2,846). 인코딩 기준선도 확보: multilingual-e5-small이 CPU에서 960문단을 4.8초에 인코딩(384차원), all-MiniLM-L6-v2가 scifact 5,183문서를 9.8초(530문장/초). 여기에 multilingual-e5-base, paraphrase-multilingual-MiniLM, bge-m3, ko-sroberta 계열을 더한다. 축은 한국어 STS 스피어만 / 검색 품질 / 문장초 / 디스크 MB / 최대 시퀀스 5개. 모델 최초 다운로드 시간도 표에 기록한다. |
| 40 | `bench-chunkers` | 청크 분할기 실측: 길이 분포·문장 절단률·한국어 경계 오류 | LangChain RecursiveCharacterTextSplitter, LlamaIndex SentenceSplitter, 토큰 기반 분할, 규칙 기반 분할을 같은 한국어 문서에 걸어 청크 길이 분포, 문장 중간에서 잘린 비율, 표·코드 블록 파손율, 속도를 잰다. chunk_size를 문자로 지정했을 때 실제 토큰 수가 얼마나 튀는지가 이 글의 핵심 수치다. /articles/rag-chunking-strategies가 전략 분류를 맡고 이 글은 구현체 실측만 맡는다. | 청크 길이 히스토그램을 문자 축과 토큰 축 둘 다로 낸다 — 한국어 팽창률(cl100k 1.05자/토큰, o200k 1.72) 때문에 두 축이 크게 어긋나고, 그것이 chunk_size를 문자로 지정하는 관행의 실제 위험이다. 문장 절단률 %, 처리 속도, 그리고 같은 청크로 만든 인덱스의 Recall 차이(기준 0.7867)를 함께 낸다. 라이브러리 버전을 표에 고정하고, 설치가 실패하는 항목은 실패 메시지와 함께 표에 남긴다. |
| 41 | `bench-ann-recall-qps-tradeoff` | 근사 최근접 인덱스의 recall-지연 지도 — 그리고 난수 벤치마크의 함정 | 브루트포스를 기준선으로 두고 HNSW와 IVF의 파라미터 격자를 돌려 recall@10 대 질의당 ms를 지도로 그린다. 같은 파라미터가 난수 코퍼스와 실제 임베딩에서 완전히 다른 recall을 내는 것이 이 글의 결론이고, '벤치마크 데이터가 결과를 좌우한다'가 그 한 줄이다. | 실측 확인. 난수 2만개에서 HNSW M=8/efS=50이 recall@10 0.5855인데, 같은 설정이 실제 scifact 5,183개에서는 0.9920이다. faiss-cpu 1.15.0 + hnswlib 0.8.0 설치 23초, IndexFlatIP 완전탐색이 5,183개 0.00초·2만개 0.02초·5만개 0.09초. 파라미터 격자 × (recall@10, QPS, 빌드 초, 인덱스 MB) 표를 두 코퍼스에서 각각 내고 나란히 놓는다. 코퍼스 크기를 늘려 가며 ANN이 브루트포스를 이기기 시작하는 교차점도 찾는다. OMP Error #15 회피를 환경 블록에 명시한다. |
| 42 | `bench-retrieval-eval-metrics` | Recall@k·MRR·nDCG는 언제 서로 다른 결론을 내는가 — 그리고 앞선 글들의 소급 감사 | 앞선 글들이 쓴 지표들을 마지막에 검증한다. 같은 실행 결과에 세 지표를 다 적용해 순위가 뒤바뀌는 사례를 찾고, 부트스트랩으로 '이 정도 차이는 우연'의 경계를 숫자로 정한다. 그다음 그 최소 유의 차이를 앞선 글들에 소급 적용해 결론이 흔들리는 글이 있으면 그것도 적는다. /articles/ml-ranking-metrics가 지표 정의를 맡으므로 이 글은 판정만 맡는다. | 이미 저장해 둔 실행 결과들(384/256/128/64/32차원, BM25, dense, 하이브리드)에 Recall@1/5/10·MRR·nDCG@10을 전부 적용해 순위 역전 사례를 전수 탐색한다. 질의 300개에서 부트스트랩 1,000회로 각 값의 95% 신뢰구간을 내면 0.6451 대 0.6370(256차원) 같은 차이가 구간 안인지 밖인지 판정된다. e5 접두사 글의 1.7%p(0.7867 대 0.8033)가 특히 이 판정 대상이다. 계산만 하므로 수 초. |

## 뺀 것

## 뺀 것과 이유

**게이트·부재 ID 전부.** meta-llama/*, google/gemma-*는 토큰 없이 무조건 `OSError: gated repo`. naver-hyperclovax/HyperCLOVAX-SEED-Text-Base-1.5B는 게이트가 아니라 존재하지 않는 ID였다(decide-with-data 계획서 안에 이미 들어 있던 결함 — 지운 eval-llm-benchmarks를 죽인 것과 같은 종류다). LGAI-EXAONE는 trust_remote_code 필요 + config 로딩 깨짐. 전부 제외하고 토큰 없이 열리는 것만 남겼다.

**causal LM 의존 글 대부분.** Qwen2.5-0.5B 최초 다운로드가 372.6초다. probe 3편만 남기고 같은 날에 몰아 캐시를 공유하게 배치했다. 투기적 디코딩 수용률 실측은 모델 두 개가 필요해 최초 실행 10분 이상이 다운로드로만 나가므로 뺐다.

**read-papers의 논문 24편 중 14편.** 정독만으로 끝나는 글(FlashAttention, Sleeper Agents, RoPE, InstructGPT, DPO, RULER, Lost in the Middle 등)은 CPU 재계산이 안 붙는다. 재계산 없는 논문 노트는 학습 글이지 리서치 글이 아니다 — 이 판정이 지운 9편과 이 계획을 가르는 선이다. 남긴 11편은 전부 주장 하나를 CPU에서 다시 풀 수 있는 것이다.

**decide-with-data의 tools 24편 중 16편.** probe 5편은 sdpa에서 output_attentions가 빈 튜플을 조용히 반환하는 함정 위에 있어 3편으로 줄이고 eager 지정을 brief에 못 박았다. bench-gguf-quant-cpu(llama.cpp 빌드+양자화+퍼플렉시티)는 세션 초과 위험이 가장 커서 뺐다. bench-pdf-extractors, bench-local-vector-stores, spec-structured-output-matrix, spec-rate-limits, spec-data-retention은 근거는 되지만 검색 실험대와 연결이 없어 섹션이 산만해진다.

**lab-e5-prefix-ablation은 전제가 뒤집혀 다시 썼다.** 측정 결과 접두사 생략이 Recall@1 0.7867 → 0.8033으로 오히려 높다. 원 brief의 "손실이 큰 흔한 실수"는 거짓이므로 제목과 결론을 "빼도 거의 안 잃는다, 뒤바꾸면 잃는다"로 반대로 세웠다.

## 집필 중에 계획이 틀린 것으로 확인된 것

**설계 단계의 KorQuAD 수치는 전부 300질의 표본이다.** 2026-08-13에 7·8번을 쓰면서 질의 5,774개 전부로 다시 재니 기준선이 달라졌다 — dense(e5-small, 접두사 정상) Recall@1 0.7785 / @5 0.9409 / @10 0.9719이고 MRR@10 0.8507이다. 위 표의 0.7867 / 0.9567 / 0.9767은 그 5,774개에서 뽑은 300개짜리 표본값이다. **이 코퍼스에서 질의 300개의 R@1 신뢰구간 반폭이 2.67%p이고 5,774개에서도 0.87%p다.** 그러니 300질의로 잰 1~2%p짜리 차이는 표에 적혀 있어도 근거가 아니다. 9·20·23·42번이 재사용할 기준선은 5,774질의 쪽이다.

**7번의 "통념이 뒤집힌 자리"는 재현되지 않았다.** 5,774질의에서 접두사 양쪽 생략은 R@1 −0.36%p(신뢰구간 [−0.90, +0.21])로 판정 불가이고, 설계 단계의 +1.7%p는 300개짜리 표본 20,000회 재표집에서 5.6%의 확률로 나오는 값이었다. 실재하는 손해는 **질의 쪽에 `passage: `를 붙인 세 조합뿐**(−1.40 ~ −3.12%p)이다. 제목을 「뒤집힘도 손해도 300질의가 만든 것이었다」로 다시 세웠다.

**8번의 "BM25가 이기는 질의"는 "BM25가 대체로 이긴다"였다.** 형태소(kiwi) BM25가 R@1 0.8914, 문자 2-gram이 0.8961로 dense 0.7785를 11%p 이상 앞선다. 공백 토큰화만 0.7503으로 dense에 진다 — 통념의 근거가 토큰화였다. 승패를 가르는 축은 숫자·연도·고유명사가 아니라 질의와 정답 문단의 형태소 겹침이고 경계가 0.6이다(그 아래 6.2%에서만 dense가 이긴다). KorQuAD 질문이 문단을 읽고 만들어져 겹침 평균이 0.770이라는 점이 이 결론의 최대 한계이며 본문에 명시했다. 9·20·23번이 재사용할 BM25 순위는 `bm25_{whitespace,kiwi-morph,char-2gram}.npy`로 남는다.

**arXiv HTML에서 표를 긁을 때의 함정 하나.** LaTeXML은 캡션을 표 **앞**에 놓는다. 그래서 「Table 2: Alternative Prompt」 같은 캡션 문자열을 찾아 그 앞의 `<table>`을 잡으면 한 칸 밀린 표가 잡힌다. 29번을 쓰면서 실제로 표 두 장을 서로 바꿔 읽을 뻔했다 — 두 표는 행 이름이 같고 숫자만 달라서 **바뀌어도 눈으로는 안 걸린다.** 기준점은 캡션 문자열이 아니라 LaTeXML이 붙인 id를 쓴다(`id="A4.T1.4"` 꼴, `A4`는 부록 D). 그리고 캡션에는 `<span>`이 섞여 들어가 원문에 리터럴 문자열로 존재하지 않는 경우가 있어 `raw.index("Table 2: Alternative Prompt")` 자체가 `ValueError`로 죽는다.

**bm25s의 함정 하나.** `BM25.index()`는 넘겨받은 vocab 딕셔너리에 빈 문자열 항목을 제자리에서 추가한다. 색인 뒤에 `len(vocab)`을 세면 실제 어휘보다 1이 크고, 그 id를 질의에 넣으면 `maximum token ID in the query is higher than the number of tokens in the index`로 죽는다. 어휘 수는 색인 전에 센다.

## 이 계획을 실행하기 전에 고쳐야 할 것

**리서치 접두사 다섯의 대응은 CLAUDE.md의 산문이 들고 있다.** 2026-08-05 카테고리 개편으로 접두사 표가 학습 여덟 칸 전용이 되면서 `ai-guide`도 잔여 칸이 아니게 됐다 — 이제 `lab-`·`paper-`·`cost-`·`spec-`·`bench-`는 표에 걸리지 않고, 리서치 항목 산문의 대응을 따른다.

```
| `lab-` | lab-notes | 실험대 위에서 직접 돌린 측정 |
| `paper-` | paper-notes | 논문 주장을 CPU에서 다시 푼 것 |
| `cost-` | tools | 벤더 가격 문서 × 직접 센 토큰 |
| `spec-` | tools | 문서 조항·SDK 기본값 대조 |
| `bench-` | tools | 같은 자로 잰 후보 비교 |
```

`eval-`은 학습(`ml-ops`)으로 남는다 — 리서치로 보내던 6편이 2026-08-04에 삭제된 것들이다. 이 다섯 접두사를 쓰는 학습 글은 하나도 없어 충돌이 없다.

**author는 `PALDYN Team`으로 통일한다.** 지운 글은 `Paldyn Papers`를 썼는데 나머지 296편은 전부 `PALDYN Team`이다.

## 검증이 남긴 경고 넷 (루틴 프롬프트에 전부 반영함)

1. faiss + hnswlib + torch를 한 프로세스에 올리면 `OMP: Error #15`로 즉사한다(macOS 실측, 리눅스는 다를 수 있음). `KMP_DUPLICATE_LIB_OK=TRUE`로 우회.
2. transformers 기본 sdpa에서 `output_attentions=True`는 경고만 찍고 빈 튜플을 반환한다. `attn_implementation="eager"` 필수 — 이걸 모르고 쓰면 placeholder 사건과 같은 조용한 은폐가 된다.
3. 리눅스 pypi torch는 CUDA 의존을 끌어와 2GB를 넘는다. `--index-url https://download.pytorch.org/whl/cpu` 필수.
4. 절대 시간은 환경 종속이다. 검증은 macOS arm64 14코어에서 쟀고 루틴은 리눅스 컨테이너에서 돈다. 결론은 비율과 순위로만 낸다.

## 이 계획으로도 못 막는 것

`npm test`는 frontmatter·내부 링크·에셋·색 대비만 본다. **출력 블록에 손으로 쓴 가짜 숫자가 있어도 통과한다.** 정직함을 강제하는 것은 루틴 프롬프트의 자기검사 절차뿐이고, 그 절차를 지키는 주체는 매일 글을 쓰는 루틴 자신이다. 더 단단히 하려면 본문 코드 펜스를 실제로 실행해 보는 검사를 별도로 두어야 하는데, 그건 이 설계 밖의 과제다.
