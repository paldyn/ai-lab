# AI 가이드 — `/playbook` 계획

> **2026-09-16 저녁 — 서랍을 비우고 다시 세우는 중이다.**
> 이름이 「활용 가이드」에서 **「AI 가이드」**로 바뀌었고, 도구를 평평하게 여섯 두던
> 구조 위에 **기업 층**이 들어간다(Anthropic · OpenAI · Google → 그 아래 제품).
> 노트 8편·주장 55건·확인 로그를 전부 지웠다 — 커밋 `ed4580f`·`11e3ec8`에 남아 있다.
>
> **뼈대는 섰다**(아래 「새 뼈대」). 본문의 나머지 상당 부분은 아직 옛 구조를 전제하고
> 쓰여 있다 — 내용이 돌아오면서 차례로 다시 쓴다.
>
> ## 새 뼈대
>
> | 층 | 어디에 | 지금 |
> | --- | --- | --- |
> | 기업 | `src/data/guideVendors.ts` | Anthropic · OpenAI · Google |
> | 제품 | `src/data/guideProducts.ts` | 9개 |
> | 모델 | `src/data/guideModels.ts` | 23개 — 이름과 id만, 제품이 참조한다 |
>
> 원고 층은 **없다.** 2026-09-17에 걷어냈다 — 아래 「여기에는 글이 없다」를 본다.
>
> ```
>            챗           업무              코딩
> Anthropic  Claude       Claude Cowork     Claude Code
> OpenAI     ChatGPT      ChatGPT Work      Codex
> Google     Gemini app   (없음)            Google Antigravity · Gemini CLI
> ```
>
> **격자로 짜지 않았다.** Google에는 업무가 없고 코딩이 둘이다. 갈래를 머리글로 두면
> 한 갈래에 하나인 자리마다 카드가 전폭을 먹어 목록이 세로로 늘어졌다(처음 세웠을 때
> 2,628px). 갈래를 **카드 꼬리표**로 내리고 제품을 한 줄로 흘려서 1,574px이 됐다 —
> 회사마다 한 줄이라 회사끼리 견주기도 쉬워졌다.
>
> **이름은 공식 표기 그대로다.** 세 기업 공식 페이지를 훑고 **표기를 다시 열어 대조하는
> 검증**을 한 번 더 지났다. 그 과정에서 나온 것 셋:
> - 「Claude Work」는 없다. Anthropic은 **Claude Cowork**, **ChatGPT Work**는 OpenAI 것이다
> - 표면(터미널·IDE·데스크톱·웹)은 별개 제품이 아니다 — 용어집이 못 박는다.
>   표면을 제품으로 세면 세 회사 합쳐 예순이 넘는다
> - Pro·Max·Plus·Team·Enterprise는 전부 요금제다. 「Claude Enterprise」도 그렇다
>
> ## 화면 설계 — 왼쪽 레일과 오른쪽 읽는 칸 (2026-09-16)
>
> **다섯 번째다.** 판을 앉히고 조판까지 다시 짰는데도 「UI/UX가 별로야. 화면 설계가..」
> 였다. 네 번을 말로 고치려다 빗나갔으므로 이번에는 **설계안 넷을 실제로 그려서 보여
> 주고 고르게 했다**(레일 · 얇은 줄 · 견주는 표 · 한 열 문서). 레일이 뽑혔다.
>
> **진단이 앞의 셋과 달랐다.** 카드 격자·펼침 패널·아코디언 트리는 전부 「고르는 자리가
> 크기와 모양을 바꾼다」였는데, 판은 그걸 고쳤는데도 별로였다. 남은 원인은 축이다 —
> **첫 화면을 통째로 고르는 자리가 먹고 정작 읽을 것은 스크롤 아래에 있다.** 고른
> 뒤에도 눈이 위아래로 오간다. 레일은 그 축을 좌우로 돌린다.
>
> | | 판 | 레일 |
> | --- | --- | --- |
> | 고르는 자리 | 화면 **위** 264px | 화면 **왼쪽** 224px |
> | 읽는 것이 시작되는 곳 | 스크롤 아래 | **첫 화면 안, 나란히** |
> | 문서 높이 | 1,632px | **1,247px** |
> | 스크롤하면 | 고르는 자리가 사라짐 | **따라옴**(sticky) |
>
> **레일 초안이 또 한 번 틀렸고 그것을 고친 것이 이 설계의 핵심이다.** 처음 그린
> 레일은 열세 줄 전부에 「0편」을 달았다 — 왼쪽에 **영(零)의 기둥**이 서서 레일에서
> 가장 큰 소리를 내는 것이 「여긴 다 비었다」가 됐다. 원장에서 빈 절을 안 세우기로
> 한 규칙을 레일이 열세 번 어긴 자리다. 지금은 **1편 이상인 줄에만 수가 붙는다.**
>
> 둘째 실패는 계급이었다. 기업 줄과 제품 줄이 들여쓰기와 로고 크기만 다르고 글자
> 크기·색이 같아, 사용자가 직접 말한 「기업 고르고 제품 고르고」 두 층이 눈에 안
> 갈렸다. 기업을 **모노 대문자 + 오른쪽으로 뻗는 괘선**으로 바꿔 구역 머리글로
> 읽히게 했다 — 남는 폭을 괘선이 먹으므로 수가 오른쪽 끝에 붕 뜨던 것도 같이 풀렸다.
>
> 고른 줄은 **반전이 아니다.** 판에서는 칸 하나를 뒤집는 것이 맞았지만, 레일에서
> 줄 하나를 통째로 뒤집으면 그 줄만 칠해진 막대가 되어 목록의 리듬이 끊긴다.
> 면을 한 단 올리고 글자를 진하게·굵게 하는 것이 전부다 — 왼쪽에 브랜드색 띠를
> 세웠다가 걷어냈다. 라이트에서 면 대비가 1.11:1이라 **선택을 실제로 지탱하는 것은
> 글자색과 굵기**이고 면은 거드는 정도다.
>
> 잰 것: 레일 224px, 주소 열둘 전부에서 488px로 같다. 라이트·다크 둘 다 성립하고
> 375px에 가로 스크롤이 없다.
>
> **남는 값 하나** — 1024px 아래에서는 레일이 위로 올라가 다시 첫 화면을 먹는다.
> 좌우로 못 세우는 폭에서는 피할 수 없어 간격만 조였다(468 → 414px, 읽는 칸 시작
> 891 → 813px). 더 줄이면 누르는 자리가 44px 아래로 내려가 손가락에 안 맞는다.
>
> ## 값의 주인을 셋으로 넓혔다 (2026-09-16)
>
> 화면 설계를 네 번 갈아엎고도 안 맞길래 한 층 위를 봤다 — 「기업별 제품이 맞나,
> 기업별 모델이 맞나, 기업별 제품별 모델이 맞나」. **트리가 아닌 것을 트리로 그리려
> 했다면 어떤 배치도 안 맞는다.**
>
> 답은 `Claim.topic` 여섯에 이미 적혀 있었다.
>
> | topic | 실제 주인 |
> | --- | --- |
> | `tier` `limit` `feature` `habit` | 제품 |
> | `context` | **모델** |
> | `price` | **갈린다** — 구독료는 제품, 토큰 단가는 모델 |
>
> 넷은 제품 것이고 하나는 모델 것이고 하나는 둘로 갈리는데, 타입은
> `product: string`을 **필수**로 박아 두고 있었다. 모델 값이 갈 자리가 없었다.
>
> **화면의 층과 값의 주인은 다르다.** 화면은 기업 → 제품 둘로 두고 주인만 셋으로
> 넓혔다. 모델을 층으로 안 세운 이유 셋:
>
> 1. 같은 모델이 한 회사의 제품 여럿에서 돈다 — 표면 논증이 한 층 아래에도 걸린다
> 2. 뉴스 서랍이 이미 모델을 다룬다. 카탈로그를 또 세우면 둘이 갈린다
> 3. 모델은 빨리 썩고 제품은 안 썩는다. 뼈대로 삼으면 **뼈대가 썩는다**
>
> 이득은 `claimsForProduct`에 있다 — 컨텍스트 창을 Claude·Cowork·Code 세 군데
> 적지 않고 한 번만 적는다. `claimsForVendor`는 id로 겹침을 없애 한 모델을 제품
> 둘이 돌려도 한 번만 센다.
>
> **주장이 0개고 `.product`를 부르던 곳이 다섯뿐일 때 고쳤다.** 마흔 개 쌓인 뒤면
> 전부 손봐야 했다.
>
> `guideModels.ts`는 **비운 채로 뒀다.** 어느 제품에서 어느 모델을 고를 수 있는지는
> 공식 페이지를 열어 확인할 일이다 — 기억에서 적지 않는다.
>
> ## 화면 설계 — 조판으로 다시 짠다 (2026-09-16)
>
> 「좀 더 모던하고 트렌디하게」라는 말에서 시작했다. 설계안 다섯을 서로 다른 각도로
> 독립해 뽑고(밀도 · 면 · 움직임 · 계기 · 편집) 심사위원 셋이 나란히 견줬다.
>
> | 안 | 각도 | 합산 |
> | --- | --- | --- |
> | **계기판 조판** | 색도 면도 안 쓰고 타이포·여백만 | **25** |
> | 선 하나 | 선을 덜어내고 정렬만 남긴다 | 19.5 |
> | 계기 | 데이터 단말의 어휘로 민다 | 15.5 |
> | 잉크 스윕 | 상태 전이와 모션으로 | 12 |
> | 면과 층 | 면·깊이·backdrop-filter로 | **11.5** |
>
> **가장 「모던해 보이는」 답이 꼴찌였다.** 면·그림자·유리판을 더하는 안이 셋 다에게
> 가장 낮은 점수를 받았고, 이긴 안은 새 색도 새 면도 새 상호작용도 안 더한다.
> 세 심사위원이 공통으로 적은 말이 「모던해 보이려고 더한 티가 가장 빨리 낡는다」였다.
>
> **그래서 고친 것이 크기·굵기·자간·행간·여백·정렬뿐이다.** 근거는 인상이 아니라 셋이다 —
> 글자가 여덟 단(9.5~13.5px)으로 4px 안에 뭉쳐 단 사이가 평균 0.57px이었고, 여백이
> 열여섯 종이었고, 라벨 스물한 개가 여섯 규격이었다. 위계가 아니라 얼룩이다.
>
> **그리고 이 서랍은 모노가 한 번도 켜진 적이 없었다.** `--font-mono` 호출이 열여섯인데
> 그 이름의 정의가 0곳이라 라벨 전부가 조용히 본문 글꼴로 떨어져 있었다 — 사이트에서
> 모노 라벨이 없는 화면이 여기 하나뿐이었다는 뜻이다. 검사를 붙였다
> (`src/styles.test.ts`). 자세한 것은 CLAUDE.md의 「스타일시트」.
>
> | | 전 | 후 |
> | --- | --- | --- |
> | 글자 단 | 여덟(4px 안) | **다섯** |
> | 여백 종류 | 열여섯 | **넷**(2배씩) |
> | 라벨 규격 | 여섯 | **한 급**(글꼴로만 갈린다) |
> | 글줄 길이 | 62ch가 581·484·620px 셋으로 | **592px 하나** |
> | 중단점 | 719 · 720 · 860(이 서랍 전용) | **640 · 1024**(집의 값) |
> | 헤어라인 대비 | 라이트 1.26 : 다크 1.21 | **1.37 : 1.39** |
>
> **덩이 셋이 세로줄 둘에 선다.** 판의 기업 열 104px을 원장의 두 표가 그대로 써서
> 1280px에서 기업 이름·라벨이 x=73, 칸·호스트가 x=189다. 83%가 비어 있던 지표 띠는
> 1fr 셋이 폭을 나눠 가지며 저절로 찼다 — **감싸서가 아니라 맞춰서 세운다.**
>
> 재어서 확인한 것: 판 높이가 주소 열셋 전부에서 264px, 375px에서 가로 스크롤 없고
> 「Google Antigravity」가 낱말 경계에서 줄바꿈, 계기 칸 112px.
>
> **덤으로 잡은 것**: 원장의 페이드가 아홉 칸 중 여덟 번의 이동에서 안 돌고 있었다.
> 제품 → 제품은 둘 다 `ProductLedger`라 React가 같은 DOM을 재사용해 애니메이션이 다시
> 안 걸렸다 — 「눌렀는데 아무 일도 안 일어난다」의 절반이 스타일이 아니라 재조정이었다.
> `key`를 달아 고쳤다.
>
> ## 화면 설계 — 붙박이 판과 원장 (2026-09-16)
>
> **세 번 거절당하고 네 번째에 앉혔다.** 카드 격자 → 페이지 이동, 격자 아래 펼침,
> 기업 → 제품 아코디언 트리가 차례로 「별로다」였다. 셋의 공통 원인은 하나였다 —
> **고르는 자리가 크기와 모양을 바꾼다.** 열리고 닫히며 아래를 밀어내면 눈이 목록을
> 다시 훑어야 하고, 그게 「트리가 다 열려 있으면 찾기도 힘들다」는 말이었다.
>
> 판은 **아홉 칸이 처음부터 전부 서 있고 여닫히지 않는다.** 고르면 잉크만 바뀐다 —
> 고른 칸이 반전되고 아래 원장의 내용만 갈린다. 다시 훑을 일이 없다.
>
> | | 트리 | 판과 원장 |
> | --- | --- | --- |
> | 제품에 닿기까지 | 두 번(기업 열고 제품) | **한 번** |
> | 고르는 자리의 높이 | 접힘 3줄 ~ 펼침 9줄 | **260px 고정** |
> | 열셋 주소에서 판 높이 | — | **전부 260px**(측정) |
> | 안 고른 제품이 보이나 | 다른 기업은 접혀서 안 보임 | **아홉이 늘 보임** |
>
> **3×3인 것은 우연이 아니다.** 기업마다 제품이 정확히 셋이라 기업으로 줄을 세우면
> 빈칸이 없다. 갈래로 세우면 Google의 업무가 비고 코딩이 둘이라 어긋난다 —
> 「빈 자리는 비워 둔다」가 여기서는 **비울 자리가 없다**로 풀린다.
>
> **원장은 통째로 다시 짰다.** 「제품 선택했을 때 나오는 구성 좀 초기화하자」에
> 답한 자리다. 옛 패널은 빈 절을 제목만 세우고 「아직 없습니다」로 채우고 있었다 —
> 서랍이 비었다는 사실을 아홉 번 되풀이하는 화면이었다. 지금은
>
> - **빈 절을 아예 안 그린다.** 사과 문장도 없앴다
> - 수 셋(`노트 · 아는 값 · 마지막 확인`)을 26px 모노 `tabular-nums`로 맨 위에 둔다
> - **`0`과 `—`를 가른다** — `0`은 세어 본 영이고 `—`는 한 번도 확인 안 한 것이다
>
> 그래서 열셋 어느 주소에서도 원장이 비지 않는다(`entry-server.test.tsx`가 검사한다).
> 제품이면 그 제품의 것, 기업이면 그 기업 셋의 합, 첫 화면이면 서랍 전체다.
>
> **좁아지면 자르지 않고 줄을 바꾼다.** 한 줄로 묶고 말줄임을 걸었더니 375px에서
> 「Claude …」가 둘이 되어 Cowork와 Code를 못 갈랐다. 이름이 그 칸의 전부이므로
> 칸 높이를 자라게 두는 쪽이 맞다.
>
> ## 화면 설계 — 기업 → 제품 트리 (2026-09-16, 판 이전)
>
> **칩 + 카드 격자를 트리 하나로 바꿨다.** 먼저 상세를 격자 아래에 펼치는 쪽으로
> 갔는데, 그건 「도착지가 사막이다」는 진단에만 답한 것이었다 — **주소는
> `/playbook/<기업>/<제품>`으로 두 층인데 화면에는 그 층이 없다**는 문제가 그대로였다.
> 게다가 칩과 기업 머리글이 같은 것을 두 번 세우고 있었다(학습이 「전체」 갈래에서
> 레일을 안 세워 피한 그 중복이다).
>
> 트리는 둘을 한 번에 없앤다. 접힌 기업 셋이 서고, 기업을 누르면 제품이 열리고,
> 제품을 누르면 상세가 열린다 — **화면의 층과 주소의 층이 같은 모양**이고 칩은
> 기업 줄이 대신한다.
>
> | | 칩 + 격자 | 트리 |
> | --- | --- | --- |
> | 첫 화면 높이 | 1,574px | **1,000px** |
> | 기업을 고르는 자리 | 칩 + 머리글 둘 | 줄 하나 |
> | 주소와 화면 | 어긋남 | 같은 모양 |
>
> **여는 것은 언제나 하나다.** 주소가 기업 하나·제품 하나만 담으므로 여럿을 열면
> 그 상태를 주소에 못 적고, 링크로 보낸 화면과 내가 보던 화면이 달라진다.
>
> 제목은 `h1 → h2`(기업) `→ h3`(제품) `→ h4`(패널 절)로 이어진다. 줄 전체가 링크라
> 접근성 이름에는 갈래·표면·편수가 다 들어가지만 **제목에는 이름만** 넣는다 —
> `<a>`가 투명 요소라 안에 제목을 두는 것이 유효하다.
>
> 아래 「상세를 목록 안에서 편다」의 결정 대부분은 트리에서도 그대로다 —
> 주소는 진짜 라우트이고, 「같은 자리, 다른 회사」가 빈 상세를 막다른 길이 아니게 하고,
> 96px 스크롤 가드가 붙어 있다.
>
> ## 상세를 목록 안에서 편다 (2026-09-16, 트리 이전)
>
> 「눌러서 페이지로 들어가는 게 별로다. 이 페이지에서 단계별로 눌러서 딱 상세를
> 볼 수 있으면」이라는 말에서 시작했다. 설계안 넷(레일 · 펼침 · 시트 · 단계)을
> 독립적으로 뽑고 심사위원 셋(처음 온 개발자 · 고칠 사람 · 화면 보는 눈)이 견줬다.
> **표는 레일 2 : 펼침 1이었지만 화면을 실제로 띄워 본 심사위원의 진단을 택했다.**
>
> > 지금 `/playbook` 목록은 이미 꽤 좋다. 망가진 것은 목록이 아니라 **도착지**다 —
> > 제품 페이지는 h1 하나, 메타 한 줄, 「아직 없습니다」, 그리고 바로 푸터다.
> > 「별로」의 절반은 이동 자체가 아니라 **이동한 곳이 사막이라는 것**이다.
>
> 레일을 뽑은 둘도 같은 치명상을 적었다 — 고를 대상이 아홉인데 190px을 상시로 먹고,
> 오른쪽 칸은 결국 통째로 갈린다. `learnRailShown`이 「전체 갈래에는 레일을 안
> 세운다」로 이미 내린 판단에 걸린다.
>
> **그래서 펼침으로 가되 진 안에서 셋을 가져왔다.**
>
> | 어디서 | 무엇을 |
> | --- | --- |
> | 레일(안 1) | **「같은 자리, 다른 회사」** — 같은 갈래를 맡은 다른 회사 제품 줄. 넷 중 가장 좋은 단일 아이디어였다 |
> | 시트(안 3) | 진입 곡선 `cubic-bezier(0.16, 1, 0.3, 1)` 180ms |
> | 학습 목록 | 96px 스크롤 가드 — 늘 올리면 「넘어갔다」로 읽히고, 안 올리면 아래쪽 카드를 눌렀을 때 패널 중간부터 본다 |
>
> **주소는 진짜 라우트 그대로다.** 펼침이 순수 클라이언트 상태였으면 프리렌더 HTML에
> 상세가 안 들어가고 링크로 보낼 수도 없다 — 파이썬 265편이 그 이유로 빈 껍데기였다.
> 대신 `viewKey`가 `/playbook`·`/playbook/<기업>`·`/playbook/<기업>/<제품>` 셋을 한 칸으로
> 묶어 main을 다시 마운트하지 않는다. **주소는 깊어지는데 화면은 안 넘어간다.**
> `entry-server.test.tsx`가 제품 아홉을 실제로 그려 패널이 HTML에 들어가는지 본다.
>
> **패널이 비어 있지 않게 하는 장치가 「같은 자리, 다른 회사」다.** 노트 0편이어도
> 「이 자리에서 다른 회사는 무엇을 내놨나」는 늘 답이 있다. 기업 층을 판 이유가
> 「견줄 상대가 회사끼리」인데 그 축이 화면에 없었다.
>
> **받아들인 약점 둘.** 심사가 짚은 것을 숨기지 않는다.
> - 제품 주소 아홉에 격자 전체가 복제된다. 지금은 노트 0편이라 주소마다 다른 것이
>   패널 하나뿐이고, 노트가 차오르면 옅어진다 — 그전까지는 `<title>`·설명·canonical이
>   제품의 것이라는 데 기댄다.
> - 화면의 h1은 「AI 가이드」이고 제품 이름은 h3다. 제품 주소에서 h1을 제품으로
>   바꾸면 「페이지가 넘어갔다」가 되돌아온다 — 그쪽을 택했다.
>
> `PlaybookProductPage.tsx`는 지웠다. 제품 상세는 이제 목록 안에만 있다.
>
> ## 재구축 검토 (2026-09-16)
>
> 리팩터가 파일 열여섯에 걸쳤으므로 네 갈래(리팩터 자국 · 내용이 돌아올 때 터질 자리 ·
> 로고와 테마 · 접근성)로 훑고, 찾은 것마다 **반증을 시도하는 검증**을 붙였다.
> 30건 중 17건이 반증을 넘겼다.
>
> **가장 큰 것은 이 서랍 밖에 있었다.** `src/entry-server.tsx`의 `render()`가
> 글과 시험 노트만 본문을 미리 캐시에 넣고 있었다. `renderToString`은 동기라
> 페이지의 `useEffect`가 SSR에서 안 도는데, 그 줄이 없으면 HTML에 본문 대신
> 「본문을 불러오는 중입니다」 한 줄만 들어간다.
>
> | 서랍 | 전 | 후 |
> | --- | --- | --- |
> | 옮겨 온 파이썬 글 265편 | 12KB 껍데기 | 37KB |
> | 가이드 노트 | 껍데기가 될 예정이었다 | 배선 완료 |
>
> **조용히 깨지는 고장이다.** 파일은 생기고(프리렌더 목록에 들어 있으니까) 브라우저에서는
> hydrate 뒤 정상으로 보인다. 안 보이는 것은 검색 엔진과 자바스크립트를 못 쓰는 환경뿐이다.
> 링크·경로·사이트맵을 보는 검사는 여럿이었는데 **산출물의 내용을 보는 검사가 하나도
> 없었다** — `src/entry-server.test.tsx`가 그 자리다. 서랍을 팔 때 `render()`에 한 줄,
> 그 검사에 한 줄을 같이 판다.
>
> 나머지 고친 것:
>
> | 무엇 | 왜 검사가 못 봤나 |
> | --- | --- |
> | 제품 폴더를 빠뜨린 원고가 조용히 사라짐 | 폴더 **이름**이 틀린 것만 보고 층이 통째로 빠진 것은 안 봤다 |
> | 링크 검사가 제품 페이지를 죽은 링크로 잡음 | 비교 집합에 노트 주소만 넣었다. 원고를 검사에 맞춰 비틀게 만든다 |
> | 칩에서 Google 로고가 반전됨 | 반전 규칙이 `.is-tinted` 없이 모든 `<img>`에 걸려 있었다 |
> | 제목이 h1 → h3로 건너뜀 | 계층을 보는 검사가 없다 |
> | 기업 칩을 누르면 포커스가 body로 튕김 | `viewKey`가 칩 이동을 페이지 이동으로 쳤다 |
> | `.playbook-card-meta`가 두 번 정의돼 새 값이 죽음 | 중복 선언을 보는 검사가 없다 |
>
> 로고 파일 존재·id 충돌·`monochrome` 어긋남을 보는 `guideProducts.test.ts`를 새로 뒀다 —
> 자격증이 시행처 로고에 대해 이미 하던 것과 같은 자리다.
>
> ## 로고
>
> 약자 마크(AN·OA·GO / CL·CW·CC)를 심볼 로고로 갈았다. 기업 칩에도 단다 — 거기서
> 고르는 것은 분류가 아니라 **회사**라 로고가 가장 빠른 단서다.
>
> | 파일 | 어디서 | 라이선스 |
> | --- | --- | --- |
> | `anthropic` `openai` `google` `claude` `gemini` | 이미 저장소에 있던 것(뉴스가 쓰던 것) | Simple Icons · CC0 |
> | `claude-code.svg` | 2026-09-16에 받음 | Simple Icons · CC0 |
> | `antigravity.png` | 2026-09-16에 받음. 제품 사이트가 아이콘으로 거는 파일 | Google 상표 |
> | `gemini-color.png` | 2026-09-16에 받음. gemini.google.com이 제 아이콘으로 거는 네 색 스파클(160px으로 줄임) | Google 상표 |
>
> **기업 로고에는 포인트 색을 안 입힌다.** 처음에는 칩에도 브랜드색을 칠했는데, 거기는
> 회사를 고르는 자리이지 제품의 성격을 알려 주는 자리가 아니라 칩 넷이 서로 다른 색으로
> 튀어 「무엇이 켜져 있나」가 오히려 안 보였다. 지금은 글자색보다 한 단 세게(흰색·검은색)
> 서고, 켜지면 칩 배경이 뒤집히므로 로고도 함께 뒤집힌다.
>
> **그라디언트가 든 것은 PNG다**(Gemini·Antigravity). 마스크를 씌우면 네 색이 한 색으로
> 납작해지므로 색을 입히지도 반전시키지도 않는다. 단색 `gemini.svg`는 뉴스가 모델 마크로
> 쓰고 있어 그대로 둔다.
>
> **전용 마크가 있는 것만 전용으로 쓴다.** Simple Icons 전체를 뒤져 보니 이 서랍에
> 해당하는 것은 Claude Code 하나뿐이었다(Codex·Antigravity·Cowork·Gemini CLI는 없다).
> 제품 사이트를 직접 열어 보니 **Cowork와 Codex는 전용 마크 자체가 없었다** — Cowork
> 페이지의 파비콘도 Claude 것이고, OpenAI는 회사·ChatGPT·Codex에 같은 매듭 심볼을
> 쓴다. 없는 마크를 지어 그리지 않고 계열 심볼을 그대로 둔다.
>
> 겹치는 자리는 이름과 갈래 꼬리표가 가른다. 단색 로고만 다크에서 반전시키고,
> 색이 든 로고(Google·Antigravity)는 그대로 둔다 — 반전을 걸면 다른 로고가 된다.
>
> **찾아낸 버그 하나.** 옛 도구 카드에는 **링크가 하나도 없었다.** `/playbook`의
> 링크 12개가 전부 벤더 공식 페이지로 나가는 바깥 링크였고, 노트 8편으로 들어가는
> 길이 화면에 0개였다 — 주소를 직접 쳐야만 닿았다. 카드를 링크로 만들면서 안쪽
> 링크가 13개가 됐다.
> 비우면서 지킨 것 셋: 최소선(6·40·8)을 **안 낮췄고**, 검사를 지우는 대신 「비어
> 있다」를 못 박아 두었고(반쯤 찬 채로 남는 것이 가장 나쁘다), 값이 0이 되자
> **nav가 스스로 내려갔다**.

AI 도구를 **잘 쓰고 아껴 쓰는 법**을 담는 다섯 번째 서랍. 상단 nav에 「가이드」로 선다.

**2026-09-16 방향 전환 — 이 서랍은 코딩 에이전트 운용 가이드다.**
답하려는 질문이 「어느 챗 앱 요금제를 살까」가 아니라 **「Claude Code와 Codex를 어떤
모델과 강도로 돌리고, 세션을 언제 새로 파고, 언제 압축하는가」**다. 읽는 사람은
구독을 고르는 사람이 아니라 **터미널에서 에이전트를 하루 종일 돌리는 개발자**다.

| 축 | 답해야 하는 질문 |
| --- | --- |
| 모델 | 어느 일에 어느 모델인가. 기본을 무엇에 두고 언제 올리나 |
| 추론 강도 | 노력·사고 예산을 어디서 어떻게 조절하나. 언제 내리나 |
| 토큰 발생 | 어느 동작이 얼마나 먹나. 서브에이전트·팀·플랜 모드·캐시 미스 |
| 세션 전환 | 언제 새로 파나. 파면 무엇이 초기화되고 무엇을 잃나 |
| 수동 압축 | 언제 직접 줄이나. 지시문을 어떻게 주나. 자동과 어떻게 갈리나 |

**왜 좁히는가.** 6일 차까지 쓴 것이 챗 앱 요금제·티어 쪽으로 기울어 있었다. 그런데
그쪽은 벤더가 값을 안 싣는 영역이라 「모름」만 늘고(주장 24건 중 8건), 정작 값이
많은 쪽은 CLI 문서였다 — Claude Code 비용 문서 **한 페이지**에서 값 일곱이 나왔다.
**값이 있는 곳과 독자가 매일 판단하는 곳이 같은 자리에 있다.**

**챗 앱 셋을 지우지는 않는다.** 주장은 원장에 그대로 두고 레일에도 남긴다 — 다만
값을 긷는 순서에서 CLI 둘이 앞에 선다. 챗 앱의 티어·요금 쪽은 지금 파면 「모름」만
세 줄 는다. **팁 쪽은 반대다** — 챗 앱에도 공식 팁이 있고 그건 값과 무관하게 긷힌다.

이 파일은 설계 결정의 기록이자 루틴이 집어 가는 작업 큐다. 마친 항목은 줄을 지운다 —
무엇을 언제 마쳤는지는 git log가 안다.

마지막 갱신: 2026-09-17 (노트 인프라를 통째로 걷어내고 노트 큐를 팁 큐로 바꿨다)

---

## 왜 별도 서랍인가

학습 글은 **원리**를 담는다. 여기 담을 것은 **지금 그 제품의 상태**다 — 요금제, 사용
한도, 티어 이름, 어느 모델이 무엇에 맞는가. 반년 뒤 틀릴 수 있는 문장이 논지를
떠받치면 그건 학습 글이 아니다.

**경계 한 줄: 원리를 다루면 학습, 지금 그 제품의 상태에 기대면 가이드다.**

이미 있는 것과 없는 것이 갈린다(2026-09-16 실측).

| 있는 것 | 없는 것 |
| --- | --- |
| `context-*` 10편 · `cost-*` 4편 — 토큰 절약 기법을 실측으로 덮는다 | 챗 앱(ChatGPT·Gemini·Claude 앱)을 제품으로 다룬 글 **0편** |
| `model-selection-strategy` — 이름을 안 박고 절차만 적어 안 썩는다 | 구독 티어·사용 한도(Plus/Pro/Max·5시간 창·주간 한도) **0편** |
| `spec-*` 4편 — 단가·상한·은퇴일을 벤더 문서 대조로 | 벤더 사용 지침(context editing·Agent Skills·plan mode) **0편** |

있는 것은 **전부 API 개발자 화자다.** 정액제에서 절약은 청구서가 아니라 한도 관리
문제인데 그 개념 자체가 저장소에 없다.

**썩는 속도도 실측이 있다.** 글 54편이 특정 모델 버전을 본문에 박아 두었고
`GPT-4` 157회 · `GPT-3` 42회인 반면 최신 세대는 통틀어 한 줌이다. `ai-coding-codex`는
「GPT-4o는 현재 OpenAI의 주력 모델」을 단정문으로 들고 있고, `gemini-sdk`는 폐기된
`google-generativeai` 패키지를 쓰는데 같은 저장소의 `spec-sdk-defaults`는 이미 현행
`google-genai`로 실측해 두었다 — **저장소가 스스로와 모순된다.**

---

## 이 서랍의 설계를 정한 실측 하나

자격증 서랍의 루틴 둘을 같은 기간(2026-08-25~09-15)으로 재면 실행률이 갈린다.

| 루틴 | 하는 일 | 실행률 |
| --- | --- | --- |
| 시험 노트 (매일) | **새 파일을 만든다** | 22일 중 16일 = **73%** |
| 자격증 데이터 (매주) | **기존 필드를 제자리에서 고친다** | 월요일 셋 중 하루 = **33%** |

**차이는 주기가 아니라 산출물의 모양이다.** 새 파일을 만드는 루틴은 안 돈 날이
`ls`에 빈칸으로 남고 진도율이 화면에 찍히므로 누락이 스스로 드러난다. 제자리를
고치는 루틴은 아무 자국도 안 남긴다 — **git은 「값이 안 바뀜」과 「확인을 안 함」을
구별하지 못한다.**

그 결과가 지금 `certs.ts`다. `verifiedAt` 열넷 중 아홉이 22일 묵었고 `unknowns` 122항목이
그대로인데, `certs.test.ts` 369줄에 **`unknowns`와 `verifiedAt` 신선도를 보는 검사가
0개**라 `npm test` 53건이 전부 초록이다. 묵은 값이 화면에 그대로 나간다.

**그래서 이 서랍은 「확인했다」를 필드로 두지 않는다.**

---

## 정해진 것

| 항목 | 결정 | 정한 날 |
| --- | --- | --- |
| 자리 | 상단 nav 다섯째 칸, 주소 `/playbook` | 2026-09-16 |
| 이름 | **가이드** | 2026-09-16 |
| `SectionId` | **안 늘린다.** `SearchScope`만 넓힌다 | 2026-09-16 |
| 쓰는 루틴 | **새로 세운다**(아침 루틴이 다섯이 된다) | 2026-09-16 |
| 첫 달 `price`·`limit` 주장 | **8건 상한.** 나머지는 「모름」으로 연다 | 2026-09-16 |
| 커뮤니티 근거 | **싣는다.** 등급·반례를 필수로 | 2026-09-16 |

**`SectionId`를 안 늘리는 이유.** 그것은 섹션 일반이 아니라 `Category.section`의 값이다
(`src/types/article.ts:18,25`). 늘리면 `categoriesIn('playbook')`이 영원히 빈 배열이고
`bySection`이 0편, `countByScope` 루프가 영원히 0을 센다. 자격증이 `SectionId`가
아니었던 이유가 그대로 있다.

**nav 칸은 0편으로 안 켠다.** 최소선(도구 6장 + 주장 40건 + 노트 8편)을 넘긴 날에만
`Layout.tsx` 두 줄을 더한다. 라우트는 그전에도 산다.

**`HomePage.tsx:192`의 「다른 곳에서도 볼 수 있는 가이드고」를 고친다** — 지금 그
문장이 학습 섹션을 「가이드」라고 부른다. 낱말이 한 뜻만 갖게 한다.

---

## 여기에는 글이 없다 — 전부 데이터다

**2026-09-17에 노트를 인프라째 걷어냈다.** 원래 이 절의 제목은 「무엇이 데이터고
무엇이 글인가」였고, 원고가 값을 `:claim[...]`으로 부르는 왕복이 이 서랍 설계의
절반이었다. 한 편도 안 쓴 채 뼈대만 서 있었고, 정작 읽는 사람이 원한 것은 4,000자짜리
글이 아니라 **「어떻게 써야 잘 쓰고 아껴 쓰나」 한 줄**이었다.

그래서 왕복이 통째로 사라졌다. 값을 원고에 안 적는 것이 아니라 **원고가 없다.**

```
src/data/guideVendors.ts              기업 3
src/data/guideProducts.ts             제품 9
src/data/guideModels.ts               모델 23 — 이름과 id만
src/data/playbookClaims.ts            주장 — 서랍의 본체. 값과 팁이 여기 다 있다
src/data/playbook-checks/<날짜>.ts     append-only 확인 로그
src/data/playbook.ts                  위를 합쳐 checkedAt·나이·상태를 계산
```

**팁도 주장이다** — `topic: 'habit'`, `value: null`, `detail`에 이유 한 줄.
팁 하나를 더하는 것은 파일을 만드는 일이 아니라 배열에 항목 하나를 더하는 일이다.

**`checkedAt`은 필드가 아니라 파생값이다.**

```ts
// src/data/playbook-checks/2026-09-23.ts
export default [
  { claimId: 'claude-max-5h-window', result: '그대로', excerpt: '그날 그 페이지에서 본 값 원문 한 줄' },
  { claimId: 'chatgpt-pro-price',    result: '바뀜', changedTo: '...', excerpt: '...' },
];
```

`playbook.ts`가 `max(그 id가 로그에 찍힌 날)`로 계산한다. **신선해지는 유일한 길이
파일 추가**라 73% 쪽 모양이 된다. `excerpt`는 「URL을 안 열고 파일만 쓰기」의 비용을 올린다.

**`articles/`와 다른 서랍이다** — 사슬도 카테고리도 태그도 마무리 블록도 없고
6,000자 하한·채우기 큐·「신규 글 멈춤」에 하나도 안 걸린다. 「학습 전체 466편」에도
안 더한다. 셀 편수 자체가 없다.

---

## 근거 세 등급

**한 주장은 등급 하나만 갖는다.** 섞으면 독자가 못 가른다. 커뮤니티 주장을 우리가
돌려 확인했으면 등급을 `ours`로 올리고 원 게시물은 `source`에 남긴다 — 승격이 로그에
기록으로 남는다.

색이 아니라 **모양**으로 먼저 가른다(흑백·색맹에서도 갈린다). 색은 새로 내지 않고
기존 `-text` 토큰을 쓴다 — `theme.test.ts` 파급 0.

| 등급 | 배지 | 모양 | 배지에 늘 함께 나가는 것 |
| --- | --- | --- | --- |
| `vendor` | 공식 | 채움 · `--brand-text` | 벤더 호스트 그대로 + 「N일 전 확인」 |
| `ours` | 실측 | 실선 테두리 · 모노 · `--cat-lab-notes-text` | 돌린 날 · env(모델 id·버전) |
| `field` | 현장 | **점선** 테두리 · `--cat-tools-text` | 「n곳 · 반례 있음」 |

대조표에서 `field` 값은 **굵게 쓰지 않고 앞에 `~`를 단다.** 배지를 봐도 같아 보이는
문제를 막는 자리다.

**커뮤니티 근거에 반드시 있어야 할 넷 — 넷 다 테스트가 강제한다.**

1. **단일 게시물 URL** — 스레드 요약이나 남의 정리글이 아니라 주장이 실제로 적힌 곳.
   호스트가 벤더 도메인이면 그건 통설이 아니라 공식이다(검사가 선다).
2. **`postedAt`** — 그 글이 쓰인 날. 우리가 연 날과 다른 값이다.
3. **`corroboration.kind`** — `재현`(우리가 한 번 해 봤다) / `다수 보고`(서로 독립된 곳
   셋 이상) / `벤더 확인`. **한 사람이 한 번 한 말은 통설이 아니라 일화이고 싣지 않는다.**
4. **`counter`가 비어 있으면 안 된다** — 반례 또는 안 통하는 조건. 못 찾았으면
   「우리 쪽에서는 확인 못 함」이라고 적어야 통과한다. 그 문장이 곧 등급이 `ours`가
   아닌 이유다.

**커뮤니티 글은 인용하지 않고 주장만 한 줄로 다시 쓴다.** 뉴스에서 원문 번역
재발행을 금지한 것과 같은 선이다.

`field`는 **10건 상한**이고 `postedAt` 기준 18개월을 넘으면 등급을 잃는다 — 재확인해
`ours`로 올리거나 지운다. 집합이 줄어드는 방향으로만 움직인다.

**부딪히는 자리는 90번대 「대질」 노트로 판다** — 통설·공식·실측 셋을 한 표에 나란히
놓는 글이고, `field → ours` 승격의 산출물이 된다.

---

## 갱신을 아무도 안 했을 때

전제: **재확인 루틴은 33%로 매긴다**(위 실측). 강제하는 설계가 아니라 **안 돌아도 안
속는 설계**를 낸다. 아래는 루틴이 하나도 안 돌아도 작동한다.

1. **만료되면 값이 흐려지는 게 아니라 사라진다.** 그 자리는 「2026-08-14 확인 ·
   유효기간 지남 · 원문에서 확인하기 →」가 된다. 회색 숫자는 여전히 읽히지만
   **없는 숫자는 못 믿는다.** 자격증의 `verifiedAt`이 반만 작동한 자리가 여기다.
2. **나이를 읽는 날 기준으로 센다.** 프리렌더는 빌드일로 그리고, hydrate 뒤 오늘
   날짜로 다시 계산해 만료 칸을 죽인다. **배포가 멎으면 화면이 스스로 비어 간다.**
3. **날짜가 아니라 나이를 적는다.** 「2026-08-25 확인」이 아니라 「22일 전 확인」.
   날짜는 권위로 읽히고 나이는 위험으로 읽힌다.
4. **soft/hard 두 단, 썩는 속도마다 다르게.**

   | volatility | 무엇 | soft(흐려짐) | hard(**값 제거**) |
   | --- | --- | --- | --- |
   | `price` | 요금·티어 | 30일 | 60일 |
   | `limit` | 한도·창 | 21일 | 45일 |
   | `model` | 모델 id·컨텍스트 창 | 45일 | 90일 |
   | `concept` | 절약 기법의 원리 | 없음 | 없음 |

   `concept`에 임계를 안 두는 것이 중요하다 — 원리는 안 썩으므로 유효기간을 두면
   만료 비율만 부풀린다.
5. **도구 카드 머리에 신선도 한 줄** — 「사실 18칸 중 만료 5 · 미확인 2 · 가장 오래된
   것 41일」. 코드가 뽑고 숫자를 어디에도 적어 두지 않는다(뉴스의 「최근 7일」과 같은 이유).
6. **서랍이 스스로 nav에서 내려간다.** 만료 비율이 40%를 넘거나 확인 로그가 21일
   비면 `navVisible = false`. **nav에 안 서는 것이 거짓말하는 것보다 낫다.**
   되살아나는 조건은 로그 파일 하나다.

**테스트를 두 갈래로 가른다.**

- **`npm test`가 막는 것(값에 매인 것만)** — 스키마 필수 칸, `field` 넷 · `ours` 넷,
  벤더 호스트 허용 목록, `value: null` ↔ `open` **양방향**(자격증에 0개인 그 검사),
  본문 숫자 금지, frontmatter, 내부 링크, `claims` id 실재.
**URL 검사는 403을 죽은 링크로 치지 않는다.** 2026-09-16에 아홉을 직접 열어 보니
`chatgpt.com` · `claude.ai` · `openai.com/chatgpt/pricing` · `help.openai.com`이 403이다 —
봇 차단이지 없는 주소가 아니다(`CLAUDE.md`가 `help.openai.com` 403을 이미 기록해 두었다).
막을 것은 404와 연결 실패다. `platform.claude.com/docs/en/docs/claude-code/overview`가
실제로 404여서 Claude Code 문서는 `code.claude.com/docs`를 쓴다.

- **`npm run check:playbook`이 보는 것(나이에 매인 것)** — 로그 14일 공백, 만료 25%
  초과, `field` 상한과 18개월. **빌드를 안 세운다.** 루틴만 돌리고, 실패하면 6번이
  서랍을 nav에서 내린다.

신선도 검사를 `npm test`에 걸면 1년 중 어느 날 반드시 서고, 그때 값을 채우는 것보다
**임계를 올리거나 검사를 지우는 쪽이 언제나 더 싸다.** 게다가 매일 도는 글 루틴 셋·
시험 노트·뉴스 수집이 전부 같은 `npm test`를 지나므로 무관한 커밋이 막히는 아침이 온다.

**막혔을 때 1분짜리 탈출구** — 공식 페이지를 다시 열어 로그를 쓰거나, 못 열겠으면
`value: null`로 내리고 `open`에 이름을 넣는다. **모른다고 적는 것은 언제나 통과한다.
틀린 값만 막힌다.**

**첫 달에는 `price`·`limit`을 8건으로 묶는다.** 유효기간을 늘리는 대신 주장 수를
줄인다. 모른다고 적힌 표가 3주 묵은 숫자보다 낫다.

**아직 못 푼 구멍 하나.** 벤더가 티어 이름이나 한도 체계를 갈면 썩는 것은 값이 아니라
**구조**인데, 나이 장치는 값의 나이만 센다. `tiers[]`가 통째로 뜻을 잃은 날에도 화면은
「9일 전 확인」이라고 말한다. `PLAYBOOK-ROUTINE.md`에 「분기에 한 번 구조를 본다」로만
적어 둔다.

---

## 첫 한 주

- [x] **1일 — 골격 배선.** (2026-09-16 끝) `plugins/playbook-index.ts` · `src/lib/playbookBody.ts` ·
      `src/content.d.ts` 선언 · `vite.config.ts` 등록 · `src/App.tsx` 라우트 셋 · `src/routes.ts`
      → **빈 폴더 상태로 `npm run build`와 `npm run typecheck`가 초록.**
      (`tsc -b`로 확인한다. `npx tsc --noEmit`은 아무것도 검사하지 않는다.)
- [x] **2일 — 타입과 도구 6장.** (2026-09-16 끝) `playbookTools.ts` · `playbook.ts` · `PlaybookPage.tsx`
      → 카드 여섯이 서고 **지표가 편수가 아니라 「값 N개 · M개가 14일 안 확인」**으로 찍힌다.
- [~] **3일 — 주장 40건과 확인 로그.** (2026-09-16 장치는 끝, 주장은 13/40) `playbookClaims.ts`(vendor 24 · field 10 · ours 6) ·
      `playbook-checks/` 첫 셋 · `ClaimRow` · `EvidenceBadge` · `FreshnessMeter` · CSS
      → **로그 파일을 하나 지우면 그 주장들의 나이가 사라지고 화면이 「유효기간 지남」이 된다.**
      안 되면 `checkedAt`이 어딘가에 필드로 남아 있는 것이다.
- [x] **4일 — 검사.** (2026-09-16 끝) `playbookClaims.test.ts` · `playbook.test.ts` ·
      `src/content/playbook.test.ts` · `scripts/check-playbook.mjs`
      → **`counter`를 비우고, `value: null`인데 `open`에서 빼고, 벤더 url을 엉뚱한 호스트로
      바꿔 넣었을 때 각각 `npm test`가 선다.** 그리고 `check:playbook`을 통과 못 하게 만들어도
      `npm test`는 초록이다 — 갈라 둔 것이 실제로 갈렸는지 보는 자리다.
- [x] **5일 — 본문 파이프라인과 규약.** (2026-09-16 끝) `rehypeEvidence` · `PlaybookToolPage.tsx` ·
      `PlaybookNotePage.tsx` · `CLAUDE.md` · `PLAYBOOK-ROUTINE.md`
      → **`npm test` 통째로 초록.** `emphasis.test.ts`가 331편을 실제 렌더하므로 rehype 추가가
      기존 글에 회귀를 냈으면 여기서 잡힌다.
- [~] **6~7일 — 노트 8편.** (2026-09-16에 검색 합류와 4편) `search.ts`·`SearchOverlay.tsx`·
      `search.test.ts` · `shared/01~03` · `claude-code/01`
      → 8편 전부에 `:claim` 참조가 있고 **본문에 통화 기호·모델 id·토큰 수가 하나도 없다.**
      그리고 사이트에서 「토큰 절약」을 검색했을 때 이 서랍의 노트가 나온다 — 안 나오면
      `search.ts` 합류가 안 된 것이고 이 서랍을 만든 이유가 무색해진다.
      **검색 쪽은 끝났고 그 조건이 `search.test.ts`의 검사로 박혔다.** 남은 넷은
      `chatgpt/01` · `claude/01` · `gemini/01` · `codex/01`이다.
- [x] **최소선을 넘긴 날 nav 다섯째 칸을 켠다.** (2026-09-16 끝) `Layout.tsx` — 데스크톱과
      모바일 양쪽. `playbookNavVisible(todayInSeoul())`로 감쌌으므로 **조건이 깨지면
      아무도 안 고쳐도 메뉴에서 내려간다.** 확인 로그 파일 하나를 치우고 새로고침해
      「가이드」가 사라지는 것을, 되돌려 다시 서는 것을 눈으로 봤다.
      `HomePage.tsx`의 「다른 곳에서도 볼 수 있는 가이드고」는 「개론」으로 고쳤다 —
      그 낱말은 이제 nav 한 칸의 이름이라 한 뜻만 가져야 한다.
      **번들은 안 늘었다** — 검색 합류 때 `data/playbook`이 이미 초기 청크에 들어와
      있었고, 빌드 산출물을 열어 확인했다.

### 방향 전환과 CLI 문서 스윕 (2026-09-16 저녁)

Claude Code와 Codex 공식 문서를 **영역 여섯**(각 도구의 모델·강도 / 세션·압축 /
토큰·측정)으로 나눠 훑고, 영역마다 **인용을 다시 열어 대조하는 검증자**를 따로 붙였다.
사실 281건을 모았고 267건이 대조를 통과했다.

**검증이 실제로 일을 했다.** 통과 못 한 것이 넷이고 전부 「그럴듯한데 원문에 없는」
꼴이었다.

| 잡힌 것 | 무엇이었나 |
| --- | --- |
| 과장 | 「문서가 팀메이트에 Sonnet을 권한다」 — 원문에는 모델 지정 방법의 예문 한 줄뿐 |
| 추론 | 「현재 설정을 확인하는 네 자리」 — 서로 다른 세 페이지를 합쳐 만든 목록 |
| 추론 | 버전 조건을 다른 기능의 것과 뒤섞음 |
| 과장 | Codex 강도 단계를 「CLI는 Low 하나」로 읽음 — 원문은 Low와 Light가 **같은 등급의 두 이름**이고 CLI에도 여섯 단계가 있다 |

마지막 것이 특히 중요하다. 그대로 실었으면 **이 서랍이 답하려는 바로 그 질문에서
틀린 값을 세울 뻔했다.** 조사자 하나로 끝냈으면 넷 다 통과했을 것이다.

**주장이 24 → 55건으로 늘었다.** 값이 챗 앱 쪽에는 없고 CLI 문서에는 많다는 것이
숫자로 확인된 셈이다. 새 값은 전부 `model`·`concept`로 두었다 — **첫 달 요금·한도
예산 여덟이 이미 차 있어서, 값을 넣으려고 상한을 올리는 대신 상한이 안 걸리는 것만
넣었다.** 실제로 이 값들은 요금제가 아니라 제품 동작이라 그쪽이 옳은 분류이기도 하다.

**못 실은 것 하나.** Codex의 빠르게 도는 모드는 속도 배수와 크레딧 배수가 문서에
숫자로 있는데, 그건 `price`라 예산에 걸린다. 지금은 **배수 대신 관계만**(크레딧
증가분이 속도 증가분보다 크다) `concept` 주장으로 실었다. 숫자를 실으려면 예산을
하나 비우거나 상한을 올려야 한다 — 그건 사람이 정할 일이다.

### 최소선을 넘겼다 (2026-09-16)

도구 6 · **주장 55** · **노트 8**로 셋 다 넘겼다. `playbookNavVisible`이 `true`가
되면서 「지금 안 선다」를 못 박아 둔 검사가 섰다 — **설계대로 동작한 것이다.**

검사의 방향을 뒤집었다. 그전에는 최소선을 슬그머니 낮추는 것을 막았는데, 넘긴
뒤로는 반대쪽이 위험하다 — 주장이나 노트를 지우다가 문턱 아래로 내려가면 화면은
그대로인 채 근거만 사라진다. 지금은 셋을 각각 재고 파생값이 그것과 맞는지 함께 본다.

**다만 `Layout.tsx`는 아직 안 고쳤다.** nav는 사이트의 가장 바깥 층이고, 서랍의
주제를 바꾼 바로 그날 켜는 것은 성급하다. 켜는 것은 사람이 정한다.

### 검사가 하나 더 붙었다 — 본문의 `:claim`

> **이 검사는 2026-09-17에 사라졌다.** `:claim[...]` 장치를 노트 인프라와 함께
> 걷어냈기 때문이다 — 부를 원고가 없으면 왕복도 없다. 기록은 남긴다: 「화면에
> 나가는 쪽을 검사하고 있는가」를 묻는 방식 자체는 다른 자리에서도 쓴다.

**frontmatter만 보고 본문은 안 보고 있었다.** 화면에 나가는 것은 본문의
`:claim[...]`인데, 오타가 나면 값 대신 아이디가 덩그러니 찍힌다. `fillClaimRefs`가
그 자리를 일부러 안 지우므로(구멍을 숨기지 않으려고) 원고 검사가 잡아야 한다.
양방향으로 본다 — 본문에 있는데 `claims`에 없는 것, `claims`에 있는데 본문이 안
부르는 것. 일부러 아이디 한 글자를 바꿔 두 줄이 다 서는 것을 확인했다.

### 6일 차에 찾은 것

**1. 값이 아니라 주소가 썩었다.** `developers.openai.com/codex/`가
`learn.chatgpt.com/docs`로 **301** 된다. 404가 아니라 301이라 URL 검사에 안 걸리고,
나이 장치는 값의 나이만 세므로 화면은 「0일 전 확인」이라고 말한다 — 계획이
「분기에 한 번 구조를 본다」로만 적어 둔 그 구멍을 첫 달에 밟았다. 도구 URL과 주장
출처를 옮기고 `learn.chatgpt.com`을 벤더 호스트에 넣었다. **넉 달을 기다렸으면 그동안
독자가 옛 주소로 갔다.** 「분기에 한 번」이 충분한 주기인지는 다음에 한 번 더 밟으면 다시 본다.

**2. 벤더마다 값을 싣는 정도가 다르다.** 챗 앱 요금제 페이지 셋은 배수만 적고 절대
한도를 안 싣는데, Claude Code **비용 문서 한 페이지**에서 값 일곱이 나왔다(캐시 수명,
활동일 단가, 지침 파일 권장 길이, 도구 정의 지연 로딩, 모델 배치, 사고 토큰 과금,
그리고 「한도는 문서에 없고 제품 안에 있다」는 사실 자체). **주장을 못 채우는 것은
우리 문제가 아니라 그 벤더의 선택이다** — 어느 쪽인지 구별해 두면 다음에 어디를 열지가 정해진다.

**3. 검사가 자기가 권하는 모양을 잡았다.** 「본문에 모델 id를 적지 않는다」 검사가
`:claim[claude-max-usage]`를 모델 id로 읽었다. 주장 id가 도구 이름으로 시작하기
때문인데, 그 자리는 값을 박은 곳이 아니라 **데이터에서 부르고 있는 곳**, 곧 검사가
권하는 바로 그 모양이다. 안 고쳤으면 **규칙을 지킬수록 검사가 더 많이 서서 사람이
부르기를 줄이게 된다.** `:claim[...]`을 먼저 걷어내고 검사한다.

**4. 새 서랍은 원고 검사에 무방비로 열린다.** `emphasis.test.ts`는 `articles/`만,
`certPrep.test.ts`는 시험 노트만 훑는다. 가이드 노트에는 닫히지 않은 강조와 한 줄로
붙는 줄바꿈을 보는 검사가 하나도 없었다 — 둘 다 **원고만 보고는 눈으로 못 잡는**
자리다. `playbook.test.ts`에 걸었다. **서랍을 새로 팔 때마다 이 둘을 같이 판다.**

### 검사가 고쳐 놓은 설계 둘 (4일 차)

**1. `tool.open`을 없애고 파생값으로 바꿨다.** 손으로 적는 할 일 목록과 화면의 「모름」
줄이 서로 안 맞는 것을 양방향 검사가 잡았다. 자격증의 `unknowns`가 정확히 그 모양이라
122항목이 화면에도 안 나가고 검사도 안 보는 채로 묵었다. 지금은 **모르는 값을
`value: null`인 주장으로 세워 화면에 「모름」 줄로 내보내고**, 할 일 목록은 그것에서
계산한다(`toolOpenItems`). 손으로 쓰는 목록이 없으면 어긋날 자리도 없다.

**2. 「모름」은 만료가 아니다.** 값을 안 내보내고 있으니 독자를 속이지 않는다. 만료는
「값이 있었는데 이제 못 믿는다」일 때만이다 — 그래야 만료율로 서랍을 nav에서 내리는
장치가 진짜 위험만 센다. 처음에는 둘을 같이 세어 만료율이 33%로 잘못 나왔다.

**첫 달 상한 여덟도 「값이 있는」 것만 센다.** 상한의 목적이 「매주 다시 열어야 하는
URL 수」를 묶는 것이라, 값이 없는 주장은 부담을 안 만든다.

### 아직 못 채운 것 (3일 차에서 남음)

**주장이 마흔이 아니라 스물넷이다**(2026-09-16 저녁 기준. 3일 차에는 열셋이었다).
못 채운 이유가 셋 다 다르고, 셋 다 지어내는 것보다 비워 두는 쪽이 옳았다.

| 갈래 | 목표 | 지금 | 왜 |
| --- | --- | --- | --- |
| `vendor` | 24 | **55** | 챗 앱 쪽은 벤더가 값을 안 싣는다 — ChatGPT 요금제 페이지는 티어와 기능 비교는 싣는데 **금액 숫자가 없고**(「/월」만 있다), Gemini AI Pro도 배수만 있고 금액이 없다. 반면 CLI 문서 쪽은 값이 많아 6일 차에 일곱이 늘었다 |
| `field` | 10 | **0** | 단일 게시물 URL · 게시일 · 교차 확인(독립된 곳 셋) · 반례 넷을 갖춘 것을 아직 못 모았다. 넷 중 하나라도 빠지면 그건 일화다 |
| `ours` | 6 | **0** | 우리가 직접 돌린 수가 있어야 하는데 아직 안 돌렸다. `measurement`의 command·result·ranAt·env를 지어내면 이 서랍의 존재 이유가 사라진다 |

`field`와 `ours`가 0이라 **배지 셋 중 둘이 화면에서 아직 안 쓰인다.** 컴포넌트는 서 있고
CSS도 있지만 실제 값으로 검증된 적이 없다 — 첫 `field`·`ours` 주장이 들어오는 날 눈으로 본다.

**nav 최소선은 2026-09-16 저녁에 넘겼다**(주장 55 · 노트 8). 위 「최소선을 넘겼다」를 본다.

**첫 달 요금·한도 예산 여덟을 다 썼다**(2026-09-16). 값이 있는 `price`·`limit` 주장이
여섯에서 여덟이 됐다 — 캐시 수명과 활동일 단가가 들어갔다. **다음에 요금·한도 값을 하나
더 넣으려면 먼저 하나를 내려야 한다.** 상한을 올리는 쪽이 언제나 더 싸므로, 올릴 거면
`playbookClaims.ts` 머리말과 `check-playbook.mjs`와 이 줄을 함께 고쳐 흔적을 남긴다.

### 팁 큐

**2026-09-17에 노트 큐를 여기로 갈아 끼웠다.** 앞에 있던 것은 `01-limit-not-bill.md`
꼴의 원고 목록이었고, 그 원고가 놓일 폴더를 같은 날 걷어냈다. 이 서랍이 파는 것은
글이 아니라 팁이므로 큐도 팁으로 센다.

큐의 한 줄은 **제품 하나 × 질문 하나**다. 채우는 것은 `playbookClaims.ts`의 항목
하나씩이고, 한 질문에 팁이 둘셋 나오는 것이 보통이다.

**지금 어디까지 찼는지는 화면이 안다** — `/playbook/<기업>/<제품>`의 「이렇게 쓰면
아낀다」 절 길이가 그 제품의 팁 수다. 오늘 기준 예순셋(공식 34 · 현장 29)인데
**둘에 몰려 있다** — Claude Code 18 · Codex 12이고 나머지 일곱은 둘에서 다섯이다.
값이 가장 많이 나온 자리를 먼저 팠기 때문이고(6일 차 실측), 그래서 레일의 아래쪽
일곱 줄은 아직 얇다.

**질문 넷은 제품마다 같다.** 그래야 제품끼리 견줄 수 있고, 레일에서 옆줄로 옮겼을 때
같은 자리에 같은 종류의 답이 선다.

```
세션을 언제 새로 파나       — 언제 끊고 언제 이어 가나
컨텍스트를 어떻게 줄이나     — 자르기 · 요약 · 새 창 · 파일로 빼기
어느 모델로, 어느 강도로     — 기본값을 그대로 쓰면 안 되는 자리
무엇을 안 읽게 하나         — 넣지 않는 것이 가장 싼 절약이다
```

**얇은 제품부터 집는다.** 아홉 줄이 고르게 차야 레일의 어느 줄을 눌러도 읽을 것이 있다.
괄호는 오늘 기준 팁 수다 — 채우면 이 수를 고친다.

```
[ ] anthropic/claude          (2)  질문 넷
[ ] openai/chatgpt-work       (3)  질문 넷
[ ] google/gemini-app         (3)  질문 넷
[ ] google/gemini-cli         (3)  질문 넷
[ ] anthropic/claude-cowork   (4)  질문 넷
[ ] openai/chatgpt            (5)  질문 넷
[ ] google/antigravity        (5)  질문 넷
```

Claude Code(18)와 Codex(12)는 당분간 안 집는다. 한 제품이 열여덟이면 그 화면은 이미
읽는 데 시간이 걸리고, **고르게 차는 것이 더 쌓이는 것보다 낫다.**

**공식이 먼저, 현장이 나중이다.** `check:playbook`이 팁의 두 등급을 세어 현장이 공식을
넘으면 선다 — 현장 통설은 넷(URL·게시일·교차 확인·반례)을 다 갖춰도 여전히 가장 약한
등급이고, 이 서랍이 「사람들이 그러더라」로 뒤덮이면 값을 공식 페이지에서 읽어 오는
나머지 장치가 장식이 된다.

**긷는 자리에서 가장 많이 버려지는 셋**은 실측으로 겪었다 — 별칭·접근 권한을
「고를 수 있다」로 부풀린 것, 자기 벤치마크, 경쟁사 마케팅 글이다. 처음 훑은 일흔셋에서
열이 이렇게 빠졌다. 한 사람이 한 번 한 말도 통설이 아니라 일화라 안 싣는다.

### 둘째 주부터

하루 팁 넷. **계획은 루틴이 늘리지 않는다** — 매일 도는 루틴이 계획을 고치면 목표가
매일 흔들린다.

**무엇을 길을지는 바로 위 팁 큐가 정한다.** 한때 `playbookPlan.ts`를 두고 화면에
「8 / 40편」과 「예정」 줄을 세우려 했는데(자격증의 `certPrepPlan.ts`를 따라간 것이다)
**그 파일은 만든 적이 없고 화면도 그 줄을 안 그린다.** 원장이 적는 것은 예정이 아니라
**지금 있는 것**이다 — 없는 절은 아예 안 선다. 그 규칙 위에 「앞으로 40편 쓸 예정」을
얹으면 빈 서랍에 다짐을 적는 꼴이 되어 「구성이 별로」로 되돌아간다.

데이터로 옮길 이유가 생기는 때는 **화면이 진도를 보여 줘야 할 때**다. 그때까지는
큐가 이 파일에 글로 있는 편이 낫다 — 고치는 값이 한 곳뿐이다.

---

## 안 하기로 한 것

| 안 하는 것 | 왜 |
| --- | --- |
| `SectionId` 늘리기 | `categoriesIn()`이 영원히 빈 배열, `bySection` 0편, `countByScope` 0. 타입만 늘고 화면에는 0이 박힌다 |
| 학습 칩 줄의 두 번째 곁다리 | 곁다리가 둘이면 그건 그냥 갈래 둘이다. 375px에서 `margin-left:auto`가 무효가 되어 칩 여섯으로 읽힌다 |
| 리서치의 두 번째 갈래 | 리서치의 「직접 돌려서 확인했다」는 9편을 지우고 세운 선이다. 옆에 통설 갈래를 대등하게 놓으면 그 선이 물러지고, 침식은 어떤 테스트에도 안 잡힌다 |
| 신선도 검사를 `npm test`에 걸기 | 1년 중 가장 먼저 죽을 검사이고, 죽는 방식이 「다른 루틴을 인질로 잡다가 지워지는 것」이다 |
| 기존 글 이동 | `context-*` 10편 · `cost-*` 4편은 `articles/`에 그대로 두고 링크만 한다. 옮기면 사슬·번호·접두사 표가 전부 움직인다 |
| 썩은 글을 이 서랍으로 이사 | `ai-coding-codex`·`gemini-sdk`·`openai-sdk`·계보 셋은 **고쳐야 할 글이지 이사할 글이 아니다.** 별건으로 남긴다 |
| 팁에 썩는 값 적기 | 모델 id·요금·한도·토큰 수. 값은 값 주장이 따로 들고 팁은 행동만 적는다 — 박으면 그 팁이 값과 같은 속도로 늙는다 |
| 이 서랍에 원고 되살리기 | 2026-09-17에 인프라째 걷어냈다. 한 편도 안 쓴 채 뼈대만 서 있었고, 이 서랍이 파는 것은 글이 아니라 팁과 값이다. 길게 쓸 것이 생기면 그건 학습 쪽 글이다 |
| 홈 히어로 지표 건드리기 | 「최신 가이드」 카드만 리서치 블록 아래에 붙인다 |
