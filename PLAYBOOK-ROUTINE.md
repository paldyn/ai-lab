# 활용 가이드 루틴

매일 활용 노트 **두 편**을 쓰고, 값이 묵었으면 그날 한 편 자리를 갱신에 쓴다.
규칙은 `CLAUDE.md`의 「활용 가이드」 절, 계획과 결정 기록은 `PLAYBOOK-PLAN.md`에 있다.

저장소는 `paldyn/ai-lab`. `main`에 직접 커밋·푸시한다.

---

## STEP 0 — 값이 묵었는지 먼저 본다

```bash
npm run check:playbook
```

**넘친 것이 있으면 그날 두 편 중 한 편 자리를 갱신에 쓴다.** 글을 한 편 덜 쓰는 것이
틀린 값을 하루 더 세워 두는 것보다 낫다.

갱신은 이렇게 한다.

1. 짚힌 주장의 `source.url`을 **실제로 연다.** 안 열고 로그만 쓰면 이 장치가 통째로
   거짓이 된다 — `excerpt`를 필수로 둔 이유가 그것이다.
2. 값이 그대로면 `result: '그대로'`, 바뀌었으면 `'바뀜'` + `changedTo`.
   **그날 그 페이지에서 본 원문 한 줄을 `excerpt`에 옮긴다.**
3. 오늘 날짜로 `src/data/playbook-checks/<YYYY-MM-DD>.ts`를 **새로 만든다.**
   기존 파일을 고치지 않는다 — 파일이 늘어야 신선해지는 것이 이 설계의 전부다.
4. 값이 바뀌었으면 `playbookClaims.ts`의 `value`도 함께 고친다.
5. **페이지를 못 열겠거나 값이 없어졌으면 `value: null`로 내린다.**
   그러면 화면에 「모름 · 공식 페이지에서 확인 →」으로 서고, 그것으로 통과다.
   **모른다고 적는 것은 언제나 통과한다. 틀린 값만 막힌다.**

**STEP 1로 돌아가지 않는다.** 갱신을 하다가 「이 주장도 고쳐야겠다」가 보이면
`PLAYBOOK-PLAN.md`에 적어 두고 그날은 짚힌 것만 한다.

403은 죽은 링크가 아니다 — `chatgpt.com`·`claude.ai`·`openai.com/chatgpt/pricing`은
봇 차단이지만 사람에게는 열린다. 브라우저로 열어 읽는다.

---

## STEP 1 — 오늘 쓸 두 편을 고른다

```bash
ls src/content/playbook/*/
```

`PLAYBOOK-PLAN.md`의 「첫 노트 여덟」부터 위에서 차례로 집는다. 여덟을 다 쓰면
`playbookPlan.ts`(도구별 주제)가 다음을 정한다. **계획은 이 루틴이 늘리지 않는다** —
매일 도는 루틴이 계획을 고치면 화면의 목표가 매일 흔들린다.

---

## STEP 2 — 쓴다

원고는 `src/content/playbook/<도구 id>/NN-슬러그.md`. 분량은 4,000~7,000자.

```
---
title: ""
description: ""          # 30~160자
kind: ""                 # 설정 · 기법 · 한도 · 비교 · 대질
pubDate: "YYYY-MM-DD"    # TZ='Asia/Seoul' date +%Y-%m-%d
claims: []               # 이 노트가 기대는 주장 id
---
```

**본문에 썩는 값을 적지 않는다.** 요금·한도·컨텍스트 창·모델 id는 한 개도 안 적고
`:claim[claude-pro-price]`로 부른다. 통화 기호·「N 토큰」·소문자 하이픈 모델 id가
본문에 있으면 `npm test`가 선다. 부른 id는 `claims`에도 적는다 — 주장 하나가
만료됐을 때 어느 노트가 흔들리는지 되짚는 유일한 실이다.

**새 값이 필요하면 주장을 먼저 세운다.** 공식 페이지를 열고, 값을 `playbookClaims.ts`에
넣고, 같은 날 확인 로그를 남긴 뒤 본문에서 부른다. 순서를 거꾸로 하면 본문에 숫자가
박힌다.

글 규칙은 학습 글과 같다 — 절 제목은 이름, 굵게는 처음 이름 붙이는 자리에만
(1,000자당 두셋), 낱말은 처음 나오는 자리에서 짚고, 나란히 놓을 줄은 목록이나
역슬래시, 수식은 `$$...$$`.

**현장 통설을 실으려면 넷이 다 있어야 한다** — 단일 게시물 URL, 게시일, 교차 확인,
반례. 하나라도 없으면 싣지 않는다. 한 사람이 한 번 한 말은 일화다.
커뮤니티 글은 인용하지 않고 주장만 한 줄로 다시 쓴다.

---

## STEP 3 — 검증

```bash
npm test
npm run lint
npm run typecheck   # tsc -b — npx tsc --noEmit은 아무것도 검사하지 않는다
npm run build
npm run check:playbook
```

`check:playbook`은 **빌드를 안 세운다.** 넘친 것이 남아 있으면 그대로 두지 말고
STEP 0으로 돌아가 값을 내리거나 확인한다 — 만료가 40%를 넘으면 서랍이 nav에서
스스로 내려간다.

---

## STEP 4 — 커밋

의미 단위로 나눈다. 노트와 데이터를 한 커밋에 섞지 않는다.

```
post: 활용 노트 2편 (YYYY-MM-DD)
data: 활용 가이드 값 N건 확인 (YYYY-MM-DD)
```

```bash
git push "https://x-access-token:${GITHUB_TOKEN}@github.com/paldyn/ai-lab.git" HEAD:main
```

---

## 분기에 한 번 — 구조를 본다

**나이 장치는 값의 나이만 센다.** 벤더가 티어 이름이나 한도 체계를 통째로 갈면
썩는 것은 값이 아니라 **구조**이고, 그때도 화면은 「9일 전 확인」이라고 말한다.
이 구멍은 어떤 검사도 못 막는다.

분기에 한 번, 도구 여섯의 `docsUrl`을 열어 **주장 목록 자체가 아직 말이 되는지** 본다.
티어가 사라졌거나 새 축이 생겼으면 주장을 더하거나 지우고 `PLAYBOOK-PLAN.md`에 남긴다.
