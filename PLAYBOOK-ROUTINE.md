# AI 가이드 루틴

매일 **팁 넷**을 긷고, 값이 묵었으면 그날 한 자리를 갱신에 쓴다.
규칙은 `CLAUDE.md`의 「AI 가이드」 절, 계획과 결정 기록은 `PLAYBOOK-PLAN.md`에 있다.

저장소는 `paldyn/ai-lab`. `main`에 직접 커밋·푸시한다.

**2026-09-17에 이 루틴이 바뀌었다 — 노트를 쓰던 자리에서 팁을 긷는 자리가 됐다.**
그 전에는 하루 두 편씩 `src/content/playbook/`에 원고를 썼다. 그 폴더는 이제 없다.
이 서랍이 파는 것은 글이 아니라 **「어떻게 써야 잘 쓰고 아껴 쓰나」 한 줄들**이고,
읽는 사람이 원하는 것도 4,000자짜리 글이 아니라 그 줄이다.

---

## STEP 0 — 값이 묵었는지 먼저 본다

```bash
npm run check:playbook
```

**넘친 것이 있으면 그날 팁 넷 중 둘 자리를 갱신에 쓴다.** 팁을 둘 덜 긷는 것이
틀린 값을 하루 더 세워 두는 것보다 낫다.

갱신은 이렇게 한다.

1. 짚힌 주장의 `source.url`을 **실제로 연다.** 안 열고 로그만 쓰면 이 장치가 통째로
   거짓이 된다 — `excerpt`를 필수로 둔 이유가 그것이다.
2. 값이 그대로면 `result: '그대로'`, 바뀌었으면 `'바뀜'` + `changedTo`.
   **그날 그 페이지에서 본 원문 한 줄을 `excerpt`에 옮긴다.**
3. 오늘 날짜로 `src/data/playbook-checks/<YYYY-MM-DD>.ts`를 **새로 만든다.**
   기존 파일을 고치지 않는다 — 파일이 늘어야 신선해지는 것이 이 설계의 전부다.
4. 값이 바뀌었으면 `playbookClaims.ts`의 `value`도 함께 고친다.
5. **페이지를 못 열겠거나 값이 없어졌으면 `value: null`로 내린다.**
   그러면 화면에 「모름 · 공식 페이지에서 확인 →」으로 서고, 그것으로 통과다.
   **모른다고 적는 것은 언제나 통과한다. 틀린 값만 막힌다.**

403은 죽은 링크가 아니다 — `chatgpt.com`·`claude.ai`·`openai.com/chatgpt/pricing`은
봇 차단이지만 사람에게는 열린다. 브라우저로 열어 읽는다.

---

## STEP 1 — 오늘 어느 제품에서 길을지 고른다

```bash
npm run check:playbook   # 「팁 N건 (공식 · 현장)」 줄을 본다
```

제품마다 팁이 몇인지는 화면에서 바로 보인다 — `/playbook/<기업>/<제품>`을 열면
「이렇게 쓰면 아낀다」 절의 길이가 그 수다. **가장 얇은 제품부터 집는다.**
아홉 제품이 고르게 차야 레일의 어느 줄을 눌러도 읽을 것이 있다.

무엇을 길을지는 `PLAYBOOK-PLAN.md`의 팁 큐가 정한다. **계획은 이 루틴이 늘리지
않는다** — 매일 도는 루틴이 계획을 고치면 목표가 매일 흔들린다.

---

## STEP 2 — 긷는다

팁 하나는 `src/data/playbookClaims.ts`의 항목 하나다. 원고 파일은 없다.

```ts
{
  id: 'claude-code-tip-19',
  subject: { kind: 'product', id: 'claude-code' },
  topic: 'habit',
  statement: '',          // 무엇을 하라는 한 줄. 8~140자
  detail: '',             // 왜 그런가 한 줄. 팁에만 붙는다
  value: null,            // 팁은 언제나 null이다 — 세는 값이 아니라 바꾸는 행동이다
  tier: 'vendor',         // 또는 'field'
  volatility: 'concept',  // 원리는 안 썩으므로 유효기간이 없다
  source: { label: '', url: '' },
}
```

**문장이 곧 화면이다.** 원고가 없으니 다듬을 자리도 여기뿐이다.

- `statement`는 **읽고 나서 오늘 당장 다르게 할 수 있는 것**을 적는다.
  「컨텍스트를 아끼세요」는 팁이 아니다 — 「새 일을 시작하면 세션을 새로 판다」가 팁이다.
- `detail`은 **왜 그런가 한 줄**이다. 두 줄이 되면 그건 학습 글로 갈 내용이다.
- 값을 적지 않는다. 요금·한도·컨텍스트 창은 값 주장이 따로 들고, 팁에 숫자를 박으면
  그 팁이 값과 같은 속도로 늙는다.

### 공식 팁 (`tier: 'vendor'`)

벤더가 제 문서에 적어 둔 권장이다. `source.url`은 반드시 벤더 호스트여야 하고
(`playbookClaims.test.ts`의 `VENDOR_HOSTS`), **같은 날 확인 로그도 함께 쓴다** —
공식 문서는 인용해도 되므로 그날 본 원문 한 줄을 `excerpt`에 남긴다.

### 현장 팁 (`tier: 'field'`)

**넷이 다 있어야 한다** — 단일 게시물 URL, `postedAt`, `corroboration.note`
(재현·다수 보고·벤더 확인), 그리고 `corroboration.counter`(반례).
반례를 못 찾았으면 「우리 쪽에서는 확인 못 함」이라고 적어야 통과한다.
그 문장이 곧 이 팁이 실측이 아닌 이유다.

- **한 사람이 한 번 한 말은 통설이 아니라 일화다.** 싣지 않는다.
- **커뮤니티 글은 인용하지 않고 주장만 한 줄로 다시 쓴다.** 그래서 현장 팁에는
  확인 로그를 안 쓴다 — 로그의 `excerpt`가 원문 한 줄이라 쓰는 순간 인용이 된다.
  `check:playbook`이 값 없는 주장을 로그에서 빼고 세는 것이 이 때문이다.
- **자기 벤치마크·경쟁사 마케팅 글·별칭을 「고를 수 있다」로 부풀린 것**은 전부 버린다.
  이 셋이 실제로 가장 많이 걸러진 자리다.

**현장이 공식을 넘지 않는다.** `check:playbook`이 팁의 두 등급을 세어 비교한다 —
넘으면 공식 문서에서 더 긷거나 약한 현장 팁을 내린다.

---

## STEP 3 — 검증

```bash
npm test
npm run lint
npm run typecheck   # tsc -b — npx tsc --noEmit은 아무것도 검사하지 않는다
npm run build
npm run check:playbook
```

`check:playbook`은 **빌드를 안 세운다.** 넘친 것이 남아 있으면 그대로 두지 말고
STEP 0으로 돌아가 값을 내리거나 확인한다 — 만료가 40%를 넘으면 서랍이 nav에서
스스로 내려간다.

**화면도 한 번 연다.** 팁은 데이터이자 곧 화면이라 원고 검사가 대신 봐 줄 것이 없다.
`/playbook/<기업>/<제품>`을 열어 새 줄이 벽처럼 보이지 않는지 본다 — 팁 한 칸은
문장 한 줄과 이유 한 줄이다.

---

## STEP 4 — 커밋

의미 단위로 나눈다. 팁과 값 확인을 한 커밋에 섞지 않는다.

```
data: 가이드 팁 4건 (YYYY-MM-DD)
data: AI 가이드 값 N건 확인 (YYYY-MM-DD)
```

```bash
git push "https://x-access-token:${GITHUB_TOKEN}@github.com/paldyn/ai-lab.git" HEAD:main
```

---

## 분기에 한 번 — 구조를 본다

**나이 장치는 값의 나이만 센다.** 벤더가 티어 이름이나 한도 체계를 통째로 갈면
썩는 것은 값이 아니라 **구조**이고, 그때도 화면은 「9일 전 확인」이라고 말한다.
이 구멍은 어떤 검사도 못 막는다.

분기에 한 번, `guideProducts.ts`의 제품들이 든 `officialUrl`·`docsUrl`을 열어 **주장
목록 자체가 아직 말이 되는지** 본다(`docsUrl`이 `null`인 제품은 `officialUrl`만 연다).
티어가 사라졌거나 새 축이 생겼으면 주장을 더하거나 지우고 `PLAYBOOK-PLAN.md`에 남긴다.

**같은 날 팁도 훑는다.** 팁은 `volatility: 'concept'`이라 유효기간이 없는데, 제품이
동작을 바꾸면 팁은 조용히 틀려진다 — 「`/compact`은 자동으로 터지기를 기다리지 말고
직접 친다」는 자동 압축이 좋아지는 순간 반대가 된다. 값과 달리 이걸 잡아 주는 장치가
없으므로 눈으로 본다.

**같은 날 제품 목록도 본다.** 벤더가 제품을 새로 내거나 이름을 갈면 `guideProducts.ts`가
틀려진다 — 그 파일의 이름은 전부 공식 표기 그대로여야 하고, 표면(터미널·IDE·데스크톱)은
별개 제품이 아니라 그 제품의 `surfaces`다.
