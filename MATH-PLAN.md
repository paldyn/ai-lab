# 수학 커리큘럼 계획

`src/data/curriculum.ts`의 순서를 왜 그렇게 잡았는지와, 각 글이 무엇을 다뤄야 하는지를
적어 둔다. 자동 작성 루틴이 글을 쓸 때 이 파일의 `brief`를 근거로 삼는다.

트랙이 셋이고 **슬러그 접두사가 트랙을 정한다.**

| 접두사 | 트랙 | level | 성격 |
| --- | --- | --- | --- |
| `math-basics-` | 기초 23편 | 입문 | 본선을 읽다 막혔을 때 한 편만 꺼내 보는 사전. **순서 없음** |
| `math-` | 본선 80편 | 중급 | 세로 축. 1번부터 아무 선행 없이 시작한다 |
| `math-adv-` | 심화 63편 | 심화 | 본선을 마친 뒤. 논문의 이론 절을 스스로 읽는 것이 목표 |

셋 다 `category: "math-for-ai"`다. **쓰는 순서는 기초 → 본선 → 심화**다.
2026-08-05에 뒤집었다 — 본선부터 쓰니 첫 배치 5편이 전부 `level: 중급`이라
"기초부터라면서 난이도가 높다"는 말이 나왔고, 그 말이 맞다. 뒤집으면 걸리는 것은
링크 하나뿐이며 **아직 쓰지 않은 글로 나가는 본문 링크를 걸지 않는 것**으로 푼다.
자세한 것은 맨 아래 「쓰기 순서」 절에 있다. 화면에 보이는 순서(본선 → 기초 → 심화)는
그대로다 — `curriculumOrder()`의 오프셋은 건드리지 않았다.

## 설계 근거

이전 계획은 144편 중 10편에서 멈췄다. 실패 원인은 분량이 아니라 보상이 늦게 온다는 것이었다. 수의 체계부터 쌓으면 AI가 나오기까지 수십 편이 걸린다. 그래서 두 가지를 동시에 걸었다. 선별은 "이 글이 어떤 AI 동작을 설명하는가"를 한 줄로 못 쓰면 넣지 않는다는 필터 하나로 했고, 순서는 AI 시스템의 흐름을 따르되 앞 글이 뒤 글의 도구를 먼저 정의하도록 강제했다. 그 결과 8편이면 임베딩 검색의 기하가, 31편이면 사전학습 손실의 유도가, 43편이면 어텐션 한 줄이 닫힌다. 어느 절단점에서 멈춰도 그때까지가 완결된 묶음으로 남는다. 편당 개념을 서너 개 묶어 8,000~15,000자를 채우게 했고, 기존 296편과 겹치는 자리에는 "그 글은 무엇을 맡고 이 글은 무엇을 맡는가"를 brief에 못 박아 루틴이 재탕을 쓰지 않게 했다.

## 이전 계획에서 뺀 것

이전 144편에서 64편이 빠졌다. (1) 수 체계·정수·유리수·실수·다항식·인수분해·이차방정식 등 대수 기초 20편 전부 — 독자는 계산을 할 줄 아는 개발자다. 지수·로그는 softmax와 엔트로피에 필요한 만큼 18번 한 편으로 되짚는다. **다만 2026-08-05에 이 묶음에서 셋을 기초 트랙으로 되살렸다** — 좌표평면·직선(계수 벡터가 그 직선의 법선이라는 것과 점-초평면 거리 \|wᵀx+b\|/‖w‖를 가르치는 자리가 없는데 본선 67번의 무작위 초평면 해싱과 기존 ml-svm의 마진 2/‖w‖가 결과만 쓴다), 이차식(본선 12번이 2×2 특성방정식을 풀라 하고 46번이 이차함수에서 학습률 경계를 실험하는데 그 방정식을 푸는 절차와 근과 계수의 관계 ↔ tr A·det A 대응이 어디에서도 유도되지 않는다), 두 변수 함수의 곡면·등고선(본선 8단원 전체가 '손실 지형' 위에서 돌고 32번이 첫 절부터 등고선과 그래디언트가 수직이라 하는데 기초에 2변수 함수를 그려 본 글이 한 편도 없었다). 잘라 낸 판단 자체가 틀린 것은 아니다 — y=mx+b도 통분도 여전히 필요 없다. 틀린 것은 잘린 범위가 '개발자가 실제로 모르는 자리'까지 넘어간 것이고, 되살릴 것은 20편이 아니라 정확히 이 셋이다. 나머지는 되살리지 않는다: 수 체계는 ℝ·ℤ 같은 기호를 기초 기호 사전이, 실수와 부동소수점의 간극을 본선 70번이 맡아 남는 것이 없고, 지수·로그는 본선 18번과 기초 네 편(함수와 그래프·수열과 극한·도함수 규칙·자릿수)이 이미 다섯 겹으로 갖고 있으며, 분수·비율·퍼센트는 기초 자릿수 글과 대수 조작 글이 명시적으로 나눠 갖고 있다. (2) 함수 단원 10편과 삼각함수 7편 — 회전행렬(11번)과 사인파 위치 인코딩(43번)에 필요한 것만 남기고 항등식·사인법칙·역삼각함수·푸리에는 뺐다. (3) 이산수학·논리 12편(집합론·명제·술어논리·증명법·비둘기집·정수론·모듈러) — 증명 훈련과 CS 이론이지 모델 수식을 읽는 데 안 쓴다. 그래프는 GNN 글이 사이트에 한 편도 없어 근거가 없다. (4) 극한의 엡실론-델타, 적분 기법, 이상적분, 급수 수렴 판정 — 적분은 기댓값 정의와 ODE 이산화 자리에서만 등장한다. (5) 가우스 소거·LU·행렬식·역행렬·유사역행렬·선형계획법 — 딥러닝에 Ax=b를 직접 푸는 자리가 없고, 정규화 흐름 글이 없어 log det의 수요도 없다. 고윳값에 필요한 2×2 행렬식만 12번 안에서 정의한다. (6) t검정·분산분석·카이제곱·p값 등 고전 추론통계 6편 — 76번 쌍체 검정·순열 검정 한 편으로 대체했다. 표를 찾는 대신 재표본으로 계산한다. (7) 보간·수치미분 일반론·MCMC·EM·켤레사전분포. 대신 원래 계획에 없던 것을 넣었다. 정렬 수학 7편(Bradley-Terry에서 DPO 손실까지의 유도), 생성 수학 6편(재매개변수화에서 흐름 매칭까지), 평가 통계 7편(pass@k·Elo·보정·멱법칙), 어텐션 고유 수학 4편(√d_k·softmax 야코비안·LSE·RoPE), 고차원 기하 5편, 표기 규약 3편이다. 사이트에 rl-ppo·llm-dpo·llm-rlhf 등 정렬 글 14편과 cv-* 확산 글 9편이 있는데 이전 계획에는 그 밑을 받치는 수학이 한 편도 없었다. 심사가 지적한 네 가지 순서 사고도 전부 고쳤다. 젠센 부등식(29번)을 KL·ELBO 앞에 두었고, 분산(21번)을 √d_k(40번) 앞에, 야코비안(34번)을 softmax 야코비안(41번) 앞에, 직교(7번)를 사영보다 먼저 두었다. RoPE는 복소수 없이 2×2 회전행렬(11번)로만 유도해 오일러 공식 의존을 없앴다.

## 목록 — 80편

### 1단원 · 수식을 읽는 법

> 논문과 코드의 수식 한 줄을 소리 내어 읽고, 기호마다 어떤 텐서가 들어가는지 말할 수 있다. 이후 77편 전부가 이 표기 위에서 돈다.

| # | 슬러그 | 제목 | 무엇을 쓰는가 |
|---:|---|---|---|
| 1 | `math-attention-formula-anatomy` | 어텐션 식 한 줄에 들어 있는 수학 전부: 이 커리큘럼의 지도 | softmax(QK^T/√d_k)V를 왼쪽부터 오른쪽까지 기호 단위로 쪼개고, 각 기호를 읽는 데 필요한 수학이 이 커리큘럼의 몇 번째 글인지 표로 대응시킨다. 내적·행렬곱·softmax·분산·마스킹·미분이 각각 어느 자리에 있는지 표시하는 지도 글이다. 어텐션이 왜 필요한지와 Q·K·V의 직관은 transformer-attention-from-first-principles가 이미 다루므로 여기서는 설명하지 않고, 오직 '이 식을 읽으려면 무엇이 필요한가'만 답한다. 식 자체의 계산과 유도는 40~43번 글에서 끝낸다. |
| 2 | `math-notation-conventions` | 논문 수식 표기 규약: 스칼라·벡터·행렬·텐서를 구별해서 읽기 | x와 굵은 x와 X와 𝒳의 구분, 열벡터 관례, 전치와 위·아래 첨자, 원소 표기와 슬라이스, ⊙·⊗·‖·‖ 같은 연산 기호, f: R^n → R^m 서술의 정의역·공역·상. 논문 첫 페이지의 Notation 절을 사전 없이 읽고 같은 기호가 코드의 어떤 텐서 축에 대응하는지 맞춰 보는 것이 목표다. 기존 296편 어디에도 표기 규약을 다룬 글이 없어 중복 위험이 없다. |
| 3 | `math-sigma-index-and-einsum` | Σ와 첨자, 그리고 einsum: 수식을 그대로 코드로 옮기기 | 시그마·파이 표기, 이중합과 합의 순서 교환, 자유 첨자와 더미 첨자, 아인슈타인 합 규약, 그리고 np.einsum/torch.einsum으로 같은 식을 한 줄로 쓰는 법. 어텐션 점수 QK^T와 합성곱 한 칸을 첨자로 완전히 풀어 쓴 뒤 einsum으로 되돌리는 연습이 중심이다. 논문 수식과 구현 코드의 축을 일대일로 맞추는 능력이 이후 모든 글의 전제다. |

### 2단원 · 벡터: 임베딩 공간의 기하

> 임베딩 검색에서 '가깝다'가 무엇인지 세 지표의 수식과 그 관계로 설명하고, 어떤 지표를 언제 쓸지 계산으로 답한다. 【절단점 1 — 여기까지 8편이면 임베딩 공간의 기하가 완결된다】

| # | 슬러그 | 제목 | 무엇을 쓰는가 |
|---:|---|---|---|
| 4 | `math-vector-as-meaning` | 벡터: 임베딩 한 줄이 숫자 배열이 아니라 벡터인 이유 | 벡터의 정의(덧셈과 스칼라배에 닫힌 대상), 선형결합, 좌표와 화살표 두 관점, 브로드캐스팅으로 본 벡터 연산. 토큰 임베딩과 은닉 상태를 벡터로 다룬다는 것이 무엇을 보장하는지, 'king − man + woman' 같은 임베딩 산술이 왜 선형결합 하나인지를 다룬다. embedding-basics가 임베딩의 쓰임을 다루므로 이 글은 벡터라는 수학 대상만 맡는다. |
| 5 | `math-norms-and-distance` | 노름과 거리: L1·L2·L∞와 단위구의 모양 | 노름의 세 공리, L1·L2·L∞의 정의와 단위구를 그려 비교하기, 노름이 유도하는 거리, 노름 동치. 그래디언트 클리핑이 재는 양(L2 노름), 가중치 감쇠가 벌하는 양, 희소성을 만드는 L1 단위구의 모서리가 각각 어떤 성질에서 나오는지 연결한다. |
| 6 | `math-dot-product-and-cosine` | 내적·코사인·L2 거리는 언제 같은 순위를 주고 언제 갈리는가 | 내적의 정의와 기하적 의미 ‖a‖‖b‖cosθ, 코시-슈바르츠 부등식, 코사인 유사도, 그리고 ‖a−b‖² = ‖a‖² − 2a·b + ‖b‖² 전개. 정규화된 벡터에서 세 지표의 순위가 정확히 일치함을 증명하고, 정규화하지 않으면 순위가 뒤집히는 반례를 직접 만든다. QK^T가 '질의와 키의 관련도'인 이유가 여기서 나온다. vector-similarity-metrics는 지표 선택 실전 가이드이므로 이 글은 그 위층인 동치 조건의 증명만 맡는다. |
| 7 | `math-orthogonality-and-projection` | 직교와 사영: 한 벡터를 다른 벡터로 설명하고 남은 것 | 직교의 정의, 정규직교기저, 한 벡터를 다른 벡터·부분공간 위로 사영하는 공식과 사영 행렬 P = A(AᵀA)⁻¹Aᵀ, 잔차가 부분공간에 직교한다는 최소제곱의 기하. 직교 초기화와 직교 정규화가 무엇을 강제하는지, 그리고 멀티헤드가 '서로 다른 것을 본다'는 말이 직교와 어떻게 다른지(학습된 절단이지 직교 사영이 아니다)를 짚는다. |
| 8 | `math-span-basis-and-coordinates` | 생성·일차독립·기저: 같은 벡터를 다른 축으로 다시 쓰기 | 생성(span), 일차독립, 기저와 차원, 좌표변환 행렬. 임베딩이 실제로 몇 차원을 쓰고 있는가(표현 붕괴)라는 질문을 일차독립으로 정의하고, 주성분 축이나 회전 위치 인코딩처럼 '축을 바꾼다'는 조작이 전부 같은 연산임을 보인다. 수치적으로 독립성을 판정하는 문제는 15번 글(SVD와 랭크)로 넘긴다. |

### 3단원 · 행렬: 선형사상과 분해

> 행렬을 숫자표가 아니라 공간을 옮기는 함수로 읽고, 어텐션 텐서의 shape와 LoRA의 r을 스펙트럼 근거로 설명한다. 【절단점 2 — 여기까지 17편이면 어텐션·LoRA의 행렬 수학이 닫힌다】

| # | 슬러그 | 제목 | 무엇을 쓰는가 |
|---:|---|---|---|
| 9 | `math-matrix-as-linear-map` | 행렬은 함수다: W_Q가 벡터에 하는 일 | 선형사상의 정의, 행렬이 기저의 상(image)을 열에 적어 둔 것이라는 관점, 단위 정사각형이 회전·전단·축소되는 그림, 합성사상이 곧 행렬곱이라는 것. nn.Linear 한 층과 W_Q·W_K·W_V 투영이 공간에 무슨 짓을 하는지 기하로 읽는 습관을 만든다. |
| 10 | `math-matmul-and-shape-arithmetic` | 행렬곱 네 가지 시선과 배치·헤드 shape 산수 | 행렬곱을 원소·행·열·외적의 합 네 관점으로 계산하고, (B, H, T, d_h) 텐서에서 어느 축이 축약되는지 손으로 따라간다. 멀티헤드 어텐션의 reshape·transpose·matmul 순서를 shape만 보고 재구성하고 shape 오류를 실행 전에 예측하는 연습이 중심이다. 3번 글의 einsum 표기를 그대로 쓴다. |
| 11 | `math-rotation-and-2d-linear-maps` | 회전·전단·스케일: 2×2 행렬로 보는 선형변환의 전부 | 단위원에서 사인·코사인을 정의하고 회전행렬 R(θ)를 세운 뒤, R(α)R(β) = R(α+β)를 행렬곱으로 직접 확인해 각도 합 공식이 사실 행렬 항등식임을 보인다. 회전이 내적과 노름을 보존한다는 성질(직교행렬)까지. 복소수와 오일러 공식 없이 진행하며, 이 결과가 43번 RoPE 글의 유일한 전제가 된다. |
| 12 | `math-eigenvalues` | 고윳값과 고유벡터: 방향이 변하지 않는 축 | Av = λv의 정의, 2×2 특성방정식(필요한 행렬식은 그 자리에서 부피 배율로 정의한다), 고유공간, 대각화 A = PΛP⁻¹와 대각화가 안 되는 경우. '스펙트럼', '주축', '고유 방향' 같은 표현을 정확히 읽는 도구이며, 이후 곡률(45번)·공분산(23번)·안정성(13번)에서 반복 등장한다. |
| 13 | `math-matrix-powers-and-power-iteration` | 행렬 거듭제곱과 거듭제곱법: 스펙트럼 반지름이 정하는 것 | A^k가 최대 고윳값의 k제곱에 지배된다는 사실, 스펙트럼 반지름, 그리고 반복 곱셈만으로 최대 고윳값·고유벡터를 구하는 거듭제곱법과 그 수렴 속도가 \|λ₂/λ₁\|로 결정됨. 순환망에서 기울기가 폭발·소실하는 것이 곱해지는 행렬의 스펙트럼 반지름 문제임을 계산으로 보인다. nn-vanishing-gradient는 현상과 해결책을 다루므로 이 글은 '왜 지수적인가'만 맡는다. 스펙트럼 정규화용 σ_max 추정은 17번이 이어받는다. |
| 14 | `math-spectral-theorem-and-quadratic-forms` | 대칭 행렬: 직교 대각화와 이차형식의 부호 | 대칭 행렬이 항상 직교 대각화된다는 스펙트럼 정리, 이차형식 xᵀAx, 양정치·양반정치·부정부호 판정과 고윳값 부호의 대응. AI에서 만나는 행렬(공분산·헤세·그람)이 대부분 대칭인 이유를 짚고, 45번에서 손실 지형의 국소 모양을 판정하는 근거를 여기서 마련한다. **그릇 z=x²+y²과 안장 z=x²−y²의 개형 그림은 기초 `math-basics-surfaces-and-contours`의 것이다** — 이 글은 그 그림을 다시 그리지 않고 고윳값 부호로 그 둘을 판정하는 절차만 맡는다. |
| 15 | `math-svd-and-rank` | 특잇값 분해: 모든 행렬을 회전-늘림-회전으로 쪼개고 랭크를 읽기 | A = UΣVᵀ의 구성과 기하적 의미, 특잇값과 AᵀA 고윳값의 관계, 랭크의 정의와 특잇값 문턱으로 판정하는 수치 랭크, 특잇값 분포를 그려 표현이 실제로 몇 차원을 쓰는지 재는 법. ml-pca가 sklearn PCA 사용법과 설명 분산비를 다루므로 이 글은 그 밑의 선형대수만 맡는다. |
| 16 | `math-low-rank-approximation-and-lora` | 저계수 근사와 에카르트-영: LoRA의 r을 스펙트럼으로 고르기 | 상위 k개 특잇값만 남긴 근사가 프로베니우스·스펙트럼 노름 모두에서 최적이라는 에카르트-영 정리, 잘라 낸 오차가 남은 특잇값의 제곱합이라는 계산, 그리고 ΔW = BA라는 LoRA의 형태를 계수 제약으로 다시 쓰기. r을 감이 아니라 실제 가중치의 특잇값 분포에 근거해 고르는 절차와 α/r 스케일의 의미까지. finetuning-lora는 PEFT 설정법이므로 이 글은 근거만 맡는다. |
| 17 | `math-matrix-norms` | 행렬 노름: 프로베니우스·스펙트럼 노름과 립시츠 상수 | 유도 노름의 정의, ‖A‖₂ = σ_max, 프로베니우스 노름과 특잇값 제곱합의 관계, 부분곱셈성 ‖AB‖ ≤ ‖A‖‖B‖, 그리고 선형층의 립시츠 상수가 곧 스펙트럼 노름이라는 것. 스펙트럼 정규화·그래디언트 클리핑·립시츠 제약이 전부 같은 언어를 쓴다는 것을 보이고, 13번의 거듭제곱법으로 σ_max를 실제로 추정한다. |

### 4단원 · 확률: 모델의 출력은 분포다

> 언어모델이 내놓는 것이 왜 확률분포이고, 그 분포를 어떻게 만들고 어떻게 뽑는지 수식으로 잇는다.

| # | 슬러그 | 제목 | 무엇을 쓰는가 |
|---:|---|---|---|
| 18 | `math-exp-and-log` | 지수와 로그: AI 코드에 log가 도처에 있는 이유 | 지수·로그 법칙, 자연로그와 e, 곱을 합으로 바꾸는 성질, 로그의 단조성(argmax가 보존된다), 그리고 로그 공간 계산이 언더플로를 피하는 방식. logits·log_softmax·log-likelihood·logprob이 왜 전부 로그인지, 확률 수백 개를 곱할 때 fp32에서 무슨 일이 일어나는지 수치로 확인한다. |
| 19 | `math-probability-rules-and-chain-rule` | 확률 규칙: 결합·주변·조건부, 베이즈, 그리고 자기회귀 분해 | 확률의 공리, 결합·주변·조건부 확률과 독립, 베이즈 규칙에서 사전·가능도·사후·증거의 자리, 그리고 확률의 연쇄법칙 p(x₁…x_T) = Π p(x_t\|x_<t). 자기회귀 언어모델이 문장 하나의 확률을 정의하는 방식이 이 분해 그 자체임을 보인다. 사후분포는 61번(확산 역과정)에서 다시 쓴다. |
| 20 | `math-random-variables-and-expectation` | 확률변수와 기댓값: 베르누이·범주형·이항부터 LOTUS까지 | 확률변수의 정의, pmf와 pdf의 차이(밀도는 1을 넘을 수 있다), cdf, 베르누이·범주형·이항 분포와 각각이 AI 어디에 쓰이는지(드롭아웃·토큰 샘플링·정답 개수), 기댓값의 정의와 선형성, LOTUS. '손실은 데이터 분포 위의 기댓값'이라는 문장을 실제로 계산할 수 있게 되는 것이 목표다. |
| 21 | `math-variance-and-sampling-error` | 분산과 표준오차: 미니배치는 추정량이다 | 분산·표준편차·공분산의 정의, 독립 합의 분산 가법성, 표본평균의 분산이 σ²/n이라는 결과와 대수의 법칙, 불편 추정의 n−1. 배치 크기가 그래디언트 잡음과 평가 점수의 흔들림을 동시에 좌우하는 이유가 전부 이 한 식에서 나오며, 40번(√d_k)·49번(배치와 학습률)·74번(평가 오차막대)이 이 결과를 그대로 재사용한다. |
| 22 | `math-gaussian-and-clt` | 정규분포가 어디에나 있는 이유: 성질과 중심극한정리 | 정규분포의 밀도, 선형변환에 닫혀 있음, 독립 합의 재생성, 표준화, 68-95-99.7 규칙, 그리고 중심극한정리와 그 수렴 속도를 시뮬레이션으로 확인하기. 가중치 초기화·확산 노이즈·GELU·평가 신뢰구간에서 정규분포가 반복 등장하는 근거를 한 번에 마련한다. |
| 23 | `math-covariance-and-multivariate-gaussian` | 공분산 행렬과 다변량 정규분포: 상관 있는 표본 만들기 | 공분산 행렬의 정의와 양반정치성, 상관계수, 다변량 정규분포의 밀도와 등고선이 타원인 이유, 그리고 촐레스키 분해(대칭 양정치 행렬의 제곱근)를 그 자리에서 세워 z ~ N(0,I)에서 상관 있는 표본 Lz를 만드는 법. 14번 스펙트럼 정리를 재사용하며, 59번(확산 노이즈)과 68번(임베딩 이방성)이 이 도구를 다시 쓴다. |
| 24 | `math-softmax-derivation` | softmax 유도: 점수 벡터를 확률로 바꾸는 함수는 왜 그 모양인가 | 실수 벡터를 확률분포로 보내는 함수가 만족해야 할 조건(양수·정규화·순서 보존·평행이동 불변)을 세우고 exp가 강제되는 과정을 유도한다. 로짓을 로그 확률로 읽는 관점, 이진 분류에서 sigmoid로 환원됨, 온도 파라미터가 분포에 하는 일, 상수를 더해도 값이 같다는 성질까지. 분류 출력·어텐션 가중치·MoE 라우팅이 전부 같은 함수 하나를 통과한다. llm-temperature-top-k-top-p는 튜닝 가이드이므로 이 글은 함수 자체의 유도만 맡는다. |
| 25 | `math-sampling-from-categorical` | 범주분포에서 뽑기: 온도·top-k·top-p·Gumbel-max의 수학 | 역변환 표집으로 범주분포에서 뽑는 법, 온도·top-k·top-p가 확률질량의 어느 부분을 자르고 어떻게 재정규화하는지 그림과 식으로, 그리고 Gumbel-max 트릭(로짓에 Gumbel 잡음을 더해 argmax를 취하면 정확히 softmax 샘플링과 같다)의 증명. 이산 선택을 미분 가능하게 만드는 Gumbel-softmax까지 이어 붙인다. llm-sampling-strategies·llm-decoding-methods는 전략 비교이므로 이 글은 수학과 등가성 증명만 맡는다. |

### 5단원 · 정보: 손실 한 줄이 곧 정보량이다

> 사전학습 손실 한 줄이 어디서 나왔는지 끝까지 유도하고, perplexity·KL 페널티·증류 손실·대조 손실이 같은 틀에서 나온 값임을 안다. 【절단점 3 — 여기까지 31편이면 '무엇을 최소화하는가'가 닫힌다】

| # | 슬러그 | 제목 | 무엇을 쓰는가 |
|---:|---|---|---|
| 26 | `math-entropy-and-perplexity` | 엔트로피와 정보량: perplexity가 재고 있는 것 | 정보량 −log p, 엔트로피 H(p) = −Σ p log p, 비트와 내트, 균등분포에서 최대가 되는 이유의 증명, 그리고 perplexity = exp(H)가 '평균적으로 몇 개 중에 고민하는가'라는 해석. loss 2.1과 perplexity 8.2가 같은 숫자의 다른 표현임을 확인하고, bits-per-byte와 '언어모델은 압축기다'라는 주장을 수치로 읽는다. |
| 27 | `math-maximum-likelihood` | 최대가능도: '학습'이라는 말의 가장 정확한 정의 | 가능도와 확률의 차이, 로그가능도, i.i.d. 가정에서 합으로 분해되는 과정, 가능도 곡면을 그려 최댓값을 찾는 실험. 정규분포 가정에서 MSE가, 범주분포 가정에서 교차엔트로피가 떨어져 나오는 유도를 끝까지 해서 손실함수가 임의로 정해진 것이 아님을 보인다. ai-loss-functions는 손실 카탈로그이므로 이 글은 출처의 유도만 맡는다. |
| 28 | `math-cross-entropy-and-nll` | 교차엔트로피 = 음의 로그가능도: 사전학습 손실 한 줄의 유도 | 교차엔트로피 H(p,q)의 정의, 원-핫 정답에서 −log q_target으로 줄어드는 계산, 그것이 27번의 음의 로그가능도와 정확히 같은 식임을 확인. 라벨 스무딩이 목표 분포 p를 바꾸는 조작이라는 관점, 토큰 단위 평균 손실이 무엇의 추정량인지, 그리고 손실 값과 perplexity·bits-per-token의 환산까지. 이 글로 다음 토큰 예측 손실 한 줄의 유도가 완결된다. |
| 29 | `math-jensen-inequality` | 젠센 부등식: 볼록함수와 기댓값의 순서 | 볼록함수의 정의(현이 그래프 위에 있다)와 이계도함수 판정, 젠센 부등식 f(E[X]) ≤ E[f(X)]의 증명과 등호 조건. 이것으로 KL ≥ 0을 증명하고, '구하기 어려운 로그가능도 대신 하한을 최대화한다'는 발상의 뿌리를 놓는다. 여기서 정의한 볼록성을 44번(손실 지형)과 60번(ELBO)이 그대로 확장해 쓴다. 기존 296편에 젠센 부등식을 다룬 글이 없다. |
| 30 | `math-kl-divergence` | KL 발산: 거리가 아닌 거리와 방향의 선택 | KL(p‖q)의 정의, 교차엔트로피 − 엔트로피 분해, 젠센으로 얻은 비음수성, 비대칭성과 삼각부등식 실패, 그리고 정방향 KL이 분포를 넓게 덮게 하고 역방향 KL이 한 봉우리에 몰리게 하는 mode-covering/mode-seeking 그림. JS 발산도 여기서 정의한다. RLHF의 KL 페널티·증류 손실·변분 하한이 모두 같은 양임을 확인한다. llm-dpo·llm-rlhf·distillation은 기법이므로 이 글은 정의와 성질만 맡는다. |
| 31 | `math-mutual-information` | 상호정보량: 두 변수가 공유하는 정보와 InfoNCE 하한 | I(X;Y) = H(X) − H(X\|Y) = KL(p(x,y)‖p(x)p(y))의 세 표현이 같음을 보이고, 표본만으로는 추정이 어렵다는 사실, 그리고 InfoNCE가 상호정보량의 하한이라는 유도 — 배치 크기 N이 하한의 천장 log N을 정한다는 결론까지. 대조학습이 실제로 최대화하는 양이 무엇이며 왜 배치가 커야 하는지에 답한다. cv-clip은 CLIP 학습 방식이므로 이 글은 목적식의 근거만 맡는다. |

### 6단원 · 미분: 역전파는 연쇄법칙 한 줄이다

> 층 하나의 그래디언트를 shape까지 맞춰 종이 위에서 유도하고, 그 유도가 맞는지 유한차분으로 검증한다. 【절단점 4 — 여기까지 39편이면 '어떻게 최소화하는가'가 닫힌다】

| # | 슬러그 | 제목 | 무엇을 쓰는가 |
|---:|---|---|---|
| 32 | `math-derivative-and-gradient` | 도함수는 민감도다: 국소 선형근사에서 그래디언트까지 | 도함수를 극한이 아니라 '최선의 선형근사'로 정의하고, 편미분, 그래디언트 벡터, 방향도함수, −∇f가 가장 가파른 내리막인 이유의 증명(코시-슈바르츠), 등고선과 그래디언트가 수직인 이유. 파라미터 수십억 개에 대한 미분이 벡터 하나로 정리되는 관점을 만든다. |
| 33 | `math-chain-rule` | 연쇄법칙: 층을 통과하며 곱해지는 비율 | 합성함수의 미분, 다변수 연쇄법칙(경로마다 곱하고 경로끼리 더한다), 계산 그래프 위에서 손으로 끝까지 따라가는 예제, 그리고 곱이 길어질 때 값이 지수적으로 커지거나 작아진다는 관찰. 역전파의 유일한 원리다. nn-forward-backward는 PyTorch autograd 알고리즘과 학습 루프를 다루므로 이 글은 법칙 자체만 맡는다. |
| 34 | `math-jacobian` | 야코비안: 벡터함수의 미분은 행렬이다 | R^n → R^m 함수의 야코비안 정의와 배치, 선형사상 근사로서의 의미, 합성함수의 야코비안이 야코비안의 곱이라는 사실, 야코비안 행렬식이 부피 배율이라는 것과 변수변환에서 밀도가 바뀌는 이유. 층 하나의 미분이 행렬이라는 사실이 역전파 비용 계산의 단위가 된다. 기존 296편에 '야코비안'이라는 말이 한 번도 나오지 않는 완전한 안전지대다. |
| 35 | `math-vjp-and-jvp` | VJP와 JVP: 딥러닝이 역방향으로 미분하는 이유를 연산량으로 증명하기 | 벡터-야코비안 곱과 야코비안-벡터 곱의 정의, 전방 모드와 역방향 모드가 각각 무엇을 한 번에 계산하는지, 입력 n·출력 m일 때 비용이 각각 n회와 m회의 통과라는 계산. 손실이 스칼라(m=1)이고 파라미터가 수십억(n이 큼)이라는 형태가 역방향을 강제한다는 결론까지. autograd가 야코비안 행렬을 실제로 만들지 않고도 역전파하는 이유가 여기서 나온다. |
| 36 | `math-matrix-calculus` | 행렬 미분: 규약을 정하고 shape로 검산하기 | 분자 배치와 분모 배치 규약(논문마다 전치가 달라 보이는 이유), ∂(Wx)/∂x = W, ∂L/∂W = δxᵀ, ∂(xᵀAx)/∂x = (A+Aᵀ)x 같은 핵심 공식의 유도, 그리고 공식을 외우는 대신 shape가 맞는 유일한 조합을 찾아 복원하는 기술. 논문의 그래디언트 식을 그대로 코드로 옮기는 실무 능력이 목표다. |
| 37 | `math-softmax-cross-entropy-gradient` | softmax와 교차엔트로피의 기울기가 정확히 p − y인 이유 | softmax의 야코비안 diag(p) − ppᵀ를 유도하고, 교차엔트로피와 합성했을 때 그 행렬이 깨끗하게 소거되어 ∂L/∂z = p − y가 되는 과정을 끝까지 계산한다. 두 함수를 왜 항상 붙여서 구현하는지(수치적 이유 포함), 그리고 모든 분류 모델의 마지막 층에서 실제로 흐르는 기울기가 무엇인지. 41번의 어텐션 그래디언트가 이 결과를 그대로 쓴다. |
| 38 | `math-activation-derivatives` | 활성함수의 미분: GELU가 x·Φ(x)인 이유 | ReLU·LeakyReLU·sigmoid·tanh·GELU·SiLU의 도함수 유도와 그래프, 포화 구간에서 기울기가 0에 붙는다는 계산, GELU가 표준정규 CDF Φ와 입력의 곱으로 정의되는 근거(확률적 게이팅)와 tanh 근사식의 오차, ReLU가 0에서 미분 불가능한 것이 실제로 문제가 되는지. nn-activation-functions는 선택 가이드이므로 이 글은 미분과 유도만 맡는다. |
| 39 | `math-gradient-checking` | 유도가 맞는지 확인하기: 유한차분, 오차 차수, 기울기 검사 | 전진차분과 중심차분의 절단오차 차수 O(h)·O(h²)를 테일러로 유도하고, 반올림오차와 만나는 최적 스텝 h를 로그-로그 그래프로 찾는 실험, 상대오차 기준과 허용 오차 설정, 그리고 직접 유도한 기울기를 검사하는 절차. 이 글이 정한 검증 규약을 이후 모든 유도 글이 마지막 절에 붙여 쓴다. |

### 7단원 · 어텐션의 수학

> 1단원에서 지도로만 봤던 어텐션 식을 이제 기호 하나하나 계산과 유도로 닫는다. 【절단점 5 — 여기까지 43편이면 어텐션 한 줄이 완전히 해부된다】

| # | 슬러그 | 제목 | 무엇을 쓰는가 |
|---:|---|---|---|
| 40 | `math-scaling-by-sqrt-dk` | √d_k는 어디서 나왔나: 내적의 분산 계산 | q와 k의 성분이 평균 0·분산 1로 독립이라 가정하면 q·k의 분산이 정확히 d_k가 된다는 계산(21번의 독립 합 분산 가법성). 분산이 커질수록 softmax 입력의 스케일이 커져 분포가 한 점에 몰리고, 37번에서 유도한 기울기와 41번의 야코비안이 함께 0으로 죽는다는 연결. 스케일링을 빼면 학습이 멈추는 현상을 작은 실험으로 재현하고, 초기화 스케일·LayerNorm과 역할이 어떻게 나뉘는지까지 정리한다. |
| 41 | `math-softmax-jacobian-and-attention-gradient` | 어텐션의 그래디언트: softmax 야코비안이 분포에 따라 하는 일 | 37번의 diag(p) − ppᵀ를 어텐션 가중치에 적용해 ∂L/∂Q, ∂L/∂K, ∂L/∂V를 shape까지 맞춰 끝까지 유도한다. 분포가 뾰족할수록 야코비안의 값이 0에 가까워져 어텐션이 한 토큰에 몰리면 학습 신호가 죽는다는 결론, 그리고 잔차 연결이 이 경로를 우회하는 이유. 3~6단원의 도구를 한자리에 합치는 캡스톤이다. |
| 42 | `math-log-sum-exp-and-online-softmax` | log-sum-exp: 최댓값 빼기, −inf 마스킹, 그리고 온라인 softmax | LSE 항등식 log Σexp(x_i) = m + log Σexp(x_i − m)의 증명과 오버플로·언더플로 방지, 마스킹을 왜 0을 곱하지 않고 −inf를 더해서 하는지(그리고 한 행이 전부 −inf일 때 NaN이 나오는 이유), 그리고 부분합 두 개의 (max, sum)을 이어 붙이는 점화식으로 softmax를 스트리밍 계산하는 유도. transformer-flash-attention은 IO 병목과 타일링 관점이므로 이 글은 그 밑의 대수만 맡는다. |
| 43 | `math-positional-encoding-math` | 위치를 수로 적기: 사인파 주파수 사다리와 RoPE의 회전 불변성 | 정수 위치를 주파수가 기하급수적으로 감소하는 사인·코사인 쌍으로 적는 이유와 파장이 만드는 해상도 사다리, 그리고 RoPE가 차원쌍마다 11번의 회전행렬 R(mθ)를 적용했을 때 내적 ⟨R(mθ)q, R(nθ)k⟩가 오직 (m−n)에만 의존한다는 증명. 상대 위치 불변성이 회전행렬의 직교성에서 그냥 따라 나온다는 것을 보인다. transformer-positional-encoding과 transformer-rotary는 구현과 외삽 문제를 다루므로 이 글은 유도만 맡는다. |

### 8단원 · 최적화: 손실 지형을 내려가기

> 학습률·배치 크기·워밍업·Adam 하이퍼파라미터를 곡률과 그래디언트 잡음으로 설명하고 값을 근거 있게 고른다. 【절단점 6 — 여기까지 51편이면 학습 루프의 모든 손잡이가 설명된다】

| # | 슬러그 | 제목 | 무엇을 쓰는가 |
|---:|---|---|---|
| 44 | `math-loss-landscape-and-convexity` | 손실 지형: 딥러닝은 볼록이 아닌데 왜 학습이 되는가 | 29번에서 정의한 볼록성을 다변수로 확장(볼록집합, 헤세 양반정치 판정), 지역해가 전역해가 되는 조건, 그리고 신경망 손실이 왜 비볼록인지(뉴런 치환 대칭성). 고차원에서 진짜 장애물이 국소최소가 아니라 안장점과 평지라는 주장을 고윳값 부호가 전부 같을 확률로 설명한다. |
| 45 | `math-taylor-hessian-and-curvature` | 테일러 2차 근사와 헤세 행렬: 곡률, 안장점, 조건수 | 다변수 테일러 전개, 헤세 행렬, 2차 판정(14번의 이차형식 부호 재사용), 헤세의 고윳값이 각 방향의 곡률이라는 해석, 그리고 조건수 λ_max/λ_min이 등고선을 얼마나 길쭉하게 만드는지. '날카로운 최소점', '평평한 최소점', '잘 조건화된 문제' 같은 표현을 정확히 읽는 도구이며 다음 세 글이 전부 이 결과를 쓴다. |
| 46 | `math-gradient-descent-and-lr-bound` | 경사하강법 유도와 학습률 상한이 2/L인 이유 | 1차 근사에서 경사하강 갱신식을 유도하고, L-매끄러움(그래디언트의 립시츠 상수)을 정의한 뒤 2차 상계로 손실이 반드시 줄어드는 구간 0 < η < 2/L을 유도한다. 이차함수에서 η를 키울 때 수렴 → 진동 → 발산으로 넘어가는 정확한 경계를 실험으로 재현하고, 조건수가 나쁘면 수렴이 몇 배 느려지는지 계산한다. |
| 47 | `math-momentum-and-ema` | 모멘텀·네스테로프·EMA는 같은 점화식이다 | 지수이동평균 v ← βv + (1−β)g의 점화식과 유효 평균 창 1/(1−β), 모멘텀이 EMA된 그래디언트로 움직이는 것이라는 관점, 네스테로프의 미리보기 항, 그리고 좁은 골짜기에서 모멘텀이 얻는 이득을 조건수의 함수로 계산하기. 옵티마이저 모멘텀·EMA 체크포인트·BatchNorm 러닝 통계가 전부 같은 식이라는 것을 확인한다. |
| 48 | `math-adam-from-moments` | 1차·2차 모멘트에서 Adam을 직접 조립하기 | 그래디언트의 1차·2차 모멘트를 EMA로 추정하고 m̂/(√v̂+ε)로 나누는 갱신식을 처음부터 조립한다. 편향 보정 1/(1−βᵗ)이 왜 필요한지의 유도, β₁·β₂·ε가 각각 무엇을 조절하는지, 왜 이것이 대각선 프리컨디셔너인지, 그리고 AdamW의 분리형 가중치 감쇠가 L2 정규화 항과 수학적으로 어디서 갈리는지. ai-regularization은 정규화 기법 소개이므로 이 글은 갱신식만 맡는다. |
| 49 | `math-sgd-noise-batch-and-schedule` | 그래디언트 잡음이 정하는 것: 배치 크기, 선형 스케일링, 워밍업, 코사인 감쇠 | 미니배치 그래디언트가 불편 추정량이고 그 분산이 σ²/B라는 계산(21번), 배치를 k배 키울 때 학습률을 얼마나 올려야 잡음 대비 신호가 유지되는지(선형 스케일링 법칙과 그것이 깨지는 지점), 초반 그래디언트 통계가 불안정해 워밍업이 필요한 이유, 그리고 코사인·역제곱근 감쇠의 식과 후반 미세 수렴에서의 역할. |
| 50 | `math-weight-init-variance` | 초기화 분산 전파: Xavier·He 유도와 폭 스케일링 | 한 층을 통과할 때 활성값의 분산이 어떻게 변하는지 유도하고, 순전파와 역전파 양쪽에서 분산이 유지되는 조건에서 Xavier와 He 초기화를 얻는다. 깊이가 늘 때 분산이 지수적으로 어긋나는 계산, 잔차 연결과 정규화가 그것을 어떻게 막는지, 그리고 폭이 커질 때 학습률을 어떻게 스케일해야 하는가(muP의 출발점). nn-weight-init은 구현과 실험이므로 이 글은 유도만 맡는다. |
| 51 | `math-newton-and-second-order` | 뉴턴법과 2차 방법: 왜 안 쓰는가, 요즘은 무엇을 근사하는가 | 뉴턴 갱신 H⁻¹∇f의 유도와 이차수렴, 그러나 파라미터 n개에서 H가 n²개 항목이고 역이 n³ 연산이라는 비용 계산, 헤세를 만들지 않고 헤세-벡터 곱만으로 2차 정보를 쓰는 방법. 그리고 Adam의 대각 근사, K-FAC의 크로네커 근사, Shampoo·Muon 계열이 각각 헤세(또는 피셔 정보)의 무엇을 근사하고 있는지 정리한다. |

### 9단원 · 정렬: 제약이 걸린 최적화

> PPO 목적식과 DPO 손실을 각각 유도하고, 왜 둘이 같은 문제의 두 해법인지 설명한다. 사이트에 정렬 글이 14편 있는데 그 밑의 수학이 한 편도 없던 자리다. 【절단점 7 — 여기까지 58편이면 정렬 논문의 수식이 열린다】

| # | 슬러그 | 제목 | 무엇을 쓰는가 |
|---:|---|---|---|
| 52 | `math-expected-reward-objective` | 정책 위의 기댓값: '좋은 답을 내게 한다'를 최적화 문제로 적기 | 정책 π_θ, 궤적, 그리고 J(θ) = E_{y~π_θ}[r(y)]라는 목적식을 세우는 과정. 지도학습의 손실과 달리 기댓값을 취하는 분포 자체가 θ에 의존한다는 점이 왜 문제인지(그래디언트를 기댓값 안으로 그냥 넣을 수 없다), 그리고 그 문제가 다음 글의 출발점이 된다는 것. rl-basics·rl-policy-gradient는 강화학습 전반을 다루므로 이 글은 목적식의 형식과 난점만 맡는다. |
| 53 | `math-log-derivative-trick` | 로그 미분 트릭: 샘플만으로 그래디언트를 얻기 | ∇_θ E_{x~p_θ}[f(x)] = E_{x~p_θ}[f(x)∇_θ log p_θ(x)]의 유도와 성립 조건(적분과 미분의 교환), 이것이 불편 추정량이라는 확인, 그리고 왜 분산이 큰지. 재매개변수화 트릭(59번)과 무엇이 다르며 이산 토큰에서는 왜 로그 미분 트릭밖에 없는지까지. rl-policy-gradient는 REINFORCE 구현이므로 이 글은 항등식과 그 성립 조건만 맡는다. |
| 54 | `math-baseline-and-advantage` | 베이스라인과 어드밴티지: 편향 없이 분산만 줄이기 | E[b·∇log p] = 0이라는 계산으로 어떤 상수(또는 상태에만 의존하는) 베이스라인도 편향을 만들지 않음을 증명하고, 분산을 최소로 만드는 베이스라인을 유도한다. 어드밴티지 A = r − b의 의미, 그리고 GRPO가 그룹 평균을 빼는 것이 왜 이 정리의 직접 적용인지. rl-actor-critic·reasoning-rl-training은 알고리즘이므로 이 글은 증명만 맡는다. |
| 55 | `math-bradley-terry-preference` | 선호 쌍에서 보상 모델 손실 유도하기 | Bradley-Terry 모델 P(A ≻ B) = σ(r_A − r_B)를 세우고, 27번의 최대가능도를 적용해 −log σ(r_w − r_l)이라는 보상 모델 손실을 얻는 전 과정. 보상이 상수 평행이동에 불변이라 절대값에는 의미가 없다는 성질, 그리고 비교 데이터 n개로 추정할 때 개별 보상의 불확실성이 얼마나 되는지. 78번 Elo가 정확히 이 모델을 다시 쓴다. |
| 56 | `math-lagrange-and-kkt` | 라그랑주 승수와 KKT: 제약을 목적식 안으로 넣기 | 등식 제약에서 ∇f = λ∇g가 되는 기하적 이유, 라그랑지안, 부등식 제약에서의 KKT 조건과 상보 여유, 그리고 승수가 '제약을 한 단위 풀었을 때의 이득'이라는 해석. 'KL 예산 안에서 보상을 최대화한다'를 식 하나로 쓰는 법을 익히고 다음 글이 그것을 실제로 푼다. 기존 296편에 라그랑주·KKT를 다룬 글이 없다. |
| 57 | `math-kl-constrained-optimum-and-dpo` | KL 제약 보상 최대화의 닫힌 해, 그리고 거기서 떨어져 나오는 DPO 손실 | max_π E[r] − βKL(π‖π_ref)의 해가 π*(y\|x) ∝ π_ref(y\|x)·exp(r(x,y)/β)라는 볼츠만 형태임을 유도하고, 이 식을 r에 대해 뒤집어 r = β log(π/π_ref) + log Z를 얻은 뒤 55번의 Bradley-Terry에 대입하면 분배함수 Z가 소거되며 DPO 손실이 나오는 과정을 끝까지 따라간다. β가 조절하는 것이 무엇인지도 여기서 답한다. llm-dpo는 실무 가이드와 변형 소개이므로 이 글은 유도만 맡는다. |
| 58 | `math-importance-ratio-and-clipping` | 중요도 비율과 클리핑: 예전 정책의 샘플을 재사용하는 대가 | 중요도 표집으로 다른 분포의 샘플에서 기댓값을 추정하는 식과 그 불편성, 비율 r(θ) = π_θ/π_old의 분산이 두 분포가 멀어질수록 폭발한다는 계산과 유효 표본 수, 그리고 PPO의 clip이 목적함수를 조각별 선형으로 바꿔 한 걸음을 제한하는 방식. 신뢰 영역과 KL 조기 중단이 막으려는 붕괴가 정확히 무엇인지까지. rl-ppo는 알고리즘 구현이므로 이 글은 추정량의 성질만 맡는다. |

### 10단원 · 생성: 노이즈를 푸는 미분방정식

> 확산의 forward·reverse를 식으로 쓰고, 샘플링 스텝 수와 guidance scale이 결과를 바꾸는 이유를 계산으로 설명한다.

| # | 슬러그 | 제목 | 무엇을 쓰는가 |
|---:|---|---|---|
| 59 | `math-reparameterization-and-forward-process` | 재매개변수화 트릭과 폐형 forward 과정 | x = μ + σz라는 한 줄로 샘플링을 미분 가능하게 만드는 트릭과, 이것이 53번의 로그 미분 트릭보다 분산이 작은 이유. 그리고 가우시안을 t번 더한 결과가 다시 가우시안이라는 성질(22번)을 써서 q(x_t\|x_0)를 한 번에 계산하는 폐형 유도 — 이 폐형이 있어야 임의의 t에서 학습이 가능해진다. VAE와 확산이 같은 장치를 공유한다는 것도 여기서 확인한다. |
| 60 | `math-elbo` | ELBO: 못 구하는 로그가능도를 아래에서 받치기 | log p(x) = ELBO + KL(q‖p)라는 분해를 29번의 젠센 부등식과 30번의 KL 정의 두 가지 경로로 유도하고, 하한을 최대화하는 것이 왜 정당한지, 재구성 항과 정규화 항이 어떻게 갈리는지, 하한이 얼마나 헐거운지를 무엇이 정하는지. VAE와 확산의 손실이 같은 뼈대 위에 있다는 것을 보이고 EM도 같은 발상임을 짚는다. 기존 296편에 ELBO·변분 하한을 다룬 글이 없다. |
| 61 | `math-score-matching` | 스코어 ∇log p: '노이즈를 예측한다'가 곧 스코어 추정인 이유 | 스코어 함수 ∇_x log p(x)의 정의와 정규화 상수가 미분에서 사라진다는 결정적 성질, 가우시안에서 스코어가 −(x−μ)/σ²라는 계산, 그리고 노이즈 예측 ε_θ와 스코어가 상수배로 이어진다는 denoising score matching의 유도. cv-diffusion-basics는 U-Net 구조와 DDPM 구현을 다루므로 이 글은 목적식의 정체만 맡는다. |
| 62 | `math-sde-ode-and-discretization` | SDE·ODE와 이산화 오차: 스텝 수가 품질을 정한다 | 확산을 연속시간 SDE로 적고 대응하는 확률 흐름 ODE를 세운 뒤, 오일러 이산화의 국소·전역 오차 차수를 유도한다. 20스텝과 1000스텝의 차이가 어디서 오는지, DDIM이 왜 결정적이며 더 적은 스텝으로도 되는지, 고차 solver가 무엇을 벌어들이고 무엇을 대가로 치르는지 계산으로 답한다. |
| 63 | `math-guidance-as-extrapolation` | classifier-free guidance는 스코어 공간의 외삽이다 | ε̃ = ε_uncond + w(ε_cond − ε_uncond)라는 식을 세우고, 이것이 조건부 분포를 p(y\|x)^w로 기울이는 것과 같음을 스코어 공간에서 유도한다. w를 올리면 왜 선명해지다가 어느 지점부터 채도와 다양성이 무너지는지를 분포의 꼬리를 잘라 내는 효과로 설명한다. cv-controlnet·cv-stable-diffusion은 사용법이므로 이 글은 유도만 맡는다. |
| 64 | `math-flow-matching-and-optimal-transport` | 흐름 매칭과 최적수송: 확률 경로를 직선으로 펴기 | 두 분포를 잇는 확률 경로와 그것을 만드는 속도장, 조건부 흐름 매칭 목적식의 유도, 그리고 직선 경로가 왜 적은 스텝으로도 잘 도는지. 바서슈타인 거리와 최적수송의 최소한(왜 KL이 아니라 수송 비용을 재는가, KL이 정의되지 않는 경우)을 여기서 정의하고, 62번의 ODE 관점과 어떻게 이어지는지 정리한다. 기존 296편에 흐름 매칭·최적수송을 다룬 글이 없다. |

### 11단원 · 고차원과 검색

> 임베딩 차원·유사도·인덱스 선택을 감이 아니라 고차원 기하와 충돌 확률로 결정한다.

| # | 슬러그 | 제목 | 무엇을 쓰는가 |
|---:|---|---|---|
| 65 | `math-high-dimensional-geometry` | 고차원에서 직관이 무너지는 지점: 거리 집중과 부피 쏠림 | d차원 초구의 부피가 껍질에 몰리는 계산, 무작위 두 벡터가 거의 항상 직교에 가깝다는 결과, 그리고 최근접 이웃과 최원거리 이웃의 거리 비가 1로 수렴하는 거리 집중 현상을 시뮬레이션으로 확인한다. 1024차원 임베딩 공간이 3차원과 다르게 행동하는 방식이 검색 품질에 어떻게 나타나는지, 그리고 언제 근사 검색으로 충분하고 언제 검색 자체가 무의미해지는지. |
| 66 | `math-random-projection-and-jl` | 랜덤 사영과 존슨–린덴스트라우스 보조정리 | 무작위 사영이 모든 쌍의 거리를 (1±ε) 안에서 보존하는 데 필요한 차원이 k = O(log n / ε²)이라는 결과와 유도 개요, 점 개수 n에는 로그로만 의존하고 원래 차원에는 아예 의존하지 않는다는 사실. 임베딩 차원을 줄여도 되는 이유이며, LSH와 Matryoshka 임베딩이 딛고 선 보장이 여기다. |
| 67 | `math-lsh-collision-probability` | 무작위 초평면 해싱: 충돌 확률을 유도하고 recall을 확률로 읽기 | 무작위 초평면 하나로 두 벡터가 같은 쪽에 놓일 확률이 1 − θ/π라는 유도, 비트 k개와 테이블 L개를 쓸 때의 1 − (1−p^k)^L 곡선, 그리고 그 곡선에서 recall과 후보 수의 트레이드오프를 읽는 법을 시뮬레이션과 대조한다. vector-ann-algorithms는 HNSW·IVF·LSH 알고리즘 비교이므로 이 글은 확률 유도만 맡는다. |
| 68 | `math-anisotropy-and-whitening` | 임베딩 분포의 이방성: 공분산으로 재고 whitening으로 펴기 | 23번의 공분산 행렬로 임베딩이 좁은 원뿔에 몰리는 현상을 고윳값 분포로 측정하고(설명 분산비, 이방성 지표), 평균 제거와 whitening 변환 Σ^(−1/2)이 코사인 유사도 분포를 어떻게 바꾸는지 계산한다. 유사도가 전부 0.8 근처에 몰려 순위가 흐려지는 문제의 정체와, 후처리가 무엇을 대가로 치르는지까지. |
| 69 | `math-kernels-and-gram-matrices` | 커널과 그람 행렬: 내적을 바꾸면 무엇이 달라지나 | 그람 행렬의 정의와 양반정치성, 커널이 암묵적 특징공간의 내적이라는 Mercer 관점, RBF·다항 커널과 그 특징공간, 그리고 어텐션 점수 행렬도 (비대칭이라는 차이는 있지만) 같은 자리에 있는 유사도 행렬이라는 관점. ml-svm은 커널 트릭 사용법이므로 이 글은 그람 행렬의 성질과 PSD 판정만 맡는다. |

### 12단원 · 유한한 기계의 수학

> bf16·fp8·int4에서 무엇이 언제 깨지는지 미리 예측하고, 모델의 연산량을 손으로 센다.

| # | 슬러그 | 제목 | 무엇을 쓰는가 |
|---:|---|---|---|
| 70 | `math-floating-point-and-cancellation` | 부동소수점은 어디서 새는가: 반올림, 상쇄, 더하는 순서 | 부호·지수·가수 구조에서 나오는 상대오차 상한(머신 엡실론), 같은 16비트인데 bf16과 fp16이 왜 서로 다른 지점에서 터지는지, 비슷한 수를 뺄 때 유효자리가 통째로 사라지는 파괴적 상쇄, 그리고 긴 합(어텐션 로짓·손실 누산)에서 더하는 순서가 결과를 바꾼다는 실험과 카한 합산. quantization-basics가 비트 레이아웃과 PTQ/QAT를 이미 다루므로 이 글은 오차가 새어 나가는 자리만 맡는다. |
| 71 | `math-numerical-stability-patterns` | 수치적으로 안정한 계산 패턴 모음 | 최댓값 빼기(42번), 로그 공간 계산, log1p·expm1, 분산의 두 공식 중 무엇이 안전한가, fp32 누산과 혼합정밀도에서 무엇을 어디에 두는가, 정규화의 ε이 실제로 막는 것, 역행렬 대신 solve를 쓰는 이유(조건수). 프레임워크가 몰래 해 주는 일을 목록으로 정리하고 각각 어떤 고장을 막는지 붙인다. |
| 72 | `math-quantization-error` | 양자화 오차: 격자 사상, 오차 분산, 이상치 채널 | 실수 구간을 2^b개 격자에 사상할 때의 스텝 크기와 균등 양자화 오차의 분산 Δ²/12, 그것이 SNR로 번역되는 계산, 대칭·비대칭 스케일의 차이, 그리고 이상치 하나가 스케일을 통째로 늘려 나머지 값의 유효 비트를 잡아먹는 계산. int4에서 성능이 무너지는 지점을 오차 예산으로 미리 예측한다. quantization-basics·quantization-awq-gptq는 기법과 도구이므로 이 글은 오차의 수학만 맡는다. |
| 73 | `math-asymptotics-and-flops` | 점근 표기와 연산량 손계산: O·Θ·Ω, 파라미터 수, 어텐션의 O(n²) | O·Θ·Ω의 정의와 상수를 버리는 것이 언제 정당한가를 먼저 정리하고, 행렬 shape에서 곱셈-덧셈 횟수를 직접 세어 선형층·어텐션·FFN의 FLOPs와 트랜스포머 블록의 파라미터 수를 손으로 유도한다. 시퀀스 길이가 두 배가 될 때 무엇이 네 배가 되고 무엇이 두 배가 되는지 구분하는 것이 목표다. llm-scaling-laws는 경험 법칙, inference-kv-cache는 메모리 최적화이므로 이 글은 세는 법만 맡는다. |

### 13단원 · 측정: 모든 점수에는 오차막대가 있다

> 벤치마크 점수 차이가 실제 차이인지 판정하고, 리더보드와 스케일링 곡선을 오해 없이 읽는다. 뉴스로 벤치마크를 매일 다루는 이 사이트에 가장 직접 쓰이는 단원이다.

| # | 슬러그 | 제목 | 무엇을 쓰는가 |
|---:|---|---|---|
| 74 | `math-estimator-and-standard-error` | 추정량과 표준오차: eval 점수에 오차막대 붙이기 | 추정량의 불편성·일치성, 정확도 같은 비율 추정량의 표준오차 √(p(1−p)/n), 정규 근사 신뢰구간과 소표본에서 더 정확한 Wilson 구간. 500문항에서 82%와 84%가 유의한 차이인지 직접 계산해 답하고, 오차막대 없이 발표된 벤치마크 표를 어떻게 읽어야 하는지 정리한다. |
| 75 | `math-bootstrap` | 부트스트랩: 분포 가정 없이 신뢰구간 만들기 | 재표집으로 표본분포를 근사하는 원리, 백분위수 구간과 BCa의 차이, 필요한 재표집 횟수, 그리고 평균이 아닌 지표(F1, nDCG, pass@k)에도 그대로 쓸 수 있다는 것. 해석식이 없는 평가 지표의 불확실성을 재는 표준 도구이며, 77번과 함께 쓰면 거의 모든 eval 지표에 구간을 붙일 수 있다. |
| 76 | `math-paired-test-and-multiple-comparisons` | 같은 문항으로 두 모델 비교하기: 쌍체 검정, 효과 크기, 그리고 여러 번 재면 생기는 일 | 같은 문항을 쓰면 문항 난이도의 변동이 상쇄되어 훨씬 적은 표본으로 결론이 난다는 계산, 이항 부호 검정과 순열 검정, 효과 크기와 검정력, 그리고 벤치마크를 k개 돌리면 우연히 이길 확률이 1−(1−α)^k로 늘어난다는 사실과 Bonferroni·FDR 보정. t검정·분산분석·카이제곱 같은 고전 추론통계를 이 한 편으로 대체한다. |
| 77 | `math-monte-carlo-and-passk` | 몬테카를로 추정과 pass@k: 샘플 n개로 무엇을 말할 수 있나 | 기댓값을 표본평균으로 추정할 때 오차가 1/√n로 줄어든다는 것과 그 실전적 함의, 그리고 n번 시도해 c번 성공했을 때 pass@k의 불편 추정량 1 − C(n−c,k)/C(n,k)의 유도. 순진한 추정이 왜 위로 편향되는지, 원하는 정밀도를 위해 n을 몇으로 잡아야 하는지 계산한다. HumanEval류 지표를 스스로 계산하고 오차를 붙일 수 있게 되는 것이 목표다. |
| 78 | `math-elo-and-arena-ranking` | Elo와 아레나 순위: Bradley–Terry로 리더보드 읽기 | 55번의 Bradley-Terry 모델에서 Elo 갱신식이 확률적 경사상승 한 걸음이라는 유도, K 계수의 의미, 온라인 갱신과 최대가능도 일괄 추정의 차이, 그리고 대전 수가 적은 모델의 순위 신뢰구간이 얼마나 넓은지. 순위표에서 몇 위 차이가 사실은 구분되지 않는지 계산으로 판정한다. llm-comparison-benchmarks는 벤치마크 소개이므로 이 글은 순위 모델의 수학만 맡는다. |
| 79 | `math-calibration-and-ece` | 보정: 모델이 말하는 확률을 믿어도 되는가 | 보정의 정의(신뢰도 0.8이라고 한 것 중 실제로 80%가 맞아야 한다), 신뢰도-정확도 도표, ECE의 계산과 빈 개수에 따라 값이 달라진다는 약점, Brier 점수와 그 분해, 그리고 온도 스케일링으로 사후 보정하는 법. logprob이나 자기 신뢰도를 라우팅·거절·검증에 쓸 때 깔려 있는 전제를 검사한다. |
| 80 | `math-power-law-and-scaling-curves` | 멱법칙과 로그-로그 직선: 스케일링 곡선 읽기와 외삽의 한계 | y = ax^(−b) 꼴이 로그-로그에서 직선이 되는 이유와 기울기가 곧 지수라는 것, 최소제곱으로 지수를 적합할 때 로그 변환이 오차 구조에 하는 일, 환원 불가 항 y = ax^(−b) + c를 넣으면 무엇이 달라지는지, 그리고 두 자릿수 밖으로 외삽할 때 깨지는 지점. llm-scaling-laws는 Kaplan·Chinchilla의 내용이므로 이 글은 곡선을 적합하고 해석하는 통계만 맡는다. |

---

# 기초 트랙 — 23편

본선의 선행 조건이 아니다. 번호를 매기지 않는 이유가 여기 있다 — 번호가 붙는 순간
'순서대로 읽어야 하는 것'이 되고, 그것이 이전 144편 계획을 죽인 인식이다.
각 글에는 '본선 몇 번 앞'이라는 좌표만 붙는다.

2026-08-05에 셋을 더해 19편에서 22편이 됐다 — `math-basics-coordinate-plane-and-lines`,
`math-basics-quadratic-and-parabola`, `math-basics-surfaces-and-contours`(무엇을 왜
되살렸는지는 「이전 계획에서 뺀 것」에 있다). 이 셋은 `src/data/curriculum.ts`의
`mathFoundation` 배열에서 앞쪽에 둔다 — 목록 화면에서 가장 낮은 계단이 먼저 보이게
하려는 것이고, 기초에 순서가 없다는 원칙은 그대로다. 그래서 배열 순서와 아래 절 묶음이
정확히 일치하지 않는다.

### 기초 0 · 진입점 (번호 없음 · 어디서 시작할지 정하는 한 편)

| 슬러그 | 제목 | 무엇을 쓰는가 | 본선 대응 |
|---|---|---|---|
| `math-basics-self-diagnosis` | 어디서 시작할까: 30문항 자가진단과 막힘별 처방전 | 가르치는 글이 아니라 길을 정해 주는 글이다. 기호 읽기·대수 조작·함수 모양·미분 계산·확률·선형대수 여섯 영역에서 5문항씩 30문항을 내고, 답안 패턴에 따라 세 처방을 준다. (A) 전부 풀렸다 → 본선 1번으로 직행하고 기초는 쳐다보지 않는다. (B) 특정 영역만 막혔다 → 그 영역의 기초 2~3편만 집어 읽고 본선 1번으로. (C) 목표가 좁다 → MATH-PLAN이 이미 정해 둔 절단점 표를 그대로 쓴다: 임베딩 검색만 필요하면 본선 4번, 어텐션 수식만이면 40번, 학습 루프면 44번, 정렬이면 52번, 생성이면 59번, 평가 통계면 74번부터 시작해도 그 앞이 필요 없다. 마지막 절에 '기초는 순서가 없고 번호도 없다 — 본선을 읽다 막힌 자리에서 한 편만 꺼내 보는 사전이다'를 못 박는다. 본선 N번 ↔ 기초 대응표 전체는 src/data/curriculum.ts 한 곳에만 두고 여기에 복제하지 않는다. **이 글은 기초 트랙이 아니라 전체의 맨 마지막에 쓴다** — 처방 링크가 본선으로 나가므로 본선 80편과 기초 21편이 전부 있어야 살아난다. 쓰는 순서를 기초 → 본선으로 뒤집었으니 기초 구간 끝이 아니라 본선 구간 마지막 날의 6편째다(「쓰기 순서」 참조). 배열에서의 자리와 화면 순서는 기초 맨 앞 그대로다. | 본선 1번 앞. 읽지 않아도 되는 유일한 안내 글이며 어떤 글의 선행 조건도 아니다. |

### 기초 1 · 기호와 문장: 수식을 소리 내어 읽기

| 슬러그 | 제목 | 무엇을 쓰는가 | 본선 대응 |
|---|---|---|---|
| `math-basics-symbol-glossary` | 수식 기호 발음 사전: ∀부터 ⊙까지 소리 내어 읽기 | 논문 수식의 기호를 소리 내어 읽는 법만 모은 사전이다. 그리스 문자 24자를 이름과 함께 읽고 AI 논문에서 각 문자가 관습적으로 맡는 역할을 정리한다(θ 파라미터, η 학습률, λ 정규화 계수이자 고윳값, σ 표준편차이자 시그모이드, ε 작은 수, μ 평균, π 정책이자 원주율, ∇ 나블라). 집합(∈ ∉ ⊂ ∪ ∩ ∅ × {x : P(x)}, 그리고 칠판 굵은체 ℕ ⊂ ℤ ⊂ ℚ ⊂ ℝ ⊂ ℂ를 무엇이라 읽는가와 구간 표기 [a,b]·(a,b)의 구분 — 수 체계를 글 한 편으로 되살리지 않는 대신 그 두 줄만 여기 둔다), 논리(∀ ∃ ∃! ⇒ ⟺ ¬), 관계(≈ ≜ := ≡ ∝ ≪ ≫), 연산(Σ Π ∫ ∂ ∇ ⊙ ⊗ ∘ ⟨·,·⟩ ‖·‖ \|·\| ⌊·⌋), 극값(max min sup inf argmax argmin — sup과 max가 갈리는 자리는 본선 17번의 유도 노름이고 argmax는 값이 아니라 위치를 준다), 그리고 라틴 약어(i.i.d., w.r.t., w.l.o.g., a.s., iff, s.t., resp.). 기호마다 '어떻게 읽는가 / 무엇을 뜻하는가 / 자주 하는 오독' 세 줄. 마지막은 논문 초록의 수식 다섯 줄을 통째로 한국어 문장으로 옮기는 연습. 본선 2번이 서체 규약(x/굵은 x/X/𝒳)·열벡터 관례·f: R^n → R^m 서술·텐서 축 대응을 맡으므로 이 글은 그 앞 단계인 '기호를 입으로 읽기'만 맡는다. | 본선 1~3번 앞. 17번 앞에도. |
| `math-basics-functions-and-graphs` | 함수와 그래프: 평행이동·확대·반사와 다섯 가지 기본 모양 | 함수를 '입력 하나에 출력 하나'로 정의하고 정의역·공역·치역, 합성 g∘f와 그 정의역 조건, 단사·전사·전단사, 역함수가 존재할 조건과 단조증가 함수의 역함수가 잘 정의된다는 것(본선 25번의 역변환 표집이 cdf의 역함수를 쓴다), 우함수·기함수, 조각별 정의 함수와 지시함수 1[·]. 그다음 y=f(x−a)+b, y=cf(x), y=f(cx), y=−f(x), y=f(−x)가 그래프에 하는 일을 한 장으로 정리하고 파라미터를 하나씩 움직여 손으로 그린다. 마지막으로 다섯 곡선의 개형을 좌표 몇 점만으로 재구성한다 — 지수, 로그, 가우시안 종형, 멱함수, 그리고 계단형 포화 곡선. 각각의 점근선·변곡점·단조 구간·볼록 구간을 표시한다. 활성함수(sigmoid·tanh·ReLU·GELU·SiLU)의 개형과 선택 가이드는 기존 nn-activation-functions가 이미 절 단위로 다루고 미분은 본선 38번이 맡으므로 여기서 다루지 않는다. **다섯 곡선에 포물선을 여섯 번째로 더하지 않는다** — 이차식의 개형과 꼭짓점은 `math-basics-quadratic-and-parabola`가 갖는다. 이 글은 1변수 y=f(x)만 맡고 두 변수 z=f(x,y)의 곡면·등고선은 `math-basics-surfaces-and-contours`가 갖는다. f: R^n → R^m 서술은 본선 2번의 것이다. | 본선 18번·24번 앞. 25번 앞에도. |
| `math-basics-surfaces-and-contours` | 두 변수의 함수를 그림으로 보기: 곡면·등고선·단면 | z = f(x, y)가 평면 위 각 점에 높이를 얹은 것이라는 정의에서 시작해 같은 함수를 보는 세 방법을 익힌다 — 3차원 곡면, 등고선(같은 높이를 잇는 선), 한 변수를 고정한 단면. 등산 지도에서 등고선이 촘촘한 곳이 가파른 곳이라는 사실이 본선 32번의 '∇f는 등고선에 수직이고 그 크기가 가파름'이라는 문장의 그림 그 자체임을 확인한다. 손으로 그려 볼 대상은 넷이다 — 그릇 z=x²+y²(등고선이 동심원), 안장 z=x²−y²(한 방향은 오르고 다른 방향은 내려간다), 길쭉한 골짜기 z=x²+10y²(등고선이 늘어진 타원이 되는 이유와 그 늘어짐의 비율이 본선 45번의 조건수다), 봉우리 z=exp(−(x²+y²)/2)(본선 22·23번 정규분포 밀도의 모양이며 공분산이 등고선을 기울어진 타원으로 만드는 자리다). 편미분은 여기서 '한 변수만 남기고 단면을 잘라 그 위에서 미분하는 것'으로 그림으로만 읽어 둔다 — 계산 규칙은 기초 도함수 규칙 글이, 정의와 그래디언트 벡터는 본선 32번이 맡는다. 마지막으로 3차원을 넘어가면 그림이 불가능해지고 '지형'과 '등고선'이 은유로만 남는다는 것을 못 박아, 본선 44번의 '고차원의 진짜 장애물은 국소최소가 아니라 안장점과 평지'와 65번의 거리 집중이 왜 직관을 배신하는지를 예고한다. 볼록성의 정의와 판정은 본선 29·44번, 이 글이 그리는 그릇과 안장을 이차형식 xᵀAx의 부호와 고윳값 부호로 **판정하는** 절차는 14번(이 글은 그림만 맡고 판정은 하지 않는다), 헤세 행렬과 곡률·조건수는 45번, 다변수 테일러 전개도 45번, 1변수 테일러는 기초 테일러 글의 것이다. 1변수 함수의 평행이동·확대·반사와 다섯 곡선의 개형은 기초 함수와 그래프 글이 맡으므로 여기서 반복하지 않는다. | 본선 32번 앞. 14·23·44·45번 앞에도. |
| `math-basics-reading-proofs` | 증명을 읽는 법: 정의·정리·따름정리와 '따라서'의 정체 | 증명을 쓰는 훈련이 아니라 읽는 훈련이다. 수학 문서의 구조(정의·보조정리·정리·따름정리·명제·증명 종료 기호)와 각각이 약속하는 것, 가정과 결론을 문장에서 분리하는 법, '~라고 하자'가 하는 일, '~임을 보이면 충분하다'가 목표를 어떻게 바꾸는지. 조건문 P ⇒ Q와 역·대우·이의 구분, ⟺를 증명할 때 양방향을 다 보여야 한다는 것, 반례 하나로 끝나는 문장과 그렇지 않은 문장. 직접 증명·대우 증명·귀류법·경우 나누기·수학적 귀납법·구성적 증명의 뼈대를 각각 짧은 예로 보이고, '일반성을 잃지 않고', '충분히 큰 n에 대하여', '거의 확실히', '어떤 상수 C가 존재하여', 'it suffices to show' 같은 관용구를 해석한다. 마지막은 논문 부록의 증명을 끝에서 거꾸로 읽어 '어디서 어떤 가정이 쓰였는가'만 뽑는 실전 독법. 본선 6번(코시-슈바르츠)·29번(젠센)·30번(KL≥0)·43번(RoPE 상대 위치 불변성)·54번(베이스라인 무편향)이 증명으로 닫는 첫 글들이라 그 앞에 둔다. 증명 자체는 각 본선 글의 것이고 여기서는 예시로만 쓴다. | 본선 6번 앞. 29·30·43·54번 앞에도. 심화 트랙 전체의 입구이기도 하다. |
| `math-basics-algebra-manipulation` | 식 한 줄에서 다음 줄로: 유도를 따라가게 하는 대수 조작 | 유도가 막히는 자리는 개념이 아니라 '이 줄에서 저 줄로 어떻게 갔는가'다. 이항과 등식의 성질, 분배와 묶기, 분수식의 통분·약분·복잡한 분수 정리, 지수와 근호의 상호 변환, 완전제곱 만들기(ax²+bx+c → a(x+p)²+q), 절댓값의 정의와 \|a+b\| ≤ \|a\|+\|b\|의 경우 나누기, 부등식을 조작할 때 부호에 따라 방향이 바뀌는 자리와 양변 제곱이 동치가 되는 조건, √(x²)=\|x\|, 산술-기하 평균 부등식, 그리고 '제곱의 합은 음이 아니다'와 이차식의 판별식만으로 부등식을 세우는 기술. 등호 조건을 항상 함께 적는 습관. 실습은 전부 본선이 실제로 건너뛰는 줄의 복원이다 — 본선 6번의 ‖a−b‖² 전개, 22번 정규분포 밀도 지수부의 완전제곱, 28번의 교차엔트로피 정리, 46번의 2차 상계 유도에서 생략된 줄을 한 줄씩 채워 넣는다. 본선 6번의 코시-슈바르츠 증명과 5번의 노름 세 공리는 각 글의 것이므로 여기서는 그 증명에 쓰이는 판별식 기법만 가르치고 결론은 넘긴다. 시그마 조작과 합의 순서 교환은 본선 3번이 맡는다. **완전제곱과 판별식은 여기서 '유도의 생략된 줄을 복원하는 조작법'으로만 다룬다** — 근의 공식, 근과 계수의 관계, 포물선의 꼭짓점은 `math-basics-quadratic-and-parabola`가 갖는다. | 본선 5번·6번 앞. 22·28·29·46번 앞에도. |

### 기초 2 · 수와 스케일

| 슬러그 | 제목 | 무엇을 쓰는가 | 본선 대응 |
|---|---|---|---|
| `math-basics-orders-of-magnitude-and-error` | 자릿수와 오차: 1e-8은 큰 수인가 | 과학적 표기와 유효숫자, 절대오차와 상대오차의 정의와 언제 어느 쪽을 봐야 하는지, 오차가 덧셈·곱셈·함수 통과에서 전파되는 1차 근사, 상대오차의 로그가 '맞는 자릿수'라는 관점. 자릿수 감각의 실습 대상은 전부 수치 쪽이다 — 머신 엡실론 1e-7(fp32)과 1e-3(fp16), Adam의 ε=1e-8, 학습률 1e-5, LayerNorm의 ε, 기울기 검사의 허용 상대오차가 각각 무슨 규모이고 서로 몇 배인지 말하기. 그다음 로그 축 그래프를 읽고 그리는 법 — 반로그와 로그-로그가 각각 무엇을 직선으로 펴는지, 눈금 간격이 왜 균등하지 않은지. 마지막으로 개발자가 실제로 틀리는 자리: 퍼센트와 퍼센트포인트, 2배 증가와 200% 증가, 배수의 평균은 산술평균이 아니라 기하평균이라는 것. 지수·로그 법칙과 로그 공간 계산·언더플로는 본선 18번이, FLOPs·파라미터 수 손계산은 본선 73번이, KV 캐시·메모리 산정은 기존 inference-kv-cache 계열이 맡으므로 여기서 다루지 않는다. | 본선 39번 앞. 70·71·72번 앞에도. |
| `math-basics-ratio-and-normalization` | 비·비율·정규화: ∝로 쓴 식을 =로 되돌리기 | 비 a:b와 비율, 비례식, 정비례 y=kx와 반비례 y=k/x의 그래프, 그리고 ∝가 '상수배를 버리고 관계만 적는다'는 뜻이라는 것. 여기서 본론으로 간다 — 확률처럼 전체가 1이어야 하는 양은 아무 양수 벡터든 합으로 나누면 만들 수 있고, 그 분모를 정규화 상수(분배함수)라 부르며, 분모를 못 구하거나 구할 필요가 없을 때 식을 ∝로 적는다. 그렇게 적힌 식 셋을 실제로 =로 되돌려 본다: 본선 19번의 사후 ∝ 가능도×사전, 본선 24번 softmax의 분모 Σexp(z_j), 본선 57번의 π* ∝ π_ref·exp(r/β). 마지막이 특히 중요한데 DPO 유도에서 소거되는 분배함수 Z가 정확히 이 정규화 상수이고, 본선 61번이 '스코어 ∇log p에서 정규화 상수가 미분에 사라진다'고 말할 때의 상수도 같은 것이다. 이어서 정규화가 순위를 바꾸지 않는다는 것(모두 같은 양수로 나누므로)과 그래서 argmax는 분모를 몰라도 답할 수 있다는 계산, 그리고 벡터를 v/‖v‖로 정규화하는 것과 확률을 합으로 나누는 것이 같은 조작을 다른 대상에 한 것이라는 정리. 반비례 감각의 실습 대상은 표본평균의 분산 σ²/n(본선 21번)과 배치를 k배 키울 때의 학습률(본선 49번)이다. **퍼센트와 퍼센트포인트, 2배 증가와 200% 증가, 배수의 평균이 기하평균이라는 것은 `math-basics-orders-of-magnitude-and-error`가 이미 맡았으므로 반복하지 않고, 통분·약분·복잡한 분수 정리 같은 분수식 조작은 `math-basics-algebra-manipulation`의 것이다.** softmax의 함수 형태가 왜 exp인지의 유도는 본선 24번, 베이즈 규칙에서 각 항의 이름은 본선 19번, KL 제약 최적해의 유도는 본선 57번이 맡는다. | 본선 19·24번 앞. 21·49·57·61번 앞에도 |
| `math-basics-sequences-limits-and-series` | 수열·극한·등비급수: 1/(1−r)이 어디서 나오는가 | 수열 표기 a_n과 점화식을 몇 항 손으로 펼치기, 극한을 엡실론-델타 없이 '결국 어떤 값에 붙는다'로 정의하기, 좌·우극한, 0/0과 ∞/∞ 부정형을 약분·유리화로 푸는 기술, x→∞에서 지수·다항·로그의 성장 속도 서열, (1+1/n)^n → e, 연속의 정의와 ReLU가 연속이지만 한 점에서 미분 불가능하다는 것. 그다음 등차·등비수열과 등비급수 합 1/(1−r)을 부분합의 극한으로 유도하고 \|r\|<1 조건을 확인한다. 이 결과 하나가 본선 47번의 EMA 유효 창, 48번 Adam의 편향 보정 1/(1−βᵗ), 13번 거듭제곱법의 수렴률 \|λ₂/λ₁\|^k에서 그대로 쓰인다. 조화급수가 발산한다는 것과 r^k → 0의 속도까지. 마지막으로 도함수의 정의식 [f(x+h)−f(x)]/h가 왜 극한이어야 하는지를 h를 줄여 가며 수치로 확인한다(본선 32번은 도함수를 극한이 아니라 '최선의 선형근사'로 정의하고 시작하므로 그 정의가 왜 같은 말인지를 여기서 받친다). β=0.9와 0.999가 몇 스텝을 평균하는지의 계산은 본선 47번의 것이고, 이중합과 첨자 조작은 본선 3번의 것이다. 급수 수렴 판정법 일반론은 다루지 않는다 — 등비급수 하나면 충분하다. | 본선 32번 앞. 13·47·48번 앞에도. |

### 기초 3 · 본선이 통째로 뺀 계산 도구

앞의 두 편이 2026-08-05에 되살린 학교 수학이다. 절 제목이 그대로 설명한다.

| 슬러그 | 제목 | 무엇을 쓰는가 | 본선 대응 |
|---|---|---|---|
| `math-basics-coordinate-plane-and-lines` | 좌표평면·직선·초평면: 두 점 사이 거리에서 wᵀx + b = 0까지 | 순서쌍 (x, y)와 좌표평면에서 시작한다. 점을 성분 목록으로 적는 것이 벡터의 출발점이고(본선 4번이 그 위에서 '벡터'라는 수학 대상을 정의한다), 피타고라스 정리에서 두 점 사이 거리 √((x₁−x₂)²+(y₁−y₂)²)를 세운 뒤 3차원, 그리고 n차원으로 항을 늘리기만 하면 된다는 것을 확인한다 — 본선 5번이 L2 노름을 세 공리로 정의하는 순간 그 식이 어디서 온 것인지 알게 하는 것이 첫 목적이다. 그다음 기울기 Δy/Δx와 '한 칸 갈 때 얼마나 오르는가', 절편, 직선의 세 표현(y=mx+b, 점-기울기, ax+by=c)과 서로 옮겨 적기, 평행·수직 조건 m₁m₂=−1. 여기서부터가 본론이다 — 일반형 ax+by+c=0의 계수쌍 (a, b)가 그 직선의 법선벡터라는 것을 계산으로 보이고(수직 조건이 본선 6번의 '내적이 0이면 직교'와 같은 말이라는 확인), 그것을 그대로 n차원으로 올린 wᵀx+b=0이 초평면이며 w가 법선이라는 것, 점에서 초평면까지의 거리 \|wᵀx+b\|/‖w‖의 유도, 부호 sign(wᵀx+b)가 '어느 쪽에 있는가'를 답한다는 것, 그리고 차원을 올려도 식 하나가 공간을 두 쪽으로 가른다는 것까지. 마지막은 아핀과 선형의 구분이다 — b가 붙는 순간 원점을 지나지 않으므로 선형사상이 아니고, 그래서 Wx+b의 편향 항이 따로 관리된다. 이 계산 하나가 기존 ml-svm이 유도 없이 결과만 던지는 마진 2/‖w‖, 본선 67번의 무작위 초평면 해싱, 본선 7번 사영의 잔차 길이를 전부 같은 식으로 읽게 만든다. 노름의 세 공리와 단위구는 본선 5번, 사영 공식 p=(a·b/‖b‖²)b와 사영 행렬·잔차 직교성의 최소제곱 기하는 본선 7번, 선형사상의 정의와 '행렬은 기저의 상을 열에 적어 둔 것'이라는 관점은 본선 9번, 등고선과 그래디언트가 수직이라는 결과는 본선 32번의 것이다(여기서는 '법선'이라는 말만 미리 세운다). 연립일차방정식을 두 직선의 교점으로 읽는 그림과 해가 하나·무한·없음의 세 경우는 기초 연립방정식 글의 것이고, 로그-로그 축에서 직선의 기울기를 읽는 법은 기초 자릿수 글의 것이다. | 본선 5번·6번 앞. 7·9·67번 앞에도. 기존 ml-svm·ml-linear-regression을 읽을 때도. |
| `math-basics-quadratic-and-parabola` | 이차식 하나: 근의 공식, 근과 계수의 관계, 그리고 포물선의 꼭짓점 | 완전제곱에서 근의 공식 x = (−b ± √(b²−4ac))/2a를 한 번 유도하고, 판별식 D = b²−4ac의 부호가 실근 둘·중근·복소근으로 갈리는 대응을 확인한다. 그다음이 착지점이다 — 근과 계수의 관계 α+β = −b/a, αβ = c/a. 본선 12번은 고윳값을 2×2 특성방정식 det(A−λI)=0으로 정의하고 지나가는데, 그것을 전개하면 λ² − (tr A)λ + det A = 0이고 근과 계수의 관계가 곧 '고윳값의 합은 대각합, 고윳값의 곱은 행렬식'이라는 문장이 된다. 실제 2×2 행렬 몇 개로 이 대응을 손으로 확인하고, 고윳값을 풀지 않고도 tr A와 det A만으로 스펙트럼을 읽는 습관을 만든다. 판별식이 음수라 복소 고윳값이 나오는 경우가 회전행렬이라는 것까지 짚어 기초 복소수 글로 넘긴다. 셋째로 포물선 y = ax²+bx+c — 꼭짓점 x = −b/2a가 완전제곱의 결과 그 자체라는 것, a의 부호가 그릇인지 뚜껑인지를 정한다는 것, 최솟값이 정확히 얼마인지. 실습은 f(x) = ½ax² 위에서 경사하강 한 걸음을 손으로 계산해 η가 2/a를 넘는 순간 값이 커지는 것을 확인하는 것 하나면 족하다 — 본선 46번이 그 경계를 L-매끄러움으로 일반화하고, 44번의 그릇과 안장, 45번의 2차 근사가 전부 이 개형 위에 선다. 완전제곱 만들기와 판별식으로 부등식을 세우는 기술은 기초 대수 조작 글의 조작법이므로 여기서는 그 결과만 인용해 쓰고, 이차형식 xᵀAx의 부호 판정은 본선 14번, 헤세와 곡률은 45번, 학습률 상한 2/L의 유도는 46번, 특성방정식이 왜 고윳값을 주는지는 12번, 행렬식의 기하적 의미는 기초 행렬식 글의 것이다. 다항식 나눗셈·인수정리와 3차 이상의 근의 공식은 커리큘럼 어디에서도 쓰지 않으므로 다루지 않는다. | 본선 12번 앞. 14·44·45·46번 앞에도. |
| `math-basics-linear-systems-and-elimination` | 연립방정식과 가우스 소거: Ax=b를 실제로 푸는 절차 | 연립일차방정식을 행렬로 적기, 가우스 소거와 행 사다리꼴, 후진 대입, 해가 하나·무한·없음으로 갈리는 세 경우와 그것이 열의 일차독립과 어떻게 대응하는지, 자유변수와 해공간의 차원, LU 분해의 형태와 왜 같은 A로 여러 b를 풀 때 이득인지, 그리고 최소제곱 정규방정식 AᵀAx = Aᵀb를 세우는 과정. MATH-PLAN은 가우스 소거·LU·역행렬을 '딥러닝에 Ax=b를 직접 푸는 자리가 없다'는 이유로 통째로 뺐지만, 본선 7번의 사영 행렬 A(AᵀA)⁻¹Aᵀ가 정확히 이 정규방정식이고 8번의 일차독립·15번의 랭크가 이 소거 절차의 결과다. 그 배경을 이 글이 맡는다. 마지막에 실제 코드가 왜 역행렬을 만들지 않고 solve를 쓰는지를 조건수와 함께 짚어 본선 71번으로 넘긴다. 사영 공식의 유도와 최소제곱의 기하는 본선 7번의 것이고, 특잇값 문턱으로 판정하는 수치 랭크는 본선 15번의 것이다. | 본선 7번 앞. 8·15번 앞에도. |
| `math-basics-determinant-and-inverse` | 행렬식과 역행렬: 부피 배율, 가역성, 그리고 코드가 역행렬을 만들지 않는 이유 | 2×2·3×3 행렬식의 계산과 여인수 전개, 행렬식이 부호 있는 부피 배율이라는 기하적 의미와 det=0이 공간을 납작하게 뭉갠다는 뜻, det(AB)=det(A)det(B), det=0과 가역성·랭크 결손·열의 일차종속이 전부 같은 말이라는 것. 2×2 역행렬 공식과 역행렬이 존재할 조건, (AB)⁻¹=B⁻¹A⁻¹, (Aᵀ)⁻¹=(A⁻¹)ᵀ, 유사역행렬의 소개. 본선 12번은 2×2 특성방정식에 필요한 행렬식만 '그 자리에서 부피 배율로' 정의하고 지나가고, 34번은 야코비안 행렬식을 부피 배율로 쓰며, 7번의 사영 행렬과 12번의 대각화 A=PΛP⁻¹가 역행렬을 전제한다 — 그 배경 전체를 이 글이 통째로 맡는다. 마지막으로 조건수를 소개해 왜 수치 코드가 inv(A)@b 대신 solve(A,b)를 쓰는지 답하고 본선 71번으로 넘긴다. 야코비안 행렬식과 변수변환에서 밀도가 바뀌는 이유는 본선 34번의 것이다. | 본선 12번 앞. 7·15·34번 앞에도. |
| `math-basics-trigonometry-and-unit-circle` | 삼각비와 단위원: cos θ가 정확히 무엇인지, 그리고 파동의 파라미터 | 직각삼각형의 sin·cos·tan에서 시작해 단위원 위의 좌표로 확장하고, 도와 라디안의 환산과 왜 미적분이 라디안을 쓰는지(호의 길이라서 미분이 깨끗해진다). 피타고라스 항등식 sin²+cos²=1, 특수각 값, 사인·코사인 그래프와 대칭성. 핵심은 파형 파라미터다 — A·sin(ωt+φ)에서 진폭·각주파수·위상·주기·파장이 각각 곡선의 무엇을 바꾸는지 하나씩 움직여 보고, 주파수를 기하급수적으로 줄이면 파장의 사다리가 만들어진다는 것을 그림으로 확인한다. 본선 6번이 내적을 ‖a‖‖b‖cos θ로 적는 순간 cos θ의 정의를 모르면 2단원 전체가 막히고, 43번의 사인파 위치 인코딩은 이 사다리를 읽어야 한다. 각도 합 공식은 결과만 제시하고 증명은 본선 11번에 넘긴다 — 그 글이 R(α)R(β)=R(α+β)를 행렬곱으로 확인해 '각도 합 공식이 사실 행렬 항등식'임을 보이는 것이 결론이기 때문이다. 삼각함수 항등식·사인법칙·역삼각함수는 본선이 쓰지 않으므로 다루지 않는다. | 본선 6번 앞. 11·43번 앞에도. |
| `math-basics-complex-numbers-and-euler` | 복소수와 오일러 공식: 회전을 e^{iθ} 하나로 적기 | 허수단위와 복소평면, 사칙연산, 켤레와 절댓값, 극형식 r(cos θ + i sin θ), 그리고 곱셈이 '크기는 곱하고 각도는 더한다'는 기하적 사실. 여기서 오일러 공식 e^{iθ}=cos θ+i sin θ를 테일러 급수로 확인하고, e^{iθ}·e^{iφ}=e^{i(θ+φ)}가 본선 11번의 R(α)R(β)=R(α+β)와 같은 말임을 나란히 둔다. 드무아브르 정리, 1의 n제곱근이 단위원 위에 균등하게 놓인다는 것, 실계수 다항식의 복소근이 켤레쌍으로 나온다는 것(본선 12번에서 회전행렬처럼 복소 고윳값이 나오는 경우). 본선은 오일러 공식 의존을 없애려고 RoPE를 2×2 회전행렬만으로 유도하지만 논문 원문과 대부분의 구현은 e^{imθ} 표기로 쓰여 있다. 그 표기를 읽고 두 유도가 같은 대상임을 확인하는 것이 이 글의 목적이며, 심화 A9의 푸리에 해석과 A10의 표현론이 여기를 전제로 시작한다. RoPE의 상대 위치 불변성 증명은 본선 43번의 것이다. | 본선 43번을 논문 표기로 다시 읽을 때. 심화 A9·A10 단원의 실질적 선행. |

### 기초 4 · 변화율: 미적분 최소 세트

| 슬러그 | 제목 | 무엇을 쓰는가 | 본선 대응 |
|---|---|---|---|
| `math-basics-derivative-rules` | 도함수 계산 규칙: 손으로 20문제 풀어 굳히기 | 본선 6단원은 '무엇을 왜 미분하는가'를 다루고 계산 규칙 자체는 알고 있다고 전제한다. 그 전제를 채우는 글이다. 도함수 표기 세 가지(f′, dy/dx, ∂), 기본 도함수 표(상수·xⁿ·e^x·a^x·ln x·log_a x·sin·cos), e^x의 도함수가 자기 자신인 이유와 그래서 자연로그가 '자연'인 이유, 상수배·합·곱·몫 규칙, 연쇄법칙의 계산 절차(겉미분×속미분)와 합성이 세 겹인 경우, 음함수 미분, 고계도함수. 그리고 f″의 부호로 볼록·오목을 판정하고 변곡점을 찾기 — 본선 29번의 젠센 부등식이 볼록성 판정에 이계도함수를 쓰는데 본선의 미분 단원은 32번부터라 그 사이가 비어 있고, 32번은 그래디언트를 '민감도'로 재정의하는 개념 글이라 계산 훈련을 하지 않는다. 이 글이 두 자리를 함께 메운다. 실습은 x²e^{−x²} 같은 혼합 예제 20문제를 손으로 풀고 SymPy로 채점하는 것. σ′=σ(1−σ)·tanh′=1−tanh²·softplus′=σ는 본선 38번이, log-sum-exp의 미분은 본선 42번이, softmax 야코비안은 본선 37번이 갖는다 — 문제 목록에서 전부 뺀다. | 본선 29번 앞. 32~38번 전체 앞에도. |
| `math-basics-integral-as-area` | 적분: 넓이와 누적, 그리고 ∫p(x)dx=1을 읽는 법 | 리만합에서 정적분으로, 부정적분과 미적분의 기본정리, 기본 적분 표, 치환적분과 부분적분 각 하나씩, 정적분의 성질(구간 분할, 선형성). 넓이가 1이 되도록 맞추는 정규화 상수의 개념과 ∫e^{−x²/2}dx=√(2π)를 결과로 받아들여 정규분포 밀도의 앞 계수가 어디서 왔는지 확인하기. 다중적분을 '한 축씩 차례로'로 읽는 법과 주변화 ∫p(x,y)dy가 왜 주변분포인지, 그리고 1차원 변수변환에서 \|dx/du\|가 붙는 이유. 목표는 딱 하나다 — 본선 20번이 ∫p(x)dx=1과 E[X]=∫x·p(x)dx를 쓰는 순간, 53번이 미분과 적분의 교환을 성립 조건으로 다는 순간, 65번이 초구의 부피를 계산하는 순간 그 줄에서 멈추지 않는 것. MATH-PLAN이 적분 기법을 통째로 뺐으므로 이 글이 그 자리를 맡는다. 68-95-99.7 규칙의 확인은 본선 22번의 것이고, 야코비안 행렬식(다차원 변수변환)은 본선 34번의 것이다. 이상적분과 적분 기법 일반론은 다루지 않는다. | 본선 20번 앞. 22·34·53·65번 앞에도. |
| `math-basics-taylor-first-steps` | 테일러 전개 첫걸음: 다항식으로 함수 흉내내기 | 한 점 주위에서 접선이 1차 근사라는 관찰에서 출발해 2차·n차 테일러 다항식을 세우고, 차수를 올릴수록 근사 구간이 어떻게 넓어지는지 그래프로 겹쳐 그린다. 매클로린 전개로 e^x, log(1+x), 1/(1−x), sin x, (1+x)^α를 얻고 각각이 \|x\|가 얼마일 때까지 몇 자리 정확한지 수치로 잰다. 나머지항과 절단오차가 O(h^{n+1}) 차수라는 것, 근사가 국소적이며 수렴 반경 밖에서는 쓸모없다는 것, 그리고 x가 작을 때 log(1+x)≈x 같은 근사가 코드에서 log1p·expm1으로 나타나는 자리. 본선 39번이 전진차분 O(h)와 중심차분 O(h²)를 테일러로 유도하는데 본선의 테일러 전개는 45번(다변수)이라 순서가 어긋난다 — 이 글이 그 1변수 판을 39번 앞에 미리 놓는다. 45번은 이 결과를 벡터·헤세로 확장하는 글이므로 겹치지 않는다. 오일러 공식의 급수 확인은 기초 복소수 글이 맡는다. | 본선 39번 앞. 45·46·71번 앞에도. |
| `math-basics-ode-first-steps` | 미분방정식 첫걸음: dx = f(x)dt를 읽는 법 | 미분방정식이 '변화율을 알고 있을 때 함수를 되찾는 문제'라는 정의, 초기값 문제와 해가 하나로 정해지는 이유, 방향장 그림으로 해를 눈으로 보기. 분리형 미분방정식과 1계 선형 미분방정식을 손으로 풀고, 지수 성장·감쇠 dx/dt=−kx의 해와 반감기, 로지스틱 방정식. 해석해가 없는 것이 보통이라는 사실과 오일러 방법으로 수치해를 그리는 실습, 스텝 크기를 절반으로 줄이면 오차가 어떻게 줄어드는지의 관찰. 본선 62번은 확산을 연속시간 SDE와 확률 흐름 ODE로 적고 오일러 이산화의 국소·전역 오차 차수를 유도하는데, '미분방정식'이라는 대상을 한 번도 본 적이 없으면 그 글의 첫 문단에서 멈춘다. 세 안 중 이 자리를 짚은 것이 하나뿐이었고, 본선 80편에도 기존 296편에도 미분방정식을 대상으로 다룬 글이 없다. 이산화 오차의 차수 유도와 DDIM·고차 solver 비교는 본선 62번의 것이다. | 본선 62번 앞. 심화 A7의 SDE 단원 앞에도. |

### 기초 5 · 세는 법과 확률의 언어

| 슬러그 | 제목 | 무엇을 쓰는가 | 본선 대응 |
|---|---|---|---|
| `math-basics-counting-and-binomial` | 세는 법: 곱의 법칙에서 이항계수 C(n,k)까지 | 본선이 정의 없이 쓰는 것 중 하나가 이항계수다. 곱의 법칙과 합의 법칙, 순열 P(n,k)와 조합 C(n,k)의 구분과 언제 순서를 세는지, 이항계수의 대칭성과 파스칼 항등식, 이항정리, 중복조합 최소한, 그리고 포함배제 원리로 합집합의 크기를 세는 법 — 본선 19번의 확률 공리에서 P(A∪B)=P(A)+P(B)−P(A∩B)로 다시 나온다. 팩토리얼이 금방 커져 로그 공간에서 다뤄야 한다는 것과 lgamma를 쓰는 이유, 스털링 근사의 결과만. 이 한 편이 본선 20번의 이항분포 계수, 44번의 뉴런 치환 대칭성, 67번의 LSH 충돌 계산, 77번의 pass@k 불편 추정량 1 − C(n−c,k)/C(n,k)를 읽을 수 있게 만든다. pass@k 식을 실제 값에 대입해 계산하고 시뮬레이션과 맞추는 것은 본선 77번의 유도 보상이므로 여기서 하지 않는다 — 기호를 읽을 수 있게 되는 데서 멈춘다. 비둘기집·생성함수 같은 이산수학 일반론도 다루지 않는다. | 본선 19번 앞. 20·44·67·77번 앞에도. |
| `math-basics-descriptive-statistics` | 기술통계: 분포를 눈으로 읽고 손으로 계산하기 | 평균(산술·가중·기하·조화)이 각각 무엇에 답하는지 — 속도의 평균이 왜 조화평균이고 배수의 평균이 왜 기하평균인지, 중앙값·최빈값과 갈리는 때, 꼬리가 긴 분포에서 평균이 대표성을 잃는 이유. 편차 제곱합에서 분산과 표준편차를 손으로 계산하기, 사분위수·백분위수·IQR·상자그림, 히스토그램의 빈 개수가 그림을 통째로 바꾼다는 것과 커널 밀도 추정 최소한, 로그 축으로 바꿔야 보이는 것, 이상치를 정의하는 규칙들이 얼마나 임의적인지. 지연시간 p50/p95/p99를 읽는 감각과 그것이 평균과 어떻게 다른지도 같은 도구로 설명한다. 이상치 하나가 평균과 중앙값에 하는 일을 수치로 비교하고 왜 벤치마크 점수를 평균만으로 보고하면 안 되는지 예로 보인다. 본선 21번이 분산의 추정량 성질(독립 합의 가법성, 표본평균의 분산 σ²/n, 불편 추정의 n−1)을 맡으므로 이 글은 '계산하고 읽을 줄 안다'만 맡는다. 본선 79번의 ECE가 빈 개수에 따라 달라진다는 약점을 지적할 때도 이 감각이 전제다. | 본선 21번 앞. 74·75·79번 앞에도. |
| `math-basics-hypothesis-testing-logic` | 가설검정의 논리: 귀무가설·유의수준·p값이 실제로 말하는 것 | 왜 '차이가 있다'가 아니라 '차이가 없다'를 가정하고 시작하는가, 귀무가설과 대립가설, 검정통계량, 유의수준 α와 기각역, 그리고 p값의 정확한 정의와 p값이 아닌 것들 — 가설이 참일 확률이 아니고, 효과 크기가 아니고, 재현 확률이 아니다. 1종 오류와 2종 오류의 맞바꿈, 신뢰구간과 검정이 같은 것을 보는 두 방식이라는 것, 통계적 유의성과 실질적 중요성의 구분. 본선 76번은 순열 검정과 부호 검정으로 계산을 직접 하고 효과 크기·검정력·다중 비교 보정까지 맡는 글이라 이 판정의 언어 자체는 전제로 두는데, 그 전제를 여기서 세운다. MATH-PLAN이 t검정·분산분석·카이제곱 같은 고전 추론통계 6편을 76번 한 편으로 대체하기로 한 결정을 그대로 따라, 표를 찾아 임계값을 읽는 절차는 여기서도 다루지 않는다. | 본선 76번 앞. 74번 앞에도. |

### 기초 6 · 검산 도구

| 슬러그 | 제목 | 무엇을 쓰는가 | 본선 대응 |
|---|---|---|---|
| `math-basics-symbolic-and-axis-checking` | 손유도를 기계로 채점하기: SymPy 검산과 첨자 ↔ 축 대응 | 수학이 녹슨 개발자가 가진 유일한 무기는 코드다. 그 무기를 수식 검산에 쓰는 두 가지 방법을 다룬다. 첫째, SymPy로 자기 유도를 채점하기 — symbols·diff·expand·simplify·solve·series의 최소 사용법, 손으로 구한 결과와 simplify(a−b)==0으로 대조하는 절차, 두 결과가 다를 때 의심할 순서(부호, 연쇄법칙 누락, 전치 규약, 상수배). 둘째, 수식의 첨자를 배열의 축으로 옮기기 — i·j·h·t가 몇 번째 축인지 대응시키기, shape를 읽는 법, 브로드캐스팅 규칙 세 줄과 그것이 오류 없이 조용히 틀린 답을 내는 경우(형상이 우연히 맞아떨어질 때), sum·mean에서 axis와 keepdims가 하는 일, '어느 축이 사라졌는가'로 연산을 역추적하기. 기존 python-for-ai가 NumPy 배열 연산과 Matplotlib 시각화 절을 이미 갖고 있고, 본선 3번이 einsum과 시그마 조작을, 10번이 (B,H,T,d_h) shape 산수를, 39번이 유한차분 기울기 검사 규약을 맡으므로 이 글은 '손유도가 맞는지 기계로 채점하는 절차'와 '첨자 ↔ 축 대응'만 맡는다. einsum 실습과 (B,H,T,d) 재구성은 여기서 하지 않는다. | 본선 3번·37번 앞. 10·39번 앞에도. |

---

# 심화 트랙 — 63편

본선 80편을 마친 뒤 읽는다. 이 안에서는 A1 → A14 순서를 지킨다 —
뒤 단원이 앞 단원을 이름으로 참조한다.

### A1 · 엄밀함의 도구 (5편) — 이후 전 단원의 공용 언어

| 슬러그 | 제목 | 무엇을 쓰는가 | 무엇이 열리는가 |
|---|---|---|---|
| `math-adv-metric-normed-and-fixed-points` | 거리공간·노름공간·축약사상: '수렴한다'를 정확히 말하기 | 거리공간과 열린집합, 수열 수렴과 코시 수열, 완비성, 콤팩트성과 최대·최소의 존재, 립시츠 연속. 그리고 축약사상 원리(바나흐 고정점 정리)를 증명까지 — 이 정리 하나가 A7의 Sinkhorn 수렴과 A14의 벨만 작용소 수렴에서 그대로 재사용된다. 후반부는 노름공간과 바나흐 공간: L^p 공간과 횔더·민코프스키 부등식, 유한차원에서는 모든 노름이 동치이지만 무한차원에서는 아니라는 것, 쌍대공간과 유계 선형범함수, 약수렴의 정의. 본선 5번의 노름 세 공리를 무한차원으로 올리고, 본선 46번의 L-매끄러움이 실은 그래디언트의 립시츠 조건이라는 것을 여기서 확정한다. 증명 수위: 축약사상 원리와 횔더 부등식은 완전 증명, 나머지는 진술과 사용법. | 논문 정리부의 'let (X,d) be a complete metric space', 'f is L-Lipschitz', 'the operator is a γ-contraction'을 사전 없이 읽게 된다. Sinkhorn(A7)·벨만(A14)·Robbins–Monro(A4)의 수렴 증명이 전부 이 한 정리로 닫힌다. 본선 5·17·46번. |
| `math-adv-hilbert-spaces-and-spectral-theorem` | 힐베르트 공간과 무한차원 스펙트럼 정리: 사영·리스 표현·고유함수 전개 | 내적공간과 완비성, 정규직교계와 파르스발 항등식, 사영 정리(닫힌 부분공간 위의 최적 근사)와 리스 표현 정리(모든 유계 선형범함수는 내적이다), 가분성. 그다음 유계 선형작용소와 작용소 노름, 수반작용소와 자기수반성, 컴팩트 작용소, 컴팩트 자기수반 작용소의 스펙트럼 정리와 고유함수 전개, 고유값이 0으로 수렴한다는 것, 힐베르트–슈미트·트레이스 클래스, SVD의 무한차원판. 본선 7번의 사영과 14·15번의 스펙트럼 정리·SVD를 무한차원으로 올린 판이며, 무엇이 그대로이고 무엇이 깨지는지 하나씩 대조한다. 리스 표현 정리는 A5의 RKHS 재생 성질 그 자체이고 사영 정리는 A1-5의 조건부 기댓값 정의이므로, 그 두 개념이 태어나는 자리는 이 글 하나뿐이다. 증명 수위: 사영 정리와 리스 표현 정리는 완전 증명, 스펙트럼 정리는 증명 개요 + 핵심 보조정리. | RKHS(A5)·NTK 스펙트럼(A5)·가우시안 과정(A5)·Mercer 전개(A5)·확산의 생성원(A7)·Neural Operator 계열 논문이 전부 이 언어로 쓰여 있다. 본선 7·14·15·17번. |
| `math-adv-lebesgue-integral-and-convergence` | 측도와 르베그 적분: ∇와 E를 언제 바꿔 써도 되는가 | σ-대수와 측도, 르베그 측도의 구성 개요와 비가측 집합의 존재(왜 '모든 부분집합'에 확률을 줄 수 없는가), 확률공간 (Ω,ℱ,P)와 가측함수로서의 확률변수, 단순함수에서 쌓아 올리는 르베그 적분과 리만 적분이 갈리는 지점. 단조수렴·파투·지배수렴 정리, 곱측도와 푸비니–토넬리(적분 순서 교환의 조건), 독립성의 측도론적 정의. 그것으로 본선 53번이 '적분과 미분의 교환'이라는 한 줄로 넘긴 조건을 정확히 진술하고 그 조건이 깨지는 실제 예(불연속 보상, 지지집합이 θ에 의존하는 분포)를 만든다. 후반부는 확률변수 수렴의 네 종류(개별점·확률·L^p·분포)와 함의 관계 및 반례, 특성함수와 레비 연속성, 린데베르그 CLT, 슬러츠키 정리와 델타 방법 — 본선 22·74번의 정규 근사가 언제 정당한지의 판정 기준이다. 증명 수위: 지배수렴과 미분-적분 교환은 완전 증명, CLT는 증명 개요. | 정책 그래디언트 정리의 가정부, score matching과 재매개변수화 논문 각주의 정규성 조건, 'almost surely'로 적힌 모든 수렴 진술, 그리고 추정량의 점근 분포를 유도하는 델타 방법을 직접 쓰게 된다. 본선 20·22·53·74번. |
| `math-adv-radon-nikodym-and-density-ratio` | 라돈–니코딤 도함수: KL·중요도 비율·측도 변환의 공통 정의 | 절대연속성 P≪Q와 라돈–니코딤 정리, dP/dQ가 '밀도비'의 일반형이며 밀도란 기준 측도에 대한 이 도함수라는 관점, 측도 변환을 기댓값 재가중으로 읽기. 그것으로 KL(P‖Q)=E_P[log dP/dQ]가 본선 30번의 Σ p log(p/q)를 포함하는 더 넓은 정의임을 확인하고, 지지집합이 어긋나면 KL이 왜 정확히 +∞인지, 중요도 비율의 분모가 0이 되는 사건이 이론적으로 무엇인지, f-발산 일반이 이 도함수 위에서 정의된다는 것(A6-1로 이어진다), 그리고 절대연속이 아닌 두 분포 사이에서 KL 대신 무엇을 써야 하는지(A7의 바서슈타인). 증명 수위: 라돈–니코딤 정리는 진술과 사용법, KL의 성질은 완전 증명. | off-policy RL의 중요도 표집 가정, GAN이 붕괴하는 지지집합 불일치 논증, DPO/PPO 논문의 π/π_ref 항, f-발산 계열(f-GAN·f-DPO), 그리고 기르사노프 정리(A7)가 전부 이 도함수 하나를 돌려쓴다. 본선 30·58번. |
| `math-adv-conditional-expectation-and-martingales` | 조건부 기댓값은 사영이다, 그리고 마팅게일 | σ-대수에 대한 조건부 기댓값의 측도론적 정의와 그것이 L²에서의 직교 사영과 같다는 정리(A1-2의 사영 정리), 타워 성질 E[E[X\|G]]=E[X]와 전분산 공식, 조건부 젠센, 회귀함수 E[Y\|X]가 제곱오차를 최소화하는 예측자라는 결과 — 확산 모델의 최적 디노이저가 조건부 평균이라는 문장이 여기서 정확해진다. 후반부는 여과와 적응 과정, 마팅게일·서브·슈퍼마팅게일, 옵셔널 스토핑 정리, 마팅게일 수렴 정리, Doob 부등식, 그리고 마팅게일 차분에 대한 Azuma–Hoeffding. 본선 54번의 베이스라인 무편향이 여기서 훨씬 일반적인 형태로 다시 나온다. 증명 수위: 사영 등가성과 타워 성질은 완전 증명, 옵셔널 스토핑은 증명 개요. | 'the optimal denoiser is the conditional expectation'이라는 확산 논문의 한 줄, SGD 수렴 증명의 잡음 분해(A4), 온라인 학습의 후회 경계, 이토 적분의 마팅게일성(A7), e-값과 항시 유효 신뢰수열(A13)이 전부 이 단원 위에 세워진다. 본선 54번. |

### A2 · 대규모 선형대수 (2편) — 본선 3단원의 위층

| 슬러그 | 제목 | 무엇을 쓰는가 | 무엇이 열리는가 |
|---|---|---|---|
| `math-adv-sketching-and-matrix-concentration` | 스케치와 행렬 집중부등식: 큰 행렬을 줄여도 스펙트럼이 남는 이유 | Matrix Chernoff·Matrix Bernstein 부등식의 진술(스칼라 결과가 스펙트럼 노름으로 그대로 올라간다)과 그것으로 얻는 보장들. 부분공간 임베딩의 정의와 오블리비어스 스케치, 카운트 스케치와 부분표본 무작위 아다마르 변환(SRHT), 무작위 스케치로 열공간을 포착하는 range finder, randomized SVD의 알고리즘과 오차 상계, 전력 반복으로 스펙트럼 감쇠가 느릴 때를 보정하기, Nyström 근사와 CUR 분해. 상위 k개 특잇값만 필요할 때 전체 분해가 왜 낭비인지를 연산량으로 비교하고, 본선 16번의 저계수 근사를 수십억 파라미터 행렬 위에서 실제로 수행하는 절차를 만든다. 본선 66번의 존슨–린덴스트라우스를 행렬 수준으로 확장한 판이다. 증명 수위: Matrix Bernstein은 진술과 사용법, randomized SVD 오차 상계는 증명 개요. | Halko–Martinsson–Tropp의 randomized SVD 논문과 그 오차 보장, LoRA 초기화를 SVD로 잡는 논문(PiSSA·LoRA-XS), Performer·Nyströmformer·Linformer의 근사 오차 보장 절. 본선 15·16·66번. |
| `math-adv-krylov-lanczos-and-spectral-density` | 란초스로 헤세 스펙트럼을 재고 랜덤 행렬로 잡음을 걷어 내기 | 크릴로프 부분공간 {v, Av, A²v, …}의 구조와 아르놀디·란초스 반복, 삼중대각화와 리츠 값이 극단 고윳값부터 수렴하는 이유, 켤레경사법이 같은 부분공간 위의 최적해라는 관점과 수렴률이 √조건수로 개선되는 계산. 행렬을 만들지 않고 헤세-벡터 곱만으로 도는 방법, Hutchinson 대각합 추정과 stochastic Lanczos quadrature로 스펙트럼 밀도를 그리는 절차, 재직교화가 없으면 유령 고윳값이 생기는 수치 함정. 후반부는 그렇게 얻은 스펙트럼을 무엇과 비교할 것인가 — 위그너 반원 법칙과 마르첸코–파스투르 분포, 종횡비가 벌크의 지지구간을 정하는 방식, 스파이크 모형과 BBP 상전이(신호 고유벡터가 언제 잡음 벌크에서 분리되는가), 무거운 꼬리 스펙트럼. 무작위 초기화 가중치의 특잇값 분포를 마르첸코–파스투르와 겹쳐 그리고, 학습이 진행되며 꼬리가 무거워지는 변화를 측정한다. 본선 13번의 거듭제곱법과 15번의 특잇값 분포 그림에 '무엇이 잡음인가'라는 기준선을 준다. | 딥러닝 손실 지형의 헤세 스펙트럼 논문(Ghorbani·Papyan의 벌크+이상치 구조), heavy-tailed self-regularization 계열의 모델 품질 진단, K-FAC·Shampoo 구현의 내부, 신뢰영역 방법의 CG-Steihaug 하위 문제. 본선 12·13·15·45·51번. |

### A3 · 확률 부등식과 일반화 (3편) 【심화 절단점 1 — 여기까지 10편이면 유한 표본 보장과 일반화 경계 논문이 열린다】

| 슬러그 | 제목 | 무엇을 쓰는가 | 무엇이 열리는가 |
|---|---|---|---|
| `math-adv-concentration-inequalities` | 집중부등식: 표본 하나가 기댓값에서 얼마나 벗어나는가 | 마르코프에서 체비셰프로, 체르노프 방법의 적률생성함수 기법, 서브가우시안·서브지수 확률변수의 동치 정의들과 오를리츠 노름, n개 최댓값의 기대 상계 √(2σ²log n). 그 위에 Hoeffding(유계), Bernstein(분산을 아는 경우가 왜 훨씬 좋은가), 유계 차분 조건에서의 McDiarmid, 마팅게일 차분에 대한 Azuma(A1-5), 그리고 합집합 경계로 여러 사건을 한 번에 다루기. 각 부등식이 주는 필요 표본 수를 실제로 계산해 본선 74번의 정규 근사 신뢰구간과 나란히 놓고, 정규 근사가 언제 무너지는지 판정한다. 벡터·행렬로의 확장은 A2-1이 맡는다. 증명 수위: 체르노프 방법과 Hoeffding은 완전 증명, Bernstein·McDiarmid는 증명 개요. | 존슨–린덴스트라우스(본선 66번)와 LSH 보장의 원래 증명, PAC 학습 경계, 밴딧 UCB의 신뢰폭 유도(A14), 그리고 '표본 n개면 오차 ε를 확률 1−δ로 보장한다'는 모든 알고리즘 정리를 직접 유도하게 된다. 본선 21·66·74번. |
| `math-adv-uniform-convergence-and-pac-bayes` | 일반화 경계: 고전 이론이 공허해지는 자리와 PAC-Bayes | 경험위험과 실제위험의 간극을 균등수렴으로 다루는 틀, 성장함수와 VC 차원, Sauer 보조정리, Rademacher 복잡도와 대칭화 논법, 마진 기반·노름 기반 경계. 그리고 무작위 라벨을 외울 수 있는 모델이 실제 데이터에서는 일반화한다는 관찰이 이 틀에 던지는 문제 — 파라미터 수 앞에서 경계가 왜 공허해지는가. 후반부는 PAC-Bayes 정리의 구성과 증명, KL(사후‖사전) 항의 역할, 사전분포 선택이 경계를 좌우한다는 것, 비공허(non-vacuous) 경계를 실제로 계산한 사례, 평평한 최소점이 왜 작은 KL로 번역되는지, 압축 기반 경계와 알고리즘 안정성 기반 경계. 증명 수위: Rademacher 대칭화와 PAC-Bayes는 완전 증명, VC는 증명 개요. | Zhang 등의 'rethinking generalization', PAC-Bayes 비공허 경계 논문, SAM 같은 평탄성 지향 최적화의 이론적 근거. 본선 44·49번. |
| `math-adv-benign-overfitting-and-double-descent` | 이중 하강과 양성 과적합: 보간해도 일반화되는 이유 | 최소노름 보간 해의 명시적 폐형, 랜덤 특징 모형에서 위험 곡선을 정확히 계산하기, 보간 문턱에서 위험이 치솟는 이유(설계 행렬의 최소 특잇값)와 그 너머에서 다시 내려가는 이유, 양성 과적합이 성립하는 공분산 스펙트럼 조건(유효 차원과 스펙트럼 감쇠), 그리고 암묵적 편향 — 경사하강이 어떤 노름을 최소화하는 해로 수렴하는가. A2-2의 마르첸코–파스투르가 계산 도구로 그대로 쓰인다. 기존 ml-bias-variance는 고전적 편향-분산 분해와 과녁 비유·학습 곡선 진단을 다루는 글이므로, 이 글은 그 서사가 과매개변수 영역에서 어떻게 수정되는지를 정면으로 계산해 보인다. 증명 수위: 최소노름 해의 폐형과 위험 계산은 완전 증명. | double descent·benign overfitting·implicit regularization 계열 논문의 계산을 스스로 재현하게 되고, A13의 스케일링 법칙 이론이 딛는 커널 회귀 계산과 여기서 만난다. 본선 44·49·80번. |

### A4 · 볼록해석과 최적화 이론 (7편) — 본선 8단원의 위층

| 슬러그 | 제목 | 무엇을 쓰는가 | 무엇이 열리는가 |
|---|---|---|---|
| `math-adv-convex-analysis-and-subgradients` | 볼록해석: 하위기울기, 강볼록성, 매끄러움의 쌍대 | 볼록집합과 볼록함수의 여러 특징화(1차·2차 조건, 에피그래프), 확장실수값 함수로의 일반화와 인디케이터 함수, 하위미분 ∂f와 최적성 조건 0 ∈ ∂f, ReLU와 L1이 0에서 갖는 것, Clarke 일반화 미분과 ReLU가 0에서 어떤 값을 골라도 되는 이유의 정확한 서술. 강볼록성과 L-매끄러움이 서로 켤레라는 사실, 그리고 그 둘이 함께 주는 부등식들. 본선 29번의 볼록성과 44번의 헤세 판정을 미분 불가능한 경우까지 확장한다. 증명 수위: 최적성 조건과 강볼록–매끄러움 쌍대는 완전 증명. | L1·ReLU·핵노름처럼 미분 불가능한 항이 든 목적식의 최적성 조건을 논문 수준으로 정확히 쓰게 되고, 비매끄러운 SGD 수렴 정리의 가정부를 읽게 된다. 본선 29·38·44·46번. |
| `math-adv-fenchel-and-lagrangian-duality` | 르장드르–펜첼 변환과 쌍대성: 문제를 뒤집어 푸는 법 | 켤레함수 f*(y)=sup_x{⟨x,y⟩−f(x)}의 정의와 기하(접선의 절편), 이중 켤레 정리와 볼록 폐포, 지지함수, 노름과 쌍대 노름의 켤레 관계. 라그랑주 쌍대 문제의 구성, 약쌍대·강쌍대와 Slater 조건, 쌍대 간극으로 최적성을 증명하기. 본선 56번의 KKT를 쌍대성의 특수 경우로 다시 세우고, 57번의 π* ∝ π_ref·exp(r/β)라는 닫힌 해가 사실 엔트로피 항의 켤레 계산 하나라는 것을 보인다. 증명 수위: 약쌍대성과 켤레의 성질은 완전 증명, 강쌍대성은 증명 개요. | 엔트로피 정규화 문제의 해가 softmax·볼츠만 형태로 나오는 이유가 켤레 하나로 설명되고, 최적수송 쌍대(A7)·f-발산 변분 표현(A6)·미니맥스 정리(A14)가 전부 이 글의 응용이다. 본선 56·57번. |
| `math-adv-proximal-and-mirror-descent` | 근접 연산자와 거울 하강: 기하를 바꿔서 내려가기 | 근접 연산자의 정의와 사영·soft-thresholding·핵노름의 특잇값 수축이 그 특수경우라는 유도, Moreau 포락, 근접 경사법(ISTA)과 가속(FISTA)의 수렴률 O(1/k²), ADMM의 뼈대. 가중치 감쇠를 근접 단계로 읽으면 본선 48번의 AdamW가 왜 그 형태인지가 다시 설명된다. 후반부는 브레그만 발산의 정의와 KL이 음의 엔트로피의 브레그만 발산이라는 계산, 거울 하강의 갱신식 유도, 단체(simplex) 위에서 그것이 정확히 지수가중 갱신이 된다는 것, 그리고 본선 57번의 KL 제약 최적 정책이 거울 하강 한 걸음과 같다는 관점. 증명 수위: soft-threshold 유도와 단체 위 거울 하강은 완전 증명, FISTA 수렴률은 증명 개요. | 희소 오토인코더와 구조적 가지치기의 근접 단계, AdamW와 분리형 정규화의 이론적 정당화, TRPO·MDPO 계열이 KL로 한 걸음을 재는 이유, 온라인 볼록 최적화의 후회 분석. 본선 46·48·56·57번. |
| `math-adv-stochastic-approximation-and-sgd-sde` | 확률적 근사와 SGD의 연속 극한: 수렴한다는 말의 정확한 뜻 | Robbins–Monro의 스텝 조건 Ση=∞, Ση²<∞와 그 필요성, 마팅게일 잡음 분해(A1-5), ODE 방법으로 이산 갱신을 연속 흐름의 근사로 보기. 볼록·강볼록·PL 조건 각각에서의 수렴률 O(1/√T)·O(1/T)와 비볼록에서 그래디언트 노름 기준의 보장이 전역해를 뜻하지 않는다는 것, 오라클 복잡도 하한과 네스테로프 가속이 그 하한과 맞물린다는 계산, 분산 감소(SVRG·SAG)가 무엇을 벌고 왜 딥러닝에서는 잘 안 쓰이는지. 후반부는 미니배치 잡음을 확산항으로 근사해 SGD를 SDE로 적고 lr/batch가 온도처럼 작동해 정상분포를 정한다는 관점, 그 정상분포가 평평한 최소점에 질량을 몰아준다는 논증, 수정 방정식으로 얻는 암묵적 그래디언트 정규화 항, 그리고 edge of stability — 학습률이 2/λ_max를 넘어도 발산하지 않고 곡률 자체가 내려오는 현상. 본선 46번의 2/L 상한과 49번의 잡음·스케줄 논의가 여기서 이론으로 닫힌다. | Adam·TD 학습의 수렴 증명, SGD의 SDE 근사와 배치–학습률 스케일링 규칙, 암묵적 편향과 edge of stability 논문, 학습률 스케줄을 이론에서 유도하는 최근 연구. 본선 46·47·49번. |
| `math-adv-preconditioning-and-second-order-theory` | 프리컨디셔닝의 이론: Adam에서 K-FAC·Shampoo·Muon까지 한 줄로 | 프리컨디셔너 P⁻¹∇의 일반형과 아핀 불변성, 뉴턴·가우스–뉴턴·피셔·경험적 피셔가 각각 무엇을 근사하는지의 관계도, 준뉴턴 BFGS의 시컨트 조건과 저장 비용, 신뢰영역과 3차 정규화가 안장점을 탈출한다는 보장. 그다음 K-FAC의 크로네커 인수분해를 유도하고 그것이 딛는 층간·활성-그래디언트 독립 가정을 명시한다(이 유도는 커리큘럼 전체에서 여기 한 곳에만 둔다 — A6의 정보기하 글은 피셔가 무엇인지와 자연그래디언트의 불변성만 맡는다). Shampoo의 행·열 프리컨디셔너와 행렬 p-제곱근 계산, Muon을 스펙트럴 노름 아래 최급강하로 읽는 유도와 Newton–Schulz 반복, 그리고 이들이 어떤 극한에서 서로 일치하는지. 본선 51번이 나열만 한 근사들의 이론적 자리를 정한다. | K-FAC·Shampoo·SOAP·Muon 논문의 유도 절, 3차 정규화의 안장점 탈출 정리, 옵티마이저 벤치마크 논문의 이론 근거. 본선 51번. |
| `math-adv-riemannian-optimization` | 다양체 위의 최적화: 직교 제약과 Muon의 직교화가 하는 일 | 매끄러운 다양체·차트·접공간·접다발, 미분사상(pushforward), 리만 계량이 정하는 내적과 거리, 측지선과 지수사상·retraction, 평행 이동. 스티펠·그라스만·양정치 행렬 다양체와 확률 단체의 구체적 표현, 리만 그래디언트가 유클리드 그래디언트의 접공간 사영이라는 계산, 직교 제약 아래 경사하강을 실제로 도는 알고리즘, 극분해로 후퇴를 구현하기, Newton–Schulz 반복으로 UVᵀ를 근사하는 계산과 수렴 조건, 그리고 수치적 함정(직교성이 누적 오차로 무너지는 자리). A6의 자연그래디언트가 통계다양체 위의 같은 이야기라는 것도 여기서 예고한다. 증명 수위: 리만 그래디언트 = 접공간 사영은 완전 증명. | Muon 옵티마이저의 직교화 단계, 직교·유니터리 RNN, 초구 위 임베딩 학습, 자연 그래디언트를 다양체 언어로 적은 논문. 본선 7·11·51번. |
| `math-adv-loss-landscape-and-mode-connectivity` | 손실 지형의 이론: 대칭, 모드 연결, 날카로움, 그리고 안장점 | 신경망 파라미터화의 대칭군(뉴런 치환, ReLU 스케일 재조정, 회전)과 그것이 만드는 등가 다양체, 모드 연결성과 치환을 맞추면 선형 모드 연결이 성립한다는 결과, 손실 헤세의 스펙트럼 구조(벌크 + 클래스 수만큼의 이상치)와 그 출처(A2-2에서 실제로 측정한 그림). 1차 방법이 안장점을 빠져나오는 데 잡음이 하는 역할과 엄격 안장 성질, PL·KL 부등식 아래에서 비볼록인데도 선형 수렴이 나오는 계산, 과매개변수화가 지형을 바꾸는 방식. 후반부는 날카로움의 정의들이 재파라미터화에 불변이 아니라는 문제, SAM이 실제로 최소화하는 양의 1차 근사, 평평함과 일반화를 잇는 주장이 어디서 성립하고 어디서 깨지는지. 본선 44번의 서술을 정리로 바꾼다. | Git Re-Basin과 선형 모드 연결 논문, SAM과 그 변형, 'gradient descent escapes saddle points' 계열 정리, 모델 병합이 왜 되는지를 다루는 연구. 본선 44·45번. |

### A5 · 커널과 무한폭 (6편) — 본선 11단원과 50번의 위층

| 슬러그 | 제목 | 무엇을 쓰는가 | 무엇이 열리는가 |
|---|---|---|---|
| `math-adv-rkhs-representer-and-gaussian-processes` | RKHS와 표현자 정리, 그리고 그 베이즈 쌍둥이인 가우시안 과정 | 재생 커널의 정의와 평가 범함수의 유계성, A1-2의 리스 표현 정리로 얻는 재생 성질 f(x)=⟨f,k(x,·)⟩, 커널과 RKHS의 일대일 대응(Moore–Aronszajn), RKHS 노름이 재는 함수의 매끄러움. 그리고 표현자 정리의 완전 증명 — 정규화된 경험위험 최소화의 해가 반드시 데이터 점의 커널 결합 Σαᵢk(xᵢ,·)이 된다는 것과 그 성립 조건 — 에서 커널 능선회귀의 폐형 해를 얻는다. 후반부는 같은 결과의 베이즈 판: 가우시안 과정을 '함수 위의 정규분포'로 정의하고 본선 23번의 다변량 정규 조건부 공식에서 사후 평균·분산의 폐형을 유도해 사후 평균이 정확히 커널 능선회귀임을 확인하며, 주변가능도로 하이퍼파라미터를 학습하는 것이 자동으로 오컴의 면도날이 되는 이유와 O(n³) 비용, 그리고 EI·UCB 획득함수를 마지막 절에 짧게 붙인다. 기존 ml-svm은 sklearn SVM 사용법과 커널 트릭·주요 커널 함수 소개이고 본선 69번은 그람 행렬의 PSD 판정이므로, 이 글은 함수공간 이론과 표현자 정리만 맡는다. | 커널 방법의 원 논문들, NTK를 커널 회귀로 읽는 관점(A5-5·6), NNGP, 베이즈 최적화와 대리모델 논문. 본선 23·69번. |
| `math-adv-mercer-and-random-features` | Mercer 정리와 랜덤 특징: 커널의 스펙트럼과 유한 차원 근사 | 커널 적분작용소와 Mercer 전개(A1-2의 컴팩트 자기수반 스펙트럼 정리의 응용), 고유값 감쇠율이 함수 클래스의 매끄러움을 정한다는 것, 유효 차원(effective dimension)과 그것이 학습 곡선의 지수를 정하는 계산, 커널 회귀의 편향–분산 분해. 후반부는 보흐너 정리(평행이동 불변 커널 ↔ 양의 측도의 푸리에 변환)와 랜덤 푸리에 특징의 구성, 근사 오차의 집중 분석(A3-1), 양수성을 보장하는 특징 사상, Nyström 근사와의 비교. 증명 수위: 보흐너와 랜덤 특징 근사 오차는 완전 증명, Mercer는 증명 개요. | Performer의 FAVOR+와 선형 어텐션 계열이 정확히 이 근사를 어텐션 커널에 적용한 것이고, NTK 스펙트럼과 스펙트럴 편향(A5-5, A9-1), 커널 회귀 학습 곡선의 지수(A13의 스케일링 법칙 이론)가 전부 이 결과의 응용이다. 본선 69번. |
| `math-adv-mmd-and-kernel-two-sample-tests` | MMD와 커널 두 표본 검정: 두 분포가 다른지 표본만으로 판정하기 | 적분 확률 계량의 틀, 커널 평균 임베딩 μ_P=E_P[k(X,·)]와 특성 커널일 때 임베딩이 단사라는 것, MMD의 정의와 불편 추정량, 검정통계량의 귀무분포와 순열 검정으로 임계값을 얻는 절차, 커널 대역폭 선택이 검정력에 미치는 영향. MMD가 f-발산·바서슈타인과 어떻게 다른지(지지집합이 어긋나도 유한하다), 독립성 검정 HSIC, 그리고 스타인 항등식과 커널 스타인 불일치로 '표본이 이 분포에서 나왔는가'를 정규화 상수 없이 검정하기. 본선 75~76번의 재표본 검정과 곧장 이어진다. | 생성 표본 품질 검정과 MMD-GAN, 분포 시프트 탐지, 커널 스타인 검정, 임베딩 공간에서 두 데이터셋을 비교하는 실무 절차의 이론적 근거. 본선 68·75·76번. |
| `math-adv-signal-propagation-and-nngp` | 평균장 신호 전파와 NNGP: 깊이 방향의 질서–혼돈 경계 | 폭이 넓은 무작위 신경망에서 활성값의 분산과 두 입력 사이의 상관이 층을 지나며 따르는 사상(map)을 유도하고, 그 사상의 고정점과 안정성으로 질서 상(모든 입력이 같아짐)과 혼돈 상(상관이 소멸)의 상전이를 그린다. 임계선 위에서만 정보가 깊이 전파된다는 결과, dynamical isometry(야코비안 특잇값이 1 근처에 몰리는 조건)와 직교 초기화, 그리고 잔차 연결과 정규화가 망을 임계에 붙들어 두는 방식. 후반부는 폭이 무한대로 갈 때 초기화된 신경망의 사전분포가 가우시안 과정으로 수렴한다는 NNGP 결과와 층마다의 커널 재귀식 유도, 활성함수별 아크코사인 커널, 깊이에 따른 커널의 고정점. 본선 50번의 분산 전파를 상관과 극한까지 민 결과다. 증명 수위: 분산·상관 재귀식은 완전 증명, GP 수렴은 증명 개요. | mean field theory of deep networks와 dynamical isometry 논문, NNGP 계열의 커널 재귀식을 직접 유도하고 코드로 재현하게 된다. 매우 깊은 망이 왜 학습 가능해졌는지의 정량적 답. 본선 50번. |
| `math-adv-neural-tangent-kernel` | NTK: 학습이 선형이 되는 영역과 그 커널의 스펙트럼 | 파라미터에 대한 1차 전개로 얻는 접커널의 정의, 폭이 무한대일 때 학습 중 커널이 상수로 고정된다는 정리와 그 아래 학습이 커널 회귀와 같아진다는 결과, 수렴 속도가 NTK 고유값으로 결정된다는 계산(A5-2의 고유값 감쇠), 저주파를 먼저 학습하는 스펙트럴 편향, 유한 폭에서의 보정항. 게으른(lazy) 영역이 특징 학습을 못 한다는 한계와 그것이 실제 딥러닝을 얼마나 설명하고 어디서 실패하는지. 본선 46번의 수렴 논의가 여기서 스펙트럼으로 분해된다. 증명 수위: 접커널의 상수성은 증명 개요 + 핵심 보조정리, 커널 회귀 등가는 완전 증명. | Jacot의 NTK 논문과 그 후속 이론 전반, 무한폭 극한을 쓰는 모든 분석, 스펙트럴 편향 논문, 그리고 다음 글 muP가 '왜 표준 파라미터화가 특징 학습을 죽이는가'를 논하는 출발점. 본선 46·50번. |
| `math-adv-feature-learning-and-mup` | 특징 학습 영역과 muP: 폭에 따라 무엇을 어떻게 스케일하나 | 폭 n이 커질 때 각 층의 활성값과 갱신 크기가 어떻게 스케일해야 하는지 좌표 단위로 요구조건을 세우고(coordinate check를 실제로 돌리는 절차 포함), 표준 파라미터화가 그것을 왜 위반해 게으른 영역으로 떨어지는지, abc-파라미터화 분석으로 극한이 갈리는 지점을 정리한다. 최대 갱신 파라미터화(muP)의 초기화 분산·학습률·출력 배율 규칙을 유도하고, 폭 전이와 깊이 전이의 차이, 그리고 작은 모델에서 튜닝한 학습률이 큰 모델로 옮겨 가는 이유(muTransfer)를 실험으로 확인한다. 본선 50번의 마지막 절이 여기서 완결된다. | Yang의 Tensor Programs와 muP·muTransfer 논문의 스케일링 표를 그대로 읽고 자기 모델에 적용하게 된다. 심화 트랙에서 실무 효용이 가장 즉각적인 글이다. 본선 50번. |

### A6 · 정보와 기하 (4편) 【심화 절단점 2 — 여기까지 27편이면 목적식 논문의 유도가 열린다】

| 슬러그 | 제목 | 무엇을 쓰는가 | 무엇이 열리는가 |
|---|---|---|---|
| `math-adv-f-divergences-and-variational-representations` | f-발산과 변분 표현: GAN·MINE·InfoNCE가 한 틀에서 나온다 | f-발산의 통일된 정의와 KL·역KL·헬링거·전변동·χ²·JS가 전부 볼록함수 f 하나의 선택이라는 표, 데이터 처리 부등식, 그리고 켤레 변환(A4-2)으로 얻는 변분 표현 D_f(P‖Q)=sup_T{E_P[T]−E_Q[f*(T)]}. 여기서 Donsker–Varadhan 표현과 그것이 주는 KL 하한을 얻고, GAN의 판별자가 f-발산을 추정하는 장치임을 보이며, MINE·InfoNCE·NWJ 하한을 한 표에 놓아 편향과 분산을 비교한다. 본선 31번의 log N 천장이 어느 하한의 어떤 성질에서 나오는지 정확히 짚고, 발산을 새로 제안하는 논문이 무엇을 바꾸고 무엇을 유지했는지 분류하는 절차로 닫는다. 증명 수위: 변분 표현과 Donsker–Varadhan은 완전 증명. | f-GAN·MINE·InfoNCE 계열 대조학습 논문의 목적식 절, KL 페널티를 다른 발산으로 바꾼 정렬 논문(f-DPO 등). 본선 30·31번. |
| `math-adv-exponential-family-and-max-entropy` | 지수족과 최대엔트로피: softmax와 볼츠만 해가 같은 곳에서 나온다 | 지수족의 표준형 p(x)=h(x)exp(η·T(x)−A(η)), 충분통계량과 자연 파라미터, 로그 분배함수 A(η)가 볼록이고 그 그래디언트가 충분통계량의 기댓값·헤세가 공분산이라는 정리, 자연 파라미터와 평균(모멘트) 파라미터가 르장드르 변환으로 이어진 쌍대 좌표라는 구조. 그리고 최대엔트로피 원리 — 모멘트 제약 아래 엔트로피를 최대화하면 반드시 지수족이 나온다는 것 — 을 라그랑주 승수로 유도한다. 이것으로 본선 24번의 softmax, 57번의 KL 제약 최적 정책 π* ∝ π_ref·exp(r/β), 에너지 기반 모델 p ∝ exp(−E)가 전부 그 한 정리의 사례임을 보인다. 가우시안·범주분포·베르누이가 한 가족임을 확인하는 표까지. 증명 수위: A(η)의 미분 성질과 최대엔트로피 유도는 완전 증명. | 변분추론의 자연 파라미터 갱신(A8-2), 최대엔트로피 강화학습(SAC)의 목적식 유도, RLHF 최적 정책을 지수 기울임으로 적는 정렬 논문, 에너지 기반 모델(A8-1). 본선 24·26·56·57번. |
| `math-adv-fisher-information-and-natural-gradient` | 정보기하와 자연 그래디언트: 분포 공간에서 내려가기 | 점수 함수의 기댓값이 0이라는 항등식, 피셔 정보 행렬의 두 표현(점수의 공분산 = 음의 기대 헤세)과 동치 증명, 크라메르–라오 하한, 그리고 피셔가 KL의 2차 근사로 나온다는 계산 — 이 계산은 커리큘럼 전체에서 여기 한 곳에만 둔다. 통계다양체 위의 리만 계량으로서의 피셔와 첸초프 정리(사실상 유일한 불변 계량)의 서술, 자연 그래디언트 F⁻¹∇L의 유도와 그것이 파라미터 재표현에 불변인 이유(A4-6의 리만 최적화가 여기 그대로 적용된다), 지수족의 이중 평탄 구조와 e-사영·m-사영, 경험적 피셔와 진짜 피셔의 차이, 가우스–뉴턴 행렬과의 관계. K-FAC의 크로네커 인수분해 유도는 A4-5가 맡으므로 여기서는 '무엇을 근사하는 대상인가'만 확정한다. 본선 61번의 스코어 ∇ₓ log p와 여기의 점수 ∇_θ log p가 다른 대상임을 명확히 구분한다. | Amari의 자연 그래디언트, TRPO가 KL 신뢰영역을 피셔로 근사하는 절, K-FAC·Shampoo 논문이 근사하는 대상, EWC 같은 연속학습 정규화의 피셔 항. 본선 26·30·51·58번. |
| `math-adv-rate-distortion-mdl-and-compression` | 율–왜곡·정보 병목·MDL: 압축으로서의 학습 | 율–왜곡 함수 R(D)의 정의와 볼록성, Blahut–Arimoto 알고리즘, 채널 용량과의 이중성, 정보 병목 목적식 min I(X;Z)−βI(Z;Y)와 그 라그랑주 구조·자기일관 방정식, 변분 근사(VIB)와 β-VAE가 같은 교환 곡선 위에 있다는 관점. 후반부는 최소 기술 길이 — 콜모고로프 복잡도의 정의와 계산 불가능성, 무접두 부호와 크래프트 부등식, 정보량과 부호 길이의 대응, 2부 부호와 MDL 원리, 그리고 '언어모델은 압축기다'라는 주장을 산술부호화와 로그가능도의 대응으로 정확히 적어 본선 26번의 bits-per-byte를 이론으로 받친다. 증명 수위: 크래프트 부등식과 로그 손실 = 부호 길이는 완전 증명. | 정보 병목 기반 딥러닝 해석 논문과 그 비판, 변분 정보 병목, 무손실 압축률로 모델을 비교하는 최근 연구, MDL 기반 일반화 경계. 본선 26·28·31번. |

### A7 · 확률과정과 생성의 연속시간 이론 (9편) 【심화 절단점 3 — 여기까지 36편이면 확산·흐름 매칭의 이론이 완결된다. 수렴점 둘이 여기 있다】

| 슬러그 | 제목 | 무엇을 쓰는가 | 무엇이 열리는가 |
|---|---|---|---|
| `math-adv-markov-chains-mixing-and-mcmc` | 마르코프 연쇄·혼합 시간·MCMC: 상세 균형에서 HMC까지 | 전이 커널과 불변분포, 기약성·비주기성·에르고딕 정리, 상세 균형, 전변동 거리로 정의한 혼합 시간, 스펙트럼 갭과 혼합 시간의 관계, 커플링 논법. 후반부는 메트로폴리스–헤이스팅스의 상세 균형 증명, 해밀토니안 몬테카를로의 구성과 립프로그 적분기가 부피를 보존한다는 것(심플렉틱), 수용률과 스텝 크기의 최적 조정, R̂와 유효 표본 수 같은 수렴 진단. 다봉분포에서 혼합이 지수적으로 느려지는 이유와 그것이 A7-5의 랑주뱅 수렴률과 어떻게 이어지는지. 증명 수위: 상세 균형과 MH의 정당성은 완전 증명, 에르고딕 정리는 진술과 사용법. | 베이지안 딥러닝과 표집 기반 추론 논문의 알고리즘·진단 지표, MCMC 수렴 진단, 그리고 랑주뱅 기반 샘플러(A7-5)의 속도 논의가 여기서 정량화된다. |
| `math-adv-brownian-motion-and-ito` | 브라운 운동과 이토 미적분: dW²=dt가 뜻하는 것 | 브라운 운동의 정의와 존재, 정상 독립증분, 경로가 거의 모든 점에서 미분 불가능하다는 성질, 이차변동이 t라는 결정적 계산. 이토 적분의 구성과 왜 보통 적분(스틸체스)으로는 안 되는지, 이토 적분이 마팅게일이라는 것(A1-5), 이토 보조정리의 유도와 (dW)²=dt로 요약되는 2차 항이 왜 보통 연쇄법칙과 다른지, 스트라토노비치 적분과의 차이. SDE의 강해·약해와 존재·유일성 조건, 기하 브라운 운동과 OU 과정 같은 닫힌 해, 오일러–마루야마 이산화로 시뮬레이션해 이론과 맞추기. 기초 트랙의 미분방정식 첫걸음과 본선 62번 사이를 잇는다. 증명 수위: 이차변동과 이토 보조정리는 완전 증명. | Song 등의 score-based SDE 논문 §3의 첫 여섯 식이 그대로 이 언어로 쓰여 있다. 확산 모델·SGD의 연속 근사(A4-4) 논문의 계산이 전부 이토 보조정리 한 줄로 이루어진다. 본선 62번. |
| `math-adv-fokker-planck-and-time-reversal` | 포커–플랑크와 시간 역전: 역과정에 스코어가 나타나는 이유 | SDE의 생성원, 콜모고로프 전방 방정식(포커–플랑크)의 유도로 밀도의 시간 전개를 적기, 정상분포가 깁스 분포가 되는 조건, 그리고 본선 62번의 확산 forward 과정을 SDE로 정확히 적기. 핵심은 Anderson의 시간 역전 정리 — 역시간 SDE의 표류항에 ∇log p_t(x)가 정확히 등장한다는 것 — 의 진술과 증명이다. 본선 61번이 '노이즈 예측이 곧 스코어 추정'이라 말한 자리에서, 왜 하필 스코어여야 역과정이 닫히는지에 정리로 답한다. 확률 흐름 ODE가 같은 주변분포를 갖는다는 결과도 여기서 유도하고, SDE 샘플러와 ODE 샘플러의 차이를 정확히 말한다. 증명 수위: 포커–플랑크와 시간 역전 정리는 완전 증명 — 이 커리큘럼의 수렴점 중 하나다. | 확산 모델 이론의 심장부. DDPM·score SDE·DDIM·consistency model 논문의 유도가 전부 이 정리 하나에서 갈라져 나온다. 본선 61·62번. |
| `math-adv-girsanov-and-path-measures` | 기르사노프 정리와 경로 측도: 표류를 바꾸면 확률이 어떻게 바뀌나 | 경로공간 위의 측도, 지르사노프 정리(표류항을 바꾸는 것이 라돈–니코딤 도함수를 곱하는 것과 같다 — A1-4의 직접 확장), 그리고 두 확산 과정 사이의 경로공간 KL이 표류 차이의 제곱 적분으로 떨어지는 계산. 이것으로 score matching 목적식이 두 과정 사이 KL의 상한임을 보이고, 확산 모델의 우도 경계가 어디서 나오는지 적는다. 그리고 '유도(guidance)를 건다'는 조작이 목표 측도를 어떻게 기울이는지, KL 정규화된 확산 미세조정의 목적식이 무엇인지. 증명 수위: 경로공간 KL 계산은 완전 증명, 기르사노프는 증명 개요 + 핵심 보조정리. | 확산 모델의 RL 미세조정(DDPO·DPOK), 확률 제어로 본 guidance, Schrödinger bridge의 목적식, 그리고 경로공간 KL이 등장하는 제어된 생성 논문. 본선 62·63번. |
| `math-adv-langevin-and-log-sobolev` | 랑주뱅 동역학의 수렴: 로그–소볼레프 부등식과 혼합 속도 | 오버댐프드 랑주뱅 SDE와 그 정상분포가 목표 분포라는 것, 상대 엔트로피가 지수적으로 감소한다는 계산, 로그–소볼레프·푸앵카레 부등식이 정하는 수렴률과 두 부등식의 관계, 이산화(ULA)의 편향과 MALA 보정, 다봉분포에서 혼합이 느려지는 이유(A7-1의 스펙트럼 갭). 강볼록 퍼텐셜에서 LSI 상수가 어떻게 나오는지와 그것이 요구하는 가정이 실제 데이터 분포에서 성립하지 않는다는 정직한 지적까지. 증명 수위: 엔트로피 감소율과 LSI ⇒ 지수 수렴은 완전 증명. | 랑주뱅 기반 표집과 확산 모델의 수렴 보장 논문이 요구하는 가정이 무엇인지 읽게 되고, 에너지 기반 모델(A8-1)의 샘플러가 왜 그렇게 도는지 알게 된다. |
| `math-adv-continuity-equation-and-neural-ode` | 연속성 방정식과 뉴럴 ODE: 속도장이 밀도를 옮기는 법 | 질량 보존에서 연속성 방정식 ∂_t p + ∇·(pv)=0을 세우고 속도장 v가 밀도를 어떻게 수송하는지 그림과 시뮬레이션으로 확인한다. 그다음 뉴럴 ODE와 순간 변수변환 공식 d log p/dt = −tr(∂v/∂x) — 본선 34번의 야코비안 행렬식이 연속 시간에서 대각합으로 바뀌는 것 — 을 유도하고, 수반(adjoint) 민감도 방법으로 메모리 없이 역전파하는 원리, Hutchinson 추정기로 대각합을 싸게 추정하는 트릭. 정규화 흐름의 변수변환 공식과 log det 야코비안을 싸게 만드는 구조들(결합층·자기회귀·행렬식이 삼각인 설계)이 연속 시간에서 왜 필요 없어지는지. 증명 수위: 연속성 방정식과 순간 변수변환은 완전 증명. | Neural ODE·FFJORD·CNF 논문과 정규화 흐름(RealNVP·Glow)의 log det 절, 본선 64번 흐름 매칭의 결정론 쪽 토대. 본선 34·62·64번. |
| `math-adv-optimal-transport-duality-and-brenier` | 최적수송: 몽주–칸토로비치 쌍대성과 브레니에 정리 | 몽주 문제와 칸토로비치 완화, 수송 계획과 결합측도, 쌍대 문제와 c-볼록성(A4-2의 켤레 변환이 그대로 응용된다), W_p 거리가 계량 공리를 만족한다는 것, 1차원에서 분위수 함수로 닫힌 형태가 나오는 계산, Kantorovich–Rubinstein 공식(W₁이 1-립시츠 함수 위의 상한 — WGAN 비평자의 립시츠 제약이 정확히 이것), 브레니에 정리(2차 비용의 최적 사상은 볼록함수의 그래디언트), 변위 보간과 측지선, 그리고 W가 KL과 달리 지지집합이 어긋나도 유한하다는 성질. 본선 64번이 한 문단으로 스친 자리를 통째로 연다. 증명 수위: 약쌍대성과 KR 공식은 완전 증명, 브레니에는 증명 개요. | WGAN의 쌍대 논증, 흐름 매칭·정류 흐름 논문의 최적수송 절, 도메인 정렬과 표현 정렬 논문의 수송 비용. 본선 64번. |
| `math-adv-entropic-ot-and-sinkhorn` | 엔트로피 정규화와 Sinkhorn: 미분 가능한 매칭 | 엔트로피 정규화된 최적수송의 쌍대와 해가 대각 스케일링 형태 diag(u)K diag(v)로 나온다는 유도, Sinkhorn 반복이 교대 사영이며 A1-1의 축약사상 원리로 수렴한다는 증명과 수렴 속도, 정규화 세기가 만드는 편향과 Sinkhorn divergence로 그것을 보정하기, 계산 복잡도, 그리고 반복이 미분 가능해 손실로 쓸 수 있다는 것(unrolling과 암묵 미분 두 방식). 로그 영역 구현이 왜 필요한지도 수치로 짚는다. 증명 수위: 해의 형태와 수렴은 완전 증명. | Sinkhorn 기반 매칭·클러스터링·균형 잡힌 MoE 라우팅, 도메인 적응의 Sinkhorn 손실, 그리고 다음 글의 IPF/Schrödinger bridge. 본선 64번. |
| `math-adv-wasserstein-gradient-flow-and-mean-field` | 바서슈타인 경사흐름과 평균장 극한: 확산·학습·흐름 매칭이 한 정리 아래 모인다 | 측도 공간 위의 에너지 범함수와 그 경사흐름을 정의하고, JKO 스킴(바서슈타인 근접점 반복 — A4-3의 근접 연산자를 분포 공간에서 다시 쓴 것)의 극한이 포커–플랑크 방정식이라는 결과, 즉 확산이 KL 자유에너지의 바서슈타인 경사하강이라는 정리를 유도한다. 그다음 같은 틀로 두 층 신경망을 뉴런의 경험 측도로 다시 쓰고, 폭이 무한대일 때 학습이 측도의 바서슈타인 경사흐름이 된다는 평균장 극한과 전역 수렴 정리, 그리고 A5-5의 NTK(게으른) 영역과의 대비. 마지막으로 슈뢰딩거 다리 문제(두 분포를 잇는 경로 중 사전 과정에 가장 가까운 것)와 IPF 반복, 확률적 보간자와 정류 흐름이 이 틀에서 어디에 앉는지 정리한다. 증명 수위: JKO ⇒ 포커–플랑크는 완전 증명 — 이 커리큘럼의 수렴점 중 하나다. | 따로 배운 세 가지(확산·평균장 학습·흐름 매칭)가 한 정리 아래 모이는 것을 보게 된다. mean-field 이론 논문, rectified flow·stochastic interpolant·Schrödinger bridge 계열 최신 생성모델의 이론부. 본선 62·64번. |

### A8 · 에너지·변분·트랜스포머의 이론 (6편)

| 슬러그 | 제목 | 무엇을 쓰는가 | 무엇이 열리는가 |
|---|---|---|---|
| `math-adv-energy-based-models-and-stein` | 에너지 기반 모델과 스타인: 정규화 상수를 피하는 방법들 | 에너지 기반 모델 p ∝ exp(−E)와 분배함수가 계산 불가능하다는 문제(A6-2의 지수족이 배경), 대조 발산과 그 편향, 노이즈 대조 추정(NCE)의 일치성, Hyvärinen의 원형 점수 매칭과 그 미분 항이 헤세 대각합이라는 계산, 그리고 denoising score matching이 그 대각합을 우회하는 방식의 완전한 유도 — 본선 61번이 결과만 쓴 자리다. 랑주뱅 동역학으로 EBM에서 표집하기(A7-5)와 이산화 편향의 MALA 보정, 스타인 항등식과 커널 스타인 불일치, 스타인 변분 경사하강(SVGD)이 바서슈타인 경사흐름의 커널 근사라는 관점(A7-9). 증명 수위: 원형 ↔ denoising 점수 매칭 등가는 완전 증명. | Hyvärinen의 점수 매칭 원 논문, NCE와 그 대조학습 후예, 랑주뱅 기반 샘플러, 커널 스타인 검정, EBM으로 본 LLM 정렬 논문. 본선 61번. |
| `math-adv-variational-inference-theory` | 변분추론의 이론: 하한을 얼마나 조일 수 있는가 | 변분족과 ELBO의 간극이 정확히 KL(q‖p)라는 것, 평균장 근사와 좌표 상승 VI의 갱신식, 그것이 만드는 편향과 분산 과소추정, 정방향·역방향 KL의 기하, 확률적 VI와 자연 파라미터 갱신(A6-2·A6-3), amortization gap. 재매개변수화 그래디언트와 점수 함수 그래디언트의 분산 비교와 통제 변량 설계(본선 53·59번의 상위판), 중요도 가중 ELBO(IWAE)가 K를 늘리면 더 조여진다는 증명과 그 대가로 인코더 그래디언트 신호가 약해진다는 분석, 사후 붕괴의 원인과 β-VAE의 라그랑주 해석. 증명 수위: ELBO 분해와 IWAE 단조성은 완전 증명. | IWAE, 정규화 흐름(RealNVP·Glow), β-VAE, 확산의 변분 관점(VDM) 논문의 유도 절. 변분 목적식을 새로 제안하는 논문이 무엇을 완화했고 무엇을 대가로 치렀는지 판정하게 된다. 본선 53·59·60번. |
| `math-adv-automatic-differentiation-theory` | 자동미분의 이론: 무엇이 최적 배치이고 무엇이 조용히 틀리는가 | 계산 그래프를 미분하는 세 방식(이중수, 연산자 오버로딩, 소스 변환), 전방·역방향을 임의로 섞는 혼합 모드와 최적 제거 순서 결정이 NP-난해라는 사실, Baur–Strassen 정리(그래디언트 비용이 함숫값 비용의 상수배), 체크포인팅의 최적 재계산 스케줄과 메모리-연산 맞바꿈 곡선. 고계 미분을 JVP와 VJP의 합성으로 얻기, 헤세-벡터 곱을 두 번의 통과로 계산하기(A2-2가 그것을 쓴다), 제어 흐름·인플레이스 연산·비매끄러운 원시연산에서 미분이 조용히 틀리는 자리, 그리고 암묵 함수 정리로 고정점과 최적화 문제 자체를 미분하기. 본선 35번의 비용 논증을 정리로 바꾼다. | JAX의 변환 조합 설계, 그래디언트 체크포인팅 논문, 암묵 미분 기반 이중수준 최적화(메타학습·하이퍼파라미터 최적화), DEQ(심층 균형 모델). 본선 33·34·35번. |
| `math-adv-attention-as-kernel-and-associative-memory` | 어텐션을 커널과 연관 기억으로 다시 쓰기 | softmax 어텐션을 비대칭 커널의 Nadaraya–Watson 회귀로 적기, 커널 관점에서 선형 어텐션이 특징 사상 φ의 선택일 뿐이며 그때 계산이 O(n)로 떨어지는 대수(A5-2의 랜덤 특징이 φ의 한 선택), 현대 홉필드 네트워크의 갱신 규칙이 어텐션 한 층과 같다는 대응과 거기서 나오는 저장 용량 상계, 연관 기억으로서의 KV 캐시와 항목 간 간섭의 계산, 어텐션 행렬의 저계수 구조와 근사 가능성의 하한. 본선 40~43번이 계산한 어텐션을 다른 언어로 다시 읽는 글이다. | Performer·Linear Transformer의 특징 사상 유도, Hopfield Networks is All You Need, 어텐션 근사의 하한을 다루는 이론 논문. 본선 40~43·69번. |
| `math-adv-in-context-learning-theory` | 문맥 내 학습의 이론: 어텐션이 경사하강을 구현한다는 구성 | 선형 회귀를 문맥에서 푸는 트랜스포머를 손으로 구성해 한 층의 어텐션이 경사하강 한 걸음과 같아지는 가중치를 명시하고, 층 수와 반복 횟수의 대응을 확인한다(구성적 증명이므로 완전 증명으로 싣는다). 베이즈 관점 — 문맥이 잠재 과제의 사후분포를 좁힌다 — 과 그 예측이 실측 곡선과 어긋나는 지점, 유도 헤드(induction head) 회로의 구조와 그것이 나타나는 학습 상전이, 문맥 길이에 따른 오차가 함수 클래스별로 어떻게 줄어드는지. 본선 41번이 유도한 어텐션 그래디언트가 여기서 '학습 알고리즘을 구현하는 회로'로 다시 읽힌다. | Garg 등의 문맥 내 학습 함수 클래스 연구, von Oswald의 경사하강 구성, Olsson의 유도 헤드 논문, 회로 기반 해석가능성 연구. 본선 41번. |
| `math-adv-expressivity-and-circuit-complexity` | 표현력의 한계: 한 번의 전방 통과로 풀 수 없는 문제의 증명 | 보편 근사 정리의 정확한 진술과 그것이 약속하지 않는 것(폭·깊이·학습 가능성), 필요한 폭이 지수적일 수 있다는 것, ReLU 망이 만드는 조각별 선형 영역의 개수가 깊이에 지수적으로 늘어난다는 계산과 깊이 분리 정리. 그다음 회로 복잡도 계급 AC⁰·TC⁰와 고정 깊이·고정 정밀도 트랜스포머가 균등 TC⁰에 갇힌다는 결과의 논증 구조, 그래서 상태 추적·괄호 맞추기 같은 문제가 한 번의 전방 통과로는 불가능하다는 것, 하드 어텐션과 소프트 어텐션의 차이. 사고 사슬이 중간 토큰을 작업 기억으로 써서 표현 가능한 계급을 넓힌다는 정리, 통신 복잡도로 얻는 어텐션 하한, 깊이·수치 정밀도·문맥 길이가 표현력에 들어오는 방식, RASP류 프로그램 표현. 본선 73번의 연산량과는 다른 축(계산 복잡도)의 이야기임을 첫 절에서 못 박는다. 기존 reasoning-test-time-compute의 실무 논의에 이론적 근거를 붙인다. | Merrill–Sabharwal의 트랜스포머 표현력 논문, CoT가 표현력을 늘린다는 정리 논문, RASP·Tracr 같은 프로그램 표현 연구. 'transformers cannot X' 계열 주장의 범위를 정확히 읽게 된다. 본선 73번. |

### A9 · 스펙트럴 이론의 응용: 시퀀스와 그래프 (4편) 【심화 절단점 4 — 여기까지 46편】

| 슬러그 | 제목 | 무엇을 쓰는가 | 무엇이 열리는가 |
|---|---|---|---|
| `math-adv-fourier-analysis-and-spectral-bias` | 조화해석과 스펙트럼 편향: 신경망이 저주파를 먼저 배우는 이유 | 푸리에 급수와 변환, 합성곱 정리, 파세발 항등식, 이산 푸리에 변환과 FFT의 O(n log n), 표본화 정리와 에일리어싱. 기초 트랙의 복소수·오일러 공식 위에서 진행한다. 그다음 A5-5의 NTK 고유값 분해가 주파수별 학습 속도를 정한다는 계산으로 스펙트럼 편향 정리를 유도하고(이 정리는 커리큘럼에서 여기 한 곳에서만 유도한다), 푸리에 특징 사상 γ(x)=[cos(2πBx), sin(2πBx)]이 그 편향을 어떻게 고쳐 NeRF를 가능하게 했는지 잇는다. 본선 43번의 사인파 위치 인코딩이 사실 이 특징 사상의 특수 경우임을 보이고, 학습된 망의 출력 스펙트럼을 실제로 측정하는 실험으로 닫는다. 증명 수위: 합성곱 정리와 파세발은 완전 증명, 스펙트럼 편향은 증명 개요. | 스펙트럼 편향·Fourier features·NeRF·FNO(Fourier Neural Operator)·긴 합성곱 모델 논문의 계산이 전부 이 정리 위에 있다. 본선 43번. A10의 표현론이 여기를 전제로 시작한다. |
| `math-adv-orthogonal-polynomials-and-hippo` | 직교다항식과 HiPPO: 상태공간 모델이 과거를 압축하는 방식 | 가중 내적 아래의 직교다항식(르장드르·라게르·체비셰프)과 그람–슈미트 구성, 최적 다항 근사와 계수의 의미, 그리고 함수의 이력을 직교 기저 계수로 요약하는 문제에서 HiPPO 행렬이 유도되는 전 과정과 그 계수가 만족하는 ODE. 측도(균등·지수 감쇠)를 바꾸면 어떤 행렬이 나오는지, 그것이 '무엇을 기억하고 무엇을 잊는가'를 어떻게 정하는지. 본선 7번의 사영을 함수 공간의 시간축으로 옮긴 결과다. 증명 수위: HiPPO 행렬 유도는 완전 증명. | HiPPO·S4 논문의 초기화가 왜 하필 그 특정 행렬인지에 유도로 답하게 된다. 긴 문맥 모델의 기억 감쇠 분석. 본선 7번. |
| `math-adv-toeplitz-circulant-and-state-space-models` | 토플리츠·순환 행렬과 상태공간 모델: 선형 시간 시퀀스의 선형대수 | 토플리츠·순환 행렬의 구조와 순환 행렬이 DFT로 대각화된다는 정리, 그래서 긴 합성곱이 FFT로 O(n log n)이 된다는 계산. 선형 시불변 시스템 x′=Ax+Bu의 해와 합성곱 커널, 이산화(쌍선형·ZOH)와 안정성 조건, S4의 대각+저계수 구조와 그것이 커널을 FFT로 계산하게 하는 이유, 그리고 입력 의존 파라미터(선택성)를 넣으면 왜 합성곱 형태를 잃고 병렬 스캔이 필요한지. 순환·합성곱·어텐션 세 형태의 상호 변환표와 각각의 기억 감쇠 특성, 본선 73번의 O(n²)와의 정면 대조. 기존 transformer-efficient는 효율 트랜스포머 다섯 갈래를 비교하며 Mamba·S4를 한 절로 소개하는 글이므로, 이 글은 커널 계산과 이산화의 선형대수만 맡는다. 증명 수위: 순환 행렬의 DFT 대각화는 완전 증명. | S4·Mamba 논문의 이론 절, 선형 어텐션과 SSM을 통합하는 최근 프레임워크, 긴 합성곱 모델의 커널 계산. 본선 13·42·73번. |
| `math-adv-spectral-graph-theory` | 그래프 라플라시안의 스펙트럼: 연결성·Cheeger·과평활화 | 인접·차수·라플라시안 행렬과 정규화 라플라시안, 고유값 0의 중복도가 연결 성분 수라는 정리, 피들러 값과 확장성, Cheeger 부등식으로 절단과 스펙트럼을 잇기, 스펙트럴 군집화가 이산 절단 문제의 완화라는 논증. 라플라시안 고유벡터를 그래프 위의 푸리에 기저로 보는 관점에서 스펙트럴 합성곱과 체비셰프 다항 국소화(ChebNet→GCN)를 유도하고, 메시지 전달을 반복하면 표현이 한 점으로 수렴하는 과평활화를 라플라시안 스펙트럼과 디리클레 에너지로 정량화한다. 1-WL 검정과 메시지 전달 GNN의 구별력이 같다는 정리를 한 절로 붙이고, 확산 지도와 랜덤 워크 관점, 그래프 위치 인코딩으로 닫는다. MATH-PLAN이 그래프를 뺀 근거가 '사이트에 GNN 글이 한 편도 없다'는 것이었고 실제로 GNN·그래프 신경망·라플라시안이 296편에 0건이므로, 이 글은 어떤 기존 글에도 기대지 않고 자체 완결적으로 쓴다. 본선 14번의 이차형식을 그래프 위에서 다시 쓰는 것이 출발점이다. | 스펙트럴 GNN(ChebNet·GCN)의 유도, 과평활화·과압착 분석 논문, 스펙트럴 군집화의 이론, 그래프 트랜스포머의 라플라시안 위치 인코딩, GraphRAG의 커뮤니티 탐지가 딛는 수학. 본선 12·14번. |

### A10 · 대칭과 등변성 (3편) 【심화 절단점 5 — 여기까지 49편이면 과학 AI 논문의 등변 층이 열린다】

| 슬러그 | 제목 | 무엇을 쓰는가 | 무엇이 열리는가 |
|---|---|---|---|
| `math-adv-groups-actions-and-representations` | 군·군작용·표현론: 대칭을 수학으로 적기 | 군과 부분군, 준동형, 군작용과 궤도·안정자군, 상군, 그리고 불변 f(g·x)=f(x)와 등변 f(g·x)=ρ(g)f(x)의 엄밀한 구분. 이미지의 평행이동군, 집합의 치환군 S_n, 3D 회전군 SO(3)과 SE(3)가 각각 어떤 군인지 대응시킨다. 후반부는 표현론: 군 표현과 동치, 기약 표현, 완전 가약성(마슈케 정리), 슈어 보조정리, 지표와 직교 관계, 유한군·콤팩트군에서의 푸리에 분해(A9-1의 푸리에가 순환군의 표현론이라는 관점). 리 군과 리 대수의 최소한과 지수 사상 — 본선 11번의 회전행렬 R(θ)가 SO(2)의 원소이고 exp(θJ)로 적힌다는 것. 본선 8·15번의 기저 분해가 대칭을 존중하는 방식으로 일반화된다. 증명 수위: 슈어 보조정리는 완전 증명. | geometric deep learning 프레임워크 논문이 첫 절에서 세우는 언어를 그대로 얻고, 다음 두 글의 구조 정리를 검증할 수 있게 된다. 본선 8·11·15번. |
| `math-adv-equivariant-networks` | 등변 신경망의 구조 정리: 등변 선형층은 왜 합성곱이 되는가 | 층 설계를 '등변 선형사상의 공간을 구하는 문제'로 환원하고, 평행이동 등변성이 본질적으로 합성곱을 강제한다는 정리를 증명한다. 치환 등변성이 DeepSets와 메시지 전달 형태를 강제한다는 결과, 군 합성곱(G-CNN)으로의 일반화와 회전군에 적용한 경우, 스티어러블 CNN의 일반 틀, 규격(gauge) 등변과 다양체 위의 합성곱, 그리고 등변 층을 설계할 때 무엇이 자유 파라미터로 남는지. 등변성을 구조로 강제하는 것과 데이터 증강으로 배우게 하는 것의 표본 효율 차이를 정량 비교하며 닫는다. 증명 수위: 평행이동 등변 ⇒ 합성곱은 완전 증명. | Cohen–Welling의 군 합성곱, 등변 아키텍처 논문의 'the most general equivariant linear map is…'라는 문장을 검증하게 된다. 기하 딥러닝(Bronstein 등)의 통합 관점. |
| `math-adv-spherical-harmonics-and-se3` | 구면 조화함수와 SE(3) 등변성: 3D 구조 모델의 수학 | 구면 위의 조화해석, 구면 조화함수가 SO(3)의 기약 표현이라는 것과 Wigner D-행렬, 스칼라·벡터·고차 텐서 특징이 각각 어떤 표현으로 변환되는지, 클렙시–고르단 계수로 두 표현의 텐서곱을 다시 기약 표현으로 분해하는 등변 층의 구성, 그리고 SE(3)에서 병진을 상대 좌표로 처리하는 방식. e3nn·SE(3)-Transformer 계열이 쓰는 연산을 하나씩 이 언어로 적고, 등변 층의 계산 비용이 차수 ℓ에 어떻게 의존하는지까지. 증명 수위: 구면 조화함수가 기약 표현이라는 것은 증명 개요, 클렙시–고르단 분해는 진술과 사용법. | e3nn·SE(3)-Transformer·AlphaFold의 구조 모듈·등변 분자 확산 모델 논문의 등변 층 정의를 읽게 된다. 과학 AI 논문에서 가장 자주 막히는 수학이다. |

### A11 · 표현의 기하 (2편) — 본선 11단원의 위층

| 슬러그 | 제목 | 무엇을 쓰는가 | 무엇이 열리는가 |
|---|---|---|---|
| `math-adv-compressed-sensing-and-superposition` | 압축 감지와 중첩: 희소 오토인코더가 딛고 선 정리 | 희소 신호를 미달결정 선형계에서 정확히 복원하는 조건, 제한 등거리 성질(RIP)과 무작위 행렬이 높은 확률로 그것을 만족한다는 결과(A3-1의 집중부등식), L0 대신 L1을 풀어도 같은 해가 나오는 이유의 기하(교차 다면체와 모서리 — A4-3의 soft-threshold가 그 알고리즘), 코히런스 기반 조건. 본선 66번의 존슨–린덴스트라우스와의 관계, 거의 직교하는 벡터를 d차원에 지수적으로 많이 넣을 수 있다는 계산(본선 65번의 고차원 기하가 배경), 그리고 그것이 '중첩 가설' — 특징 수가 차원보다 많아도 각 입력에서 소수만 활성이면 공존할 수 있다 — 에 주는 정확한 근거와 사전 학습의 식별 가능성 조건. 증명 수위: L1 복원의 기하는 완전 증명, RIP는 진술과 사용법. | Elhage 등의 toy models of superposition, 희소 오토인코더 기반 해석가능성 연구, 사전 학습의 복원 보장, Matryoshka 임베딩의 이론. 본선 65·66번. |
| `math-adv-representation-geometry-and-intrinsic-dimension` | 표현의 기하: 내재 차원, 유효 차원, 그리고 표현 비교의 함정 | 다양체 가설과 내재 차원 추정량(TwoNN·MLE·상관 차원)과 각각의 편향과 표본 요구량, 참여 비율과 스펙트럼 감쇠 지수로 표현의 유효 차원을 재기(본선 68번의 이방성 측정을 일반화). 후반부는 두 표현을 비교하는 지표들 — CCA·SVCCA·CKA·프로크루스테스 — 의 정의와 각각이 회전·스케일·아핀 중 무엇에 불변인지, 같은 두 모델에 대해 지표마다 다른 결론이 나오는 사례, 지표를 고르는 것이 곧 '무엇을 같다고 볼 것인가'를 고르는 것이라는 결론, 그리고 '서로 다른 모델의 표현이 수렴한다'는 주장을 검증할 때 필요한 통계적 주의. A13의 스케일링 법칙 이론이 여기서 잰 내재 차원을 지수로 쓴다. | 내재 차원과 스케일링 지수를 잇는 논문, CKA와 그 비판, Platonic Representation Hypothesis, 표현 유사도로 모델을 비교하는 해석가능성 연구. 본선 65·68번. |

### A12 · 유한한 기계의 이론 (2편) — 본선 12단원의 위층

| 슬러그 | 제목 | 무엇을 쓰는가 | 무엇이 열리는가 |
|---|---|---|---|
| `math-adv-backward-error-analysis` | 후방 오차 분석: '정확한 답'이 아니라 '가까운 문제의 정확한 답' | 전방 오차와 후방 오차의 구분, 조건수 × 후방 오차 = 전방 오차라는 골격, 부동소수점 연산의 표준 모델 fl(a op b)=(a op b)(1+δ)로 합산·내적·행렬곱의 오차 상계를 유도하기(√n·n 의존성이 어디서 나오는가). 후방 안정 알고리즘의 정의와 QR·LU·촐레스키의 안정성 비교, 혼합정밀도 누산의 오차 모델과 fp16 입력·fp32 누산이 실제로 무엇을 보장하는지, 확률적 반올림이 오차 상계를 n에서 √n으로 낮추는 논증, 텐서 코어의 내부 정밀도가 결과에 미치는 영향. 본선 70번이 관찰한 상쇄와 71번의 안정한 패턴들에 정량적 근거를 붙인다. 증명 수위: 합산·내적의 오차 상계는 완전 증명. | Higham의 정확도·안정성 분석과 혼합정밀도 논문, 확률적 반올림 연구, fp8·bf16 저정밀 학습 논문의 오차 분석 절. 본선 70·71번. |
| `math-adv-io-complexity-and-lower-bounds` | I/O 복잡도와 하한: FlashAttention이 최적이라는 논증 | 행렬곱의 지수 ω와 Strassen류 알고리즘이 실무에서 왜 잘 안 쓰이는지(수치 안정성과 상수), Hong–Kung의 red-blue pebble 게임과 캐시 크기 M에서 행렬곱의 I/O 하한 Ω(n³/√M)의 논증 구조, 통신 회피 알고리즘과 타일링이 그 하한에 맞닿는다는 것. 같은 틀로 어텐션의 I/O 하한을 세우고 그 하한이 달성 가능하다는 것을 보인다. roofline 모델과 산술 강도, 그리고 디코딩이 왜 연산이 아니라 메모리 대역폭에 묶이는지를 산술 강도로 계산하기. 기존 transformer-flash-attention이 'IO가 병목'이라는 관찰과 타일링·online softmax 구현을 이미 다루고 본선 42번이 online softmax의 대수를, 73번이 FLOPs 세기를 맡으므로, 이 글은 오직 하한의 증명 구조와 산술 강도만 맡는다. 증명 수위: red-blue pebble 하한은 증명 개요 + 핵심 보조정리. | FlashAttention 1·2·3 논문의 복잡도 절, 통신 회피 선형대수 논문, roofline 기반 성능 분석, 추론 최적화 논문의 대역폭 논증. 본선 42·73번. |

### A13 · 측정의 보장 (6편) 【심화 절단점 6 — 여기까지 59편이면 평가와 보장의 이론이 닫힌다】

| 슬러그 | 제목 | 무엇을 쓰는가 | 무엇이 열리는가 |
|---|---|---|---|
| `math-adv-sequential-testing-and-e-values` | 순차 검정과 e-값: 계속 보면서도 틀리지 않기 | 고정 표본 검정을 중간에 들여다보면 1종 오류가 부풀어 오르는 계산을 시뮬레이션으로 먼저 보이고, 검정 마팅게일과 빌의 부등식(A1-5의 옵셔널 스토핑), e-값과 e-과정의 정의와 곱으로 증거를 누적하는 성질, 항시 유효(anytime-valid) 신뢰수열의 구성. 옵셔널 스토핑 아래 유효한 순차 우도비 검정(SPRT)과 그 기대 표본 수, 혼합 SPRT, e-값 기반 FDR 통제. 그리고 벤치마크를 배치로 늘려 가며 '언제 멈춰도 되는가'를 판정하는 절차를 본선 74~76번의 고정 표본 결과와 나란히 계산해 표본 비용 차이를 낸다. 증명 수위: 빌의 부등식과 e-값의 유효성은 완전 증명. | e-값·신뢰수열의 최근 논문(Ramdas 등), 순차 A/B 테스트 방법론, 평가를 점진적으로 돌리는 LLM 벤치마킹, 모니터링 중 유의성을 판정해야 하는 모든 실무 상황. 본선 74·76번. |
| `math-adv-conformal-prediction` | 적합 예측: 분포 가정 없이 보장되는 예측 집합 | 교환가능성 가정만으로 유한 표본 커버리지 P(y ∈ C(x)) ≥ 1−α를 주는 split conformal의 구성과 그 짧은 완전 증명, 비적합 점수의 선택이 집합 크기에 하는 일, 분류에서 예측 집합·회귀에서 구간을 만드는 법, 적응적 집합(APS·RAPS). 주변 커버리지와 조건부 커버리지의 차이와 조건부 보장이 분포 무관하게는 불가능하다는 정리, 그룹별 보정과 Mondrian conformal, 분포 이동 아래의 가중 conformal, conformal risk control, 그리고 LLM 출력에 붙일 때 점수 함수를 로그확률·자기평가·검색 근거 중 무엇으로 잡을지. 본선 79번의 보정이 '확률값의 정확성'이라면 이쪽은 '집합의 보장'이라는 대비를 첫 절에서 세운다. | Angelopoulos–Bates의 conformal prediction, conformal risk control, LLM 거절·라우팅·선택적 예측에 커버리지 보장을 붙이는 최근 논문. 실무에서 곧바로 쓸 수 있는 보장 도구다. 본선 74·79번. |
| `math-adv-causal-inference-and-interventions` | 인과추론: 상관을 넘어서 개입을 재기 | 잠재 결과 틀과 인과 효과의 정의, 교환가능성·일치성·양의성 가정, 무작위 배정이 정확히 무엇을 보장하는지, 성향 점수와 역확률 가중·이중 강건 추정, 도구변수. 구조적 인과 모형과 인과 그래프, d-분리, 개입 분포 p(y\|do(x))와 관측 분포의 차이, 백도어·프런트도어 기준, do-계산의 세 규칙과 식별 가능성 판정, 충돌자를 통제하면 오히려 생기는 편향, 매개 분석. 응용은 전부 AI 평가다 — 로그 데이터로 새 정책을 평가하는 오프라인 정책 평가, 사용자가 본 것만 관측되는 선택 편향, 그리고 활성값 패칭과 절제 실험이 정확히 do-개입이라는 관점에서 해석가능성 실험을 다시 읽기. 벤치마크 상관에서 인과를 주장할 때 필요한 조건을 표로 정리하며 닫는다. | Pearl의 do-계산, 해석가능성의 인과적 매개 분석(causal tracing·activation patching), 오프라인 정책 평가(OPE), 추천·검색의 편향 보정. 본선 76·79번. |
| `math-adv-heavy-tails-and-extreme-values` | 무거운 꼬리와 극단값: 평균이 의미를 잃는 곳 | 무거운 꼬리의 정의(정칙 변동)와 파레토·멱법칙, 유한 분산이 없으면 중심극한정리 대신 안정 분포와 일반화 CLT가 나온다는 것, 그래서 표본평균과 표준오차가 무의미해지는 자리 — 본선 21번의 분산 가법성과 74번의 √(p(1−p)/n)가 전제하는 것이 정확히 어디서 깨지는지. 멱법칙 적합의 함정과 Clauset–Shalizi–Newman 절차(x_min 선택, KS 거리, 로그-로그 회귀가 왜 편향되는가), Hill 추정량. 극단값 이론의 세 유형(Gumbel·Fréchet·Weibull)과 블록 최댓값·문턱 초과(POT) 모형, 그리고 최악 사례 지연시간·안전 사건 빈도·SGD 그래디언트 잡음의 꼬리를 실제로 추정하기. 본선 80번의 멱법칙 적합 통계와는 다른 축 — 저쪽은 곡선 적합이고 이쪽은 꼬리 자체의 추론이다. | Clauset 등의 멱법칙 적합 논문, SGD 잡음이 무거운 꼬리라는 연구, heavy-tailed self-regularization 스펙트럼 진단(A2-2), 안전·신뢰성 평가의 꼬리 위험. 본선 21·74·80번. |
| `math-adv-scaling-law-theory` | 스케일링 법칙의 이론: 지수가 어디서 나오는가 | 데이터 다양체의 내재 차원 d에서 손실 지수 α ≈ 4/d가 나오는 근사 이론 논증(A11-2에서 잰 내재 차원이 여기 들어간다), 무작위 특징·커널 회귀 모형에서 스펙트럼 감쇠 지수가 손실 곡선의 지수를 정한다는 정확한 계산(A5-2와 A3-3의 결과 재사용), 세 축을 결합한 L = A/N^α + B/D^β + E의 형태와 최적 배분(치칠라 조건)이 라그랑주 한 줄로 나오는 과정. 관측 지수를 적합할 때의 통계적 함정(가중, 오차 구조, 환원 불가 항 E의 식별 불안정)과 두 자릿수 밖 외삽이 깨지는 이유. 본선 80번은 곡선을 적합하고 해석하는 통계를, 기존 llm-scaling-laws는 Kaplan·Chinchilla의 경험 법칙을, llm-emergent-abilities는 창발이 측정 방식의 산물인지의 논쟁을 이미 맡고 있으므로, 이 글은 오직 '왜 그 함수 형태이고 지수가 무엇에서 나오는가'만 맡는다. | Sharma–Kaplan의 차원 논증, Bahri 등의 스케일링 법칙 이론, Kaplan·Chinchilla 적합 절차의 재분석. 본선 50·80번. |
| `math-adv-differential-privacy-accounting` | 차등 프라이버시의 수학: (ε,δ)·레니 발산·모멘트 회계 | (ε,δ)-DP의 정의와 프라이버시 손실 확률변수, 민감도 계산과 라플라스·가우시안 메커니즘, 기본 조합 정리와 그것이 왜 느슨한지, 고급 합성, 레니 DP와 모멘트 회계로 얻는 훨씬 좁은 경계, 부분표집 증폭. 그리고 DP-SGD를 분해해 그래디언트 클리핑 상한 C와 잡음 배율 σ가 각각 ε에 어떻게 들어가는지, 학습 스텝 수가 프라이버시 예산을 어떻게 소모하는지 실제로 계산한다. 기존 ai-privacy는 차분 프라이버시를 개념과 규제·연합학습·동형암호와 나란히 한 절로 소개하는 글이므로, 이 글은 회계의 수학만 맡는다. 증명 수위: 가우시안 메커니즘과 레니 DP 변환은 완전 증명. | DP-SGD 논문의 ε 계산을 스스로 재현하고 프라이버시 회계 도구(Opacus·TF-Privacy)의 파라미터가 무엇을 뜻하는지 판정하게 된다. 기억화·멤버십 추론 감사 논문의 위협 모형. |

### A14 · 정렬과 의사결정의 이론 (4편) 【심화 절단점 7 — 63편으로 완결】

| 슬러그 | 제목 | 무엇을 쓰는가 | 무엇이 열리는가 |
|---|---|---|---|
| `math-adv-mdp-bellman-and-trust-region` | MDP와 벨만 작용소: 축약 사상 하나로 증명되는 것들 | 마르코프 결정 과정의 형식적 정의, 벨만 기대·최적 작용소가 sup 노름에서 γ-축약이고 A1-1의 바나흐 고정점 정리로 유일한 해를 준다는 완전 증명, 값 반복·정책 반복의 수렴과 정책 개선 정리, 함수 근사가 들어올 때 축약이 깨져 발산하는 치명적 삼요소. 그리고 성능차 보조정리(두 정책의 성능 차이를 어드밴티지의 기댓값으로 적는 항등식)의 유도와 거기서 나오는 TRPO의 단조 개선 하한, 신뢰영역 반지름과 KL 제약의 대응(A4-3의 거울 하강과 A6-3의 자연그래디언트가 같은 자리에서 만난다), GAE의 λ가 편향과 분산을 맞바꾸는 계산, 토큰 단위 MDP로 본 RLHF에서 무엇이 상태이고 무엇이 종료 보상인지. 기존 rl-basics·rl-q-learning·rl-ppo는 개념과 구현이므로 이 글은 정리와 증명만 맡고, 본선 52~58번이 목적식과 추정량의 성질을 맡으므로 여기서는 그 위의 보장만 맡는다. | TRPO·PPO 원 논문의 정리 절, GAE 논문, 오프라인 RL의 분포 시프트 경계, RLHF를 MDP로 정식화하고 토큰 수준 신용 할당을 다루는 정렬 논문. 본선 52·54·58번. |
| `math-adv-bandits-and-regret` | 밴딧과 후회 경계: 탐색의 값을 정확히 계산하기 | 확률적 다중 손잡이 밴딧과 후회의 정의, UCB의 신뢰폭이 A3-1의 집중부등식에서 유도되는 과정과 O(log T) 후회 상계의 완전 증명, Lai–Robbins 하한(그 이상 잘할 수 없다는 것), 톰슨 표집의 베이즈적 유도와 경험적 우위, 선형 문맥 밴딧(LinUCB), 적대적 설정의 EXP3와 그것이 A4-3의 지수가중 갱신이라는 것. 순수 탐색·최선 팔 식별 설정과 그것이 모델 선택·프롬프트 선택 문제와 같다는 관점. | 탐색 전략·능동 학습·모델 라우팅·RLHF 데이터 수집의 적응적 할당 논문, 후회 경계 언어로 쓰인 온라인 학습 문헌. A13-1의 순차 검정과 짝을 이룬다. |
| `math-adv-minimax-games-and-equilibria` | 최소최대 게임과 균형: 자기대전이 수렴한다는 말 | 정규형 게임과 내시 균형, 안장점 문제와 미니맥스 정리, 2인 영합 게임의 미니맥스가 선형계획 쌍대성과 같다는 대응(A4-2), 동시·교대 경사법이 회전하며 발산할 수 있다는 2×2 계산(고윳값이 순허수), 외삽(extragradient)과 낙관적 갱신이 마지막 반복 수렴을 주는 이유, 변분 부등식과 단조 작용소 관점. 후회 최소화와 평균 전략이 균형에 수렴한다는 정리, 곱셈 가중치 갱신이 거울 하강이라는 것, 확장형 게임과 CFR의 최소한, 그리고 비볼록–비오목 설정에서 국소 안장점·Stackelberg 균형의 구분과 무엇을 목표로 삼아야 하는가. 생성자–판별자와 자기대전 정렬을 같은 언어로 다시 적는다. 증명 수위: 미니맥스 정리와 회전 발산 계산은 완전 증명. | GAN 수렴·발산 분석, Nash-RLHF와 SPPO의 수렴 정리, 적대적 강건 최적화, 다중 에이전트 학습의 균형 논의. 본선 57·58번. |
| `math-adv-social-choice-and-preference-aggregation` | 선호를 하나의 보상으로 접을 때 잃는 것 | 사회 선택 이론의 틀, 애로 불가능성 정리와 깁바드–새터스웨이트 정리의 서술과 증명 개요, 콩도르세 역설과 다수 선호가 비순환이 아닐 수 있다는 사실, Borda·Kemeny 집계와 그 계산 복잡도. Plackett–Luce로 순위 데이터를 모델링하기, 평가자 간 이질성을 혼합 모형으로 다루기, 그리고 본선 55번의 Bradley–Terry와 78번의 Elo가 전제하는 이행성이 실제 선호 데이터에서 얼마나 깨지는지를 측정하는 절차. 폰 노이만 승자·최소최대 승자 같은 대안 해 개념과 그것이 A14-3의 미니맥스 게임으로 이어지는 자리. 사이트에 정렬 글이 14편 있는데 '여러 사람의 선호를 스칼라 보상 하나로 접는 것이 무엇을 잃는가'를 다룬 글이 한 편도 없는 자리를 메운다. | Nash Learning from Human Feedback, 자기대전 선호 최적화(SPO·SPPO), 다원적 정렬과 보상 모델의 한계를 다루는 논문. 본선 55·78번. |

---

## 읽기와 쓰기 순서

## 읽기 순서 — 길은 하나다

세로 축은 본선 80편뿐이다. 기초 23편은 그 길 옆에 붙는 사전이고, 심화 63편은 길이 끝난 자리에서 시작하는 다음 산이다.

- **본선은 1번부터 아무 선행 없이 시작한다.** 기초 23편 중 어느 것도 본선 1번의 선행 조건이 아니다. 기초에는 번호를 매기지 않는다 — 번호가 붙는 순간 '순서대로 읽어야 하는 것'이 되고, 그것이 144편 계획을 죽인 인식이다.
- 기초는 **본선 N번에서 막혔을 때 한 편만 꺼내 보는 것**이다. 전 편에 `unlocks`(본선 N번 앞)가 붙고, 그 대응은 `src/data/curriculum.ts` 한 곳에만 둔다.
- 심화는 **본선 80번을 마친 뒤**다. 심화 안에서만 A1 → A14 순서를 지킨다. A1(5편)이 유일한 도구 단원이고, 나머지 도구는 그것을 쓰는 단원 안에 배치했다.
- 완주율과 진행 막대의 **분모는 본선 80편만**이다. 기초에는 진행 표시를 두지 않고, 심화는 단원별 완료 뱃지만 준다.
- 절단점: 본선은 8·17·31·39·43·51·58편. 심화는 10편(보장 도구) · 27편(목적식의 유도) · 36편(확산·흐름 매칭의 완결) · 46편(시퀀스와 그래프) · 49편(등변성) · 59편(측정의 보장) · 63편(완결).

## 쓰기 순서 — 세 구간, 33일

**루틴은 기초 → 본선 → 심화 순서로 쓴다. 섞지 않는다.** 2026-08-05에 뒤집었다. 그날 본선 1~5번이 나갔는데 다섯 편 모두 `level: 중급`이라 "기초부터라면서 난이도가 높다"는 지적이 나왔고, 그 지적이 맞다. 나간 5편은 되돌리지 않는다 — 접두사 규칙상 중급이 맞고, 앞으로의 순서만 바꾸면 된다.

**뒤집으면 걸리는 것은 링크 하나뿐이고, 해법은 링크를 걸지 않는 것이다.** `src/routes.test.ts`의 내부 링크 검사는 *존재하는 `.md`에서 나가는* 링크만 훑으므로, 순서를 뒤집는 것 자체로는 깨지지 않는다. 깨지는 경우는 하나 — 먼저 나간 기초 원고 안에 아직 없는 본선 슬러그로 나가는 인라인 링크가 있을 때다. 그래서 규칙을 이렇게 못 박는다.

> **기초·심화 글은 본문에서 아직 쓰지 않은 글로 나가는 인라인 링크(`](/articles/math-...)`)를 쓰지 않는다.** 본선을 가리켜야 할 때는 「본선 12번 · 고윳값과 고유벡터」처럼 번호와 제목만 텍스트로 적는다. 눌러서 이동하는 링크는 `src/data/curriculum.ts`의 대응 데이터가 그리는 배지가 맡고, 그 배지는 실제 `.md`가 있는 슬러그만 렌더한다. 링크를 걸어도 되는 것은 **쓰는 시점에 이미 있는 글**과 **같은 날 배치에 함께 쓰는 글** 둘뿐이다(2026-08-05 배치가 이미 그렇게 나갔다 — 1번이 2번을, 4번이 5번을 링크한다). 기존 296편은 전부 실재하므로 자유롭게 건다. 슬래시(`/articles/x/`)나 앵커(`/articles/x#y`)를 붙여 검사를 피하지 않는다 — 검사는 통과하지만 죽은 링크가 남는다. 그 우회를 막으려고 검사 정규식을 두 형태까지 잡도록 넓혀 두었다.

이 규칙이면 링크 검사는 쓰는 순서와 무관해진다. 오히려 배지가 나아진다 — 배지의 방향은 본선 → 기초이므로, 기초를 먼저 쓰면 본선 1번이 나가는 날 대응 기초가 이미 전부 있어 첫날부터 채워진 채로 보인다.

**배지는 2026-08-05에 넣었다.** 1구간이 시작되기 전에 끝내야 했던 셋이 전부 코드에 있다.

- (a) 대응 데이터 — `src/data/curriculum.ts`의 `mathSupport`. 키는 기초·심화 슬러그, 값은 **본선 번호 배열**이고 첫 번호가 대표다. 아래 두 표의 「본선 대응」 칸을 그대로 옮긴 것이라 이 문서와 나란히 놓고 보면 된다. 슬러그가 아니라 번호인 이유는 원고가 본선을 부르는 단위가 번호이기 때문이다 — 원고에 적힌 「본선 12번」과 데이터의 `12`가 같은 것을 가리켜야 한다. 심화 여섯 편(마르코프 연쇄·랑주뱅·등변 신경망·구면조화함수·DP 회계·밴딧)은 대응 칸이 본선을 하나도 부르지 않아 뺐다.
- (b) 양방향 블록 — `src/pages/ArticlePage.tsx`의 `CurriculumLinks`가 본문 옆 사이드바에 그린다. 본선 글에는 「막히면 먼저」(기초)와 「더 깊이」(심화), 기초 글에는 「이 글이 받치는 본선」, 심화 글에는 「이 글이 확장하는 본선」이다. 본선 쪽 항목에는 「본선 12번」이라는 번호를 함께 찍는다 — 원고 본문에 적힌 텍스트와 같은 이름으로 보여야 눌러서 가는 곳이 그것임을 안다.
- (c) 실재 필터 — 같은 컴포넌트가 `getArticleBySlug`로 거른다. 아직 `.md`가 없는 슬러그는 조용히 빠지고 그 글이 나가는 날 저절로 채워진다. 한 무리가 통째로 비면 제목줄까지 렌더하지 않는다.

`src/data/curriculum.test.ts`가 키·값의 소속과 번호 범위, 그리고 양방향이 맞물리는지를 검사한다.

예외는 `math-basics-self-diagnosis` 한 편이다. 30문항의 처방이 본선 1·4·40·44·52·59·74번으로 실제 이동해야 쓸모가 있는 글이라 기초·본선 양쪽 전부에 의존한다. **기초 구간에서 빼서 본선 구간 마지막 날에 6편째로 붙인다.** 배열에서의 자리(`mathFoundation[0]`, 화면 1000번)는 그대로 두고 `mathWritingOrder`에서만 뒤로 뽑는다.

- **1구간 · 1~5일, 2026-08-06 ~ 08-10 (기초 21편, 5편/일)** — 22편에서 self-diagnosis를 뺀 21편. 되살린 학교 수학(좌표평면·직선 → 이차식 → 곡면·등고선) → 기초 1(기호와 문장) → 기초 2(수와 스케일) → 기초 4(미적분) → 기초 3의 나머지(연립방정식·행렬식·삼각비·복소수) → 기초 5(세는 법·통계) → 기초 6(검산). 기초는 순서가 없지만 서로 참조는 하므로 배치는 선행 관계를 따른다 — 되살린 셋이 나머지의 전제이고(식 조작이 도함수 규칙 앞), 자릿수·오차 감각이 테일러 절단오차 앞이며, 검산 글은 앞 편들의 유도를 실습 재료로 쓰므로 맨 뒤다. 마지막 날은 1편이다.
- **2구간 · 6~20일, 08-11 ~ 08-25 (본선 75편 + self-diagnosis, 5편/일)** — 2026-08-05에 나간 1~5번 다음인 `math-dot-product-and-cosine`부터 `mathCurriculum` 순서 그대로. 한 글자도 바꾸지 않는다. 마지막 날(08-25)만 6편으로, 6편째가 `math-basics-self-diagnosis`다.
- **3구간 · 21~33일, 08-26 ~ 09-07 (심화 63편, 5편/일)** — A1 → A14 순서 고정. 심화는 앞 단원을 이름으로 참조하므로(A1-2의 리스 표현 → A5-1의 재생 성질, A1-1의 축약사상 → A7-8의 Sinkhorn과 A14-1의 벨만, A4-2의 켤레 → A6-1·A7-7) 단원을 건너뛰고 쓰면 참조가 빈다. 심화는 여전히 맨 뒤다 — 본선을 이름으로 참조하고 읽는 순서도 본선을 마친 뒤라 앞으로 당길 이유가 없다.

합계 5 + 15 + 13 = 33일. 되살린 3편이 늘었지만 본선 5편이 이미 나갔고 self-diagnosis가 6편째로 얹혀 상쇄된다.

## 루틴이 지킬 규칙

1. **슬러그 접두사가 트랙을 정한다.** `math-basics-` = 기초(`level: "입문"`, `tags`에 `기초 트랙`), `math-` = 본선(`중급`, `본선`), `math-adv-` = 심화(`심화`, `심화 트랙`). 셋 다 `category: "math-for-ai"`. 이 규칙을 CLAUDE.md와 MATH-PLAN.md에 한 줄씩 박는다.
2. **brief의 카브 문장을 어긴 원고는 다시 쓴다.** "…는 본선 N번의 것이다", "기존 ○○이 맡는다"는 금지 목록이지 참고 사항이 아니다.
3. **심화는 brief의 증명 수위를 지킨다.** '완전 증명'으로 표시된 정리는 반드시 끝까지 쓴다 — 그것이 "논문 이론 절을 스스로 읽게 된다"를 검증 가능하게 만드는 유일한 장치다.
4. **기초 글은 상단에 "이 글이 필요한지 30초 확인" 접이식 문제 2개**로 시작하고, 풀리면 본선으로 돌려보낸다. 기초가 시간을 잡아먹지 않게 하는 안전장치다.
5. **아직 커밋되지 않은 글은 본문에서 링크하지 않는다.** `](/articles/<slug>)`가 가리켜도 되는 것은 (a) 이미 나간 `.md`, (b) 같은 날 배치에 함께 쓰는 글 둘뿐이다. 아직 없는 글은 「본선 12번 · 고윳값과 고유벡터」처럼 번호와 제목만 텍스트로 적는다. 슬래시나 앵커를 붙여 `src/routes.test.ts`의 검사를 피하지 않는다 — 그 우회는 정규식을 넓혀 막아 두었다. 기초를 먼저 쓰기로 한 이상 이 규칙이 순서를 지탱하는 유일한 장치다.
6. **기초·심화 ↔ 본선 대응은 본문 링크가 아니라 `src/data/curriculum.ts`의 데이터가 그린다.** 원고 안에서는 번호와 제목만 적고, 실제로 눌러 이동하는 배지는 데이터에서 렌더한다. 대응이 바뀌면 데이터를 고치지 원고를 고치지 않는다.
7. `src/data/curriculum.test.ts`가 세 배열의 슬러그 중복 없음, 접두사 일관성, 그리고 **쓰는 순서의 불변식 셋**(자가진단을 뺀 기초가 맨 앞 블록 · 자가진단이 모든 본선보다 뒤 · 심화가 맨 뒤 블록)을 검사한다. 배열 deep-equal로 두지 않는 이유는 그것이 결정을 못 박는 대신 순서를 바꿀 때마다 갱신해야 하는 사본이 되기 때문이다. `mathSupport` 대응도 여기서 검사한다 — 키가 기초·심화에 있음, 값이 본선 번호 1~80 안임, 기초 23편이 전부 대응을 가짐, 그리고 뒤집은 방향이 맞물림.
8. `curriculumOrder()`는 기초 `1000+i`, 본선 `i`, 심화 `3000+i`로 오프셋을 주되 **본선은 0부터 유지**한다. **화면 순서와 쓰는 순서는 일부러 다르다** — 화면은 본선 → 기초 → 심화, 쓰기는 기초 → 본선 → 심화다. 오프셋을 쓰는 순서에 맞추려 들지 마라. `src/data/articles.ts`의 분기는 `order: 0`을 falsy로 버리지 않도록 `order !== undefined`여야 한다(이미 그렇게 돼 있다).
