# 수학 커리큘럼 계획

`src/data/curriculum.ts`의 순서를 왜 그렇게 잡았는지와, 각 글이 무엇을 다뤄야 하는지를
적어 둔다. 자동 작성 루틴이 글을 쓸 때 이 파일의 `brief`를 근거로 삼는다.

트랙이 셋이고 **슬러그 접두사가 트랙을 정한다.**

| 접두사 | 트랙 | level | 성격 |
| --- | --- | --- | --- |
| `math-basics-` | 초급 48편 | 초급 | 계산과 표기부터 다시 세우는 과정. **1번부터 순서대로** |
| `math-` | 중급 80편 | 중급 | 세로 축. 1번부터 아무 선행 없이 시작한다 |
| `math-adv-` | 고급 63편 | 고급 | 중급을 마친 뒤. 논문의 이론 절을 스스로 읽는 것이 목표 |

셋 다 `category: "math-for-ai"`다. **쓰는 순서는 초급 → 중급 → 고급**이다.
2026-08-05에 뒤집었다 — 중급부터 쓰니 첫 배치 5편이 전부 `level: 중급`이라
"기초부터라면서 난이도가 높다"는 말이 나왔고, 그 말이 맞다. 뒤집으면 걸리는 것은
링크 하나뿐이며 **아직 쓰지 않은 글로 나가는 본문 링크를 걸지 않는 것**으로 푼다.
자세한 것은 맨 아래 「쓰기 순서」 절에 있다.

**화면에 보이는 순서는 쓰는 순서의 역순이다** — 최신 글이 맨 위다. 그래서 고급
마지막 편이 목록 맨 위에 서고 초급 1번이 맨 아래에 선다. 같은 날 다섯 편이 나가
`pubDate`가 같으므로 날짜만으로는 그 안의 순서가 잡히지 않는다. `curriculumOrder()`가
`mathWritingOrder` 안에서의 자리를 역순으로 돌려주어 그 자리를 대신한다. 예전에는
화면이 중급 → 초급 → 고급이었는데, 그러면 그날 나간 글이 목록 어디에 꽂혔는지
읽는 사람이 알 수 없었다.

## 설계 근거

이전 계획은 144편 중 10편에서 멈췄다. 실패 원인은 분량이 아니라 보상이 늦게 온다는 것이었다. 수의 체계부터 쌓으면 AI가 나오기까지 수십 편이 걸린다. 그래서 두 가지를 동시에 걸었다. 선별은 "이 글이 어떤 AI 동작을 설명하는가"를 한 줄로 못 쓰면 넣지 않는다는 필터 하나로 했고, 순서는 AI 시스템의 흐름을 따르되 앞 글이 뒤 글의 도구를 먼저 정의하도록 강제했다. 그 결과 8편이면 임베딩 검색의 기하가, 31편이면 사전학습 손실의 유도가, 43편이면 어텐션 한 줄이 닫힌다. 어느 절단점에서 멈춰도 그때까지가 완결된 묶음으로 남는다. 편당 개념을 서너 개 묶어 8,000~15,000자를 채우게 했고, 기존 296편과 겹치는 자리에는 "그 글은 무엇을 맡고 이 글은 무엇을 맡는가"를 brief에 못 박아 루틴이 재탕을 쓰지 않게 했다.

## 이전 계획에서 뺀 것

**이 문단은 중급 80편이 무엇을 다루지 않는가에 대한 것이다.** 여기서 '뺐다'는
커리큘럼 전체에 없다는 뜻이 아니라 중급의 몫이 아니라는 뜻이고, 뺀 것의 대부분은
초급 48편에 소유자가 있다. 루틴이 이 문단을 '무엇을 쓰지 않는가'의 근거로 읽으므로
절단마다 초급의 소유자를 함께 적어 둔다 — 적어 두지 않으면 초급 원고를 쓸 때
상반된 지시를 함께 읽게 된다.

이전 144편에서 64편이 빠졌다. (1) 수 체계·정수·유리수·실수·다항식·인수분해·이차방정식 등 대수 기본기 20편 전부 — **초급 4~18번이 갖는다**(자릿수와 세로셈, 약수와 소인수분해, 분수, 소수와 비, 거듭제곱과 근호, 문자와 대입, 전개와 인수분해, 방정식, 부등식, 식 조작). 중급은 그 계산을 할 줄 안다고 전제하고 시작하며, 전제를 채우는 것이 초급의 일이다. 지수·로그는 softmax와 엔트로피에 필요한 만큼 18번 한 편으로 되짚는다 — **정의와 법칙은 초급 13·21·22번이 갖고** 중급 18번에 남는 것은 로그 공간 계산과 언더플로다. 수 체계 기호 ℝ·ℤ는 초급 9번의 기호 사전이, 실수와 부동소수점의 간극은 중급 70번이 맡는다. (2) 함수 단원 10편과 삼각함수 7편 — 회전행렬(11번)과 사인파 위치 인코딩(43번)에 필요한 것만 남기고 항등식·사인법칙·역삼각함수·푸리에는 뺐다. **단위원·라디안·호의 길이·파형 파라미터는 초급 25번이 갖고**, 중급 11번에 남는 것은 회전행렬 R(θ)와 그 직교성이다. (3) 이산수학·논리 12편(집합론·명제·술어논리·증명법·비둘기집·정수론·모듈러) — 증명 훈련과 CS 이론이지 모델 수식을 읽는 데 안 쓴다. **증명 뼈대 여섯과 문서 구조·관용구는 초급 46번이 갖는다** — 중급이 가르치지 않는다는 뜻이지 커리큘럼에 없다는 뜻이 아니다. 그래프는 GNN 글이 사이트에 한 편도 없어 근거가 없다. (4) 극한의 엡실론-델타, 적분 기법, 이상적분, 급수 수렴 판정 — 적분은 기댓값 정의와 ODE 이산화 자리에서만 등장한다. **치환·부분적분은 예제까지 초급 34번이, 등비급수는 29번이, 극한은 28번이 갖는다**(엡실론-델타는 초급도 쓰지 않는다 — 수치 표로 먼저 보고 대수로 확정하는 것이 그 트랙의 규약이다). (5) 가우스 소거·LU·행렬식·역행렬·유사역행렬·선형계획법 — 딥러닝에 Ax=b를 직접 푸는 자리가 없고, 정규화 흐름 글이 없어 log det의 수요도 없다. **소거는 초급 43번이, 행렬식과 역행렬은 44번이 전부 갖는다** — 그래서 중급 12번은 행렬식을 자기 자리에서 다시 정의하지 않고 특성방정식과 대각화만 맡는다. (6) t검정·분산분석·카이제곱·p값 등 고전 추론통계 6편 — 76번 쌍체 검정·순열 검정 한 편으로 대체했다. 표를 찾는 대신 재표본으로 계산한다. **귀무가설·유의수준·p값이라는 판정의 언어 자체는 초급 41번이 세운다.** (7) 보간·수치미분 일반론·MCMC·EM·켤레사전분포. 대신 원래 계획에 없던 것을 넣었다. 정렬 수학 7편(Bradley-Terry에서 DPO 손실까지의 유도), 생성 수학 6편(재매개변수화에서 흐름 매칭까지), 평가 통계 7편(pass@k·Elo·보정·멱법칙), 어텐션 고유 수학 4편(√d_k·softmax 야코비안·LSE·RoPE), 고차원 기하 5편, 표기 규약 3편이다. 사이트에 rl-ppo·llm-dpo·llm-rlhf 등 정렬 글 14편과 cv-* 확산 글 9편이 있는데 이전 계획에는 그 밑을 받치는 수학이 한 편도 없었다. 심사가 지적한 네 가지 순서 사고도 전부 고쳤다. 젠센 부등식(29번)을 KL·ELBO 앞에 두었고, 분산(21번)을 √d_k(40번) 앞에, 야코비안(34번)을 softmax 야코비안(41번) 앞에, 직교(7번)를 사영보다 먼저 두었다. RoPE는 복소수 없이 2×2 회전행렬(11번)로만 유도해 오일러 공식 의존을 없앴다.

**초급이 받지 않는 것 둘은 중급 안에 소유자를 만들었다.** 점-초평면 거리
\|wᵀx+b\|/‖w‖는 중급 5번 brief가, λ²−(tr A)λ+det A로 읽는 근과 계수 ↔ tr A·det A
대응은 중급 12번 brief가 자기 것으로 적는다. 초급 24번은 2차원 점-직선 거리까지이고
차원을 올리는 것을 명시적으로 거절하며, 초급 23번은 근과 계수의 관계까지다.

## 목록 — 80편

### 1단원 · 수식을 읽는 법

> 논문과 코드의 수식 한 줄을 소리 내어 읽고, 기호마다 어떤 텐서가 들어가는지 말할 수 있다. 이후 77편 전부가 이 표기 위에서 돈다.

| # | 슬러그 | 제목 | 무엇을 쓰는가 |
|---:|---|---|---|
| 1 | `math-attention-formula-anatomy` | 어텐션 식 한 줄에 들어 있는 수학 전부: 이 커리큘럼의 지도 | softmax(QK^T/√d_k)V를 왼쪽부터 오른쪽까지 기호 단위로 쪼개고, 각 기호를 읽는 데 필요한 수학이 이 커리큘럼의 몇 번째 글인지 표로 대응시킨다. 내적·행렬곱·softmax·분산·마스킹·미분이 각각 어느 자리에 있는지 표시하는 지도 글이다. 어텐션이 왜 필요한지와 Q·K·V의 직관은 transformer-attention-from-first-principles가 이미 다루므로 여기서는 설명하지 않고, 오직 '이 식을 읽으려면 무엇이 필요한가'만 답한다. 식 자체의 계산과 유도는 40~43번 글에서 끝낸다. |
| 2 | `math-notation-conventions` | 논문 수식 표기 규약: 스칼라·벡터·행렬·텐서를 구별해서 읽기 | x와 굵은 x와 X와 𝒳의 구분, 열벡터 관례, 전치와 위·아래 첨자, 원소 표기와 슬라이스, ⊙·⊗·‖·‖ 같은 연산 기호, f: R^n → R^m 서술의 정의역·공역·상. 논문 첫 페이지의 Notation 절을 사전 없이 읽고 같은 기호가 코드의 어떤 텐서 축에 대응하는지 맞춰 보는 것이 목표다. 기존 296편 어디에도 표기 규약을 다룬 글이 없어 중복 위험이 없다. |
| 3 | `math-sigma-index-and-einsum` | Σ와 첨자, 그리고 einsum: 수식을 그대로 코드로 옮기기 | 시그마·파이 표기, 이중합과 합의 순서 교환, 자유 첨자와 더미 첨자, 아인슈타인 합 규약, 그리고 np.einsum/torch.einsum으로 같은 식을 한 줄로 쓰는 법. 어텐션 점수 QK^T와 합성곱 한 칸을 첨자로 완전히 풀어 쓴 뒤 einsum으로 되돌리는 연습이 중심이다. 논문 수식과 구현 코드의 축을 일대일로 맞추는 능력이 이후 모든 글의 전제다. |

### 2단원 · 벡터: 임베딩 공간의 기하

> 임베딩 검색에서 '가깝다'가 무엇인지 세 지표의 수식과 그 관계로 설명하고, 어떤 지표를 언제 쓸지 계산으로 답한다. 【절단점 1 — 여기까지 8편이면 임베딩 공간의 기하가 완결된다】

| # | 슬러그 | 제목 | 무엇을 쓰는가 |
|---:|---|---|---|
| 4 | `math-vector-as-meaning` | 벡터: 임베딩 한 줄이 숫자 배열이 아니라 벡터인 이유 | 벡터의 정의(덧셈과 스칼라배에 닫힌 대상), 선형결합, 좌표와 화살표 두 관점, 브로드캐스팅으로 본 벡터 연산. 토큰 임베딩과 은닉 상태를 벡터로 다룬다는 것이 무엇을 보장하는지, 'king − man + woman' 같은 임베딩 산술이 왜 선형결합 하나인지를 다룬다. embedding-basics가 임베딩의 쓰임을 다루므로 이 글은 벡터라는 수학 대상만 맡는다. |
| 5 | `math-norms-and-distance` | 노름과 거리: L1·L2·L∞와 단위구의 모양 | 노름의 세 공리, L1·L2·L∞의 정의와 단위구를 그려 비교하기, 노름이 유도하는 거리, 노름 동치. 그래디언트 클리핑이 재는 양(L2 노름), 가중치 감쇠가 벌하는 양, 희소성을 만드는 L1 단위구의 모서리가 각각 어떤 성질에서 나오는지 연결한다. **초평면과 점-초평면 거리 \|wᵀx+b\|/‖w‖가 이 글의 것이다** — 초급 24번은 2차원 점-직선 거리 \|ax₀+by₀+c\|/√(a²+b²)까지이고 항을 n개로 늘리는 것을 명시적으로 거절한다. ‖w‖로 나누는 것이 방금 세운 노름이므로 여기가 그 자리이고, 67번의 무작위 초평면 해싱과 기존 ml-svm의 마진 2/‖w‖가 결과만 쓰는 식을 이 글이 유도한다. |
| 6 | `math-dot-product-and-cosine` | 내적·코사인·L2 거리는 언제 같은 순위를 주고 언제 갈리는가 | 내적의 정의와 기하적 의미 ‖a‖‖b‖cosθ, 코시-슈바르츠 부등식, 코사인 유사도, 그리고 ‖a−b‖² = ‖a‖² − 2a·b + ‖b‖² 전개. 정규화된 벡터에서 세 지표의 순위가 정확히 일치함을 증명하고, 정규화하지 않으면 순위가 뒤집히는 반례를 직접 만든다. QK^T가 '질의와 키의 관련도'인 이유가 여기서 나온다. vector-similarity-metrics는 지표 선택 실전 가이드이므로 이 글은 그 위층인 동치 조건의 증명만 맡는다. |
| 7 | `math-orthogonality-and-projection` | 직교와 사영: 한 벡터를 다른 벡터로 설명하고 남은 것 | 직교의 정의, 정규직교기저, 한 벡터를 다른 벡터·부분공간 위로 사영하는 공식과 사영 행렬 P = A(AᵀA)⁻¹Aᵀ, 잔차가 부분공간에 직교한다는 최소제곱의 기하. 직교 초기화와 직교 정규화가 무엇을 강제하는지, 그리고 멀티헤드가 '서로 다른 것을 본다'는 말이 직교와 어떻게 다른지(학습된 절단이지 직교 사영이 아니다)를 짚는다. |
| 8 | `math-span-basis-and-coordinates` | 생성·일차독립·기저: 같은 벡터를 다른 축으로 다시 쓰기 | 생성(span), 일차독립, 기저와 차원, 좌표변환 행렬. 임베딩이 실제로 몇 차원을 쓰고 있는가(표현 붕괴)라는 질문을 일차독립으로 정의하고, 주성분 축이나 회전 위치 인코딩처럼 '축을 바꾼다'는 조작이 전부 같은 연산임을 보인다. 수치적으로 독립성을 판정하는 문제는 15번 글(SVD와 랭크)로 넘긴다. |

### 3단원 · 행렬: 선형사상과 분해

> 행렬을 숫자표가 아니라 공간을 옮기는 함수로 읽고, 어텐션 텐서의 shape와 LoRA의 r을 스펙트럼 근거로 설명한다. 【절단점 2 — 여기까지 17편이면 어텐션·LoRA의 행렬 수학이 닫힌다】

| # | 슬러그 | 제목 | 무엇을 쓰는가 |
|---:|---|---|---|
| 9 | `math-matrix-as-linear-map` | 행렬은 함수다: W_Q가 벡터에 하는 일 | 선형사상의 정의, 행렬이 기저의 상(image)을 열에 적어 둔 것이라는 관점, 단위 정사각형이 회전·전단·축소되는 그림, 합성사상이 곧 행렬곱이라는 것. nn.Linear 한 층과 W_Q·W_K·W_V 투영이 공간에 무슨 짓을 하는지 기하로 읽는 습관을 만든다. |
| 10 | `math-matmul-and-shape-arithmetic` | 행렬곱 네 가지 시선과 배치·헤드 shape 산수 | 행렬곱을 원소·행·열·외적의 합 네 관점으로 계산하고, (B, H, T, d_h) 텐서에서 어느 축이 축약되는지 손으로 따라간다. 멀티헤드 어텐션의 reshape·transpose·matmul 순서를 shape만 보고 재구성하고 shape 오류를 실행 전에 예측하는 연습이 중심이다. 3번 글의 einsum 표기를 그대로 쓴다. |
| 11 | `math-rotation-and-2d-linear-maps` | 회전·전단·스케일: 2×2 행렬로 보는 선형변환의 전부 | **사인·코사인의 정의는 초급 25번(단위원·라디안·파형 파라미터)이고, 여기서는 회전행렬 R(θ)와 그 직교성만 맡는다.** 단위원 위의 좌표 (cos θ, sin θ)를 받아 R(θ)를 세운 뒤, R(α)R(β) = R(α+β)를 행렬곱으로 직접 확인해 각도 합 공식이 사실 행렬 항등식임을 보인다. 회전이 내적과 노름을 보존한다는 성질(직교행렬)까지. 복소수와 오일러 공식 없이 진행하며, 이 결과가 43번 RoPE 글의 유일한 전제가 된다. |
| 12 | `math-eigenvalues` | 고윳값과 고유벡터: 방향이 변하지 않는 축 | Av = λv의 정의, 2×2 특성방정식, 고유공간, 대각화 A = PΛP⁻¹와 대각화가 안 되는 경우. **행렬식의 정의와 부피 배율이라는 뜻은 초급 44번의 것이라 여기서 다시 정의하지 않고 쓰기만 한다.** 대신 **특성방정식을 전개한 λ² − (tr A)λ + det A = 0을 이 글이 자기 것으로 갖는다** — 근과 계수의 관계(초급 23번)를 그대로 읽으면 '고윳값의 합은 대각합, 곱은 행렬식'이라는 문장이 되고, 2×2 몇 개로 손으로 확인해 고윳값을 풀지 않고도 tr A와 det A만으로 스펙트럼을 읽는 습관을 만든다(초급 42번은 tr A라는 이름 한 줄만 준다). 판별식이 음수라 복소 고윳값이 나오는 경우가 회전행렬이라는 것까지 짚는다. '스펙트럼', '주축', '고유 방향' 같은 표현을 정확히 읽는 도구이며, 이후 곡률(45번)·공분산(23번)·안정성(13번)에서 반복 등장한다. |
| 13 | `math-matrix-powers-and-power-iteration` | 행렬 거듭제곱과 거듭제곱법: 스펙트럼 반지름이 정하는 것 | A^k가 최대 고윳값의 k제곱에 지배된다는 사실, 스펙트럼 반지름, 그리고 반복 곱셈만으로 최대 고윳값·고유벡터를 구하는 거듭제곱법과 그 수렴 속도가 \|λ₂/λ₁\|로 결정됨. 순환망에서 기울기가 폭발·소실하는 것이 곱해지는 행렬의 스펙트럼 반지름 문제임을 계산으로 보인다. nn-vanishing-gradient는 현상과 해결책을 다루므로 이 글은 '왜 지수적인가'만 맡는다. 스펙트럼 정규화용 σ_max 추정은 17번이 이어받는다. |
| 14 | `math-spectral-theorem-and-quadratic-forms` | 대칭 행렬: 직교 대각화와 이차형식의 부호 | 대칭 행렬이 항상 직교 대각화된다는 스펙트럼 정리, 이차형식 xᵀAx, 양정치·양반정치·부정부호 판정과 고윳값 부호의 대응. AI에서 만나는 행렬(공분산·헤세·그람)이 대부분 대칭인 이유를 짚고, 45번에서 손실 지형의 국소 모양을 판정하는 근거를 여기서 마련한다. **그릇 z=x²+y²과 안장 z=x²−y²의 개형 그림은 초급 45번 `math-basics-surfaces-and-contours`의 것이다** — 이 글은 그 그림을 다시 그리지 않고 고윳값 부호로 그 둘을 판정하는 절차만 맡는다. |
| 15 | `math-svd-and-rank` | 특잇값 분해: 모든 행렬을 회전-늘림-회전으로 쪼개고 랭크를 읽기 | A = UΣVᵀ의 구성과 기하적 의미, 특잇값과 AᵀA 고윳값의 관계, 랭크의 정의와 특잇값 문턱으로 판정하는 수치 랭크, 특잇값 분포를 그려 표현이 실제로 몇 차원을 쓰는지 재는 법. ml-pca가 sklearn PCA 사용법과 설명 분산비를 다루므로 이 글은 그 밑의 선형대수만 맡는다. |
| 16 | `math-low-rank-approximation-and-lora` | 저계수 근사와 에카르트-영: LoRA의 r을 스펙트럼으로 고르기 | 상위 k개 특잇값만 남긴 근사가 프로베니우스·스펙트럼 노름 모두에서 최적이라는 에카르트-영 정리, 잘라 낸 오차가 남은 특잇값의 제곱합이라는 계산, 그리고 ΔW = BA라는 LoRA의 형태를 계수 제약으로 다시 쓰기. r을 감이 아니라 실제 가중치의 특잇값 분포에 근거해 고르는 절차와 α/r 스케일의 의미까지. finetuning-lora는 PEFT 설정법이므로 이 글은 근거만 맡는다. |
| 17 | `math-matrix-norms` | 행렬 노름: 프로베니우스·스펙트럼 노름과 립시츠 상수 | 유도 노름의 정의, ‖A‖₂ = σ_max, 프로베니우스 노름과 특잇값 제곱합의 관계, 부분곱셈성 ‖AB‖ ≤ ‖A‖‖B‖, 그리고 선형층의 립시츠 상수가 곧 스펙트럼 노름이라는 것. 스펙트럼 정규화·그래디언트 클리핑·립시츠 제약이 전부 같은 언어를 쓴다는 것을 보이고, 13번의 거듭제곱법으로 σ_max를 실제로 추정한다. |

### 4단원 · 확률: 모델의 출력은 분포다

> 언어모델이 내놓는 것이 왜 확률분포이고, 그 분포를 어떻게 만들고 어떻게 뽑는지 수식으로 잇는다.

| # | 슬러그 | 제목 | 무엇을 쓰는가 |
|---:|---|---|---|
| 18 | `math-exp-and-log` | 지수와 로그: AI 코드에 log가 도처에 있는 이유 | **지수·로그 법칙과 자연로그·e의 정의는 초급 13·21·22번의 것이고, 여기서는 로그 공간 계산과 언더플로만 맡는다.** 곱을 합으로 바꾸는 성질과 로그의 단조성(argmax가 보존된다)을 결과로 받아, 그것이 계산 안정성에서 무엇을 하는지로 곧장 간다. logits·log_softmax·log-likelihood·logprob이 왜 전부 로그인지, 확률 수백 개를 곱할 때 fp32에서 무슨 일이 일어나는지 수치로 확인한다. |
| 19 | `math-probability-rules-and-chain-rule` | 확률 규칙: 결합·주변·조건부, 베이즈, 그리고 자기회귀 분해 | 확률의 공리, 결합·주변·조건부 확률과 독립, 베이즈 규칙에서 사전·가능도·사후·증거의 자리, 그리고 확률의 연쇄법칙 p(x₁…x_T) = Π p(x_t\|x_<t). 자기회귀 언어모델이 문장 하나의 확률을 정의하는 방식이 이 분해 그 자체임을 보인다. 사후분포는 61번(확산 역과정)에서 다시 쓴다. |
| 20 | `math-random-variables-and-expectation` | 확률변수와 기댓값: 베르누이·범주형·이항부터 LOTUS까지 | 확률변수의 정의, pmf와 pdf의 차이(밀도는 1을 넘을 수 있다), cdf, 베르누이·범주형·이항 분포와 각각이 AI 어디에 쓰이는지(드롭아웃·토큰 샘플링·정답 개수), 기댓값의 정의와 선형성, LOTUS. '손실은 데이터 분포 위의 기댓값'이라는 문장을 실제로 계산할 수 있게 되는 것이 목표다. |
| 21 | `math-variance-and-sampling-error` | 분산과 표준오차: 미니배치는 추정량이다 | 분산·표준편차·공분산의 정의, 독립 합의 분산 가법성, 표본평균의 분산이 σ²/n이라는 결과와 대수의 법칙, 불편 추정의 n−1. 배치 크기가 그래디언트 잡음과 평가 점수의 흔들림을 동시에 좌우하는 이유가 전부 이 한 식에서 나오며, 40번(√d_k)·49번(배치와 학습률)·74번(평가 오차막대)이 이 결과를 그대로 재사용한다. |
| 22 | `math-gaussian-and-clt` | 정규분포가 어디에나 있는 이유: 성질과 중심극한정리 | 정규분포의 밀도, 선형변환에 닫혀 있음, 독립 합의 재생성, 표준화, 68-95-99.7 규칙, 그리고 중심극한정리와 그 수렴 속도를 시뮬레이션으로 확인하기. 가중치 초기화·확산 노이즈·GELU·평가 신뢰구간에서 정규분포가 반복 등장하는 근거를 한 번에 마련한다. |
| 23 | `math-covariance-and-multivariate-gaussian` | 공분산 행렬과 다변량 정규분포: 상관 있는 표본 만들기 | 공분산 행렬의 정의와 양반정치성, 상관계수, 다변량 정규분포의 밀도와 등고선이 타원인 이유, 그리고 촐레스키 분해(대칭 양정치 행렬의 제곱근)를 그 자리에서 세워 z ~ N(0,I)에서 상관 있는 표본 Lz를 만드는 법. 14번 스펙트럼 정리를 재사용하며, 59번(확산 노이즈)과 68번(임베딩 이방성)이 이 도구를 다시 쓴다. |
| 24 | `math-softmax-derivation` | softmax 유도: 점수 벡터를 확률로 바꾸는 함수는 왜 그 모양인가 | 실수 벡터를 확률분포로 보내는 함수가 만족해야 할 조건(양수·정규화·순서 보존·평행이동 불변)을 세우고 exp가 강제되는 과정을 유도한다. 로짓을 로그 확률로 읽는 관점, 이진 분류에서 sigmoid로 환원됨, 온도 파라미터가 분포에 하는 일, 상수를 더해도 값이 같다는 성질까지. 분류 출력·어텐션 가중치·MoE 라우팅이 전부 같은 함수 하나를 통과한다. llm-temperature-top-k-top-p는 튜닝 가이드이므로 이 글은 함수 자체의 유도만 맡는다. |
| 25 | `math-sampling-from-categorical` | 범주분포에서 뽑기: 온도·top-k·top-p·Gumbel-max의 수학 | 역변환 표집으로 범주분포에서 뽑는 법, 온도·top-k·top-p가 확률질량의 어느 부분을 자르고 어떻게 재정규화하는지 그림과 식으로, 그리고 Gumbel-max 트릭(로짓에 Gumbel 잡음을 더해 argmax를 취하면 정확히 softmax 샘플링과 같다)의 증명. 이산 선택을 미분 가능하게 만드는 Gumbel-softmax까지 이어 붙인다. llm-sampling-strategies·llm-decoding-methods는 전략 비교이므로 이 글은 수학과 등가성 증명만 맡는다. |

### 5단원 · 정보: 손실 한 줄이 곧 정보량이다

> 사전학습 손실 한 줄이 어디서 나왔는지 끝까지 유도하고, perplexity·KL 페널티·증류 손실·대조 손실이 같은 틀에서 나온 값임을 안다. 【절단점 3 — 여기까지 31편이면 '무엇을 최소화하는가'가 닫힌다】

| # | 슬러그 | 제목 | 무엇을 쓰는가 |
|---:|---|---|---|
| 26 | `math-entropy-and-perplexity` | 엔트로피와 정보량: perplexity가 재고 있는 것 | 정보량 −log p, 엔트로피 H(p) = −Σ p log p, 비트와 내트, 균등분포에서 최대가 되는 이유의 증명, 그리고 perplexity = exp(H)가 '평균적으로 몇 개 중에 고민하는가'라는 해석. loss 2.1과 perplexity 8.2가 같은 숫자의 다른 표현임을 확인하고, bits-per-byte와 '언어모델은 압축기다'라는 주장을 수치로 읽는다. |
| 27 | `math-maximum-likelihood` | 최대가능도: '학습'이라는 말의 가장 정확한 정의 | 가능도와 확률의 차이, 로그가능도, i.i.d. 가정에서 합으로 분해되는 과정, 가능도 곡면을 그려 최댓값을 찾는 실험. 정규분포 가정에서 MSE가, 범주분포 가정에서 교차엔트로피가 떨어져 나오는 유도를 끝까지 해서 손실함수가 임의로 정해진 것이 아님을 보인다. ai-loss-functions는 손실 카탈로그이므로 이 글은 출처의 유도만 맡는다. |
| 28 | `math-cross-entropy-and-nll` | 교차엔트로피 = 음의 로그가능도: 사전학습 손실 한 줄의 유도 | 교차엔트로피 H(p,q)의 정의, 원-핫 정답에서 −log q_target으로 줄어드는 계산, 그것이 27번의 음의 로그가능도와 정확히 같은 식임을 확인. 라벨 스무딩이 목표 분포 p를 바꾸는 조작이라는 관점, 토큰 단위 평균 손실이 무엇의 추정량인지, 그리고 손실 값과 perplexity·bits-per-token의 환산까지. 이 글로 다음 토큰 예측 손실 한 줄의 유도가 완결된다. |
| 29 | `math-jensen-inequality` | 젠센 부등식: 볼록함수와 기댓값의 순서 | 볼록함수의 정의(현이 그래프 위에 있다)와 이계도함수 판정, 젠센 부등식 f(E[X]) ≤ E[f(X)]의 증명과 등호 조건. 이것으로 KL ≥ 0을 증명하고, '구하기 어려운 로그가능도 대신 하한을 최대화한다'는 발상의 뿌리를 놓는다. 여기서 정의한 볼록성을 44번(손실 지형)과 60번(ELBO)이 그대로 확장해 쓴다. 기존 296편에 젠센 부등식을 다룬 글이 없다. |
| 30 | `math-kl-divergence` | KL 발산: 거리가 아닌 거리와 방향의 선택 | KL(p‖q)의 정의, 교차엔트로피 − 엔트로피 분해, 젠센으로 얻은 비음수성, 비대칭성과 삼각부등식 실패, 그리고 정방향 KL이 분포를 넓게 덮게 하고 역방향 KL이 한 봉우리에 몰리게 하는 mode-covering/mode-seeking 그림. JS 발산도 여기서 정의한다. RLHF의 KL 페널티·증류 손실·변분 하한이 모두 같은 양임을 확인한다. llm-dpo·llm-rlhf·distillation은 기법이므로 이 글은 정의와 성질만 맡는다. |
| 31 | `math-mutual-information` | 상호정보량: 두 변수가 공유하는 정보와 InfoNCE 하한 | I(X;Y) = H(X) − H(X\|Y) = KL(p(x,y)‖p(x)p(y))의 세 표현이 같음을 보이고, 표본만으로는 추정이 어렵다는 사실, 그리고 InfoNCE가 상호정보량의 하한이라는 유도 — 배치 크기 N이 하한의 천장 log N을 정한다는 결론까지. 대조학습이 실제로 최대화하는 양이 무엇이며 왜 배치가 커야 하는지에 답한다. cv-clip은 CLIP 학습 방식이므로 이 글은 목적식의 근거만 맡는다. |

### 6단원 · 미분: 역전파는 연쇄법칙 한 줄이다

> 층 하나의 그래디언트를 shape까지 맞춰 종이 위에서 유도하고, 그 유도가 맞는지 유한차분으로 검증한다. 【절단점 4 — 여기까지 39편이면 '어떻게 최소화하는가'가 닫힌다】

| # | 슬러그 | 제목 | 무엇을 쓰는가 |
|---:|---|---|---|
| 32 | `math-derivative-and-gradient` | 도함수는 민감도다: 국소 선형근사에서 그래디언트까지 | 도함수를 극한이 아니라 '최선의 선형근사'로 정의하고, 그래디언트 벡터, 방향도함수, −∇f가 가장 가파른 내리막인 이유의 증명(코시-슈바르츠), 등고선과 그래디언트가 수직인 이유. **편미분의 정의와 계산은 초급 45번의 것이라 여기서 다시 세우지 않고 받아 쓴다** — 이 글이 갖는 것은 그 편도함수들을 벡터 하나로 묶은 뒤의 이야기다. 파라미터 수십억 개에 대한 미분이 벡터 하나로 정리되는 관점을 만든다. |
| 33 | `math-chain-rule` | 연쇄법칙: 층을 통과하며 곱해지는 비율 | 합성함수의 미분, 다변수 연쇄법칙(경로마다 곱하고 경로끼리 더한다), 계산 그래프 위에서 손으로 끝까지 따라가는 예제, 그리고 곱이 길어질 때 값이 지수적으로 커지거나 작아진다는 관찰. 역전파의 유일한 원리다. nn-forward-backward는 PyTorch autograd 알고리즘과 학습 루프를 다루므로 이 글은 법칙 자체만 맡는다. |
| 34 | `math-jacobian` | 야코비안: 벡터함수의 미분은 행렬이다 | R^n → R^m 함수의 야코비안 정의와 배치, 선형사상 근사로서의 의미, 합성함수의 야코비안이 야코비안의 곱이라는 사실, 야코비안 행렬식이 부피 배율이라는 것과 변수변환에서 밀도가 바뀌는 이유. 층 하나의 미분이 행렬이라는 사실이 역전파 비용 계산의 단위가 된다. 기존 296편에 '야코비안'이라는 말이 한 번도 나오지 않는 완전한 안전지대다. |
| 35 | `math-vjp-and-jvp` | VJP와 JVP: 딥러닝이 역방향으로 미분하는 이유를 연산량으로 증명하기 | 벡터-야코비안 곱과 야코비안-벡터 곱의 정의, 전방 모드와 역방향 모드가 각각 무엇을 한 번에 계산하는지, 입력 n·출력 m일 때 비용이 각각 n회와 m회의 통과라는 계산. 손실이 스칼라(m=1)이고 파라미터가 수십억(n이 큼)이라는 형태가 역방향을 강제한다는 결론까지. autograd가 야코비안 행렬을 실제로 만들지 않고도 역전파하는 이유가 여기서 나온다. |
| 36 | `math-matrix-calculus` | 행렬 미분: 규약을 정하고 shape로 검산하기 | 분자 배치와 분모 배치 규약(논문마다 전치가 달라 보이는 이유), ∂(Wx)/∂x = W, ∂L/∂W = δxᵀ, ∂(xᵀAx)/∂x = (A+Aᵀ)x 같은 핵심 공식의 유도, 그리고 공식을 외우는 대신 shape가 맞는 유일한 조합을 찾아 복원하는 기술. 논문의 그래디언트 식을 그대로 코드로 옮기는 실무 능력이 목표다. |
| 37 | `math-softmax-cross-entropy-gradient` | softmax와 교차엔트로피의 기울기가 정확히 p − y인 이유 | softmax의 야코비안 diag(p) − ppᵀ를 유도하고, 교차엔트로피와 합성했을 때 그 행렬이 깨끗하게 소거되어 ∂L/∂z = p − y가 되는 과정을 끝까지 계산한다. 두 함수를 왜 항상 붙여서 구현하는지(수치적 이유 포함), 그리고 모든 분류 모델의 마지막 층에서 실제로 흐르는 기울기가 무엇인지. 41번의 어텐션 그래디언트가 이 결과를 그대로 쓴다. |
| 38 | `math-activation-derivatives` | 활성함수의 미분: GELU가 x·Φ(x)인 이유 | ReLU·LeakyReLU·sigmoid·tanh·GELU·SiLU의 도함수 유도와 그래프, 포화 구간에서 기울기가 0에 붙는다는 계산, GELU가 표준정규 CDF Φ와 입력의 곱으로 정의되는 근거(확률적 게이팅)와 tanh 근사식의 오차, ReLU가 0에서 미분 불가능한 것이 실제로 문제가 되는지. nn-activation-functions는 선택 가이드이므로 이 글은 미분과 유도만 맡는다. |
| 39 | `math-gradient-checking` | 유도가 맞는지 확인하기: 유한차분, 오차 차수, 기울기 검사 | 전진차분과 중심차분의 절단오차 차수 O(h)·O(h²)를 테일러로 유도하고, 반올림오차와 만나는 최적 스텝 h를 로그-로그 그래프로 찾는 실험, 상대오차 기준과 허용 오차 설정, 그리고 직접 유도한 기울기를 검사하는 절차. 이 글이 정한 검증 규약을 이후 모든 유도 글이 마지막 절에 붙여 쓴다. |

### 7단원 · 어텐션의 수학

> 1단원에서 지도로만 봤던 어텐션 식을 이제 기호 하나하나 계산과 유도로 닫는다. 【절단점 5 — 여기까지 43편이면 어텐션 한 줄이 완전히 해부된다】

| # | 슬러그 | 제목 | 무엇을 쓰는가 |
|---:|---|---|---|
| 40 | `math-scaling-by-sqrt-dk` | √d_k는 어디서 나왔나: 내적의 분산 계산 | q와 k의 성분이 평균 0·분산 1로 독립이라 가정하면 q·k의 분산이 정확히 d_k가 된다는 계산(21번의 독립 합 분산 가법성). 분산이 커질수록 softmax 입력의 스케일이 커져 분포가 한 점에 몰리고, 37번에서 유도한 기울기와 41번의 야코비안이 함께 0으로 죽는다는 연결. 스케일링을 빼면 학습이 멈추는 현상을 작은 실험으로 재현하고, 초기화 스케일·LayerNorm과 역할이 어떻게 나뉘는지까지 정리한다. |
| 41 | `math-softmax-jacobian-and-attention-gradient` | 어텐션의 그래디언트: softmax 야코비안이 분포에 따라 하는 일 | 37번의 diag(p) − ppᵀ를 어텐션 가중치에 적용해 ∂L/∂Q, ∂L/∂K, ∂L/∂V를 shape까지 맞춰 끝까지 유도한다. 분포가 뾰족할수록 야코비안의 값이 0에 가까워져 어텐션이 한 토큰에 몰리면 학습 신호가 죽는다는 결론, 그리고 잔차 연결이 이 경로를 우회하는 이유. 3~6단원의 도구를 한자리에 합치는 캡스톤이다. |
| 42 | `math-log-sum-exp-and-online-softmax` | log-sum-exp: 최댓값 빼기, −inf 마스킹, 그리고 온라인 softmax | LSE 항등식 log Σexp(x_i) = m + log Σexp(x_i − m)의 증명과 오버플로·언더플로 방지, 마스킹을 왜 0을 곱하지 않고 −inf를 더해서 하는지(그리고 한 행이 전부 −inf일 때 NaN이 나오는 이유), 그리고 부분합 두 개의 (max, sum)을 이어 붙이는 점화식으로 softmax를 스트리밍 계산하는 유도. transformer-flash-attention은 IO 병목과 타일링 관점이므로 이 글은 그 밑의 대수만 맡는다. |
| 43 | `math-positional-encoding-math` | 위치를 수로 적기: 사인파 주파수 사다리와 RoPE의 회전 불변성 | 정수 위치를 주파수가 기하급수적으로 감소하는 사인·코사인 쌍으로 적는 이유와 파장이 만드는 해상도 사다리, 그리고 RoPE가 차원쌍마다 11번의 회전행렬 R(mθ)를 적용했을 때 내적 ⟨R(mθ)q, R(nθ)k⟩가 오직 (m−n)에만 의존한다는 증명. 상대 위치 불변성이 회전행렬의 직교성에서 그냥 따라 나온다는 것을 보인다. transformer-positional-encoding과 transformer-rotary는 구현과 외삽 문제를 다루므로 이 글은 유도만 맡는다. |

### 8단원 · 최적화: 손실 지형을 내려가기

> 학습률·배치 크기·워밍업·Adam 하이퍼파라미터를 곡률과 그래디언트 잡음으로 설명하고 값을 근거 있게 고른다. 【절단점 6 — 여기까지 51편이면 학습 루프의 모든 손잡이가 설명된다】

| # | 슬러그 | 제목 | 무엇을 쓰는가 |
|---:|---|---|---|
| 44 | `math-loss-landscape-and-convexity` | 손실 지형: 딥러닝은 볼록이 아닌데 왜 학습이 되는가 | 29번에서 정의한 볼록성을 다변수로 확장(볼록집합, 헤세 양반정치 판정), 지역해가 전역해가 되는 조건, 그리고 신경망 손실이 왜 비볼록인지(뉴런 치환 대칭성). 고차원에서 진짜 장애물이 국소최소가 아니라 안장점과 평지라는 주장을 고윳값 부호가 전부 같을 확률로 설명한다. |
| 45 | `math-taylor-hessian-and-curvature` | 테일러 2차 근사와 헤세 행렬: 곡률, 안장점, 조건수 | 다변수 테일러 전개, 헤세 행렬, 2차 판정(14번의 이차형식 부호 재사용), 헤세의 고윳값이 각 방향의 곡률이라는 해석, 그리고 조건수 λ_max/λ_min이 등고선을 얼마나 길쭉하게 만드는지. '날카로운 최소점', '평평한 최소점', '잘 조건화된 문제' 같은 표현을 정확히 읽는 도구이며 다음 세 글이 전부 이 결과를 쓴다. |
| 46 | `math-gradient-descent-and-lr-bound` | 경사하강법 유도와 학습률 상한이 2/L인 이유 | 1차 근사에서 경사하강 갱신식을 유도하고, L-매끄러움(그래디언트의 립시츠 상수)을 정의한 뒤 2차 상계로 손실이 반드시 줄어드는 구간 0 < η < 2/L을 유도한다. 이차함수에서 η를 키울 때 수렴 → 진동 → 발산으로 넘어가는 정확한 경계를 실험으로 재현하고, 조건수가 나쁘면 수렴이 몇 배 느려지는지 계산한다. |
| 47 | `math-momentum-and-ema` | 모멘텀·네스테로프·EMA는 같은 점화식이다 | 지수이동평균 v ← βv + (1−β)g의 점화식과 유효 평균 창 1/(1−β), 모멘텀이 EMA된 그래디언트로 움직이는 것이라는 관점, 네스테로프의 미리보기 항, 그리고 좁은 골짜기에서 모멘텀이 얻는 이득을 조건수의 함수로 계산하기. 옵티마이저 모멘텀·EMA 체크포인트·BatchNorm 러닝 통계가 전부 같은 식이라는 것을 확인한다. |
| 48 | `math-adam-from-moments` | 1차·2차 모멘트에서 Adam을 직접 조립하기 | 그래디언트의 1차·2차 모멘트를 EMA로 추정하고 m̂/(√v̂+ε)로 나누는 갱신식을 처음부터 조립한다. 편향 보정 1/(1−βᵗ)이 왜 필요한지의 유도, β₁·β₂·ε가 각각 무엇을 조절하는지, 왜 이것이 대각선 프리컨디셔너인지, 그리고 AdamW의 분리형 가중치 감쇠가 L2 정규화 항과 수학적으로 어디서 갈리는지. ai-regularization은 정규화 기법 소개이므로 이 글은 갱신식만 맡는다. |
| 49 | `math-sgd-noise-batch-and-schedule` | 그래디언트 잡음이 정하는 것: 배치 크기, 선형 스케일링, 워밍업, 코사인 감쇠 | 미니배치 그래디언트가 불편 추정량이고 그 분산이 σ²/B라는 계산(21번), 배치를 k배 키울 때 학습률을 얼마나 올려야 잡음 대비 신호가 유지되는지(선형 스케일링 법칙과 그것이 깨지는 지점), 초반 그래디언트 통계가 불안정해 워밍업이 필요한 이유, 그리고 코사인·역제곱근 감쇠의 식과 후반 미세 수렴에서의 역할. |
| 50 | `math-weight-init-variance` | 초기화 분산 전파: Xavier·He 유도와 폭 스케일링 | 한 층을 통과할 때 활성값의 분산이 어떻게 변하는지 유도하고, 순전파와 역전파 양쪽에서 분산이 유지되는 조건에서 Xavier와 He 초기화를 얻는다. 깊이가 늘 때 분산이 지수적으로 어긋나는 계산, 잔차 연결과 정규화가 그것을 어떻게 막는지, 그리고 폭이 커질 때 학습률을 어떻게 스케일해야 하는가(muP의 출발점). nn-weight-init은 구현과 실험이므로 이 글은 유도만 맡는다. |
| 51 | `math-newton-and-second-order` | 뉴턴법과 2차 방법: 왜 안 쓰는가, 요즘은 무엇을 근사하는가 | 뉴턴 갱신 H⁻¹∇f의 유도와 이차수렴, 그러나 파라미터 n개에서 H가 n²개 항목이고 역이 n³ 연산이라는 비용 계산, 헤세를 만들지 않고 헤세-벡터 곱만으로 2차 정보를 쓰는 방법. 그리고 Adam의 대각 근사, K-FAC의 크로네커 근사, Shampoo·Muon 계열이 각각 헤세(또는 피셔 정보)의 무엇을 근사하고 있는지 정리한다. |

### 9단원 · 정렬: 제약이 걸린 최적화

> PPO 목적식과 DPO 손실을 각각 유도하고, 왜 둘이 같은 문제의 두 해법인지 설명한다. 사이트에 정렬 글이 14편 있는데 그 밑의 수학이 한 편도 없던 자리다. 【절단점 7 — 여기까지 58편이면 정렬 논문의 수식이 열린다】

| # | 슬러그 | 제목 | 무엇을 쓰는가 |
|---:|---|---|---|
| 52 | `math-expected-reward-objective` | 정책 위의 기댓값: '좋은 답을 내게 한다'를 최적화 문제로 적기 | 정책 π_θ, 궤적, 그리고 J(θ) = E_{y~π_θ}[r(y)]라는 목적식을 세우는 과정. 지도학습의 손실과 달리 기댓값을 취하는 분포 자체가 θ에 의존한다는 점이 왜 문제인지(그래디언트를 기댓값 안으로 그냥 넣을 수 없다), 그리고 그 문제가 다음 글의 출발점이 된다는 것. rl-basics·rl-policy-gradient는 강화학습 전반을 다루므로 이 글은 목적식의 형식과 난점만 맡는다. |
| 53 | `math-log-derivative-trick` | 로그 미분 트릭: 샘플만으로 그래디언트를 얻기 | ∇_θ E_{x~p_θ}[f(x)] = E_{x~p_θ}[f(x)∇_θ log p_θ(x)]의 유도와 성립 조건(적분과 미분의 교환), 이것이 불편 추정량이라는 확인, 그리고 왜 분산이 큰지. 재매개변수화 트릭(59번)과 무엇이 다르며 이산 토큰에서는 왜 로그 미분 트릭밖에 없는지까지. rl-policy-gradient는 REINFORCE 구현이므로 이 글은 항등식과 그 성립 조건만 맡는다. |
| 54 | `math-baseline-and-advantage` | 베이스라인과 어드밴티지: 편향 없이 분산만 줄이기 | E[b·∇log p] = 0이라는 계산으로 어떤 상수(또는 상태에만 의존하는) 베이스라인도 편향을 만들지 않음을 증명하고, 분산을 최소로 만드는 베이스라인을 유도한다. 어드밴티지 A = r − b의 의미, 그리고 GRPO가 그룹 평균을 빼는 것이 왜 이 정리의 직접 적용인지. rl-actor-critic·reasoning-rl-training은 알고리즘이므로 이 글은 증명만 맡는다. |
| 55 | `math-bradley-terry-preference` | 선호 쌍에서 보상 모델 손실 유도하기 | Bradley-Terry 모델 P(A ≻ B) = σ(r_A − r_B)를 세우고, 27번의 최대가능도를 적용해 −log σ(r_w − r_l)이라는 보상 모델 손실을 얻는 전 과정. 보상이 상수 평행이동에 불변이라 절대값에는 의미가 없다는 성질, 그리고 비교 데이터 n개로 추정할 때 개별 보상의 불확실성이 얼마나 되는지. 78번 Elo가 정확히 이 모델을 다시 쓴다. |
| 56 | `math-lagrange-and-kkt` | 라그랑주 승수와 KKT: 제약을 목적식 안으로 넣기 | 등식 제약에서 ∇f = λ∇g가 되는 기하적 이유, 라그랑지안, 부등식 제약에서의 KKT 조건과 상보 여유, 그리고 승수가 '제약을 한 단위 풀었을 때의 이득'이라는 해석. 'KL 예산 안에서 보상을 최대화한다'를 식 하나로 쓰는 법을 익히고 다음 글이 그것을 실제로 푼다. 기존 296편에 라그랑주·KKT를 다룬 글이 없다. |
| 57 | `math-kl-constrained-optimum-and-dpo` | KL 제약 보상 최대화의 닫힌 해, 그리고 거기서 떨어져 나오는 DPO 손실 | max_π E[r] − βKL(π‖π_ref)의 해가 π*(y\|x) ∝ π_ref(y\|x)·exp(r(x,y)/β)라는 볼츠만 형태임을 유도하고, 이 식을 r에 대해 뒤집어 r = β log(π/π_ref) + log Z를 얻은 뒤 55번의 Bradley-Terry에 대입하면 분배함수 Z가 소거되며 DPO 손실이 나오는 과정을 끝까지 따라간다. β가 조절하는 것이 무엇인지도 여기서 답한다. llm-dpo는 실무 가이드와 변형 소개이므로 이 글은 유도만 맡는다. |
| 58 | `math-importance-ratio-and-clipping` | 중요도 비율과 클리핑: 예전 정책의 샘플을 재사용하는 대가 | 중요도 표집으로 다른 분포의 샘플에서 기댓값을 추정하는 식과 그 불편성, 비율 r(θ) = π_θ/π_old의 분산이 두 분포가 멀어질수록 폭발한다는 계산과 유효 표본 수, 그리고 PPO의 clip이 목적함수를 조각별 선형으로 바꿔 한 걸음을 제한하는 방식. 신뢰 영역과 KL 조기 중단이 막으려는 붕괴가 정확히 무엇인지까지. rl-ppo는 알고리즘 구현이므로 이 글은 추정량의 성질만 맡는다. |

### 10단원 · 생성: 노이즈를 푸는 미분방정식

> 확산의 forward·reverse를 식으로 쓰고, 샘플링 스텝 수와 guidance scale이 결과를 바꾸는 이유를 계산으로 설명한다.

| # | 슬러그 | 제목 | 무엇을 쓰는가 |
|---:|---|---|---|
| 59 | `math-reparameterization-and-forward-process` | 재매개변수화 트릭과 폐형 forward 과정 | x = μ + σz라는 한 줄로 샘플링을 미분 가능하게 만드는 트릭과, 이것이 53번의 로그 미분 트릭보다 분산이 작은 이유. 그리고 가우시안을 t번 더한 결과가 다시 가우시안이라는 성질(22번)을 써서 q(x_t\|x_0)를 한 번에 계산하는 폐형 유도 — 이 폐형이 있어야 임의의 t에서 학습이 가능해진다. VAE와 확산이 같은 장치를 공유한다는 것도 여기서 확인한다. |
| 60 | `math-elbo` | ELBO: 못 구하는 로그가능도를 아래에서 받치기 | log p(x) = ELBO + KL(q‖p)라는 분해를 29번의 젠센 부등식과 30번의 KL 정의 두 가지 경로로 유도하고, 하한을 최대화하는 것이 왜 정당한지, 재구성 항과 정규화 항이 어떻게 갈리는지, 하한이 얼마나 헐거운지를 무엇이 정하는지. VAE와 확산의 손실이 같은 뼈대 위에 있다는 것을 보이고 EM도 같은 발상임을 짚는다. 기존 296편에 ELBO·변분 하한을 다룬 글이 없다. |
| 61 | `math-score-matching` | 스코어 ∇log p: '노이즈를 예측한다'가 곧 스코어 추정인 이유 | 스코어 함수 ∇_x log p(x)의 정의와 정규화 상수가 미분에서 사라진다는 결정적 성질, 가우시안에서 스코어가 −(x−μ)/σ²라는 계산, 그리고 노이즈 예측 ε_θ와 스코어가 상수배로 이어진다는 denoising score matching의 유도. cv-diffusion-basics는 U-Net 구조와 DDPM 구현을 다루므로 이 글은 목적식의 정체만 맡는다. |
| 62 | `math-sde-ode-and-discretization` | SDE·ODE와 이산화 오차: 스텝 수가 품질을 정한다 | 확산을 연속시간 SDE로 적고 대응하는 확률 흐름 ODE를 세운 뒤, 오일러 이산화의 국소·전역 오차 차수를 유도한다. 20스텝과 1000스텝의 차이가 어디서 오는지, DDIM이 왜 결정적이며 더 적은 스텝으로도 되는지, 고차 solver가 무엇을 벌어들이고 무엇을 대가로 치르는지 계산으로 답한다. |
| 63 | `math-guidance-as-extrapolation` | classifier-free guidance는 스코어 공간의 외삽이다 | ε̃ = ε_uncond + w(ε_cond − ε_uncond)라는 식을 세우고, 이것이 조건부 분포를 p(y\|x)^w로 기울이는 것과 같음을 스코어 공간에서 유도한다. w를 올리면 왜 선명해지다가 어느 지점부터 채도와 다양성이 무너지는지를 분포의 꼬리를 잘라 내는 효과로 설명한다. cv-controlnet·cv-stable-diffusion은 사용법이므로 이 글은 유도만 맡는다. |
| 64 | `math-flow-matching-and-optimal-transport` | 흐름 매칭과 최적수송: 확률 경로를 직선으로 펴기 | 두 분포를 잇는 확률 경로와 그것을 만드는 속도장, 조건부 흐름 매칭 목적식의 유도, 그리고 직선 경로가 왜 적은 스텝으로도 잘 도는지. 바서슈타인 거리와 최적수송의 최소한(왜 KL이 아니라 수송 비용을 재는가, KL이 정의되지 않는 경우)을 여기서 정의하고, 62번의 ODE 관점과 어떻게 이어지는지 정리한다. 기존 296편에 흐름 매칭·최적수송을 다룬 글이 없다. |

### 11단원 · 고차원과 검색

> 임베딩 차원·유사도·인덱스 선택을 감이 아니라 고차원 기하와 충돌 확률로 결정한다.

| # | 슬러그 | 제목 | 무엇을 쓰는가 |
|---:|---|---|---|
| 65 | `math-high-dimensional-geometry` | 고차원에서 직관이 무너지는 지점: 거리 집중과 부피 쏠림 | d차원 초구의 부피가 껍질에 몰리는 계산, 무작위 두 벡터가 거의 항상 직교에 가깝다는 결과, 그리고 최근접 이웃과 최원거리 이웃의 거리 비가 1로 수렴하는 거리 집중 현상을 시뮬레이션으로 확인한다. 1024차원 임베딩 공간이 3차원과 다르게 행동하는 방식이 검색 품질에 어떻게 나타나는지, 그리고 언제 근사 검색으로 충분하고 언제 검색 자체가 무의미해지는지. |
| 66 | `math-random-projection-and-jl` | 랜덤 사영과 존슨–린덴스트라우스 보조정리 | 무작위 사영이 모든 쌍의 거리를 (1±ε) 안에서 보존하는 데 필요한 차원이 k = O(log n / ε²)이라는 결과와 유도 개요, 점 개수 n에는 로그로만 의존하고 원래 차원에는 아예 의존하지 않는다는 사실. 임베딩 차원을 줄여도 되는 이유이며, LSH와 Matryoshka 임베딩이 딛고 선 보장이 여기다. |
| 67 | `math-lsh-collision-probability` | 무작위 초평면 해싱: 충돌 확률을 유도하고 recall을 확률로 읽기 | 무작위 초평면 하나로 두 벡터가 같은 쪽에 놓일 확률이 1 − θ/π라는 유도, 비트 k개와 테이블 L개를 쓸 때의 1 − (1−p^k)^L 곡선, 그리고 그 곡선에서 recall과 후보 수의 트레이드오프를 읽는 법을 시뮬레이션과 대조한다. vector-ann-algorithms는 HNSW·IVF·LSH 알고리즘 비교이므로 이 글은 확률 유도만 맡는다. |
| 68 | `math-anisotropy-and-whitening` | 임베딩 분포의 이방성: 공분산으로 재고 whitening으로 펴기 | 23번의 공분산 행렬로 임베딩이 좁은 원뿔에 몰리는 현상을 고윳값 분포로 측정하고(설명 분산비, 이방성 지표), 평균 제거와 whitening 변환 Σ^(−1/2)이 코사인 유사도 분포를 어떻게 바꾸는지 계산한다. 유사도가 전부 0.8 근처에 몰려 순위가 흐려지는 문제의 정체와, 후처리가 무엇을 대가로 치르는지까지. |
| 69 | `math-kernels-and-gram-matrices` | 커널과 그람 행렬: 내적을 바꾸면 무엇이 달라지나 | 그람 행렬의 정의와 양반정치성, 커널이 암묵적 특징공간의 내적이라는 Mercer 관점, RBF·다항 커널과 그 특징공간, 그리고 어텐션 점수 행렬도 (비대칭이라는 차이는 있지만) 같은 자리에 있는 유사도 행렬이라는 관점. ml-svm은 커널 트릭 사용법이므로 이 글은 그람 행렬의 성질과 PSD 판정만 맡는다. |

### 12단원 · 유한한 기계의 수학

> bf16·fp8·int4에서 무엇이 언제 깨지는지 미리 예측하고, 모델의 연산량을 손으로 센다.

| # | 슬러그 | 제목 | 무엇을 쓰는가 |
|---:|---|---|---|
| 70 | `math-floating-point-and-cancellation` | 부동소수점은 어디서 새는가: 반올림, 상쇄, 더하는 순서 | **초급 47번이 이 글로 통째로 넘긴 자리다** — 그 글은 자릿수와 오차를 손계산으로만 다루고 0.1+0.2가 0.3이 아닌 이유와 머신 엡실론을 여기로 넘긴다(밑이 2인 진법을 세운 글이 초급에 없다). 부호·지수·가수 구조에서 나오는 상대오차 상한(머신 엡실론), 같은 16비트인데 bf16과 fp16이 왜 서로 다른 지점에서 터지는지, 비슷한 수를 뺄 때 유효자리가 통째로 사라지는 파괴적 상쇄, 그리고 긴 합(어텐션 로짓·손실 누산)에서 더하는 순서가 결과를 바꾼다는 실험과 카한 합산. quantization-basics가 비트 레이아웃과 PTQ/QAT를 이미 다루므로 이 글은 오차가 새어 나가는 자리만 맡는다. |
| 71 | `math-numerical-stability-patterns` | 수치적으로 안정한 계산 패턴 모음 | 최댓값 빼기(42번), 로그 공간 계산, log1p·expm1, 분산의 두 공식 중 무엇이 안전한가, fp32 누산과 혼합정밀도에서 무엇을 어디에 두는가, 정규화의 ε이 실제로 막는 것, 역행렬 대신 solve를 쓰는 이유(조건수). 프레임워크가 몰래 해 주는 일을 목록으로 정리하고 각각 어떤 고장을 막는지 붙인다. |
| 72 | `math-quantization-error` | 양자화 오차: 격자 사상, 오차 분산, 이상치 채널 | 실수 구간을 2^b개 격자에 사상할 때의 스텝 크기와 균등 양자화 오차의 분산 Δ²/12, 그것이 SNR로 번역되는 계산, 대칭·비대칭 스케일의 차이, 그리고 이상치 하나가 스케일을 통째로 늘려 나머지 값의 유효 비트를 잡아먹는 계산. int4에서 성능이 무너지는 지점을 오차 예산으로 미리 예측한다. quantization-basics·quantization-awq-gptq는 기법과 도구이므로 이 글은 오차의 수학만 맡는다. |
| 73 | `math-asymptotics-and-flops` | 점근 표기와 연산량 손계산: O·Θ·Ω, 파라미터 수, 어텐션의 O(n²) | **O·Θ·Ω의 정의와 상수를 버리는 것이 언제 정당한가는 초급 47번이 h를 반씩 줄이며 차수를 재는 자리에서 이미 세웠다. 이 글은 shape에서 FLOPs를 세는 법만 맡는다.** 행렬 shape에서 곱셈-덧셈 횟수를 직접 세어 선형층·어텐션·FFN의 FLOPs와 트랜스포머 블록의 파라미터 수를 손으로 유도한다. 시퀀스 길이가 두 배가 될 때 무엇이 네 배가 되고 무엇이 두 배가 되는지 구분하는 것이 목표다. llm-scaling-laws는 경험 법칙, inference-kv-cache는 메모리 최적화이므로 이 글은 세는 법만 맡는다. |

### 13단원 · 측정: 모든 점수에는 오차막대가 있다

> 벤치마크 점수 차이가 실제 차이인지 판정하고, 리더보드와 스케일링 곡선을 오해 없이 읽는다. 뉴스로 벤치마크를 매일 다루는 이 사이트에 가장 직접 쓰이는 단원이다.

| # | 슬러그 | 제목 | 무엇을 쓰는가 |
|---:|---|---|---|
| 74 | `math-estimator-and-standard-error` | 추정량과 표준오차: eval 점수에 오차막대 붙이기 | 추정량의 불편성·일치성, 정확도 같은 비율 추정량의 표준오차 √(p(1−p)/n), 정규 근사 신뢰구간과 소표본에서 더 정확한 Wilson 구간. 500문항에서 82%와 84%가 유의한 차이인지 직접 계산해 답하고, 오차막대 없이 발표된 벤치마크 표를 어떻게 읽어야 하는지 정리한다. |
| 75 | `math-bootstrap` | 부트스트랩: 분포 가정 없이 신뢰구간 만들기 | 재표집으로 표본분포를 근사하는 원리, 백분위수 구간과 BCa의 차이, 필요한 재표집 횟수, 그리고 평균이 아닌 지표(F1, nDCG, pass@k)에도 그대로 쓸 수 있다는 것. 해석식이 없는 평가 지표의 불확실성을 재는 표준 도구이며, 77번과 함께 쓰면 거의 모든 eval 지표에 구간을 붙일 수 있다. |
| 76 | `math-paired-test-and-multiple-comparisons` | 같은 문항으로 두 모델 비교하기: 쌍체 검정, 효과 크기, 그리고 여러 번 재면 생기는 일 | 같은 문항을 쓰면 문항 난이도의 변동이 상쇄되어 훨씬 적은 표본으로 결론이 난다는 계산, 이항 부호 검정과 순열 검정, 효과 크기와 검정력, 그리고 벤치마크를 k개 돌리면 우연히 이길 확률이 1−(1−α)^k로 늘어난다는 사실과 Bonferroni·FDR 보정. t검정·분산분석·카이제곱 같은 고전 추론통계를 이 한 편으로 대체한다. |
| 77 | `math-monte-carlo-and-passk` | 몬테카를로 추정과 pass@k: 샘플 n개로 무엇을 말할 수 있나 | 기댓값을 표본평균으로 추정할 때 오차가 1/√n로 줄어든다는 것과 그 실전적 함의, 그리고 n번 시도해 c번 성공했을 때 pass@k의 불편 추정량 1 − C(n−c,k)/C(n,k)의 유도. 순진한 추정이 왜 위로 편향되는지, 원하는 정밀도를 위해 n을 몇으로 잡아야 하는지 계산한다. HumanEval류 지표를 스스로 계산하고 오차를 붙일 수 있게 되는 것이 목표다. |
| 78 | `math-elo-and-arena-ranking` | Elo와 아레나 순위: Bradley–Terry로 리더보드 읽기 | 55번의 Bradley-Terry 모델에서 Elo 갱신식이 확률적 경사상승 한 걸음이라는 유도, K 계수의 의미, 온라인 갱신과 최대가능도 일괄 추정의 차이, 그리고 대전 수가 적은 모델의 순위 신뢰구간이 얼마나 넓은지. 순위표에서 몇 위 차이가 사실은 구분되지 않는지 계산으로 판정한다. llm-comparison-benchmarks는 벤치마크 소개이므로 이 글은 순위 모델의 수학만 맡는다. |
| 79 | `math-calibration-and-ece` | 보정: 모델이 말하는 확률을 믿어도 되는가 | 보정의 정의(신뢰도 0.8이라고 한 것 중 실제로 80%가 맞아야 한다), 신뢰도-정확도 도표, ECE의 계산과 빈 개수에 따라 값이 달라진다는 약점, Brier 점수와 그 분해, 그리고 온도 스케일링으로 사후 보정하는 법. logprob이나 자기 신뢰도를 라우팅·거절·검증에 쓸 때 깔려 있는 전제를 검사한다. |
| 80 | `math-power-law-and-scaling-curves` | 멱법칙과 로그-로그 직선: 스케일링 곡선 읽기와 외삽의 한계 | y = ax^(−b) 꼴이 로그-로그에서 직선이 되는 이유와 기울기가 곧 지수라는 것, 최소제곱으로 지수를 적합할 때 로그 변환이 오차 구조에 하는 일, 환원 불가 항 y = ax^(−b) + c를 넣으면 무엇이 달라지는지, 그리고 두 자릿수 밖으로 외삽할 때 깨지는 지점. llm-scaling-laws는 Kaplan·Chinchilla의 내용이므로 이 글은 곡선을 적합하고 해석하는 통계만 맡는다. |

---

# 초급 트랙 — 48편

**1번부터 48번까지 순서가 있는 과정이다.** 옛 23편은 순서도 번호도 없는 사전이었고
자가진단 한 편으로 시작했는데, 그 둘을 다 버렸다. 읽는 사람이 자기 수준을 판정할
필요가 없다 — 1번부터 읽으면 된다.

이 48편은 앞서 만든 33편 초안을 늘린 것이다. **33편 가운데 일곱 편이 둘로 갈라져
40자리가 되었고, 그 앞에 완전히 새로 지은 여덟 편이 붙었다.** 33 + 7 + 8 = 48.
아래에서 '초안'이라 부르는 것이 그 33편이다.

## 이 트랙은 무엇인가

**독자는 둘이다.** (가) 고등학교 수학을 오래 안 써서 잊은 개발자, (나) 수학을 아예
모르는 채로 시작하는 사람. 계산을 할 줄 안다고 전제하지 않는다 — 세로셈이 왜 자리를
맞추는지, 분수 나눗셈이 왜 역수의 곱인지, 음수를 곱하면 왜 부등호가 뒤집히는지부터
다시 세운다. **다만 코드는 쓸 줄 안다고 본다.**

**AI와 엮지 않는다.** 본문에 AI 예시·코드·용어를 넣지 않고, "이건 나중에 어텐션에서
쓰입니다" 같은 예고도 넣지 않는다. 수학을 수학으로 설명하고 끝낸다. 어디에 쓰이는지는
글 밖의 장치 — `curriculum.ts`의 `mathSupport`가 그리는 대응 배지 — 가 맡는다.
**따라서 이 문서의 brief도 AI로 존재 이유를 적지 않는다.** 각 글은 트랙 안에서
'앞 글이 세운 것을 받아 뒤 글에 넘긴다'는 자리로만 정당화된다.

**코드와 대조하는 것은 −1단계 세 편(1·2·3번)에서만 허용한다.** 이 독자가 수학에서
막히는 첫 자리는 계산이 아니라 표기 관습이고 — `=`가 할당이 아니라는 것, 연산
우선순위가 코드와 갈리는 자리, 생략된 곱셈 기호, 분수선이 묶는 범위 — 그것을 설명할
때 쓸 수 있는 유일한 발판이 프로그래밍 일반이다. AI가 아니라 프로그래밍 일반과의
대조이며, 4번부터 48번까지는 코드가 한 줄도 없다(마지막 48번의 SymPy만 예외이고
그것은 검산 도구 그 자체다).

**분량은 5,000~9,000자.** 중급·고급의 8,000~15,000자보다 짧다. 개념을 서너 개씩
묶지 않고, 한 편이 한 계단만 오른다.

**순서 있는 과정이다. 사전이 아니다.** 1번부터 48번까지 번호를 매기고 앞 글이 뒤 글의
전제가 되게 배치했다. 옛 23편 계획의 "순서가 없다"는 원칙과 정반대이며, 자가진단·수준확인
같은 안내 글은 넣지 않는다. 읽는 사람이 자기 수준을 판정할 필요가 없다 — 1번부터
읽으면 된다.

**손으로 하는 계산이 본체다.** 초급에서 개념 설명은 계산을 시키기 위한 준비이고,
각 편은 무엇을 몇 문제 시킬지를 brief에 적어 둔다.

**한 편이 '세우는 것'은 여섯 안쪽이다.** 분량이 중급의 60%인데 항목 수는 중급보다
많으면 루틴은 둘 중 하나를 한다 — 상한을 넘겨 쓰거나, 넘기지 않으려고 계산 예제를
지우고 개념 나열로 만든다. 후자가 되면 이 트랙의 전제가 첫 배치부터 깨진다. 그래서
아래 brief의 '무엇을 쓰는가' 칸은 두 층으로 읽는다.

- **세우는 것** — 정의를 내리고, 유도를 한 줄씩 적고, 손계산을 붙이는 것. 편당 여섯 안쪽.
- **한 줄로 지나가는 것** — 이름과 결과만 적고 넘어가는 것. 칸 안에 "한 줄로",
  "결과만", "이름만", "쓰기만 한다"라고 적혀 있으면 이쪽이다.

33편을 48편으로 늘리면서 편당 부담이 줄었지만 **항목을 다시 늘리지 않았다.** 쪼갠
편들은 원본의 절을 나눠 가진 뒤 남은 자리에 손계산을 넓혔을 뿐이다. 항목을 도로
늘리면 쪼갠 이유가 사라진다. **그 반대 방향으로는 손을 댔다** — 쪼개지지 않고 33편
초안에서 그대로 온 아홉 편(16·17·18·23·28·32·35·44·47번)이 일곱에서 아홉 항목이었고,
전부 여섯 안쪽으로 접었다. 접는 기준은 하나다 — 초급에서 다시 쓰는 자리가 있는가.
없으면 이름과 결과만 남기고 그 자리를 손계산으로 돌렸다.
**새로 지은 여덟 편은 다섯에 맞춰 두었다** — 첫 배치에서 상한을 꽉 채우면 집필 중
하나만 늘어도 넘어간다. 여섯인 것은 문자 약속 한 문단을 받은 6번 하나다.

늘어난 것은 셋뿐이고 전부 '소유자가 없어진 것을 되찾은' 자리다 — 6번의 문자 약속
한 문단, 12번의 ∝와 합으로 나누기, 45번의 두 축 적분 한 절. 셋 다 해당 편이 여섯을
넘지 않는 선에서 들어갔다.

트랙 이름은 frontmatter의 `level` 값과 같은 어휘를 쓴다 — **초급 / 중급 / 고급**.
다른 트랙을 부를 때는 「중급 12번」처럼 적는다. 옛 이름은 쓰지 않는다.

**계단은 11개(−1~9단계), 글은 48편이다.** 단계별 편수는 3·5·7·3·5·3·3·7·5·4·3이고,
더하면 48이다.

| 단계 | 이름 | 편수 | 번호 |
| ---: | --- | ---: | --- |
| −1 | 수학을 읽고 쓰는 법 | 3 | 1~3 |
| 0 | 자연수와 도형 | 5 | 4~8 |
| 1 | 수와 계산 | 7 | 9~15 |
| 2 | 방정식과 부등식 | 3 | 16~18 |
| 3 | 함수 | 5 | 19~23 |
| 4 | 좌표와 도형 | 3 | 24~26 |
| 5 | 수열과 극한 | 3 | 27~29 |
| 6 | 미적분 | 7 | 30~36 |
| 7 | 확률과 통계 | 5 | 37~41 |
| 8 | 선형대수 준비 | 4 | 42~45 |
| 9 | 읽기와 검산 | 3 | 46~48 |


### −1단계 · 수학을 읽고 쓰는 법 (3편)

> 학년 밖이다. 개발자가 수학에서 막히는 첫 자리는 계산이 아니라 표기 관습이라는
> 판단에서 나왔다. 1번이 `=` 하나, 2번이 '무엇을 먼저 하는가' 하나, 3번이 '어떻게
> 적고 어떻게 검산하는가' 하나를 맡는다. **코드와 대조하는 것이 허용되는 세 편이다.**

| # | 슬러그 | 제목 | 무엇을 쓰는가 | 전제 |
|---:|---|---|---|---|
| 1 | `math-basics-what-equals-means` | 등호가 뜻하는 것: =는 넣으라는 명령이 아니다 | 등호를 처음 만나는 자리다. `=`가 '왼쪽에 오른쪽을 넣으라'는 명령이 아니라 '이 둘이 같은 것을 가리킨다'는 선언임을 못 박는 데서 시작한다 — **코드와 대조하는 것을 허용하는 세 편 중 첫 편이고**, x = x + 1이 코드에서는 매일 쓰는 줄이지만 수식으로는 참이 되는 x가 없는 식이라는 대조 하나가 출발점이다. 그다음 같은 기호를 세 갈래로 가른다 — 어떤 값에서도 참인 항등식, 특정한 값에서만 참인 방정식, 새 이름을 붙이는 정의. 셋 다 `=` 하나를 쓰므로 문맥으로 가려 읽어야 한다는 데까지가 여기의 몫이고, 방정식을 실제로 푸는 절차는 16번 · 일차방정식과 이항의 것이다. 이어 등호 사슬을 세운다 — 유도가 왜 한 줄에 하나씩 등호로 이어지는지, 맨 왼쪽과 맨 오른쪽이 같다는 것, 중간 한 줄이 틀리면 사슬 전체가 무너진다는 것. 양변에 같은 것을 하면 등호가 유지된다는 원칙도 말로 세운다(성질 넷을 절차로 굳히는 것은 16번). **≠·≈·<·≤ 같은 관계 기호의 읽는 법은 여기서 적지 않는다** — 읽는 법은 9번 · 수식 기호 읽기 하나가 맡는다. 손으로 할 것은 열 줄짜리 판정이다 — 주어진 등식이 항등식인지 방정식인지 정의인지 가르고, 등호 사슬 다섯 개에 숫자를 넣어 맨 왼쪽과 맨 오른쪽이 정말 같은지 확인한다. **틀린 줄을 짚어 고치는 훈련은 3번 · 손으로 푸는 습관의 것이다** — 여기서는 사슬이 무엇을 약속하는지까지만 한다. 기호의 발음을 모은 사전은 9번 · 수식 기호 읽기, ⇒와 ⟺의 구분과 증명 문서의 관용구는 46번 · 증명 읽는 법의 것이다. | 없음 — 첫 글이다 |
| 2 | `math-basics-reading-order-of-expressions` | 식을 읽는 순서: 괄호 없이 묶여 있는 것들 | 식 하나에 기호가 여럿 놓였을 때 무엇을 먼저 하는가만 다룬다. 연산 우선순위를 괄호 → 거듭제곱 → 곱셈·나눗셈 → 덧셈·뺄셈으로 세우고, 같은 순위는 왼쪽부터라는 것, 뺄셈과 나눗셈은 순서를 바꾸면 값이 달라지므로 그 규칙이 필요했다는 것을 숫자로 확인한다. 그다음이 본체이고 **표기 관습이 실제로 발목을 잡는 네 자리**다. (가) 곱셈 기호를 생략하는 관습 — 3a, 2(x+1), ab는 전부 곱이며 코드로 옮길 때는 생략할 수 없다. (나) 분수선이 위아래를 통째로 묶는 괄호라는 것 — 손으로 쓴 분수를 한 줄로 옮기며 (a+b)/2를 a+b/2로 적는 것이 가장 흔한 사고다. (다) 함수 이름과 근호가 덮는 범위 — sin 2x와 sin(2x)와 sin x + 1, √a+b와 √(a+b), log ab(**셋이 무슨 계산인지는 여기서 다루지 않는다. 선이 어디까지 덮는가만 본다.**) (라) 위첨자와 아래첨자 — **a²를 '같은 것을 두 번 곱했다'로 읽는 약속을 트랙에서 처음 주는 자리가 여기다**(지수법칙과 a⁰·음의 지수는 13번 · 거듭제곱과 근호의 것이고, 위첨자를 읽을 줄 알아야 5번 · 약수와 배수·소인수분해의 2³×3²×5를 읽는다). 위첨자가 무엇까지 덮는지는 −3²과 (−3)²로 보이되 **값은 계산하지 않는다** — 음수의 곱셈은 10번 · 수의 종류와 수직선이 세운다. a_n의 첨자는 곱이 아니라 이름표라는 것도 여기서 못 박는다. 손으로 할 것은 셋이다. 괄호가 하나도 없는 식 열 개에 괄호를 채워 뜻을 확정하기, 손글씨 분수 다섯을 한 줄로 옮겨 적기, 겉보기에 같아 보이는 식 다섯 쌍에 숫자를 넣어 값이 갈리는 것을 확인하기. **넣는 수는 자연수만 골라 양쪽 값이 전부 자연수로 떨어지게 한다** — 2·3²과 (2·3)², (4+6)/2과 4+6/2 같은 쌍이다. −3²과 (−3)², √a+b와 √(a+b)처럼 음수·분수·근호 계산이 필요한 쌍은 **읽는 법만 적고 값을 확인하지 않는다** — 그 계산은 10·11·13번의 것이고 여기서 여덟에서 열한 편 뒤다. 지수법칙과 근호 계산 자체는 13번 · 거듭제곱과 근호, 기호 하나하나의 발음을 모은 사전은 9번 · 수식 기호 읽기, Σ와 ∫의 위아래 끝이 덮는 범위는 그 기호를 세우는 27번 · 시그마 표기와 수열과 33번 · 정적분의 것이다. | 1번 |
| 3 | `math-basics-working-by-hand` | 손으로 푸는 습관: 한 줄에 한 조작, 그리고 숫자로 검산 | 앞의 두 편이 읽는 법이었다면 이 글은 쓰는 법이다. **한 줄에 한 조작**을 규칙으로 세운다 — 여러 조작을 한 줄에 몰면 답이 틀렸을 때 어느 줄이 틀렸는지 찾을 수 없다. 그래서 줄 왼쪽에 '양변에 2를 곱함'처럼 무슨 조작을 했는지 이름을 적고, 등호를 세로로 맞춰 쓴다. 그다음이 이 트랙 마흔다섯 편이 계속 쓰게 될 검산 습관 셋이다. (가) 문자로 얻은 결과에 x = 0, 1, 2를 넣어 양변의 값을 맞춰 보기 — **넣는 수는 0과 자연수로 한정한다.** 음수와 분수를 넣는 것은 10·11번 뒤의 일이고, 대입을 검산 요령이 아니라 정식 계산 도구로 세우는 것은 14번 · 문자와 대입이다. 값이 갈리면 유도가 틀렸다는 것이 확실하고, 맞는다고 맞다는 보장은 아니라는 것까지 함께 적는다. (나) 극단값으로 그럴듯함 보기 — 0이나 아주 큰 수를 넣었을 때 답이 말이 되는가. (다) 같은 것을 두 방법으로 계산해 맞춰 보기. 마지막은 틀렸을 때 의심할 순서를 목록으로 준다 — 부호, 괄호, 옮겨 적다 빠뜨린 항, 분모. 손으로 할 것은 일부러 한 줄이 틀린 풀이 다섯 개에서 틀린 줄을 짚는 것과, 앞 두 편의 예제를 한 줄에 한 조작으로 다시 적는 것이다. **여기서 들이는 습관이 48번 · SymPy 검산이 기계로 옮길 절차의 손 판본이고**, 그 글은 이 대조 방식을 코드로 적기만 한다. 유효숫자와 오차의 크기를 재는 것은 47번 · 자릿수와 오차, 남이 쓴 유도의 생략된 줄을 복원하는 것은 18번 · 식 조작의 것이다. | 1·2번 |

### 0단계 · 자연수와 도형 (5편)

> 구구단을 가르치는 자리가 아니다. 4·5번은 세로셈이 왜 그렇게 생겼는지와 수를 곱으로
> 쪼개 두는 법을, 6·7·8번은 뒤에서 계속 빌려 갈 그림 셋(직사각형 넓이, 넓이 비교,
> 닮은 삼각형의 비)을 세운다. **6·7·8번은 33편 초안이 빌려 쓰기만 하던 것을 갚는
> 자리다** — 초안의 좌표평면 글은 피타고라스를 증명 없이 받아 썼고, 삼각비 글은
> 닮음을 세운 글 없이 시작했다. 코드는 이 다섯 편에 한 줄도 없다.

| # | 슬러그 | 제목 | 무엇을 쓰는가 | 전제 |
|---:|---|---|---|---|
| 4 | `math-basics-natural-numbers-and-place-value` | 자연수의 사칙연산과 자릿수: 세로셈이 자리를 맞추는 이유 | 자연수의 계산을 다시 세우되 구구단이 아니라 **자리**를 다룬다. 3705가 3×1000 + 7×100 + 0×10 + 5라는 위치 기수법에서 시작해, 0이 값이 아니라 자리를 지키는 기호라는 것과 자리가 하나 늘면 값이 10배가 된다는 것을 확인한다. 그 위에서 세로셈이 왜 자리를 맞춰 쓰는지 답한다 — 덧셈의 받아올림과 뺄셈의 받아내림이 '한 자리에 열이 모이면 윗자리 하나'라는 규칙 그대로이고, 곱셈 세로셈의 네 줄은 23×45를 20+3과 40+5의 자리별 곱 넷으로 나눠 적은 것과 같다는 것을 펼쳐서 대조한다. **그 규칙에 분배법칙이라는 이름을 붙여 음수의 부호를 유도하는 데 쓰는 것은 10번 · 수의 종류와 수직선의 것이다** — 여기서는 세로셈이 그렇게 생긴 이유를 자릿수로 설명하는 데까지만 간다. 다음은 나눗셈이다. 17을 5로 나누면 몫 3에 나머지 2가 남고 17 = 5×3 + 2이며 나머지는 언제나 0 이상 5 미만이라는 것 — 이 한 줄이 5번 · 약수와 배수·소인수분해의 출발점이다. 마지막 두 가지는 한 줄씩 지나간다. 덧셈과 곱셈은 순서를 바꿔도 되지만 뺄셈과 나눗셈은 안 된다는 것(2번 · 식을 읽는 순서의 '왼쪽부터'가 왜 필요했는가), 그리고 만·억까지의 자릿수 감각. 손으로 할 것은 자릿수 전개 다섯, 세로셈 덧셈·뺄셈·곱셈 각 다섯, 몫과 나머지 열이다. 음수는 10번, 분수는 11번, 10의 거듭제곱으로 큰 수를 적는 과학적 표기는 13번 · 거듭제곱과 근호의 것이다. | 1·2·3번 |
| 5 | `math-basics-divisors-and-prime-factorization` | 약수와 배수·소인수분해: 수를 곱으로 쪼개 두기 | 4번이 얻은 '나머지가 0이면 나누어떨어진다'에서 곧장 출발한다. 약수와 배수가 같은 관계를 양쪽에서 부르는 두 이름이라는 것을 세우고, 36의 약수 아홉 개를 1부터 짝지어 빠짐없이 찾는 절차를 손에 익힌다. 다음은 소수다 — 1보다 크고 약수가 1과 자기 자신뿐인 수. **1을 소수에서 빼는 이유를 약속이 아니라 결론으로 적는다**: 1을 넣으면 곱으로 쪼개는 방법이 무한히 많아져 뒤에 나올 유일성이 깨진다. 에라토스테네스의 체로 100까지 직접 걸러 소수 스물다섯을 얻는다. 그 위에 소인수분해를 세운다 — 작은 소수부터 나눠 내려가는 절차, 360 = 2³×3²×5 같은 표기, 그리고 순서를 빼면 그 분해가 하나뿐이라는 사실(증명하지 않는다. 46번 · 증명 읽는 법이 √2가 무리수임을 보이며 이 유일성을 쓴다). 마지막은 최대공약수와 최소공배수다. **33편 초안이 분수 글 안에 두었던 것을 여기로 앞당겼다** — 11번 · 분수의 사칙연산은 약분·통분과 사칙연산 넷을 세우는 것만으로 이미 분량에 차고, 소인수분해를 방금 세운 자리에서 지수의 작은 쪽·큰 쪽을 골라 읽는 것이 절차로도 짧다. 11번은 결과를 받아 쓰기만 한다. 손으로 할 것은 체로 100까지 거르기, 소인수분해 열, 최대공약수와 최소공배수 열 쌍, 72와 120으로 두 값의 곱이 원래 두 수의 곱과 같은지 확인하기다. 유클리드 호제법과 합동식은 이 과정 어디에서도 쓰지 않으므로 다루지 않는다. | 4번 |
| 6 | `math-basics-area-and-volume` | 넓이와 부피: 잘라 붙여 직사각형으로 만들기 | **트랙에서 문자를 처음 쓰는 글이라 한 문단을 먼저 쓴다** — 길이나 반지름처럼 정해진 양 하나에 a·r 같은 글자로 이름을 붙여 두면 도형이 바뀔 때마다 같은 말을 다시 하지 않아도 된다는 것, 그리고 글자와 글자 사이의 곱셈 기호를 생략한다는 것(읽는 법은 2번 · 식을 읽는 순서가 이미 줬다). **여기서 세우는 것은 '글자가 정해진 양 하나를 대신한다'는 약속 하나뿐이고**, 그 글자가 미지수·변수·상수·계수로 갈리는 것과 식에 값을 넣어 계산하는 것은 14번 · 문자와 대입의 것이다. 그 위에서 넓이를 '단위 정사각형을 몇 개 깔 수 있는가'로 정의하는 데서 시작한다. 그러면 직사각형의 넓이가 가로×세로라는 것은 공식이 아니라 4번이 세운 곱셈의 뜻 그대로다. 평행사변형·삼각형·사다리꼴은 잘라 붙여 직사각형으로 만드는 그림 세 장으로 얻는다 — 삼각형이 왜 밑변×높이의 절반인지를 외우지 않고 복원하게 하는 것이 목적이다. 원은 지름에 대한 둘레의 비로 π를 정의해 둘레 2πr를 세운 뒤, 부채꼴로 잘라 번갈아 붙이면 밑변 πr에 높이 r인 평행사변형에 가까워진다는 그림으로 넓이 πr²를 얻는다. 부피도 같은 방식이다 — 단위 정육면체, 직육면체, 기둥은 밑넓이×높이. **뿔의 3분의 1과 구의 부피·겉넓이는 결과만 적는다**(뿔은 같은 밑면·높이의 그릇에 물을 세 번 부어 확인하는 데까지). 마지막은 단위다 — 1 m²가 왜 100 cm²가 아니라 10,000 cm²인지를 그림으로 못 박는다. 손으로 할 것은 도형 열 개의 넓이, 잘라 붙이는 그림 셋을 직접 그려 공식 복원하기, 부피 다섯, 단위 환산 다섯이다. **여기서 그린 직사각형 넓이 그림을 뒤의 세 편이 그대로 빌려 간다** — 7번의 넓이 비교 증명, 10번 · 수의 종류와 수직선의 분배법칙, 15번 · 전개와 인수분해의 (a+b)² 정사각형이 전부 이 그림 위에 선다. 한 변을 2배 하면 넓이가 4배가 되는 것은 직사각형 하나로만 확인하고 일반화는 8번 · 닮음과 비례, 호의 길이와 부채꼴 넓이를 각으로 적는 것은 25번 · 삼각비와 단위원, 곡선 아래를 잘게 쪼개 더하는 것은 33번 · 정적분의 것이다. | 2·4번 |
| 7 | `math-basics-pythagorean-theorem` | 피타고라스 정리: 정사각형 하나를 두 가지로 채워 증명하기 | 직각삼각형에서 빗변과 나머지 두 변에 이름을 붙이고 a²+b²=c²를 진술한 뒤, **이 트랙에서 결과만 받지 않고 끝까지 증명하는 몇 안 되는 정리로 다룬다.** 증명은 한 변이 a+b인 정사각형 안에 같은 직각삼각형 넷을 두 가지로 놓고 남는 넓이를 비교하는 그림 하나다 — 6번 · 넓이와 부피가 세운 정사각형과 삼각형의 넓이만 쓰므로 문자식을 전개할 필요가 없고, 실제로 15번 · 전개와 인수분해는 여기서 여덟 편 뒤라 쓸 수도 없다. 그림을 직접 다시 그려 넓이 등식을 복원하는 것이 첫 실습이다. 이어 역도 성립한다는 것(세 변이 그 관계를 만족하면 그 삼각형은 직각이다)을 결과로 적고 3·4·5와 5·12·13을 확인한다. 다음은 모눈 위에서 비스듬한 선분의 길이를 재는 연습이다 — 가로 차이와 세로 차이로 직각삼각형을 만들어 길이를 얻는 그림 하나이고, **24번 · 좌표평면과 직선이 이 그림을 좌표로 옮겨 두 점 사이 거리 공식으로 적는다. 33편 초안의 좌표평면 글이 '증명하지 않고 결과로 받아 쓴다'며 미뤄 둔 자리를 갚는 것이 이 편이다.** 마지막으로 한 변이 1인 정사각형의 대각선이 √2임을 얻어, 자연수와 그 비로는 적을 수 없는 길이가 도형에서 실제로 나온다는 것을 본다. **근호 기호를 트랙에서 처음 쓰는 자리이므로 '제곱해서 2가 되는 양수'라는 뜻과 '루트 2'라는 발음을 한 줄로 준다** — 10번 · 수의 종류와 수직선이 √2를 수직선에 앉힐 때 이 한 줄에 기댄다(근호가 든 식의 계산과 유리화는 13번 · 거듭제곱과 근호, 무리수라는 증명은 46번 · 증명 읽는 법의 것이다). 손으로 할 것은 세 변 중 하나를 구하는 열 문제, 증명 그림 복원, 대각선 길이 다섯이다. sin²+cos²=1이 이 정리의 다른 옷이라는 것은 25번 · 삼각비와 단위원이 맡는다. | 2·6번 |
| 8 | `math-basics-similarity-and-proportion` | 닮음과 비례: 크기가 달라도 변하지 않는 것 | 크기만 다르고 모양이 같은 두 도형을 닮음이라 부르고 그 배율을 닮음비 k로 적는 데서 시작한다(모양과 크기가 모두 같은 합동과 구분한다). 삼각형의 닮음 조건은 두 각이 같으면 닮음이라는 것 하나만 그림으로 세우고 나머지 둘은 이름만 적는다. **그다음이 이 편의 존재 이유다** — 닮은 삼각형에서 대응하는 변의 비가 같다는 것, 따라서 직각삼각형에서 한 예각의 크기만 정해지면 두 변의 비는 삼각형이 크든 작든 같은 값이 된다는 것. 크기만 다른 직각삼각형 셋을 **모눈 위에 정수배로** 그려(세 변이 3·4·5와 6·8·10과 9·12·15) 대응변이 전부 같은 배수만큼 늘어난 것을 손으로 확인시킨다. **비의 값을 0.75 같은 수 하나로 적지 않는다** — 소수도 분수도 아직 없고 11·12번이 넉 편 뒤다. 여기서 확인하는 것은 '세 삼각형 어디서나 가로 3칸에 세로 4칸이라는 같은 계단이 나온다'는 데까지이고, **그 계단을 하나의 수로 적는 것은 12번 · 소수·백분율과 비의 몫이다.** **25번 · 삼각비와 단위원이 sin·cos를 '삼각형이 아니라 각 하나에만 달린 값'으로 정의할 때 전제로 삼는 문장이 정확히 이것이고, 33편 초안의 삼각비 글은 이 사실을 세운 앞 글 없이 시작하고 있었다.** 이어 비를 a:b로 적는 표기와 '두 비가 같다'가 한쪽에 같은 수를 곱해 다른 쪽으로 옮겨 갈 수 있다는 뜻임을 세운다 — **트랙에서 a:b라는 표기를 처음 세우는 자리가 여기다**(분수 표기 a/b는 11번이 아직 안 나왔으므로 쓰지 않는다). 그리고 닮음비가 k이면 넓이비는 k², 부피비는 k³ — 6번이 직사각형 하나로만 확인한 것을 여기서 일반화한다. **평행선이 만드는 비례는 그림 한 장과 실습으로 지나간다** — 삼각형의 한 변에 평행한 직선이 나머지 두 변을 같은 비로 자른다는 것을 결과로 적고, 그림자 길이로 나무 높이를 재는 실측에서 그것을 쓰기만 한다(초급에서 이 정리를 다시 쓰는 자리가 없다). **새로 지은 여덟 편은 세우는 것을 다섯에 맞췄다**(문자 약속 한 문단을 받은 6번만 여섯이다) — 상한이 여섯인데 첫 배치에서 여섯을 채우면 집필 중 하나만 늘어도 넘어간다. 손으로 할 것은 닮음비로 모르는 변을 구하는 열 문제, 모눈 위 직각삼각형 셋의 계단 재기, 넓이비·부피비 다섯, 그림자 문제 셋이고 **닮음비 k와 답이 전부 자연수로 떨어지는 수만 고른다** — 4번 · 자연수의 사칙연산과 자릿수가 준 몫과 나머지 말고는 나눗셈을 적을 수단이 아직 없다. 비를 분수와 백분율로 옮겨 적는 것은 12번 · 소수·백분율과 비, 비례식을 내항곱=외항곱으로 고쳐 푸는 절차는 16번 · 일차방정식과 이항, y=kx로 적는 것은 14번 · 문자와 대입의 것이다. | 4·6번 |

### 1단계 · 수와 계산 (7편)

> 계산의 바닥을 다시 깐다. 여기서 세운 부호 규칙·분수·지수·괄호 조작을 이후 33편이
> 한 번도 다시 설명하지 않고 그대로 쓴다. **맨 앞의 9번은 기호 사전이다** — 33편
> 초안은 이것을 맨 뒤(30번)에 두었는데, 뒤에서 만날 때마다 돌아와 찾아보는 물건을
> 마지막에 두면 정작 필요할 때 없다. 성격을 '앞에서 쓴 기호를 모은다'에서 '앞으로
> 만날 기호를 미리 읽어 둔다'로 바꿔 앞으로 옮겼다.

| # | 슬러그 | 제목 | 무엇을 쓰는가 | 전제 |
|---:|---|---|---|---|
| 9 | `math-basics-symbol-glossary` | 수식 기호 읽기: 앞으로 만날 기호를 소리 내어 읽어 두기 | 앞으로 서른아홉 편에서 만날 기호를 미리 소리 내어 읽어 두는 사전이다. **1~8번에서 이미 쓴 셋(위첨자, √, a:b)은 예외이고 그것들은 쓰는 자리에서 읽는 법을 받았다** — 위첨자는 2번 · 식을 읽는 순서, √는 7번 · 피타고라스 정리, a:b는 8번 · 닮음과 비례다. 여기서는 표에 한 줄씩 다시 실을 뿐이고 나머지는 전부 앞으로 만날 것이다. 뜻을 가르치지 않는다 — 개념은 그 개념을 맡은 글에 있고 여기서는 낯선 글자 앞에서 멈추지 않게만 한다. 2번 · 식을 읽는 순서가 기호들이 놓인 '순서'를 맡았다면 이 글은 기호 하나하나의 '이름'을 맡는다. 여든 개 가까이 되므로 무게를 셋으로 나눈다. 첫째, 뒤에서 실제로 오독이 생기는 열둘만 세 줄('어떻게 읽는가 / 무엇을 뜻하는가 / 자주 하는 오독')로 적는다 — \|·\|(10번의 절댓값, 37번의 원소 개수, 44번의 행렬식이 같은 세로줄을 쓴다), Σ와 σ, ∈과 ⊂, ⇒와 ⟺, ≈와 ≡와 :=, ≪, argmax와 max, ∂와 d, ∫의 위아래 끝, ∀와 ∃의 순서, ε와 δ, n!과 C(n,k). 둘째, 나머지는 '읽는 법 + 한 줄 뜻'만 적은 표로 지나간다 — ℕ⊂ℤ⊂ℚ⊂ℝ⊂ℂ, 구간 [a,b]·(a,b), 집합·논리·연산 기호, 관계 기호(∝를 빠뜨리지 않는다 — 12번 · 소수·백분율과 비가 정비례에 붙인 표기다), max·min, 라틴 약어(i.i.d., w.r.t., s.t., iff, Q.E.D.), 그리고 그리스 문자 24자도 표 한 장(α·β 계수와 각, θ 각, λ 비례상수, μ 평균, σ 표준편차, π 원주율, Δ 변화량)이고 한 자씩 절을 두지 않는다. 셋째, 이 트랙에서 한 번도 쓰지 않는 것(∇ ⊗ ⟨·,·⟩ sup inf ⌊·⌋ ⊙)은 표 맨 뒤에 읽는 법만 모은다. 손으로 할 것은 뒤에 나올 식 다섯 줄을 뜻은 몰라도 소리 내어 끝까지 읽고 그 줄을 배우는 글의 번호를 찾아 적는 것이다 — 풀지 않는다. 사용법도 적는다: 한 번에 외우지 말고 뒤에서 기호를 만날 때마다 돌아온다. O(·)는 47번 · 자릿수와 오차, 증명 문서의 관용구는 46번 · 증명 읽는 법, 서체로 스칼라·벡터·행렬을 가르는 규약은 중급 2번의 것이다. | 1·2·3번 |
| 10 | `math-basics-numbers-and-number-line` | 수의 종류와 수직선: 음수끼리 곱하면 왜 양수가 되는가 | 4번 · 자연수의 사칙연산과 자릿수가 센 자연수에서 출발해 수를 종류별로 갈라 이름 붙인다. 빼기가 늘 되게 하려고 붙인 음수와 정수, 나누기가 늘 되게 하려고 붙인 유리수, 7번 · 피타고라스 정리가 대각선 길이로 만들어 낸 √2처럼 분수로 적을 수 없는 무리수까지 — 각 확장이 '앞 단계에서 안 되던 계산 하나'를 되게 하려고 생겼다는 줄거리로 엮고 수직선 위에 자리를 표시한다. 소수 표기는 4번의 위치 기수법을 소수점 아래로 이어 쓴 것이라고 한 줄로 지나간다(유한소수·순환소수의 구분과 분수↔소수 변환은 12번 · 소수·백분율과 비의 것이다). 본체는 음수가 섞인 사칙연산의 부호 규칙, 특히 (−3)×(−4)가 왜 +12인지다. 분배법칙 a(b+c)=ab+ac는 4번의 세로셈이 자릿수마다 쓴 규칙이고 6번 · 넓이와 부피의 '직사각형을 세로로 자른다'는 그림이 그것을 눈으로 보여 주므로 여기서는 이름을 붙여 한 줄로 되짚고, 3×(4+(−4))을 두 방향으로 계산해 (−3)×(−4)에 +12 말고 다른 값을 줄 수 없다는 것을 본다. 규칙 암기가 아니라 '앞에서 하던 계산이 계속 맞으려면 그럴 수밖에 없다'는 논법이고, 13번의 a⁰=1이 이 논법을 그대로 되풀이한다. 절댓값은 '0에서 떨어진 거리'로 정의하고 두 수 사이 거리가 \|a−b\|임을 수직선에서 읽는다. 대소 비교는 '수직선에서 오른쪽이 크다'로 통일해 −7 < −3에서 흔들리지 않게 한다. 실습은 부호가 섞인 계산 스무 문제와 −7/2·√2·2.7·−0.3을 수직선에 배치해 정렬하는 문제다. 분수의 사칙연산은 11번, 근호 계산은 13번, 같은 법칙을 문자로 확장하는 것은 15번 · 전개와 인수분해, √2가 무리수라는 증명은 46번 · 증명 읽는 법의 것이다. 허수는 26번 · 복소수와 오일러 공식까지 꺼내지 않는다. | 4·6·7·9번 |
| 11 | `math-basics-fractions` | 분수의 사칙연산: 나눗셈이 왜 역수의 곱인가 | 분수를 '나눗셈을 끝내지 않고 적어 둔 것'으로 정의하는 데서 시작한다 — 4번 · 자연수의 사칙연산과 자릿수가 세운 몫과 나머지를 여기서 이어받는다. 약분과 통분을 손에 익히되, 그 밑에 있는 최대공약수·최소공배수는 5번 · 약수와 배수·소인수분해가 소인수분해로 구하는 절차까지 이미 세웠으므로 다시 설명하지 않고 쓰기만 한다. 본체는 사칙연산 넷이다. 덧셈·뺄셈은 통분이 왜 필요한지를 같은 크기로 자른 조각 그림으로 보이고 분모끼리 더하는 흔한 오답을 그 그림에서 반증한다. 곱셈은 '전체의 얼마의 얼마'라는 두 번 자르기로 세운다. 나눗셈이 이 글의 착지점이다 — ½로 나눈다는 것은 ½가 몇 개 들어가는지 묻는 것이라는 그림으로 역수의 곱을 납득시켜, 뒤집어 곱한다는 규칙을 외우지 않게 한다. 이어 두 분수의 대소를 통분해 비교하고 10번 · 수의 종류와 수직선이 그린 수직선 위에 앉힌다. 실습은 괄호와 대분수가 섞인 분수 계산 스무 문제이고, 답을 끝까지 약분하는 것과 3번 · 손으로 푸는 습관이 준 검산을 매 문제에 붙인다. 분수를 소수·백분율로 옮겨 적는 변환표는 12번 · 소수·백분율과 비, 문자가 든 분수식의 통분과 번분수 정리는 18번 · 식 조작, 분수 지수 a^(1/2)는 13번 · 거듭제곱과 근호, 산술·기하·조화평균이 각각 무엇에 답하는지는 40번 · 기술통계의 것이다. | 3·4·5·10번 |
| 12 | `math-basics-decimals-percent-ratio` | 소수·백분율과 비: 같은 양을 적는 세 가지 옷 | 같은 양을 세 가지 옷으로 갈아입히는 글이다. 소수를 4번 · 자연수의 사칙연산과 자릿수의 위치 기수법을 소수점 아래로 이어 쓴 것으로 세우고, 분수를 실제로 나눠 보면 유한소수이거나 순환소수가 된다는 것을 나눗셈으로 직접 확인한다. 분수·소수·백분율을 서로 옮겨 적는 변환표를 직접 채우고, 순환소수를 분수로 되돌리는 절차도 여기서 손에 익힌다 — 다만 그 절차가 왜 맞는지는 29번 · 등비급수가 답한다. 이어 **8번 · 닮음과 비례가 도형에서 세운 비 a:b를 여기서 수의 언어로 받는다** — a:b라는 표기 자체를 다시 세우지 않고, 그 비를 분수·소수·백분율로 옮겨 적는 것과 두 줄짜리 표에서 한쪽이 2배가 될 때 다른 쪽도 2배가 되는 관계(정비례)와 반으로 줄어드는 관계(반비례)를 값으로 읽는 데까지다. **정비례에 붙는 표기 ∝도 여기서 준다** — '상수배를 버리고 관계만 적는다'는 뜻이고, =로 되돌리면 그 버린 상수가 비례상수라는 것까지가 표기의 전부다(그 상수에 문자를 붙여 y=kx로 적는 것은 14번 · 문자와 대입이다). y=kx·y=k/x처럼 문자로 적는 것은 문자가 무엇을 대신하는지 세운 뒤의 일이라 14번 · 문자와 대입의 몫이고, 비례식 a:b=c:d를 내항곱=외항곱으로 푸는 절차는 등식을 변형하는 16번 · 일차방정식과 이항, 두 곡선의 개형은 19번 · 함수란 무엇인가의 것이다. **백분율이 무슨 조작인지도 같은 자리에서 한 번 일반화한다** — 항목마다의 값을 전부 더한 뒤 그 합으로 각각을 나누면 합이 1인 묶음이 되고 100을 곱하면 그것이 구성비다. 아무 양수 묶음이든 이 나눗셈 하나로 전체를 1에 맞출 수 있다는 것, 그리고 전부를 같은 양수로 나누는 것이라 큰 쪽과 작은 쪽의 순서가 바뀌지 않는다는 것 — 그래서 **어느 항목이 가장 큰지는 그 합을 몰라도 답할 수 있다.** 이 트랙이 대체하는 옛 23편 초급에서 지우는 `math-basics-ratio-and-normalization`의 본론이 여기로 왔다 — 앞부분(비 a:b, 정비례·반비례)만 옮기고 본론을 흘리면 소유자가 사라진다. 이어 퍼센트에서 틀리기 쉬운 자리만 골라 계산으로 못 박는다 — 20% 오른 뒤 20% 내리면 원래보다 작다는 것, 퍼센트와 퍼센트포인트의 구분, '2배 증가'와 '200% 증가'가 가리키는 값이 다르다는 것, 증가율을 이어 붙일 때 더하지 않고 곱한다는 것. 실습은 변환표 채우기, 할인·이자·농도 문장제 다섯, 그리고 그 퍼센트 함정 넷을 각각 수로 확인하기다. 유효숫자와 상대오차, 로그 축을 읽는 법은 47번 · 자릿수와 오차의 것이다. | 4·8·10·11번 |
| 13 | `math-basics-powers-and-roots` | 거듭제곱과 근호: a⁰=1은 약속이 아니라 결론이다 | **위첨자를 '같은 것을 몇 번 곱했는가'로 읽는 약속은 2번 · 식을 읽는 순서가, √의 뜻과 발음은 7번 · 피타고라스 정리가 쓰는 자리에서 이미 줬다. 이 글이 세우는 것은 그 표기 위의 법칙과 계산이다** — 5·6·7·8·10번이 표기를 빌려 쓴 것은 그래서 앞당겨쓰기가 아니다. 거듭제곱의 정의를 한 줄로 되짚고 지수법칙 다섯을 하나씩 세운다. a⁰=1과 a⁻ⁿ=1/aⁿ은 외워야 할 약속이 아니라, a³/a³와 a³/a⁵를 두 방향으로 계산해 보면 앞의 법칙이 계속 성립하도록 그 값밖에 고를 수 없다는 것을 확인한다 — 10번 · 수의 종류와 수직선이 (−3)×(−4)에 쓴 논법 그대로이며 이 트랙에서 그 논법을 두 번째로 쓰는 자리다. 제곱근은 '제곱해서 그 수가 되는 음이 아닌 수'로 정의하고 √(x²)=\|x\|가 되는 이유를 x가 음수인 예로 짚는다(절댓값은 10번의 것을 그대로 쓴다). 7번 · 피타고라스 정리가 대각선 길이로 만들어 낸 √2가 여기서 계산 대상이 된다. 근호가 든 식의 계산을 손으로 굳힌다 — √50=5√2처럼 밖으로 빼기(5번 · 약수와 배수·소인수분해가 세운 소인수분해가 그대로 쓰인다), 분모의 유리화, 켤레를 곱해 √가 든 분모 없애기. 이어 n제곱근과 유리수 지수 a^(1/n)=ⁿ√a, a^(m/n)이 지수법칙에서 강제되는 정의임을 확인한다. 마지막은 크기 감각이다 — 2¹⁰≈10³이라는 어림, 10의 거듭제곱으로 큰 수와 작은 수를 적는 과학적 표기, 밑이 1보다 큰지 작은지에 따라 거듭제곱이 폭발하는지 0으로 사라지는지. 실습은 지수법칙으로 식을 정리하는 스무 문제와 유리화 열 문제다. y=aˣ의 그래프는 21번 · 지수함수, e와 로그는 22번 · 로그, 유효숫자·반올림·오차 전파는 47번 · 자릿수와 오차, 완전제곱 만들기는 18번 · 식 조작이 맡는다. | 5·7·10·11번 |
| 14 | `math-basics-variables-and-expressions` | 문자와 대입: 문자 하나가 수 전부를 대신할 때 | **문자로 이름을 붙이는 것 자체는 6번 · 넓이와 부피가 한 문단으로 약속했고 7·8번이 a²+b²=c²와 닮음비 k로 써 왔다** — 거기서 글자는 정해진 양 하나에 붙인 이름 하나뿐이었다. 이 글은 그 글자가 무엇을 대신하는지를 처음 가르는 자리다 — 값을 모르는 미지수, 값이 변하는 변수, 정해진 상수, 항에 붙은 계수. 곱셈 기호를 생략하는 관례 3a·ab와 분수선이 어디까지 묶는가는 2번 · 식을 읽는 순서가 읽는 법으로 이미 줬으므로 다시 세우지 않고 쓰기만 하며, 항과 차수, 단항식과 다항식, 동류항 정리로 바로 넘어간다. 그다음이 이 글의 손계산이다 — 식에 수를 대입해 값을 구하는 것. 3번 · 손으로 푸는 습관이 검산 요령으로 준 '숫자를 넣어 본다'가 여기서 정식 계산 도구가 되고, 이후 서른네 편이 이 조작 하나에 계속 기댄다. a=2·b=−3처럼 음수를 대입할 때 괄호를 반드시 치는 습관을 못 박고, 같은 식에 여러 값을 넣어 표를 채워 본다. 12번 · 소수·백분율과 비가 표의 값으로만 읽고 넘긴 정비례와 반비례를 여기서 y=kx·y=k/x로 처음 적어, 문자로 적으면 관계 하나가 식 한 줄이 된다는 것을 보인다. 마지막은 문장을 식으로 옮기는 훈련이다 — '어떤 수보다 3 큰 수', '정가의 20%를 깎은 값'처럼 12번에서 수로 계산했던 문장을 이번에는 문자로 적는다. 실습은 대입 계산 스무 문제와 문장을 식으로 옮기기 열 문제다. 괄호를 풀고 다시 묶는 조작과 곱셈 공식은 15번 · 전개와 인수분해, 방정식을 푸는 절차는 16번 · 일차방정식과 이항, f(x) 표기와 정의역은 19번 · 함수란 무엇인가의 것이다. | 2·3·6·8·11·12번 |
| 15 | `math-basics-expanding-and-factoring` | 전개와 인수분해: 괄호를 풀고 다시 묶기 | 이 트랙에서 가장 많이 쓰게 될 두 조작을 훈련한다. 10번 · 수의 종류와 수직선이 수에 대해 세운 분배법칙을 문자로 확장해 괄호를 푸는 것, 그리고 그 반대로 공통인수를 찾아 묶는 것. 둘을 같은 분량으로 다루는 것이 이 글의 설계다 — 전개는 절차라서 금방 익고 인수분해는 찾기라서 훈련이 필요한데, 뒤에서 실제로 막히는 쪽은 늘 후자다. 곱셈 공식 (a+b)², (a−b)², (a+b)(a−b)는 외우지 않고도 복원할 수 있게 6번 · 넓이와 부피의 정사각형 넓이 그림으로 유도한다. 그 역방향인 인수분해는 셋으로 나눈다 — 공통인수 빼기, 곱셈 공식 되돌리기, x²+(a+b)x+ab 꼴에서 곱이 ab이고 합이 a+b인 두 수 찾기. 마지막은 한 줄이다 — **1번 · 등호가 뜻하는 것이 이미 이름까지 갈라 둔 항등식과 방정식을 (a+b)²=a²+2ab+b²와 2x+1=7로 확인만 하고 다음 글에 넘긴다.** 이름을 여기서 다시 붙이지 않는다. 실습은 전개한 뒤 다시 인수분해해 원래 식으로 돌아오는지 확인하는 스무 문제이고, 14번 · 문자와 대입의 대입을 매 문제에 붙여 x=2에서 양변 값이 같은지 확인한다. 분수식·근호식의 조작과 완전제곱 만들기는 18번 · 식 조작, 근의 공식은 23번 · 이차함수와 포물선의 것이다. 삼차 이상의 인수정리·조립제법은 이 과정 어디에서도 쓰지 않으므로 다루지 않는다. | 1·6·10·14번 |

### 2단계 · 방정식과 부등식 (3편)

> 등호와 부등호가 놓인 식을 x=□ 꼴로 갈아입히는 절차, 그리고 유도의 생략된 줄을
> 복원하는 훈련. 16·17번이 도구를 주고 18번이 그것을 '읽는 기술'로 묶는다.
> 이 단계로 식 다루기가 끝나고 19번부터 대상이 식에서 함수로 바뀐다.

| # | 슬러그 | 제목 | 무엇을 쓰는가 | 전제 |
|---:|---|---|---|---|
| 16 | `math-basics-linear-equations` | 일차방정식과 이항: 등식의 성질 넷으로 x만 남기기 | 방정식의 해를 '등식을 참으로 만드는 값'으로 정의하고, 푼다는 것이 x=□ 꼴로 옷을 갈아입히는 과정임을 못 박는다. 도구는 등식의 성질 넷뿐이다 — 양변에 같은 수를 더하고, 빼고, 곱하고, 0이 아닌 수로 나눈다. 이항은 그중 앞의 둘을 줄여 쓴 것일 뿐이며, '넘기면 부호가 바뀐다'는 규칙이 실은 양변에서 같은 것을 빼는 조작이라는 것을 한 번 보이고 나면 더 외울 것이 없다. 3번 · 손으로 푸는 습관의 '한 줄에 한 조작' 규칙이 여기서 처음 값을 한다. 푸는 절차를 순서로 고정한다 — 괄호 풀기, 양변에 분모의 최소공배수를 곱해 분수 없애기, 동류항 정리, 미지수를 한쪽으로 이항, 계수로 나누기. 해가 없는 경우(0·x=5)와 무수히 많은 경우(0·x=0)를 각각 만들어 보고, 구한 값을 원래 식에 도로 넣어 검산하는 습관을 붙인다. 이어서 여러 문자가 든 등식을 특정 문자에 대해 푸는 공식 변형, 그리고 미지수가 둘인 연립일차방정식을 대입법과 가감법으로 소거해 푸는 데까지 간다. **비례식 a:b=c:d의 내항곱=외항곱은 한 줄로 지나간다** — 양변에 bd를 곱하는 것이라 방금 세운 등식의 성질 그대로이고 새로 세울 것이 없다(8번 · 닮음과 비례가 세우고 12번 · 소수·백분율과 비가 값으로 읽은 비가 여기서 식이 된다). 실습은 계수에 분수가 섞인 일차방정식 스무 문제, 연립 열 문제, 문장제 다섯이다. **연립을 43번 · 가우스 소거의 앞머리로 옮기는 안을 검토했으나 여기 남긴다** — 24번 · 좌표평면과 직선이 두 직선의 교점을 실제로 구해야 하고 그 글이 43번보다 열아홉 편 앞이다. 43번이 맡는 것은 미지수 셋부터이며 그 글의 첫 절은 여기서 익힌 것을 복습으로 되짚는다. 같은 소거를 행렬로 적어 체계화하는 것과 미지수가 셋 이상일 때는 43번 · 가우스 소거, 두 식을 두 직선으로 보고 교점을 읽는 그림은 24번 · 좌표평면과 직선, 이차방정식과 근의 공식은 23번 · 이차함수와 포물선, 부등식은 17번 · 부등식과 부호가 맡는다. | 3·11·12·14·15번 |
| 17 | `math-basics-inequalities-and-signs` | 부등식과 부호: 음수를 곱하면 왜 방향이 뒤집히는가 | 등호 자리에 부등호가 놓이면 무엇이 달라지는지를 다룬다. 부등호 넷의 읽기(9번 · 수식 기호 읽기가 이미 소리를 줬으므로 확인만 한다), 해가 값 하나가 아니라 구간이라는 것, 그 구간을 수직선에 칠하고 [a,b]·(a,b)·(−∞,b] 표기로 적기. 부등식의 성질은 등식과 같지만 딱 한 자리에서 갈린다 — 음수를 양변에 곱하거나 나누면 방향이 뒤집힌다. −2x>6의 답을 x<−3으로 적는 사고를 수직선 위에서 재현해 이유를 눈으로 보게 한다. 일차부등식과 연립부등식 — 두 부등식을 각각 푼 뒤 수직선에 나란히 칠해 겹치는 구간을 읽는다. **'두 해집합의 교집합'이라는 말은 쓰지 않는다** — 집합과 교집합이라는 표기는 37번 · 확률과 표본공간이 세우고 그 글은 여기서 스무 편 뒤다. 이어 절댓값 부등식 \|x−c\|<r을 'c에서 거리가 r보다 가깝다'로 읽어 푸는 법, 양변을 제곱해도 동치인 조건(양변이 모두 음이 아닐 때)과 그 조건을 어겼을 때 가짜 해가 생기는 예. **삼각부등식 \|a+b\|≤\|a\|+\|b\|는 결과만 적는다** — 부호로 경우를 나눠 확인하는 것은 46번 · 증명 읽는 법이 '경우 나누기'의 예제로 쓸 자리이고, 두 편이 같은 확인을 두 번 하지 않는다. **곱과 몫의 부호표도 이름과 표 한 장만 보이고 절차로 굳히지 않는다** — 초급에서 이차부등식을 푸는 자리는 23번 · 이차함수와 포물선 하나이고 그 글은 부호표가 아니라 그래프로 읽는다. 마지막은 부등식을 세우는 두 상투 수단이다. (실수)²≥0에서 출발하기, 그리고 거기서 나오는 산술-기하 평균 부등식 a+b≥2√(ab). 등호가 언제 성립하는지를 늘 함께 적는 습관을 여기서 들인다. 실습은 일차·연립부등식 스무 문제, 절댓값 부등식 다섯, 양변을 제곱해 가짜 해가 생기는 것을 직접 만들어 보는 셋, 그리고 a+1/a≥2를 (√a−1/√a)²≥0에서 세워 보는 유도다 — **항목을 둘 덜어 낸 자리를 전부 문제 수로 돌렸다.** 이차부등식의 해를 포물선 개형과 판별식으로 읽는 것은 23번 · 이차함수와 포물선이 맡고, 코시-슈바르츠·젠센 같은 부등식은 중급 6·29번의 것이다. | 10·13·15·16번 |
| 18 | `math-basics-algebra-manipulation` | 식 조작: 유도의 생략된 줄을 복원하는 기술 | 앞의 아홉 편에서 익힌 조작을 '유도를 따라가는 기술'로 묶는 글이다. 유도가 막히는 자리는 개념이 아니라 이 줄에서 저 줄로 어떻게 갔는가이고, 그 간격을 메우는 훈련이 본체다. 다루는 것은 문자가 든 식의 정리이고 **세우는 것은 넷뿐이다** — 유리식의 통분과 약분, 근호가 든 식에 켤레를 곱해 유리화하기, 완전제곱 만들기 ax²+bx+c → a(x+p)²+q, 반복되는 덩어리를 문자 하나로 치환해 식을 줄이기. 넷을 고른 기준은 뒤에서 실제로 다시 쓰이는가 하나다(약분과 유리화는 28번 · 극한의 부정형, 완전제곱은 23·24번, 치환은 34번 · 미적분의 기본정리와 적분 기법). **번분수 (1/a−1/b)/(a−b)와 부분분수 1/(x(x+1))=1/x−1/(x+1)은 예를 하나씩 보이고 지나간다** — 초급에서 다시 쓰는 자리가 없다. 그다음이 이 글만의 몫 둘이다 — 전개할 것인가 묶을 것인가를 고르는 감각, 그리고 몇 줄이 생략된 유도를 받아 빠진 줄을 채워 넣기. **각 줄에 무슨 조작을 했는지 이름을 붙이는 습관 자체는 3번 · 손으로 푸는 습관이 이미 들였으므로 여기서 다시 세우지 않고 남이 쓴 유도에 적용하기만 한다.** 실습이 정확히 그것이다 — (a+b)³을 전개하고 다시 묶어 돌아오기, a²+b²를 (a+b)²와 ab로 되적기, x²+6x+5를 완전제곱으로 고쳐 최솟값 읽기, 가운데 세 줄이 비어 있는 유도 다섯 개를 복원하기. 이항과 등식의 성질은 16번, 부등식의 방향과 양변 제곱·산술기하평균은 17번, 지수와 근호의 상호 변환은 13번, 분수 계산은 11번, 분배와 묶기·곱셈 공식은 15번이 이미 맡았으므로 여기서 다시 설명하지 않고 쓰기만 한다. 완전제곱을 근의 공식·판별식·꼭짓점으로 잇는 것은 23번 · 이차함수와 포물선, Σ가 든 식의 조작은 27번 · 시그마 표기와 수열의 것이다. | 11·13·15·16·17번 |

### 3단계 · 함수 (5편)

> 대응이라는 대상(19) → 그 대상에 가하는 조작 전부(20) → 그 조작을 그대로 받아 쓰는
> 세 함수족(21·22·23). **19번은 대상만, 20번은 조작만 맡는다** — 옮기고 늘이고 뒤집는
> 것도, y=x에 비춰 역함수를 얻는 것도 20번이다. 한 편이 정의와 조작을 함께 가지면
> 예제가 한 문제로 줄어든다. 20번이 세운 규칙을 21·22·23번이 다시 설명하지 않는 것이
> 이 단계의 분담 규칙이다.

| # | 슬러그 | 제목 | 무엇을 쓰는가 | 전제 |
|---:|---|---|---|---|
| 19 | `math-basics-functions-and-graphs` | 함수란 무엇인가: 입력 하나에 출력 하나, 그리고 그것을 그림으로 보기 | 함수를 '입력 하나에 출력 하나가 정해지는 대응'으로 정의하고 f(x) 표기를 소리 내어 읽는 데서 시작한다. 정의역·공역·치역을 구별하고, f(3) 같은 대입을 손으로 계산하며, f(x+1)과 f(x)+1이 왜 다른지를 대입으로 확인한다. 그다음 좌표평면을 함수의 그림 도구로 세운다 — 축과 원점, 순서쌍 (x, y), 표를 만들어 점을 찍고 잇기, 세로선 판정법으로 어떤 곡선이 함수가 아닌지 가리기. 분량의 절반은 손으로 그리는 데 쓴다. **뒤에서 계속 소재가 되는 다섯 곡선 y=x, y=x², y=1/x, y=√x, y=\|x\|를 표 다섯 칸씩 채워 개형까지 그린다** — 값이 없는 자리(1/x의 x=0, √x의 음수)와 꺾이는 자리를 표에서 직접 만나 정의역이 왜 함수의 일부인지 손으로 알게 하는 것이 목적이고, 20번 · 그래프 변형이 이 다섯을 '이미 그려 본 곡선'으로 전제한다. 증가와 감소, 조각별로 정의된 함수도 한 번씩 그린다. **역함수와 우함수·기함수는 20번이, 합성함수는 그것을 실제로 쓰는 유일한 자리인 32번 · 도함수 계산 규칙이 맡는다** — 대상의 정의와 그 위의 조작을 한 편에 함께 넣으면 예제가 한 문제로 줄어든다. 좌표평면은 여기서 점을 찍는 데까지만 쓰고, 두 점 사이 거리와 직선은 24번 · 좌표평면과 직선, 극한과 연속은 28번 · 극한, 두 변수의 곡면은 45번 · 두 변수 함수와 등고선의 것이다. | 10·13·14번 |
| 20 | `math-basics-graph-transformations` | 그래프 변형: 옮기고 늘이고 뒤집기 | 이미 그려 본 곡선 하나를 옮기고 늘이고 뒤집어 새 곡선을 얻는 규칙만 모은 글이다. y=f(x)+k와 y=f(x−a)를 다루면서, 왜 −a인데 오른쪽으로 가는지를 '같은 값이 나오려면 x가 얼마여야 하는가'라는 대입으로 설명한다 — 여기가 가장 많이 틀리는 자리다. 이어 y=cf(x)의 세로 확대, y=f(cx)의 가로 변화가 c배가 아니라 1/c배인 이유, y=−f(x)와 y=f(−x)의 두 뒤집기, 그리고 변형을 두 번 겹칠 때 순서가 결과를 바꾼다는 것을 같은 곡선으로 비교한다. 대칭도 함께 정리한다 — y축 대칭·원점 대칭·y=x 대칭이 식에서 각각 무엇을 바꾸는가. 우함수·기함수는 이 절에서 이름만 붙인다. **y=x 대칭 자리가 역함수의 자리다** — 19번 · 함수란 무엇인가에서 넘겨받아, 일대일일 때만 역함수가 생긴다는 것, 구하는 절차(y에 대해 푼 뒤 문자를 맞바꾼다), 그 그래프가 원래 그래프를 y=x에 비춘 것이라는 사실을 여기서 세운다. 22번 · 로그가 통째로 기대는 절이 이것이다. 점근선은 '가까이 가지만 닿지 않는 직선'으로 그림에서만 소개하고 극한을 쓴 정의는 28번 · 극한에 넘긴다. 소재는 19번이 표로 그려 본 다섯 곡선으로 한정한다. 손으로 할 것은 f(x)=2x−3의 역함수를 구해 두 그래프가 y=x에 대칭인지 확인하기, y=x²에서 출발해 y=−2(x−3)²+1을 단계마다 옮겨 그리기, y=1/(x−2)+3의 점근선 두 개 찾기, 옮겨 놓은 그래프의 그림만 보고 식을 되쓰기다. 지수·로그 곡선과 이차식의 표준형은 21·22·23번이 이 규칙을 그대로 받아 쓰므로 여기서 미리 그리지 않는다. | 14·16·19번 |
| 21 | `math-basics-exponential-function` | 지수함수: 한 칸 갈 때 몇 배가 되는가, 그리고 e | 13번 · 거듭제곱과 근호가 세운 지수법칙을 함수로 바꾸는 한 계단이다. y=2ˣ의 표를 만들어 그리고 밑이 1보다 클 때와 0과 1 사이일 때의 개형, 값이 언제나 양수라는 것, (0,1)을 지난다는 것, y=0이 점근선이라는 것을 확인한다. 지수를 유리수까지 넓힌 것은 13번이고, 그 사이를 이어 실수 전체에서 매끄러운 곡선으로 읽는 것은 그림으로 한 줄만 지나간다. 20번 · 그래프 변형을 그대로 적용해 y=2^(x−1)+3을 그린다. 다음은 자라는 속도다 — '한 칸 갈 때 몇 배가 되는가'로 지수함수를 읽고, 배가시간과 반감기를 표로 굴려 x²과 2ˣ의 값을 같은 표에서 비교한다. 마지막이 e다 — **(1+1/n)ⁿ에 n=10·100·1000·10000을 넣어 값이 2.718…에 붙어 더 움직이지 않는 것을 표로 보고 그 수에 e라는 이름을 붙인다. 트랙에서 e를 정의하는 자리는 여기 하나이고, 22번 · 로그는 이 정의를 받아 '밑이 e인 로그'라 부르기만 한다.** '지수곡선이 (0,1)에서 기울기 1이 되게 하는 밑'이라는 특징짓기는 쓰지 않는다 — 곡선 위 한 점의 기울기는 31번 · 도함수의 정의가 처음 세우는 개념이고 직선의 기울기조차 24번 · 좌표평면과 직선이 처음 정의한다. 그 특징짓기는 32번 · 도함수 계산 규칙이 (eˣ)′=eˣ를 다루며 뒤늦게 갚고, 28번 · 극한은 이 표를 lim 기호로 옮겨 적기만 한다. 손계산은 2ˣ와 (1/2)ˣ의 표 채우기, y=2^(x−1)+3 그리기, 반감기가 주어졌을 때 원래의 1/8이 되는 시각 구하기, e의 표 네 줄 채우기다. 2ˣ=10처럼 지수 자리가 미지수인 식은 22번의 몫이다. | 13·19·20번 |
| 22 | `math-basics-logarithms` | 로그: 되돌리는 계산에 이름 붙이기 | 21번 · 지수함수가 남긴 2ˣ=10에서 시작한다 — 지금까지의 도구로는 풀 수 없고, 그래서 그 답에 이름을 붙인다. log_a b는 'a를 몇 제곱해야 b가 되는가'에 붙인 이름일 뿐이고, 지수와 로그는 20번 · 그래프 변형이 세운 역함수 관계라서 y=log_a x의 그래프는 y=aˣ를 y=x에 비춘 것이다. 정의역이 양수뿐인 이유와 y축이 점근선인 이유도 그 그림에서 그대로 읽는다. 로그 법칙 셋(곱은 합으로, 몫은 차로, 거듭제곱은 상수배로)과 밑변환 공식을 전부 지수법칙에서 한 줄씩 유도하고, log(x+y)는 쪼갤 수 없다는 것을 반례로 못 박는다. 상용로그와 자연로그는 이름과 쓰임만 가른다 — **e를 여기서 다시 정의하지 않는다.** 21번이 (1+1/n)ⁿ 표로 이미 정의했으므로 '밑이 e인 로그를 자연로그라 부른다'는 한 줄이면 된다. 손으로 할 것은 log₂64와 log₂0.125, 밑변환으로 log₂1000을 소수 둘째 자리까지 구하기, 3ˣ=50과 log₃(x−1)=2 풀기(등식을 변형하는 절차는 16번 · 일차방정식과 이항의 것을 그대로 쓴다), 20번의 규칙으로 y=log₂(x−1)+1 그리기, 큰 수 둘의 곱을 로그를 취해 합으로 바꿔 계산한 뒤 되돌리기다. 로그 눈금 그래프와 자릿수 감각은 47번 · 자릿수와 오차, (ln x)′=1/x는 32번 · 도함수 계산 규칙, 같은 법칙을 계산 안정성 쪽에서 다시 쓰는 자리는 중급 18번이다. | 13·16·20·21번 |
| 23 | `math-basics-quadratic-and-parabola` | 이차함수와 포물선: 꼭짓점, 근, 그리고 판별식 | y=ax²의 개형에서 시작해 a의 부호와 크기가 그릇의 방향과 폭을 정하는 것을 본 뒤, 18번 · 식 조작의 완전제곱 만들기를 그대로 적용해 y=ax²+bx+c를 y=a(x−p)²+q로 고쳐 쓴다. 그러면 꼭짓점과 대칭축 x=−b/2a가 20번 · 그래프 변형의 평행이동 그 자체로 읽힌다. **꼭짓점을 읽는 것이 곧 최댓값·최솟값을 읽는 것이라 따로 세우지 않고**, 정의역이 구간으로 제한되면 답이 꼭짓점이 아니라 끝점이 되는 경우만 한 줄 덧붙인다. 다음은 x축과 만나는 자리다 — 이차방정식을 인수분해로 풀고 완전제곱으로 풀고, 같은 조작을 문자 계수로 한 번 더 해서 근의 공식을 유도한다. 판별식 D=b²−4ac의 부호가 교점 둘·하나·없음과 대응하는 것을 그림과 나란히 놓고, D<0일 때 무엇이 근을 대신하는지는 26번 · 복소수와 오일러에 넘긴다. 근과 계수의 관계 α+β=−b/a, αβ=c/a를 근의 공식에서 유도해 근을 구하지 않고 합과 곱을 말하고, 거꾸로 합과 곱만으로 이차식을 세운다. 마지막으로 이차부등식의 해를 부호 규칙이 아니라 '그래프가 x축 위에 있는 구간'으로 읽는다 — 17번 · 부등식과 부호가 세운 조작 위에 그림 한 장을 얹는 것이다. 같은 그림에서 한 걸음 더 나가 **'모든 x에서 ax²+bx+c ≥ 0'이 'a>0이고 D≤0'과 같은 말임을 읽는다** — 위로 열린 그릇이 x축을 뚫지 않으면 값이 음수가 될 자리가 없다. 손으로 할 것은 2x²−8x+5를 비롯한 표준형·꼭짓점·최솟값 다섯, 근의 공식으로 구한 두 근을 합과 곱으로 검산하는 다섯, x²−x−6>0을 비롯한 이차부등식 다섯, x²+2kx+9>0이 모든 x에서 성립하는 k의 범위다 — **항목 하나를 접은 자리를 문제 수로 돌렸다.** 삼차 이상의 다항식은 다루지 않는다. | 15·17·18·19·20번 |

### 4단계 · 좌표와 도형 (3편)

> 19번이 점을 찍는 데까지만 쓴 좌표평면을 도형의 무대로 바꾼다. 24번이 원을 세우고
> 25번이 그 위에서 각을 좌표로 적으며, 26번이 그 좌표에 곱셈을 얹는다. **0단계의
> 7·8번이 실제로 값을 하는 자리가 여기 둘이다** — 24번은 피타고라스를, 25번은 닮음을
> 앞에서 세운 그대로 받아 쓴다.

| # | 슬러그 | 제목 | 무엇을 쓰는가 | 전제 |
|---:|---|---|---|---|
| 24 | `math-basics-coordinate-plane-and-lines` | 좌표평면과 직선: 거리, 기울기, 그리고 원 | 19번 · 함수란 무엇인가가 점을 찍는 데까지만 쓴 좌표평면을 이번에는 도형의 무대로 쓴다. **7번 · 피타고라스 정리가 증명까지 세워 둔 것을 좌표 위에 얹어** 두 점 사이 거리 √((x₁−x₂)²+(y₁−y₂)²)를 만든다 — 직각삼각형의 두 변이 x좌표의 차와 y좌표의 차라는 것만 보면 공식이 그 자리에서 나온다(중점 공식은 그 옆에 한 줄로 적는다). **기울기 Δy/Δx를 트랙에서 처음 정의하는 자리가 여기다** — '가로로 한 칸 갈 때 세로로 얼마나 오르는가'로 세우고, 30번 · 평균변화율과 할선이 곡선으로 넘어갈 때 그대로 받아 쓴다. 세 점이 한 직선 위에 있는지 기울기로 판정하는 것은 한 줄이다. 직선을 적는 세 가지 방법 y=mx+b, 점과 기울기, ax+by+c=0을 서로 옮겨 적고 y축에 평행한 직선에는 기울기가 없다는 예외를 짚는다. 평행 조건과 수직 조건 m₁m₂=−1을 직각삼각형으로 유도하고, 점과 직선 사이 거리 \|ax₀+by₀+c\|/√(a²+b²)를 유도한 뒤 절댓값을 벗기면 그 부호가 '직선의 어느 쪽에 있는가'를 답한다는 것까지 본다. 여기서 계수쌍 (a, b)가 그 직선에 수직인 방향을 가리킨다는 것을 계산으로 확인하고 '법선'이라는 이름을 붙인다. 이어 거리 공식의 직접적인 결과로 원의 방정식 (x−p)²+(y−q)²=r²을 세우고, x²+y²+Ax+By+C=0을 완전제곱으로 되돌려 중심과 반지름을 읽는다. **공간으로 항을 하나 늘리는 절은 두지 않는다** — 세 변수 좌표와 평면의 방정식은 초급에서 쓰는 자리가 없다. 항을 n개까지 늘려 초평면과 점-초평면 거리 \|aᵀx+b\|/‖a‖로 가는 것은 노름을 세우는 중급 5번이 자기 것으로 적는다(그 brief에 그렇게 적어 두었다). 두 직선의 교점을 소거로 푸는 절차는 16번 · 일차방정식과 이항과 43번 · 가우스 소거, z=f(x,y)의 곡면과 등고선은 45번, 거리를 재는 방식을 여럿 두고 비교하는 노름은 중급 5번의 것이다. | 7·13·16·18·19번 |
| 25 | `math-basics-trigonometry-and-unit-circle` | 삼각비와 단위원: 각을 좌표로 적는 법 | 직각삼각형의 세 변의 비 sin·cos·tan에서 시작한다. **8번 · 닮음과 비례가 세운 '닮으면 대응변의 비가 같다'가 여기서 정의를 떠받친다** — 그 사실 덕분에 이 값이 삼각형의 크기가 아니라 각 하나에만 달렸고, 한 문단으로 그것을 확인하고 연다. 30°·45°·60°의 값은 정삼각형 반쪽과 정사각형 반쪽에서 7번 · 피타고라스 정리로 직접 구한다. 그다음 24번 · 좌표평면과 직선의 원 위로 옮겨 간다 — 반지름 1인 원에서 각 θ만큼 돈 점의 좌표가 (cos θ, sin θ)이고, 그 원의 방정식이 그대로 sin²θ+cos²θ=1이다. 이 정의로 90°를 넘는 각, 음의 각, 한 바퀴를 넘는 각까지 값이 정해지고 사분면마다 부호가 갈리는 표가 나온다. 라디안은 '호의 길이로 각을 재는 것'으로 도입해 도와의 환산표를 만들고, 6번 · 넓이와 부피가 세운 원의 넓이와 둘레에서 비례로 호의 길이 rθ와 부채꼴 넓이 ½r²θ를 얻는다. 그래프 y=sin x와 y=cos x를 단위원을 한 바퀴 돌려 가며 그리고, 주기와 우함수·기함수(20번 · 그래프 변형이 대칭에 붙인 이름을 여기서 확인만 한다), 평행이동으로 둘이 서로 옮겨 간다는 것을 확인한 뒤 20번을 그대로 적용해 y=A sin(ωx+φ)+k에서 진폭·주기 2π/ω·위상이동을 읽는다. y=tan x는 개형과 점근선 자리만 한 줄로 적고 성질은 다루지 않는다. 손으로 할 것은 sin 150°와 cos(−π/3), 반지름 3·중심각 2라디안인 호의 길이와 부채꼴 넓이, y=3 sin(2x−π/3)을 그리고 진폭과 주기 말하기다. **덧셈정리는 결과만 적어 두고 증명은 26번 · 복소수와 오일러가 맡는다.** 사인법칙·코사인법칙과 역삼각함수는 이 과정에서 쓰지 않으므로 다루지 않는다. | 6·7·8·13·20·24번 |
| 26 | `math-basics-complex-numbers-and-euler` | 복소수와 오일러 공식: 곱셈이 회전이 되는 자리 | 23번 · 이차함수와 포물선에서 판별식이 음수라 근이 없던 자리를 메우는 데서 출발한다. i²=−1로 허수단위를 정의하고 a+bi의 덧셈·뺄셈·곱셈, 켤레와 절댓값, 분모에 켤레를 곱해 실수로 만드는 나눗셈을 손으로 익힌다. 그다음 복소평면 — 실수축과 허수축, 덧셈이 평행이동이라는 그림. 여기서 25번 · 삼각비와 단위원의 좌표를 가져와 극형식 r(cos θ + i sin θ)를 세우고, 두 복소수를 곱해 전개하면 '크기는 곱해지고 각은 더해진다'가 나온다는 것을 확인한다 — **그 전개의 실수부와 허수부가 정확히 25번이 결과만 적어 둔 덧셈정리이며, 이 글의 착지점이 그것이다.** 이어 드무아브르 정리로 (1+i)⁸ 같은 거듭제곱을 각도 계산 한 줄로 끝내고, 1의 n제곱근이 단위원 위에 정n각형으로 놓인다는 것과 실계수 이차식의 두 복소근이 언제나 켤레쌍이라는 것을 확인한다. 마지막으로 e^(iθ)=cos θ+i sin θ를 소개한다 — **급수를 쓰지 않는다.** 곱하면 각이 더해지는 이 대상의 성질이 21번 · 지수함수가 쓰는 지수법칙과 모양이 같다는 관찰로 표기를 정당화하고 e^(iπ)+1=0을 읽는 데까지만 간다. 급수로 이 등식을 확인하는 일은 35번 · 테일러 첫걸음의 것이므로 그 글을 읽은 뒤 돌아오라고 적어 둔다. 손으로 할 것은 (3+2i)/(1−i)의 유리화, 1+i의 극형식과 그 여덟 제곱, 1의 세제곱근 셋 구하기다. | 13·21·23·25번 |

### 5단계 · 수열과 극한 (3편)

> 여기서 트랙의 엄밀함 수위를 정한다 — **엡실론-델타를 쓰지 않고, 수치 표로 먼저
> 보고 그다음 대수로 확정한다.** 28번이 이 규약을 세우고 미적분 일곱 편이 그대로
> 따른다.

| # | 슬러그 | 제목 | 무엇을 쓰는가 | 전제 |
|---:|---|---|---|---|
| 27 | `math-basics-sequences-and-sigma` | 시그마 표기와 수열: Σ를 펼쳤다 다시 접기 | 수열은 순서가 정해진 수의 나열이라는 정의에서 시작해 a_n 표기와 첨자 읽는 법, 일반항과 점화식의 차이를 세운다. 등차수열 a_n = a₁ + (n−1)d와 등비수열 a_n = a₁r^(n−1)을 각각 몇 항 손으로 펼쳐 규칙을 확인하고, 점화식 a_(n+1) = 2a_n + 1을 다섯 항 굴려 본다. 뒷부분이 본체다 — **Σ 표기를 'k가 1부터 n까지 a_k를 더하라'로 소리 내어 읽고**(기호를 발음하는 법은 9번 · 수식 기호 읽기가 이미 표로 주었으므로 여기서는 그것이 무슨 계산인지를 세운다) 접힌 식을 손으로 펼쳤다가 다시 접는 연습을 반복한다. 더미 첨자를 바꿔도 값이 같다는 것, 상수를 밖으로 빼는 것, 합을 항별로 쪼개는 것, 첨자를 옮겨 적는 것, 그리고 Σ1 = n·Σk = n(n+1)/2·Σk² 세 공식. 1부터 100까지를 앞뒤로 짝지어 더하는 방법으로 등차수열의 합을 직접 유도하고, Π 표기는 '더하기 대신 곱하는 Σ'로 한 문단만 둔다. 무한히 더하는 이야기는 전부 29번 · 등비급수의 것이고, '한없이 커지면 어디에 붙는가'는 28번 · 극한이 맡는다. **이중합과 첨자 교환 규칙은 초급에서 다루지 않는다**(중급 3번의 것이다) — 48번 · SymPy 검산이 같은 이중합을 두 축 순서로 더해 값이 같은지 확인하는 데까지만 건드린다. 수학적 귀납법은 46번 · 증명 읽는 법의 것이라 여기서는 공식을 세우기만 하고 증명하지 않는다. | 13·14·18번 |
| 28 | `math-basics-limits` | 극한: 한없이 다가간다는 말을 표로 확인하기 | 극한을 엡실론-델타 없이 다룬다 — '한없이 진행하면 결국 어떤 값에 붙는다'를 수치 표로 확인하는 것이 이 글이 쓰는 유일한 도구이고, **이 규약을 여기서 세워 미적분 일곱 편이 그대로 따른다.** 먼저 수열의 극한: 1/n에 n = 10·100·1000을 넣어 0에 붙는 것을 표로 보고, lim 기호를 읽는 법, 수렴·발산·진동을 예로 가른다. r^n이 \|r\|<1, r=1, r>1, r≤−1 네 경우로 갈리는 것도 표로 확인한다(29번 · 등비급수가 이 결과 하나를 그대로 쓴다). 다음은 함수의 극한 — x→a에서의 좌극한과 우극한, 둘이 달라 극한이 없는 계단 모양, 그리고 f(a)의 값과 극한이 다를 수 있다는 것. **극한의 사칙연산은 표 한 장에 결과만 적고 지나간다**(유도할 것이 없고 뒤에서 쓰기만 한다). 그 위에서 부정형을 푼다: (x²−1)/(x−1)은 약분으로, (√(x+1)−1)/x는 유리화로, ∞/∞는 최고차항으로 나누어(약분과 유리화는 18번 · 식 조작의 것을 그대로 쓴다). x→∞에서 로그·다항·지수의 성장 속도 서열을 큰 수 몇 개로 재고, 21번 · 지수함수가 e를 정의하며 만든 (1+1/n)ⁿ 표를 여기서 다시 꺼내 lim 기호로 옮겨 적는다 — **e를 다시 정의하지 않는다. 그 표가 이 글이 방금 세운 '수렴'의 한 예라는 것만 확인한다.** 마지막은 연속이다 — '끊지 않고 그린다'가 lim f(x) = f(a)라는 식과 같은 말임을 세우고 끊기는 세 모양을 그린다. 급수의 합은 29번, 도함수 정의식의 극한은 31번 · 도함수의 정의가 맡는다. 수렴 판정법 일반론과 샌드위치 정리는 다루지 않는다. | 18·19·20·21·27번 |
| 29 | `math-basics-geometric-series` | 등비급수: 무한히 더해도 값이 유한한 이유와 1/(1−r) | 급수는 수열의 부분합이 만드는 또 하나의 수열이라는 정의를 세우는 것이 첫 절이다. S_n = a₁ + … + a_n을 표로 적어 n이 커질 때 어디로 가는지 보고, '무한히 더했는데 값이 유한하다'가 왜 모순이 아닌지를 그 표 하나로 답한다. 본체는 등비급수다 — S_n에서 rS_n을 빼는 손계산으로 S_n = a(1−r^n)/(1−r)을 유도하고, 28번 · 극한이 세운 r^n의 거동을 그대로 넣어 \|r\|<1일 때만 S = a/(1−r)로 붙고 그 밖에서는 발산한다는 결론을 낸다. r = 1/2·1/3·0.9·−1/2 넷을 손으로 계산해 부분합 표와 맞춰 본다. 이어서 0.999… = 1을 이 공식으로 확인하고, 0.4747…처럼 순환마디가 있는 소수를 분수로 되돌린다 — **12번 · 소수·백분율과 비에서 절차로만 익혔던 변환의 정체가 등비급수다.** 마지막으로 조화급수 1 + 1/2 + 1/3 + …을 2의 거듭제곱 단위로 묶어 발산을 보이고, 항이 0으로 가도 합이 유한하지 않을 수 있다는 반례로 못 박는다. 비판정법·적분판정법 같은 수렴 판정 일반론은 다루지 않는다 — 초급이 쓰는 급수는 등비급수 하나다. 멱급수와 테일러 전개는 35번 · 테일러 첫걸음의 것이다. | 12·27·28번 |

### 6단계 · 미적분 (7편)

> 표로 재기(30) → 정의(31) → 계산 규칙(32) → 넓이 더하기(33) → 되돌리기와 기법(34)
> → 근사(35) → 미지수가 함수인 방정식(36). 일곱 편 모두 28번의 규약을 따른다 —
> 수치 표로 먼저 보고 대수로 확정한다. **30/31과 33/34는 33편 초안이 한 편이던 것을
> 갈랐다** — 갈라지면서 30번이 수치 표를, 33번이 리만합만으로 정적분 하나를 끝까지
> 구하는 경험을, 34번이 부분적분 예제 셋을 되찾았다.

| # | 슬러그 | 제목 | 무엇을 쓰는가 | 전제 |
|---:|---|---|---|---|
| 30 | `math-basics-average-rate-of-change` | 평균변화율과 할선: 두 점을 잇는 직선의 기울기를 수로 재기 | 평균변화율에서 시작한다. 두 점 (a, f(a))와 (b, f(b))를 잇는 직선을 할선이라 부르고, 그 기울기 [f(b)−f(a)]/(b−a)가 24번 · 좌표평면과 직선에서 익힌 Δy/Δx 그대로임을 확인한다. 시간-거리 표에서 구간마다 평균속도를 재어 같은 함수라도 어느 구간을 잡느냐에 따라 값이 달라진다는 것을 손으로 겪는 것이 첫 절이다. 그다음이 본체다 — 구간을 좁히면 그 값이 어디로 가는가. f(x)=x²에서 a=1로 고정하고 h를 0.1 → 0.01 → 0.001로 줄이며 [f(a+h)−f(a)]/h를 표로 채워 2에 붙는 것을 보고, 같은 식을 전개해 h를 약분하면 정확히 2a+h임을 대수로 확정한다. 표에 적힌 값이 2+h였다는 것을 되짚어 수치와 유도가 같은 답을 준다는 경험을 남기는 것이 목적이다. h를 음수로 잡아 왼쪽에서 좁혀도 같은 값에 붙는 것을 표 하나로 더 확인한다. 손으로 할 것은 셋이다 — x²·x³·1/x에서 점을 하나씩 잡고 h 표를 채워 값이 붙는 곳을 눈으로 찾기. **접선은 '할선을 계속 좁혀 얻는 직선'이라고 그림에서만 예고하고 정의하지 않는다** — lim 기호를 붙여 f′(a)로 적는 것은 31번 · 도함수의 정의의 것이다. 28번 · 극한이 세운 규약(수치 표로 먼저 보고 대수로 확정한다)을 여기서 계산에 처음 쓰며, 극한 자체는 다시 설명하지 않는다. | 19·24·28번 |
| 31 | `math-basics-what-is-a-derivative` | 도함수의 정의: 할선이 접선이 되는 자리에 이름 붙이기 | 30번 · 평균변화율과 할선이 표로 찾아낸 값에 이름과 기호를 붙인다. h → 0에서 [f(a+h)−f(a)]/h가 붙는 값을 f′(a)로 적고, 이것이 28번 · 극한의 lim 기호를 그대로 쓴 정의라는 것을 한 줄로 못 박는다. 그 값이 할선을 좁혀 가며 다가가는 직선의 기울기이므로 접선의 기울기라 부르고, 접선의 방정식을 점-기울기꼴로 실제로 써 본다. 그다음 관점을 바꾼다 — a를 고정된 수가 아니라 변수로 두면 f′는 '점마다 기울기를 돌려주는 새 함수'가 되고 그것이 도함수다. 표기 f′(x)·dy/dx·df/dx 셋을 나란히 놓고 각각을 소리 내어 읽는다. 정의식만으로 x²·x³·1/x·√x 넷의 도함수를 손으로 유도한다 — 30번이 수치로 값을 찾아 둔 셋을 여기서 문자 하나로 한 번에 끝내는 것이고, 18번 · 식 조작의 약분과 유리화가 그대로 쓰인다. 마지막 두 절은 부호와 예외다 — f′의 부호가 증가·감소를 말한다는 것, f′=0이지만 극값이 아닌 x³, 그리고 미분이 안 되는 세 모양(\|x\|의 꺾인 점, 수직 접선, 끊긴 점)과 '미분가능하면 연속이지만 역은 아니다'. 곱·몫·연쇄법칙과 기본 도함수 표는 전부 32번 · 도함수 계산 규칙의 것이고 이계도함수와 볼록·오목도 거기서 다룬다. 두 변수의 편미분은 45번 · 두 변수 함수와 등고선의 것이다. | 18·20·23·28·30번 |
| 32 | `math-basics-derivative-rules` | 도함수 계산 규칙: 곱·몫·연쇄법칙을 손으로 스무 문제 | 계산 규칙만 모아 손에 익히는 글이다. 기본 도함수 표(상수·xⁿ·eˣ·aˣ·ln x·log_a x·sin x·cos x)를 세우되 (eˣ)′=eˣ만은 (e^h−1)/h가 h→0에서 1에 붙는 것을 수치로 확인해 '밑을 e로 잡으면 계수가 1이 되어 계산이 가장 깨끗해진다'를 답으로 남긴다(밑 e를 (1+1/n)ⁿ의 표로 정의하는 일은 21번 · 지수함수의 것이고 여기서는 그 정의가 미분에서 어떻게 갚아지는지만 본다). 상수배·합·차 규칙은 한 줄로 적고, 곱의 규칙 (fg)′=f′g+fg′를 직사각형 넓이가 늘어나는 그림으로 유도한 뒤 몫의 규칙. 연쇄법칙 앞에서 **합성함수 g∘f를 이 자리에서 세운다** — f를 먹인 결과를 다시 g에 먹인다는 정의, 순서를 바꾸면 값이 달라진다는 것을 f(x)=x²과 g(x)=x+1로 확인하기, 겉과 속을 갈라 부르는 법까지 한 절이다. 19번 · 함수란 무엇인가가 합성을 여기로 넘겼다 — 초급에서 합성을 쓰는 자리가 연쇄법칙 하나뿐이라 정의를 쓰는 자리에 붙였다. 그 위에서 연쇄법칙을 '겉을 미분하고 속을 미분해 곱한다'는 단계로 쪼갠 절차로 만들고 세 겹 합성까지 올린다. 고계도함수 표기를 세우고 f″의 부호가 휘는 방향을 말한다는 것과 변곡점을 그래프 위에서 확인하는 것을 한 항목으로 묶는다(35번 · 테일러 첫걸음이 f″를 그대로 받아 쓴다). **음함수 미분은 예 하나만 보이고 지나간다** — 초급에서 다시 쓰는 자리가 없고, 합성함수 정의를 한 절 받은 만큼 다른 데서 덜어야 실습 스무 문제가 남는다. 실습이 본체다 — (2x+1)⁵, ln(1+x²), x²e^(−x²), sin(x²), x/(1+x) 같은 혼합 문제 스무 개를 손으로 풀고, 틀렸을 때 의심할 순서(부호, 속미분 누락, 음의 지수 처리)를 목록으로 준다. 답을 기계로 맞춰 보는 절차는 48번 · SymPy 검산의 것이라 여기서는 그런 방법이 있다고만 적는다. 정의식과 접선·미분 가능성은 31번이 끝냈으므로 반복하지 않고, 적분은 33·34번, 편미분은 45번이 맡는다. | 19·21·22·25·31번 |
| 33 | `math-basics-integral-as-area` | 정적분: 넓이를 잘게 쪼개 더하기 | 적분을 '가늘게 쪼개 더한 넓이'로 세운다. 곡선 아래를 폭이 같은 직사각형으로 덮는 리만합에서 출발해 ∫₀¹x²dx를 n=4와 n=10으로 직접 계산하고, 그다음 27번 · 시그마 표기와 수열의 Σk²=n(n+1)(2n+1)/6을 써서 일반 n의 리만합을 (1/3)(1+1/n)(1+1/2n) 꼴로 접는다. 28번 · 극한을 그대로 적용하면 n이 커질 때 값이 정확히 1/3이다 — **기본정리 없이 정적분 하나를 끝까지 손으로 구해 보는 경험이 이 글의 핵심이고, 그래서 여기서 계산하는 적분은 전부 리만합이거나 삼각형·사다리꼴 넓이다.** ∫가 늘여 쓴 S이고 dx가 '가늘게 쪼갠 폭'이라는 것, 적분 구간과 피적분함수를 소리 내어 읽는 법도 여기서 익힌다. 이어 규칙 넷을 세운다 — 축 아래 넓이가 음수라는 부호 규약, 선형성, 구간을 쪼개 더하기, 위아래 끝을 바꾸면 부호가 뒤집히는 것. 손계산은 리만합 표 셋(x²·√x·1/x를 n=4와 10으로)과 넓이 도형으로 값을 즉답하는 다섯 문제다. **미분과의 관계는 여기서 한 줄도 적지 않는다** — 누적 넓이 함수와 미적분의 기본정리, 부정적분과 +C, 적분 기법은 전부 34번 · 미적분의 기본정리와 적분 기법의 것이다. 도형의 넓이는 6번 · 넓이와 부피, 급수의 합은 29번 · 등비급수가 맡고, 이상적분은 다루지 않는다. **변수가 둘인 적분을 한 축씩 차례로 읽는 법은 45번 · 두 변수 함수와 등고선의 것이다.** | 6·27·28번 |
| 34 | `math-basics-fundamental-theorem-of-calculus` | 미적분의 기본정리와 적분 기법: 미분을 거꾸로 돌리면 넓이가 나온다 | 33번 · 정적분이 리만합으로만 구하던 것을 한 줄에 끝내는 도구를 만든다. 누적 넓이 함수 A(x)=∫ₐˣf를 폭 h의 얇은 띠로 근사해 A′(x)=f(x)를 그림으로 유도하고, 미분과 적분이 서로를 되돌리는 연산이라는 미적분의 기본정리를 얻는다(평균값 정리는 꺼내지 않는다). 33번이 리만합으로 얻은 ∫₀¹x²dx=1/3을 이 정리로 한 줄에 다시 얻어 두 방법이 같은 답을 준다는 것을 확인하는 것이 첫 손계산이다. 이어 부정적분과 적분상수 +C가 왜 붙는지, 그리고 기본 적분 표(xⁿ·1/x·eˣ·sin·cos)를 32번 · 도함수 계산 규칙의 표를 거꾸로 읽어 세운다. 그다음이 기법 둘이다 — 치환적분은 연쇄법칙을 거꾸로 돌린 것임을 u=x²로 ∫2xe^(x²)dx에서 보이고, 부분적분은 곱의 규칙을 거꾸로 돌린 것임을 유도한 뒤 ∫xeˣdx·∫x sin x dx·∫ln x dx 셋을 손으로 푼다. **무엇을 u로 잡을지 고르는 감각까지가 이 글의 몫이다** — 한 편이 정의와 기법을 함께 가지면 부분적분이 식 한 줄로 던져지는데, 갈라져 나오면서 되찾은 자리가 정확히 그것이다. 손계산은 정적분 여덟 문제, 치환 셋, 부분적분 셋이다. 부분분수·삼각치환 같은 기법 일반론과 이상적분은 다루지 않으며, **이중적분을 한 축씩 읽는 법은 45번 · 두 변수 함수와 등고선이 단면 넓이를 더하는 절로 맡는다.** 초급에서 한 축짜리 적분을 실제로 쓰는 자리는 36번 · 미분방정식 첫걸음 하나다. 답을 기계로 맞추는 절차는 48번 · SymPy 검산의 것이다. | 32·33번 |
| 35 | `math-basics-taylor-first-steps` | 테일러 첫걸음: 함수를 다항식으로 흉내 내기 | 함수를 다항식으로 흉내 내는 법을 접선에서 시작한다. f(a+h) ≈ f(a) + f′(a)h가 31번 · 도함수의 정의에서 그린 접선 그 자체임을 확인하고, 2차 항을 붙이면 휘는 정도까지 맞출 수 있다는 것을 보인다. 계수가 왜 f″(a)/2인지는 '양변을 미분해 a에서 값을 맞춰 본다'는 방법 하나로 유도하고, 그대로 밀어 n차 계수가 f⁽ⁿ⁾(a)/n!이 되는 이유를 설명한다 — **n!이라는 표기를 트랙에서 처음 쓰는 자리이므로 계승의 정의를 그 자리에서 세운다**(세는 도구로서의 계승과 순열은 39번 · 세는 법의 것이다). 그다음 매클로린 전개로 eˣ, sin x, cos x, ln(1+x), 1/(1−x), (1+x)^α를 세우되 **표 한 장에 전개식만 적고, 차수를 1·2·3으로 올려 원래 곡선과 겹쳐 그리는 그림은 eˣ 하나에만 붙인다** — 여섯을 각각 세 차수로 그리면 그림이 열여덟 장이 되고 그만큼 손계산 넷이 밀려난다. 손계산은 넷이다 — e^0.1을 1·2·3차로 근사해 참값과 맞는 자릿수 세기, sin(0.1) ≈ 0.1의 오차, √1.02를 (1+x)^(1/2)의 1차 근사로 구하기, ln(1+x) ≈ x가 x를 키울수록 무너지는 것을 표로. **1/(1−x)의 전개가 29번 · 등비급수에서 유도한 식과 글자 하나까지 같고 그 급수가 \|x\|<1에서만 붙었다는 것을 나란히 놓아, 근사가 국소적이라는 것을 한 항목으로 못 박는다** — 대조와 국소성을 따로 세우지 않는다. 나머지항은 h^(n+1) 규모라는 사실만 쓰고 라그랑주 형태의 증명은 하지 않으며(그 규모를 O(·)로 적는 약속은 47번 · 자릿수와 오차가 세운다), 수렴 반경 판정과 여러 변수 테일러는 다루지 않는다. 마지막 한 절에서 eˣ·sin·cos 세 급수를 나란히 놓아 26번 · 복소수와 오일러가 결과로만 받아들였던 오일러 공식을 뒤늦게 확인한다. | 22·26·28·29·31·32번 |
| 36 | `math-basics-ode-first-steps` | 미분방정식 첫걸음: 변화율을 알 때 함수를 되찾기 | 미지수가 수가 아니라 함수인 방정식을 처음 만난다. 16번 · 일차방정식과 이항이 푸는 것은 'x가 얼마인가'인데 여기서는 '변화율이 이렇게 생긴 함수는 무엇인가'를 푼다는 대비로 연다. dy/dx=f(x)처럼 오른쪽에 x만 있는 경우는 그냥 적분이고, 그래서 해가 하나가 아니라 +C만큼 무리로 나오며 초기조건 하나가 그중 하나를 고른다 — 34번 · 미적분의 기본정리와 적분 기법에서 이유 없이 붙였던 적분상수의 정체가 여기서 밝혀진다. 방향장을 그려 해곡선을 눈으로 따라가는 절을 하나 두고, 변수분리형 dy/dx=g(x)h(y)를 양변으로 갈라 적분하는 절차를 손으로 굴린다. 실제로 푸는 것은 셋이다 — 지수 성장·감쇠 dy/dt=ky의 해 y₀e^(kt)와 반감기·배가시간 계산, 냉각 법칙 dy/dt=−k(y−A)가 A로 붙는 모양, 로지스틱 dy/dt=ky(1−y/M)의 S자 개형과 평형해 y=0·y=M. 마지막은 해석해가 없는 것이 보통이라는 사실이다 — 오일러 방법 y_(n+1)=y_n+h·f를 표에 다섯 줄 손으로 굴려 참값과 비교하고, h를 절반으로 줄이면 오차도 대략 절반이 되는 것을 확인한다(이 관찰에 O(h)라는 표기를 붙이는 것은 47번 · 자릿수와 오차의 것이다). 2계 방정식·연립 미분방정식·편미분방정식·라플라스 변환은 다루지 않고, 해의 존재와 유일성은 진술만 하며, 룽게-쿠타 같은 고차 수치해법은 이름만 남긴다. 적분 기법은 34번, 지수함수의 성질은 21번의 것이다. | 21·31·32·34번 |

### 7단계 · 확률과 통계 (5편)

> 37번이 집합과 표본공간을 세우고, 38번이 조건을 얹고, 39번이 손으로 다 적을 수 없는
> 것을 세는 도구를 주고, 40번이 자료를 요약하고, 41번이 앞의 넷으로 판정을 내리는
> 문장의 구조를 다룬다. **세는 법(39)을 확률 뒤에 둔 이유는 아래 「순서 때문에
> 트랙 안에서 닫은 의존」의 8번에 적었다.**

| # | 슬러그 | 제목 | 무엇을 쓰는가 | 전제 |
|---:|---|---|---|---|
| 37 | `math-basics-probability-and-sample-space` | 확률과 표본공간: 일어날 수 있는 것을 전부 적어 놓고 세기 | **첫 절이 집합을 세운다** — 원소와 ∈, 부분집합 ⊂, 합집합 ∪, 교집합 ∩, 여집합, 공집합 ∅, 그리고 원소 개수 \|A\|를 벤 다이어그램과 함께 정의한다. 9번 · 수식 기호 읽기가 이 기호들을 소리 내어 읽는 법을 이미 실었으므로 **읽는 법은 9번, 뜻과 연산은 여기다.** 17번 · 부등식과 부호가 겹치는 구간을 말로만 읽고 넘긴 것이 여기서 교집합이라는 이름을 얻고, 39번 · 세는 법의 포함배제가 \|A∪B\|를 셀 때 이 표기를 그대로 쓴다. **\|A\|는 10번 · 수의 종류와 수직선이 절댓값에 이미 쓴 기호이므로, 같은 세로줄이 대상에 따라 다른 뜻을 갖는다는 것을 그 자리에서 못 박는다**(44번 · 행렬식과 역행렬이 같은 세로줄을 한 번 더 다른 뜻으로 쓴다). 그 위에서 시행·표본공간 S·사건 A를 적고 확률을 두 관점으로 정의한다 — 동등가능일 때의 P(A)=\|A\|/\|S\|와 반복했을 때의 상대도수. 이어 0≤P(A)≤1, 여사건 P(Aᶜ)=1−P(A), 덧셈정리 P(A∪B)=P(A)+P(B)−P(A∩B)와 배반. **표본공간을 손으로 다 적을 수 있는 예제만 쓴다** — 주사위 두 개의 합을 6×6 표 서른여섯 칸에 전부 적어 P(합=7)과 P(합≥10)을 세고, 동전 세 번의 결과 여덟 가지를 나무그림으로 적는다. 그 6×6 표가 실은 곱의 법칙 그 자체이고 39번이 거기에 이름을 붙인다. 조건부확률과 기댓값은 38번, 다 못 적는 크기를 세는 도구는 39번의 것이다. | 11·12·17번 |
| 38 | `math-basics-conditional-probability-and-expectation` | 조건부확률과 기댓값: 조건이 붙으면 표본공간이 좁아진다 | 37번 · 확률과 표본공간이 만든 표를 다시 쓰면서 조건을 얹는다. P(A\|B)=P(A∩B)/P(B)를 공식으로 던지지 않고 '표본공간을 B로 좁혀 다시 센다'로 정의해, 36칸 표에서 B에 해당하는 칸만 남기고 실제로 세어 두 값이 같은지 확인한다. 여기 쓰인 세로줄은 37번이 못 박은 \|A\|의 세로줄과 달리 짝이 없는 하나이고 '주어졌을 때'로 읽는다는 것을 한 줄로 적는다. 곱셈정리는 이 식의 양변에 P(B)를 곱해 옮겨 적은 것이므로 한 줄로 지나간다. 독립을 P(A∩B)=P(A)P(B)로 정의하고 **배반과 독립이 다르다는 것을 반례로 못 박는다** — 처음 배우는 사람이 가장 자주 뭉개는 자리다. 이어 베이즈 정리를 조건부확률 두 개를 뒤집어 유도하고, 유병률 1%·민감도 99%·특이도 95%인 검사에서 양성일 때 실제로 병일 확률을 2×2 표로 계산해 직관과 어긋나는 답을 확인한다. 마지막은 이산확률변수와 확률분포표, 그리고 기댓값 E[X]=Σ x·p(x)다 — 27번 · 시그마 표기와 수열의 Σ가 여기서 처음 확률에 쓰이고, 동전 세 번의 앞면 개수로 분포표를 만들어 기댓값 1.5를 얻는다. **기댓값의 선형성은 결과만 적는다** — 초급에서 그것을 쓰는 자리는 40번 · 기술통계의 Var(X)=E[X²]−μ² 한 줄뿐이다. 연속확률변수의 밀도와 확률의 공리적 정의는 중급 19·20번의 것이다. | 27·37번 |
| 39 | `math-basics-counting-and-binomial` | 세는 법: 곱의 법칙에서 조합 C(n,k)와 이항정리까지 | 37·38번이 손으로 다 적을 수 있는 표본공간만 다뤘으므로, 다 적을 수 없는 것을 세는 도구를 여기서 만든다. 합의 법칙과 곱의 법칙(37번이 손으로 적은 6×6 표가 그것이다), 나무그림, 팩토리얼과 순열 P(n,k)(n! 표기 자체는 35번 · 테일러 첫걸음에서 이미 나왔고 여기서 세는 도구로 다시 쓴다), 중복순열 nʳ(한 줄), 조합 C(n,k)=n!/(k!(n−k)!)와 '순서를 세지 않는다'가 왜 k!로 나누는 것인지, 대칭성 C(n,k)=C(n,n−k), 파스칼 항등식과 삼각형, 이항정리 (a+b)ⁿ=Σ C(n,k)aⁿ⁻ᵏbᵏ, 그리고 포함배제 원리로 합집합의 크기를 세는 법(37번의 덧셈정리가 개수 판으로 다시 나온 것임을 확인한다). 실습은 파스칼 삼각형을 여덟 줄까지 손으로 채워 이항계수를 얻은 뒤 (1+x)⁵를 전개해 그 줄과 맞춰 보기, 로또 1등 확률 1/C(45,6)을 계산하기, 30명 중 생일이 겹칠 확률을 여사건으로 세워 답이 70%를 넘는 것을 확인하기, 그리고 반복 시행에서 성공 k번의 확률 C(n,k)pᵏ(1−p)ⁿ⁻ᵏ를 n=5로 직접 계산해 합이 1이 되는지 검산하기다. 그 식을 이항분포라는 이름으로 다루고 기댓값·분산·정규 근사로 잇는 것은 중급 20·22번이므로 여기서는 계산만 한다. 41번 · 가설검정의 논리가 p값을 손으로 더할 때 쓰는 것이 이 이항계수다. 팩토리얼이 금방 커지는 감각은 47번 · 자릿수와 오차가 맡고, 중복조합·비둘기집 원리·생성함수는 다루지 않는다. | 13·27·35·37·38번 |
| 40 | `math-basics-descriptive-statistics` | 기술통계: 평균·분산·분위수로 자료 뭉치를 요약하기 | 손에 든 숫자 뭉치를 요약하는 법이다. 산술평균·가중평균·기하평균·조화평균이 각각 어떤 질문에 답하는지(왕복 속도의 평균이 왜 조화평균이고 연간 성장 배수의 평균이 왜 기하평균인지 실제 수로 — 부등식 a+b≥2√(ab)는 17번 · 부등식과 부호가 이미 세웠다), 중앙값·최빈값과 평균이 갈리는 때, 꼬리가 긴 자료에서 평균이 대표성을 잃는 이유. 편차의 제곱합에서 분산과 표준편차를 세우고, 같은 식을 확률분포에 적용한 Var(X)=E[(X−μ)²]=E[X²]−μ²를 나란히 두어 '자료의 분산'과 '확률변수의 분산'이 같은 식을 다른 대상에 쓴 것임을 못 박는다(기댓값의 정의는 38번 · 조건부확률과 기댓값이 이미 세웠다). 이어 사분위수·백분위수·IQR·상자그림, 히스토그램의 구간 개수가 같은 자료를 다른 그림으로 만든다는 것, 이상치를 정하는 규칙들이 얼마나 임의적인지. 치우친 자료는 **'축을 바꿔 그리면 그림이 통째로 달라진다'는 관찰까지만** 둔다 — 로그 축을 읽고 그리는 법은 47번 · 자릿수와 오차의 것이다. 손으로 시키는 것은 열두 개짜리 자료 하나를 끝까지 요약하는 것이다 — 평균·중앙값·분산·표준편차·사분위수를 전부 손으로 구하고, 거기에 큰 값 하나를 더해 평균과 중앙값이 각각 얼마나 움직이는지 비교하고, 구간을 4개와 12개로 나눈 히스토그램 둘을 그려 결론이 달라지는 것을 본다. **분산을 손에 든 수로 계산하고 읽는 데까지가 이 글의 몫이고, 그것을 추정량으로 다루는 것은 중급 21번의 것이다** — 표본과 모집단의 구분, n−1로 나누는 이유, 표본평균이 흔들리는 폭은 전부 거기이며 여기서는 관례로만 언급한다. 산점도와 상관계수는 중급 23번, 유의성 판정은 41번이 맡는다. | 12·13·17·27·38번 |
| 41 | `math-basics-hypothesis-testing-logic` | 가설검정의 논리: 귀무가설·유의수준·p값이 실제로 말하는 것 | 앞의 네 편이 확률을 계산하고 자료를 요약했다면 이 글은 그 둘로 판정을 내리는 문장의 구조를 다룬다. 왜 '차이가 있다'가 아니라 '차이가 없다'를 가정하고 시작하는가, 귀무가설과 대립가설, 검정통계량, 유의수준 α와 기각역, p값의 정확한 정의('귀무가설이 참이라면 관측된 것만큼 극단적인 결과가 나올 확률')와 p값이 아닌 것 셋 — 가설이 참일 확률이 아니고, 효과의 크기가 아니고, 재현 확률이 아니다. 1종 오류와 2종 오류의 맞바꿈, 검정력, 통계적 유의성과 실질적 크기의 구분. **신뢰구간은 일반론으로 다루지 않는다** — 표본과 모집단의 구분과 표본평균이 흔들리는 폭은 40번 · 기술통계가 중급 21번으로 넘긴 재료라 초급 안에 없고, 재료 없이 결론만 진술하게 된다. 대신 아래 동전 예제 안에서 '기각되지 않는 p들의 범위'를 직접 구해, 검정과 구간이 같은 계산을 두 방향으로 읽은 것이라는 사실을 그 한 예제로만 보인다. 계산은 표를 찾지 않고 39번 · 세는 법의 이항계수로 직접 한다 — 공정한 동전을 20번 던져 앞면이 15번 나왔을 때 P(X≥15)=Σ C(20,k)/2²⁰를 손으로 더해 p값을 얻고 α=0.05와 비교한다. **이 절차에 이름(부호 검정)을 붙이지 않는다** — 검정 기법의 이름과 목록은 중급 76번의 것이고, 여기서는 p값의 정의가 실제로 무슨 계산인지 확인하는 데까지만 간다. **여러 번 재면 우연히 통과하는 것이 생긴다는 사실도 한 문장으로만 적고 계산하지 않는다** — 1−(1−α)^k와 다중 비교 보정은 중급 76번이 자기 것으로 적어 두었다. t검정·카이제곱·분산분석의 분포표를 읽는 절차, 순열 검정과 부트스트랩, 효과 크기는 다루지 않는다. | 37·38·39·40번 |

### 8단계 · 선형대수 준비 (4편)

> 42번이 **행렬이라는 표기를 트랙에서 처음 세우는 전담 편이다** — 새 대수 대상 하나를
> 정의하는 일만 하고, 그 위의 알고리즘은 43·44번이 굴린다. 33편 초안은 이 자리가
> 없어 소거 글과 행렬식 글이 표기를 나눠 세웠는데, 그러면 9,000자 안에서 행렬곱이
> '이렇게 곱한다'로 던져지고 그 자리가 정확히 막히는 자리다. 45번이 입력을 둘로
> 늘려 편미분까지 간다.

| # | 슬러그 | 제목 | 무엇을 쓰는가 | 전제 |
|---:|---|---|---|---|
| 42 | `math-basics-matrix-notation` | 행렬이라는 표기: 수를 직사각형으로 늘어놓으면 무엇이 편해지는가 | **행렬을 트랙에서 처음 세우는 글이고, 새 대수 대상 하나를 정의하는 일만 한다.** 수를 직사각형으로 늘어놓은 것이 행렬이고 m×n이라는 크기와 성분 a_ij의 첨자를 '행 먼저, 열 나중'으로 읽는다는 약속에서 시작한다. 덧셈과 스칼라배는 성분끼리 하는 것이라 한 줄로 지나간다. **세로로 적은 수의 묶음(열)** 은 24번 · 좌표평면과 직선이 만든 순서쌍 (x, y)를 세워 적은 것일 뿐이라는 데까지만 세우고, **'벡터'라는 수학 대상 자체(덧셈과 스칼라배에 닫힌 것, 선형결합, 화살표 관점)는 정의하지 않는다** — 그것은 중급 4번의 것이고, 42~44번이 쓰는 것은 '수를 세로로 적어 둔 것'이라는 표기와 '그 두 수를 좌표평면의 점으로 읽는다'는 관점뿐이다. 그 위에서 행렬과 열의 곱 Ax를 '각 행과 x의 곱의 합'으로 정의해 연립방정식을 계수행렬과 함께 Ax=b 한 줄로 적고, 이어 행렬끼리의 곱 AB를 같은 규칙의 확장으로 세운다 — 크기가 맞아야 곱해진다는 것, 결과의 크기, 그리고 AB≠BA의 반례. 단위행렬 I와 전치 Aᵀ, (AB)ᵀ=BᵀAᵀ까지가 이 글이 세우는 마지막이고, **대각선 성분을 더한 값에 대각합 tr A라는 이름을 붙이는 것만 한 줄 덧붙인다** — 표기를 전담하는 자리가 트랙에 여기뿐이라 이름을 둘 곳이 달리 없다(성질은 적지 않는다). **전치는 중급 7·14·15·36번이 계속 쓰는데 중급 2번이 서체 규약만 맡으므로 초급에서 한 번 정의해 두어야 하는 표기다.** 손계산 여덟 문제 — Ax 셋, 2×2와 2×3 곱 셋, 크기가 안 맞아 곱해지지 않는 예 하나, (AB)ᵀ와 BᵀAᵀ 대조 하나. 소거 절차는 43번, 행렬식과 역행렬은 44번, 행렬을 '공간을 옮기는 함수'로 읽는 관점은 중급 9번의 것이다. | 16·24·27번 |
| 43 | `math-basics-linear-systems-and-elimination` | 가우스 소거: 미지수 셋을 요령이 아니라 절차로 푸는 법 | 미지수 둘짜리 연립을 대입·가감으로 푸는 복습에서 출발해, 그 해가 24번 · 좌표평면과 직선에서 그린 두 직선의 교점이라는 것을 확인한다(손 소거 자체는 16번 · 일차방정식과 이항이 이미 맡았다). 미지수가 셋을 넘으면 요령이 아니라 절차가 필요하고, 그 절차를 적는 종이가 42번 · 행렬이라는 표기의 계수행렬 오른쪽에 b를 붙인 첨가행렬 [A\|b]다. 그 위에서 가우스 소거를 세운다 — 해를 바꾸지 않는 세 가지 행 연산(두 행 맞바꾸기, 한 행에 0이 아닌 수 곱하기, 한 행의 몇 배를 다른 행에 더하기), 피벗, 행 사다리꼴, 후진 대입, 그리고 해가 하나·무한히 많음·없음으로 갈리는 세 경우가 소거 마지막 줄에 0=0으로 나오는지 0=5로 나오는지로 판별된다는 것. **자유변수를 두어 무한한 해를 매개변수로 적는 법은 다루지 않는다** — 해가 무한하다는 판별까지만 하고, 그 해집합을 적는 언어는 중급 8번 · 생성·일차독립·기저의 것이다. 손으로 시키는 것은 3×3 연립 하나를 첨가행렬로 적어 사다리꼴까지 소거하고 후진 대입으로 답을 낸 뒤 원래 식에 대입해 검산하는 것, 그리고 해가 무한한 계와 해가 없는 계를 하나씩 같은 절차로 돌려 마지막 줄을 비교하는 것이다. 행렬식과 역행렬은 44번이 바로 이어받고, 일차독립·랭크·최소제곱은 중급 8·9·15번의 것이다. LU 분해와 조건수는 이름만 언급하고 넘어간다. | 16·18·24·42번 |
| 44 | `math-basics-determinant-and-inverse` | 행렬식과 역행렬: ad−bc는 무엇을 재는 수인가 | 43번 · 가우스 소거의 2×2 연립을 문자 계수로 끝까지 풀면 분모에 ad−bc가 나온다. 그 값이 0이 아닐 때만 해가 하나라는 관찰에서 행렬식을 정의하고, **두 열을 24번 · 좌표평면과 직선의 좌표로 읽었을 때** 그 두 점이 원점과 만드는 평행사변형의 부호 있는 넓이가 바로 그 값이라는 것을 좌표로 확인한다. 이어 3×3 행렬식은 **여인수 전개를 한 예로 펼쳐 보이는 데까지만 한다** — 절차로 굳혀 훈련하지 않는다(사루스 규칙은 이름도 적지 않는다. 3×3에서만 통하는 요령이라 4×4에서 그대로 틀리고, 여인수 전개는 크기와 무관하게 맞는다). det=0과 '해가 하나가 아니다'와 '열이 서로의 배수 조합이다'와 '넓이가 0으로 뭉개진다'가 전부 같은 말이라는 것(det(AB)=det(A)det(B)와 det(Aᵀ)=det(A)는 2×2로 한 번 확인하고 결과만 남긴다 — 행렬곱과 전치는 42번 · 행렬이라는 표기가 이미 세웠다). 그다음 역행렬 — AA⁻¹=A⁻¹A=I라는 정의, 2×2 역행렬 공식이 왜 1/(ad−bc)를 앞에 다는지, 가우스-조던으로 [A\|I]를 [I\|A⁻¹]까지 소거해 3×3 역행렬을 구하는 절차, 역행렬이 존재할 조건. (AB)⁻¹=B⁻¹A⁻¹와 (Aᵀ)⁻¹=(A⁻¹)ᵀ는 순서가 뒤집힌다는 것만 한 줄로 적는다. 그리고 Ax=b의 해를 x=A⁻¹b로 적을 수 있다는 것과 실제로는 43번의 소거가 더 빠르다는 것은 한 줄로 지나간다. **42번을 신설해 표기를 넘긴 것만으로는 이 편의 부담이 줄지 않아, 여인수 전개와 x=A⁻¹b 둘을 이렇게 접어 세우는 것을 다섯으로 맞췄다.** 손으로 시키는 것은 2×2 세 개의 행렬식과 역행렬을 구해 곱이 I가 되는지 검산하기, det=0인 행렬로 Ax=b를 소거해 무슨 일이 나는지 보기, 3×3 하나를 가우스-조던으로 뒤집기다. 유사역행렬·랭크·고윳값은 중급 12·15번, 계산이 자릿수를 잃는 문제는 47번 · 자릿수와 오차가 맡는다. | 24·42·43번 |
| 45 | `math-basics-surfaces-and-contours` | 두 변수 함수와 등고선: z=f(x,y)를 곡면·등고선·단면으로 보기 | 지금까지의 함수는 전부 입력 하나에 출력 하나였다. 여기서 입력을 둘로 늘린다. z=f(x,y)가 평면 위 각 점에 높이를 얹은 것이라는 정의에서 시작해 같은 함수를 보는 세 방법을 익힌다 — 3차원 곡면, 등고선(같은 높이를 잇는 선), 한 변수를 상수로 고정한 단면. 등산 지도에서 등고선이 촘촘한 곳이 가파른 곳이라는 사실을 그림으로 확인하고, **편미분 ∂f/∂x를 '다른 변수를 상수로 두고 자른 단면 위에서 하는 보통의 미분'으로 정의해** 32번 · 도함수 계산 규칙을 그대로 적용한다(31·32번이 편미분을 여기로 넘겼다). 손으로 그릴 대상은 넷이다 — 그릇 z=x²+y²(등고선이 동심원), 안장 z=x²−y²(한 방향은 오르고 다른 방향은 내려간다), 길쭉한 골짜기 z=x²+10y²(등고선이 늘어진 타원이 되고 그 늘어짐이 x·y 방향의 계수 비에서 온다), 봉우리 z=exp(−(x²+y²)/2). 각각에 대해 z=0,1,2,4의 등고선을 손으로 그리고, x=0과 y=0 단면을 1변수 그래프로 옮겨 그리고, 두 편도함수를 계산한다. **이어 변수가 둘인 적분을 읽는 법을 한 절 세운다** — z=f(x,y) 아래의 부피는 x를 하나 고정해 얻은 단면의 넓이를 다시 x축을 따라 더한 것이고, 그래서 ∬f dx dy는 33번 · 정적분이 세운 한 축짜리 적분을 차례로 두 번 한 것일 뿐이라는 것. 방금 그린 단면이 그대로 재료가 되므로 새로 세울 그림이 없다. 계산은 직사각형 구간 위의 간단한 것 둘까지만 하고 적분 순서를 바꿔도 같은 값이 나오는 것을 그 둘로 확인한다. **트랙에서 다중적분을 읽는 자리는 여기 하나이고**(옛 초급 적분 글이 갖고 있던 몫을 33·34번이 아니라 이 글이 받았다), 순서를 바꿔도 되는 조건과 변수변환은 다루지 않는다. 마지막으로 변수가 셋을 넘으면 그림이 불가능해지고 '지형'과 '등고선'이 말로만 남는다는 것을 못 박는다. **편미분의 정의와 계산은 이 글이 갖는다** — 두 편도함수를 하나로 묶은 그래디언트 벡터, 방향도함수, 등고선과 수직이라는 성질은 중급 32번의 것이다. 이차형식의 부호 판정은 중급 14번, 헤세 행렬과 다변수 테일러는 중급 45번의 것이다. 1변수 그래프의 평행이동·확대·반사는 20번, 포물선의 꼭짓점은 23번이 이미 맡았다. | 19·20·21·23·24·32·33·34번 |

### 9단계 · 읽기와 검산 (3편)

> 앞의 45편에서 쓴 것을 각각 다르게 확인한다 — **46번이 논증을 읽는 법, 47번이 수의
> 크기를 읽는 법, 48번이 기계로 맞춰 보는 법**이다. 새 개념을 거의 늘리지 않고 예제가
> 전부 앞 글을 되짚는다. 33편 초안에서 이 단계의 문을 열던 기호 사전은 9번으로
> 옮겨 갔다.

| # | 슬러그 | 제목 | 무엇을 쓰는가 | 전제 |
|---:|---|---|---|---|
| 46 | `math-basics-reading-proofs` | 증명 읽는 법: 정의·정리·따름정리와 '따라서'의 정체 | 증명을 쓰는 훈련이 아니라 읽는 훈련이다. 수학 문서의 구조부터 익힌다 — 정의·공리·보조정리·정리·따름정리·명제가 각각 약속하는 것과 증명 종료 기호. 그다음 한 문장에서 가정과 결론을 분리하는 법, '~라고 하자'가 하는 일, '~임을 보이면 충분하다'가 목표를 어떻게 바꾸는지, 조건문 P ⇒ Q와 역·대우·이의 구분, ⟺를 보이려면 양방향을 다 해야 한다는 것, 반례 하나로 끝나는 문장과 그렇지 않은 문장. 증명 뼈대는 여섯을 짧은 예로 보인다 — 직접 증명, 대우 증명, 귀류법, 경우 나누기, 수학적 귀납법, 구성적 증명. 관용구 넷('일반성을 잃지 않고', '충분히 큰 n에 대하여', '어떤 상수 C가 존재하여', '자명하게')은 뜻만 한 줄씩 적는다. 예제는 전부 앞에서 결과만 받아 쓴 것을 되짚는 것이다 — √2가 무리수임을 귀류법으로(10번 · 수의 종류와 수직선이 결과만 받아 쓴 것이고, 5번 · 약수와 배수·소인수분해의 유일성이 증명의 마지막 줄을 맡는다), 1+2+⋯+n=n(n+1)/2을 귀납법으로(27번), 등비급수의 합을 부분합의 극한으로(29번), \|a+b\| ≤ \|a\|+\|b\|를 경우 나누기로(17번 · 부등식과 부호가 부호로 확인만 하고 넘긴 것을 여기서 증명의 형식으로 다시 쓴다). 마지막은 '1=2'를 증명하는 가짜 증명에서 0으로 나눈 줄을 찾아내는 연습 하나다. 긴 증명을 끝에서 거꾸로 읽는 독법은 한 문단으로 적고 예제를 붙이지 않는다. 논리 기호의 발음은 9번 · 수식 기호 읽기가 이미 맡았고, 극한의 ε-δ 정의는 28번이 직관으로 정의하고 넘어간 대로 여기서도 다루지 않는다. 집합론과 술어논리 일반론도 다루지 않는다. | 5·9·10·17·27·29번 |
| 47 | `math-basics-orders-of-magnitude-and-error` | 자릿수와 오차: 10⁻⁸은 큰 수인가 | 숫자의 크기와 정확도를 감으로 말할 수 있게 만드는 글이다. 과학적 표기는 13번 · 거듭제곱과 근호가 10의 거듭제곱으로 세웠으므로 그 표기를 그대로 쓰고(**`1e−8`은 쓰지 않는다** — 수학 표기가 아니라 부동소수점 리터럴이고, 트랙에 그것을 세운 글도 없다) 유효숫자와 반올림에서 시작한다. 절대오차와 상대오차의 정의, 언제 어느 쪽을 봐야 하는지, 그리고 그 상대오차의 로그를 '맞는 자릿수'로 읽는 것까지가 한 항목이다. 다음이 전파의 1차 근사다 — 더할 때는 절대오차가, 곱할 때는 상대오차가 더해진다는 것, 그리고 그것이 가장 아프게 나타나는 자리인 자릿수 소거(크기가 비슷한 두 수를 빼면 유효숫자가 통째로 날아간다). **컴퓨터의 수가 자리가 유한해서 생기는 일 가운데 트랙 안에서 닫히는 것만 다룬다** — 1/3을 소수 다섯 자리에서 자르면 생기는 오차처럼 12번 · 소수·백분율과 비가 세운 순환소수를 잘라 쓰는 자리까지다. **0.1+0.2가 0.3이 아닌 이유와 머신 엡실론은 다루지 않는다** — 밑이 2일 때 0.1이 순환한다는 사실이 있어야 설명이 붙는데 진법을 세운 글이 48편 안에 없고(4번 · 자연수의 사칙연산과 자릿수는 10진 위치 기수법까지다), 비트 레이아웃과 함께 다루는 것이 중급 70번의 몫이다. 그다음 로그 축을 읽고 그리는 법 — 반로그 축은 지수적으로 자라는 것을, 로그-로그 축은 거듭제곱 관계를 직선으로 편다는 것과 직선의 기울기가 그 지수라는 것. 배수의 평균이 기하평균이라는 것도 40번 · 기술통계에서 받아 여기서 쓴다. 손으로 시키는 것은 10⁻⁸·10⁻³·10⁵가 서로 몇 배인지 즉답하기, √(x+1)−√x를 x=10⁸에서 **유효숫자 다섯 자리로 잘라 놓고** 그대로 계산한 값과 유리화한 값을 비교하기(자릿수 소거를 부동소수점 없이 손으로 재현하는 자리다), 등비수열을 반로그 축에 그려 직선이 되는 것 확인하기, 35번 · 테일러 첫걸음의 절단오차를 h를 반씩 줄이며 실제로 재어 차수를 눈으로 보기다. **그 마지막 실습 자리에서 점근 표기 O(·)를 세운다** — h를 반으로 줄일 때 오차도 반이 되면 O(h), 넷으로 나뉘면 O(h²)라고 적는다는 약속이고, 상수와 낮은 차수 항을 버리는 것이 언제 정당한지까지. Θ·Ω와의 차이는 한 줄로만 적는다. 35번이 나머지항을, 36번 · 미분방정식 첫걸음이 오일러 방법의 오차를 말로만 적고 지나갔으므로 **트랙에서 이 기호의 뜻을 세우는 자리는 여기 하나이고**, 9번 · 수식 기호 읽기에는 읽는 법만 한 줄 실린다. 퍼센트와 퍼센트포인트의 구분은 12번 · 소수·백분율과 비가 이미 맡았으므로 한 줄로 되짚기만 한다. 같은 표기를 연산량 세기에 쓰는 것은 중급 73번, **부동소수점 규격과 이진 표현·머신 엡실론·수치 안정성은 전부 중급 70·71번의 것이다**(중급 70번 brief에 초급 47번이 그것을 넘긴다는 것을 적어 두었다). | 12·13·21·22·35·40번 |
| 48 | `math-basics-symbolic-checking` | SymPy 검산: 손으로 푼 것을 기계로 채점하기 | 마지막 글은 앞의 47편을 검산하는 도구를 준다. 손으로 푼 것을 기계로 채점하는 절차이며 방법은 둘이다. 첫째 기호 계산 — SymPy의 symbols·diff·integrate·expand·factor·simplify·solve·limit·summation·series 최소 사용법, 손으로 얻은 답 a와 기계의 답 b를 simplify(a−b)==0으로 대조하기, 두 답이 다를 때 의심할 순서(부호, 연쇄법칙의 속미분 누락, 괄호, 정의역 조건). 둘째 수치 검산 — 기호로 안 풀리거나 결과가 복잡할 때 특수값(x=0, 1, −1)을 넣어 양쪽을 비교하기, 여러 점에서 평가해 그래프로 겹쳐 보기, 정적분을 리만합으로, 도함수를 중심차분 [f(x+h)−f(x−h)]/2h로, 확률을 난수 시뮬레이션으로 근사해 손계산과 맞추기(어느 자릿수까지 맞으면 맞다고 볼 것인가는 47번 · 자릿수와 오차의 기준을 쓴다). **3번 · 손으로 푸는 습관이 손으로 하던 검산 셋을 그대로 코드로 옮기는 것이 이 글이고, 새로 배우는 것은 도구 사용법뿐이다.** 그리고 Σ를 코드로 옮길 때의 습관 하나 — 같은 이중합을 두 축 순서로 더해 값이 같은지 확인한다. **첨자 교환 규칙 자체는 중급 3번의 것이라 여기서는 두 값이 같은지 보는 데까지만 한다.** 실습 대상은 전부 앞의 글이다. 32번 · 도함수 계산 규칙의 스무 문제를 diff로 채점하고, 35번의 테일러 계수를 series로 확인하고, 34번의 정적분을 integrate로 맞추고, 29번의 등비급수 합을 summation으로 얻고, 39번 · 세는 법의 로또 확률과 생일 문제를 난수 10만 번으로 재현하고, 38번의 검사 문제도 같은 방법으로 베이즈의 답과 맞춘다. 파이썬 문법과 설치·주피터 사용법은 다루지 않는다(기존 `python-for-ai`·`notebook-jupyter`가 있다) — 코드 열 줄을 읽으면 된다. 수치 알고리즘 일반론은 중급 70·71번이 맡는다. | 3·29·32·34·35·37·38·39·47번 (앞 47편 전부가 검산 대상이다) |

---

## 기존 306편과의 경계

`src/content/articles/`의 306편 중 순수 수학을 다루는 글은 없다. 수학 트랙에서 이미
나간 것은 중급 다섯 편(`math-attention-formula-anatomy` 1번,
`math-notation-conventions` 2번, `math-sigma-index-and-einsum` 3번,
`math-vector-as-meaning` 4번, `math-norms-and-distance` 5번)뿐이고,
**`math-basics-*.md`는 한 편도 없다.** 새로 지은 슬러그 열여덟 개 중 기존 파일과
부딪히는 것도 없다. 겹칠 수 있는 자리는 다섯이며 전부 위 brief에 경계를 적어 두었다.

| 이 트랙 | 이미 나간 글 | 경계 |
| --- | --- | --- |
| 9번 수식 기호 읽기 | 중급 2번 `math-notation-conventions` | 9번은 읽는 법(발음)만. 서체로 스칼라·벡터·행렬을 가르는 규약과 기호↔코드 축 대응은 중급 2번 |
| 27번 시그마 표기 · 48번 SymPy 검산 | 중급 3번 `math-sigma-index-and-einsum` | 초급은 한 겹 Σ를 펼쳤다 접는 것까지. 이중합의 첨자 교환과 einsum은 중급 3번 |
| 24번 좌표평면과 직선 | 중급 5번 `math-norms-and-distance` | 24번은 두 점 사이 거리 공식 하나. 거리를 재는 방식을 여럿 두고 비교하는 노름은 중급 5번 |
| 45번 두 변수 함수와 등고선 | 중급 14번 `math-spectral-theorem-and-quadratic-forms` | 45번이 그릇·안장 개형을 그리고, 중급 14번은 그 그림을 다시 그리지 않고 고윳값 부호로 판정만 한다(중급 14번 brief에 이미 그렇게 적혀 있다) |
| 48번 SymPy 검산 | `python-for-ai` · `notebook-jupyter` | 48번은 검산 절차만. 파이썬 문법·설치·주피터 사용법은 그 두 편 |

`ml-pca`·`ml-svm`·`ml-linear-regression`·`ml-classification-metrics` 계열은 라이브러리
사용법과 모델 평가 지표를 다루므로 40·42·43·44번과 겹치지 않는다. −1·0단계 여덟 편이
다루는 것(등호·연산 우선순위·세로셈·약수·넓이·피타고라스·닮음)은 306편 어디에도 없다.

---

## 순서 때문에 트랙 안에서 닫은 의존

**소유자 없는 도구를 만들지 않는다.** 뒤 글이 쓰는 것은 앞 글 어딘가가 반드시
'세운다'고 적혀 있어야 하고, 그 자리는 하나뿐이어야 한다. 아래 스물하나가 48편 순서
때문에 자리를 옮겨 잡았거나 한 자리로 몰아 둔 것들이다. **뒤의 다섯(17~21)은 첫
초안이 이 원칙을 스스로 어긴 자리를 찾아 메운 것이다** — 앞 여덟 편이 문자와 위첨자를
소유자보다 먼저 쓰고 있었고, 지운 세 편 가운데 하나의 본론이 어디로도 옮겨지지
않았으며, 옛 초급 적분 글의 다중적분이 통째로 사라져 있었다.

1. **분배법칙** — 4번이 곱셈 세로셈에서 절차로 쓰고, 6번이 자른 직사각형 넓이 그림으로
   보이고, **이름을 붙여 (−3)×(−4)=+12를 유도하는 것은 10번**이다. 15번이 그것을
   문자로 확장한다. 세 편이 같은 것을 세 번 세우지 않는다.
2. **피타고라스 정리** — **7번이 넓이 비교 그림으로 증명까지 갖는다.** 한 변이 a+b인
   정사각형을 두 가지로 채우는 방식을 고른 이유는 문자식 전개가 필요 없기 때문이다 —
   15번 · 전개와 인수분해가 여덟 편 뒤라 (a+b)²을 쓸 수 없고, 6번의 넓이만으로 닫힌다.
   24번이 좌표로 옮겨 거리 공식을, 25번이 30°·45°·60° 값을, 10·13번이 √2를 받는다.
3. **닮음과 비 표기 a:b** — **8번이 세운다.** 도형에서 비가 처음 나오는 자리가 8번이고
   12번보다 넉 편 앞이다. 8번은 분수 표기 a/b가 아직 없으므로 '한쪽에 같은 수를 곱해
   다른 쪽으로 옮긴다'로만 적고, 12번이 그 비를 분수·소수·백분율로 옮겨 적으며,
   16번이 비례식을 내항곱=외항곱으로 푼다. 25번이 sin·cos를 '각 하나에만 달린 값'으로
   정의할 때 전제로 삼는 문장도 8번의 것이다.
4. **소인수분해와 최대공약수·최소공배수** — **5번이 전부 갖는다.** 초안은 분수 글
   안에 두었는데, 11번은 약분·통분과 사칙연산 넷을 '왜 역수의 곱인가'까지 세우는
   것만으로 이미 상한에 가깝고, 소인수분해를 방금 세운 자리에서 지수의 작은 쪽·큰 쪽을
   골라 읽는 것이 절차로도 짧다. 11번·13번(√50=5√2)·46번(√2 무리수 증명의 마지막 줄)이
   받아 쓴다.
5. **기호 읽는 법** — **9번 하나.** 뜻과 개념은 그 개념을 맡은 글에 있다. 2번(놓인
   순서)과 겹치지 않고, 중급 2번(서체 규약)과도 갈린다.
6. **e** — **21번이 (1+1/n)ⁿ의 표로 한 번만 정의한다.** 곡선 위 한 점의 기울기는
   31번이, 직선의 기울기조차 24번이 처음 세우므로 21번에서 '기울기 1이 되는 밑'이라고
   적을 수 없다. 22번은 '밑이 e인 로그'라 부르기만 하고, 28번은 같은 표를 lim 기호로
   옮겨 적기만 하며, 32번이 (eˣ)′=eˣ를 다루며 그 특징짓기를 뒤늦게 갚는다.
7. **계승 n!** — 35번이 테일러 계수에서 먼저 쓰므로 정의를 그 자리에 둔다. 39번은
   세는 도구로 다시 쓰기만 한다.
8. **세는 법을 확률 뒤에 둔 이유(37 → 38 → 39)** — 초안의 순서를 검토한 끝에 그대로
   확정했다. 근거 셋이다. (가) **39번의 실습 넷 중 셋이 확률 문제다** — 로또 1등 확률,
   생일 문제를 여사건으로, 이항 확률의 합이 1인지 검산. 앞으로 옮기면 셋을 다 버려야
   하고 남는 것은 P(n,k)·C(n,k)·파스칼·이항정리를 동기 없이 늘어놓은 공식 연습이 된다.
   "경우의 수를 셀 줄 알아야 확률을 계산한다"는 지적은 맞지만 역도 참이다 — 초보자는
   확률을 알아야 경우의 수를 왜 세는지 안다. **예제가 걸린 쪽이 뒤로 간다.**
   (나) **37·38번은 세는 법을 한 번도 쓰지 않는다** — 주사위 36칸, 동전 8가지, 검사
   2×2 표가 전부 손으로 다 적힌다. 반대로 39번의 포함배제는 37번의 덧셈정리 없이는
   "왜 겹친 것을 빼는가"를 설명할 수 없다. (다) **쪼개면서 연결이 하나 더 생겼다** —
   37번이 손으로 적게 하는 6×6 표가 곱의 법칙 그 자체여서, 39번을 "네가 이미 한 것에
   이름을 붙인다"로 열 수 있다. 그리고 41번이 p값을 Σ C(20,k)/2²⁰로 직접 더하므로
   39번은 41번 앞이어야 한다. 결국 39번의 자리는 확률 둘과 가설검정 사이 하나뿐이다.
9. **집합 표기** — 17번이 겹치는 구간을, 39번이 포함배제를, 중급 19번이 확률 공리를
   같은 표기 위에 세운다. **뜻과 연산은 37번 첫 절이 필요한 만큼(∈ ⊂ ∪ ∩ 여집합 ∅
   \|A\|) 세우고, 읽는 법은 9번이 앞에서 이미 준다.** 17번은 그래서 '교집합'이라는
   말을 쓰지 않고 수직선 그림으로만 읽는다.
10. **세로줄 \|·\|의 네 가지 뜻** — 절댓값(10번), 원소 개수(37번), '주어졌을 때'(38번),
    행렬식(44번). 37번이 그 자리에서 갈래를 못 박고, 9번의 오독 목록 맨 앞에 실린다.
11. **행렬 표기** — **42번 전담이다.** 초안은 이 자리가 없어 소거 글과 행렬식 글이
    표기를 나눠 세웠고, 그 결과 초안 28번이 33편 중 최장(1,244자)이 되었다. 42번이
    Ax·AB·I·Aᵀ까지 전부 갖고, 43번은 첨가행렬 [A\|b]라는 작업 용지만, 44번은 행렬식과
    역행렬만 맡는다.
12. **열 표기와 벡터의 경계** — 42~44번이 '수를 세로로 적은 묶음'을 쓰는데 초급에
    벡터를 정의하는 글이 없다. 그래서 **표기와 '열을 24번의 좌표로 읽는다'는 관점까지만**
    쓰고, 벡터라는 대상(덧셈과 스칼라배에 닫힌 것, 선형결합)은 중급 4번에 남긴다고 각
    brief에 못 박았다.
13. **점근 표기 O(·)** — 35번이 나머지항을, 36번이 오일러 방법의 오차를 말로만 적고
    지나가므로 **47번이 h를 반씩 줄이며 차수를 재는 실습 자리에서 세운다.** 9번에는
    읽는 법 한 줄만 실린다. 연산량 세기에 쓰는 것은 중급 73번이다.
14. **삼각함수 덧셈정리** — 25번이 결과만 주고 26번의 극형식 곱셈 전개가 그 증명이
    된다. 초급에 벡터·행렬 글이 없으므로 중급 11번(회전행렬)에 넘기지 않고 트랙 안에서
    닫았다.
15. **합성함수와 역함수** — 합성은 32번(연쇄법칙 바로 앞), 역함수는 20번(y=x 대칭
    자리). 19번은 함수라는 대상만 맡는다. 대상의 정의와 그 위의 조작을 한 편에 함께
    넣으면 예제가 한 문제로 줄어든다.
16. **검산 습관** — 3번이 손 판본(숫자 넣어 보기·극단값·두 방법으로 계산)을 세우고,
    48번이 그것을 코드로 옮기기만 한다. 48번이 새로 가르치는 것은 도구 사용법뿐이다.
    자릿수 기준은 47번이 준다.
17. **문자로 이름 붙이기** — **6번이 세운다.** '글자 하나가 정해진 양을 대신한다'는
    약속을 한 문단으로 두고, 7번의 a²+b²=c²와 8번의 닮음비 k가 그 위에서 돈다.
    **14번은 문자를 처음 쓰는 글이 아니라 문자의 갈래(미지수·변수·상수·계수)를 처음
    가르고 대입을 정식 계산 도구로 세우는 글이다.** 0단계를 말과 그림으로만 적고
    문자를 14번 뒤로 미루는 안도 있었으나, 그러면 7번이 피타고라스 정리를 문장으로만
    진술하게 되어 증명 그림이 성립하지 않는다.
18. **위첨자와 근호 표기** — 읽는 법은 쓰는 자리에서 준다. 위첨자는 2번('같은 것을
    몇 번 곱했다'), √는 7번('제곱해서 그 수가 되는 양수'). **지수법칙·a⁰·음의 지수·
    근호가 든 식의 계산은 전부 13번이고**, 9번은 표에 한 줄씩 다시 실을 뿐이다.
    5번의 2³×3²×5와 8번의 k²·k³가 손계산 대상인데 13번이 여섯에서 여덟 편 뒤라
    이렇게 갈랐다. 같은 이유로 2번의 숫자 확인은 자연수만 쓰고, 음수·분수가 필요한
    쌍(−3²과 (−3)²)은 읽는 법만 적는다.
19. **∝와 합으로 나누기** — **12번이 세운다.** ∝는 정비례에 붙는 표기이고 =로
    되돌리면 비례상수가 나온다는 것, 양수 묶음을 합으로 나누면 전체가 1이 된다는 것
    (백분율이 그것의 한 예다), 전부를 같은 양수로 나누는 것이라 순서가 바뀌지 않아
    가장 큰 자리는 합을 몰라도 답한다는 것. 옛 23편 초급에서 지우는
    `math-basics-ratio-and-normalization`의 본론이 여기로 왔다 — 앞부분만 12번이
    받고 본론은 어느 편에도 없어 소유자가 사라져 있던 자리다. 9번의 관계 기호 표에
    ∝를 함께 싣는다.
20. **변수가 둘인 적분** — 33·34번은 한 축짜리 적분만 하고, **두 축을 차례로 읽는
    법은 45번이 단면 넓이를 더하는 절로 세운다.** 45번이 이미 단면을 그리므로 절
    하나면 닫히고, 45번의 전제에 33·34번을 더했다. 옛 초급 적분 글이 갖고 있던 몫을
    새 초안이 통째로 흘린 자리를 되돌린 것이다.
21. **대각합 tr A** — 42번이 행렬 표기를 전담하므로 이름 한 줄도 거기 둔다. 44번은
    행렬식과 역행렬만 맡는다.

---

# 고급 트랙 — 63편

중급 80편을 마친 뒤 읽는다. 이 안에서는 A1 → A14 순서를 지킨다 —
뒤 단원이 앞 단원을 이름으로 참조한다.

### A1 · 엄밀함의 도구 (5편) — 이후 전 단원의 공용 언어

| 슬러그 | 제목 | 무엇을 쓰는가 | 무엇이 열리는가 |
|---|---|---|---|
| `math-adv-metric-normed-and-fixed-points` | 거리공간·노름공간·축약사상: '수렴한다'를 정확히 말하기 | 거리공간과 열린집합, 수열 수렴과 코시 수열, 완비성, 콤팩트성과 최대·최소의 존재, 립시츠 연속. 그리고 축약사상 원리(바나흐 고정점 정리)를 증명까지 — 이 정리 하나가 A7의 Sinkhorn 수렴과 A14의 벨만 작용소 수렴에서 그대로 재사용된다. 후반부는 노름공간과 바나흐 공간: L^p 공간과 횔더·민코프스키 부등식, 유한차원에서는 모든 노름이 동치이지만 무한차원에서는 아니라는 것, 쌍대공간과 유계 선형범함수, 약수렴의 정의. 중급 5번의 노름 세 공리를 무한차원으로 올리고, 중급 46번의 L-매끄러움이 실은 그래디언트의 립시츠 조건이라는 것을 여기서 확정한다. 증명 수위: 축약사상 원리와 횔더 부등식은 완전 증명, 나머지는 진술과 사용법. | 논문 정리부의 'let (X,d) be a complete metric space', 'f is L-Lipschitz', 'the operator is a γ-contraction'을 사전 없이 읽게 된다. Sinkhorn(A7)·벨만(A14)·Robbins–Monro(A4)의 수렴 증명이 전부 이 한 정리로 닫힌다. 중급 5·17·46번. |
| `math-adv-hilbert-spaces-and-spectral-theorem` | 힐베르트 공간과 무한차원 스펙트럼 정리: 사영·리스 표현·고유함수 전개 | 내적공간과 완비성, 정규직교계와 파르스발 항등식, 사영 정리(닫힌 부분공간 위의 최적 근사)와 리스 표현 정리(모든 유계 선형범함수는 내적이다), 가분성. 그다음 유계 선형작용소와 작용소 노름, 수반작용소와 자기수반성, 컴팩트 작용소, 컴팩트 자기수반 작용소의 스펙트럼 정리와 고유함수 전개, 고유값이 0으로 수렴한다는 것, 힐베르트–슈미트·트레이스 클래스, SVD의 무한차원판. 중급 7번의 사영과 14·15번의 스펙트럼 정리·SVD를 무한차원으로 올린 판이며, 무엇이 그대로이고 무엇이 깨지는지 하나씩 대조한다. 리스 표현 정리는 A5의 RKHS 재생 성질 그 자체이고 사영 정리는 A1-5의 조건부 기댓값 정의이므로, 그 두 개념이 태어나는 자리는 이 글 하나뿐이다. 증명 수위: 사영 정리와 리스 표현 정리는 완전 증명, 스펙트럼 정리는 증명 개요 + 핵심 보조정리. | RKHS(A5)·NTK 스펙트럼(A5)·가우시안 과정(A5)·Mercer 전개(A5)·확산의 생성원(A7)·Neural Operator 계열 논문이 전부 이 언어로 쓰여 있다. 중급 7·14·15·17번. |
| `math-adv-lebesgue-integral-and-convergence` | 측도와 르베그 적분: ∇와 E를 언제 바꿔 써도 되는가 | σ-대수와 측도, 르베그 측도의 구성 개요와 비가측 집합의 존재(왜 '모든 부분집합'에 확률을 줄 수 없는가), 확률공간 (Ω,ℱ,P)와 가측함수로서의 확률변수, 단순함수에서 쌓아 올리는 르베그 적분과 리만 적분이 갈리는 지점. 단조수렴·파투·지배수렴 정리, 곱측도와 푸비니–토넬리(적분 순서 교환의 조건), 독립성의 측도론적 정의. 그것으로 중급 53번이 '적분과 미분의 교환'이라는 한 줄로 넘긴 조건을 정확히 진술하고 그 조건이 깨지는 실제 예(불연속 보상, 지지집합이 θ에 의존하는 분포)를 만든다. 후반부는 확률변수 수렴의 네 종류(개별점·확률·L^p·분포)와 함의 관계 및 반례, 특성함수와 레비 연속성, 린데베르그 CLT, 슬러츠키 정리와 델타 방법 — 중급 22·74번의 정규 근사가 언제 정당한지의 판정 기준이다. 증명 수위: 지배수렴과 미분-적분 교환은 완전 증명, CLT는 증명 개요. | 정책 그래디언트 정리의 가정부, score matching과 재매개변수화 논문 각주의 정규성 조건, 'almost surely'로 적힌 모든 수렴 진술, 그리고 추정량의 점근 분포를 유도하는 델타 방법을 직접 쓰게 된다. 중급 20·22·53·74번. |
| `math-adv-radon-nikodym-and-density-ratio` | 라돈–니코딤 도함수: KL·중요도 비율·측도 변환의 공통 정의 | 절대연속성 P≪Q와 라돈–니코딤 정리, dP/dQ가 '밀도비'의 일반형이며 밀도란 기준 측도에 대한 이 도함수라는 관점, 측도 변환을 기댓값 재가중으로 읽기. 그것으로 KL(P‖Q)=E_P[log dP/dQ]가 중급 30번의 Σ p log(p/q)를 포함하는 더 넓은 정의임을 확인하고, 지지집합이 어긋나면 KL이 왜 정확히 +∞인지, 중요도 비율의 분모가 0이 되는 사건이 이론적으로 무엇인지, f-발산 일반이 이 도함수 위에서 정의된다는 것(A6-1로 이어진다), 그리고 절대연속이 아닌 두 분포 사이에서 KL 대신 무엇을 써야 하는지(A7의 바서슈타인). 증명 수위: 라돈–니코딤 정리는 진술과 사용법, KL의 성질은 완전 증명. | off-policy RL의 중요도 표집 가정, GAN이 붕괴하는 지지집합 불일치 논증, DPO/PPO 논문의 π/π_ref 항, f-발산 계열(f-GAN·f-DPO), 그리고 기르사노프 정리(A7)가 전부 이 도함수 하나를 돌려쓴다. 중급 30·58번. |
| `math-adv-conditional-expectation-and-martingales` | 조건부 기댓값은 사영이다, 그리고 마팅게일 | σ-대수에 대한 조건부 기댓값의 측도론적 정의와 그것이 L²에서의 직교 사영과 같다는 정리(A1-2의 사영 정리), 타워 성질 E[E[X\|G]]=E[X]와 전분산 공식, 조건부 젠센, 회귀함수 E[Y\|X]가 제곱오차를 최소화하는 예측자라는 결과 — 확산 모델의 최적 디노이저가 조건부 평균이라는 문장이 여기서 정확해진다. 후반부는 여과와 적응 과정, 마팅게일·서브·슈퍼마팅게일, 옵셔널 스토핑 정리, 마팅게일 수렴 정리, Doob 부등식, 그리고 마팅게일 차분에 대한 Azuma–Hoeffding. 중급 54번의 베이스라인 무편향이 여기서 훨씬 일반적인 형태로 다시 나온다. 증명 수위: 사영 등가성과 타워 성질은 완전 증명, 옵셔널 스토핑은 증명 개요. | 'the optimal denoiser is the conditional expectation'이라는 확산 논문의 한 줄, SGD 수렴 증명의 잡음 분해(A4), 온라인 학습의 후회 경계, 이토 적분의 마팅게일성(A7), e-값과 항시 유효 신뢰수열(A13)이 전부 이 단원 위에 세워진다. 중급 54번. |

### A2 · 대규모 선형대수 (2편) — 중급 3단원의 위층

| 슬러그 | 제목 | 무엇을 쓰는가 | 무엇이 열리는가 |
|---|---|---|---|
| `math-adv-sketching-and-matrix-concentration` | 스케치와 행렬 집중부등식: 큰 행렬을 줄여도 스펙트럼이 남는 이유 | Matrix Chernoff·Matrix Bernstein 부등식의 진술(스칼라 결과가 스펙트럼 노름으로 그대로 올라간다)과 그것으로 얻는 보장들. 부분공간 임베딩의 정의와 오블리비어스 스케치, 카운트 스케치와 부분표본 무작위 아다마르 변환(SRHT), 무작위 스케치로 열공간을 포착하는 range finder, randomized SVD의 알고리즘과 오차 상계, 전력 반복으로 스펙트럼 감쇠가 느릴 때를 보정하기, Nyström 근사와 CUR 분해. 상위 k개 특잇값만 필요할 때 전체 분해가 왜 낭비인지를 연산량으로 비교하고, 중급 16번의 저계수 근사를 수십억 파라미터 행렬 위에서 실제로 수행하는 절차를 만든다. 중급 66번의 존슨–린덴스트라우스를 행렬 수준으로 확장한 판이다. 증명 수위: Matrix Bernstein은 진술과 사용법, randomized SVD 오차 상계는 증명 개요. | Halko–Martinsson–Tropp의 randomized SVD 논문과 그 오차 보장, LoRA 초기화를 SVD로 잡는 논문(PiSSA·LoRA-XS), Performer·Nyströmformer·Linformer의 근사 오차 보장 절. 중급 15·16·66번. |
| `math-adv-krylov-lanczos-and-spectral-density` | 란초스로 헤세 스펙트럼을 재고 랜덤 행렬로 잡음을 걷어 내기 | 크릴로프 부분공간 {v, Av, A²v, …}의 구조와 아르놀디·란초스 반복, 삼중대각화와 리츠 값이 극단 고윳값부터 수렴하는 이유, 켤레경사법이 같은 부분공간 위의 최적해라는 관점과 수렴률이 √조건수로 개선되는 계산. 행렬을 만들지 않고 헤세-벡터 곱만으로 도는 방법, Hutchinson 대각합 추정과 stochastic Lanczos quadrature로 스펙트럼 밀도를 그리는 절차, 재직교화가 없으면 유령 고윳값이 생기는 수치 함정. 후반부는 그렇게 얻은 스펙트럼을 무엇과 비교할 것인가 — 위그너 반원 법칙과 마르첸코–파스투르 분포, 종횡비가 벌크의 지지구간을 정하는 방식, 스파이크 모형과 BBP 상전이(신호 고유벡터가 언제 잡음 벌크에서 분리되는가), 무거운 꼬리 스펙트럼. 무작위 초기화 가중치의 특잇값 분포를 마르첸코–파스투르와 겹쳐 그리고, 학습이 진행되며 꼬리가 무거워지는 변화를 측정한다. 중급 13번의 거듭제곱법과 15번의 특잇값 분포 그림에 '무엇이 잡음인가'라는 기준선을 준다. | 딥러닝 손실 지형의 헤세 스펙트럼 논문(Ghorbani·Papyan의 벌크+이상치 구조), heavy-tailed self-regularization 계열의 모델 품질 진단, K-FAC·Shampoo 구현의 내부, 신뢰영역 방법의 CG-Steihaug 하위 문제. 중급 12·13·15·45·51번. |

### A3 · 확률 부등식과 일반화 (3편) 【고급 절단점 1 — 여기까지 10편이면 유한 표본 보장과 일반화 경계 논문이 열린다】

| 슬러그 | 제목 | 무엇을 쓰는가 | 무엇이 열리는가 |
|---|---|---|---|
| `math-adv-concentration-inequalities` | 집중부등식: 표본 하나가 기댓값에서 얼마나 벗어나는가 | 마르코프에서 체비셰프로, 체르노프 방법의 적률생성함수 기법, 서브가우시안·서브지수 확률변수의 동치 정의들과 오를리츠 노름, n개 최댓값의 기대 상계 √(2σ²log n). 그 위에 Hoeffding(유계), Bernstein(분산을 아는 경우가 왜 훨씬 좋은가), 유계 차분 조건에서의 McDiarmid, 마팅게일 차분에 대한 Azuma(A1-5), 그리고 합집합 경계로 여러 사건을 한 번에 다루기. 각 부등식이 주는 필요 표본 수를 실제로 계산해 중급 74번의 정규 근사 신뢰구간과 나란히 놓고, 정규 근사가 언제 무너지는지 판정한다. 벡터·행렬로의 확장은 A2-1이 맡는다. 증명 수위: 체르노프 방법과 Hoeffding은 완전 증명, Bernstein·McDiarmid는 증명 개요. | 존슨–린덴스트라우스(중급 66번)와 LSH 보장의 원래 증명, PAC 학습 경계, 밴딧 UCB의 신뢰폭 유도(A14), 그리고 '표본 n개면 오차 ε를 확률 1−δ로 보장한다'는 모든 알고리즘 정리를 직접 유도하게 된다. 중급 21·66·74번. |
| `math-adv-uniform-convergence-and-pac-bayes` | 일반화 경계: 고전 이론이 공허해지는 자리와 PAC-Bayes | 경험위험과 실제위험의 간극을 균등수렴으로 다루는 틀, 성장함수와 VC 차원, Sauer 보조정리, Rademacher 복잡도와 대칭화 논법, 마진 기반·노름 기반 경계. 그리고 무작위 라벨을 외울 수 있는 모델이 실제 데이터에서는 일반화한다는 관찰이 이 틀에 던지는 문제 — 파라미터 수 앞에서 경계가 왜 공허해지는가. 후반부는 PAC-Bayes 정리의 구성과 증명, KL(사후‖사전) 항의 역할, 사전분포 선택이 경계를 좌우한다는 것, 비공허(non-vacuous) 경계를 실제로 계산한 사례, 평평한 최소점이 왜 작은 KL로 번역되는지, 압축 기반 경계와 알고리즘 안정성 기반 경계. 증명 수위: Rademacher 대칭화와 PAC-Bayes는 완전 증명, VC는 증명 개요. | Zhang 등의 'rethinking generalization', PAC-Bayes 비공허 경계 논문, SAM 같은 평탄성 지향 최적화의 이론적 근거. 중급 44·49번. |
| `math-adv-benign-overfitting-and-double-descent` | 이중 하강과 양성 과적합: 보간해도 일반화되는 이유 | 최소노름 보간 해의 명시적 폐형, 랜덤 특징 모형에서 위험 곡선을 정확히 계산하기, 보간 문턱에서 위험이 치솟는 이유(설계 행렬의 최소 특잇값)와 그 너머에서 다시 내려가는 이유, 양성 과적합이 성립하는 공분산 스펙트럼 조건(유효 차원과 스펙트럼 감쇠), 그리고 암묵적 편향 — 경사하강이 어떤 노름을 최소화하는 해로 수렴하는가. A2-2의 마르첸코–파스투르가 계산 도구로 그대로 쓰인다. 기존 ml-bias-variance는 고전적 편향-분산 분해와 과녁 비유·학습 곡선 진단을 다루는 글이므로, 이 글은 그 서사가 과매개변수 영역에서 어떻게 수정되는지를 정면으로 계산해 보인다. 증명 수위: 최소노름 해의 폐형과 위험 계산은 완전 증명. | double descent·benign overfitting·implicit regularization 계열 논문의 계산을 스스로 재현하게 되고, A13의 스케일링 법칙 이론이 딛는 커널 회귀 계산과 여기서 만난다. 중급 44·49·80번. |

### A4 · 볼록해석과 최적화 이론 (7편) — 중급 8단원의 위층

| 슬러그 | 제목 | 무엇을 쓰는가 | 무엇이 열리는가 |
|---|---|---|---|
| `math-adv-convex-analysis-and-subgradients` | 볼록해석: 하위기울기, 강볼록성, 매끄러움의 쌍대 | 볼록집합과 볼록함수의 여러 특징화(1차·2차 조건, 에피그래프), 확장실수값 함수로의 일반화와 인디케이터 함수, 하위미분 ∂f와 최적성 조건 0 ∈ ∂f, ReLU와 L1이 0에서 갖는 것, Clarke 일반화 미분과 ReLU가 0에서 어떤 값을 골라도 되는 이유의 정확한 서술. 강볼록성과 L-매끄러움이 서로 켤레라는 사실, 그리고 그 둘이 함께 주는 부등식들. 중급 29번의 볼록성과 44번의 헤세 판정을 미분 불가능한 경우까지 확장한다. 증명 수위: 최적성 조건과 강볼록–매끄러움 쌍대는 완전 증명. | L1·ReLU·핵노름처럼 미분 불가능한 항이 든 목적식의 최적성 조건을 논문 수준으로 정확히 쓰게 되고, 비매끄러운 SGD 수렴 정리의 가정부를 읽게 된다. 중급 29·38·44·46번. |
| `math-adv-fenchel-and-lagrangian-duality` | 르장드르–펜첼 변환과 쌍대성: 문제를 뒤집어 푸는 법 | 켤레함수 f*(y)=sup_x{⟨x,y⟩−f(x)}의 정의와 기하(접선의 절편), 이중 켤레 정리와 볼록 폐포, 지지함수, 노름과 쌍대 노름의 켤레 관계. 라그랑주 쌍대 문제의 구성, 약쌍대·강쌍대와 Slater 조건, 쌍대 간극으로 최적성을 증명하기. 중급 56번의 KKT를 쌍대성의 특수 경우로 다시 세우고, 57번의 π* ∝ π_ref·exp(r/β)라는 닫힌 해가 사실 엔트로피 항의 켤레 계산 하나라는 것을 보인다. 증명 수위: 약쌍대성과 켤레의 성질은 완전 증명, 강쌍대성은 증명 개요. | 엔트로피 정규화 문제의 해가 softmax·볼츠만 형태로 나오는 이유가 켤레 하나로 설명되고, 최적수송 쌍대(A7)·f-발산 변분 표현(A6)·미니맥스 정리(A14)가 전부 이 글의 응용이다. 중급 56·57번. |
| `math-adv-proximal-and-mirror-descent` | 근접 연산자와 거울 하강: 기하를 바꿔서 내려가기 | 근접 연산자의 정의와 사영·soft-thresholding·핵노름의 특잇값 수축이 그 특수경우라는 유도, Moreau 포락, 근접 경사법(ISTA)과 가속(FISTA)의 수렴률 O(1/k²), ADMM의 뼈대. 가중치 감쇠를 근접 단계로 읽으면 중급 48번의 AdamW가 왜 그 형태인지가 다시 설명된다. 후반부는 브레그만 발산의 정의와 KL이 음의 엔트로피의 브레그만 발산이라는 계산, 거울 하강의 갱신식 유도, 단체(simplex) 위에서 그것이 정확히 지수가중 갱신이 된다는 것, 그리고 중급 57번의 KL 제약 최적 정책이 거울 하강 한 걸음과 같다는 관점. 증명 수위: soft-threshold 유도와 단체 위 거울 하강은 완전 증명, FISTA 수렴률은 증명 개요. | 희소 오토인코더와 구조적 가지치기의 근접 단계, AdamW와 분리형 정규화의 이론적 정당화, TRPO·MDPO 계열이 KL로 한 걸음을 재는 이유, 온라인 볼록 최적화의 후회 분석. 중급 46·48·56·57번. |
| `math-adv-stochastic-approximation-and-sgd-sde` | 확률적 근사와 SGD의 연속 극한: 수렴한다는 말의 정확한 뜻 | Robbins–Monro의 스텝 조건 Ση=∞, Ση²<∞와 그 필요성, 마팅게일 잡음 분해(A1-5), ODE 방법으로 이산 갱신을 연속 흐름의 근사로 보기. 볼록·강볼록·PL 조건 각각에서의 수렴률 O(1/√T)·O(1/T)와 비볼록에서 그래디언트 노름 기준의 보장이 전역해를 뜻하지 않는다는 것, 오라클 복잡도 하한과 네스테로프 가속이 그 하한과 맞물린다는 계산, 분산 감소(SVRG·SAG)가 무엇을 벌고 왜 딥러닝에서는 잘 안 쓰이는지. 후반부는 미니배치 잡음을 확산항으로 근사해 SGD를 SDE로 적고 lr/batch가 온도처럼 작동해 정상분포를 정한다는 관점, 그 정상분포가 평평한 최소점에 질량을 몰아준다는 논증, 수정 방정식으로 얻는 암묵적 그래디언트 정규화 항, 그리고 edge of stability — 학습률이 2/λ_max를 넘어도 발산하지 않고 곡률 자체가 내려오는 현상. 중급 46번의 2/L 상한과 49번의 잡음·스케줄 논의가 여기서 이론으로 닫힌다. | Adam·TD 학습의 수렴 증명, SGD의 SDE 근사와 배치–학습률 스케일링 규칙, 암묵적 편향과 edge of stability 논문, 학습률 스케줄을 이론에서 유도하는 최근 연구. 중급 46·47·49번. |
| `math-adv-preconditioning-and-second-order-theory` | 프리컨디셔닝의 이론: Adam에서 K-FAC·Shampoo·Muon까지 한 줄로 | 프리컨디셔너 P⁻¹∇의 일반형과 아핀 불변성, 뉴턴·가우스–뉴턴·피셔·경험적 피셔가 각각 무엇을 근사하는지의 관계도, 준뉴턴 BFGS의 시컨트 조건과 저장 비용, 신뢰영역과 3차 정규화가 안장점을 탈출한다는 보장. 그다음 K-FAC의 크로네커 인수분해를 유도하고 그것이 딛는 층간·활성-그래디언트 독립 가정을 명시한다(이 유도는 커리큘럼 전체에서 여기 한 곳에만 둔다 — A6의 정보기하 글은 피셔가 무엇인지와 자연그래디언트의 불변성만 맡는다). Shampoo의 행·열 프리컨디셔너와 행렬 p-제곱근 계산, Muon을 스펙트럴 노름 아래 최급강하로 읽는 유도와 Newton–Schulz 반복, 그리고 이들이 어떤 극한에서 서로 일치하는지. 중급 51번이 나열만 한 근사들의 이론적 자리를 정한다. | K-FAC·Shampoo·SOAP·Muon 논문의 유도 절, 3차 정규화의 안장점 탈출 정리, 옵티마이저 벤치마크 논문의 이론 근거. 중급 51번. |
| `math-adv-riemannian-optimization` | 다양체 위의 최적화: 직교 제약과 Muon의 직교화가 하는 일 | 매끄러운 다양체·차트·접공간·접다발, 미분사상(pushforward), 리만 계량이 정하는 내적과 거리, 측지선과 지수사상·retraction, 평행 이동. 스티펠·그라스만·양정치 행렬 다양체와 확률 단체의 구체적 표현, 리만 그래디언트가 유클리드 그래디언트의 접공간 사영이라는 계산, 직교 제약 아래 경사하강을 실제로 도는 알고리즘, 극분해로 후퇴를 구현하기, Newton–Schulz 반복으로 UVᵀ를 근사하는 계산과 수렴 조건, 그리고 수치적 함정(직교성이 누적 오차로 무너지는 자리). A6의 자연그래디언트가 통계다양체 위의 같은 이야기라는 것도 여기서 예고한다. 증명 수위: 리만 그래디언트 = 접공간 사영은 완전 증명. | Muon 옵티마이저의 직교화 단계, 직교·유니터리 RNN, 초구 위 임베딩 학습, 자연 그래디언트를 다양체 언어로 적은 논문. 중급 7·11·51번. |
| `math-adv-loss-landscape-and-mode-connectivity` | 손실 지형의 이론: 대칭, 모드 연결, 날카로움, 그리고 안장점 | 신경망 파라미터화의 대칭군(뉴런 치환, ReLU 스케일 재조정, 회전)과 그것이 만드는 등가 다양체, 모드 연결성과 치환을 맞추면 선형 모드 연결이 성립한다는 결과, 손실 헤세의 스펙트럼 구조(벌크 + 클래스 수만큼의 이상치)와 그 출처(A2-2에서 실제로 측정한 그림). 1차 방법이 안장점을 빠져나오는 데 잡음이 하는 역할과 엄격 안장 성질, PL·KL 부등식 아래에서 비볼록인데도 선형 수렴이 나오는 계산, 과매개변수화가 지형을 바꾸는 방식. 후반부는 날카로움의 정의들이 재파라미터화에 불변이 아니라는 문제, SAM이 실제로 최소화하는 양의 1차 근사, 평평함과 일반화를 잇는 주장이 어디서 성립하고 어디서 깨지는지. 중급 44번의 서술을 정리로 바꾼다. | Git Re-Basin과 선형 모드 연결 논문, SAM과 그 변형, 'gradient descent escapes saddle points' 계열 정리, 모델 병합이 왜 되는지를 다루는 연구. 중급 44·45번. |

### A5 · 커널과 무한폭 (6편) — 중급 11단원과 50번의 위층

| 슬러그 | 제목 | 무엇을 쓰는가 | 무엇이 열리는가 |
|---|---|---|---|
| `math-adv-rkhs-representer-and-gaussian-processes` | RKHS와 표현자 정리, 그리고 그 베이즈 쌍둥이인 가우시안 과정 | 재생 커널의 정의와 평가 범함수의 유계성, A1-2의 리스 표현 정리로 얻는 재생 성질 f(x)=⟨f,k(x,·)⟩, 커널과 RKHS의 일대일 대응(Moore–Aronszajn), RKHS 노름이 재는 함수의 매끄러움. 그리고 표현자 정리의 완전 증명 — 정규화된 경험위험 최소화의 해가 반드시 데이터 점의 커널 결합 Σαᵢk(xᵢ,·)이 된다는 것과 그 성립 조건 — 에서 커널 능선회귀의 폐형 해를 얻는다. 후반부는 같은 결과의 베이즈 판: 가우시안 과정을 '함수 위의 정규분포'로 정의하고 중급 23번의 다변량 정규 조건부 공식에서 사후 평균·분산의 폐형을 유도해 사후 평균이 정확히 커널 능선회귀임을 확인하며, 주변가능도로 하이퍼파라미터를 학습하는 것이 자동으로 오컴의 면도날이 되는 이유와 O(n³) 비용, 그리고 EI·UCB 획득함수를 마지막 절에 짧게 붙인다. 기존 ml-svm은 sklearn SVM 사용법과 커널 트릭·주요 커널 함수 소개이고 중급 69번은 그람 행렬의 PSD 판정이므로, 이 글은 함수공간 이론과 표현자 정리만 맡는다. | 커널 방법의 원 논문들, NTK를 커널 회귀로 읽는 관점(A5-5·6), NNGP, 베이즈 최적화와 대리모델 논문. 중급 23·69번. |
| `math-adv-mercer-and-random-features` | Mercer 정리와 랜덤 특징: 커널의 스펙트럼과 유한 차원 근사 | 커널 적분작용소와 Mercer 전개(A1-2의 컴팩트 자기수반 스펙트럼 정리의 응용), 고유값 감쇠율이 함수 클래스의 매끄러움을 정한다는 것, 유효 차원(effective dimension)과 그것이 학습 곡선의 지수를 정하는 계산, 커널 회귀의 편향–분산 분해. 후반부는 보흐너 정리(평행이동 불변 커널 ↔ 양의 측도의 푸리에 변환)와 랜덤 푸리에 특징의 구성, 근사 오차의 집중 분석(A3-1), 양수성을 보장하는 특징 사상, Nyström 근사와의 비교. 증명 수위: 보흐너와 랜덤 특징 근사 오차는 완전 증명, Mercer는 증명 개요. | Performer의 FAVOR+와 선형 어텐션 계열이 정확히 이 근사를 어텐션 커널에 적용한 것이고, NTK 스펙트럼과 스펙트럴 편향(A5-5, A9-1), 커널 회귀 학습 곡선의 지수(A13의 스케일링 법칙 이론)가 전부 이 결과의 응용이다. 중급 69번. |
| `math-adv-mmd-and-kernel-two-sample-tests` | MMD와 커널 두 표본 검정: 두 분포가 다른지 표본만으로 판정하기 | 적분 확률 계량의 틀, 커널 평균 임베딩 μ_P=E_P[k(X,·)]와 특성 커널일 때 임베딩이 단사라는 것, MMD의 정의와 불편 추정량, 검정통계량의 귀무분포와 순열 검정으로 임계값을 얻는 절차, 커널 대역폭 선택이 검정력에 미치는 영향. MMD가 f-발산·바서슈타인과 어떻게 다른지(지지집합이 어긋나도 유한하다), 독립성 검정 HSIC, 그리고 스타인 항등식과 커널 스타인 불일치로 '표본이 이 분포에서 나왔는가'를 정규화 상수 없이 검정하기. 중급 75~76번의 재표본 검정과 곧장 이어진다. | 생성 표본 품질 검정과 MMD-GAN, 분포 시프트 탐지, 커널 스타인 검정, 임베딩 공간에서 두 데이터셋을 비교하는 실무 절차의 이론적 근거. 중급 68·75·76번. |
| `math-adv-signal-propagation-and-nngp` | 평균장 신호 전파와 NNGP: 깊이 방향의 질서–혼돈 경계 | 폭이 넓은 무작위 신경망에서 활성값의 분산과 두 입력 사이의 상관이 층을 지나며 따르는 사상(map)을 유도하고, 그 사상의 고정점과 안정성으로 질서 상(모든 입력이 같아짐)과 혼돈 상(상관이 소멸)의 상전이를 그린다. 임계선 위에서만 정보가 깊이 전파된다는 결과, dynamical isometry(야코비안 특잇값이 1 근처에 몰리는 조건)와 직교 초기화, 그리고 잔차 연결과 정규화가 망을 임계에 붙들어 두는 방식. 후반부는 폭이 무한대로 갈 때 초기화된 신경망의 사전분포가 가우시안 과정으로 수렴한다는 NNGP 결과와 층마다의 커널 재귀식 유도, 활성함수별 아크코사인 커널, 깊이에 따른 커널의 고정점. 중급 50번의 분산 전파를 상관과 극한까지 민 결과다. 증명 수위: 분산·상관 재귀식은 완전 증명, GP 수렴은 증명 개요. | mean field theory of deep networks와 dynamical isometry 논문, NNGP 계열의 커널 재귀식을 직접 유도하고 코드로 재현하게 된다. 매우 깊은 망이 왜 학습 가능해졌는지의 정량적 답. 중급 50번. |
| `math-adv-neural-tangent-kernel` | NTK: 학습이 선형이 되는 영역과 그 커널의 스펙트럼 | 파라미터에 대한 1차 전개로 얻는 접커널의 정의, 폭이 무한대일 때 학습 중 커널이 상수로 고정된다는 정리와 그 아래 학습이 커널 회귀와 같아진다는 결과, 수렴 속도가 NTK 고유값으로 결정된다는 계산(A5-2의 고유값 감쇠), 저주파를 먼저 학습하는 스펙트럴 편향, 유한 폭에서의 보정항. 게으른(lazy) 영역이 특징 학습을 못 한다는 한계와 그것이 실제 딥러닝을 얼마나 설명하고 어디서 실패하는지. 중급 46번의 수렴 논의가 여기서 스펙트럼으로 분해된다. 증명 수위: 접커널의 상수성은 증명 개요 + 핵심 보조정리, 커널 회귀 등가는 완전 증명. | Jacot의 NTK 논문과 그 후속 이론 전반, 무한폭 극한을 쓰는 모든 분석, 스펙트럴 편향 논문, 그리고 다음 글 muP가 '왜 표준 파라미터화가 특징 학습을 죽이는가'를 논하는 출발점. 중급 46·50번. |
| `math-adv-feature-learning-and-mup` | 특징 학습 영역과 muP: 폭에 따라 무엇을 어떻게 스케일하나 | 폭 n이 커질 때 각 층의 활성값과 갱신 크기가 어떻게 스케일해야 하는지 좌표 단위로 요구조건을 세우고(coordinate check를 실제로 돌리는 절차 포함), 표준 파라미터화가 그것을 왜 위반해 게으른 영역으로 떨어지는지, abc-파라미터화 분석으로 극한이 갈리는 지점을 정리한다. 최대 갱신 파라미터화(muP)의 초기화 분산·학습률·출력 배율 규칙을 유도하고, 폭 전이와 깊이 전이의 차이, 그리고 작은 모델에서 튜닝한 학습률이 큰 모델로 옮겨 가는 이유(muTransfer)를 실험으로 확인한다. 중급 50번의 마지막 절이 여기서 완결된다. | Yang의 Tensor Programs와 muP·muTransfer 논문의 스케일링 표를 그대로 읽고 자기 모델에 적용하게 된다. 고급 트랙에서 실무 효용이 가장 즉각적인 글이다. 중급 50번. |

### A6 · 정보와 기하 (4편) 【고급 절단점 2 — 여기까지 27편이면 목적식 논문의 유도가 열린다】

| 슬러그 | 제목 | 무엇을 쓰는가 | 무엇이 열리는가 |
|---|---|---|---|
| `math-adv-f-divergences-and-variational-representations` | f-발산과 변분 표현: GAN·MINE·InfoNCE가 한 틀에서 나온다 | f-발산의 통일된 정의와 KL·역KL·헬링거·전변동·χ²·JS가 전부 볼록함수 f 하나의 선택이라는 표, 데이터 처리 부등식, 그리고 켤레 변환(A4-2)으로 얻는 변분 표현 D_f(P‖Q)=sup_T{E_P[T]−E_Q[f*(T)]}. 여기서 Donsker–Varadhan 표현과 그것이 주는 KL 하한을 얻고, GAN의 판별자가 f-발산을 추정하는 장치임을 보이며, MINE·InfoNCE·NWJ 하한을 한 표에 놓아 편향과 분산을 비교한다. 중급 31번의 log N 천장이 어느 하한의 어떤 성질에서 나오는지 정확히 짚고, 발산을 새로 제안하는 논문이 무엇을 바꾸고 무엇을 유지했는지 분류하는 절차로 닫는다. 증명 수위: 변분 표현과 Donsker–Varadhan은 완전 증명. | f-GAN·MINE·InfoNCE 계열 대조학습 논문의 목적식 절, KL 페널티를 다른 발산으로 바꾼 정렬 논문(f-DPO 등). 중급 30·31번. |
| `math-adv-exponential-family-and-max-entropy` | 지수족과 최대엔트로피: softmax와 볼츠만 해가 같은 곳에서 나온다 | 지수족의 표준형 p(x)=h(x)exp(η·T(x)−A(η)), 충분통계량과 자연 파라미터, 로그 분배함수 A(η)가 볼록이고 그 그래디언트가 충분통계량의 기댓값·헤세가 공분산이라는 정리, 자연 파라미터와 평균(모멘트) 파라미터가 르장드르 변환으로 이어진 쌍대 좌표라는 구조. 그리고 최대엔트로피 원리 — 모멘트 제약 아래 엔트로피를 최대화하면 반드시 지수족이 나온다는 것 — 을 라그랑주 승수로 유도한다. 이것으로 중급 24번의 softmax, 57번의 KL 제약 최적 정책 π* ∝ π_ref·exp(r/β), 에너지 기반 모델 p ∝ exp(−E)가 전부 그 한 정리의 사례임을 보인다. 가우시안·범주분포·베르누이가 한 가족임을 확인하는 표까지. 증명 수위: A(η)의 미분 성질과 최대엔트로피 유도는 완전 증명. | 변분추론의 자연 파라미터 갱신(A8-2), 최대엔트로피 강화학습(SAC)의 목적식 유도, RLHF 최적 정책을 지수 기울임으로 적는 정렬 논문, 에너지 기반 모델(A8-1). 중급 24·26·56·57번. |
| `math-adv-fisher-information-and-natural-gradient` | 정보기하와 자연 그래디언트: 분포 공간에서 내려가기 | 점수 함수의 기댓값이 0이라는 항등식, 피셔 정보 행렬의 두 표현(점수의 공분산 = 음의 기대 헤세)과 동치 증명, 크라메르–라오 하한, 그리고 피셔가 KL의 2차 근사로 나온다는 계산 — 이 계산은 커리큘럼 전체에서 여기 한 곳에만 둔다. 통계다양체 위의 리만 계량으로서의 피셔와 첸초프 정리(사실상 유일한 불변 계량)의 서술, 자연 그래디언트 F⁻¹∇L의 유도와 그것이 파라미터 재표현에 불변인 이유(A4-6의 리만 최적화가 여기 그대로 적용된다), 지수족의 이중 평탄 구조와 e-사영·m-사영, 경험적 피셔와 진짜 피셔의 차이, 가우스–뉴턴 행렬과의 관계. K-FAC의 크로네커 인수분해 유도는 A4-5가 맡으므로 여기서는 '무엇을 근사하는 대상인가'만 확정한다. 중급 61번의 스코어 ∇ₓ log p와 여기의 점수 ∇_θ log p가 다른 대상임을 명확히 구분한다. | Amari의 자연 그래디언트, TRPO가 KL 신뢰영역을 피셔로 근사하는 절, K-FAC·Shampoo 논문이 근사하는 대상, EWC 같은 연속학습 정규화의 피셔 항. 중급 26·30·51·58번. |
| `math-adv-rate-distortion-mdl-and-compression` | 율–왜곡·정보 병목·MDL: 압축으로서의 학습 | 율–왜곡 함수 R(D)의 정의와 볼록성, Blahut–Arimoto 알고리즘, 채널 용량과의 이중성, 정보 병목 목적식 min I(X;Z)−βI(Z;Y)와 그 라그랑주 구조·자기일관 방정식, 변분 근사(VIB)와 β-VAE가 같은 교환 곡선 위에 있다는 관점. 후반부는 최소 기술 길이 — 콜모고로프 복잡도의 정의와 계산 불가능성, 무접두 부호와 크래프트 부등식, 정보량과 부호 길이의 대응, 2부 부호와 MDL 원리, 그리고 '언어모델은 압축기다'라는 주장을 산술부호화와 로그가능도의 대응으로 정확히 적어 중급 26번의 bits-per-byte를 이론으로 받친다. 증명 수위: 크래프트 부등식과 로그 손실 = 부호 길이는 완전 증명. | 정보 병목 기반 딥러닝 해석 논문과 그 비판, 변분 정보 병목, 무손실 압축률로 모델을 비교하는 최근 연구, MDL 기반 일반화 경계. 중급 26·28·31번. |

### A7 · 확률과정과 생성의 연속시간 이론 (9편) 【고급 절단점 3 — 여기까지 36편이면 확산·흐름 매칭의 이론이 완결된다. 수렴점 둘이 여기 있다】

| 슬러그 | 제목 | 무엇을 쓰는가 | 무엇이 열리는가 |
|---|---|---|---|
| `math-adv-markov-chains-mixing-and-mcmc` | 마르코프 연쇄·혼합 시간·MCMC: 상세 균형에서 HMC까지 | 전이 커널과 불변분포, 기약성·비주기성·에르고딕 정리, 상세 균형, 전변동 거리로 정의한 혼합 시간, 스펙트럼 갭과 혼합 시간의 관계, 커플링 논법. 후반부는 메트로폴리스–헤이스팅스의 상세 균형 증명, 해밀토니안 몬테카를로의 구성과 립프로그 적분기가 부피를 보존한다는 것(심플렉틱), 수용률과 스텝 크기의 최적 조정, R̂와 유효 표본 수 같은 수렴 진단. 다봉분포에서 혼합이 지수적으로 느려지는 이유와 그것이 A7-5의 랑주뱅 수렴률과 어떻게 이어지는지. 증명 수위: 상세 균형과 MH의 정당성은 완전 증명, 에르고딕 정리는 진술과 사용법. | 베이지안 딥러닝과 표집 기반 추론 논문의 알고리즘·진단 지표, MCMC 수렴 진단, 그리고 랑주뱅 기반 샘플러(A7-5)의 속도 논의가 여기서 정량화된다. |
| `math-adv-brownian-motion-and-ito` | 브라운 운동과 이토 미적분: dW²=dt가 뜻하는 것 | 브라운 운동의 정의와 존재, 정상 독립증분, 경로가 거의 모든 점에서 미분 불가능하다는 성질, 이차변동이 t라는 결정적 계산. 이토 적분의 구성과 왜 보통 적분(스틸체스)으로는 안 되는지, 이토 적분이 마팅게일이라는 것(A1-5), 이토 보조정리의 유도와 (dW)²=dt로 요약되는 2차 항이 왜 보통 연쇄법칙과 다른지, 스트라토노비치 적분과의 차이. SDE의 강해·약해와 존재·유일성 조건, 기하 브라운 운동과 OU 과정 같은 닫힌 해, 오일러–마루야마 이산화로 시뮬레이션해 이론과 맞추기. 초급 36번의 미분방정식 첫걸음과 중급 62번 사이를 잇는다. 증명 수위: 이차변동과 이토 보조정리는 완전 증명. | Song 등의 score-based SDE 논문 §3의 첫 여섯 식이 그대로 이 언어로 쓰여 있다. 확산 모델·SGD의 연속 근사(A4-4) 논문의 계산이 전부 이토 보조정리 한 줄로 이루어진다. 중급 62번. |
| `math-adv-fokker-planck-and-time-reversal` | 포커–플랑크와 시간 역전: 역과정에 스코어가 나타나는 이유 | SDE의 생성원, 콜모고로프 전방 방정식(포커–플랑크)의 유도로 밀도의 시간 전개를 적기, 정상분포가 깁스 분포가 되는 조건, 그리고 중급 62번의 확산 forward 과정을 SDE로 정확히 적기. 핵심은 Anderson의 시간 역전 정리 — 역시간 SDE의 표류항에 ∇log p_t(x)가 정확히 등장한다는 것 — 의 진술과 증명이다. 중급 61번이 '노이즈 예측이 곧 스코어 추정'이라 말한 자리에서, 왜 하필 스코어여야 역과정이 닫히는지에 정리로 답한다. 확률 흐름 ODE가 같은 주변분포를 갖는다는 결과도 여기서 유도하고, SDE 샘플러와 ODE 샘플러의 차이를 정확히 말한다. 증명 수위: 포커–플랑크와 시간 역전 정리는 완전 증명 — 이 커리큘럼의 수렴점 중 하나다. | 확산 모델 이론의 심장부. DDPM·score SDE·DDIM·consistency model 논문의 유도가 전부 이 정리 하나에서 갈라져 나온다. 중급 61·62번. |
| `math-adv-girsanov-and-path-measures` | 기르사노프 정리와 경로 측도: 표류를 바꾸면 확률이 어떻게 바뀌나 | 경로공간 위의 측도, 지르사노프 정리(표류항을 바꾸는 것이 라돈–니코딤 도함수를 곱하는 것과 같다 — A1-4의 직접 확장), 그리고 두 확산 과정 사이의 경로공간 KL이 표류 차이의 제곱 적분으로 떨어지는 계산. 이것으로 score matching 목적식이 두 과정 사이 KL의 상한임을 보이고, 확산 모델의 우도 경계가 어디서 나오는지 적는다. 그리고 '유도(guidance)를 건다'는 조작이 목표 측도를 어떻게 기울이는지, KL 정규화된 확산 미세조정의 목적식이 무엇인지. 증명 수위: 경로공간 KL 계산은 완전 증명, 기르사노프는 증명 개요 + 핵심 보조정리. | 확산 모델의 RL 미세조정(DDPO·DPOK), 확률 제어로 본 guidance, Schrödinger bridge의 목적식, 그리고 경로공간 KL이 등장하는 제어된 생성 논문. 중급 62·63번. |
| `math-adv-langevin-and-log-sobolev` | 랑주뱅 동역학의 수렴: 로그–소볼레프 부등식과 혼합 속도 | 오버댐프드 랑주뱅 SDE와 그 정상분포가 목표 분포라는 것, 상대 엔트로피가 지수적으로 감소한다는 계산, 로그–소볼레프·푸앵카레 부등식이 정하는 수렴률과 두 부등식의 관계, 이산화(ULA)의 편향과 MALA 보정, 다봉분포에서 혼합이 느려지는 이유(A7-1의 스펙트럼 갭). 강볼록 퍼텐셜에서 LSI 상수가 어떻게 나오는지와 그것이 요구하는 가정이 실제 데이터 분포에서 성립하지 않는다는 정직한 지적까지. 증명 수위: 엔트로피 감소율과 LSI ⇒ 지수 수렴은 완전 증명. | 랑주뱅 기반 표집과 확산 모델의 수렴 보장 논문이 요구하는 가정이 무엇인지 읽게 되고, 에너지 기반 모델(A8-1)의 샘플러가 왜 그렇게 도는지 알게 된다. |
| `math-adv-continuity-equation-and-neural-ode` | 연속성 방정식과 뉴럴 ODE: 속도장이 밀도를 옮기는 법 | 질량 보존에서 연속성 방정식 ∂_t p + ∇·(pv)=0을 세우고 속도장 v가 밀도를 어떻게 수송하는지 그림과 시뮬레이션으로 확인한다. 그다음 뉴럴 ODE와 순간 변수변환 공식 d log p/dt = −tr(∂v/∂x) — 중급 34번의 야코비안 행렬식이 연속 시간에서 대각합으로 바뀌는 것 — 을 유도하고, 수반(adjoint) 민감도 방법으로 메모리 없이 역전파하는 원리, Hutchinson 추정기로 대각합을 싸게 추정하는 트릭. 정규화 흐름의 변수변환 공식과 log det 야코비안을 싸게 만드는 구조들(결합층·자기회귀·행렬식이 삼각인 설계)이 연속 시간에서 왜 필요 없어지는지. 증명 수위: 연속성 방정식과 순간 변수변환은 완전 증명. | Neural ODE·FFJORD·CNF 논문과 정규화 흐름(RealNVP·Glow)의 log det 절, 중급 64번 흐름 매칭의 결정론 쪽 토대. 중급 34·62·64번. |
| `math-adv-optimal-transport-duality-and-brenier` | 최적수송: 몽주–칸토로비치 쌍대성과 브레니에 정리 | 몽주 문제와 칸토로비치 완화, 수송 계획과 결합측도, 쌍대 문제와 c-볼록성(A4-2의 켤레 변환이 그대로 응용된다), W_p 거리가 계량 공리를 만족한다는 것, 1차원에서 분위수 함수로 닫힌 형태가 나오는 계산, Kantorovich–Rubinstein 공식(W₁이 1-립시츠 함수 위의 상한 — WGAN 비평자의 립시츠 제약이 정확히 이것), 브레니에 정리(2차 비용의 최적 사상은 볼록함수의 그래디언트), 변위 보간과 측지선, 그리고 W가 KL과 달리 지지집합이 어긋나도 유한하다는 성질. 중급 64번이 한 문단으로 스친 자리를 통째로 연다. 증명 수위: 약쌍대성과 KR 공식은 완전 증명, 브레니에는 증명 개요. | WGAN의 쌍대 논증, 흐름 매칭·정류 흐름 논문의 최적수송 절, 도메인 정렬과 표현 정렬 논문의 수송 비용. 중급 64번. |
| `math-adv-entropic-ot-and-sinkhorn` | 엔트로피 정규화와 Sinkhorn: 미분 가능한 매칭 | 엔트로피 정규화된 최적수송의 쌍대와 해가 대각 스케일링 형태 diag(u)K diag(v)로 나온다는 유도, Sinkhorn 반복이 교대 사영이며 A1-1의 축약사상 원리로 수렴한다는 증명과 수렴 속도, 정규화 세기가 만드는 편향과 Sinkhorn divergence로 그것을 보정하기, 계산 복잡도, 그리고 반복이 미분 가능해 손실로 쓸 수 있다는 것(unrolling과 암묵 미분 두 방식). 로그 영역 구현이 왜 필요한지도 수치로 짚는다. 증명 수위: 해의 형태와 수렴은 완전 증명. | Sinkhorn 기반 매칭·클러스터링·균형 잡힌 MoE 라우팅, 도메인 적응의 Sinkhorn 손실, 그리고 다음 글의 IPF/Schrödinger bridge. 중급 64번. |
| `math-adv-wasserstein-gradient-flow-and-mean-field` | 바서슈타인 경사흐름과 평균장 극한: 확산·학습·흐름 매칭이 한 정리 아래 모인다 | 측도 공간 위의 에너지 범함수와 그 경사흐름을 정의하고, JKO 스킴(바서슈타인 근접점 반복 — A4-3의 근접 연산자를 분포 공간에서 다시 쓴 것)의 극한이 포커–플랑크 방정식이라는 결과, 즉 확산이 KL 자유에너지의 바서슈타인 경사하강이라는 정리를 유도한다. 그다음 같은 틀로 두 층 신경망을 뉴런의 경험 측도로 다시 쓰고, 폭이 무한대일 때 학습이 측도의 바서슈타인 경사흐름이 된다는 평균장 극한과 전역 수렴 정리, 그리고 A5-5의 NTK(게으른) 영역과의 대비. 마지막으로 슈뢰딩거 다리 문제(두 분포를 잇는 경로 중 사전 과정에 가장 가까운 것)와 IPF 반복, 확률적 보간자와 정류 흐름이 이 틀에서 어디에 앉는지 정리한다. 증명 수위: JKO ⇒ 포커–플랑크는 완전 증명 — 이 커리큘럼의 수렴점 중 하나다. | 따로 배운 세 가지(확산·평균장 학습·흐름 매칭)가 한 정리 아래 모이는 것을 보게 된다. mean-field 이론 논문, rectified flow·stochastic interpolant·Schrödinger bridge 계열 최신 생성모델의 이론부. 중급 62·64번. |

### A8 · 에너지·변분·트랜스포머의 이론 (6편)

| 슬러그 | 제목 | 무엇을 쓰는가 | 무엇이 열리는가 |
|---|---|---|---|
| `math-adv-energy-based-models-and-stein` | 에너지 기반 모델과 스타인: 정규화 상수를 피하는 방법들 | 에너지 기반 모델 p ∝ exp(−E)와 분배함수가 계산 불가능하다는 문제(A6-2의 지수족이 배경), 대조 발산과 그 편향, 노이즈 대조 추정(NCE)의 일치성, Hyvärinen의 원형 점수 매칭과 그 미분 항이 헤세 대각합이라는 계산, 그리고 denoising score matching이 그 대각합을 우회하는 방식의 완전한 유도 — 중급 61번이 결과만 쓴 자리다. 랑주뱅 동역학으로 EBM에서 표집하기(A7-5)와 이산화 편향의 MALA 보정, 스타인 항등식과 커널 스타인 불일치, 스타인 변분 경사하강(SVGD)이 바서슈타인 경사흐름의 커널 근사라는 관점(A7-9). 증명 수위: 원형 ↔ denoising 점수 매칭 등가는 완전 증명. | Hyvärinen의 점수 매칭 원 논문, NCE와 그 대조학습 후예, 랑주뱅 기반 샘플러, 커널 스타인 검정, EBM으로 본 LLM 정렬 논문. 중급 61번. |
| `math-adv-variational-inference-theory` | 변분추론의 이론: 하한을 얼마나 조일 수 있는가 | 변분족과 ELBO의 간극이 정확히 KL(q‖p)라는 것, 평균장 근사와 좌표 상승 VI의 갱신식, 그것이 만드는 편향과 분산 과소추정, 정방향·역방향 KL의 기하, 확률적 VI와 자연 파라미터 갱신(A6-2·A6-3), amortization gap. 재매개변수화 그래디언트와 점수 함수 그래디언트의 분산 비교와 통제 변량 설계(중급 53·59번의 상위판), 중요도 가중 ELBO(IWAE)가 K를 늘리면 더 조여진다는 증명과 그 대가로 인코더 그래디언트 신호가 약해진다는 분석, 사후 붕괴의 원인과 β-VAE의 라그랑주 해석. 증명 수위: ELBO 분해와 IWAE 단조성은 완전 증명. | IWAE, 정규화 흐름(RealNVP·Glow), β-VAE, 확산의 변분 관점(VDM) 논문의 유도 절. 변분 목적식을 새로 제안하는 논문이 무엇을 완화했고 무엇을 대가로 치렀는지 판정하게 된다. 중급 53·59·60번. |
| `math-adv-automatic-differentiation-theory` | 자동미분의 이론: 무엇이 최적 배치이고 무엇이 조용히 틀리는가 | 계산 그래프를 미분하는 세 방식(이중수, 연산자 오버로딩, 소스 변환), 전방·역방향을 임의로 섞는 혼합 모드와 최적 제거 순서 결정이 NP-난해라는 사실, Baur–Strassen 정리(그래디언트 비용이 함숫값 비용의 상수배), 체크포인팅의 최적 재계산 스케줄과 메모리-연산 맞바꿈 곡선. 고계 미분을 JVP와 VJP의 합성으로 얻기, 헤세-벡터 곱을 두 번의 통과로 계산하기(A2-2가 그것을 쓴다), 제어 흐름·인플레이스 연산·비매끄러운 원시연산에서 미분이 조용히 틀리는 자리, 그리고 암묵 함수 정리로 고정점과 최적화 문제 자체를 미분하기. 중급 35번의 비용 논증을 정리로 바꾼다. | JAX의 변환 조합 설계, 그래디언트 체크포인팅 논문, 암묵 미분 기반 이중수준 최적화(메타학습·하이퍼파라미터 최적화), DEQ(심층 균형 모델). 중급 33·34·35번. |
| `math-adv-attention-as-kernel-and-associative-memory` | 어텐션을 커널과 연관 기억으로 다시 쓰기 | softmax 어텐션을 비대칭 커널의 Nadaraya–Watson 회귀로 적기, 커널 관점에서 선형 어텐션이 특징 사상 φ의 선택일 뿐이며 그때 계산이 O(n)로 떨어지는 대수(A5-2의 랜덤 특징이 φ의 한 선택), 현대 홉필드 네트워크의 갱신 규칙이 어텐션 한 층과 같다는 대응과 거기서 나오는 저장 용량 상계, 연관 기억으로서의 KV 캐시와 항목 간 간섭의 계산, 어텐션 행렬의 저계수 구조와 근사 가능성의 하한. 중급 40~43번이 계산한 어텐션을 다른 언어로 다시 읽는 글이다. | Performer·Linear Transformer의 특징 사상 유도, Hopfield Networks is All You Need, 어텐션 근사의 하한을 다루는 이론 논문. 중급 40~43·69번. |
| `math-adv-in-context-learning-theory` | 문맥 내 학습의 이론: 어텐션이 경사하강을 구현한다는 구성 | 선형 회귀를 문맥에서 푸는 트랜스포머를 손으로 구성해 한 층의 어텐션이 경사하강 한 걸음과 같아지는 가중치를 명시하고, 층 수와 반복 횟수의 대응을 확인한다(구성적 증명이므로 완전 증명으로 싣는다). 베이즈 관점 — 문맥이 잠재 과제의 사후분포를 좁힌다 — 과 그 예측이 실측 곡선과 어긋나는 지점, 유도 헤드(induction head) 회로의 구조와 그것이 나타나는 학습 상전이, 문맥 길이에 따른 오차가 함수 클래스별로 어떻게 줄어드는지. 중급 41번이 유도한 어텐션 그래디언트가 여기서 '학습 알고리즘을 구현하는 회로'로 다시 읽힌다. | Garg 등의 문맥 내 학습 함수 클래스 연구, von Oswald의 경사하강 구성, Olsson의 유도 헤드 논문, 회로 기반 해석가능성 연구. 중급 41번. |
| `math-adv-expressivity-and-circuit-complexity` | 표현력의 한계: 한 번의 전방 통과로 풀 수 없는 문제의 증명 | 보편 근사 정리의 정확한 진술과 그것이 약속하지 않는 것(폭·깊이·학습 가능성), 필요한 폭이 지수적일 수 있다는 것, ReLU 망이 만드는 조각별 선형 영역의 개수가 깊이에 지수적으로 늘어난다는 계산과 깊이 분리 정리. 그다음 회로 복잡도 계급 AC⁰·TC⁰와 고정 깊이·고정 정밀도 트랜스포머가 균등 TC⁰에 갇힌다는 결과의 논증 구조, 그래서 상태 추적·괄호 맞추기 같은 문제가 한 번의 전방 통과로는 불가능하다는 것, 하드 어텐션과 소프트 어텐션의 차이. 사고 사슬이 중간 토큰을 작업 기억으로 써서 표현 가능한 계급을 넓힌다는 정리, 통신 복잡도로 얻는 어텐션 하한, 깊이·수치 정밀도·문맥 길이가 표현력에 들어오는 방식, RASP류 프로그램 표현. 중급 73번의 연산량과는 다른 축(계산 복잡도)의 이야기임을 첫 절에서 못 박는다. 기존 reasoning-test-time-compute의 실무 논의에 이론적 근거를 붙인다. | Merrill–Sabharwal의 트랜스포머 표현력 논문, CoT가 표현력을 늘린다는 정리 논문, RASP·Tracr 같은 프로그램 표현 연구. 'transformers cannot X' 계열 주장의 범위를 정확히 읽게 된다. 중급 73번. |

### A9 · 스펙트럴 이론의 응용: 시퀀스와 그래프 (4편) 【고급 절단점 4 — 여기까지 46편】

| 슬러그 | 제목 | 무엇을 쓰는가 | 무엇이 열리는가 |
|---|---|---|---|
| `math-adv-fourier-analysis-and-spectral-bias` | 조화해석과 스펙트럼 편향: 신경망이 저주파를 먼저 배우는 이유 | 푸리에 급수와 변환, 합성곱 정리, 파세발 항등식, 이산 푸리에 변환과 FFT의 O(n log n), 표본화 정리와 에일리어싱. 초급 26번의 복소수·오일러 공식 위에서 진행한다. 그다음 A5-5의 NTK 고유값 분해가 주파수별 학습 속도를 정한다는 계산으로 스펙트럼 편향 정리를 유도하고(이 정리는 커리큘럼에서 여기 한 곳에서만 유도한다), 푸리에 특징 사상 γ(x)=[cos(2πBx), sin(2πBx)]이 그 편향을 어떻게 고쳐 NeRF를 가능하게 했는지 잇는다. 중급 43번의 사인파 위치 인코딩이 사실 이 특징 사상의 특수 경우임을 보이고, 학습된 망의 출력 스펙트럼을 실제로 측정하는 실험으로 닫는다. 증명 수위: 합성곱 정리와 파세발은 완전 증명, 스펙트럼 편향은 증명 개요. | 스펙트럼 편향·Fourier features·NeRF·FNO(Fourier Neural Operator)·긴 합성곱 모델 논문의 계산이 전부 이 정리 위에 있다. 중급 43번. A10의 표현론이 여기를 전제로 시작한다. |
| `math-adv-orthogonal-polynomials-and-hippo` | 직교다항식과 HiPPO: 상태공간 모델이 과거를 압축하는 방식 | 가중 내적 아래의 직교다항식(르장드르·라게르·체비셰프)과 그람–슈미트 구성, 최적 다항 근사와 계수의 의미, 그리고 함수의 이력을 직교 기저 계수로 요약하는 문제에서 HiPPO 행렬이 유도되는 전 과정과 그 계수가 만족하는 ODE. 측도(균등·지수 감쇠)를 바꾸면 어떤 행렬이 나오는지, 그것이 '무엇을 기억하고 무엇을 잊는가'를 어떻게 정하는지. 중급 7번의 사영을 함수 공간의 시간축으로 옮긴 결과다. 증명 수위: HiPPO 행렬 유도는 완전 증명. | HiPPO·S4 논문의 초기화가 왜 하필 그 특정 행렬인지에 유도로 답하게 된다. 긴 문맥 모델의 기억 감쇠 분석. 중급 7번. |
| `math-adv-toeplitz-circulant-and-state-space-models` | 토플리츠·순환 행렬과 상태공간 모델: 선형 시간 시퀀스의 선형대수 | 토플리츠·순환 행렬의 구조와 순환 행렬이 DFT로 대각화된다는 정리, 그래서 긴 합성곱이 FFT로 O(n log n)이 된다는 계산. 선형 시불변 시스템 x′=Ax+Bu의 해와 합성곱 커널, 이산화(쌍선형·ZOH)와 안정성 조건, S4의 대각+저계수 구조와 그것이 커널을 FFT로 계산하게 하는 이유, 그리고 입력 의존 파라미터(선택성)를 넣으면 왜 합성곱 형태를 잃고 병렬 스캔이 필요한지. 순환·합성곱·어텐션 세 형태의 상호 변환표와 각각의 기억 감쇠 특성, 중급 73번의 O(n²)와의 정면 대조. 기존 transformer-efficient는 효율 트랜스포머 다섯 갈래를 비교하며 Mamba·S4를 한 절로 소개하는 글이므로, 이 글은 커널 계산과 이산화의 선형대수만 맡는다. 증명 수위: 순환 행렬의 DFT 대각화는 완전 증명. | S4·Mamba 논문의 이론 절, 선형 어텐션과 SSM을 통합하는 최근 프레임워크, 긴 합성곱 모델의 커널 계산. 중급 13·42·73번. |
| `math-adv-spectral-graph-theory` | 그래프 라플라시안의 스펙트럼: 연결성·Cheeger·과평활화 | 인접·차수·라플라시안 행렬과 정규화 라플라시안, 고유값 0의 중복도가 연결 성분 수라는 정리, 피들러 값과 확장성, Cheeger 부등식으로 절단과 스펙트럼을 잇기, 스펙트럴 군집화가 이산 절단 문제의 완화라는 논증. 라플라시안 고유벡터를 그래프 위의 푸리에 기저로 보는 관점에서 스펙트럴 합성곱과 체비셰프 다항 국소화(ChebNet→GCN)를 유도하고, 메시지 전달을 반복하면 표현이 한 점으로 수렴하는 과평활화를 라플라시안 스펙트럼과 디리클레 에너지로 정량화한다. 1-WL 검정과 메시지 전달 GNN의 구별력이 같다는 정리를 한 절로 붙이고, 확산 지도와 랜덤 워크 관점, 그래프 위치 인코딩으로 닫는다. MATH-PLAN이 그래프를 뺀 근거가 '사이트에 GNN 글이 한 편도 없다'는 것이었고 실제로 GNN·그래프 신경망·라플라시안이 296편에 0건이므로, 이 글은 어떤 기존 글에도 기대지 않고 자체 완결적으로 쓴다. 중급 14번의 이차형식을 그래프 위에서 다시 쓰는 것이 출발점이다. | 스펙트럴 GNN(ChebNet·GCN)의 유도, 과평활화·과압착 분석 논문, 스펙트럴 군집화의 이론, 그래프 트랜스포머의 라플라시안 위치 인코딩, GraphRAG의 커뮤니티 탐지가 딛는 수학. 중급 12·14번. |

### A10 · 대칭과 등변성 (3편) 【고급 절단점 5 — 여기까지 49편이면 과학 AI 논문의 등변 층이 열린다】

| 슬러그 | 제목 | 무엇을 쓰는가 | 무엇이 열리는가 |
|---|---|---|---|
| `math-adv-groups-actions-and-representations` | 군·군작용·표현론: 대칭을 수학으로 적기 | 군과 부분군, 준동형, 군작용과 궤도·안정자군, 상군, 그리고 불변 f(g·x)=f(x)와 등변 f(g·x)=ρ(g)f(x)의 엄밀한 구분. 이미지의 평행이동군, 집합의 치환군 S_n, 3D 회전군 SO(3)과 SE(3)가 각각 어떤 군인지 대응시킨다. 후반부는 표현론: 군 표현과 동치, 기약 표현, 완전 가약성(마슈케 정리), 슈어 보조정리, 지표와 직교 관계, 유한군·콤팩트군에서의 푸리에 분해(A9-1의 푸리에가 순환군의 표현론이라는 관점). 리 군과 리 대수의 최소한과 지수 사상 — 중급 11번의 회전행렬 R(θ)가 SO(2)의 원소이고 exp(θJ)로 적힌다는 것. 중급 8·15번의 기저 분해가 대칭을 존중하는 방식으로 일반화된다. 증명 수위: 슈어 보조정리는 완전 증명. | geometric deep learning 프레임워크 논문이 첫 절에서 세우는 언어를 그대로 얻고, 다음 두 글의 구조 정리를 검증할 수 있게 된다. 중급 8·11·15번. |
| `math-adv-equivariant-networks` | 등변 신경망의 구조 정리: 등변 선형층은 왜 합성곱이 되는가 | 층 설계를 '등변 선형사상의 공간을 구하는 문제'로 환원하고, 평행이동 등변성이 본질적으로 합성곱을 강제한다는 정리를 증명한다. 치환 등변성이 DeepSets와 메시지 전달 형태를 강제한다는 결과, 군 합성곱(G-CNN)으로의 일반화와 회전군에 적용한 경우, 스티어러블 CNN의 일반 틀, 규격(gauge) 등변과 다양체 위의 합성곱, 그리고 등변 층을 설계할 때 무엇이 자유 파라미터로 남는지. 등변성을 구조로 강제하는 것과 데이터 증강으로 배우게 하는 것의 표본 효율 차이를 정량 비교하며 닫는다. 증명 수위: 평행이동 등변 ⇒ 합성곱은 완전 증명. | Cohen–Welling의 군 합성곱, 등변 아키텍처 논문의 'the most general equivariant linear map is…'라는 문장을 검증하게 된다. 기하 딥러닝(Bronstein 등)의 통합 관점. |
| `math-adv-spherical-harmonics-and-se3` | 구면 조화함수와 SE(3) 등변성: 3D 구조 모델의 수학 | 구면 위의 조화해석, 구면 조화함수가 SO(3)의 기약 표현이라는 것과 Wigner D-행렬, 스칼라·벡터·고차 텐서 특징이 각각 어떤 표현으로 변환되는지, 클렙시–고르단 계수로 두 표현의 텐서곱을 다시 기약 표현으로 분해하는 등변 층의 구성, 그리고 SE(3)에서 병진을 상대 좌표로 처리하는 방식. e3nn·SE(3)-Transformer 계열이 쓰는 연산을 하나씩 이 언어로 적고, 등변 층의 계산 비용이 차수 ℓ에 어떻게 의존하는지까지. 증명 수위: 구면 조화함수가 기약 표현이라는 것은 증명 개요, 클렙시–고르단 분해는 진술과 사용법. | e3nn·SE(3)-Transformer·AlphaFold의 구조 모듈·등변 분자 확산 모델 논문의 등변 층 정의를 읽게 된다. 과학 AI 논문에서 가장 자주 막히는 수학이다. |

### A11 · 표현의 기하 (2편) — 중급 11단원의 위층

| 슬러그 | 제목 | 무엇을 쓰는가 | 무엇이 열리는가 |
|---|---|---|---|
| `math-adv-compressed-sensing-and-superposition` | 압축 감지와 중첩: 희소 오토인코더가 딛고 선 정리 | 희소 신호를 미달결정 선형계에서 정확히 복원하는 조건, 제한 등거리 성질(RIP)과 무작위 행렬이 높은 확률로 그것을 만족한다는 결과(A3-1의 집중부등식), L0 대신 L1을 풀어도 같은 해가 나오는 이유의 기하(교차 다면체와 모서리 — A4-3의 soft-threshold가 그 알고리즘), 코히런스 기반 조건. 중급 66번의 존슨–린덴스트라우스와의 관계, 거의 직교하는 벡터를 d차원에 지수적으로 많이 넣을 수 있다는 계산(중급 65번의 고차원 기하가 배경), 그리고 그것이 '중첩 가설' — 특징 수가 차원보다 많아도 각 입력에서 소수만 활성이면 공존할 수 있다 — 에 주는 정확한 근거와 사전 학습의 식별 가능성 조건. 증명 수위: L1 복원의 기하는 완전 증명, RIP는 진술과 사용법. | Elhage 등의 toy models of superposition, 희소 오토인코더 기반 해석가능성 연구, 사전 학습의 복원 보장, Matryoshka 임베딩의 이론. 중급 65·66번. |
| `math-adv-representation-geometry-and-intrinsic-dimension` | 표현의 기하: 내재 차원, 유효 차원, 그리고 표현 비교의 함정 | 다양체 가설과 내재 차원 추정량(TwoNN·MLE·상관 차원)과 각각의 편향과 표본 요구량, 참여 비율과 스펙트럼 감쇠 지수로 표현의 유효 차원을 재기(중급 68번의 이방성 측정을 일반화). 후반부는 두 표현을 비교하는 지표들 — CCA·SVCCA·CKA·프로크루스테스 — 의 정의와 각각이 회전·스케일·아핀 중 무엇에 불변인지, 같은 두 모델에 대해 지표마다 다른 결론이 나오는 사례, 지표를 고르는 것이 곧 '무엇을 같다고 볼 것인가'를 고르는 것이라는 결론, 그리고 '서로 다른 모델의 표현이 수렴한다'는 주장을 검증할 때 필요한 통계적 주의. A13의 스케일링 법칙 이론이 여기서 잰 내재 차원을 지수로 쓴다. | 내재 차원과 스케일링 지수를 잇는 논문, CKA와 그 비판, Platonic Representation Hypothesis, 표현 유사도로 모델을 비교하는 해석가능성 연구. 중급 65·68번. |

### A12 · 유한한 기계의 이론 (2편) — 중급 12단원의 위층

| 슬러그 | 제목 | 무엇을 쓰는가 | 무엇이 열리는가 |
|---|---|---|---|
| `math-adv-backward-error-analysis` | 후방 오차 분석: '정확한 답'이 아니라 '가까운 문제의 정확한 답' | 전방 오차와 후방 오차의 구분, 조건수 × 후방 오차 = 전방 오차라는 골격, 부동소수점 연산의 표준 모델 fl(a op b)=(a op b)(1+δ)로 합산·내적·행렬곱의 오차 상계를 유도하기(√n·n 의존성이 어디서 나오는가). 후방 안정 알고리즘의 정의와 QR·LU·촐레스키의 안정성 비교, 혼합정밀도 누산의 오차 모델과 fp16 입력·fp32 누산이 실제로 무엇을 보장하는지, 확률적 반올림이 오차 상계를 n에서 √n으로 낮추는 논증, 텐서 코어의 내부 정밀도가 결과에 미치는 영향. 중급 70번이 관찰한 상쇄와 71번의 안정한 패턴들에 정량적 근거를 붙인다. 증명 수위: 합산·내적의 오차 상계는 완전 증명. | Higham의 정확도·안정성 분석과 혼합정밀도 논문, 확률적 반올림 연구, fp8·bf16 저정밀 학습 논문의 오차 분석 절. 중급 70·71번. |
| `math-adv-io-complexity-and-lower-bounds` | I/O 복잡도와 하한: FlashAttention이 최적이라는 논증 | 행렬곱의 지수 ω와 Strassen류 알고리즘이 실무에서 왜 잘 안 쓰이는지(수치 안정성과 상수), Hong–Kung의 red-blue pebble 게임과 캐시 크기 M에서 행렬곱의 I/O 하한 Ω(n³/√M)의 논증 구조, 통신 회피 알고리즘과 타일링이 그 하한에 맞닿는다는 것. 같은 틀로 어텐션의 I/O 하한을 세우고 그 하한이 달성 가능하다는 것을 보인다. roofline 모델과 산술 강도, 그리고 디코딩이 왜 연산이 아니라 메모리 대역폭에 묶이는지를 산술 강도로 계산하기. 기존 transformer-flash-attention이 'IO가 병목'이라는 관찰과 타일링·online softmax 구현을 이미 다루고 중급 42번이 online softmax의 대수를, 73번이 FLOPs 세기를 맡으므로, 이 글은 오직 하한의 증명 구조와 산술 강도만 맡는다. 증명 수위: red-blue pebble 하한은 증명 개요 + 핵심 보조정리. | FlashAttention 1·2·3 논문의 복잡도 절, 통신 회피 선형대수 논문, roofline 기반 성능 분석, 추론 최적화 논문의 대역폭 논증. 중급 42·73번. |

### A13 · 측정의 보장 (6편) 【고급 절단점 6 — 여기까지 59편이면 평가와 보장의 이론이 닫힌다】

| 슬러그 | 제목 | 무엇을 쓰는가 | 무엇이 열리는가 |
|---|---|---|---|
| `math-adv-sequential-testing-and-e-values` | 순차 검정과 e-값: 계속 보면서도 틀리지 않기 | 고정 표본 검정을 중간에 들여다보면 1종 오류가 부풀어 오르는 계산을 시뮬레이션으로 먼저 보이고, 검정 마팅게일과 빌의 부등식(A1-5의 옵셔널 스토핑), e-값과 e-과정의 정의와 곱으로 증거를 누적하는 성질, 항시 유효(anytime-valid) 신뢰수열의 구성. 옵셔널 스토핑 아래 유효한 순차 우도비 검정(SPRT)과 그 기대 표본 수, 혼합 SPRT, e-값 기반 FDR 통제. 그리고 벤치마크를 배치로 늘려 가며 '언제 멈춰도 되는가'를 판정하는 절차를 중급 74~76번의 고정 표본 결과와 나란히 계산해 표본 비용 차이를 낸다. 증명 수위: 빌의 부등식과 e-값의 유효성은 완전 증명. | e-값·신뢰수열의 최근 논문(Ramdas 등), 순차 A/B 테스트 방법론, 평가를 점진적으로 돌리는 LLM 벤치마킹, 모니터링 중 유의성을 판정해야 하는 모든 실무 상황. 중급 74·76번. |
| `math-adv-conformal-prediction` | 적합 예측: 분포 가정 없이 보장되는 예측 집합 | 교환가능성 가정만으로 유한 표본 커버리지 P(y ∈ C(x)) ≥ 1−α를 주는 split conformal의 구성과 그 짧은 완전 증명, 비적합 점수의 선택이 집합 크기에 하는 일, 분류에서 예측 집합·회귀에서 구간을 만드는 법, 적응적 집합(APS·RAPS). 주변 커버리지와 조건부 커버리지의 차이와 조건부 보장이 분포 무관하게는 불가능하다는 정리, 그룹별 보정과 Mondrian conformal, 분포 이동 아래의 가중 conformal, conformal risk control, 그리고 LLM 출력에 붙일 때 점수 함수를 로그확률·자기평가·검색 근거 중 무엇으로 잡을지. 중급 79번의 보정이 '확률값의 정확성'이라면 이쪽은 '집합의 보장'이라는 대비를 첫 절에서 세운다. | Angelopoulos–Bates의 conformal prediction, conformal risk control, LLM 거절·라우팅·선택적 예측에 커버리지 보장을 붙이는 최근 논문. 실무에서 곧바로 쓸 수 있는 보장 도구다. 중급 74·79번. |
| `math-adv-causal-inference-and-interventions` | 인과추론: 상관을 넘어서 개입을 재기 | 잠재 결과 틀과 인과 효과의 정의, 교환가능성·일치성·양의성 가정, 무작위 배정이 정확히 무엇을 보장하는지, 성향 점수와 역확률 가중·이중 강건 추정, 도구변수. 구조적 인과 모형과 인과 그래프, d-분리, 개입 분포 p(y\|do(x))와 관측 분포의 차이, 백도어·프런트도어 기준, do-계산의 세 규칙과 식별 가능성 판정, 충돌자를 통제하면 오히려 생기는 편향, 매개 분석. 응용은 전부 AI 평가다 — 로그 데이터로 새 정책을 평가하는 오프라인 정책 평가, 사용자가 본 것만 관측되는 선택 편향, 그리고 활성값 패칭과 절제 실험이 정확히 do-개입이라는 관점에서 해석가능성 실험을 다시 읽기. 벤치마크 상관에서 인과를 주장할 때 필요한 조건을 표로 정리하며 닫는다. | Pearl의 do-계산, 해석가능성의 인과적 매개 분석(causal tracing·activation patching), 오프라인 정책 평가(OPE), 추천·검색의 편향 보정. 중급 76·79번. |
| `math-adv-heavy-tails-and-extreme-values` | 무거운 꼬리와 극단값: 평균이 의미를 잃는 곳 | 무거운 꼬리의 정의(정칙 변동)와 파레토·멱법칙, 유한 분산이 없으면 중심극한정리 대신 안정 분포와 일반화 CLT가 나온다는 것, 그래서 표본평균과 표준오차가 무의미해지는 자리 — 중급 21번의 분산 가법성과 74번의 √(p(1−p)/n)가 전제하는 것이 정확히 어디서 깨지는지. 멱법칙 적합의 함정과 Clauset–Shalizi–Newman 절차(x_min 선택, KS 거리, 로그-로그 회귀가 왜 편향되는가), Hill 추정량. 극단값 이론의 세 유형(Gumbel·Fréchet·Weibull)과 블록 최댓값·문턱 초과(POT) 모형, 그리고 최악 사례 지연시간·안전 사건 빈도·SGD 그래디언트 잡음의 꼬리를 실제로 추정하기. 중급 80번의 멱법칙 적합 통계와는 다른 축 — 저쪽은 곡선 적합이고 이쪽은 꼬리 자체의 추론이다. | Clauset 등의 멱법칙 적합 논문, SGD 잡음이 무거운 꼬리라는 연구, heavy-tailed self-regularization 스펙트럼 진단(A2-2), 안전·신뢰성 평가의 꼬리 위험. 중급 21·74·80번. |
| `math-adv-scaling-law-theory` | 스케일링 법칙의 이론: 지수가 어디서 나오는가 | 데이터 다양체의 내재 차원 d에서 손실 지수 α ≈ 4/d가 나오는 근사 이론 논증(A11-2에서 잰 내재 차원이 여기 들어간다), 무작위 특징·커널 회귀 모형에서 스펙트럼 감쇠 지수가 손실 곡선의 지수를 정한다는 정확한 계산(A5-2와 A3-3의 결과 재사용), 세 축을 결합한 L = A/N^α + B/D^β + E의 형태와 최적 배분(치칠라 조건)이 라그랑주 한 줄로 나오는 과정. 관측 지수를 적합할 때의 통계적 함정(가중, 오차 구조, 환원 불가 항 E의 식별 불안정)과 두 자릿수 밖 외삽이 깨지는 이유. 중급 80번은 곡선을 적합하고 해석하는 통계를, 기존 llm-scaling-laws는 Kaplan·Chinchilla의 경험 법칙을, llm-emergent-abilities는 창발이 측정 방식의 산물인지의 논쟁을 이미 맡고 있으므로, 이 글은 오직 '왜 그 함수 형태이고 지수가 무엇에서 나오는가'만 맡는다. | Sharma–Kaplan의 차원 논증, Bahri 등의 스케일링 법칙 이론, Kaplan·Chinchilla 적합 절차의 재분석. 중급 50·80번. |
| `math-adv-differential-privacy-accounting` | 차등 프라이버시의 수학: (ε,δ)·레니 발산·모멘트 회계 | (ε,δ)-DP의 정의와 프라이버시 손실 확률변수, 민감도 계산과 라플라스·가우시안 메커니즘, 기본 조합 정리와 그것이 왜 느슨한지, 고급 합성, 레니 DP와 모멘트 회계로 얻는 훨씬 좁은 경계, 부분표집 증폭. 그리고 DP-SGD를 분해해 그래디언트 클리핑 상한 C와 잡음 배율 σ가 각각 ε에 어떻게 들어가는지, 학습 스텝 수가 프라이버시 예산을 어떻게 소모하는지 실제로 계산한다. 기존 ai-privacy는 차분 프라이버시를 개념과 규제·연합학습·동형암호와 나란히 한 절로 소개하는 글이므로, 이 글은 회계의 수학만 맡는다. 증명 수위: 가우시안 메커니즘과 레니 DP 변환은 완전 증명. | DP-SGD 논문의 ε 계산을 스스로 재현하고 프라이버시 회계 도구(Opacus·TF-Privacy)의 파라미터가 무엇을 뜻하는지 판정하게 된다. 기억화·멤버십 추론 감사 논문의 위협 모형. |

### A14 · 정렬과 의사결정의 이론 (4편) 【고급 절단점 7 — 63편으로 완결】

| 슬러그 | 제목 | 무엇을 쓰는가 | 무엇이 열리는가 |
|---|---|---|---|
| `math-adv-mdp-bellman-and-trust-region` | MDP와 벨만 작용소: 축약 사상 하나로 증명되는 것들 | 마르코프 결정 과정의 형식적 정의, 벨만 기대·최적 작용소가 sup 노름에서 γ-축약이고 A1-1의 바나흐 고정점 정리로 유일한 해를 준다는 완전 증명, 값 반복·정책 반복의 수렴과 정책 개선 정리, 함수 근사가 들어올 때 축약이 깨져 발산하는 치명적 삼요소. 그리고 성능차 보조정리(두 정책의 성능 차이를 어드밴티지의 기댓값으로 적는 항등식)의 유도와 거기서 나오는 TRPO의 단조 개선 하한, 신뢰영역 반지름과 KL 제약의 대응(A4-3의 거울 하강과 A6-3의 자연그래디언트가 같은 자리에서 만난다), GAE의 λ가 편향과 분산을 맞바꾸는 계산, 토큰 단위 MDP로 본 RLHF에서 무엇이 상태이고 무엇이 종료 보상인지. 기존 rl-basics·rl-q-learning·rl-ppo는 개념과 구현이므로 이 글은 정리와 증명만 맡고, 중급 52~58번이 목적식과 추정량의 성질을 맡으므로 여기서는 그 위의 보장만 맡는다. | TRPO·PPO 원 논문의 정리 절, GAE 논문, 오프라인 RL의 분포 시프트 경계, RLHF를 MDP로 정식화하고 토큰 수준 신용 할당을 다루는 정렬 논문. 중급 52·54·58번. |
| `math-adv-bandits-and-regret` | 밴딧과 후회 경계: 탐색의 값을 정확히 계산하기 | 확률적 다중 손잡이 밴딧과 후회의 정의, UCB의 신뢰폭이 A3-1의 집중부등식에서 유도되는 과정과 O(log T) 후회 상계의 완전 증명, Lai–Robbins 하한(그 이상 잘할 수 없다는 것), 톰슨 표집의 베이즈적 유도와 경험적 우위, 선형 문맥 밴딧(LinUCB), 적대적 설정의 EXP3와 그것이 A4-3의 지수가중 갱신이라는 것. 순수 탐색·최선 팔 식별 설정과 그것이 모델 선택·프롬프트 선택 문제와 같다는 관점. | 탐색 전략·능동 학습·모델 라우팅·RLHF 데이터 수집의 적응적 할당 논문, 후회 경계 언어로 쓰인 온라인 학습 문헌. A13-1의 순차 검정과 짝을 이룬다. |
| `math-adv-minimax-games-and-equilibria` | 최소최대 게임과 균형: 자기대전이 수렴한다는 말 | 정규형 게임과 내시 균형, 안장점 문제와 미니맥스 정리, 2인 영합 게임의 미니맥스가 선형계획 쌍대성과 같다는 대응(A4-2), 동시·교대 경사법이 회전하며 발산할 수 있다는 2×2 계산(고윳값이 순허수), 외삽(extragradient)과 낙관적 갱신이 마지막 반복 수렴을 주는 이유, 변분 부등식과 단조 작용소 관점. 후회 최소화와 평균 전략이 균형에 수렴한다는 정리, 곱셈 가중치 갱신이 거울 하강이라는 것, 확장형 게임과 CFR의 최소한, 그리고 비볼록–비오목 설정에서 국소 안장점·Stackelberg 균형의 구분과 무엇을 목표로 삼아야 하는가. 생성자–판별자와 자기대전 정렬을 같은 언어로 다시 적는다. 증명 수위: 미니맥스 정리와 회전 발산 계산은 완전 증명. | GAN 수렴·발산 분석, Nash-RLHF와 SPPO의 수렴 정리, 적대적 강건 최적화, 다중 에이전트 학습의 균형 논의. 중급 57·58번. |
| `math-adv-social-choice-and-preference-aggregation` | 선호를 하나의 보상으로 접을 때 잃는 것 | 사회 선택 이론의 틀, 애로 불가능성 정리와 깁바드–새터스웨이트 정리의 서술과 증명 개요, 콩도르세 역설과 다수 선호가 비순환이 아닐 수 있다는 사실, Borda·Kemeny 집계와 그 계산 복잡도. Plackett–Luce로 순위 데이터를 모델링하기, 평가자 간 이질성을 혼합 모형으로 다루기, 그리고 중급 55번의 Bradley–Terry와 78번의 Elo가 전제하는 이행성이 실제 선호 데이터에서 얼마나 깨지는지를 측정하는 절차. 폰 노이만 승자·최소최대 승자 같은 대안 해 개념과 그것이 A14-3의 미니맥스 게임으로 이어지는 자리. 사이트에 정렬 글이 14편 있는데 '여러 사람의 선호를 스칼라 보상 하나로 접는 것이 무엇을 잃는가'를 다룬 글이 한 편도 없는 자리를 메운다. | Nash Learning from Human Feedback, 자기대전 선호 최적화(SPO·SPPO), 다원적 정렬과 보상 모델의 한계를 다루는 논문. 중급 55·78번. |

---

## 읽기와 쓰기 순서

## 읽기 순서 — 길은 하나다

세로 축은 셋을 이어 붙인 하나다 — 초급 48편 → 중급 80편 → 고급 63편. 191편이 한 줄로 선다.

- **초급은 1번부터 48번까지 순서대로 읽는다.** 앞 글이 뒤 글의 전제이고, 각 편의 brief에 「전제」 칸이 그 자리를 적어 둔다. 자기 수준을 판정하고 들어가는 안내 글은 두지 않는다 — 1번부터 읽으면 된다. 계산을 이미 하는 사람이 앞을 건너뛰는 것은 자유지만 그 판단을 문서가 대신하지 않는다.
- **중급은 1번부터 아무 선행 없이 시작한다.** 초급 48편 중 어느 것도 중급 1번의 선행 조건은 아니다. 초급을 건너뛰고 중급부터 읽어도 길이 끊기지 않게 짜여 있고, 막힌 자리에서 초급으로 내려가는 문은 대응 배지가 연다.
- 고급은 **중급 80번을 마친 뒤**다. 고급 안에서만 A1 → A14 순서를 지킨다. A1(5편)이 유일한 도구 단원이고, 나머지 도구는 그것을 쓰는 단원 안에 배치했다.
- 초급·고급 ↔ 중급 대응은 `src/data/curriculum.ts`의 `mathSupport` 한 곳에만 둔다. 초급 글에는 「이 글이 받치는 중급」, 고급 글에는 「이 글이 확장하는 중급」이 붙는다.
- 완주율과 진행 막대의 **분모는 중급 80편만**이다. 초급에는 진행 표시를 두지 않고(순서는 있지만 완주를 요구하지 않는다), 고급은 단원별 완료 뱃지만 준다.
- 절단점: 중급은 8·17·31·39·43·51·58편. 고급은 10편(보장 도구) · 27편(목적식의 유도) · 36편(확산·흐름 매칭의 완결) · 46편(시퀀스와 그래프) · 49편(등변성) · 59편(측정의 보장) · 63편(완결). **초급에는 절단점을 두지 않는다** — 한 편이 한 계단만 오르는 과정이라 중간에서 끊으면 그 위가 열리지 않는다.

## 쓰기 순서 — 세 구간, 38일

**루틴은 초급 → 중급 → 고급 순서로 쓴다. 섞지 않는다.** 2026-08-05에 뒤집었다. 그날 중급 1~5번이 나갔는데 다섯 편 모두 `level: 중급`이라 "기초부터라면서 난이도가 높다"는 지적이 나왔고, 그 지적이 맞다. 나간 5편은 되돌리지 않는다 — 접두사 규칙상 중급이 맞고, 앞으로의 순서만 바꾸면 된다.

**뒤집으면 걸리는 것은 링크 하나뿐이고, 해법은 링크를 걸지 않는 것이다.** `src/routes.test.ts`의 내부 링크 검사는 *존재하는 `.md`에서 나가는* 링크만 훑으므로, 순서를 뒤집는 것 자체로는 깨지지 않는다. 깨지는 경우는 하나 — 먼저 나간 초급 원고 안에 아직 없는 중급 슬러그로 나가는 인라인 링크가 있을 때다. 그래서 규칙을 이렇게 못 박는다.

> **초급·고급 글은 본문에서 아직 쓰지 않은 글로 나가는 인라인 링크(`](/articles/math-...)`)를 쓰지 않는다.** 다른 트랙을 가리켜야 할 때는 「중급 12번 · 고윳값과 고유벡터」처럼 번호와 제목만 텍스트로 적는다. 눌러서 이동하는 링크는 `src/data/curriculum.ts`의 대응 데이터가 그리는 배지가 맡고, 그 배지는 실제 `.md`가 있는 슬러그만 렌더한다. 링크를 걸어도 되는 것은 **쓰는 시점에 이미 있는 글**과 **같은 날 배치에 함께 쓰는 글** 둘뿐이다(2026-08-05 배치가 이미 그렇게 나갔다 — 1번이 2번을, 4번이 5번을 링크한다). 기존 306편은 전부 실재하므로 자유롭게 건다. 슬래시(`/articles/x/`)나 앵커(`/articles/x#y`)를 붙여 검사를 피하지 않는다 — 검사는 통과하지만 죽은 링크가 남는다. 그 우회를 막으려고 검사 정규식을 두 형태까지 잡도록 넓혀 두었다.

같은 트랙 안에서는 사정이 다르다. **초급은 1번부터 순서대로 쓰므로 앞 번호를 링크하는 것은 언제나 안전하다** — 각 편의 「전제」 칸에 적힌 글은 그 글을 쓰는 날에 이미 나가 있다. 뒤 번호는 링크하지 않고 「23번 · 이차함수와 포물선」처럼 적는다.

**배지는 2026-08-05에 넣었다.** 1구간이 시작되기 전에 끝내야 했던 셋이 전부 코드에 있다.

- (a) 대응 데이터 — `src/data/curriculum.ts`의 `mathSupport`. 키는 초급·고급 슬러그, 값은 **중급 번호 배열**이고 첫 번호가 대표다. 슬러그가 아니라 번호인 이유는 원고가 중급을 부르는 단위가 번호이기 때문이다 — 원고에 적힌 「중급 12번」과 데이터의 `12`가 같은 것을 가리켜야 한다. **초급 48편 표에는 「중급 대응」 칸을 두지 않는다** — brief는 트랙 안에서의 「전제」만 적고, 어느 중급을 받치는가는 데이터 한 곳에만 있다. 고급 표는 대응 칸을 그대로 갖는다. 고급 여섯 편(마르코프 연쇄·랑주뱅·등변 신경망·구면조화함수·DP 회계·밴딧)은 대응 칸이 중급을 하나도 부르지 않아 뺐다.
- (b) 양방향 블록 — `src/pages/ArticlePage.tsx`의 `CurriculumLinks`가 본문 옆 사이드바에 그린다. 중급 글에는 「막히면 먼저」(초급)와 「더 깊이」(고급), 초급 글에는 「이 글이 받치는 중급」, 고급 글에는 「이 글이 확장하는 중급」이다. 중급 쪽 항목에는 「중급 12번」이라는 번호를 함께 찍는다 — 원고 본문에 적힌 텍스트와 같은 이름으로 보여야 눌러서 가는 곳이 그것임을 안다.
- (c) 실재 필터 — 같은 컴포넌트가 `getArticleBySlug`로 거른다. 아직 `.md`가 없는 슬러그는 조용히 빠지고 그 글이 나가는 날 저절로 채워진다. 한 무리가 통째로 비면 제목줄까지 렌더하지 않는다.

`src/data/curriculum.test.ts`가 키·값의 소속과 번호 범위, 그리고 양방향이 맞물리는지를 검사한다.

- **1구간 · 1~10일, 2026-08-06 ~ 08-15 (초급 48편, 5편/일)** — 1번부터 48번까지 번호 순서 그대로다. 예외는 없다. 앞 글이 뒤 글의 전제이므로 배치를 따로 짤 이유가 없고, 짜면 오히려 전제가 빈다. 마지막 날(08-15)은 3편이다.
- **2구간 · 11~25일, 08-16 ~ 08-30 (중급 75편, 5편/일)** — 2026-08-05에 나간 1~5번 다음인 `math-dot-product-and-cosine`부터 `mathCurriculum` 순서 그대로. 한 글자도 바꾸지 않는다.
- **3구간 · 26~38일, 08-31 ~ 09-12 (고급 63편, 5편/일)** — A1 → A14 순서 고정. 고급은 앞 단원을 이름으로 참조하므로(A1-2의 리스 표현 → A5-1의 재생 성질, A1-1의 축약사상 → A7-8의 Sinkhorn과 A14-1의 벨만, A4-2의 켤레 → A6-1·A7-7) 단원을 건너뛰고 쓰면 참조가 빈다. 고급은 여전히 맨 뒤다 — 중급을 이름으로 참조하고 읽는 순서도 중급을 마친 뒤라 앞으로 당길 이유가 없다. 마지막 날(09-12)은 3편이다.

합계 10 + 15 + 13 = 38일, 186편이다(초급 48 + 중급 75 + 고급 63 — 이미 나간 중급 5편은 뺀 수다). 초급이 23편에서 48편으로 늘어 1구간이 5일에서 10일이 되었고, 전체가 33일에서 38일이 되었다.

## 루틴이 지킬 규칙

1. **슬러그 접두사가 트랙을 정한다.** `math-basics-` = 초급(`level: "초급"`), `math-` = 중급(`중급`), `math-adv-` = 고급(`고급`). 셋 다 `category: "math-for-ai"`. 이 규칙을 CLAUDE.md와 MATH-PLAN.md에 한 줄씩 박는다. `tags`에는 트랙 이름(`초급`·`중급`·`고급`)을 첫 태그로 넣되, **트랙을 정하는 것은 태그가 아니라 접두사다** — 태그는 목록에서 같은 트랙끼리 묶어 보는 용도일 뿐이다. 이미 나간 다섯 편의 태그도 2026-08-05에 어휘를 통일하며 옛 이름에서 `중급`으로 바꿨다 — 낱말만 바꿨고 본문은 그대로다.
2. **brief의 카브 문장을 어긴 원고는 다시 쓴다.** "…는 중급 N번의 것이다", "기존 ○○이 맡는다"는 금지 목록이지 참고 사항이 아니다. 초급 brief의 "…는 N번이 세운다"도 같은 무게다 — 소유자가 정해진 것을 두 편이 세우지 않는다.
3. **고급은 brief의 증명 수위를 지킨다.** '완전 증명'으로 표시된 정리는 반드시 끝까지 쓴다 — 그것이 "논문 이론 절을 스스로 읽게 된다"를 검증 가능하게 만드는 유일한 장치다.
4. **초급은 1번부터 순서대로 쓰고, 글 머리에 자가진단을 두지 않는다.** '이 글이 필요한지 확인' 같은 접이식 문제로 시작하지 않고 독자를 다른 트랙으로 돌려보내지도 않는다. 한 편에 하나를 두면 48편 머리 전부에서 자가진단이 되살아나 「읽는 사람이 자기 수준을 판정할 필요가 없다」는 이 트랙의 전제가 뒤집힌다. 앞 글을 되짚는 것은 「전제」 칸에 적힌 번호를 본문에서 이름으로 부르는 것으로 한다.
5. **아직 커밋되지 않은 글은 본문에서 링크하지 않는다.** `](/articles/<slug>)`가 가리켜도 되는 것은 (a) 이미 나간 `.md`, (b) 같은 날 배치에 함께 쓰는 글 둘뿐이다. 아직 없는 글은 「중급 12번 · 고윳값과 고유벡터」처럼 번호와 제목만 텍스트로 적는다. 슬래시나 앵커를 붙여 `src/routes.test.ts`의 검사를 피하지 않는다 — 그 우회는 정규식을 넓혀 막아 두었다. 초급을 먼저 쓰기로 한 이상 이 규칙이 순서를 지탱하는 유일한 장치다.
6. **초급·고급 ↔ 중급 대응은 본문 링크가 아니라 `src/data/curriculum.ts`의 데이터가 그린다.** 원고 안에서는 번호와 제목만 적고, 실제로 눌러 이동하는 배지는 데이터에서 렌더한다. 대응이 바뀌면 데이터를 고치지 원고를 고치지 않는다.
7. `src/data/curriculum.test.ts`가 세 배열의 슬러그 중복 없음, 접두사 일관성, 그리고 **쓰는 순서의 불변식 셋**(초급 48편이 맨 앞 블록 · 중급이 가운데 블록 · 고급이 맨 뒤 블록)을 검사한다. 배열 deep-equal로 두지 않는 이유는 그것이 결정을 못 박는 대신 순서를 바꿀 때마다 갱신해야 하는 사본이 되기 때문이다. `mathSupport` 대응도 여기서 검사한다 — 키가 초급·고급에 있음, 값이 중급 번호 1~80 안임, 초급 48편이 전부 대응을 가짐, 그리고 뒤집은 방향이 맞물림.
8. **`curriculumOrder()`는 `mathWritingOrder`에서의 자리를 역순으로 돌려준다.** 화면은 최신 글이 맨 위이므로 고급 마지막 편이 가장 작은 수를, 초급 1번이 가장 큰 수를 받는다. 트랙마다 오프셋(초급 `1000+i` · 중급 `i` · 고급 `3000+i`)을 주어 화면을 중급 → 초급 → 고급으로 세우던 방식은 걷어냈다 — 같은 날 나간 다섯 편의 `pubDate`가 같아 목록 안에서 순서가 잡히지 않았고, 쓰는 순서와 무관한 화면 순서를 따로 유지할 이유도 없었다. `src/data/articles.ts`의 분기는 `order: 0`을 falsy로 버리지 않도록 `order !== undefined`여야 한다(이미 그렇게 돼 있다).
9. **초급 원고는 AI와 엮지 않는다.** 본문에 AI 예시·코드·용어를 넣지 않고 "나중에 어텐션에서 쓰입니다" 같은 예고도 넣지 않는다. 어디에 쓰이는지는 대응 배지가 맡는다. 분량은 5,000~9,000자이고 한 편이 '세우는 것'은 여섯 안쪽이다 — 상한을 넘길 것 같으면 항목을 접지 손계산을 지우지 않는다. 코드와 대조하는 것이 허용되는 자리는 −1단계 세 편(1·2·3번)과 48번의 SymPy뿐이다.
