---
title: "LLM 트레이싱 — 한 번의 응답이 어디서 시간을 쓰는가"
description: "LLM 애플리케이션은 요청 하나가 검색·모델 호출·툴 실행을 오가서 평균 지표만으로는 원인을 못 찾습니다. 스팬을 어떻게 나누고 무엇을 속성으로 남기며, 샘플링과 민감 정보를 어떻게 다루는지 정리합니다."
author: "PALDYN Team"
pubDate: "2026-09-03"
category: "ml-ops"
level: "중급"
tags: ["트레이싱", "관측성", "LLMOps", "OpenTelemetry", "디버깅"]
featured: false
draft: false
---
[지난 글](/articles/mlops-drift-detection)에서 배포된 모델이 조용히 틀려 가는 것을 어떻게 알아채는지 다뤘다. 그 글은 예측 품질을 봤는데, LLM 애플리케이션에는 그 앞에 놓인 문제가 하나 더 있다. **응답이 왜 느린지, 왜 이상한 답이 나왔는지를 사후에 재구성할 수 없다는 것**이다.

전통적인 웹 서비스라면 요청 하나가 함수 몇 개를 지나 DB를 한두 번 치고 끝난다. LLM 애플리케이션은 다르다. 사용자의 한 문장이 들어오면 질의를 다시 쓰고, 벡터를 만들고, 문서를 검색하고, 리랭커를 돌리고, 프롬프트를 조립해 모델을 부르고, 모델이 툴을 부르겠다고 하면 툴을 실행하고, 그 결과를 다시 넣어 또 부른다. 이 중 어디서 3초가 샜는지를 평균 응답 시간 그래프는 알려 주지 않는다.

## 요청 하나를 조각으로 나눈다

**트레이싱**(tracing)은 요청 하나가 시스템을 지나는 경로를 조각으로 쪼개 각 조각의 시작·끝 시각과 맥락을 함께 기록하는 방식이다. 그 조각 하나를 **스팬**(span)이라 부르고, 한 요청에 속한 스팬 전체를 **트레이스**(trace)라 부른다.

스팬은 부모·자식 관계를 갖는다. "요청 전체"라는 스팬 아래에 "검색"이 있고, 그 아래에 "임베딩"과 "벡터 검색"이 있는 식이다. 이 관계 덕분에 시간이 어디에 잠겨 있는지가 계층째 보인다.

![RAG 에이전트 요청 한 건의 스팬 분해](/assets/posts/llmops-tracing-span-gantt.svg)

이 그림이 트레이싱의 값어치를 한 장으로 보여 준다. 평균 지연이 2.8초라는 것만 알면 팀은 대개 검색 파이프라인을 만지기 시작한다 — 조각이 많고 손댈 데가 많아 보이기 때문이다. 그런데 검색 전체가 320 ms다. 거기서 절반을 줄여도 전체는 5.6%밖에 안 빨라진다. **실제로 손댈 자리는 1,900 ms를 쓴 첫 LLM 호출**이고, 답은 출력 길이를 줄이거나 더 작은 모델로 보내거나 스트리밍으로 체감 지연을 바꾸는 쪽에 있다.

트레이싱이 없으면 이 판단을 감으로 한다. 그리고 감은 대개 "복잡해 보이는 쪽"을 고른다.

## 메트릭·로그와 무엇이 다른가

이미 로그도 쌓고 대시보드도 있는데 왜 또 하나를 붙이냐는 질문이 자연스럽다. 셋은 겹치는 부분이 있지만 답하는 질문이 다르다.

![메트릭·트레이스·로그가 각각 답하는 질문](/assets/posts/llmops-tracing-three-signals.svg)

메트릭만 있으면 "어제보다 p95가 1.2초 늘었다"까지는 안다. 거기서 더 못 간다. 로그만 있으면 개별 요청의 값은 보이지만, **요청 하나에 속한 줄들을 다시 이어 붙일 방법이 없다.** 특히 비동기로 도는 부분이 있으면 로그가 시간순으로 뒤섞여 어느 줄이 어느 요청 것인지 구분이 안 된다.

트레이스가 하는 일이 정확히 그 이어 붙이기다. 모든 스팬이 같은 트레이스 ID를 달고 다니므로, 나중에 그 ID로 모으면 요청 하나의 전체 여정이 복원된다.

## 무엇을 스팬으로 만들 것인가

스팬을 너무 잘게 쪼개면 저장 비용과 화면의 소음이 늘고, 너무 굵게 잡으면 원인이 안 좁혀진다. 실용적인 기준은 하나다 — **밖으로 나가는 호출과 시간이 걸리는 계산에 각각 스팬을 준다.**

| 스팬으로 만든다 | 만들지 않는다 |
| --- | --- |
| 모델 호출 한 건 | 문자열 포매팅, 딕셔너리 조립 |
| 벡터 검색, 리랭킹, DB 조회 | 리스트 슬라이싱 같은 순수 계산 |
| 툴·외부 API 실행 한 건 | 로깅 자체 |
| 가드레일·검증 단계 | 설정 읽기 |
| 재시도 한 번 한 번 | 재시도 전체를 뭉뚱그린 하나 |

마지막 줄이 실무에서 자주 놓치는 지점이다. 재시도를 부모 스팬 하나로 감싸 두면 "모델 호출 4.2초"로 보이는데, 실제로는 1.4초짜리 호출이 세 번 일어난 것일 수 있다. **재시도마다 스팬을 따로 열어야 지연의 원인이 모델이 느려서인지 계속 실패해서인지가 갈린다.**

## 스팬에 무엇을 붙일 것인가

스팬은 시간만 재는 물건이 아니다. 속성을 붙일 수 있고, 실제 디버깅 값어치의 절반은 여기서 나온다. LLM 스팬이라면 이 정도는 남긴다.

```python
with tracer.start_as_current_span("llm.generate") as span:
    span.set_attribute("llm.vendor", "anthropic")
    span.set_attribute("llm.model", "claude-sonnet-5")
    span.set_attribute("llm.temperature", 0.2)
    span.set_attribute("llm.prompt_version", "answer_v7")
    span.set_attribute("app.retrieved_doc_ids", ",".join(doc_ids))

    response = client.messages.create(...)

    usage = response.usage
    span.set_attribute("llm.input_tokens", usage.input_tokens)
    span.set_attribute("llm.output_tokens", usage.output_tokens)
    span.set_attribute("llm.cache_read_tokens", usage.cache_read_input_tokens)
    span.set_attribute("llm.stop_reason", response.stop_reason)
```

이 중 몇 개는 왜 있는지가 바로 안 보일 수 있어 짚어 둔다.

**프롬프트 버전**이 가장 값어치가 높다. 프롬프트를 고친 뒤 품질이 나빠졌을 때, 이 속성이 없으면 "언제부터 나빠졌나"를 배포 로그와 대조해 손으로 맞춰야 한다. 있으면 버전별로 잘라 비교하면 끝이다.

**캐시로 읽은 토큰 수**는 비용 분석의 핵심이다. 프롬프트 캐싱을 켜 두고도 실제로 히트하는지 확인하지 않는 경우가 흔한데, 프롬프트 앞부분이 요청마다 미세하게 달라지면 캐시가 통째로 빗나간다. 이 값이 계속 0이면 그 상황이다.

**중단 이유**(stop reason)는 품질 문제의 조용한 원인이다. 최대 토큰에 걸려 답이 잘린 것과 모델이 스스로 끝낸 것은 사용자 눈에는 둘 다 "답이 이상하다"로 보인다.

**검색해 온 문서 ID**는 RAG에서 답이 틀렸을 때 모델 탓인지 검색 탓인지를 가른다. 엉뚱한 문서를 물어다 줬으면 프롬프트를 아무리 고쳐도 안 낫는다.

그리고 **오류는 반드시 스팬에 기록한다.** 예외를 잡아서 기본값을 돌려주고 지나가는 코드가 있으면, 그 실패는 어디에도 안 남는다. 스팬 상태를 오류로 표시하고 예외를 붙여 두면 성공한 요청 안에 숨어 있던 실패가 드러난다.

## 프롬프트와 응답 본문을 남길 것인가

여기가 판단이 갈리는 자리다. 프롬프트 전문과 응답 전문이 있으면 디버깅이 압도적으로 쉽다. 동시에 그것은 **사용자가 입력한 내용 전부**이기도 하다.

세 가지를 나눠 정하는 편이 깔끔하다.

1. **무엇을 남기는가** — 전문을 남길지, 앞뒤 일부만 남길지, 해시만 남길지. 사내 도구와 고객 데이터를 다루는 서비스는 답이 다르다
2. **어디에 남기는가** — 스팬 속성에 넣으면 트레이스 저장소가 통째로 개인정보 저장소가 된다. 본문은 접근 권한이 따로 걸린 곳에 두고 스팬에는 참조 키만 두는 구성이 안전하다
3. **얼마나 오래 두는가** — 트레이스 메타데이터는 몇 달을 둬도 가볍지만 본문은 다르다. 본문만 보관 기간을 짧게 잡는다

그리고 남기기로 했다면 **저장 직전에 마스킹을 건다.** 이메일·전화번호·주민등록번호 형태를 정규식으로 걸러 내는 정도라도 없는 것보다 훨씬 낫다. 사후에 지우는 것은 훨씬 어렵다 — 이미 인덱싱됐고 백업에도 들어가 있다.

## 전부 남길 수는 없다

트래픽이 조금만 늘어도 모든 요청의 트레이스를 저장하는 것은 비용이 안 맞는다. 그래서 **샘플링**을 건다. 다만 LLM 애플리케이션에서 무작위 1%는 나쁜 선택이다. 정작 보고 싶은 것들이 전부 소수라서다.

쓸 만한 방식은 요청이 끝난 뒤에 남길지 정하는 것이다. 스팬을 일단 메모리에 모아 두고, 트레이스가 끝나는 시점에 규칙을 적용한다.

- **오류가 난 트레이스는 전부 남긴다**
- **느린 트레이스는 전부 남긴다** — p95를 넘는 것들
- **가드레일에 걸렸거나 사용자가 부정 피드백을 준 트레이스는 전부 남긴다**
- **나머지 정상 트레이스는 1~5%만 남긴다** — 평상시가 어떤 모습인지를 아는 데는 이 정도면 충분하다

이렇게 하면 저장량은 몇 %로 줄면서 **실제로 볼 가치가 있는 것은 거의 다 남는다.** 정상 응답을 일부라도 남기는 이유는 비교 대상이 필요해서다. 이상한 것만 모아 두면 그게 정말 이상한지 판단할 기준이 없다.

## 이어 붙이기가 끊기는 자리

트레이싱을 붙였는데 트레이스가 조각조각 나 있는 경우가 있다. 원인은 대개 **문맥이 전달되지 않은 경계**다.

가장 흔한 곳이 비동기 작업이다. 요청을 받아 큐에 넣고 워커가 처리하는 구조라면, 트레이스 ID를 메시지에 실어 보내고 워커가 그것을 부모로 삼아 스팬을 열어야 한다. 안 그러면 워커 쪽이 별개의 트레이스로 뜬다. 스레드 풀에 작업을 던지는 경우, 프로세스를 나누는 경우도 같다.

서비스가 여러 개로 나뉘어 있으면 **HTTP 헤더로 문맥을 넘긴다.** 표준이 정해져 있어서(W3C Trace Context) 라이브러리가 대개 알아서 해 주지만, 직접 만든 클라이언트나 내부 RPC를 쓰는 자리는 손으로 넣어야 한다.

그리고 **스트리밍 응답에서 스팬을 언제 닫느냐**가 LLM 특유의 함정이다. 첫 토큰이 왔을 때 닫아 버리면 "200 ms"라는 예쁘고 무의미한 값이 남는다. 스트림이 끝날 때 닫되, 첫 토큰까지 걸린 시간을 속성으로 따로 남기는 것이 맞다. 사용자 체감은 첫 토큰이 정하고, 비용과 부하는 마지막 토큰이 정하기 때문이다.

## 정리

- LLM 요청은 검색·모델·툴을 오가므로 평균 지연 지표로는 원인이 안 좁혀진다. 요청 하나를 스팬으로 쪼개야 시간이 어디에 잠겼는지 보인다
- 메트릭은 "느려졌다", 트레이스는 "여기서 느려졌다", 로그는 "이 입력이었다"에 답한다. 셋 중 하나로 나머지를 대신할 수 없다
- 밖으로 나가는 호출과 시간이 걸리는 계산에 스팬을 준다. 재시도는 반드시 한 번씩 따로 연다
- 스팬 속성에 모델·프롬프트 버전·토큰 수·캐시 히트·중단 이유·검색 문서 ID를 남긴다. 디버깅 값어치의 절반이 여기서 나온다
- 프롬프트 본문은 남길 것인지, 어디에 둘 것인지, 얼마나 오래 둘 것인지를 따로 정하고 저장 직전에 마스킹한다
- 샘플링은 무작위가 아니라 요청이 끝난 뒤 판단한다. 오류·느린 것·부정 피드백은 전부 남기고 정상은 소수만 남긴다
- 비동기 경계와 서비스 경계에서 문맥이 끊긴다. 스트리밍 스팬은 스트림이 끝날 때 닫고 첫 토큰 시각은 속성으로 남긴다

---

읽어주셔서 감사합니다. 😊

**지난 글:** [드리프트 감지 — 모델이 조용히 틀려 가는 것을 어떻게 아는가](/articles/mlops-drift-detection)

**다음 글:** [데이터 버저닝 — 같은 코드가 같은 모델을 만들게 하려면](/articles/data-versioning)
