---
title: "특잇값 분해: 모든 행렬을 회전-늘림-회전으로 쪼개고 랭크를 읽기"
description: "정사각형도 대칭도 아닌 아무 행렬에 A = UΣVᵀ가 항상 존재하는 이유를 AᵀA의 스펙트럼 정리에서 끌어냅니다. 3×2 행렬로 U·Σ·V를 손으로 구하고, 랭크를 0이 아닌 특잇값의 개수로 다시 정의한 뒤 특잇값 분포를 그려 표현이 실제로 몇 차원을 쓰는지 재는 법까지 갑니다."
author: "PALDYN Team"
pubDate: "2026-08-24"
category: "math-for-ai"
level: "중급"
tags: ["중급", "특잇값 분해", "랭크", "선형대수"]
featured: false
draft: false
---

임베딩 층에서 벡터를 200개 뽑아 $$200 \times 16$$ 행렬로 쌓았다고 합시다. 선언한 차원은 16이지만, 실제로 16차원을 쓰고 있는지는 알 수 없습니다. 학습이 잘못되면 모든 벡터가 몇 개 방향으로 쏠려 사실상 5차원짜리가 되기도 합니다 — 표현이 무너진 것인데, 벡터를 하나씩 들여다봐서는 보이지 않습니다.

물어야 할 것은 **이 행렬이 실제로 몇 차원을 쓰는가**이고, 그것을 재는 도구가 이 글의 주제입니다.

[지난 글](/articles/math-spectral-theorem-and-quadratic-forms)의 스펙트럼 정리는 대칭 행렬에만 쓸 수 있었습니다. 임베딩 행렬은 대칭이기는커녕 정사각형도 아닙니다. 그런데 대칭이라는 조건을 **만들어 낼 수 있다면** 그 정리를 그대로 빌려 쓸 수 있습니다.

## 고윳값으로는 접근조차 안 되는 자리

$$m \times n$$ 행렬 $$A$$ 에 $$A\mathbf{v} = \lambda\mathbf{v}$$ 를 쓰려고 하면 $$m \ne n$$ 인 순간 막힙니다. 왼쪽은 $$m$$ 차원 벡터이고 오른쪽은 $$n$$ 차원 벡터라 **등호가 성립할 수 없습니다.** 고윳값은 정사각행렬만의 개념입니다.

정사각형이어도 안심할 수 없습니다. 전단 $$N = \begin{pmatrix} 1 & 1 \\ 0 & 1\end{pmatrix}$$ 은 고윳값이 $$1$$ 하나뿐이라 대각화되지 않았습니다. 하지만 이 행렬이 아무것도 안 하는 것은 아닙니다 — 도형을 분명히 찌그러뜨립니다. **고윳값이 그 찌그러짐을 표현하지 못하는 것뿐입니다.**

그래서 다른 질문을 던집니다. 방향이 유지되는지를 묻는 대신, **길이 1인 벡터를 넣었을 때 얼마나 늘어나는지**를 묻습니다. 이 질문에는 모양 제약이 없습니다.

## AᵀA는 언제나 대칭이다

$$A$$ 가 어떻게 생겼든 $$A^\top A$$ 는 $$n \times n$$ 정사각형이고 대칭입니다. 지난 글의 그람 행렬이 바로 이것입니다. 게다가 성질이 하나 더 있습니다.

$$\mathbf{x}^\top(A^\top A)\mathbf{x} = (A\mathbf{x})^\top(A\mathbf{x}) = \|A\mathbf{x}\|^2 \ge 0$$

**이차형식이 항상 0 이상이므로 $$A^\top A$$ 는 양반정치이고, 따라서 고윳값이 전부 0 이상입니다.** 음수 고윳값이 나올 수 없다는 이 사실이 다음 걸음을 허락합니다 — 제곱근을 씌울 수 있습니다.

$$A^\top A$$ 에 스펙트럼 정리를 적용하면 직교행렬 $$V$$ 로 $$A^\top A = V\Lambda V^\top$$ 이고, 고윳값을 큰 것부터 $$\lambda_1 \ge \lambda_2 \ge \cdots \ge 0$$ 으로 늘어놓습니다. 각각의 제곱근에 이름을 붙입니다.

> **정의.** $$\sigma_i = \sqrt{\lambda_i}$$ 를 $$A$$ 의 **특잇값**이라 하고, $$V$$ 의 열 $$\mathbf{v}_i$$ 를 **오른쪽 특이벡터**라고 한다.

특잇값이 무엇을 재는지는 위 계산에 이미 있습니다. $$\mathbf{v}_i$$ 를 넣으면

$$\|A\mathbf{v}_i\|^2 = \mathbf{v}_i^\top A^\top A\mathbf{v}_i = \lambda_i\|\mathbf{v}_i\|^2 = \lambda_i$$

이므로 $$\|A\mathbf{v}_i\| = \sigma_i$$ 입니다. **길이 1인 $$\mathbf{v}_i$$ 를 넣었더니 길이 $$\sigma_i$$ 로 나온 것**이고, 이것이 앞에서 던진 질문에 대한 답입니다.

![A에서 AᵀA를 만들어 스펙트럼 정리를 적용하고 제곱근을 씌워 특잇값을 얻는 흐름](/assets/posts/math-svd-and-rank-from-ata.svg)

나온 쪽에도 이름을 붙입니다. $$\sigma_i > 0$$ 일 때

$$\mathbf{u}_i = \frac{A\mathbf{v}_i}{\sigma_i}$$

로 두면 길이가 1이고, 서로 직교합니다 — $$\mathbf{u}_i \cdot \mathbf{u}_j = \frac{1}{\sigma_i\sigma_j}\mathbf{v}_i^\top A^\top A\mathbf{v}_j = \frac{\lambda_j}{\sigma_i\sigma_j}(\mathbf{v}_i\cdot\mathbf{v}_j)$$ 이고 $$V$$ 가 직교행렬이라 $$i \ne j$$ 면 0이기 때문입니다. 이 $$\mathbf{u}_i$$ 를 **왼쪽 특이벡터**라고 합니다.

정의를 옮겨 적으면 $$A\mathbf{v}_i = \sigma_i\mathbf{u}_i$$ 이고, 이것을 모든 $$i$$ 에 대해 열로 세우면 $$AV = U\Sigma$$, 즉

$$\boxed{\ A = U\Sigma V^\top\ }$$

여기서 $$U$$ 는 $$m \times m$$ 직교행렬, $$V$$ 는 $$n\times n$$ 직교행렬, $$\Sigma$$ 는 대각에 $$\sigma_1 \ge \sigma_2 \ge \cdots \ge 0$$ 이 놓인 $$m\times n$$ 행렬입니다. 이것을 **특잇값 분해**라고 합니다.

**조건이 하나도 없습니다.** 정사각형이 아니어도, 대칭이 아니어도, 랭크가 모자라도 모든 실행렬에 존재합니다. 고윳값 분해가 조건을 달던 자리를 전부 통과합니다.

## 기하로 보면 — 회전, 늘림, 회전

세 조각의 역할이 각각 뚜렷합니다. $$V^\top$$ 과 $$U$$ 는 직교행렬이라 길이와 각도를 보존합니다 — 돌리기(또는 뒤집기)만 합니다. 가운데 $$\Sigma$$ 만이 축마다 늘이거나 줄입니다.

$$\mathbf{x} \ \xrightarrow{\ V^\top\ } \ \text{특이축에 맞춰 돌리고} \ \xrightarrow{\ \Sigma\ } \ \text{축마다 } \sigma_i \text{배} \ \xrightarrow{\ U\ } \ \text{다시 돌린다}$$

그래서 **어떤 행렬이든 단위원을 타원으로 보냅니다.** 타원의 반지름이 특잇값이고, 그 방향이 $$\mathbf{u}_i$$ 입니다.

![단위원이 Vᵀ로 돌고 Σ로 늘어나 타원이 되고 U로 다시 도는 네 단계](/assets/posts/math-svd-and-rank-geometry.svg)

지난 세 글에서 고윳값이 «방향이 유지되는 축»이었다면, 특잇값은 «가장 크게 늘어나는 방향과 그 배율»입니다. 대칭 행렬에서는 이 둘이 거의 같아집니다 — $$A = Q\Lambda Q^\top$$ 이 이미 $$U\Sigma V^\top$$ 의 꼴이므로 $$\sigma_i = |\lambda_i|$$ 입니다. 절댓값이 붙는 것은 $$\Sigma$$ 의 대각이 0 이상이어야 하기 때문이고, 음수 고윳값의 부호는 $$U$$ 쪽 열의 방향으로 흡수됩니다.

## 손으로 한 번

$$A = \begin{pmatrix} 3 & 0 \\ 4 & 5 \\ 0 & 0 \end{pmatrix}$$ 의 특잇값 분해를 구합니다. $$3\times2$$ 라 고윳값은 아예 정의되지 않는 행렬입니다.

**1단계, $$A^\top A$$ 를 만듭니다.** $$(i,j)$$ 성분은 $$i$$ 번 열과 $$j$$ 번 열의 내적입니다. 열은 $$(3,4,0)$$ 과 $$(0,5,0)$$ 이므로

$$A^\top A = \begin{pmatrix} 25 & 20 \\ 20 & 25 \end{pmatrix}$$

**2단계, 고윳값을 구합니다.** $$\operatorname{tr} = 50$$, $$\det = 625 - 400 = 225$$ 이므로

$$\lambda^2 - 50\lambda + 225 = 0 \quad \Longrightarrow \quad \lambda = \frac{50 \pm \sqrt{2500 - 900}}{2} = \frac{50 \pm 40}{2} = 45,\ 5$$

둘 다 양수입니다 — 앞에서 본 대로 $$A^\top A$$ 의 고윳값은 음수가 될 수 없습니다.

**3단계, 제곱근을 씌워 특잇값을 얻습니다.**

$$\sigma_1 = \sqrt{45} = 3\sqrt{5} \approx 6.708, \qquad \sigma_2 = \sqrt{5} \approx 2.236$$

**4단계, $$V$$ 를 구합니다.** $$\lambda = 45$$ 에서 $$A^\top A - 45I = \begin{pmatrix} -20 & 20 \\ 20 & -20\end{pmatrix}$$ 이라 $$v_1 = v_2$$, 즉 $$(1,1)$$ 방향입니다. $$\lambda = 5$$ 에서는 같은 식으로 $$(1,-1)$$ 입니다. 길이를 1로 맞추면

$$\mathbf{v}_1 = \tfrac{1}{\sqrt2}(1,1), \qquad \mathbf{v}_2 = \tfrac{1}{\sqrt2}(1,-1)$$

**5단계, $$\mathbf{u}_i = A\mathbf{v}_i/\sigma_i$$ 로 $$U$$ 를 구합니다.**

$$A\mathbf{v}_1 = \tfrac{1}{\sqrt2}(3,\ 9,\ 0), \qquad \mathbf{u}_1 = \frac{(3,9,0)}{\sqrt2 \cdot 3\sqrt5} = \frac{(1,3,0)}{\sqrt{10}}$$

$$A\mathbf{v}_2 = \tfrac{1}{\sqrt2}(3,\ -1,\ 0), \qquad \mathbf{u}_2 = \frac{(3,-1,0)}{\sqrt2\cdot\sqrt5} = \frac{(3,-1,0)}{\sqrt{10}}$$

둘 다 길이가 $$\sqrt{10}/\sqrt{10} = 1$$ 이고, 내적은 $$3 - 3 + 0 = 0$$ 이라 직교합니다. **계산이 맞았다는 확인이 공짜로 딸려 옵니다.**

검산이 하나 더 있습니다. $$A$$ 의 모든 성분을 제곱해 더하면 $$9 + 16 + 25 = 50$$ 이고, 특잇값의 제곱을 더해도 $$45 + 5 = 50$$ 입니다. 우연이 아니라 항상 성립하는 관계인데, 그 이유는 「중급 17번 · 행렬 노름」의 몫입니다.

## 고윳값이 못 보던 것을 특잇값은 본다

앞에서 미뤄 둔 전단 $$N = \begin{pmatrix} 1 & 1 \\ 0 & 1\end{pmatrix}$$ 으로 돌아옵니다. 같은 절차를 밟으면

$$N^\top N = \begin{pmatrix} 1 & 1 \\ 1 & 2 \end{pmatrix}, \qquad \operatorname{tr} = 3,\ \det = 1 \quad \Longrightarrow \quad \lambda = \frac{3\pm\sqrt5}{2}$$

$$\sigma_1 = \sqrt{\frac{3+\sqrt5}{2}} \approx 1.618, \qquad \sigma_2 = \sqrt{\frac{3-\sqrt5}{2}} \approx 0.618$$

| | 고윳값 분해 | 특잇값 분해 |
| --- | --- | --- |
| 값 | $$1, 1$$ (중근) | $$1.618,\ 0.618$$ |
| 축 | 방향 하나뿐 — 대각화 실패 | 직교하는 두 축 |
| 말하는 것 | 제자리에 남는 방향 | 가장 늘어나는 방향과 배율 |

**고윳값은 «아무것도 안 늘어난다»고 말하고 특잇값은 «한 방향은 1.6배, 다른 방향은 0.6배»라고 말합니다.** 둘 다 참인데 후자가 도형이 실제로 찌그러진 정도를 담습니다. 회전 행렬도 마찬가지입니다 — 실수 고윳값이 아예 없지만 특잇값은 둘 다 1로 «길이를 안 바꾼다»는 사실을 정확히 알려 줍니다.

![전단에서 고유 방향은 하나뿐인데 특이축은 직교하는 둘로 나오는 대비](/assets/posts/math-svd-and-rank-eigen-vs-singular.svg)

## 랭크 — 0이 아닌 특잇값을 세면 된다

$$\Sigma$$ 의 대각에 0이 섞이면 그 축은 통째로 뭉개진다는 뜻입니다. 살아남은 축의 개수가 곧 이 행렬이 실제로 쓰는 차원 수입니다.

> **정의.** $$A$$ 의 **랭크**는 0이 아닌 특잇값의 개수다.

$$m \times n$$ 행렬의 랭크는 최대 $$\min(m,n)$$ 이고, 그 값을 채우면 **최대 랭크**라고 합니다. 위의 $$3\times2$$ 예는 특잇값이 둘 다 0이 아니므로 랭크 2 — $$\min(3,2)$$ 이라 최대 랭크입니다.

$$B = \begin{pmatrix} 1 & 2 & 3 \\ 2 & 4 & 6 \\ 3 & 6 & 9 \end{pmatrix}$$ 는 다릅니다. 둘째 행이 첫 행의 2배, 셋째 행이 3배라 독립인 정보가 한 줄뿐입니다. 특잇값을 구하면 $$14, 0, 0$$ 으로 랭크 1입니다. **$$3\times3$$ 짜리 자리를 차지하고 있지만 담긴 것은 1차원**이라는 사실이 특잇값 하나만 살아남은 것으로 드러납니다.

## 수치 랭크 — 실전에서는 0이 정확히 0이 아니다

첫머리의 임베딩 행렬로 돌아옵니다. 실제 데이터로 만든 행렬의 특잇값은 정확히 0이 되는 법이 없습니다. 노이즈와 부동소수점 반올림이 언제나 아주 작은 값을 남깁니다. 그래서 «0이 아닌 개수»를 세면 거의 항상 최대 랭크가 나옵니다 — 아무것도 알려 주지 않는 답입니다.

$$200 \times 16$$ 짜리 행렬을 실제로 만들어 특잇값을 재 보겠습니다. 속으로는 5차원짜리 구조에 작은 잡음을 얹은 것입니다.

$$80.5,\ 62.9,\ 56.3,\ 46.7,\ 39.2,\ \mathbf{0.90},\ 0.82,\ 0.76,\ 0.75,\ 0.71,\ \dots$$

**다섯 번째와 여섯 번째 사이에서 40배가 떨어집니다.** 앞의 다섯은 구조이고 뒤의 열하나는 잡음입니다. 0이 아닌 개수를 세면 16이지만, 눈으로 읽는 답은 5입니다.

이것을 규칙으로 만든 것이 **수치 랭크**입니다 — 문턱보다 큰 특잇값의 개수를 랭크로 삼는 것입니다. 흔히 쓰는 문턱이 둘 있습니다.

- **상대 문턱.** $$\sigma_i > \varepsilon\,\sigma_1$$ 인 것만 셉니다. 넘파이 `matrix_rank`의 기본값이 이 방식이고 $$\varepsilon$$ 이 기계 정밀도 수준이라 매우 엄격합니다 — 위 예에서 16을 돌려줍니다.
- **누적 에너지.** $$\sum\sigma_i^2$$ 의 몇 %를 채우는 데 몇 개가 필요한지로 셉니다. 위 예는 다섯 개로 99.97%를 채웁니다.

![특잇값 막대와 누적 에너지 곡선을 겹쳐 그린 스크리 플롯](/assets/posts/math-svd-and-rank-scree.svg)

특잇값을 큰 것부터 늘어놓은 이 그림을 **스크리 플롯**이라고 합니다 — 산비탈에 쌓인 돌무더기(scree)처럼 급히 떨어지다 완만해지는 모양에서 온 이름입니다. **급락하는 자리(무릎)가 있으면 구조와 잡음이 갈리는 지점**이고, 없이 완만하게 이어지면 진짜로 모든 차원을 조금씩 쓰고 있는 것입니다.

첫머리의 질문에는 이렇게 답합니다. 임베딩을 쌓아 스크리 플롯을 그렸을 때 무릎이 선언한 차원보다 한참 앞에 있으면, 그 층은 자리를 다 쓰지 못하고 있습니다.

## 코드로 확인하기

```python
import numpy as np

A = np.array([[3., 0], [4, 5], [0, 0]])
U, s, Vt = np.linalg.svd(A, full_matrices=False)

print(s)                                    # [6.708204 2.236068]  = √45, √5
print(np.allclose(U @ np.diag(s) @ Vt, A))  # True
print(np.linalg.eigvalsh(A.T @ A))          # [ 5. 45.]  — 특잇값의 제곱
print((A ** 2).sum(), (s ** 2).sum())       # 50.0 50.0
```

`full_matrices=False`를 붙인 것은 $$U$$ 를 $$3\times3$$ 대신 $$3\times2$$ 로 받기 위해서입니다. 세 번째 열은 $$\Sigma$$ 의 0 행과 곱해져 사라지므로 계산에 쓰이지 않고, 큰 행렬에서는 이 차이가 메모리를 크게 가릅니다.

전단의 두 분해를 나란히 보면 차이가 분명합니다.

```python
N = np.array([[1., 1], [0, 1]])
print(np.linalg.eigvals(N))                 # [1. 1.]      중근, 방향은 하나뿐
print(np.linalg.svd(N, compute_uv=False))   # [1.618034 0.618034]
```

수치 랭크는 문턱을 직접 정하는 편이 낫습니다.

```python
rng = np.random.default_rng(7)
E = rng.standard_normal((200, 5)) @ rng.standard_normal((5, 16))
E = E + 0.05 * rng.standard_normal((200, 16))       # 5차원 구조 + 잡음

s = np.linalg.svd(E, compute_uv=False)
print(s.round(2))
# [80.5  62.88 56.31 46.71 39.23  0.9   0.82 ...]   6번째에서 급락

print(np.linalg.matrix_rank(E))                     # 16  — 기본 문턱은 너무 엄격하다

energy = np.cumsum(s ** 2) / (s ** 2).sum()
print((energy < 0.99).sum() + 1)                    # 5   — 99% 기준 수치 랭크
```

## 정리

- **$$A\mathbf{v} = \lambda\mathbf{v}$$ 는 정사각행렬에서만 성립**합니다. 정사각형이어도 전단처럼 대각화되지 않는 행렬이 있습니다.
- **$$A^\top A$$ 는 언제나 대칭이고 양반정치**입니다. $$\mathbf{x}^\top A^\top A\mathbf{x} = \|A\mathbf{x}\|^2 \ge 0$$ 이므로 고윳값이 음수가 될 수 없고, 그래서 제곱근을 씌울 수 있습니다.
- **특잇값 $$\sigma_i = \sqrt{\lambda_i(A^\top A)}$$** 이고, 길이 1인 $$\mathbf{v}_i$$ 를 넣었을 때 나오는 벡터의 길이입니다.
- **$$A = U\Sigma V^\top$$ 은 모든 실행렬에 존재**합니다. 조건이 없다는 점이 고윳값 분해와 결정적으로 다릅니다.
- **기하로는 회전-늘림-회전**이고, 그래서 어떤 행렬이든 단위원을 타원으로 보냅니다. 타원의 반지름이 특잇값입니다.
- **대칭 행렬에서는 $$\sigma_i = |\lambda_i|$$** 입니다. 특잇값 분해가 고윳값 분해를 조건 없이 확장한 것임이 여기서 보입니다.
- **랭크는 0이 아닌 특잇값의 개수**입니다. 자리를 크게 차지해도 담긴 차원은 그것뿐입니다.
- **실전에서는 수치 랭크**를 씁니다. 문턱보다 큰 개수를 세거나 누적 에너지로 자르고, 스크리 플롯의 무릎이 구조와 잡음의 경계를 보여 줍니다.

특잇값을 큰 것부터 늘어놓고 뒤쪽을 잘라 내는 절차를 이 글은 «랭크를 읽는다»는 뜻으로만 썼습니다. 그런데 잘라 낸 것을 버리지 않고 **그것으로 원래 행렬을 대신하면** 어떻게 될까요. 그것이 저계수 근사이고, 놀랍게도 그 방식이 최적이라는 정리가 있습니다. LoRA가 왜 작동하는지, 그리고 $$r$$ 을 감이 아니라 스펙트럼에 근거해 고르는 방법이 다음 글입니다.

---

읽어주셔서 감사합니다. 😊

**지난 글:** [대칭 행렬: 직교 대각화와 이차형식의 부호](/articles/math-spectral-theorem-and-quadratic-forms)

**다음 글:** [저계수 근사와 에카르트-영: LoRA의 r을 스펙트럼으로 고르기](/articles/math-low-rank-approximation-and-lora)
