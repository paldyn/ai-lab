---
title: "KL 발산: 거리가 아닌 거리와 방향의 선택"
description: "RLHF의 KL 페널티, 증류 손실, ELBO의 간격은 전부 같은 양입니다. 그 양을 정의하고 «두 분포 사이의 거리»라는 설명이 어디서 틀리는지 반례로 확인한 뒤, 방향을 바꾸면 학습이 넓게 덮을지 한 봉우리에 몰릴지가 갈리는 것을 수치로 봅니다."
author: "PALDYN Team"
pubDate: "2026-08-27"
category: "math-for-ai"
level: "중급"
tags: ["중급", "KL 발산", "정보이론", "JS 발산"]
featured: false
draft: false
---

세 자리에 같은 이름이 붙어 있습니다.

```yaml
init_kl_coef: 0.05        # RLHF — 참조 모델에서 멀어지지 마라
```

```python
loss = KLDivLoss()(student_logp, teacher_p)   # 지식 증류
kl_term = ...                                  # VAE의 ELBO 두 항 중 하나
```

정책을 붙들어 두는 벌점, 교사 모델을 흉내 내는 손실, 그리고 [지난 글](/articles/math-jensen-inequality)에서 하한과 참값 사이에 남는다고 했던 간격. 셋의 정체가 같습니다. 흔한 설명은 「두 분포 사이의 거리」인데, **그 설명은 틀렸고 틀린 자리가 실무에서 중요합니다.** 방향을 어느 쪽으로 잡느냐에 따라 학습이 전혀 다른 답으로 갑니다.

## 정의 — 초과분에 이름 붙이기

지난 글에서 교차엔트로피가 엔트로피보다 작아질 수 없다는 것을 증명했습니다. 그 초과분에 이름을 붙입니다.

> **정의.** 분포 $$p$$ 에 대한 $$q$$ 의 **쿨백-라이블러 발산**(Kullback-Leibler divergence)은
> $$D_{\mathrm{KL}}(p \,\|\, q) = \sum_i p_i \log \frac{p_i}{q_i}$$
> 이다. 줄여서 **KL 발산**이라 부르고, $$\|$$ 기호는 앞뒤를 바꿔 쓸 수 없다는 표시다.

로그를 쪼개면 그대로 분해가 나옵니다.

$$D_{\mathrm{KL}}(p \,\|\, q) = \sum_i p_i \log p_i - \sum_i p_i \log q_i = H(p,q) - H(p)$$

![교차엔트로피는 엔트로피와 KL 발산의 합이다](/assets/posts/math-kl-divergence-decomposition.svg)

**교차엔트로피 = 엔트로피 + KL 발산.** 이 한 줄에서 지도학습의 익숙한 사실 하나가 떨어져 나옵니다. 정답 분포 $$p$$ 는 데이터가 주는 것이라 학습 중에 바뀌지 않으므로 $$H(p)$$ 는 상수이고, 따라서

$$\arg\min_\theta H(p, q_\theta) = \arg\min_\theta D_{\mathrm{KL}}(p \,\|\, q_\theta)$$

입니다. **교차엔트로피를 줄이는 것과 KL 발산을 줄이는 것이 같은 일**인 이유가 이것입니다. 두 값 자체는 $$H(p)$$ 만큼 다르지만 최소가 되는 자리가 같습니다.

성질 둘은 이미 증명해 두었습니다.

- $$D_{\mathrm{KL}}(p \,\|\, q) \ge 0$$ — 젠센 부등식과 $$\log$$ 의 오목성에서 나왔습니다.
- 등호는 **$$q = p$$ 일 때만** 성립합니다.

여기에 실무에서 걸리는 단서를 하나 붙입니다. **$$p_i > 0$$ 인데 $$q_i = 0$$ 이면 값이 무한대입니다.** 반대로 $$p_i = 0$$ 인 항은 $$0 \log 0 = 0$$ 으로 약속해 그냥 사라집니다. 이 비대칭이 뒤에서 방향의 차이를 만드는 뿌리입니다.

단위는 로그의 밑을 따라갑니다. 밑이 2면 비트, 자연로그면 내트입니다. 이 글에서는 손으로 세기 좋게 비트를 씁니다.

## 거리가 아닌 이유 둘

수학에서 «거리»라는 말에는 지켜야 할 조건이 붙어 있습니다. 그중 둘을 KL 발산이 어깁니다.

**첫째, 대칭이 아닙니다.** $$p = (0.9,\, 0.1)$$ 과 $$q = (0.5,\, 0.5)$$ 를 놓고 양쪽으로 재 봅니다.

$$D_{\mathrm{KL}}(p \,\|\, q) = 0.9 \log_2 \frac{0.9}{0.5} + 0.1 \log_2 \frac{0.1}{0.5} = 0.763 - 0.232 = 0.531$$

$$D_{\mathrm{KL}}(q \,\|\, p) = 0.5 \log_2 \frac{0.5}{0.9} + 0.5 \log_2 \frac{0.5}{0.1} = -0.424 + 1.161 = 0.737$$

![같은 두 분포인데 재는 방향에 따라 값이 다르다](/assets/posts/math-kl-divergence-asymmetry.svg)

같은 두 분포인데 0.531과 0.737입니다. 「A와 B 사이가 0.5인데 B와 A 사이는 0.7」인 자를 거리라고 부를 수는 없습니다.

**둘째, 삼각부등식이 성립하지 않습니다.** 거리라면 「돌아가는 길이 곧장 가는 길보다 짧을 수 없다」가 성립해야 합니다. 세 분포로 반례를 만듭니다.

$$p = (0.5,\, 0.5), \qquad q = (0.9,\, 0.1), \qquad r = (0.99,\, 0.01)$$

| 잰 것 | 값 (비트) |
| --- | --- |
| $$D_{\mathrm{KL}}(p \,\|\, q)$$ | 0.737 |
| $$D_{\mathrm{KL}}(q \,\|\, r)$$ | 0.208 |
| 두 값의 합 | 0.945 |
| $$D_{\mathrm{KL}}(p \,\|\, r)$$ | **2.329** |

$$p$$ 에서 $$q$$ 를 거쳐 $$r$$ 로 가는 길이 0.945인데 곧장 가는 길이 2.329입니다. **돌아가는 길이 절반도 안 되게 짧습니다.** 삼각부등식이 어긋나는 정도가 오차 수준이 아니라 두 배가 넘습니다.

그래서 이름이 «거리»가 아니라 **발산**(divergence)입니다. 발산은 「0 이상이고, 같을 때만 0이 되는 양」까지만 약속하고 대칭성과 삼각부등식은 약속하지 않는 이름입니다.

## 방향의 선택 — 넓게 덮을 것인가, 한 봉우리에 몰릴 것인가

대칭이 아니라는 것은 불편한 성질이 아니라 **고를 수 있는 손잡이**입니다. 어느 쪽을 앞에 두느냐가 학습의 답을 바꿉니다. 정의를 다시 보면 이유가 보입니다.

$$D_{\mathrm{KL}}(p \,\|\, q) = \sum_i p_i \log \frac{p_i}{q_i}$$

**앞에 오는 분포가 벌점의 무게를 정합니다.** 각 항에 $$p_i$$ 가 곱해져 있으니 $$p_i = 0$$ 인 자리는 $$q$$ 가 무엇을 하든 값에 안 들어옵니다. 반대로 $$p_i$$ 가 큰 자리에서 $$q_i$$ 가 0에 가까우면 로그가 폭발합니다.

두 봉우리 사이에 얕은 골이 있는 분포로 세어 봅시다.

$$p = (0.495,\; 0.01,\; 0.495)$$

여기에 후보 둘을 댑니다. $$q_A$$ 는 한 봉우리에 몰려 나머지를 아예 버렸고, $$q_B$$ 는 셋에 고르게 폈습니다.

$$q_A = (0.99,\; 0.01,\; 0), \qquad q_B = \left(\tfrac{1}{3},\; \tfrac{1}{3},\; \tfrac{1}{3}\right)$$

![정방향은 넓게 덮게 하고 역방향은 한 봉우리에 몰리게 한다](/assets/posts/math-kl-divergence-mode.svg)

| | $$q_A$$ — 한 봉우리 | $$q_B$$ — 넓게 |
| --- | --- | --- |
| 정방향 $$D_{\mathrm{KL}}(p \,\|\, q)$$ | $$\infty$$ | 0.514 |
| 역방향 $$D_{\mathrm{KL}}(q \,\|\, p)$$ | **0.990** | 1.306 |

**정방향은 $$q_B$$ 를, 역방향은 $$q_A$$ 를 고릅니다.** 같은 두 후보인데 답이 뒤집혔습니다.

이유는 앞에서 본 무한대 규칙 하나입니다.

- **정방향** $$D_{\mathrm{KL}}(p \,\|\, q)$$ 는 $$p_i > 0$$ 인 자리에서 $$q_i = 0$$ 이면 무한대입니다. 그래서 $$q$$ 는 **$$p$$ 가 조금이라도 있는 곳을 하나도 빠뜨리면 안 됩니다.** 여러 봉우리를 다 덮으려다 그 사이의 빈 골까지 덮어 버립니다. 이것을 **모드 커버링**(mode-covering)이라 부릅니다.
- **역방향** $$D_{\mathrm{KL}}(q \,\|\, p)$$ 는 $$q_i = 0$$ 인 자리를 통째로 무시합니다($$0 \log 0 = 0$$). $$q$$ 는 **자기가 확률을 준 곳에서만 $$p$$ 와 맞으면 되므로** 가장 편한 봉우리 하나에 몰리는 편이 이득입니다. 이것을 **모드 시킹**(mode-seeking)이라 부릅니다.

이 갈래가 곧 익숙한 두 학습법의 갈래입니다. **최대가능도는 정방향입니다** — 데이터 분포가 앞에 오므로 모델이 데이터의 모든 구석을 덮으려 합니다. 학습이 덜 된 생성 모델이 흐릿한 평균 같은 것을 내놓는 것이 그 그림자입니다. **변분추론과 RLHF의 KL 페널티는 역방향입니다** — 우리가 고르는 분포가 앞에 오므로 참조 분포의 한 봉우리에 안정적으로 붙습니다.

## JS 발산 — 대칭으로 만들기

방향을 고르고 싶지 않을 때가 있습니다. 두 분포를 대등하게 견주고 싶을 때인데, 그때는 가운뎃점을 하나 만들어 양쪽에서 잽니다.

> **정의.** 혼합분포 $$m = \tfrac{1}{2}(p + q)$$ 에 대해
> $$D_{\mathrm{JS}}(p,\, q) = \tfrac{1}{2} D_{\mathrm{KL}}(p \,\|\, m) + \tfrac{1}{2} D_{\mathrm{KL}}(q \,\|\, m)$$
> 를 **젠슨-섀넌 발산**(Jensen-Shannon divergence)이라 한다.

앞의 $$p = (0.9, 0.1)$$, $$q = (0.5, 0.5)$$ 로 세어 봅니다. $$m = (0.7,\, 0.3)$$ 이고

$$D_{\mathrm{KL}}(p \,\|\, m) = 0.168, \qquad D_{\mathrm{KL}}(q \,\|\, m) = 0.126$$

이므로 $$D_{\mathrm{JS}} = 0.147$$ 입니다. 얻는 것이 셋입니다.

- **대칭입니다.** 정의가 $$p$$ 와 $$q$$ 를 똑같이 다루므로 순서를 바꿔도 같은 값입니다.
- **언제나 유한합니다.** $$m$$ 은 $$p$$ 나 $$q$$ 가 양수인 모든 자리에서 양수라 무한대가 나올 자리가 없습니다. 위의 $$q_A$$ 처럼 지지집합이 어긋난 짝에서도 값이 나옵니다.
- **위로 막혀 있습니다.** 값이 $$\log 2$$ 를 넘지 않습니다. 비트로 재면 1, 내트로 재면 0.693입니다.

그리고 $$\sqrt{D_{\mathrm{JS}}}$$ 는 삼각부등식까지 만족해 **진짜 거리**가 됩니다. 대칭이 필요한 자리에서 KL 대신 쓰는 값이 이것입니다.

## 세 자리에 같은 양이 있다

처음의 세 조각으로 돌아갑니다. 무엇을 앞에 두었는지만 확인하면 각 자리의 성격이 읽힙니다.

![같은 식이 세 자리에서 다른 이름으로 불린다](/assets/posts/math-kl-divergence-three-places.svg)

| 쓰이는 자리 | 식 | 방향 | 하는 일 |
| --- | --- | --- | --- |
| RLHF·DPO의 KL 페널티 | $$\beta\, D_{\mathrm{KL}}(\pi_\theta \,\|\, \pi_{\text{ref}})$$ | 역방향 | 정책이 참조 모델의 봉우리에서 벗어나지 않게 붙든다 |
| 지식 증류 | $$D_{\mathrm{KL}}(p_{\text{교사}} \,\|\, q_{\text{학생}})$$ | 정방향 | 학생이 교사가 확률을 준 자리를 다 덮게 한다 |
| ELBO의 간격 | $$D_{\mathrm{KL}}(q(z) \,\|\, p(z \mid x))$$ | 역방향 | 하한과 참값의 거리가 정확히 이 값이다 |

가운데 줄이 「증류에서 왜 정답 하나가 아니라 교사의 분포 전체를 흉내 내는가」에 대한 답입니다. 교사가 오답들에 매긴 작은 확률까지 목표에 들어 있고, 정방향이라 학생은 그것들을 **빠뜨릴 수 없습니다.** 그 작은 확률들이 이른바 «어두운 지식」이고, 원-핫 정답에는 없는 정보입니다.

맨 아래 줄은 지난 글에서 미뤄 둔 답입니다. ELBO가 $$\log p(x)$$ 에 못 미치는 그 간격이 바로 KL 발산이고, KL이 0이 되는 조건은 $$q(z) = p(z \mid x)$$ 하나뿐이므로 **하한이 참값에 닿는 것은 우리가 고른 분포가 참 사후분포와 같아질 때뿐**입니다. 자세한 유도는 중급 60번 · 변분 하한과 ELBO가 맡습니다.

## 코드로 확인하기

```python
import numpy as np

def kl(p, q):                                  # 비트 단위, 0 log 0 = 0
    m = p > 0
    return float((p[m] * np.log2(p[m] / q[m])).sum())

p = np.array([0.9, 0.1]); q = np.array([0.5, 0.5])
print(round(kl(p, q), 4), round(kl(q, p), 4))          # 0.531 0.737  대칭이 아니다

# ① 삼각부등식 반례
a = np.array([0.5, 0.5]); b = np.array([0.9, 0.1]); c = np.array([0.99, 0.01])
print(round(kl(a, b) + kl(b, c), 4), round(kl(a, c), 4))   # 0.9454 2.3292

# ② 방향이 답을 뒤집는다
P  = np.array([0.495, 0.01, 0.495])
qA = np.array([0.99, 0.01, 0.0])                # 한 봉우리
qB = np.full(3, 1 / 3)                          # 넓게
print(kl(P, qA), round(kl(P, qB), 4))           # inf 0.5142   정방향은 qB
print(round(kl(qA, P), 4), round(kl(qB, P), 4)) # 0.99 1.306   역방향은 qA

# ③ JS 발산은 대칭이고 유한하다
def js(p, q):
    m = (p + q) / 2
    return 0.5 * kl(p, m) + 0.5 * kl(q, m)
print(round(js(p, q), 4), round(js(q, p), 4))   # 0.1468 0.1468
print(round(js(P, qA), 4))                      # 0.3082  KL은 무한이던 짝
```

②가 이 글의 핵심입니다. 같은 두 후보에 같은 식을 대는데 **인자의 순서만 바꾸면 이기는 쪽이 뒤집힙니다.**

## 정리

- **KL 발산은 $$\sum_i p_i \log(p_i/q_i)$$** 이고, 곧 $$H(p,q) - H(p)$$ 다. 「$$q$$ 를 믿고 적을 때 치르는 초과 비용」이다.
- **교차엔트로피 = 엔트로피 + KL 발산.** $$p$$ 가 고정이면 둘을 최소화하는 자리가 같아서 지도학습에서는 구별할 필요가 없다.
- $$D_{\mathrm{KL}} \ge 0$$ 이고 **등호는 $$q = p$$ 일 때만** 이다. 젠센 부등식에서 나온 결과다.
- **거리가 아니다.** 대칭이 아니고 삼각부등식도 어긴다 — 돌아가는 길이 곧장 가는 길의 절반도 안 되는 반례가 있다.
- **$$p_i > 0$$ 인데 $$q_i = 0$$ 이면 무한대**이고, $$p_i = 0$$ 인 항은 사라진다. 방향의 차이는 전부 이 규칙 하나에서 나온다.
- **정방향은 모드 커버링, 역방향은 모드 시킹**이다. 최대가능도가 앞쪽이고 변분추론과 RLHF 페널티가 뒤쪽이다.
- **JS 발산은 가운뎃점 $$m$$ 을 만들어 양쪽에서 잰 값**이라 대칭이고 $$\log 2$$ 이하로 유한하며, 제곱근은 진짜 거리다.

`init_kl_coef: 0.05`로 돌아갑니다. 이제 저 줄은 「참조 모델에서 멀어지지 마라」보다 정확하게 읽힙니다 — **역방향으로 재고 있으므로, 정책은 참조 모델의 한 봉우리에 눌러앉는 쪽으로 눌립니다.** 정책이 다양성을 잃는다는 흔한 관찰이 계수의 크기 문제만이 아니라 방향의 성질이기도 하다는 뜻입니다.

5단원의 마지막 한 편이 남았습니다. 지금까지는 분포 하나 또는 둘을 다뤘는데, 다음 글은 **두 확률변수가 얼마나 얽혀 있는가**를 재는 양으로 갑니다. 그 양도 결국 KL 발산 하나로 적힙니다.

---

읽어주셔서 감사합니다. 😊

**지난 글:** [젠센 부등식: 볼록함수와 기댓값의 순서](/articles/math-jensen-inequality)

**다음 글:** [상호정보량: 두 변수가 공유하는 정보와 InfoNCE 하한](/articles/math-mutual-information)
