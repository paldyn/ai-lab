---
title: "드리프트 감지 — 모델이 조용히 틀려 가는 것을 어떻게 아는가"
description: "배포한 모델의 성능은 소리 없이 내려간다. 정답 라벨이 늦게 오는 현실에서 무엇을 대신 재야 하는지, 입력·예측·개념 드리프트를 어떻게 가르고 경보를 어디에 거는지 정리합니다."
author: "PALDYN Team"
pubDate: "2026-09-03"
category: "ml-ops"
level: "중급"
tags: ["드리프트", "모니터링", "MLOps", "재학습", "데이터 품질"]
featured: false
draft: false
---
[지난 글](/articles/edge-npu-runtime)에서 모델을 기기 안 전용 칩에 올리는 이야기를 했다. 어디에 올렸든, 배포는 끝이 아니라 시작이다. 학습할 때 본 세상과 배포된 뒤에 마주치는 세상은 계속 벌어지고, 모델은 그 사실을 스스로 알려 주지 않는다. 예외를 던지지도 않고 응답 시간이 늘지도 않는다. 그냥 조금씩 더 자주 틀린다.

이 어긋남을 **드리프트**(drift)라고 부른다. 학습 시점의 데이터 분포와 지금 들어오는 데이터의 분포가 벌어지는 현상이다. 서버 모니터링은 CPU와 지연 시간을 보면 되지만, 모델 모니터링은 아무 데도 빨간불이 안 들어온 상태에서 성능만 새어 나간다. 그래서 별도의 감시 장치가 필요하다.

## 정확도를 기다리면 늦는다

가장 먼저 떠오르는 답은 "정확도를 계속 재면 되지 않나"다. 맞는 말이지만, 정확도를 재려면 정답이 있어야 한다. 그리고 실무에서 정답은 대개 한참 뒤에 온다.

대출 심사 모델의 정답은 연체가 발생하거나 만기가 지나야 확정되고, 이탈 예측 모델의 정답은 정의한 기간이 지나야 나온다. 콘텐츠 추천의 클릭은 빨리 오지만, "그 추천이 좋았는가"의 정답은 나중에야 붙는다. 이 시차를 **라벨 지연**(label lag)이라고 한다.

![라벨 지연 때문에 정확도와 입력 지표가 서로 다른 시점에 반응한다](/assets/posts/mlops-drift-detection-label-lag.svg)

그림의 아래 칸을 보면 실선이 6주차에서 끊긴다. 그 뒤는 실제로 일어난 일이지만 아직 우리 손에 없는 값이다. 위 칸의 입력 지표는 라벨이 필요 없으므로 매일 잰다. 5주차에 입력이 흔들린 것을 위 칸은 6주차에 알려 주고, 아래 칸은 12주차에 알려 준다.

**그래서 감시의 1차 방어선은 정확도가 아니라 입력이다.** 정확도는 뒤늦게 오는 확인용 지표로 두고, 매일 보는 화면에는 라벨이 없어도 잴 수 있는 것을 올린다.

## 무엇이 변한 것인지부터 가른다

"드리프트가 났다"는 말은 너무 뭉뚱그린 표현이다. 대응이 완전히 다른 세 가지가 그 안에 섞여 있다.

**입력 드리프트**는 들어오는 데이터의 분포가 변한 것이다. 신규 유입 채널이 열려 연령대가 달라졌거나, 앱 개편으로 특정 화면의 유입이 늘었거나, 계절이 바뀐 경우다. 입력과 정답의 관계 자체는 그대로다.

**예측 드리프트**는 모델이 내놓는 값의 분포가 변한 것이다. 승인률이 갑자기 올라갔다거나, 특정 클래스로 쏠린다거나 하는 식이다. 입력 드리프트의 결과일 수도 있고, 피처 계산이 망가져서일 수도 있다.

**개념 드리프트**는 입력과 정답 사이의 관계 자체가 변한 것이다. 같은 특징을 가진 사용자가 예전에는 우량 고객이었는데 지금은 아닌 경우다. 이것이 가장 아프다 — 입력 분포는 멀쩡한데 모델만 틀리기 때문에, 입력 지표로는 안 잡힌다.

![성능 저하의 원인을 가르는 순서도](/assets/posts/mlops-drift-detection-triage.svg)

순서도의 마지막 칸이 실무에서 가장 자주 나오는 답이라는 점을 강조하고 싶다. 성능이 떨어졌을 때 절반쯤은 드리프트가 아니라 **파이프라인 사고**다. 상류 테이블의 컬럼 이름이 바뀌어 피처가 전부 결측이 됐다거나, 집계 배치가 하루 밀려 어제 값이 들어갔다거나 하는 것들이다. 드리프트 대응은 재학습이지만 파이프라인 사고의 대응은 코드 수정이므로, 오진하면 멀쩡한 모델을 오염된 데이터로 다시 학습시키게 된다.

## 분포가 벌어진 정도를 재는 방법

두 분포가 얼마나 다른지를 하나의 숫자로 만드는 방법은 여러 가지가 있고, 데이터 종류에 따라 쓰는 것이 다르다.

가장 널리 쓰이는 것이 **PSI**(Population Stability Index)다. 값의 범위를 여러 구간으로 쪼갠 뒤, 각 구간에 들어간 비율이 기준 시점과 얼마나 달라졌는지를 더한다.

$$
\mathrm{PSI} = \sum_{i=1}^{k} (p_i - q_i) \ln \frac{p_i}{q_i}
$$

여기서 $$p_i$$ 는 현재 데이터에서 $$i$$ 번째 구간의 비율, $$q_i$$ 는 기준 데이터에서의 비율이다. 두 분포가 같으면 각 항이 0이 되어 합도 0이다. 어느 한쪽으로 쏠릴수록 커진다.

```python
import numpy as np

def psi(reference, current, bins=10, eps=1e-6):
    # 구간 경계는 기준 데이터로 한 번만 정하고 계속 재사용한다
    edges = np.quantile(reference, np.linspace(0, 1, bins + 1))
    edges[0], edges[-1] = -np.inf, np.inf

    q = np.histogram(reference, bins=edges)[0] / len(reference)
    p = np.histogram(current, bins=edges)[0] / len(current)
    q, p = np.maximum(q, eps), np.maximum(p, eps)

    return float(np.sum((p - q) * np.log(p / q)))
```

`eps`를 넣은 줄이 중요하다. 어떤 구간의 비율이 0이 되면 로그가 발산해서 PSI가 무한대로 튄다. 새 값이 나타난 구간에서 실제로 자주 일어나는 일이다.

다른 방법들도 자리가 있다.

| 방법 | 쓰는 데이터 | 성격 |
| --- | --- | --- |
| PSI | 수치형, 순서 있는 범주 | 해석 기준이 널리 공유돼 있어 팀 간 합의가 쉽다 |
| KS 통계량 | 수치형 | 두 누적분포의 최대 간격. 구간을 안 나눠도 된다 |
| 카이제곱 검정 | 범주형 | 카테고리 비율 변화에 민감하다 |
| 바서슈타인 거리 | 수치형 | 값이 얼마나 멀리 옮겨 갔는지까지 반영한다 |
| 임베딩 거리 | 텍스트·이미지 | 원본을 벡터로 바꾼 뒤 그 분포를 비교한다 |

수치 하나에 매달리지 않는 편이 좋다. 실무에서는 **주요 피처 열 개 남짓에 PSI를 걸어 두고, 그중 몇 개가 동시에 움직이는지를 본다.** 하나가 튀는 것은 흔하지만 셋이 같이 움직이면 진짜다.

PSI 값의 통상적인 읽기는 이렇다 — 0.1 아래면 안정, 0.1~0.2면 주의, 0.2를 넘으면 조사 대상이다. 다만 이 숫자는 신용 평가 쪽에서 굳어진 관례이지 보편 상수가 아니다. **자기 서비스의 평상시 값을 몇 주 모아 보고 기준을 다시 잡는 편이 낫다.** 트래픽이 원래 요일마다 크게 출렁이는 서비스라면 0.2가 매일 넘을 수도 있다.

## 무엇과 비교할 것인가

드리프트는 항상 "무엇에 견주어"라는 기준이 있어야 정의된다. 이 기준 데이터를 **참조 윈도우**(reference window)라고 부르고, 어떻게 잡느냐가 경보 품질을 크게 좌우한다.

**학습 데이터로 고정하는 방식**이 가장 단순하다. 모델이 배운 세상 그 자체와 비교하므로 의미가 분명하다. 대신 시간이 지나면 정상적인 계절 변화까지 전부 드리프트로 잡히면서 경보가 상시 켜진다.

**최근 N주를 굴리는 방식**은 서서히 일어나는 변화를 정상으로 흡수한다. 갑작스러운 사고는 잘 잡지만, **천천히 진행되는 드리프트는 기준선이 함께 따라가 버려서 영영 안 잡힌다.** 개구리를 천천히 데우는 셈이다.

그래서 실제 운영에서는 **둘을 함께 둔다.** 학습 데이터 대비 값은 "지금 모델이 배운 것에서 얼마나 멀어졌나"를 알려 주는 누적 지표로, 최근 윈도우 대비 값은 "어제와 오늘 사이에 무슨 일이 있었나"를 알려 주는 급변 감지기로 쓴다. 두 지표는 서로 다른 질문에 답한다.

비교 구간을 자를 때 흔한 실수가 하나 더 있다. **주말과 평일을 섞어서 재면 요일 효과가 그대로 드리프트로 나온다.** 하루 단위로 재고 있다면 최소한 같은 요일끼리 비교하거나, 7일 단위로 창을 잡아 요일 구성이 같아지게 한다.

## 경보를 쓸 만하게 만들기

드리프트 모니터링이 실패하는 방식은 대부분 "안 잡혀서"가 아니라 "**너무 자주 잡혀서 아무도 안 보게 돼서**"다. 피처 백 개에 각각 임계값을 걸면 매일 몇 개는 넘는다.

몇 가지가 도움이 된다.

- **연속성을 요구한다.** 한 번 넘은 것으로 울리지 않고, 사흘 연속 넘었을 때 울린다. 표본이 적은 날의 우연한 요동이 걸러진다
- **피처에 등급을 매긴다.** 모델이 실제로 크게 의존하는 피처 몇 개만 즉시 알림 대상으로 두고, 나머지는 주간 리포트로 내린다. 중요도가 낮은 피처가 흔들리는 것은 대개 무해하다
- **표본 수를 함께 본다.** 데이터가 200건뿐인 날의 PSI 0.3은 소음이다. 최소 표본 수를 정해 두고 그 아래면 판정을 미룬다
- **경보에 진단을 붙인다.** "PSI 0.27"만 오면 사람이 다시 조사해야 한다. 어느 구간의 비율이 얼마에서 얼마로 갔는지를 함께 보내면 대개 알림을 보는 즉시 원인이 짐작된다

마지막 항목이 체감 차이가 가장 크다. "연령 피처 PSI 0.31 — 20대 비율 18%에서 34%로"라고 오면, 마케팅 캠페인이 있었다는 것을 담당자가 그 자리에서 떠올린다.

## 잡은 다음에 무엇을 하는가

감지는 수단이고 목적은 대응이다. 그런데 **모든 드리프트가 재학습 대상은 아니다.** 원인에 따라 답이 갈린다.

| 상황 | 대응 |
| --- | --- |
| 피처 계산이 망가져 분포가 튐 | 파이프라인을 고친다. 재학습하면 오염된 데이터를 배운다 |
| 새 사용자군이 들어와 입력이 변함 | 그 구간의 성능을 따로 재 본다. 멀쩡하면 놔둔다 |
| 입력이 변했고 성능도 떨어짐 | 최근 데이터를 포함해 재학습한다 |
| 입력은 그대로인데 성능만 떨어짐 | 개념 드리프트. 라벨을 새로 모으는 것이 먼저다 |
| 일시적 이벤트로 며칠만 튐 | 기록만 남기고 지나간다 |

두 번째 줄을 놓치기 쉽다. 입력 분포가 변했다는 것과 모델이 그 입력에서 못한다는 것은 다른 이야기다. 새로 들어온 사용자군에서도 모델이 잘 맞으면 재학습할 이유가 없다. **드리프트 지표는 "확인해 보라"는 신호이지 "다시 학습하라"는 명령이 아니다.**

그리고 재학습을 자동으로 걸어 둘 때는 반드시 **되돌릴 길을 함께 만든다.** 오염된 데이터로 자동 재학습된 모델이 검증 없이 배포되는 것이, 드리프트를 방치하는 것보다 훨씬 빠르게 사고를 만든다. 자동 재학습의 산출물은 자동 배포가 아니라 후보 모델이어야 하고, 기존 모델과 같은 검증셋에서 비교한 뒤 사람이나 명시적인 규칙이 승인하게 둔다.

## 무엇을 남겨 둘 것인가

드리프트를 조사하려면 과거 값이 있어야 하는데, 사고가 난 뒤에 모으기 시작하면 이미 늦다. 배포 첫날부터 남겨 둘 것이 정해져 있다.

1. **예측할 때 실제로 쓴 피처 값** — 나중에 재계산한 값이 아니라 그 순간의 값이다. 상류 로직이 바뀌면 재계산은 다른 답을 준다
2. **모델이 내놓은 값과 모델 버전** — 어느 버전이 언제부터 얼마나 서빙됐는지가 없으면 비교 구간을 못 자른다
3. **정답이 붙은 시각** — 라벨이 언제 도착했는지를 기록해야 라벨 지연을 실제 값으로 알 수 있다
4. **요청의 맥락** — 유입 채널, 클라이언트 종류, 지역. 드리프트가 났을 때 어느 조각에서 났는지를 좁히는 데 이것만큼 쓸모 있는 것이 없다

4번이 있고 없고가 조사 시간을 하루와 한 주로 가른다. 전체 분포가 흔들린 것과 특정 채널 하나가 통째로 새로 들어온 것은 화면에서 똑같이 보이는데, 대응은 완전히 다르다.

## 정리

- 정확도는 라벨 지연 때문에 늦게 온다. 1차 방어선은 라벨이 필요 없는 입력·예측 분포 지표다
- 입력·예측·개념 드리프트는 대응이 다르다. 그리고 성능 저하의 절반쯤은 드리프트가 아니라 파이프라인 사고다
- PSI가 가장 흔하지만 0.2 같은 기준은 관례일 뿐이다. 평상시 값을 모아 자기 서비스의 기준을 다시 잡는다
- 참조 윈도우는 학습 데이터 고정과 최근 구간 굴림을 함께 둔다. 하나만 쓰면 서서히 오는 드리프트나 정상적인 계절 변화 중 하나를 놓친다
- 경보는 연속성·피처 등급·최소 표본 수로 걸러 내고, 숫자만 보내지 말고 무엇이 어떻게 변했는지를 함께 보낸다
- 드리프트 지표는 확인 신호이지 재학습 명령이 아니다. 자동 재학습을 걸었다면 자동 배포가 아니라 후보 모델까지만 만들게 한다

---

읽어주셔서 감사합니다. 😊

**지난 글:** [엣지 NPU 런타임 — 모델을 기기 안 전용 칩에 올릴 때](/articles/edge-npu-runtime)

**다음 글:** [LLM 트레이싱 — 한 번의 응답이 어디서 시간을 쓰는가](/articles/llmops-tracing)
