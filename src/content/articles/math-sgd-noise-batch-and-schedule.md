---
title: "그래디언트 잡음이 정하는 것: 배치 크기, 선형 스케일링, 워밍업, 코사인 감쇠"
description: "미니배치 그래디언트의 분산이 σ²/B라는 한 줄에서 학습 설정 대부분이 따라 나옵니다. 배치를 k배로 키울 때 학습률을 k배로 올리는 규칙의 유도와 그것이 깨지는 지점, 배치를 키워도 더 이상 빨라지지 않는 임계 배치 크기, 그리고 워밍업과 코사인 감쇠가 각각 무엇을 고치는지를 계산으로 따라갑니다."
author: "PALDYN Team"
pubDate: "2026-09-03"
category: "math-for-ai"
level: "중급"
tags: ["중급", "SGD", "배치 크기", "학습률 스케줄", "잡음"]
featured: false
draft: false
---

GPU를 여덟 장으로 늘리고 배치 크기를 32에서 256으로 올립니다. 학습률은 어떻게 해야 할까요. 그대로 두면 데이터를 여덟 배 빨리 넘기는데 파라미터는 8분의 1만큼만 움직입니다. 여덟 배로 올리면 첫 스텝에서 손실이 발산하는 일이 잦습니다. 실제 설정 파일에는 대개 이런 줄들이 함께 들어 있습니다.

```yaml
batch_size: 256
lr: 3.0e-4
warmup_steps: 1000
lr_schedule: cosine
```

[지난 글](/articles/math-adam-from-moments)에서 옵티마이저 안쪽은 다 열어 봤습니다. 남은 것은 밖에서 들어오는 값 $$g_t$$ 자체입니다. **그것은 데이터 전체의 그래디언트가 아니라 배치 하나로 잰 추정값**이고, 위 네 줄은 전부 그 추정의 오차를 다루는 장치입니다. 이 글은 오차의 크기를 먼저 계산하고 거기서 네 줄을 하나씩 끌어냅니다.

## 미니배치 그래디언트는 얼마나 틀리는가

데이터 $$N$$ 개짜리 손실은 표본별 손실의 평균입니다.

$$L(\theta) = \frac{1}{N}\sum_{i=1}^{N} \ell_i(\theta), \qquad \nabla L(\theta) = \frac{1}{N}\sum_{i=1}^{N} \nabla\ell_i(\theta)$$

배치 $$\mathcal{B}$$ 는 이 중 $$B$$ 개를 무작위로 고른 것이고, 우리가 쓰는 값은 그 평균입니다.

$$g_{\mathcal{B}} = \frac{1}{B}\sum_{i\in\mathcal{B}} \nabla\ell_i(\theta)$$

각 $$i$$ 가 균등하게 뽑히므로 $$E[\nabla\ell_i] = \nabla L$$ 이고, 따라서

$$E[g_{\mathcal{B}}] = \frac{1}{B}\sum_{i\in\mathcal{B}} E[\nabla\ell_i] = \nabla L(\theta)$$

입니다. 평균적으로는 정확하다는 뜻이고, 이런 추정량을 **불편 추정량**이라고 부릅니다 — 치우침(편향)이 없다는 말입니다. 흩어짐은 독립인 값들의 평균이라 [중급 21번 · 분산과 표본 오차](/articles/math-variance-and-sampling-error)에서 본 그대로입니다. 표본별 그래디언트의 분산을 $$\sigma^2$$ 라 두면

$$\operatorname{Var}(g_{\mathcal{B}}) = \frac{\sigma^2}{B}, \qquad \text{표준편차} = \frac{\sigma}{\sqrt{B}}$$

![배치 크기별 미니배치 그래디언트의 흩어짐](/assets/posts/math-sgd-noise-batch-and-schedule-snr.svg)

배치를 4배로 키우면 흩어짐이 절반이 됩니다. 신호와 잡음의 비로 적으면

$$\mathrm{SNR} = \frac{\lVert\nabla L\rVert}{\sigma/\sqrt{B}} = \frac{\sqrt{B}\,\lVert\nabla L\rVert}{\sigma}$$

이고, **배치를 키워서 얻는 것은 $$\sqrt{B}$$ 뿐**입니다. 계산량은 $$B$$ 에 비례해 늘어나는데 정확도는 그 제곱근만큼만 좋아진다는 이 어긋남이 아래 모든 이야기의 뿌리입니다.

## 배치를 k배로 하면 학습률은 몇 배인가

같은 양의 데이터를 볼 때 배치를 $$k$$ 배로 하면 스텝 수는 $$1/k$$ 배가 됩니다. 배치 $$B$$ 로 $$k$$ 스텝 걸은 것과 배치 $$kB$$ 로 한 스텝 걸은 것이 같아지려면 학습률이 얼마여야 하는지를 직접 맞춰 보면 됩니다.

작은 배치로 $$k$$ 스텝 동안 움직인 총량은 — 그 사이 $$\nabla L$$ 이 거의 변하지 않는다고 두면 —

$$\Delta\theta = -\eta\,(g_1 + g_2 + \dots + g_k)$$

이고, 평균은 $$-k\eta\nabla L$$, 분산은 독립인 $$k$$ 개가 더해지므로 $$k\eta^2\sigma^2/B$$ 입니다.

큰 배치 $$kB$$ 로 한 스텝을 학습률 $$\eta'$$ 로 걸으면 평균은 $$-\eta'\nabla L$$, 분산은 $$\eta'^2\sigma^2/(kB)$$ 입니다. 평균이 맞으려면 $$\eta' = k\eta$$ 여야 하고, 그때 분산은

$$\frac{(k\eta)^2\sigma^2}{kB} = \frac{k\eta^2\sigma^2}{B}$$

로 **분산까지 저절로 맞습니다.** 하나를 맞추면 다른 하나가 따라오는 것이 이 규칙의 핵심이고, 이것이 **선형 스케일링 법칙**입니다 — 배치를 $$k$$ 배로 하면 학습률도 $$k$$ 배로 한다.

![작은 배치 k 스텝과 큰 배치 한 스텝을 맞추는 계산](/assets/posts/math-sgd-noise-batch-and-schedule-linear-scaling.svg)

숫자로 확인해 보겠습니다. 1차원 이차함수에서 배치 32로 8스텝 간 것과 배치 256으로 학습률만 8배 올려 한 스텝 간 것을 20만 번씩 돌렸습니다.

```python
import numpy as np
L, sig, eta, B, k, N = 1.0, 3.0, 0.02, 32, 8, 200000
r = np.random.default_rng(0)

x = np.full(N, 1.0)                       # 배치 32 로 8 스텝
for _ in range(k):
    x -= eta * (L * x + r.normal(0, sig / np.sqrt(B), N))
print(f"배치 {B:<4} × {k}스텝, η={eta:<5} 평균 {x.mean():.4f} 표준편차 {x.std():.4f}")

y = np.full(N, 1.0)                       # 배치 256 으로 1 스텝, 학습률도 8 배
y -= (k * eta) * (L * y + r.normal(0, sig / np.sqrt(k * B), N))
print(f"배치 {k*B:<4} × 1스텝, η={k*eta:<5} 평균 {y.mean():.4f} 표준편차 {y.std():.4f}")

z = np.full(N, 1.0)                       # 배치만 키우고 학습률은 그대로
z -= eta * (L * z + r.normal(0, sig / np.sqrt(k * B), N))
print(f"배치 {k*B:<4} × 1스텝, η={eta:<5} 평균 {z.mean():.4f} 표준편차 {z.std():.4f}")

# 배치 32   × 8스텝, η=0.02  평균 0.8507 표준편차 0.0280
# 배치 256  × 1스텝, η=0.16  평균 0.8399 표준편차 0.0300
# 배치 256  × 1스텝, η=0.02  평균 0.9800 표준편차 0.0038
```

학습률을 함께 올린 두 번째 줄은 첫 줄과 평균도 흩어짐도 거의 같습니다. 학습률을 그대로 둔 세 번째 줄은 $$1 \to 0.98$$ 로 8분의 1만 움직였습니다 — 계산은 여덟 배를 하고 진전은 8분의 1인 셈입니다.

### 이 법칙이 깨지는 두 자리

첫째, 위 출력에서 평균이 0.8507과 0.8399로 **완전히 같지는 않습니다.** 작은 배치 쪽은 스텝마다 위치가 바뀌면서 그래디언트를 다시 재므로 $$(1-\eta L)^k = 0.98^8 = 0.8508$$ 이고, 큰 배치 쪽은 처음 위치의 그래디언트만 쓰므로 $$1 - k\eta L = 0.84$$ 입니다. 유도할 때 "$$k$$ 스텝 동안 $$\nabla L$$ 이 그대로"라고 둔 자리가 여기서 값을 냅니다. 곡률이 크거나 $$k$$ 가 크면 이 차이가 벌어집니다.

둘째, 학습률에는 애초에 상한이 있습니다. [경사하강법의 학습률 상한](/articles/math-gradient-descent-and-lr-bound)에서 본 대로 곡률 상수가 $$L$$ 이면 $$\eta < 2/L$$ 을 넘는 순간 발산합니다. 배치를 아무리 키워도 $$\eta$$ 를 그 위로 올릴 수는 없으므로, **선형 스케일링은 언젠가 반드시 멈춥니다.**

## 임계 배치 크기

멈추는 지점을 계산으로 찾을 수 있습니다. 한 스텝이 손실을 얼마나 줄이는지를 2차 근사로 적으면

$$E[L(\theta - \eta g_{\mathcal{B}})] - L(\theta) \approx -\eta\lVert\nabla L\rVert^2 + \frac{\eta^2 L}{2}E\lVert g_{\mathcal{B}}\rVert^2$$

이고, $$E\lVert g_{\mathcal{B}}\rVert^2 = \lVert\nabla L\rVert^2 + \sigma^2/B$$ 입니다(평균의 제곱에 분산을 더한 것). 우변을 $$\eta$$ 에 대한 이차식으로 보고 최소로 만드는 $$\eta$$ 를 구하면

$$
\begin{aligned}
\eta^* &= \frac{1}{L}\cdot\frac{1}{1 + \dfrac{\sigma^2}{B\lVert\nabla L\rVert^2}} \\[6pt]
\Delta L^* &= -\frac{\lVert\nabla L\rVert^2}{2L}\cdot\frac{1}{1 + \dfrac{\sigma^2}{B\lVert\nabla L\rVert^2}}
\end{aligned}
$$

분모에 있는 $$\sigma^2/\lVert\nabla L\rVert^2$$ 을 $$B_{\text{crit}}$$ 이라 이름 붙이면 스텝당 진전이 $$\dfrac{1}{1 + B_{\text{crit}}/B}$$ 에 비례합니다. 이 값을 **임계 배치 크기**라고 부릅니다.

![배치 크기에 따른 스텝당 진전의 포화](/assets/posts/math-sgd-noise-batch-and-schedule-critical-batch.svg)

- $$B \ll B_{\text{crit}}$$: 진전이 $$B$$ 에 거의 비례한다. 배치를 두 배로 하면 스텝당 두 배 나아가므로 총 스텝 수가 절반이 된다. **여기가 선형 스케일링이 통하는 구간이다.**
- $$B \gg B_{\text{crit}}$$: 진전이 $$\lVert\nabla L\rVert^2/2L$$ 에 붙어 더 안 는다. 배치를 두 배로 해도 스텝 수는 그대로이고 계산량만 두 배가 된다.
- $$B = B_{\text{crit}}$$: 한계값의 정확히 절반. 계산 효율과 스텝 수를 맞바꾸는 지점이 여기다.

$$B_{\text{crit}} = \sigma^2/\lVert\nabla L\rVert^2$$ 은 **잡음이 신호보다 몇 배 큰가**입니다. 학습 초반에는 $$\lVert\nabla L\rVert$$ 이 커서 $$B_{\text{crit}}$$ 이 작고, 후반으로 갈수록 $$\lVert\nabla L\rVert$$ 이 0에 가까워지므로 $$B_{\text{crit}}$$ 이 커집니다. 큰 모델 학습에서 배치를 학습 도중에 키워 가는 관행이 여기서 나옵니다.

## 워밍업: 초반에는 아직 잴 것이 안 모였다

워밍업은 $$\eta$$ 를 0에서 $$\eta_{\max}$$ 까지 몇백~몇천 스텝에 걸쳐 선형으로 올리는 것입니다.

$$\eta_t = \eta_{\max}\cdot\frac{t}{T_w} \quad (t \le T_w)$$

이유가 둘 있고 둘 다 앞에서 계산한 것입니다.

첫째, **초기화 근처는 곡률이 크고 그 값을 모릅니다.** 상한 $$2/L$$ 의 $$L$$ 은 우리가 미리 알 수 없는 값이고, 학습이 진행되며 대개 작아집니다. 초반의 큰 $$L$$ 에 맞춘 $$\eta$$ 를 끝까지 쓰면 너무 작고, 후반에 맞춘 $$\eta$$ 를 처음부터 쓰면 발산합니다. 워밍업은 "작게 시작해서 버티는 만큼 올린다"를 스케줄로 박아 둔 것입니다.

둘째, **Adam의 $$\hat v_t$$ 가 아직 믿을 만하지 않습니다.** 지난 글에서 편향 보정이 $$E[v_t]$$ 의 치우침은 없애 준다는 것을 봤지만, 보정이 없애는 것은 평균의 치우침뿐이고 **추정 자체의 흩어짐**은 그대로입니다. $$t$$ 가 작으면 $$v_t$$ 에 들어간 표본이 몇 개 안 되므로 $$\sqrt{\hat v_t}$$ 가 우연히 작게 나올 수 있고, 그 방향의 걸음은 그만큼 커집니다. 나눗셈이 분모에 있는 값의 잡음을 증폭하는 자리라서, 표본이 쌓일 때까지 $$\eta$$ 를 낮춰 두는 것이 안전합니다.

$$\beta_2 = 0.999$$ 의 유효 창이 1,000스텝이라는 것과 워밍업이 흔히 1,000스텝 안팎이라는 것은 우연이 아닙니다.

## 감쇠: 잡음 바닥을 끌어내린다

학습 후반에 학습률을 줄이는 이유는 "이제 다 왔으니 조심스럽게"가 아니라 계산할 수 있는 값입니다. 1차원 이차함수 $$f(x) = \frac{L}{2}x^2$$ 에서 미니배치 그래디언트가 $$g = Lx + \xi$$, $$\xi \sim (0,\ \sigma^2/B)$$ 라 하면

$$x_{t+1} = x_t - \eta(Lx_t + \xi_t) = (1-\eta L)x_t - \eta\xi_t$$

입니다. 양변의 분산을 잡고 정상 상태 $$\operatorname{Var}(x_{t+1}) = \operatorname{Var}(x_t) = V$$ 를 두면

$$V = (1-\eta L)^2 V + \eta^2\frac{\sigma^2}{B} \;\Longrightarrow\; V = \frac{\eta\,\sigma^2}{B\,L\,(2-\eta L)}$$

이고, 손실로 옮기면 $$E[f] = \frac{L}{2}V = \dfrac{\eta\sigma^2}{2B(2-\eta L)}$$ 입니다. $$\eta L$$ 이 작을 때 이 값은 대략 $$\eta\sigma^2/(4B)$$ 로 **$$\eta$$ 에 비례**합니다.

여기가 요점입니다. 최적점에 도착해도 손실이 0으로 가지 않고 이 값에서 멈춥니다. 그래디언트의 잡음이 파라미터를 계속 흔들기 때문이고, 이 높이를 **잡음 바닥**이라고 부릅시다. 바닥을 내리는 방법은 둘뿐입니다 — $$B$$ 를 키우거나 $$\eta$$ 를 줄이는 것. 배치를 키우는 것은 비싸고, 학습률을 줄이는 것은 공짜입니다.

```python
import numpy as np
L, sig, B = 1.0, 3.0, 32

def run(eta_fn, steps=4000, x0=5.0, seed=0):
    r = np.random.default_rng(seed)
    x, tail = x0, []
    for t in range(1, steps + 1):
        g = L * x + r.normal(0, sig / np.sqrt(B))   # 미니배치 그래디언트
        x -= eta_fn(t) * g
        if t > steps * 0.9:
            tail.append(0.5 * L * x * x)
    return float(np.mean(tail))

for eta in (0.2, 0.1, 0.05, 0.025):
    m = np.mean([run(lambda t, e=eta: e, seed=s) for s in range(40)])
    pred = eta * sig**2 / (2 * B * (2 - eta * L))
    print(f"상수 η={eta:<6} 잡음 바닥 {m:.5f}   식이 주는 값 {pred:.5f}")

T = 4000
m = np.mean([run(lambda t: 0.1 * (1 + np.cos(np.pi * t / T)), seed=s) for s in range(40)])
print(f"코사인 0.2 → 0        잡음 바닥 {m:.5f}")

# 상수 η=0.2    잡음 바닥 0.01595   식이 주는 값 0.01562
# 상수 η=0.1    잡음 바닥 0.00755   식이 주는 값 0.00740
# 상수 η=0.05   잡음 바닥 0.00368   식이 주는 값 0.00361
# 상수 η=0.025  잡음 바닥 0.00185   식이 주는 값 0.00178
# 코사인 0.2 → 0        잡음 바닥 0.00039
```

학습률을 반으로 줄일 때마다 바닥이 정확히 반이 되고, 측정값은 식이 주는 값과 몇 퍼센트 안에서 맞습니다. 그리고 $$0.2$$ 에서 시작해 $$0$$ 으로 내려온 코사인 감쇠는 상수 $$0.2$$ 보다 **41배** 낮은 바닥에 도착합니다. 처음부터 $$0.025$$ 로 갔다면 바닥은 낮지만 거기까지 가는 데 훨씬 오래 걸립니다 — 스케줄은 "빨리 가는 구간"과 "낮게 앉는 구간"을 한 번의 학습 안에 이어 붙이는 장치입니다.

### 두 가지 감쇠 식

$$\eta_t = \eta_{\min} + \tfrac{1}{2}(\eta_{\max}-\eta_{\min})\left(1 + \cos\frac{\pi t}{T}\right) \qquad \text{(코사인)}$$

$$\eta_t = \eta_{\max}\sqrt{\frac{T_w}{\max(t,\,T_w)}} \qquad \text{(역제곱근)}$$

![워밍업과 세 가지 학습률 스케줄](/assets/posts/math-sgd-noise-batch-and-schedule-schedules.svg)

**코사인 감쇠**는 총 스텝 수 $$T$$ 를 미리 알아야 합니다. 대신 초반에는 천천히 떨어지고 중반에 가장 가파르며 끝에서 다시 완만해져서, 앞의 절반을 큰 학습률로 벌고 마지막에 바닥까지 앉는 모양이 됩니다. $$T$$ 를 정해 두고 도는 학습에는 이쪽이 맞습니다.

**역제곱근 감쇠**는 $$T$$ 를 몰라도 됩니다. 언제 멈춰도 그 시점까지의 스케줄이 말이 되므로 학습을 이어 붙이거나 도중에 끊을 수 있고, 트랜스포머 원논문이 이 식을 쓴 이유이기도 합니다. 다만 후반에 잘 안 내려가서 — $$t$$ 가 100배가 되어야 $$\eta$$ 가 10분의 1입니다 — 마지막 바닥은 코사인보다 높습니다.

## 정리

- 미니배치 그래디언트는 **불편 추정량**이고 그 분산은 $$\sigma^2/B$$ 다. 신호 대 잡음비는 $$\sqrt{B}\lVert\nabla L\rVert/\sigma$$ 로, 계산을 $$B$$ 배 해서 정확도는 $$\sqrt{B}$$ 배만 얻는다.
- 배치를 $$k$$ 배로 하고 학습률도 $$k$$ 배로 하면 $$k$$ 스텝치 이동의 **평균과 분산이 동시에 맞는다** — 선형 스케일링 법칙. 전제는 그 $$k$$ 스텝 동안 $$\nabla L$$ 이 변하지 않는다는 것이고, 곡률이 크면 $$(1-\eta L)^k$$ 와 $$1-k\eta L$$ 의 차이만큼 어긋난다.
- 학습률에는 $$\eta < 2/L$$ 이라는 상한이 있으므로 스케일링은 반드시 멈춘다. 멈추는 지점이 **임계 배치 크기** $$B_{\text{crit}} = \sigma^2/\lVert\nabla L\rVert^2$$ 이고, 스텝당 진전은 $$1/(1+B_{\text{crit}}/B)$$ 에 비례해 포화한다.
- **워밍업**은 초기화 근처의 큰 곡률과, 표본이 몇 개 안 쌓인 $$\hat v_t$$ 의 흩어짐을 지나가는 구간이다. $$\beta_2$$ 의 창 길이와 워밍업 길이가 비슷한 값인 것은 우연이 아니다.
- **감쇠**는 잡음 바닥 $$\dfrac{\eta\sigma^2}{2B(2-\eta L)}$$ 를 끌어내린다. 학습률을 반으로 줄이면 바닥도 반이 된다. 코사인은 총 스텝 수를 알아야 하는 대신 끝에서 바닥까지 앉고, 역제곱근은 몰라도 되는 대신 후반이 덜 내려간다.

여기까지가 학습을 **어떻게 굴릴 것인가**입니다. 그런데 워밍업이 필요한 이유 중 하나로 "초기화 근처는 곡률이 크다"를 그냥 두고 왔습니다. 애초에 그 출발점의 값을 무엇으로 놓느냐는 따로 계산할 수 있는 문제입니다. 다음 글에서 층을 하나 통과할 때 활성값의 분산이 어떻게 변하는지 유도하고, 그 분산이 유지되는 조건에서 Xavier와 He 초기화를 끌어냅니다.

---

읽어주셔서 감사합니다. 😊

**지난 글:** [1차·2차 모멘트에서 Adam을 직접 조립하기](/articles/math-adam-from-moments)

**다음 글:** [초기화 분산 전파: Xavier·He 유도와 폭 스케일링](/articles/math-weight-init-variance)
