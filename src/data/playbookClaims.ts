import type { Claim } from '../types/playbook';

/**
 * 활용 가이드가 화면에 내는 **모든 값**.
 *
 * 원고에는 요금·한도·컨텍스트 창·모델 id를 한 개도 안 적습니다. 본문은
 * `:claim[claude-max-5h-window]` 한 줄로 부르고 여기 있는 값이 그 자리에 그려집니다.
 * **데이터 한 파일만 늙고 노트 마흔 편은 안 늙게 하는 것**이 이 서랍의 존재 이유입니다.
 *
 * 값을 못 확인했으면 지어내지 말고 `value: null`로 두고 그 이름을 그 도구의 `open`에
 * 넣습니다. **모른다고 적는 것은 언제나 통과합니다. 틀린 값만 막힙니다.**
 *
 * 첫 달에는 `volatility: 'price' | 'limit'`을 여덟 건으로 묶습니다 — 재확인 실행률이
 * 33%라는 실측 위에서, 유효기간을 늘리는 대신 주장 수를 줄이는 쪽을 골랐습니다.
 * 모른다고 적힌 표가 3주 묵은 숫자보다 낫습니다.
 *
 * 3일 차에 마흔 건을 채웁니다(`PLAYBOOK-PLAN.md`). 지금은 비어 있고, 그 상태로도
 * 화면과 검사가 서는지 보는 것이 2일 차의 몫입니다.
 */
export const playbookClaims: Claim[] = [];
