import type { Claim } from '../types/playbook';

/**
 * AI 가이드가 화면에 내는 **모든 값**.
 *
 * 원고에는 요금·한도·컨텍스트 창·모델 id를 한 개도 안 적습니다. 본문은
 * `:claim[아이디]` 한 줄로 부르고 여기 있는 값이 그 자리에 그려집니다.
 * **데이터 한 파일만 늙고 노트는 안 늙게 하는 것**이 이 서랍의 존재 이유입니다.
 *
 * **2026-09-16에 비웠습니다.** 기업 층을 넣어 뼈대를 다시 잡는 중입니다. 그전에
 * 쌓았던 주장 55건은 커밋 `11e3ec8`에 그대로 있고, 그 값들을 읽어 온 공식 문서
 * 스윕 결과도 함께 남아 있습니다 — 다시 채울 때 처음부터 찾을 필요는 없지만,
 * **로그는 새로 씁니다.** 그날 실제로 페이지를 열지 않고 옛 로그를 복사해 오면
 * 이 서랍의 신선도 장치가 통째로 거짓이 됩니다.
 *
 * 값을 다시 넣을 때 지킬 것:
 * - 공식 페이지를 **그날 실제로 열고**, 본 원문 한 줄을 `playbook-checks/<날짜>.ts`에 남긴다
 * - 못 본 값은 지어내지 말고 `value: null`로 둔다 — 모른다고 적는 것은 언제나 통과한다
 * - 첫 달 `price`·`limit` 상한은 **값이 있는 것만** 여덟이다
 */
export const playbookClaims: Claim[] = [];
